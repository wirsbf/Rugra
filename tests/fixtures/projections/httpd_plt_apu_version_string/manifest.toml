# B2BANK projection bank entry (mechanism B2 solidified fixture).
# Frozen anchor: any byte change to the two .projection files must go
# through re-capture from both producers and re-verification.
function = "apu_version_string"
corpus = "httpd"
entry = "0x2a690"
# PLT thunk: httpd is stripped, so the slot carries no BFD symbol in any
# table; captured through the oracle harness address-only arm
# (STAGE_PROJ_ADDR registration, console "load <addr>" mirror) and on the
# rugra side through the driver stage ledger PLT-thunk arm
# (HBANK-DRIVER-STAGELEDGER-0001, address-form selector). func_name META
# differs by construction (oracle nameFunction default vs driver ledger
# spelling) and is advisory under the bisect contract, like the constprop
# spellings and the curl PLT entries.

[oracle]
side = "oracle"
commit = "e40ed13014025f82488b1f8f7bca566894ac376b"
tag = "Ghidra_12.0.4_build"
producer_blob = "24a4bec28f2786d0b0f4b23e540c67228519b92f"
capture_command = "RUGRA_STAGE_PROJECTION_OUT=tests/fixtures/projections/httpd_plt_apu_version_string/oracle.projection bash tools/run_stage_projection_oracle.sh httpd 2a690 -"
pin_mode = "bank-sha256"
date = "2026-09-25"

[rugra]
side = "rugra"
driver_commit = "534f9802640119dbdc96a64554f086a3fc930356"
driver_profile = "fast-release"
capture_command = "RUGRA_MIRROR=1 RUGRA_STAGE_PROJ=1 RUGRA_STAGE_FUNC=0x2a690 RUGRA_STAGE_PROJ_OUT=tests/fixtures/projections/httpd_plt_apu_version_string/rugra.projection cargo run --profile fast-release --example httpd_decompile"
date = "2026-09-25"

[verification]
tool = "tools/run_stage_bisect.sh (stage_bisect.py --v1 strict offsets)"
status = "MATCH"
stages = 186
ops = 742
date = "2026-09-25"

[integrity]
oracle_sha256 = "d3803b7793346f455b2600c6ccf26a02f569676ae28faa6445014ffa73a88aca"
rugra_sha256 = "2853233afbd15e9450894f5438b55b6ff714f982f9f34785a6757048aedf0701"
binary_sha256 = "805f89cdbdce827f8b6ccd877aa7c344ff1105713b7bf7b0e6f313affa93b1c1"
