# R11-W2 Independent Review — REGISTRY-CONTINUITY-W2-0001

- **Reviewer**: independent review agent (R11, W1-precedent R1 7/7 pattern)
- **Subject**: worktree `/home/wirs/.cache/rugra-wt-registry-w2`, branch `agent/registry-continuity-w2`, HEAD `f4926d17ea54093fbb251066cf40b4aa2e56bead`
- **Scope**: checkpoint roll 9726 -> 9836 (window 92fd927..0f6b284), single commit atop base `0f6b284`
- **Mode**: read-only on worktree/git; only this report file written
- **Date**: 2026-08-25
- **Verdict**: **APPROVE** (7/7 checklist items PASS; non-blocking suggestions in section 8)

Methodology note: the archived probe (`/tmp/rugra-reports/w2_probe.py`, `w2_probe.json`) was used
only as a cross-reference. All decisive verifications below were re-derived independently:
git blob/rev-parse assertions plus the generator's own scan primitives
(`collect_rust_identities` / `assign_rust_locked_ids` / `_scan_rekey_blob` /
`_scan_rust_revision` / `todo_dependency_inventory`) driven by this reviewer's own scripts, with
`stable_id` recomputation from first principles (sha256 over length-prefixed module/owner/signature).

---

## 1. Append-only continuity vs base `0f6b284` — PASS

Object-level diff of `docs/alignment_audit/FUNCTION_ID_CONTINUITY.json` (base blob extracted via
`git show 0f6b284:...`, compared as JSON objects with full-entry serialization identity):

| section | base | HEAD | deleted | mutated |
|---|---|---|---|---|
| `lineages` | 31 | 49 | **0** | **0** |
| `introduced_live` | 91 | 208 | **0** | **0** |
| `tombstones` | 4 | 11 | **0** | **0** |
| `baseline` (whole block) | — | — | identical (object-equal) | — |
| `schema` | 1 | 1 | identical | — |
| `oracle_commit` | `e40ed130…` | `e40ed130…` | identical | — |

- Only appended entries: +18 lineages (each exactly 1 event), +117 introduced, +7 tombstones.
- Old lineage event total 31 -> new 49; delta +18 all inside the new lineages (no old lineage
  gained an event).
- Metadata changes confined to `checkpoint` (92fd927 -> 0f6b284), `history`
  (commit_count 85 -> 154, +69 = window length), `stats`.
- `FUNCTION_ID_MIGRATION.json` blob at HEAD `f4926d1` == at base `0f6b284` ==
  `8ecab1e6160b7d4d28a15aba0cad89c2fe8fc171` (untouched). (At the scheme-2 baseline 8d12962 it is
  a different historical blob, as expected — the continuity doc pins the checkpoint-time blob.)

## 2. Reviewed transitions spot-check — PASS (10/10, covers all 8 commits; requirement was >=6)

Independent per-event assertions (19 per event): parent_blob/child_blob pins equal
`git rev-parse <commit>^:<path>` / `<commit>:<path>`; `from_id` uniquely resolves in the scanned
parent-blob record set with name/owner/module/path/signature equal to the event fields; `to_id`
uniquely resolves in child likewise; `from_id` absent from child and `to_id` absent from parent
(true move); `path`/`owner`/`name` 1:1; and both ids independently recomputed via
`stable_id("RG-F", (module, owner, signature))` match.

| # | commit | identity (owner::name) | sig change | verdict |
|---|---|---|---|---|
| 1 | bd8e38e | impl:ActionDatabase::register_action_named | `Box<dyn Action>` -> `Option<Box<dyn Action>>` | PASS 19/19 |
| 2 | bd8e38e | impl:ActionRestartGroup::child_group | `&'static str` -> `&str` | PASS 19/19 |
| 3 | f87e4d8 | impl:SymbolEntry::update_type | gains `&mut TypeFactory` param | PASS 19/19 |
| 4 | f87e4d8 | impl:HighVariable::finalize_datatype | gains `&mut TypeFactory` param | PASS 19/19 |
| 5 | 3fa2802 | impl:TypeFactory::down_chain_pointer | `&TypePointer` -> `&Arc<Datatype>` | PASS 19/19 |
| 6 | c042d9a | impl:MapState::new | `(u64,u64)` -> `Vec<(u64,u64)>` | PASS 19/19 |
| 7 | 11371d5 | impl:StringManager::get_string_data | gains charsize/opaque params | PASS 19/19 |
| 8 | 7b1da21 | impl:FuncCallSpecs::deindirect | param-shape change | PASS 19/19 |
| 9 | e3e0053 | impl:TypeOpManager::new | gains `Arc<RwLock<TypeFactory>>` | PASS 19/19 |
| 10 | 0f0a060 | trait:TypeOp::get_input_local | `_op`/`_slot` underscores dropped | PASS 19/19 |

Commit distribution of all 18 events (`bd8e38e`x5, `f87e4d8`x4, `3fa2802`x2, `0f0a060`x2,
`c042d9a`x2, `e3e0053`/`11371d5`/`7b1da21` x1) matches the commit message exactly.

## 3. Tombstones — PASS (7/7), StringManagerUnicode parallel-class claim CONFIRMED

Per-tombstone assertions (9 each): blob pins match git; `base_id` uniquely resolves in parent
scan (name/owner/module match, stable_id recomputes); `base_id` absent from child (pure
deletion); no same owner+name successor exists in child.

| commit | identity | note |
|---|---|---|
| 11371d5 | impl:StringManagerUnicode::new | owner block entirely gone in child (0 fns) |
| 11371d5 | impl:StringManagerUnicode::get_string_data | same |
| 11371d5 | impl:StringManagerUnicode::is_string | same |
| bdc7f34 | trait:FuncCallSpecsExt::has_funcdata | trait+impl pair retired |
| bdc7f34 | impl:…FuncCallSpecs as FuncCallSpecsExt::has_funcdata | same |
| f87e4d8 | impl:RulePieceStructure::get_exact_piece | superseded by TypeFactory canonical |
| f87e4d8 | free::exact_piece_arc_sub_type | same |

**StringManagerUnicode decisive evidence** (parent blob `a5c0c6f9…` of 11371d5, scanned via
`_scan_rekey_blob`): parent contains BOTH `impl:StringManager` (9 fns) AND
`impl:StringManagerUnicode` (3 fns: new/get_string_data/is_string) as parallel classes:

- `get_string_data` in parent: `impl:StringManager` -> `RG-F-2282417c93d08f203cd9` AND
  `impl:StringManagerUnicode` -> `RG-F-9e46078d26f5ecc6cd37` (two distinct ids, two owners).
- The W2 lineage transition for `11371d5/get_string_data` has `from_id = RG-F-2282417c…`
  resolving to owner `impl:StringManager` — i.e. it is the signature-change of the surviving
  class, NOT a rename successor of the Unicode variant.
- In child blob `33ee849f…`: `impl:StringManager::get_string_data` exists under new id
  `RG-F-5c1a9467…` (the transition's `to_id`), while all three StringManagerUnicode methods are
  gone and the owner block has zero functions. The tombstone classification is correct.

## 4. Count accounting — PASS, zero unexplained

Independent full id-set diff from fresh scans (`_scan_rust_revision`) of both checkpoints:

- `S_old` (92fd927) = 9726 ids; `S_new` (0f6b284) = 9836 ids.
- gained = 135, lost = 25; 9726 + 135 − 25 = 9836.
- gained 135 = **117 introduced_live** (disjoint) + **18 transition to_ids** (disjoint); overlap 0.
- lost 25 = **18 transition from_ids** + **7 tombstone base_ids**; overlap 0.
- Unexplained gains: 0. Unexplained losses: 0. Stray/misfiled ids in either direction: 0.
- Probe arithmetic holds: 135 discovered − 18 claimed by reviewed transitions = 117 introduced.
- Ledger arithmetic: 9726 + 117 − 7 = 9836.
- Introduced split per scanner ground truth (`is_test` on fresh-scan records):
  **92 production + 25 test**, exactly as the commit message claims.

Cross-integrity: every lineage terminal id is live in the current ledger (0 unresolved);
no tombstoned id remains live (0); every introduced_live id is in the live ledger (0 missing).
All 49 lineage events classify as 46 `reviewed_successor_allowlist` + 3 `same_patch_hunk`
(automatic mut-binding: 594ed4fa, 36633d9d x2 — all inside the W1 window), matching
`stats.reviewed_transitions=46` and "automatic events remain the 3 replayed W1 ones".

## 5. Idempotent reproduction — PASS (all run in the worktree; tree stayed clean)

| gate | result |
|---|---|
| `python3 -m py_compile tools/generate_function_ledger.py tools/rust_fn_scanner.py` | OK |
| `python3 tools/generate_function_ledger.py --check` | green, rc0: `definitions=9494 raw=15811 rust=9836` |
| `python3 tools/generate_function_ledger.py --reconcile-continuity --check` | green, rc0: `baseline=9639 current=9836 lineages=49 introduced=208 tombstones=11` |
| `python3 tools/oracle_registry.py migration-status --strict` | **rc1** (EXIT_FINDINGS); 519 blockers all fixture-registry corpus debt (FIXTURE_FIELD_MISSING=30, REGISTRY_SCHEMA_DEVIATION=239, UNMAPPABLE_FUNCTION_ID=38, …) — no continuity/registry harness errors |
| `git status --porcelain` after all checks | clean (checks are read-only) |

Exit-code semantics verified in `tools/oracle_registry.py` (line 96-98): 0=OK, 1=FINDINGS,
2=HARNESS. rc1 therefore equals "findings only", consistent with the claimed rc2 -> rc1
improvement.

## 6. Checkpoint pin consistency — PASS

- `git rev-parse 0f6b284:src` = `3b259221298a7aaade620d578091484579cf6b14` == continuity
  `checkpoint.src_tree` pin. `checkpoint.commit` = `0f6b2843075a…`,
  `checkpoint.rust_function_records` = 9836.
- First-parent commits from scheme-2 baseline `8d12962` to `0f6b284`: **154** (matches
  `history.commit_count` and commit message).
- Fresh scans: 92fd927 -> 9726 (= previous `current_records`), 0f6b284 -> 9836 (= new
  `current_records` and ledger `rugra_functions` count, 9836 distinct ids).
- Ledger oracle pin: `e40ed13014025f82488b1f8248…` = `Ghidra_12.0.4_build`, 114 `.cc` — locked
  oracle unchanged.

## 7. DAG increment determinism — PASS

`DEPENDENCY_DAG.json` diff base -> HEAD (all other sections byte-identical: `ghidra_files` 227,
`ghidra_include_edges` 396, `rust_modules` 95, `schema`, `oracle_commit`):

- `todo_nodes` 437 -> 445 (**+8**): E2E-BASELINE-POSTINTEGRATION-0001, FLOW-NORETURN-DATA-0001,
  FSPEC-JUSTIFIED-CONTAIN-0001, INFERTYPES-CALLINPUT-D2-0001,
  STRINGMANAGER-CORE-JAVACONTRACT-0001, VARMAP-CROSSPASS-PERSISTENCE-0001,
  VARMAP-GATHEROPEN-GUARD-0001, VARMAP-NAMING-REPIN-0001. All 8 already existed in the base
  `TODO_BOARD.md` (base board inventory = 445 vs stale base DAG = 437): the commit message's
  "DAG picks up 8 board rows" is a catch-up regeneration, not invention. Each id has a board
  row at HEAD.
- `todo_dependency_edges` 160 -> 158 (**−2**): `HERITAGE-GUARD-NORMALIZE-0001 ->
  DATABASE-SCOPE-OWNERSHIP-FIXTURE-0001` and `-> FSPEC-MODEL-UNIFY-0001`. Both edges are absent
  from the base board's inventory (stale-DAG residue) and remain absent at HEAD; removal is the
  regeneration correctly reflecting the board.
- `rust_edges` 539 -> 543 (**+4**, no removals): `database -> type_system::typefactory`,
  `funcdata -> type_system::typefactory`, `variable -> type_system::typefactory`,
  `stringmanage -> crc32`. All four have textual support in HEAD sources (the same
  TypeFactory-plumbing wave visible in transitions 3/4/5/9 and the stringmanage Java-contract
  work).
- Determinism: recomputing `todo_dependency_inventory` over HEAD `TODO_BOARD.md` reproduces the
  HEAD DAG exactly (445/445 nodes; 158/158 edges, set-level identical).

## 8. Non-blocking suggestions

1. `introduced_live` entries carry no `is_test` observation; the 92/25 split in the commit
   message required cross-referencing the fresh scan. Suggest recording `is_test` (and maybe
   `origin_kind`) directly on each entry.
2. The base DAG was 8 nodes/2 edges stale relative to its own board, i.e. earlier board edits
   landed without a DAG regen. This commit cleans it up; a CI gate asserting
   `DAG == todo_dependency_inventory(board)` would prevent stale windows.
3. Reviewed vs automatic transitions are distinguishable only via `evidence[0]`
   (`reviewed_successor_allowlist` vs `same_patch_hunk`); an explicit `kind` field on events
   would make the invariant machine-checkable without evidence-string conventions.
4. `migration-status --strict` rc1 findings (519 blockers) are pre-existing fixture-registry
   corpus debt, not introduced here; they should stay bound to their existing TODO IDs per
   iron-rule 3.

## Verdict

**APPROVE.** Append-only discipline holds (zero deletion/mutation of prior continuity state),
all sampled transitions and all tombstones verify against real git blobs with independently
recomputed stable ids, the 9726 -> 9836 roll closes with zero unexplained ids, both --check
gates are green and read-only, checkpoint pins match git, and the DAG regeneration is a
deterministic catch-up to the board.
