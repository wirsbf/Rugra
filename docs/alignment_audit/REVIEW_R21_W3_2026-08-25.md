# R21-W3 Independent Review — REGISTRY-CONTINUITY-W3-0001 (checkpoint 9836→10058)

* Reviewer: R21 (independent review agent, 7/7 mode, W2-R11-isomorphic checklist)
* Date: 2026-08-25
* Target: worktree `/home/wirs/.cache/rugra-wt-registry-w3`, branch `agent/registry-continuity-w3`, HEAD `188091a8c5c6e58e5aaf6da5131c428f9540e286`
* Base (previous continuity state): `1f3aea4aa79974e4558030b01ff6453a19613393`
* Constraints honored: repo read-only (worktree clean before AND after: `git status --porcelain` = 0 lines; HEAD/branch unchanged), no cargo; scratch confined to `/tmp/rugra-w3-review/`.
* Method: all evidence below was independently recomputed from git objects and the generator's own identity pipeline (`collect_rust_identities` + `stable_id` imported from `tools/generate_function_ledger.py` at HEAD). No implementer-produced artifact (`/tmp/rugra-reports/w3_*.json|py`) was trusted as input.

## Verdict: APPROVE

All 7 checklist items verified. Zero blocking findings. Two non-blocking commit-message prose nits (Section 9).

---

## 1. Append-only integrity of FUNCTION_ID_CONTINUITY.json — PASS

Object-level deep diff `1f3aea4a:…CONTINUITY.json` vs `188091a8:…CONTINUITY.json` (extracted to blobs, keyed by `base_id`/`new_id`):

| section | base | HEAD | deleted | mutated | added | note |
|---|---|---|---|---|---|---|
| lineages | 49 | 94 | **0** | **0** | 45 | keys unique both sides |
| tombstones | 11 | 36 | **0** | **0** | 25 | keys unique both sides |
| introduced_live | 208 | 455 | 0* | 0* | 249 | *see the 2 declared re-keys below |

* The only introduced_live keys absent at HEAD are exactly the 2 declared new_id terminal moves, and both entries survive re-keyed with **every field identical except `new_id`**:
  * `RG-F-ba5f8ef92122364caff2` → `RG-F-9abcbe19cd51f069dcce` (typeop::`base_local_type`, transition @ `8e5944ca`)
  * `RG-F-d8e0a50ea15b7f15c23f` → `RG-F-d03ae0650e666c500a8e` (space::`decode_attributes`, transition @ `d9171ef1`)
  * Each also gains a lineage entry (`origin_kind=introduced_live`, alias=old id). Key arithmetic: 206 unchanged + 2 re-keyed + 247 brand-new = 455; 206+2 = 208 base entries all present.
* Other top-level sections: `baseline`, `id_scheme`, `oracle_commit`, `schema`, `semantics` **UNCHANGED**. `checkpoint`/`history`/`stats` changed exactly as declared (commit 0f6b284→1f3aea4a, records 9836→10058, commit_count 154→254, lineages 49→94, events 49→95, introduced 208→455, tombstones 11→36, reviewed 46→92, alias_tokens 49→95).
* **MIGRATION blob untouched**: `git rev-parse 1f3aea4a:docs/alignment_audit/FUNCTION_ID_MIGRATION.json` = `git rev-parse 188091a8:…` = `8ecab1e6160b7d4d28a15aba0cad89c2fe8fc171` (matches message "8ecab1e6").

## 2. 46 reviewed transitions — PASS (all 46 verified, not just ≥8)

Enumerated new events (base had 49; HEAD 95; new = 46, every one tagged `reviewed_successor_allowlist`; 0 new automatic mut-binding — the 3 W1 ones remain: 95 − 92 reviewed = 3 ✓).

Per-event machine verification (all 46):
1. **Blob pins real**: `git rev-parse <commit>^:<path>` == `parent_blob` AND `<commit>:<path>` == `child_blob` — 46/46.
2. **Owner-aware unique resolution & 1:1** (using the generator's `collect_rust_identities` + `stable_id` on both blobs): `from_id` exactly once in parent scan, absent in child scan; `to_id` exactly once in child scan, absent in parent scan — **46/46 PASS**. (An initial naive signature-substring heuristic flagged 18 events; all were false positives from substring/generic-instance shadowing — the owner-aware rescan clears every one.)
3. **ID derivation**: `stable_id("RG-F",(module,owner,signature))` recomputed from stored fields matches `from_id`/`to_id` for all 46.
4. **Global 1:1**: across all 95 events, zero duplicate `from_id`, zero duplicate `to_id`; the only id that is both a `to_id` and a later `from_id` is the legal two-hop middle `RG-F-98be7769343f7b648186`.
5. Same path/name/owner invariant (reviewed-transition rule): 46/46.
6. Commit distribution: 73b5ef2×15, 8e5944c×8, 01712a2×3, c5a421d×3, cddcefd8×3, 26d675e×2, f95369a×2, 55ef994×2, 8 singles — sums 46. (Message prose sums to 44 — see Section 9.)

### Two-hop chain (focus item): Varnode::get_local_type — base `85317e7a` CONFIRMED CORRECT

Chain: `RG-F-85317e7aebd33b457844` —(@`0e9aadfb`, gains type_factory)→ `RG-F-98be7769343f7b648186` —(@`c5a421dd`, gains userops)→ `RG-F-e1751080679995008f76`; aliases=[85317e7a, 98be7769]; origin_kind=baseline.

Independent verification:
* All three IDs recompute exactly from (varnode, impl:Varnode, signature) via the scheme-2 hash.
* **Liveness**: fresh scan of `src/varnode.rs` at previous checkpoint `0f6b284` contains `85317e7a` (with the exact pre-hop-1 signature `fn get_local_type(&self, _block_up: &mut bool) -> Option<Arc<Datatype>>`) and contains neither 98be7769 nor e1751080. At new checkpoint `1f3aea4a`: only `e1751080` is live. So `85317e7a` is the correct lineage base (a pre-window checkpoint resident), and the probe's corrected replay is right.
* Chain continuity: hop1 `child_blob` `1c05d7e2…` == hop2 `parent_blob` (exact); hop1 `parent_blob` == varnode.rs@`0e9aadfb^`; hop2 `child_blob` == varnode.rs@`c5a421dd`.
* `85317e7a` appears in none of the base continuity sections (no prior transition/tombstone/introduction) — no double-count.

## 3. 25 tombstones — PASS (all 25 verified)

New tombstones enumerated (11 old + 25 new = 36 ✓). Category breakdown matches the message exactly: EmulateFunction@73b5ef2 ×7 (default/new/execute_op/emulate_path/set_load_collect/get_varnode_value/set_varnode_value), FuncCallSpecsExt trait+impl is_inline/is_no_return @b8da22a ×4, CollapseStructure goto-cascade @cddcefd8 ×4 (run_goto_cascade/run_tracedag/select_and_mark_goto/apply_loop_exit_marks), TypeOpCopy locals @8e5944c ×2, get_split_datatype_bit + test @1644180 ×2, exact-piece supersessions @26d675e ×3, Rule reset @4495eb6 ×2, condexe pullback_new_varnode_out @e956e96 ×1. Sum = 25.

Per-tombstone machine verification (25/25 PASS):
* `parent_blob` == blob(path)@`deleted_at_commit^`, `child_blob` == blob(path)@`deleted_at_commit` (git-pinned).
* Owner-aware scan: `base_id` exactly once in parent, absent in child.
* **Pure deletion**: `base_id` is not any event's `from_id` or `to_id`; no same-commit successor (zero functions in child under same owner+name slot).
* `base_id` recomputes from stored (module, owner, signature).
* All 25 base_ids are in the checkpoint-loss set L (checkpoint residents); the 11 old tombstones correctly are not (they predate the previous checkpoint).

## 4. Ephemeral — PASS

`test_split_copy_size_mismatch_fills_unknown_pieces`:
* **Zero occurrences** in all pinned ledgers: HEAD continuity JSON, HEAD FUNCTION_LEDGER.json, base continuity JSON, base FUNCTION_LEDGER.json, MIGRATION blob (grep count = 0 in each).
* Git lifecycle (definitive via `rev-parse` + `cat-file -p`, blob-level): absent at `0f6b284` (blob e42479af, 0 hits); fn definition present at `26d675e7` (blob cae7b73c, `#[test] fn …` at line 6569); still present at `b15bd104^` (same blob cae7b73c); **deleted exactly at `b15bd104`** (blob 73226c08, 0 hits; diff hunk `@@ -6566,12 …` removes the fn); absent at `1f3aea4a` (blob 73226c08). Both endpoints inside window `0f6b284..1f3aea4a` (ancestor checks pass).
* Not to be confused with its W2-era sibling `test_split_copy_size_mismatch_is_no_change`, which is one of the 25 properly-reviewed tombstones @26d675e — independently verified in Section 3.

## 5. Count accounting — PASS (end-to-end independent rescan)

Fresh scheme-2 scans of full `src/` trees via the generator's own pipeline (file list from `git ls-tree`, IDs from `stable_id`; collision-free):

| commit | records | pinned claim |
|---|---|---|
| `8d12962` (scheme-2 baseline) | **9639** | baseline.rust_function_records=9639 ✓ |
| `0f6b284` (previous checkpoint) | **9836** | ✓ |
| `1f3aea4a` (new checkpoint) | **10058** | ✓ |

Set algebra: G = S1−S0 = **290 gained**, L = S0−S1 = **68 lost**; 9836 − 68 + 290 = **10058** exact.

Cross-tab against continuity artifacts — **zero unexplained on both sides**:
* Loss 68 = 43 new-event `from_id`s ∩ L + 25 new tombstone `base_id`s ∩ L + 0 other. The 3 remaining new-event from_ids never at a checkpoint are exactly the declared "3 re-transitioned" in-window introductions: `RG-F-44fcf9a5`/`RG-F-efcf794f` (op_input/op_output_type_local, introduced @0e9aadf, re-transitioned @c5a421d) and `RG-F-98be7769` (get_local_type mid-hop).
* Gain 290 = 249 new introduced_live keys + 45 live transition terminals − 4 overlap = 290. Overlap ids: 9abcbe19/d03ae065 (the 2 declared re-keys) + 4e04105e/94a2b6bd (op_input/op_output_type_local finals, brand-new entries whose ids are also transition terminals). 46 to_ids − 1 superseded mid-window (98be7769) = 45 ✓.
* Ledger arithmetic: 9639 + 455 − 36 = 10058 ✓ (baseline 9639 independently rescan-verified; baseline ledger blob pin `840bac84…` at 8d12962 ✓).
* Lineage structure: 45 new lineages = 41 baseline-origin (all in L ✓) + 4 introduced_live-origin (2 W2-introduced re-keys + 2 in-window re-transitions); 44 single-event + 1 two-event = 46 events ✓.

Note: the message's additive phrasing "290 = 247 + 45" reads as 292 under the most natural reading and only closes with the unstated "−2 double-counted terminals"; see Section 9. The underlying data and the totals are exact.

## 6. Idempotent reproduction (in worktree) — PASS

| command | result | declared |
|---|---|---|
| `python3 tools/generate_function_ledger.py --check` | rc0, `verified definitions=9494 raw=15811 rust=10058` | green ✓ |
| `python3 tools/generate_function_ledger.py --reconcile-continuity --check` | rc0, `reconciled continuity baseline=9639 current=10058 lineages=94 introduced=455 tombstones=36` | green ✓ (matches stats exactly) |
| `python3 tools/oracle_registry.py migration-status --strict` | **rc1** (610 blockers, all fixture-registry corpus-debt classes; registry MATCH=53/MISMATCH=27/PARTIAL=32/UNTESTED=14) | rc1 ✓ (rc2→rc1 pre-state not re-materialized — would require the parent tree; terminal state verified) |
| `python3 -m py_compile tools/{generate_function_ledger,oracle_registry,rust_fn_scanner}.py` | rc0 (`__pycache__` artifact removed; tree clean) | clean ✓ |
| DAG == board | `--check` regenerates all four artifacts incl. DEPENDENCY_DAG.json byte-for-byte (write_or_check compare); `todo_nodes`=466/466 unique, `todo_dependency_edges`=153/153, parsed from `docs/TODO_BOARD.md` by `dependency_dag()` | 466/466, 153/153 ✓ |

`--check` green covers more than demanded: FUNCTION_LEDGER.json, FUNCTION_MAP.generated.md, PROTOCOL_TABLE.json, DEPENDENCY_DAG.json all reproduce identically from the worktree state (ghidra symlink intact).

## 7. Checkpoint pins — PASS

* `git rev-parse 1f3aea4a:src` = `5254425e8c9c53cd852377d0583a217bcfac46c8` == JSON checkpoint.src_tree ✓ (matches commit message).
* `git rev-parse 1f3aea4a^{tree}` = `4df23b7a0914d4a96a5f4160d8829a292955ff22` == checkpoint.commit_tree ✓; baseline tree `5d46f835…` ✓.
* `git rev-list --count 8d12962..1f3aea4a` = **254** == history.commit_count ✓; `--first-parent 0f6b284..1f3aea4a` = **100** == message "100 first-parent commits" ✓.
* Fresh scan of 1f3aea4a = **10058** (Section 5) ✓; both `rust_projection_sha256` pins independently recomputed: 1f3aea4a → `33b98e51…a40a0` ✓, 0f6b284 → `aee24b89…b74f8` ✓.

## 8. Red-flag self-check (mechanism D analog)

* No fabricated history: every historical claim in the message was traced to real git objects (blobs pinned at exact commits, ancestor checks).
* No silent baseline rewrite: baseline section byte-identical, MIGRATION blob identical, 0 deletions/mutations in all three append lists.
* No ordinal/line-number leakage into IDs: recomputation used only (module, owner, normalized signature).
* Fail-closed behavior intact: `assign_rust_locked_ids` collision detection would have fired on my 10058-record rescans if IDs collided (none did).

## 9. Non-blocking suggestions

1. **Commit-message distribution tally**: the parenthetical "(73b5ef2 x15, 8e5944c x8, cddcefd8/c5a421d x3, 01712a2/26d675e/55ef994 x2 each, … x1)" sums to 44, not 46; actual is 01712a2 x3 and f95369a x2. Headline "46 reviewed transitions" and the data are correct; only the prose tally is off by two events.
2. **Gain decomposition prose**: "Gained 290 = 247 introduced-live terminals + 45 reviewed-transition terminals" omits the −2/−4 overlap subtraction (op_input/op_output_type_local finals are both introduced-live terminals and transition terminals; exact identity: 249 new keys + 45 live terminals − 4 overlap = 290). Totals and data exact; phrasing double-counts 2 under the natural reading. Recommend the next checkpoint message state the overlap explicitly.

Neither affects ledger data, counts, pins, or the append-only property.

## 10. Verdict

**APPROVE** — REGISTRY-CONTINUITY-W3-0001 checkpoint roll 9836→10058 is append-only, fully pinned, idempotently reproducible, and exactly accounted; every sampled surface (and in fact every one of the 46 transitions and 25 tombstones, not merely the required ≥8/samples) passed independent owner-aware verification against git objects and the scheme-2 ID hash.
