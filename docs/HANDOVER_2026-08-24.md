# 交接文档 — 2026-08-24(本地电脑续作用)

> 基线:**master = `16d3a71`**(已推 GitHub,含全部 61 个 `wt/*` 分支)。
> 本文档自包含:不看会话记录即可续作。配套:TODO_BOARD.md(W-2026-08-23-GAP 节)、ALIGNMENT_ROADMAP.md。

---

## 1. 验证基线(到机器后先跑这三条确认环境)

```bash
cargo test --lib          # 预期 1559 passed / 2 failed(test_infer_params_and_return_type、
                          #  test_type_propagation——已知 ACTIONTYPEINFER-VTYPE-0001 链,勿快修)
cargo run --release --example curl_decompile > result/curl_cur.c
python3 tools/compare_ghidra.py result/curl_cur.c tests/golden/ghidra_curl_1204.c --summary-only
                          # 预期 defects=0 / numbering=0;75 反编译/1 超时/0 panic
```

环境要点:
- `ghidra/` 子仓必须钉在 `e40ed130`(gate health 会查);oracle fixture 编译依赖
  `/tmp/rugra-ghidra-bfd-2.38`(binutils-dev 解包,机器重启会丢;重建:
  直连 `curl -O https://archive.ubuntu.com/.../binutils-dev_2.38-4ubuntu2.12_amd64.deb` 后 `dpkg-deb -x`,
  **apt 代理是坏的,直连 https 可用**)。
- worktree 内跑 runner 需 `ghidra -> /home/ls/Rugra/ghidra` symlink(gitignored,惯例)。

## 2. 本轮已完成(全部在 master,无需重做)

- **GetStr 与 Ghidra 12.0.4 函数体逐字节一致**(五域修复链:opFlipCondition 腐蚀、
  PreferComplement 忠实化、IfNoExit/ProperIf 极性、ActionExtraPopSetup 真实现、
  参数符号播种+survivor-clear、typed constants/pushConstant、CALL 输出类型种子)。
- **函数体级全语料对比:52/68 一致(33 个非平凡)**;defects=0/numbering=0;0 panic。
- 管线树 78 节点与 oracle 逐条一致;三池 150 Rule 注册序对拍;**119 个双侧 fixture 全绿**。
- 6 份跨模块审计(jumptable/coreaction/condexe/ruleaction/fspec/flow)落地,90+ TODO 登记。
- 三步走流水(找差异→分析→解决)已跑通一个周期;分析产物全部在看板 W-2026-08-23-GAP 节。

## 3. 未落地的根因图(**最有价值的资产**,规格可直接开工)

### RC-A 流边界/尾调用族【三组函数独立证实,优先级最高】
- 症状:glob_word(4.5×膨胀)、hugehelp(1/24 产出)、myprogress(吞整个 GetStr 函数体)、
  next_url/progressbarinit 尾调用丢失、SetHTTPrequest if 体丢。
- 根因(双侧行号):
  1. `flow.rs:3063` follow_flow 下界=函数入口;oracle `funcdata.cc:161-163` 是 `(space,0)~(space,~0)` 全空间。
  2. **shared-return/FlowOverride 整块缺失**:oracle Java 侧 SharedReturnAnalyzer 设 CALL_RETURN,
     C++ 消费链 `flow.cc:415-476` + `funcdata_op.cc:968-1010`(BRANCH→CALL+插 RETURN);
     Rugra `flow.rs:319-346` flowoverride_present 写死 false。
  3. PLT thunk 半边:fail_thunk(`flow.cc:727-768` truncateIndirectJump→CALLIND)+fspec deindirect 落名
     (`jumptable.cc:2304-2319` thunk 判定)。hugehelp 第 6 个 puts 靠它。
  4. 函数尾边界(防线性扫进下一函数;oracle 靠流边界非 st_size)+ noreturn PLT 终止。
- 注意:baddr=0 单独上会吸 PLT 链(direct-runner 实验 next_url 膨胀到 11k)——**必须与 override 同上**。

### RC-B RETURN 值槽位【一因修全语料】
- `x86_lift.rs:619-621` ret 只写 slot0;oracle RETURN slot0=指示、slot1=值(`printc.cc:754-763` 读 in(1))。
- 全语料 42 个 `return;` 都因此不带值。修 = lifter 对齐 SLEIGH `return [RAX]` 展开。

### RC-C 字符串/DAT 渲染 + opPtrsub 字段解析
- `printc.rs:275-288/9091` 常量裸 hex、push_ptr_char_constant 是占位;
  oracle 链 `printc.cc:1744→1698→1534`(stringManager)+`funcdata_varnode.cc:1653` mapGlobals 建 DAT_。
- opPtrsub:`printc.rs:1338` 仅精确 offset 匹配;oracle `printc.cc:929-1010` 有 union 解析/
  findTruncation(`type.cc:1580` 严格上界)/field_0x 回退。**半成品已验证**:
  `wt/myprog` 分支 `f295dcd`(find_partial_field 半开区间,bar->point→bar->width 修复,A/B 零回归)。

### RC-D DWARF 聚合参数 + 局部变量
- `debugproto.rs:359` 对 Struct/size>8 bail→match_url 的 `URLGlob glob` 参数被拒(oracle 走 cspec MEMORY 栈槽)。
- DWARF **局部变量**导入全缺(myprogress 的 format/line/outline 是真局部名,readelf <22fd><230e><231f>)。

### RC-E 规则非幂等/类型传播循环
- `RuleStructOffset0` zero_const 硬编码 offset 0(`ruleaction.rs:~16500`;oracle `ruleaction.cc:6710` `-newoff&calc_mask`);
- `STOP_TYPE_PROPAGATION` 无人消费 + `analysis/type_infer.rs:15/137/306` 自创传播器把 PTRSUB 输出传回
  struct* → 每轮再触发 → 8 层链 + "Type propagation not settling"(`coreaction.rs:4347`)。
- bisect 手法:RUGRA_RULE_STATS=1(action.rs:563)+逐规则禁用。

### Split 族门禁【已实验验证,收益最大】
- `subflow.rs:5167/5197` SplitLoad/Store 直接拆;oracle 三重门:`subflow.cc:2970-2983` getValueDatatype
  (:2910-2938 getExactPiece 仅 STRUCT/ARRAY/PARTIALSTRUCT)+ :2244-2274 numDepend>1。
- 模拟实测:my_fwrite skeleton 45→16 行、声明 30→2、全量零回归。

### merge_addr_tied 双门
- `merge.rs:900-960` 按 (addr,size) 并**所有空间**;oracle `merge.cc:609-648` 仅
  IPTR_PROCESSOR/SPACEBASE(617-619)+ 仅 addrtied 标志(631,只给 symbol-backed)。

### 其他已登记待修(看板有完整清单)
- fspec 双模型栈(生产走 315 行 SysV stub,忠实 fillin_map 是死代码)——`FSPEC-MODEL-UNIFY-0001`
- 类型传播链(TYPEOP dispatch→VARNODE local type→InferTypes)
- truncatedFlow partial clone(switch 恢复链根因)、inlineSubFunction stub

## 4. 中断 Agent 的 salvage 清单(分支已在 GitHub)

| 分支 | 可捞内容 | 处置 |
|---|---|---|
| `wt/globvars` | `1a04cbe` ADDUNSIGNED copy_symbol_arc 一行适配 | 直接复核→cherry-pick |
| `wt/myprog` | `f295dcd` find_partial_field 半开区间 | 复核→进 RC-C 派发 |
| `wt/addrtied` | 未提交 merge.rs 半成品 | 看完成度,大概率重做更快 |
| `wt/rulebisect` | 未提交 action.rs(疑 bisect 临时代码) | 忽略 |
| 其余 wt/sharedret、retstr、dwarfagg、splitgate、recovered、rangepull | **死前零提交,直接按 §3 规格重开** | — |

## 5. 操作套路与坑(会话里踩过的)

- **pin 重钉三件套**(fixture 因 src 变更失效时):runner 的 shell 变量(commit/tree/blob)+
  metadata comparand(文件 sha256)+ metadata overlays 表;**git blob id 与文件 sha256 是两种形态**
  (shell 的 `rev-parse` 校验用 blob id,`sha256` 校验用文件哈希,别混)。
- **commit message 红词**:`align/port/对齐/faithful`(连 "ported"/"RULE-PORT" ID 都算)触发机制 A,
  需 `## Alignment Evidence` 块;docs 提交措辞避开即可。
- **docs/api 同步钩子**:改 src/*.rs 必须同 commit 更新对应 docs/api/*.md(哪怕加一行说明)。
- **worktree GIT_DIR 坑已修**(tools/check_gate_health.py 清环境变量),worktree 提交不再需 --no-verify。
- **result/curl_cur.c 回流约定**:每次 E2E 后 `cp /tmp/<run>.log result/curl_cur.c`(gitignored,曾发生 30+ 小时滞留事故)。
- **度量口径疑点(未查完)**:我的全量 compare 数字(~9556)与 agents 报告(2406-2621)不一致;
  疑与日志噪音([SYM]/[STEP] stderr 混入 stdout,parse_functions 会吞进函数体)或调用方式有关。
  **以函数体级对比(python 正则提取双侧函数体逐字节比)为准**——该口径无歧义。
- Agent 编排:worktree per-writer + nice -n 10 + 白名单模块复核制;复核曾拦下"fixture 全绿但
  nzm 语义前提错误"(r1 REJECT→返工→r2 APPROVE 全流程记录在 TODO_BOARD)。

## 6. 下轮开工建议(顺序)

1. salvage 两笔(§4 前两行,半小时)。
2. **RC-A 全家**(流边界+override+fail_thunk+尾界+noreturn):一个 writer 串做,预期一次点亮
   glob_word/hugehelp/myprogress/next_url/progressbarinit/SetHTTPrequest 六函数族。
3. RC-B RETURN 槽位(独立、一小时级、全语料 42 处受益)。
4. Split 门禁 + merge_addr_tied(规格已验证,照抄)。
5. RC-C/D/E 按看板规格。
6. 每集成一批跑全量三门禁(test/compare/audit)+ 刷新 result/。

## 7. 文档索引

| 文档 | 用途 |
|---|---|
| `docs/TODO_BOARD.md` | W-2026-08-23-GAP 节=根因图全量登记(owner/write-set/验收) |
| `docs/alignment_docs/PIPELINE_STAGES_1204.md` | 管线阶段树+稳定切点+断点续跑机制 |
| `docs/alignment_audit/*_GAPS_*.md` | 6 份审计(逐函数对照+行号) |
| `AGENTS.md` | 铁律与门禁(新人必读) |
| `docs/VERIFICATION_GUIDE.md` | oracle 环境与 fixture 跑法 |
