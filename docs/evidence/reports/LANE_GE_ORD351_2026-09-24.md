# Lane GE 终报 — gp 首分歧 ord351 mergerequired 判类与修复 (wt/ord351)

- worktree: /dev/shm/rugra-worktrees/ord351, branch wt/ord351
- 基础: master **b25bce7a**(亲父); commit **1b2cc000**(src/merge.rs + docs/api/merge.md + docs/TODO_BOARD.md 同 commit)
- oracle: Ghidra 12.0.4 e40ed13014025f82488b1f8f7bca566894ac376b
- CARGO_TARGET_DIR=/dev/shm/rugra-targets/sb-ord351
- 证据: /dev/shm/rugra-tests/sb-ord351/(gp_bisect.txt 前后对照、gp.rugra.drill、
  four_projections.txt、curl/httpd_e2e.c(+.parent.c cmp 基)、run_gates.sh、snapshots/)

## A. 判类结论(判类先行,任务②)

**真分歧(非 FI 判例同款),已修复。** FI 判例(MERGE-COPYNOISE-SPILLRESTORE-0001,
2026-09-24 关闭为非缺陷)的口径是:**最终 C 输出**里"oracle 没有而 Rugra 有"的形态
必须先对 direct-runner golden 复核(headless golden 含 Java 分析器差异)。ord351 的
差异不在最终输出通道,而在**锁定 oracle 阶段投影通道本身**:oracle 投影 SNAP
350→351→371 全程显示该 phi slot 1 保留原栈读直接合并,C++ 管线侧没有该 trim——
不存在 headless-only 干扰,故按真分歧处理,走 Ghidra 源码对照修复。
(旁证:修复后 curl/httpd E2E 与亲父 cmp 逐字节相同,说明该差异在最终文本本就不可见,
只在 B2 投影可观测维度——正是投影门禁要消灭的形态。)

## B. 差异定位(任务① 复现+drill)

- 复现: b25bce7a 树 gp 投影 bisect == FW2 移交形态(ord351, op-line 86,
  phi@3f80:189a slot1 oracle `n:stack:…fa48:8` vs rugra `u:1000064d:8`)。
- oracle 侧 SNAP 轨迹: SNAP 350 `in=n:register:30:8,n:stack:…fa48:8` →
  SNAP 351 `in=u:10000645:8,n:stack:…fa48:8`(仅 slot0 被 trim,与 Rugra 同,
  u:10000645 双侧一致)→ 终态 SNAP 371 slot1 恒为原栈读。
- Rugra drill(mergerequired 块内,drill 行 40113-40174): 同一 application 内
  `u0x10000645(3f74:1c90)=RSI(i)`(slot0 trim,正确)+ 
  `u0x1000064d(4043:1c91)=s0xfffffffffffffa48(4030:1892)`(slot1 多余 trim)。

## C. 根因(铁律 2 先读 Ghidra)

`Merge::mergeOp`(merge.cc:719)Phase 1:cc:731 `op->getIn(i)->getHigh()` 与
cc:737 `op->getIn(j)->getHigh()` 均为**逐迭代活读**;`trimOpInput(op,0)` 内
`data.opSetInput(op,copyop->getOut(),slot)` 就地替换 slot 0,后续 j 层读到的是
trim COPY 输出的**全新 HighVariable**(allocateCopyTrim cc:411-438 经 newUnique,
无 input/addrtied 旗标)→ 对 addr-tied 栈读的 required 测试通过,不 trim。
Rugra `merge_op`(src/merge.rs:3179)把 inrefs 克隆成快照 vec 复用,j 层拿旧快照
里 slot 0 的 RSI 参数 high(is_input=true, !addr_tied)去测 slot1 栈读 high
(is_addr_tied=true)→ `merge_test_required` 镜像臂(cc:125-127)误判冲突 → 多余 trim。

**修复**:Phase 1 两处改 `op.0.read().unwrap().inrefs.get(i/j)` 活读,快照 vec
删除(唯一消费方即 Phase 1)。Phase 2/3 本就活读,不动。

## D. 门禁(任务③,亲父 b25bce7a 对照)

| 门禁 | 修复后 | 亲父基线 | 判定 |
|---|---|---|---|
| gp 投影 bisect | **MATCH**(371/371 阶段 snapshot 零差异,ops 913395→**913373==oracle**) | ord351 首分歧 | **首分歧消除,全 MATCH** ✓ |
| next_url/match_url/parseconfig 投影 | MATCH ×3 | MATCH ×3 | 保持 ✓ |
| myprogress 投影 | ord399 setcasts 5v6 | ord399 | 不动(FV2 域,按区间分治) ✓ |
| curl E2E | 1995 / 0 / 0 | 1995 | == 且 **cmp 逐字节相同** ✓ |
| httpd E2E | 2072 / 0 / 0 | 2072 | == 且 **cmp 逐字节相同** ✓ |
| gcc 审计 | 104 OK / 20 FAIL | (同文本,构造性相同) | ✓ |
| 单测 --lib | 1687 passed / 18 failed | 1687 / 18 同 flaky 集(funcdata alignment/ssa + heritage_creation,轮间 18↔21 摆动已复现) | 零新增 ✓ |
| InferTypes 警告 | curl/httpd `not settling` 0 | 0 | 保持 ✓ |
| --func getparameter | 532 | 532(FW2) | 文本域残差属 print/varmap 既有域,非本族 ✓ |

corpus-neutral 证明:parent blob 换入/换出双构建,curl+httpd 输出 cmp 逐字节相同
(stderr 差异仅 [PREPASS] 线程交错日志,非管线输出)。

## E. 机制 C 声明(merge.rs 白名单)

**CR 请求**(commit 1b2cc000 内已声明)。独立 reviewer 请自行打开 Ghidra 行复核
(勿采信本报告声明),重点:
1. merge.cc:730-741 Phase 1 双层循环的活读语义(cc:731/cc:737 经 op 解引用,
   trimOpInput 就地替换 slot 后 j 层必须见新 high);
2. allocateCopyTrim(merge.cc:411-438)COPY 输出新 high 无旗标 ⇒
   mergeTestRequired(cc:102-173)对 (fresh-high, addr-tied-high) 通过;
3. Rugra 侧对应物:src/merge.rs merge_op 活读 + wire_unique_high 新 high
   (src/merge.rs:2674)不承袭旗标;
4. gp 投影 MATCH 证据链(sb-ord351/gp_bisect.fixed.txt + 四投影)。

## F. 交接与回收

- gp 首分歧账本更新:FW2 行(643)移交标记已注记收敛;新增 GE 终局行
  (`MERGE-LIVEREAD-ORD351-0001`)。myprogress ord399 仍归 FV2(setcasts)。
- worktree wt/ord351 + /dev/shm/rugra-targets/sb-ord351 保留待 root 集成 + CR;
  集成后按回收纪律处理。result/curl_cur.c 已回流(=curl_e2e.c)。
- gp 文本域 ~532 残差下一步候选:print/varmap 域(与 merge 无关,投影已零差异,
  说明 gp 的 IR 终态与 oracle 一致,残差在打印/命名层)。
