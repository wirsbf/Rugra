# LANE FS3 — CR24 登记残差 R1+R2 快修（wt/r1r2）

- 日期: 2026-09-24
- 基线（亲父）: 并集树 **781046c2**（FM rulearith 26ffb3c2 × master 9458a61b）
- Commit: **0ef0e6c6**（`rulearith: AddTreeState uint4 accumulator timing + SPACEBASE unsigned extra divide (CR24 R1/R2)`）
- Write-set: `src/ruleaction.rs` + `docs/api/ruleaction.md` + `docs/TODO_BOARD.md`（登记行 `RULEARITH-ADDTREE-NUMWIDTH-0001`）
- Oracle: Ghidra 12.0.4 e40ed130（worktree ghidra symlink 验证 =锁定 commit）

## R1 累积器截断时点（CR24 R1）

- 字段 `biggest_non_mult_coeff` u64→u32（hh:54 `uint4`）。
- cc:6145 vncoeff 站点：`sval.wrapping_neg() as u32` 后 u32 比较（先截断后比较）。
- cc:6158/6210 treeCoeff 站点：`tree_coeff > field as u64` 全宽比较后 `tree_coeff as u32` 截断存储。
- `has_matching_sub_type` 两调用点 arrayHint `as u64`。
- 结构性保护核实：cc:6132-6137 `val>=size` 门使 |sval|≥2³² 仅 size==0（变长基类型）可达。

## R2 SPACEBASE 臂无符号除（CR24 R2）

- calc_subtype SPACEBASE 臂：`extra = ((extra as u64) / (wordsize as u64)) as i64`
  （cc:6294 `AddrSpace::byteToAddress` uintb 按位模式无符号除，space.hh:523-525）。
- STRUCT 臂 cc:6311 `byteToAddressInt`（有符号）保持不动。
- 分叉条件：extra<0 且 ws>1（当前全空间 ws=1 ⇒ latent）。

## 行为锁单测（3 个，全过）

1. `test_add_tree_vncoeff_truncates_before_compare`：病理 0x100000003→(uint4)3，后跟 5 终值=5（u64 全宽比较会永驻 0x100000003）。
2. `test_add_tree_treecoeff_fullwidth_compare_truncated_store`：treeCoeff 0x100000005 全宽胜出后存 5；6>5 存 6；0x100000003>6 存 3（覆盖 cc:6158 与 cc:6210 两站点）。
3. `test_add_tree_spacebase_extra_unsigned_byte_to_address`：ScopeLocal 播 int[8]@0x2000，offset 0xFF8（ws=2→offsetbytes 0x1FF0），forward walk 探 0x2010 命中，extra=-16 → 0xFFFFFFFFFFFFFFF0/2=0x7FFFFFFFFFFFFFF8（有符号会得 -8）；断言 offset=0x8000000000001000、correct=0x8000000000000008、valid、is_subtype。

## 三门禁 + 三投影方向（基线=亲父 781046c2 pristine worktree 亲测，fast-release 双侧）

| 门禁 | 本车道 | 亲父 | 判定 |
|---|---|---|---|
| curl E2E | 2145/0/0 | 2145/0/0 | cmp 字节恒等 ✓ |
| httpd E2E | 2057/0/0 | 2057/0/0 | cmp 字节恒等 ✓ |
| gcc 审计 | 82 OK / 25 FAIL | 82 / 25 | ==亲父 ✓ |
| getparameter | 532，d=0 n=0 | 532 | 恒等 ✓ |
| myprogress | 64，d=0 n=0 | 64 | 恒等 ✓ |
| next_url / match_url / parseconfig / main | 92/49/83/487，d=0 n=0 | 同 | 逐名恒等 ✓ |
| cargo test --lib | 1687 过 / 18 失败 | 1684 过 / 18 失败(+1 flaky 翻转) | 失败集逐名 ⊆ 亲父；+3 过=新单测 ✓ |

两修均为 latent 病理输入行为差异，语料内不可达 ⇒ 输出恒等即预期（非缺陷掩蔽，分叉条件见上）。

## 机制 C

本修由 CR24 独立复核报告 R1/R2 节派生（发现先于修复）。ruleaction.rs 属主管线 Rule 白名单；
root 集成时可对 cc:6145/6158/6210/6294 四锚点 + space.hh:523/541 方向对照复核。

## 回收

- baseline worktree `r1r2-baseline` 与 `/dev/shm/rugra-targets/sb-r1r2-base` 已清除。
- 本车道 worktree `wt/r1r2`（commit 0ef0e6c6）与 `/dev/shm/rugra-targets/sb-r1r2`（root 复验增量缓存）保留待集成。
- 证据件：`/dev/shm/rugra-tests/r1r2/`（curl/httpd 双侧输出+stderr、commit_msg.txt、双侧失败集清单）。
- result/curl_cur.c 已回流（worktree 内，gitignored）。
