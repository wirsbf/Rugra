# Todo Board (任务看板)

本文档的顶部“活跃 wave”是当前任务唯一事实源；后文保留历史阶段记录，不能作为当前优先级。

## 活跃 wave：`W-2026-09-01-FLEET5`（2026-09-01 起；goal=所有函数文本级对齐；并发上限 5=用户指令）

> ### 派发：STAGE-BISECT-E2E（2026-09-21 root，deepwork 最高并发指令）
> **目标**：双侧 stage 投影生产端 + curl top 函数差异归因（3718 skeleton → 根因清单）。
> **车道**：Lane A `wt/sb-oracle`（oracle 投影 harness，owner=sb-oracle-agent）；
> Lane C `wt/sb-rust`（Rust emitter，owner=sb-rust-agent）；
> Lane D inventory（主仓只读，owner=sb-inventory-agent）；
> Gate 1-4 = oracle 规范/保真/首分歧/根因表评审。
> **write-set**：A=`tools/stage_projection_1204*.cc|sh, tests/oracle/*(新增)`，
> C=`examples/*(新增), src/ 仅限 RUGRA-GLUE 只读访问器, docs/api/*`，
> D=仅 /dev/shm。临时测试代码一律 /dev/shm/rugra-tests/<branch>/。
> **验收**：next_url 双侧投影 + `tools/stage_bisect.py` 首分歧输出 + top 函数归因表。
> 状态与规范：`.slim/deepwork/stage-bisect-e2e.md`。
> **进展（2026-09-21 root）**：Gate 1 通过,规范 v1.1 锁定(M1/M2/枚举算法已钉死)。
> Lane D 完成:top-15 清单+入口地址=/dev/shm/rugra-tests/sb-inventory/top_diff_inventory.md
> (main 1248/getparameter.constprop.0 869 领跑;Rugra 单函数选择可行)。
> Lane H 完成:fresh 基线 curl **3711/0/0**(sha 023d6ab5…,已回流 result/curl_cur.c)、
> httpd **3576/0/0**——**较 W4 +1232 = 主仓回归**(main 653→1180/ap_fini_vhost_config 258→625/
> ap_pregsub 222→345/ap_update_vhost_from_headers 221→319),报告
> /dev/shm/rugra-tests/sb-baseline/BASELINE_REPORT.md。
> **派生 Lane I**(wt/sb-httpd):70f4449..0acde30 commit 二分定位回归,产物
> /dev/shm/rugra-tests/sb-httpd/HTTPD_REGRESSION_REPORT.md;**Lane M**:httpd 29/2010
> 语域调查,产物 /dev/shm/rugra-tests/sb-corpus/HTTPD_CORPUS.md。
> **Lane I 已收案(root 2026-09-21)**:bisect 判定 **first bad=79bb0f6**("align: CALLSPEC-DRIVER-0001
> flow-time callspec anchoring on the inject path",src/{flow,funcdata}.rs 等 4 文件)——httpd
> 2344→3576,父提交 0f8fc1b=2344 good,判定无歧义;curl 同期未受损。回归 profile:main +527/
> ap_fini_vhost_config +367/ap_pregsub +123 等 20+ 函数。**Lane N 已派发**(wt/sb-httpd 续用):
> M1 根因(对照 Ghidra flow.cc setupCallSpecs/fspec.cc FuncCallSpecs 四类语义)→M2 最小修复
> (门禁:httpd≤2350、curl≤3715、defects=numbering=0)→M3 定稿;禁整段回退 79bb0f6。
> **Lane M 结论**:29 函数=ELF 地址序前 30 跳过 _start(MAX_FUNCS=30),非硬编码;建议 httpd 作
> 小规模 follow-on pilot,暂缓全量 2010 扩容。
> **消费端链已闭环(root 2026-09-22,master `5b52c92`)**: Lane E(2c9c8b4)→Gate 2-E
> REJECT(B-1 嵌套/B-2 假 MATCH 通道/R-1/R-2/R-3)→Lane R 返修(006db61)+规范转正
> (74ef199)→Gate 2-E attempt2 **APPROVE**(三未决问题裁维持现状)→F-1(@RESTART 帧内
> 合法化)root 亲修(bb9920b)→集成。selftest 28/28。规范 repo 契约=
> docs/alignment_docs/STAGE_BISECT_SPEC_1204.md。
> **Lane S 完成**: sb-batch/{targets.json(153 目标),batch_driver.py(断点续跑/超时/pending),
> README}+CLI 契约(run_stage_projection_oracle.sh <corpus> <addr> <name> / RUGRA_STAGE_FUNC=<addr>)。
> **D9/D10 裁决落地(v1.2.2 已入库 master)**: D9=perform 级事件正典,oracle harness 收敛
> (删 break_action ≈15 行,oppool1 事件 28→18),Rugra/消费端零改动;D10=golden 路径正典
> (followFlow 无界-range),oracle 不动,**Rugra 需实现流跟随镜像**——登记
> `RUGRA-FLOW-MIRROR-0001`(P0,C-alignment 系列承接,write-set=examples 驱动层+FlowInfo
> 无界-range 语义;完成前 load_mode 发 `single_function_flow`,完成后切
> `single_function_bfd`)。Phase 2 next_url 对拍在 D9 修复+D10 镜像完成后开启。
> **opStackLoad+merge 链集成(2026-09-22,Cross-Review attempt1 APPROVE)**: 条件1
> (FUNCDATA-SPACEID-WIDTH-0001 登记)已在本看板满足,按 reviewer 细节补全: 宽度统一 8
> 修复路径(funcdata_op.cc:488 SEGMENTOP 同族位点 x86-64 不可达/523/547),现有读者全为
> 值通道无行为影响;条件2(main +24 函数级归因)绑定 R1c/R2a 既有族,root 集成阶段补齐。
> **AR 集成**: stack delay 1 修复合并,curl **3677/0/0**(−8 全改善),httpd 2343 恒等,
> activeparam [9,9,0] 探针复刻逐点一致。
> **⚠ oracle 捕获确定性广播(2026-09-22,Lane AT)**: oracle per-application DEBUG 流依赖早期
> 堆分配序列——argv 路径形态(相对↔绝对)决定性选择 records=1019 vs 1014 两变体(历史
> "发散窗口"=此,非随机);ASLR 使大函数几乎逐跑翻转。**正典捕获配方**=相对 argv+`env -i`
> +`setarch -R`(repo-root cwd)。**所有 oracle 捕获 lane(golden regen/fixture)必须采用同
> 配方**,否则可能钉住不同确定性变体。drill runner 已参数化(<corpus> <addr> <name>)+
> 三目标钉定: curl/main 4763 记录 4.96MB/gp 3375 记录/httpd main 6583 记录,全部五跑
> 字节一致。
> **RETURN-ARTIFICIAL-RAX-0001 改域(2026-09-22,Lane AV 裁决)**: 定性=(c) 终态语义等价
> (双侧 `2534:2cc RETURN in=c:1:4,n:register:0:8` 逐字节同)——根子=驱动把目标函数自身
> DWARF 锁定原型装入(examples/curl_decompile.rs:2531 debug_db.apply)→ActionPrototypeTypes
> 走 cc:4637-4649 锁定臂提前挂 RAX;oracle 走 initActiveOutput+heritage 挂 RAX+RDX+
> returnrecovery 裁。owner 改 fspec/驱动环境域,挂 MIRROR-ENVS-CANONICAL-0001(修复=
> 镜像态抑制目标 DWARF 装载,wt/sb-rust 空闲后落);对 +144 贡献小但钉死消费端首分歧
> (观测阻断项)。附带: CALLIND 二输入常量折叠差(c:17680 vs u:10000174)疑 BANK-COPY-159
> 同族,已记 RAX_RETURN.md §3.2。
> **ACTION-TRAVERSAL-144-0001 定案(2026-09-22,Lane AY)**: 裁决=(b) 真实未收敛,根因=
> `ActionReturnSplit::gatherReturnGotos` 的 goto 前驱检测替代实现(coreaction.rs:14153,
> "入边源块以 BRANCH/CBRANCH 结尾"代理 Ghidra gotoPrints/copy-map)R2 尾误开火→余震
> (COPY 链清理+blockstructure×5+condnegate→notdistribute→boolnegate 阻尼振荡)→R3 真报
> changes→repeatapply 正确多跑 R4。**修复=忠实移植 gatherReturnGotos goto 检测**
> (wt/sb-returnsplit,owner=returnsplit-agent)。R3 top: boolnegate 7apps/earlyremoval 11/
> propagatecopy 9(同 op 二次改写实证 0x5077:876 四连改)。
> **opStackLoad Cross-Review 条件2了结(2026-09-22,Lane AW)**: main"+24"=函数体原始行数
> 差(skeleton 正典口径仅 +17),63% 栈槽重映射(UStack_388 单对象裂解 590 行)+11% 重编号
> +38 行真内容+**形态变化 0**;~85% 归 R0 栈参/符号恢复族(修复预期下游,非新缺陷),
> 次要归 R2a(P2 观察项: 栈对象裂解后结构类型未回附),R1c 驳回。**无需新登记 P0**。
> 详见 sb-opstackload/MAIN24_ATTRIBUTION.md。
> **P0 新登记 `DETERM-COPYTRIM-0001`(2026-09-22,Lane AX 实证)**: run-to-run 输出不确定
> (16 跑 9:7 双版本,getparameter_constprop_0 单 locus 相邻 COPY 对互换)——根因头号候选
> =merge.rs:3584 process_copy_trims 的 ptr-keyed HashMap into_iter()(Ghidra merge.cc:
> 1415-1436 为 copyTrims 列表首见序,已逐行对照)——**既是不确定性 bug 也是 Ghidra 遍历
> 序偏离,动摇差分门禁可信度根基**。修复=镜像列表首见序(参照 merge.rs:2197-2209 排序
> 模式),验收=连跑≥10 次 sha256 单值+oracle 序一致;merge.rs=机制 C 白名单,修后独立
> 复核。卫生项: coreaction.rs:12936 过期"faithful no-op"注释更正/examples 4081 payload
> 序排序。报告=sb-integration/DETERMINISM.md(含已排除清单防重复排查)。
> **第二不确定性源 `DETERM-DOMINANTCOPY-0001`(P0,2026-09-22,Lane AZ)**: 三函数 Rust
> drill 全部非确定(2-3 跑 cmp 不等),同点 universal:dominantcopy 工作集漂移(gp 穿透
> 终态 C 语句序)——coreaction.rs:12950 域容器随机序,与 DETERM-COPYTRIM-0001 一并修
> (同 lane BD)。**top-3 配对基线**(wt/sb-rust@c5a8992=集成前快照): main 遍型
> oracle [3,3,1] vs Rust [8,6,2];gp switchnorm oracle 改 2 op vs Rust 3×空(集成后
> 重测);三首分歧捕获于 sb-drill/TOP3_PAIRING.md。
> **golden 配方审计闭环(2026-09-22,Lane BA)**: golden(C 文本)敏感面严格小于 DEBUG 流
> (printC 只消费终 fixpoint,容器值序确定;curl main 8/8 字节一致同期 DEBUG 翻转;
> 正典配方重生成与入库 golden 逐字节 MATCH)——**无需重钉**。登记 P3
> `GOLDEN-RECIPE-HARDEN-0001`(regen 加 setarch -R+provenance 调用形态指纹+httpd
> determinism_rerun)。**AT 广播纠偏**: argv 1019/1014 二变体今日不可复现(全形态=1019),
> 今日活跃翻转维度=ASLR(curl main 4762↔4763)。报告=sb-drill/GOLDEN_RECIPE_AUDIT.md。
> **B2-MG-RESID 登记(2026-09-22,MultiGoto Cross-Review 绑定条件)**:
> `B2-MG-RESID-1`(P2): blockmultigoto fixture family C 合成形双侧分歧撤下——fixture
> 覆盖缺口,owner=root 分诊,验收=双侧同形构造补齐;`B2-MG-RESID-2`(P2): loop_exit_
> conflict 形同上;`B2-MG-RESID-3`(P2): glob_set +5 骨架残差(91→96,defects=0,机制性
> 邻域重排),owner=root 分诊,验收=函数级 token 归因。
> **B2-MG-RESID-1/2 ✅CLOSED + ③归因闭环(2026-09-22,Lane BT wt/sb-mgresid)**:
> fixture 三件套+pin runner 正式入库 `tests/oracle/blockmultigoto_1204.{cc,rs,metadata.json}`
> + `tools/run_blockmultigoto_oracle.sh`(oracle commit 门禁+dirty 文件门禁+comparand
> sha256 门禁),四 family 全 **MATCH**(runner 实跑):RESID-1 补 family C
> `copy_switch_consumption` 同形构造(buildCopy 图+production newBlockMultiGoto 剥离+
> newBlockSwitch 同套录制调用 grabCaseBasic→identifyInternal→addBlock,观察 append 臂
> cc:3548-3553 的 gt1 追加 case+mg 作 cs[0] 被吸收零残留);RESID-2 补 family A2
> `loop_exit_conflict` 同形构造(WhileDo[cond, body=List[mg,对比 goto]] 双侧
> production 构造,scopeBreak 提升对比照——BlockGoto→f_break_goto(cc:2872-2873)而
> multigoto gotoedge 永不重分类(cc:2918-2922));撤下记录里的双侧分歧属 legacy
> cascade/合成图 rule 交互域,非 newBlockMultiGoto 缺陷,同形构造已覆盖等价语义。
> **B2-MG-RESID-3 归因(真差,不属 multigoto write-set,登记不修)**: master 3648 基线
> (124/124,76 decompiled,0 panic/timeout)重测 glob_set 函数级=**106**(.91 pre→96 lane→
> 106 现值,后续 merge 同域继续增长),gp=866(539 lane→866),双侧 defects=numbering=0;
> token 级归因全部落在两个已登记上游域:①`JUMPTABLE-TABLEAPI-0001`(P0-A)——case 标签
> 为边序占位(`case '\0'/'\x01'…`+`switch(*(( *)unique0x…))` 畸形 cast+`code_r0x…:`/
> `goto code_r0x…` goto-case 发射=printc.cc:3334-3337 臂在设计上待 label 管线解锁);
> ②typed-global/globalsym 覆盖缺口——glob_expand 原始偏移算术(`*(undefined4*)(iVar1+
> 0x50)`/int2/ZEXT-SEXT churn)vs oracle `URLGlob*`/`->pattern[].content.Set` 字段化
> (2026-09-22 globalsym 修复只覆盖 main/gp DAT,glob_set 不在射程)。multigoto 激活
> 增量(91→96 的 +5)即 goto-case 发射形态本身,非 BN 类零翻转良性重排;残差闭环
> 条件=JUMPTABLE-TABLEAPI-0001 + globalsym 扩展覆盖,不在 multigoto 域修。
> **BN 开项②物化(同 lane)**: `goto_prints_nextflowafter_1204` 第七形态
> `switch_multigoto_gotoedge` 落地(cc:3548-3553 append 臂:mg 作 cs[0]、gotoedge 目标
> 作 gt1 追加 case、末常规 t_goto case 的 nextFlowAfter 落入追加 case、multigoto 分派
> 臂 null),双侧 runner 重钉后 **MATCH**,既有六形态输出逐字节不变(caseblocks 走查
> 归一化对纯组件 switch 恒等);metadata cases/manifest sha256/comparand 重钉。
> **基线快照(2026-09-22 root 集成后)**: master=BB returnsplit+AD multigoto+wt/sb-rust
> 发射器链(env-off 字节一致)合并后,curl **3356/0/0**、httpd **2326/0/0**,已回流。
> **P0 新登记 `BLOCKSTRUCT-SWITCHOUT-NOCLEAR-0001`(2026-09-22,Lane BH 根因)**:
> blockaction.rs:6285 `set_flags(swf & !SWITCH_OUT)`=no-op(应为 clear_flags,对齐
> block.cc:1917 clearFlag)→48-case BlockSwitch 形成后带 f_switch_out 被 ruleBlockSwitch
> 自噬 415 次吞死→无 Switch 节点存活→printc 零 case。同型 no-op 顺修:
> blockaction.rs:3489(CASE_BODY)/heritage.rs:2363(TYPELOCK)。label 管道缺失
> (finalizePrinting/get_label_by_index 零调用)并入 JUMPTABLE-TABLEAPI-0001。验收=
> getparameter switch≥1 且 case≥48。报告=sb-switch/CASE_EMISSION.md。
> **DETERM 双 ID CLOSED(2026-09-22,root 依 Cross-Review 条件补登)**:
> `DETERM-COPYTRIM-0001` ✅CLOSED(e2c627c,merge.cc:1415-1436 首见序镜像,12/12 单值
> ≥AX 验收线,输出与修复前 d464 variant 逐字节同=纯序钉死,Cross-Review attempt1 APPROVE);
> `DETERM-DOMINANTCOPY-0001` ✅CLOSED(同源统一根因=processCopyTrims 序,coreaction 侧
> 仅注释更正);**OPEN 残项**: dominantcopy 槽位带非空 trims 的 mirror 态双侧对照
> (root 集成阶段执行)。**流程改进令(root)**: 连续三例"commit message 有、板上无"
> ——在途 lane 完成时 root 必须当场核对板上登记后才算回收完成。
> **集成+热修双收(2026-09-22)**: ①globalsym 合并(root 亲测 curl **3304/0/0**,main DAT
> 105→0/gp 60→0 与 oracle 逐字节同形);②BK spaceid 合并(C 输出字节恒等,funclink 分歧
> **清除**,Phase 2 首分歧推进至 ordinal 12 heritage=SUBPIECE 宽度 4vs8=subflow.rs:6040,
> 已登记 SUBFLOW-SUBPIECE-WIDTH-0001,BM 车道待派);③BI SWITCH_OUT 热修完成(自噬
> 416→3,commit 61bc251,**待机制 C 复核**);④BJ 审计: goto_prints 六分臂缺失
> (潜伏雷:while 尾 break 被吞成死循环),登记 `GOTO-PRINTS-NEXTFLOWAFTER-ARMS-0001`
> (P1,BN 车道待派);⑤BL printc infloop 递归发射(gp switch 发射唯一残阻塞)待派。
> **Lane BN 修复落地(2026-09-22,wt/sb-gotoprints,`GOTO-PRINTS-NEXTFLOWAFTER-ARMS-0001` ✅CLOSED)**:
> goto_prints 树遍历从纯兄弟规则升级为 nextFlowAfter **12 override 全分臂**单一事实源
> (block.rs `next_flow_after_successors`/`graph_sibling_successors`,coreaction ReturnSplit
> gather 同源消费);修 BB 表 Switch 两偏差(槽0 误当调度根 → 删特判;case 序改组件序=发射序,
> 真实 label 排序绑定 JUMPTABLE-TABLEAPI-0001);删 7 个无调用点 typed next_flow_after* 死代码。
> 双侧 fixture `goto_prints_nextflowafter_1204` 六形态(while 尾 break-goto/infloop 回边/switch
> fallthru/goto 套 goto/if-else 尾/dowhile 尾)**MATCH**(runner 实跑);5 函数树 dump prints
> 重测 0 mismatch;curl 全量 3304/0/0 与父提交**字节一致**、httpd 2326/0/0 与 sb-httpd 车道
> 基线**字节一致**(潜伏雷排除,当前语料零翻转);cargo test 失败集=父提交既有 flaky(heritage/
> funcdata SSA,与本改动无关,两轮对照确认)。**blockaction.rs 未触及**(机制 C 白名单字面未命中,
> 但主管线行为变化,root 集成时建议独立复核)。
> **Lane BV 修复落地(2026-09-22,wt/sb-orderblocks,`BLOCKSTRUCT-ORDERBLOCKS-0001` = CR-BO
> 条件③ cc:2191 orderBlocks 缺口 ✅落地)**: ActionFinalStructure 补 `graph.orderBlocks()`
> (block.hh:430-431)于 finalizePrinting 之前;`FlowBlock::compareFinalOrder`(block.cc:709-730)
> 三键(entry idx==0 恒首 / lastOp()==RETURN 压尾 / 其余按 index;双 RETURN 块 tie)完整移植为
> block.rs `compare_final_order`+`BlockGraph::order_blocks`(稳定 sort_by 解 tie=libstdc++
> 插入排序 phase ≤16 元素同形);**顺带补齐缺失的 per-type lastOp 委托**:BlockGoto(cc:562)/
> BlockMultiGoto(cc:590)→wrapped(此前 trait 默认 None,compareFinalOrder 依赖分派不符)。
> 语义影响=顶层输出序+scopeBreak/gotoPrints 的 next-sibling 取序。三门禁:curl 124/124 与
> httpd 29/29 全量 A/B(d847acc 基线)**字节恒等**(sha256 相同——当前语料所有函数顶层列表均
> 单元素(glob_set 15→1/getparameter 30→1/main 105→1,树 dump 证实),block.hh:431 守卫双侧
> 同步跳过);getparameter(978)/glob_set(97) --func defects=numbering=0 前后不变;B2 双侧
> fixture `blockstruct_orderblocks_1204` 5/5 case **MATCH**(runner
> tools/run_blockstruct_orderblocks_oracle.sh,真 newBlockGoto/newBlockMultiGoto 委托覆盖);
> cargo test --lib 单线程 18 failed=已知 FUNCDATA-TESTS-FLAKY-0001 集(与 BO 车道基线同集),
> block::/blockaction:: 全绿。残差:>16 元素列表 tie 置换(libstdc++ quicksort phase vs 稳定
> sort,语料内不存在该形态,已登记 fixture metadata residual)。**blockaction.rs 属机制 C
> 白名单:Cross-Review PENDING(root 派)**。
> **⚠ P0 新登记 `HTTPD-EXAMPLE-BRACE-0001`(2026-09-22,Lane BV 抓获)**: master dff36e78
> (10:04 "tool: integrate httpd pspec tracked-context ingestion")提交的
> examples/httpd_decompile.rs **无法编译**(fn worker_memory_image_bytes line 71 起未闭合
> 大括号,ea0f7a74 09:53 为最后可编译版;可运行修版困在 stash@{0} emptyelse WIP——共享 stash
> 禁触碰,需持有者释放或 root 从 ea0f7a74 重放)。BV 车道 httpd 门禁双侧统一用 ea0f7a74 驱动
> A/B(同一驱动隔离 src 变更,结论不受影响)。任何车道跑 httpd E2E 前须先修此 P0。
> **⚠ 配额事件(2026-09-22 ~04:3x)**: zai-coding-plan 5 小时用量墙,BL/BM/BN/
> SWITCH_OUT-Cross-Review 四派发全部未启动,05:32:32 重置后需重发。
> **Lane BB 修复落地(2026-09-22,wt/sb-returnsplit)**: BRANCH/CBRANCH 代理已替换为
> gatherReturnGotos 原语义(copy-map 结构树 t_goto/gotoPrints/t_if-ifgoto 检测,blockaction.cc:2205;
> gotoPrints 走逐父类型 nextFlowAfter 虚分发表 mid-pipeline live 评估)。降级 drill 验证
> (RUGRA_RULE_STATS 单函数): next_url oppool1 751→720(**21→17 passes,余震轮消失**),
> main 1794→1786。curl 全量 E2E: defects=0/numbering=0,skeleton 3693→3678
> (next_url 142→130 恢复 golden 循环形态,my_get_token 79→77,_init 11→10,77/80 函数
> 字节不变,零回归);httpd 40 函数抽查: defects=0/numbering=0,skeleton 2780→2761。
> 遗留(未决): 单函数驱动口径 main skeleton 526→769(语料口径 main 不变;疑驱动播种
> 差异+master 代理偶偿,需 root 集成时 oracle drill 裁决);关联项 BANK-COPY-159-0001 /
> RULE-PTRARITH-ADDTREE-0001 / RETURN-ARTIFICIAL-RAX-0001 待重测。
> **镜像后差集图谱(2026-09-22,Lane AU,DELTA_V2.md)**: +144 事件精确分解=mainloop 遍
> 8→12(+92)/stackstall 内迭 12→18(+30)/fullloop 轮 3→4(+9,轮型 [4,3,1] vs [4,4,3,1]);
> 银行膨胀终快照 COPY **171 vs 12**(+159 主导,与 Lane V 拷贝族分诊互证);规则级 top:
> ptrarith +19/boolnegate +16/termorder +12;首分歧推进 prototypetypes(21 行前缀);
> 3240/3843 drill 行差=ffunc_ 拼写格式化间隙(SB-DRILL-FSPEC-NAME)。**新登记**:
> `ACTION-TRAVERSAL-144-0001`(P0,mainloop 第 4 轮驱动者)与
> `BANK-COPY-159-0001`(P0,终快照 COPY 银行 159 差,归 w-selfcopy 域);
> punch list v2=11 项(依赖 DAG 见 DELTA_V2.md §6)。
> RUGRA_BARE_LOAD=1 落地(env 门控,默认 off 字节一致),load_mode 切 single_function_bfd,
> **next_url PLT 对逐字节收敛(raw ops 717=717),消费端首分歧推进到 ordinal 5 seq 6
> universal:prototypetypes**(人工 RETURN 挂 RAX 第二输入 vs oracle INDIRECT [create] 群)。
> 剩余: 事件 335vs479(Action 计数层)+ΣSNAP ops 未收窄(下游银行膨胀);镜像 E2E +113
> (PLT 名字层缺失,raw-BFD vs golden 含 thunk analyzer——root 翻转默认前需裁决)。
> **新登记**: `RETURN-ARTIFICIAL-RAX-0001`(P1,prototypetypes 首分歧,ActionActiveReturn/
> returnrecovery 域);`MIRROR-ENVS-CANONICAL-0001`(P2,RUGRA_ORACLE_FIXTURE_DATA 门正典化
> 为 mirror 组成部分+名字层裁决项)。**Lane AU 已派发**: 镜像后差集重测绘(DELTA_V2+事件差
> 归因+punch list v2)。
> AN 独立验证全过): P1-P8 全落地,74 名表机械校验,消费端剩余卡点=D10 层(PLT 对@stage2
> op0)=预期正确状态。P3 措辞偏差 root 接受(callspec_link 进 producer 注记,身份键双态
> 不变)。**`FUNCDATA-SPACEID-WIDTH-0001`(P1,**FIXED 2026-09-22**,Lane BK
> `wt/sb-spaceid`,owner=fixer,commit a62dbfa)**: funcdata.rs op_stack_load/
> op_stack_store 双处 `new_constant(1, contain.space_id())` → `new_varnode_space(contain)`
> (funcdata_varnode.cc:190-198 sizeof(spc)=8;同族 SEGMENTOP 位点 cc:488 同宽 8,
> x86-64 不可达,Rugra 未实现该分支——已在 create_stack_ref 注释标注宽度语义)。
> 验证(同机同树 fast-release): P2 复跑首分歧 funclink ordinal7 op-idx4
> (2534:2e8 LOAD in=s:ram 对齐,投影 c:3:1 残留 0)→后移至 **ordinal 12
> universal:fullloop:mainloop:heritage op-line 22**(SUBPIECE 截断常量宽 4vs8,
> 见新登记 SUBFLOW-SUBPIECE-WIDTH-0001);curl E2E defects=0/numbering=0,
> skeleton 3356 不变,C 文本与 master 基线字节级一致;httpd 29 函数
> defects=0/numbering=0,与 04:23 基线字节级一致(audit_syntax 82/25 同基线)。
> **`SUBFLOW-SUBPIECE-WIDTH-0001`(P1,**FIXED 2026-09-22**,Lane BM
> `wt/sb-subpiece`,owner=fixer,wip 6dfadab/8b4d982,终稿见分支头)**:
> 登记归因(subflow.rs:6040)经实测证伪——修后投影 producer=`rugra-tree-6dfadab…`
> 而 c:0:8 仍在。真产地=heritage `normalize_read_size`(heritage.rs:1867 硬编码
> `new_constant(8,overlap)` vs cc:393 `addr.getAddrSize()`)+`normalize_write_size`
> (heritage.rs:1919/1987/2037 传 `space.addr_size()`,而 space.rs `addr_size()`
> Register=8 错误)。oracle 证据: next_url 全语料 2668/2668 SUBPIECE 尾常量宽 4
> (`in(0)=n:register:8:8` 由 `newVarnode(size,addr)` 建,addr∈register 空间)→
> x86-64 oracle register space addrSize=4。修=space.rs Register 8→4(17 个
> addr_size 消费方同收敛,与 oracle getAddrSize 语义一致;ADDRESS-0001 仍为
> per-spec 正式载体)+heritage.rs:1867 改 `vn.space.addr_size()`(heritage 按
> 空间划分,vn/range 同空间)+subflow.rs:6040/5395 宽度 4(subflow.cc:2513
> `newConstant(4,off)` 独立有效,splitCopy cc:2735/splitStore cc:2871 共用;
> 顺修 subflow.rs:6026 幽灵引用 `RootPointer::subpieceValue`→cc:2497
> buildInSubpieces)。验证: P2 复跑首分歧 ordinal12 heritage→**ordinal 19
> returnrecovery(V1_RESULT_COUNT,result 4 vs 1)**;普查 2931→0 宽 8 全收敛,
> c:4:4/c:1:4/c:2:4 与 oracle 精确一致(486/18/9),c:0:4 余量 612=SUBPIECE
> 数量差(另域)。**连带关闭 `HERITAGE-SUBPIECE-CONST-WIDTH-0001`(312 处族,
> 同根因 register addrSize)**。E2E 三门禁见 docs/api/{space,heritage,subflow}.md
> 同日条目。
> **CR-orderBlocks 绑定条件履行(2026-09-22,root)**: 新登记
> **CV 收官并集成(2026-09-22,root 亲测)**: CR-CV APPROVE(复核独立验证强于声明: oppool2
> 全阶段字节恒等/双构建复现/my_fwrite 无回归独立证实=None 状态不承重)。**Phase 2
> 首分歧 186→332(1-331 全匹配,+146 序号)**;亲测 curl **3049/0/0**(−56)/httpd
> **2348/0/0**(−44),已回流。**条件登记**: ①测试数字口径更正(单线程稳定失败=既有
> 18 名单家族,"9 failed"=并行抖动口径);②`RESOLVEINFLOW-DRIVER-0001`(P2): 驱动器
> 缺 needsResolution→resolveInFlow(coreaction.cc:5075-5078,联合体解析传播缺口,
> 未认领);③孪生双实现(coreaction 臂+typeop trait)漂移风险记档,收敛列入 wave 议题。


> **CU 收官并集成(2026-09-22,root 亲测 9ba67aeb)**: ordinal 185 根因=镜像态残留两层
> 全分析数据符号(oracle 裸环境只 addFunction 零数据 SymbolEntry→queryContainer NULL→
> constantptr 不 fire)——bare-load 门下双抑制(examples-only,默认零变化)。**Phase 2
> 首分歧 185→186**(oppool2 4vs3,1-185 全匹配!)。亲测 curl 3105/0/0 稳定/httpd
> 2392/0/0 恒等。**新登记 `HTTPD-WORKER-CURLSYM-0001`(P2,CU 顺带发现)**:
> build_worker_architecture 的 DWARF+ELF OBJECT seeding 硬编码读 `examples/curl`——
> httpd worker 默认路径会被喂 curl 的全局符号(镜像态已被门关闭,默认 E2E 行为待
> 裁量)。readonly range 全段镜像留 P3。**CV 已派发**(fix-11,wt/sb-ord186):
> oppool2 5 条 Rule 差 1 fire 追杀——**若清掉则 next_url 全函数 phase 对齐**。


> (P1,owner=CT lane 在途): VariableGroup/GroupSymbol printc 吸收链,验收=main 3095→
> golden 的 `glob.pattern[i].xxx` 字段形态吸收;②登记 `INIMPLIED-14-173-0001`(P2):
> in-implied R=14 vs O=173(SIGSIG 公平口径,修复前后均 14=预存口径差非本修引入),
> 归因假设=SIGSIG local-scope 注册差影响 MarkImplied 输入冻结,验收=VariableGroup
> lane 闭合时成员级 join 重测;③census 表述更正: 30d6 波 40 成员 **39 个** oimpl=0/
> oexpl=1(成员 4282=未拆 8B 原拷贝同位同判);④merge.rs:52 doc 注释并行损伤顺手修。

> **CR-CR2 条件履行+CR2 集成(2026-09-22,root)**: ①登记 `SUBFLOW-CSU-MASK-0001`
> (P2,新): __libc_csu_init 间接实参 `& 0xffffffff` 掩码消失背离 golden(+1,指向
> consume/nzmask 上游=ActionDeadCode→minimalmark 域与本修 consume 守卫的交互,oracle
> 拒而 Rugra 受);②main oppool1@65 **oracle=821 vs rugra=778(残 −43,绑上游
> ordinal-5/uniq 族)**,@END 28 预存 +8——main 等值不宣称(结构性限制);③Differential
> 归因更正: curl −14=main−4/glob_word−4/glob_set−6/csu_init+1(**getparameter 未变**,
> 其投影字节恒等自证);"逐名一致"降级为"flaky 家族内抖动零新增"。

> (P2,新): __libc_csu_init 间接实参 `& 0xffffffff` 掩码消失背离 golden(+1,指向
> consume/nzmask 上游=ActionDeadCode→minimalmark 域与本修 consume 守卫的交互,oracle
> 拒而 Rugra 受);②main oppool1@65 **oracle=821 vs rugra=778(残 −43,绑上游
> ordinal-5/uniq 族)**,@END 28 预存 +8——main 等值不宣称(结构性限制);③Differential
> 归因更正: curl −14=main−4/glob_word−4/glob_set−6/csu_init+1(**getparameter 未变**,
> 其投影字节恒等自证);"逐名一致"降级为"flaky 家族内抖动零新增"。

> **CO 探查闭环(2026-09-22,CANDGEN 裁决)**: 候选生成/顺序域**零缺陷**(never-seen
> 0/0,双侧 mergeOpcode 单次,门语义逐行一致)——MERGE-COPYNOISE-CANDGEN-0001 关闭。
> 33 对定因: **+48 implied 群体**(上游 MarkImplied/op 创建波域,指纹=uniq 30d6 创建波
> 20/47,size 8/24/4 struct 分片族;merge 前 implied 已冻结=双侧同确)→**CQ 车道已派**
> (以 30d6 波定位创建者+in-implied 6 对锚点;验收=implied-out 对 R≤O+指纹消失);
> −12 const 记账面并入 junk 族回归指标;−3 req flag 漂移=P3 观察项。

> **CN 集成(2026-09-22,root)+双重复核纠偏**: minimalmask 整字节阶梯修复合并(双语料
> 字节恒等=语料中性,潜伏语义缺口真实闭合)。**双 CR 纠偏登记**(第二独立复核更严):
> ①commit message 的"httpd under-consumed"因果叙述=作者自归因,以 lane report §2
> 为准(diff=0,+2 与 minimalmask 无关);②**CR-CH 条件③(httpd +2)在 master 上 moot**
> ——root 证据:CH 合并时 httpd 与合并前字节恒等,+2 是 CH 分支本地(基于 pre-CE
> 基线)现象,从未进入 master;真凶排查不需要重开(master 无此症状);③DEADCODE-
> SELFLOOP-0001 的 minimalmask 残项措辞更新: callee 已忠实,B2 fixture(CP 在途)
> 前保持 NO_ORACLE/UNTESTED;④coveringmask 行号勘误 address.cc:760→800。


> 合并,双语料字节恒等(语料中性,CR-CN 评审亲跑门禁确认);**JUMPTABLE-HYGIENE-0001
> 交叉引用**: foldInNormalization 的 minimalmask 轴已由 7049549b 关闭(独立预存
> 缺口,非 AC 移植错——调用点本就忠实,被共享 helper 暗中破坏),coreaction 侧
> markConsumedParameters/gatherConsumedReturn 两消费者一并继承;B2 bilateral
> fixture(CP 车道)升级中。

> **CL 探查闭环(2026-09-22,OKOK-0001 裁决)**: **假阴性对=0**(双侧独立:oracle 266 次
> 真 testUntiedCallIntersection 求值 cross=1 计 0/Rust 112 mixed-tied 对零翻转)——
> CE 精确相交移植承重无反向风险,CR-CE 条件①闭环。残差 84 归因: +44 same-high
> 打印侧(归 PRINTC-CONDBLOCK-JUNKOPS-0001)/**+33 可并未尝试(候选生成/顺序域=
> 新杠杆,OKOK-0001 基线口径改锚此域,CO 车道已派)**/+5 cover 过严/+2 req0 边缘。
> UNTEDINTERSECT 维持 P3(升级条件=任一语料 oracle 侧 cross=1>0)。勘误: merge.rs
> "分支不可达"辩护被证伪(结构可达,oracle main 实测 146 次)→改记 reachable,
> empirically non-decisive, deferred。
> **CR-BZ 绑定条件履行(2026-09-22,root)**: 登记扩充——`HERITAGE-PROMOTE-SYMBOLTAIL-
> 0001` 子项(约 10 处 oracle 提升位点 Rust 缺符号分支: normalize 族 cc:391/441/462/
> 473/485 仅折叠/guard 读端 invn cc:1595 裸建/callsplit cc:1225/1332/1353/splitVarnode
> cc:1742/1750/restrain cc:2008/joinrec 族 cc:2095/2100/2241/rebuild cc:288)——同类
> 潜在缺口(部分覆盖/join 的全局读保持无类型→RulePtrArith 症状族);owner=CF lane
> (待派),验收=符号尾携带+config 域零回退重放。
> **HERITAGE-PROMOTE-SYMBOLTAIL-0001 子项交付(2026-09-22,Lane CF `wt/sb-promosite`,
> owner=fixer,commit 见分支头)**: 逐位点裁决完毕(裁决表全量见
> `docs/api/heritage.md` 2026-09-22 条目)。**补 17 个位点**(normalize 族 7:
> cc:391/440/441/461/462/473(475)/485;guardCalls 新发现 cc:1502;callsplit 3:
> cc:1225/1332/1353;guardReturnsOverlapping 新发现 cc:1634;guardReturns
> return-copy 新发现 cc:1682;splitByRefinement cc:1742/1750;guardInput 拼接目标
> cc:2008;rebuild cc:288;join 族 5 个建点: cc:2095/2100(level+read/write 2-piece
> 内联)/cc:2241)。**不补 2 类**: cc:1595(Rust `guard_loads_range` COPY 插入体为
> 既有登记 stub,位点不存在,需先落 ValueSetSolver 精化);funcdata.rs
> `new_indirect_creation_in_space`/`new_indirect_op`(cc:689/692/719,注释声称带
> 尾、实现仅 apply_new_varnode_flags 折叠——超出本 lane write-set)。
> **新登记 `FUNCDATA-INDIRECT-SYMBOLTAIL-0001`(P2)**: 上述 funcdata.rs 两 helper
> 的符号尾缺口,owner=待派(funcdata 域),验收同 BZ 模式。验证(A/B 同机同树
> base=978a0a80): curl **字节级一致**(3601/0/0)、httpd 29 函数**字节级一致**
> (2459/0/0)、config 域逐函数(main/getparameter/parseconfig/glob_set/glob_range/
> glob_url)与全语料 witness(::config.=181/DAT_00117[56]xx=0/.rodata/glob_expand
> 字段化 5 处)全部相等——零回退零改善(位点在语料未激活,Rugra 侧行为中性,
> 如实报告)。heritage.rs=白名单,root 集成前需独立 Cross-Review。


> (三重自创+计数器错接全清:maxPass 硬编码 3→model getMaxOutputDelay/apply 返 0+
> take_count_delta/onlyOpUse RETURN-case res=false/BFS 全类型),ordinal 19 四元组
> **4/4/0/1 逐轮一致**;**Phase 2 首分歧推进至 ordinal 28 stackstall:oppool1**
> (863 vs 826,ruleaction/oppool 域,CD 车道已派)。亲测 curl 3654→**3610/0/0**(−44),
> httpd **2459 恒等**(其 2465=基点树陈旧假数,仲裁为准)。新登记(checkCallDoubleUse
> 既有 RUGRA-GAP: per-slot trial-address vn.loc 近似+isAlternatePathValid 未接
> input-active 分支,P2)。
> `BLOCKSTRUCT-MARKLABELBUMPUP-0001`(P2): markLabelBumpUp(cc:2195)在 Rugra 无
> 调用点——per-type 实现存在于 block.rs 但为死代码,影响=循环内 goto 的 label
> 定位(f_label_bumpup 永不置位);apply 注释与 docs/api 已纠偏为"四调用已接线+
> 第五缺口登记";fixture 的 RETURN-尾入口判别形态留扩展项。
> **`BLOCKSTRUCT-MARKLABELBUMPUP-0001` 已修复(2026-09-22,Lane CC
> `wt/sb-labelbump`,owner=fixer,wip 03210101,终稿见分支头)**: 第五图调用接线
> (blockaction.rs apply 在 mark_unstructured 后补 mark_label_bump_up(false),
> cc:2195 位);block.rs 死代码三 override 重写为忠实形态(旧版对
> condition+body 双双平铺置位,违背 cc:3319 的"仅 list[0] 强制 true"——已按
> BlockGraph 语义:自置位→condition(true)/body(false)→!bump 时清自身);
> 新增 trait 默认实现(cc:259 基类)+BlockGraph(cc:1258)+Goto/MultiGoto/List/
> Condition/If/Switch 继承 override;printc 消费侧补 cc:3222 isLabelBumpUp
> 早退(emit_any_label_statement 顶部)。E2E: curl **3653/0/0**(基线 3654,
> −1=消除 glob_set do 体内无引用伪 label `code_r0x00004C20`,oracle golden 0 引用
> 同款)、httpd **2456/0/0**(基线 2459,−3=消除 3 个 do 体内无引用伪 label,
> golden 均 0 引用)、双 corpus goto-label 零 dangling、gcc audit FAIL 集与基线
> 相同。B2: **blockstruct_marklabelbumpup_1204 双侧 MATCH**(5 case 含
> nested_loops_front 内层 whiledo 保旗判别形态;orderblocks RETURN-尾入口判别
> 已由其 case1/2 覆盖,扩项以此闭环);连带重钉 8 个 pin block/blockaction 的
> fixture 元数据,blockmultigoto/orderblocks/scopebreak 复跑 MATCH,
> blockgoto_wrapped 维持已登记 MISMATCH(IDENTIFY-BOUNDARY-0001)。
> **新发现(未认领)**: deadregion/goto_cascade 的 tracedag pin、
> goto_prints 的 coreaction pin 在本 lane 之前已陈旧(非本 write-set);
> dowhile_absorb runner 硬编码 `/home/wirs` mkdir 报错——三项待 fixture 维护
> lane 重钉/修复。

> **gp 978 triage(2026-09-22,Lane BW,净改善判定)**: 48/48 case 值+序 100% 等于
> oracle;449 上涨=真实内容从无到有+三部曲新工件。**新登记**:
> `SWITCH-BRIDGE-DUP-0001`(P1,~121 行 switch#2 重复桥工件——48 空 case+孤立 if+
> default-check 双发,含非法 C,疑 BO label 管道×BL 递归发射交互,owner=BX lane);
> `SWITCH-CASE-TAIL-0001`(P1,case 0x23/0x35 尾赋值+break 全缺→fallthrough 真损失);
> PRINTC-SWITCH-EMIT-0001 补证(default 末位/体拍平/头缺 -0x23U&0xff);aliases 零恢复
> 并入 JUMPTABLE/varmap 残差。报告=sb-switch/GP978_TRIAGE.md。
> **双链集成(2026-09-22 root)**: SwitchNorm P0-A(b400e90+e27e985,Cross-Review attempt2
> APPROVE)+spacebase 分派修复(2b324b8..97d6857,B2 fixture 7/10)合并 master,亲测
> httpd **2343/0/0**、curl **3685/0/0**(3711−6−20 对账一致),已回流 result(curl sha
> ad0e4170…)。**新登记**:
> - `JUMPTABLE-PARENTFACTS-FIXTURE-0001`(P1,Cross-Review 绑定条件,owner=fixture lane):
>   multistage 表通道①+兄弟 BRANCHIND 通道② 双侧 fixture;固化前两通道 B2=UNTESTED。
> - `TYPE-SPACEBASE-MISSFALLBACK-0001`(P2,**FIXED 2026-09-23**,Lane DG
>   `wt/sb-ord191`,owner=fixer,commit a0c4083e+fixture 重钉 commit): Ghidra miss
>   回退 getBase(1,TYPE_UNKNOWN)(type.cc:2964-2966,恒非 null) vs Rugra (None,0)。
>   修复:`TypeSpacebase::get_sub_type` miss/无 scope 两路径返回 (1 字节匿名
>   UNKNOWN 基类型, newoff=0)。验证(本树亲测,oracle e40ed130):双侧 fixture
>   `type_spacebase_subtype_1204` **10/10 MATCH**(ghidra/rugra stdout sha256 同为
>   db3869d6…);match_url Phase 2 首分歧 **191→317**(oppool2 result/count 23/23
>   对齐,ops 80385=80385);drill path layer 104 路径计数全等(ptrarith 23=23);
>   curl E2E 2713/0/0(基线 2733,-20)、httpd 2339/0/0 持平;next_url 投影 MATCH
>   保持;单测串行与 master 基线逐条一致。
> - `MATCHURL-PREFERCOMPLEMENT-317-0001`(P2,**FIXED 2026-09-23**,Lane DH
>   `wt/sb-ord317`,owner=fixer): 根因非翻转本体——SNAP 317 op 流双侧逐字节相同
>   (52ec:108 INT_NOTEQUAL→INT_EQUAL 双侧发生),缺口是 `ActionPreferComplement`
>   的结构体 `count` 字段从未经 `take_count_delta` 收割进投影可见的
>   `ActionState.count`(Ghidra blockaction.cc:2163 `count += 1` 走继承 protected
>   成员,fixture @END 直接读 `action->count`;action.cc:319-341 perform 对正 res
>   不累加)。修复=补 ActionConstantPtr 同款 `std::mem::take` 适配器。验证(本树
>   亲测,oracle e40ed130,产物 /dev/shm/rugra-tests/sb-ord317/): match_url
>   Phase 2 首分歧 317→337,@END 317 result=1/count=1/apply=1 与 oracle 一致,
>   聚合 @END 1 universal 1323=1323;curl/httpd E2E 与 next_url 投影见 commit
>   message;docs/api/coreaction.md 同 commit。
> - `MATCHURL-SETCASTS-337-0001`(P2,**FIXED 2026-09-23**,Lane DI
>   `wt/sb-ord337`,owner=fixer;coreaction 非机制 C 白名单,root 复核建议):
>   根因是**四层叠加的类型状态缺口**(双侧 CASTOUT/CASTIN 探针实证,
>   oracle 打补丁 build /dev/shm/rugra-tests/sb-ord337/oracle-probe):
>   ①`Varnode::update_type` 不置 high TYPEDIRTY(varnode.cc:461-463 的
>   `high->typeDirty()` 被"No-op"占位)且 setcasts 的类型读绕过
>   `getHighTypeReadFacing` 惰性重推导直读 stale 缓存——castInput 的
>   double-cast 调整臂对 5449 LOAD 地址 `updateType(code * *)` 后,
>   castOutput 的 LOAD token(typeop.cc:472 pointee)仍读旧
>   `undefined8 *` → 误插 CAST(=多出的 8 字节 unique+1 拍,337 的直接
>   表现);②`cast_standard_full` 把参数包进 `Arc::new(clone)` 恒不等,
>   interned 身份比较(cast.cc:302/329/392)全部失效,返回值也丢身份;
>   ③castOutput fallback/CALL 臂的 token 用未 interned 的
>   `base_type_for`("long"),oracle 是 factory `getBase`(SLEIGH 表
>   "int8"),token==outHigh 短路永不命中且污染实例类型;④
>   `shift_input_cast` reqtype 硬编码 Int,oracle 是
>   `inputTypeLocal`(INT_RIGHT=TYPE_UINT,typeop.cc:1528)+shift 族
>   getOutputToken 覆写(1518/1558/1608=in0 high)缺失、output_metatype
>   表 `_ => Int` 粗化。修复=varnode.rs update_type/update_type_lock
>   置 typeDirty+coreaction.rs setcasts 读路径走忠实 getter/token 工厂
>   化/metatype 表补齐/shift 臂+cast.rs 签名 &Arc 化保身份。验证(本树
>   亲测,oracle e40ed130,产物 /dev/shm/rugra-tests/sb-ord337/):
>   match_url Phase 2 投影 **MATCH**(340 stages/80385 ops,337 起全量
>   一致,@END 337 result=14 count=14 双侧一致);next_url 投影 MATCH
>   保持;curl E2E 2691/0/0(亲父实测基线 2713/0/0,−22=改进)、httpd
>   2331/0/0(基线 2339/0/0);config 域 11 函数逐个 0/0 零回退;gcc
>   审计 81 OK/26 FAIL=预存基线。残差:无(match_url 投影无剩余差异;
>   后续 lane 可换新函数/新域开新首分歧追杀)。
> - `FIXTURE-STOREVARNODE-STALE-0001`(P3,新登记 2026-09-23,Lane DG 发现,owner
>   待认领): `rule_store_varnode_spacebase_1204` fixture 在 master HEAD(5b9f12cf)
>   即双重失效——crate-tree pin 漂移(任何 src 改动触发)+ metadata.overall_status
>   与 runner 硬编码 require 串大小写不一致(MATCH:…untested vs …UNTESTED);需
>   专属重钉租约,非 DG write-set。
> - `FUNCDATA-OPSTACKLOAD-CONTAIN-0001`(P0 热修,**FIXED 2026-09-22**,Lane AO
>   `wt/sb-opstackload`,owner=fixer): funcdata.rs:6157/6113 双处(load+store)
>   `spc.space_id()` → `Architecture::get_contain(spc)` 解析 contain(ram)。
>   验证(单变量 A/B,同机同树): oppool2 规则改动 725→1048(+323)、
>   `resolve_spacebase_relative` 0→1 首次执行、curl defects=0/numbering=0、
>   skeleton 3685→3694(main +24/getparameter −7,gp 862→855)、httpd 2343 不变。
>   解锁暴露并同批修复 MERGE-GATHERPIECES-ISLEAF-0001(9ab15c4)。
> - `MERGE-GATHERPIECES-ISLEAF-0001`(P0,**FIXED 2026-09-22**,Lane AO `wt/sb-opstackload`,
>   owner=fixer,commit 9ab15c4): merge.rs `gather_partial_pieces` 缺 `PieceNode::isLeaf`(op.cc:801-817)
>   递归界 → 非树形 PIECE 图(输入由读取它的同一 PIECE 定义)无限递归;在
>   FUNCDATA-OPSTACKLOAD-CONTAIN-0001 解锁 RuleLoadVarnode 后 main 于
>   ActionMergeRequired 确定性 256MB 栈溢出(worker-failure)。已补五项 isLeaf 判定
>   + root_offset/base_offset 语义(merge.cc:1381-1387/1404)。merge.rs 属核心算法
>   白名单(机制 C),**Cross-Review: PENDING**。
> - `FLOW-RANGE-MARSHALING-0001`(P0,并入 RUGRA-FLOW-MIRROR lane 队列): driver
>   [entry,u64::MAX) 出界→尾跳 BRANCH 改写 CALL+人工 return(非 Ghidra 语义);oracle
>   全空间经 jumptable fail_thunk 转 CALLIND@0x2534。
> - `RULE-PTRARITH-ADDTREE-0001`(P1 新侧面,Lane AL): rugra 类型态下 AddTreeState 把
>   PTRSUB 溶解回 INT_ADD(oracle 侧 0),与 E2E piVar10+0x38 族(≈120 行)同向;倍数受
>   DWARF 环境不对称污染。`HERITAGE-SUBPIECE-CONST-WIDTH-0001`(P2,**FIXED 2026-09-22
>   连带关闭**,Lane BM wt/sb-subpiece): 0 偏移常量 8 vs 4 字节——真根因=space.rs
>   Register addr_size 8 vs oracle 4(见 SUBFLOW-SUBPIECE-WIDTH-0001 条),312 处族
>   随 addrSize 修正全收敛(next_url 普查宽 8 2931→0)。
> 全语料 libc 签名台账锁 7/9 callee vs oracle 裸 BFD)——D10/D11 族环境假阳性;登记
> `ACTIVEPARAM-COUNT-9V2-0001`(P1),裁决路径=Rugra 投影产线加"单函数无签名"双态开关
> 重跑(预期 9,9,0)+maxdelay 实值探针(RCA-2: 有效 maxpass oracle=1 vs rugra=2,fspec 域,
> 候选真差)。**Phase 2 前置新增**: 对拍框架须先钉平签名库环境(并入 RUGRA-FLOW-MIRROR
> lane 的双态开关)。
> **Lane X 完成(2026-09-22)**: gp switch 丢失根因闭合(报告 sb-switch/GP_SWITCH_ROOTCAUSE.md)
> ——符号/导入层无罪(config 已安装,DAT 引用 100%=config 字段);真凶=①**类型分派缺
> Spacebase 覆写**(datatype.rs:838 把 Spacebase 硬编码 (None,off),oracle 经虚分派
> TypeSpacebase::getSubType type.cc:2947 保住 PTRSUB)→gp 16 行 __spacebase 族;
> ②**打印层优先级反了**(printc.rs:5752 Priority-0 地址代理先于符号解析,oracle
> pushSymbolDetail 无代理走 pushPartialSymbol)+driver span 不感知播种→main/gp 165 行
> DAT 族。**新登记**: `TYPE-SPACEBASE-SUBTYPE-DISPATCH-0001`(P0,owner=spacebase-agent,
> wt/sb-spacebase,write-set=src/datatype.rs+docs/api)与
> `PRINTC-GLOBALSYM-LEAF-PRIORITY-0001`(P0,待 MultiGoto lane 退出 printc.rs 后派,
> 注意勿伤合法 .rodata &DAT 形态)。
> **消费端 quirk 登记(v1.2.1 落地时发现)**: 锁定 oracle opcode_name[] 表 60/61/65/66
> 槽=BUILD/DELAY_SLOT/LABEL/CROSSBUILD(枚举标签为 MULTIEQUAL/INDIRECT/PTRADD/PTRSUB),
> get_opname 按表下标返回→**Rugra emitter 对这四 op 必须发表字符串**,parity 检查显式
> 覆盖(绑定 C 侧 v1.2 对齐 lane)。
> ——jumptable 恢复无罪(88 entries 精确匹配);真凶=**①ActionSwitchNorm 空壳**
> (coreaction.rs:3611-3636 matchModel/recoverLabels/foldIn* 调用全注释,fold_in_normalization/
> fold_in_guards 已移植于 jumptable.rs:2648/3095 但零调用方)+**②newBlockMultiGoto 未移植**
> (block.cc:1716-1755,try_rule_goto 显式跳过 switch 块);叠加致 ruleBlockSwitch 守卫 111 次
> 全拒于 blockaction.cc:1705。**新登记**:
> - `JUMPTABLE-TABLEAPI-0001`(并入 P0-A): 激活 SwitchNorm 真身(match_model/recover_labels
>   +接线既有死代码),owner=switchnorm-agent(wt/sb-switchnorm),write-set=src/{coreaction,jumptable}.rs
>   +docs/api/*+双侧 fixture;机制 C 白名单(jumptable.rs)→合并前独立复核。
>   **P0-A 进展(2026-09-22,wt/sb-switchnorm wip 68ba315..HEAD)**: match_model/recover_labels/
>   trivial_switch_over/foldInNormalization/foldInGuards(表级)已移植+ActionSwitchNorm 接线;
>   foldIn* 模型层语义修正(foldInOneGuard 补 hasFoldedDefault/noInterveningStatement/
>   getFlipPath/isBooleanFlip;Basic2 结构改虚派发忠实形态;Assisted 真实实现;Override 删
>   INVENTED is_trivial 分支;addBlockToSwitch lastBlock=sizeOut;foldInNormalization 改走
>   op_set_input);BlockBasic::no_intervening_statement 新移植(block.cc:2712)。
>   E2E: curl 124 defects=0/numbering=0 skeleton 3711→3705(基线 /dev/shm/rugra-tests/
>   sb-baseline/curl_new.c@0acde30 同门禁重跑);gp --func 869→864 且根因症状
>   `(0x57 < config_00 - 0x23);` 悬空残骸消除(foldInGuards 中和守卫 CBRANCH@0x3fc5);
>   glob_set 91→90 switch 表达式 cast 链缩短;gp switch 结构仍需 P0-B(bb125 sizeOut=2
>   抢占 obvious-exit,根因报告 §B3 双缺一不可);B2 双侧 fixture 待 root 集成阶段固化。
> - `JUMPTABLE-PARENTFACTS-FIXTURE-0001`(P0-A/e27e985 交叉复核绑定条件,已完成
>   2026-09-22,owner=sb-oracle-agent@wt/sb-oracle): 两通道双侧 B2 fixture 合成环境
>   全部可达——M1 multistage/usenezmask(override 标记→checkForMultistage cc:2847 置
>   partialTable→recoverMultistage 内 analyzeGuards usenzmask=false cc:1052,SUBPIECE
>   nzmask 救援守卫记录 stage1 3 条 vs stage2 2 条)+P1/P0 兄弟 BRANCHIND 身份
>   (JumpBasic2 i>0 守卫回走 cc:1083-1091:同 switch 继续=6 守卫/异 switch break=3 守卫)。
>   46/46 记录双侧 byte-identical(oracle e40ed130 直跑);检测力实证:父快照临时置
>   pre-fix dummy(partial=false/indirect=None)时 M1 3!=2、P1 3!=6 两通道均捕获。
>   衍生两处移植缺陷已修(findDeterminingVarnodes 空栈回退 panic=cc:586-590 原始
>   op/slot 语义;sanityCheck 单条目 i=1 for-init=cc:1581),commit ef2f25f。
>   残差 `JT-PARENTFACTS-CORPUS-0001`(P1,wave 收尾):sanityCheck 修复改变真实语料
>   单条目表接受面,curl/httpd 差分基线须重跑;jumptable.rs 属机制 C 白名单→并入
>   main 前独立 Cross-Review(本 fixture 即证据载体)。
> - `BLOCKSTRUCT-MULTIGOTO-0001`(新开 P0-B): 补 newBlockMultiGoto+ruleBlockGoto isSwitchOut
>   arm+printc 发射(blockaction.rs/printc.rs);待派;机制 C 白名单。
> 验收(gp_switch): --func getparameter.constprop.0 switch 结构恢复+glob_set 改善+curl/httpd
> E2E defects=numbering=0 无回归+B2 fixture。

> **基线（root 亲测 @ master `85300a1`，2026-09-01 fresh formal release E2E）**：
> curl 124/124、skeleton **3090**/defects **0**/numbering **0**，stdout sha256
> `ff6bef47cf0154be1bd3e460882556e4f80f465b580e773b202895d890768a48`（已回流 result/curl_cur.c）；
> httpd 29/29、**2274/0/0**，sha `a31db12c866efce56765cc93f0fa938b1ca36f8c944354175dc5bd557afa5ffe`。
> 归档 /tmp/rugra-base-{curl,httpd}.{c,err}。门禁健康：oracle=e40ed130 ✓ hooks=.githooks ✓
> libdecomp.a ✓ BFD ✓（/tmp/rugra-ghidra-bfd-2.38 存活）。
> **函数级 top 差异（raw ±diff 行，按地址标记配对）**：curl main 876 / getparameter.constprop.0（~672 未入清单，
> 名字带后缀）/ parseconfig.constprop.0 / next_url 168 / match_url 113 / glob_set 110 / myprogress 103 /
> glob_range 103 / my_get_line 93 / glob_word 74 / helpf 73 / my_get_token 66；
> httpd main 656 / ap_fini_vhost_config 278 / ap_pregsub 246 / ap_getparents 241 / ap_update_vhost_from_headers 218 /
> ap_ht_time 98 / ap_strcasecmp_match 89。
>
> **会话中断回收（root，2026-09-01）**：上轮 FLEET10 中断后远程遗留 15 个 agent/* 分支。已甄别：
> - `agent/cmov-pending`(+2) = **W4 `RULE-PROPCOPY-ADDRTIED-0001` 完整修复**（b1c7308 merge.rs 惰性 cover 重建
>   根因 + cc8772f merge_trim_lane_1204 双侧 fixture MATCH）。分支自测：curl 3718/0/0（P16c 277→0，6 处 CMOVcc
>   空 if 恢复体，47 函数改善；+628 已逐类解释=暴露上游既有分歧）；**Cross-Review PENDING → 已派 reviewer-cmov**。
>   暴露的上游分歧已立案：main 自拷贝噪音 `x=x;`（~204 行，varmap/markInternalCopies 域）、helpf varargs
>   寄存器保存链位置（condexe/block 域）、命名/类型（varmap/typeop 域）——W4 集成后按新 top 派 wave2。
> - `agent/w5-diagnosis`(+1) = P23 UNLINKED-REF 逐语句根因台账 + **已验证 F1 补丁**（nodeSplit clone 输出丢
>   地址空间，funcdata_block.cc:992-1004；curl P23 8→5，skeleton −5）→ 已派 w-nodesplit 落地。
> - 其余 12 个老分支 → w-salvage 甄别中（报告 /tmp/rugra-reports/SALVAGE-TRIAGE-2026-09-01.md）。
>
> **W4 已集成(2026-09-01 root,`68311fa`+`70f4449`,R-CMOV-CROSSREVIEW **APPROVE** 闭环)**:
> merge.rs aggregate_high_cover_from 惰性 cover 重建修复 + merge_trim_lane_1204 双侧 fixture。
> master 亲测:curl **3718/0/0**(sha `a08dd3b0…`,与分支字节一致)、httpd **2344/0/0**(sha `429433e7…`)。
> 6 处 CMOVcc 空 if 恢复 golden 形态、P16c 277→0、47 函数改善;+628 skeleton=暴露上游既有分歧(已立案见下)。
> **复核 F1-F4 非阻塞发现(登记待办)**:F1 merge.rs:4445/4485/4497 注释引用 12.0.4 不存在的
> merge.hh:83 aggregateHighCoverFrom 等(cited-line-drift 红旗,改锚 variable.cc);F2 inflate_test(:889)/
> shadowed_varnode(:4085)/gather_block_varnodes(:68)/test_block_intersection(:88) 仍读原始 cover 字段
> (oracle 经 getCover()/updateHigh 重建——与 W4 同根因其余入口,已交 w-selfcopy 评估);F3 allocate_copy_trim
> 缺 merge.cc:416-428 类型解析段、merge_op phase1 j 循环快照 vs 实时 getIn(j);F4 mergeTest 门
> any-instance OR-flags 近似。
>
> **salvage 甄别交付(w-salvage,报告 /tmp/rugra-reports/SALVAGE-TRIAGE-2026-09-01.md)**:
> - **scopefix2-pending 可回收(TOP)**:`de4309f`=CALLSPEC-DRIVER-0001 实现(master inject 路径确认缺失)、
>   `657ff89d`+`5805489`=FUNCDATA-SCOPELOCALOVERFLOW-0001 wrap+双侧 fixture(master funcdata.rs:174 仍裸加法);
>   两个 repin commit 勿 pick(钉的是分支自身树),须对 master 重钉;冲突面=funcdata.rs 两小 hunk,**等 w-nodesplit 释放租约**。
> - **mainspinfix 可回收**:`00b157a`+`c1f61d4`+`47bece2`=PIPE-RESTART-0001 真实重启环唯一实现(master action.rs:1105
>   仍保守降级);wip 无 Evidence,需补四类语义+机制 C 复核;examples 冻结归 root。
> - regb `3d042eb`(model_locked 门,与 master coreaction.rs:8804 相反立场)+pltstub `9f14522`(create_placeholder
>   双守卫)→ **已派 w-pltwarn 裁决修复**(PLTSTUB-WARNLOSS-0001)。
> - calloutfix/markimplied2/mergeproto/orphanfix/switchemit 主体=INTEGRATED_BY_CONTENT 或 SUPERSEDED;
>   registry-cont-w4=第九窗已并 SUPERSEDED;registryw2/tri4-triage=DEAD;irreducible 残留=5 docs+SwitchTableMetadata seam。
>
> **编排**:root(主 Agent,唯一 master writer/集成者)+ 5 常驻后台子 Agent;worktree 隔离 + 独占写集租约 +
> 专属 CARGO_TARGET_DIR(/dev/shm/rugra-tgt-*)+ ghidra symlink;子 Agent 交付(branch commit + 报告
> /tmp/rugra-reports/<name>-2026-09-01.md)→ root 串行 cherry-pick → 构建 → E2E → 差分门禁(defects 必须 0)
> → 更新本板。板面 wave 节 root 维护,子 Agent 不直接改本板。20 分钟周期监控自动化在运行。

### W-2026-09-01-FLEET5 认领租约(5 并发;write-set 互斥已核)

| Agent | ID | 类型 | 独占 write-set | 状态/交付物 |
|---|---|---|---|---|
| ~~reviewer-cmov~~ | `RULE-PROPCOPY-ADDRTIED-0001`(机制C复核) | 只读 | 无 | **DONE**:R-CMOV-CROSSREVIEW-2026-09-01.md = **APPROVE**(四类语义 MATCH+fixture 实质钉住+独立实跑 exit 0);F1-F4 非阻塞发现已登记上文 |
| w-x86flags | `X86LIFT-FLAG-PCODE-0001`(iced路径) | writer | 已释放 | **DONE(已集成 `c21f7rd`..0abe6dd 五笔:c21f7fd rol/ror 77 站点、53b4e45 imul 69、9b65664 bt 家族 20、7fe22b9 comiss/ucomiss+XMM 寄存器表 37、0abe6dd mul/div/idiv+bswap 9)**。**情报修正:cmovcc/setcc/adc/sbb/add/sub/logic 早已集成(0c80ad5..56f77d1),本板 155 行陈旧已核销**。新工具 examples/x86ext_probe 双侧对拍 111/116 MATCH;httpd/curl E2E 字节不变;测试 1645/16(+1 过)。报告 /tmp/rugra-reports/w-x86flags-2026-09-01.md;**Cross-Review: PENDING(排队)**。剩余:SSE 移动 ~465+标量算术 ~185(→已派 w-sse,`X86LIFT-SSE-0001`)、rep-string ~128(`X86LIFT-REPSTR-0001` 待认领,CALLOTHER/循环语义) |
| w-sse | `X86LIFT-SSE-0001`(新登记) | writer | `src/disasm/x86_lift.rs`+`docs/api/disasm/x86_lift.md`+`examples/x86ext_probe.rs`，branch `wt2/sse` | 在途(2026-09-01 新派):SSE 移动家族(movss/movsd/movaps/movdqa/movq xmm 等 ~465 站点)+标量算术(addss/subss/mulss/divss/addsd... ~185);方法论=sla dump op-for-op+probe 双侧对拍 |
| w-subfloat | `SUBFLOAT-TRANSFORM-RESIDUAL-0001` | writer | 已释放 | **DONE(已集成 `5317299`+`6fbf6b8`+`b1d046f`)**:SubfloatFlow 完整移植(subflow.cc:3070-3481 十一函数)+三处 defer 替换+applyOp 回 oracle 结构;fixture 9 场景双侧 MATCH;curl 输出字节不变/myprogress 103→87/defects 0;httpd 不变;测试=已知 17。判定:TransformManager 基础设施本就在 transform.rs,缺口是 SubfloatFlow 未接线。**Cross-Review: PENDING(已派 reviewer-batch1)**。新登记 `OPS-GETREPEATSLOT-COUNT1-0001`(P2):src/op.rs get_repeat_slot 缺 op.cc:101 count==1 firstSlot 早退(op.rs 空闲可认领;subflow 调用点已内联完整语义) |
| ~~reviewer-batch1~~ | 机制 C 批量复核(`3725371`/`ad42d2b`/`5317299`) | 只读 | 无 | **DONE:3/3 APPROVE**(R-BATCH1-CROSSREVIEW-2026-09-01.md;三笔 Evidence 独立核实+两 fixture 实跑 MATCH)。**范围外 P0 抓获**:root 冲突解析漏 `[[example]]` 头致 Cargo.toml 重复键 → `a60f1ca` 已修+复验;教训=**冲突解析后必须立即 cargo check 再 continue pick**(机制 F 登记此失效模式)。附:op.hh:167 只有 iter 版 getRepeatSlot,op.rs count 参数版=签名级自创无调用者 → `OPS-GETREPEATSLOT-COUNT1-0001` 升级为"评估移除非补早退" |
| w-varargs | `HELPF-VARARGS-SAVECHAIN-0001`(新登记) | writer | `src/condexe.rs`+`src/fspec.rs`+protomodel.rs(如需)+printc.rs(如需,报告声明)+docs/api+fixture+registry，branch `wt2/varargs` | 在途(2026-09-01 新派):helpf 91 行 diff 族别化攻坚——golden 有 va_list ap+8×in_XMM*_Qa+long in_FS_OFFSET,Rugra 全 raw 名+int8 宽度+无 XMM 保存;根因方向=varargs param trials/保存链位置(condexe)/va_list 渲染 |
| w-nodesplit | `FUNCDATA-NODESPLIT-SPACE-0001` | writer | 已释放 | **DONE(已集成 `ad42d2b`+`b3244ce`)**:W5 补丁落地并纠正其两处 oracle 偏差(①无条件 queryProperties 尾=new_varnode_symbol_tail(Some(op_addr));②补齐 cc:995-997 addlflag fold writemask|ptrflow|stack_store);实现 new_varnode_out_full,new_varnode_out 变 Register-pin 委托;纠 5 处 cited-line-drift 注解。fixture 27 记录双侧 MATCH。curl 3090→3085(−5 纯降,P23 8→5)/defects/numbering 0;httpd 不变;测试=已知 17。报告 /tmp/rugra-reports/w-nodesplit-2026-09-01.md;**Cross-Review: PENDING**(排队)。残差归属:curl 5 行(F2 myprogress 浮点 4+F3 next_url unique0x9100)、httpd 7 行(F4 RAX 无名 3+F5 全局播种 4)非 FUNCDATA 域 |
| w-pltwarn | `PLTSTUB-WARNLOSS-0001`(regb 裁决) | writer | 已释放 | **DONE(已集成 `3725371`)**:裁决=regb 方向是 oracle 语义(cc:4618 单合取门恢复,master `!has_model()` 反面臂删除);pltstub `9f14522` fspec 双守卫证伪(caller 门控已在 master);**51/51 剩余差集定位 debugproto.rs 播种层**(26eee4a 起两 builder 用 from_model_carrier/set_pieces 装 defaultfp 名进 locked 原型,24 PLT 桩+3 void-DWARF unknown 身份源头即丢)→ 已派 w-debugwarn。当前语料门修复惰性(输出仅 getparameter 一行 COPY 对序变,skeleton 不变 3718);warning 文本拼写双侧一致。报告 /tmp/rugra-reports/w-pltwarn-2026-09-01.md;**Cross-Review: PENDING**(排队) |
| w-debugwarn | `PLTSTUB-WARNLOSS-0001` 残留(debugproto 播种) | writer | `src/debugproto.rs`+`docs/api/debugproto.md`+fixture+registry，branch `wt2/debugwarn` | 在途(2026-09-01 新派):对象级分流——LibcSignatureTable 全部条目+DebugDb 仅 void 签名条目钉 unknown 哨兵(行为 Arc 不变,红线=ACTION-FUNCLINK-INPUT-1204 复跑 MATCH);验收=warning 51/51+defects/numbering 0 |
| w-selfcopy | `MAIN-SELF-COPY-ABSORB-0001`(新登记) | writer | `src/merge.rs`+`src/varmap.rs`+`docs/api/{merge,varmap}.md`+fixture+registry，branch `wt2/selfcopy` | 在途:main 简单赋值 900 vs golden 90,temp↔stack COPY 未被 high 吸收;含复核 F2(其余 raw-cover 入口)/F3 线索;机制 C 域(merge/varmap) |
| w-scopefix | `CALLSPEC-DRIVER-0001`+`FUNCDATA-SCOPELOCALOVERFLOW-0001`(scopefix2 重放) | writer | `src/funcdata.rs`+`docs/api/funcdata.md`+scopelocal_wrap fixture+registry(如需 pushmultiequals/scope_find_overlap 重钉)，branch `wt2/scopefix` | 在途(2026-09-01 新派,nodesplit 释放租约后):重放 `de4309f`(inject_raw_ops 出生 CALL 无 spec→移植 flow.cc:683-690 核心)+`657ff89d`/`877ea9d`(wrap 域);fixture 对 master 重钉(双形态);repin commit 勿 pick;先读 oracle 全貌再落地,发现旧分支偏差按 oracle 修正 |
| BN(w-gotoprints) | `GOTO-PRINTS-NEXTFLOWAFTER-ARMS-0001` | writer | `src/block.rs`+`src/coreaction.rs`+`docs/api/{block,blockaction,coreaction}.md`+`tests/oracle/goto_prints_nextflowafter_1204.*`+`tools/run_goto_prints_nextflowafter_oracle.sh`，branch `wt/sb-gotoprints` | **DONE(2026-09-22,见 wave 日志当日条)**:nextFlowAfter 12 分臂单一事实源落 block.rs(`next_flow_after_successors`/`graph_sibling_successors`,pub)+修 BB 表 Switch 槽0/序两偏差+删 7 死代码 typed 方法;双侧 fixture 六形态 **MATCH**;curl/httpd 全量 defects=numbering=0 且输出与父提交**字节一致**(当前语料零翻转,纯潜伏雷排除);**机制 C: blockaction.rs 未触及**(改动域=block.rs/coreaction.rs,主管线行为变化建议 root 安排独立复核);**开项②(append 臂 multigoto gotoedge 形态)已由 BT 车道物化(第七形态 MATCH,见 BT 行)** |
| BT(w-mgresid) | `B2-MG-RESID-1/2/3`(MultiGoto Cross-Review 绑定条件收口)+BN 开项② | writer | `tests/oracle/blockmultigoto_1204.{cc,rs,metadata.json}`+`tools/run_blockmultigoto_oracle.sh`+`tests/oracle/goto_prints_nextflowafter_1204.{cc,rs,metadata.json}`+`docs/TODO_BOARD.md`，branch `wt/sb-mgresid` | **DONE(2026-09-22,fixture-only,零 src 改动)**:①fixture 三件套+pin runner 入库,四 family(规则驱动 switch_double_back/loop_exit_conflict 同形构造/direct 剥离契约/copy_switch_consumption 同形构造)双侧 runner **MATCH**;②`goto_prints_nextflowafter_1204` 第七形态 `switch_multigoto_gotoedge`(cc:3548-3553 append 臂)落地,MATCH,既有六形态字节不变;③glob_set/gp 归因=真差但全量落在 JUMPTABLE-TABLEAPI-0001(label 占位+goto-case 发射)+globalsym 覆盖缺口(原始偏移 vs 字段化),multigoto 域无可修项,详见 wave 日志 B2-MG-RESID 节;master E2E 基线 3648/0/0 复测无损;**续作(同日,BO label 管道并后适配)**:BlockSwitch 新增 jump/case_order 字段,两 fixture 三处字面量按 pre-label 旁观默认填充(jump=None/case_order=空,同 printc_switch_emit_1204 模式;case_gototypes/case_values 保持观测语义原值),全部漂移 pin(block/blockaction/rust_fixture)重钉,两 runner 于 label-pipeline master 复跑 **MATCH**(观测投影字节不变) |
| BN(w-gotoprints) | `GOTO-PRINTS-NEXTFLOWAFTER-ARMS-0001` | writer | `src/block.rs`+`src/coreaction.rs`+`docs/api/{block,blockaction,coreaction}.md`+`tests/oracle/goto_prints_nextflowafter_1204.*`+`tools/run_goto_prints_nextflowafter_oracle.sh`，branch `wt/sb-gotoprints` | **DONE(2026-09-22,见 wave 日志当日条)**:nextFlowAfter 12 分臂单一事实源落 block.rs(`next_flow_after_successors`/`graph_sibling_successors`,pub)+修 BB 表 Switch 槽0/序两偏差+删 7 死代码 typed 方法;双侧 fixture 六形态 **MATCH**;curl/httpd 全量 defects=numbering=0 且输出与父提交**字节一致**(当前语料零翻转,纯潜伏雷排除);**机制 C: blockaction.rs 未触及**(改动域=block.rs/coreaction.rs,主管线行为变化建议 root 安排独立复核) |
| BV(wt/sb-orderblocks) | `BLOCKSTRUCT-ORDERBLOCKS-0001`(=CR-BO 条件③)+`HTTPD-EXAMPLE-BRACE-0001`(P0 抓获登记) | writer | `src/block.rs`+`src/blockaction.rs`+`docs/api/{block,blockaction}.md`+`tests/oracle/blockstruct_orderblocks_1204.*`+`tools/run_blockstruct_orderblocks_oracle.sh`+registry+本行，branch `wt/sb-orderblocks` | **DONE(2026-09-22,见 wave 日志当日条)**:cc:2191 orderBlocks+compareFinalOrder 三键移植+BlockGoto/MultiGoto lastOp 委托补齐;B2 fixture 5/5 MATCH;curl/httpd A/B 字节恒等(语料全单元素列表,守卫双侧同步跳过);**机制 C: blockaction.rs 触及 → Cross-Review PENDING(root 派)**;附带 P0=master httpd 示例 dff36e78 编译破损(修版在 stash@{0},勿动) |
| ~~w-salvage~~ | 12 个老 agent/* 分支甄别 | 只读 | 无 | **DONE**:SALVAGE-TRIAGE-2026-09-01.md;结论已并入上文 salvage 节 |
| sb-condreplay(wt 分支) | `PRINTC-COND-REPLAY-0001`(BL 审计遗留两处条件通道裁决+转换) | writer | `src/printc.rs`+`docs/api/printc.md`+本行 | **DONE(2026-09-22)**:裁决=两处均真偏离(oracle cc:3053-3056=comma_separate 块分派全量 walk、cc:3088-3093=only_branch 块分派重放,括号由 opCbranch 供给;golden 1592/1601/2523 comma-init 形态实证)。改动=whiledo 入口 mod 协议(cc:3012-13/3065)+普通臂/overflow 重放分派化+dowhile 入口协议+尾部 only_branch 分派化,删 dowhile 文本缓冲+块级 R50 折叠。三门禁:curl 3649/0/0(基线 3648)、httpd 2459/0/0(基线 2462)、gcc 失败集逐函数恒等(81/26、7/22);printc 单测 12/12。golden 形态落地:comma-init 族/getparameter `']'` 修复(golden 2253)/glob->size 解析/unique+`( *)` 泄漏清除。新登记 `PRINTC-CONDBLOCK-JUNKOPS-0001`。详见 docs/api/printc.md 2026-09-22 追加节 |
| DA(wt/sb-loadclaim) | `HERITAGE-LOADCLAIM-0001`(P1,VARGROUP-ABSORB-0001 真最后一环:调用点栈参 LOAD 认领路径定位+挂载重启用) | writer | `src/funcdata.rs`(spacebase 挂载)+`docs/api/funcdata.md`+本行,branch `wt/sb-loadclaim` | **DONE(2026-09-23,oracle 认领链 gdb 实证定位+挂载重启用)**:①认领路径双侧实证——oracle gdb 轨迹=funclink 建 opStackLoad LOAD→mainloop 迭代1 oppool2 **RuleLoadVarnode directify**(ruleaction.cc:4278,LOAD→COPY(n:stack:fc78:304))→restart→**heritage pass2 认领**(placeMultiequals cc:2599→collect cc:307→refinement cc:1890→**refineRead cc:1772**→concatPieces cc:507,304B 自由栈读→280+8+8+8 PIECE 梯 seq3501-3503 替换 COPY 输入;killedbycall INDIRECT 3515=guardCalls cc:1521-1525 同 pass 产物)→oppool1 RulePropagateCopy 折叠 COPY;**Rugra master 5c610849 阶段投影实证同链已在位**(marker42 oppool2 directify→marker50 heritage 认领建 3450-3452 同形梯)——旧前提"Rugra heritage 无认领"过期,无需新代码;②spacebase 挂载重启用(funcdata.cc:263-264 镜像,v1 撤回前提已消):curl 2994→2996(+2=in_RSP 声明换行 cosmetic)defects=0/numbering=0,httpd 2337==基线 0/0,**glob_set 92/92+glob_range 80/80 零行变化**(CW 撤回清单验证通过),glob_url 2/2,next_url 输出零行变化(Phase2 MATCH 保持),match_url 80→79;cosmetic 残差=8 函数 `__spacebase_1_<hash> *in_RSP` 声明名(varmap 栈符号映射既有缺口表现面,oracle 同状态渲染 BADSPACEBASE,随 §4-4 符号层收敛;**名渲染部分已由 SPACEBASE-SYMNAME-0001 于 2026-09-23 收敛为 BADSPACEBASE,声明行存续归符号层**);③main 吸收推进量=**0**(43 处 CONCAT 中间态未动)——实测挂载后梯根 u:10000a06:304 writeback URLGlob(Struct)×4 pass 成功+propagateSpacebaseRef/propagateRef 已点火(fc78 见 ptr-to=8 而非 URLGlob*),吸收剩余阻塞在 §4-4 符号/打印层(establishGroupSymbolOffset/linkProtoPartial/pushPartialSymbol+梯 implied 隐藏)+URLGlob* 指针链(LOAD 已 directify,TypeOpLoad 反传只在迭代1 infertypes LOAD 存活窗口内可用),归 VARGROUP-ABSORB-0001 后续;证据=/dev/shm/rugra-tests/sb-loadclaim(work+bk 双侧断点+gdb 轨迹) |
| DE(wt/sb-symname) | `SPACEBASE-SYMNAME-0001`(DA 开项②小车道:__spacebase_1_<hash> 声明名泄漏收敛) | writer | `src/type_system/typefactory.rs`(get_type_spacebase 名清空)+`docs/api/{type_system/typefactory,funcdata}.md`+本行,branch `wt/sb-symname` | **DONE(2026-09-23,基 master 9ac9a84b)**:双侧渲染核实——oracle `TypeSpacebase(AddrSpace*,const Address&,Architecture*)`(type.hh:735-736)基 `Datatype(0,1,TYPE_SPACEBASE)`(type.hh:214)**name 为空**,`PrintC::buildTypeStack`(printc.cc:143-163)止于匿名非 PTR/ARRAY/CODE,`pushTypeStart` 匿名分支(cc:280-285)→`genericTypeName`(cc:3373)→**`BADSPACEBASE`**(cc:3387-3389 无 size 后缀)——即同符号状态下 oracle 机械渲染 `BADSPACEBASE *in_RSP`(golden 中无该声明系 oracle 栈符号映射完整,in_RSP 符号不存在,归 VARGROUP-ABSORB-0001 §4-4);Rugra 泄漏源=get_type_spacebase 旧版把合成 dedup 键 `__spacebase_{ws}_{frame}` 同时当类型名。修复=TypeBase 名清空(键仅存续为 name-keyed BTreeMap 去重槽位,Ghidra 经 findAdd compare 树去重 type.cc:3996/3045-3055)。验证:①curl 8 函数 `__spacebase` 计数 8→0,声明全部变为 `BADSPACEBASE *in_RSP`(前后全文件 diff=仅 8 行声明改名);②curl defects=0/numbering=0 skeleton 2993==基线,httpd defects=0/numbering=0 skeleton 2339==基线;③glob_set/glob_range/glob_url 前后 IDENTICAL,next_url 输出零行变化(103/103 defects=0),match_url 79==79;cargo test --lib 18 failed==基线逐一致(全为 funcdata SSA/alignment 既有)。附注=base.flags CORETYPE 旗标 oracle 运行时构造器不设(仅 XML core="true" 解码路径,type.cc:641-644),既有行为未动,如影响继承语义另行登记 |
| CT(wt/sb-vargroup) | `VARGROUP-ABSORB-0001`(P1,CR-CQ 登记移交) | writer | `src/{heritage,subflow,ruleaction,merge,printc}.rs`+`docs/api/*` 同步+本行,branch `wt/sb-vargroup` | **在途(2026-09-22 CW 修 IR 层,commits a77d6c02→bc45a768(v2 撤回全局 fold 改 heritage 侧承接))**:CT 机制图的 §4 前提经双侧 drill/raw 实证**勘误**——oracle 同样有 280B 影写 INDIRECT 链+MULTIEQUAL@0x2806(heritage 两侧一致),真分歧=①RulePieceStructure 丢根地址空间(件落 Register 域)②栈 varnode 缺 addrtied(setVarnodeProperties 无 localmap 腿)→RuleSubRight 把 splitCopy 的 45 个栈件化成 INT_RIGHT 移位梯;①②已修(PIECESTRUCT-SPACE-0001/SUBRIGHT-ADDRTIE-0001)+propagateSpacebaseRef/propagateRef 接收端移植保留(coreaction.cc:5208-5306,spacebase 挂载**已随 HERITAGE-LOADCLAIM-0001 于 2026-09-23 重启用**);验收=curl E2E defects=0/numbering=0(skeleton 3074 vs 实测基线 3105)+httpd 2392==基线 0/0+config 域 A/B glob_set/glob_range/glob_url IDENTICAL+getparameter 向 golden 靠拢+with_piece=528(45×280 组保持);main 0x30d6 实参 IR 恢复 oracle 件落栈形态;**余项=C 文本仍 ~41 CONCAT 中间态**:~~下一环=调用点 opStackLoad LOAD 存活语义~~(已由 HERITAGE-LOADCLAIM-0001 于 2026-09-23 关闭:双侧认领链同形,oracle LOAD 同样在迭代1 oppool2 被 directify,§4-3"Ghidra 留 LOAD 到最后"勘误)+符号层(store LHS 组符号字段路径/件临时名,机制文档 §4.3-4.4);前次会话 socket 中断 salvage 完成(过期诊断识别:probe3 with_piece=0 系 master 合并前旧代码;master CQ 合并后 group_partials 实测 with_piece=452 已工作);**oracle 控制台单函数复现成功**(decomp_opt 从锁定源码重建+调用点原型手工注入,字段吸收形态 UVar3.pattern[i].xxx 与 golden 结构一致,证机制链=FuncLinkInput 304B 栈载入→SplitDatatype 拆件→RulePieceStructure 字段化→groupPartials/linkProtoPartial→printc 部分符号);**精确分歧点=SPLITCOPY_BIG probe:280B 栈读 rugra 侧 in_written=true 而 oracle 侧为 in_stack 输入影子,拆分守卫(两侧一致)因此未拒,件落入寄存器/join 域而非栈域**;机制图+复现脚本+重建命令=docs/alignment_docs/VARGROUP_ABSORB_MECHANISM_2026-09-22.md;下一步=符号/打印层(§4-4)吸收收尾 |
| (排队) | `PRINTC-CONDBLOCK-JUNKOPS-0001` | writer | `src/blockaction.rs`(whiledo cond 块构建)+相关 SSA/merge 域,owner 待派 | 排队(2026-09-22 sb-condreplay 发现):whiledo 条件块残留 junk COPY(`glob = filename`/`uVar6 = uVar6`/`__ptr = __ptr` 自拷贝)随 comma_separate 全量 walk 打印(match_url 97→99、parseconfig 197→199 波动源)——打印通道正确,多余 op 为结构层差异(golden 条件块已消除);修域=structurer/SSA copy 消除,非 printc。**根因数据补注(2026-09-22 sb-copyprop lane)**:本症状属 diff-high COPY 幸存族——main 在 ActionCopyMarker 时活 COPY 9051、same-high 8059 已标 nonprinting、**diff-high 992 幸存**,打印 608 条(Unique-out 为主,spill/restore 乒乓形态);主杠杆=merge 相 Cover 保守近似挡掉 required merge,详见 MERGE-COPYNOISE-DIFFHIGH-0001 与 docs/alignment_docs/COPYPROP_LANE_VERDICT_1204.md §3/§4。**勘误补注(2026-09-22 CE 实证修正)**:本行"主杠杆=merge 相"方向获双侧证实并已修复——具体缺口=merge_test_with_list 粗近似相交(非 Cover 模型本身),CE 1250e5d0(待复核)改走 testCache.intersection(merge.cc:1664);修后全文件自赋值 907→2,余 2 个 match_url `glob = glob` 归本 junk-COPY 族,whiledo 条件块 junk COPY 需在修复基线重测再定修域。**Lane DR 裁决补注(2026-09-23,wt/junk,基线 94f3bf58,只读)**:打印侧结论关闭——①same-high 打印 junk=0(main 终态活 COPY 26,15 same-high 全 np=1;match_url 0;curl 全语料自赋值恰 2、httpd 0),CL 时代 +44 已被 1250e5d0+castInput 臂(1cd11409/94f3bf58)收敛;②printc 消费面复核无误(printc.rs:3308-3324/3341-3355 ↔ printc.cc:2696/2703-2705;oracle pushSymbol/emitStatement 无 LHS==RHS 文本守卫,同 IR oracle 也会打 x=x);③余 2 个 match_url `glob.pattern[3]._0_8_ = glob.pattern[3]._0_8_;` 根因=ScopeLocal 参数条目错装(**VARMAP-PARAMSTORAGE-BLOB-0001**,修域 coreaction.rs/varmap.rs);④main 残余唯一 oracle 无的形态=spill/restore 对 @0x2669/@0x3188(diff-high,归 merge/copyTrims 系)+ **BLOCKACTION-ALIVELIST-GLUE-0001**(alivelist-only 删除 glue 使 @0x2669 不在 census 却仍打印);⑤三门禁基线恒等:curl 2665/0/0、httpd 2331/0/0。证据=/dev/shm/rugra-tests/sb-junk/JUNK_REPORT.md(census/挂接 backtrace/条目表) |
| (排队) | `VARMAP-PARAMSTORAGE-BLOB-0001`(P2,Lane DR 新登记) | writer(owner 待派) | `src/coreaction.rs`(ScopeLocal bootstrap 参数条目 1516-1521)+`src/varmap.rs`(storage 建模/查询)+`docs/api/{coreaction,varmap}.md`+本行 | 登记(2026-09-23 wt/junk):`fd.funcp.is_input_locked()` 时参数符号硬编码 `AddressSpace::Register`+整参数类型尺寸安装——MEMORY 类按值参数(match_url `URLGlob glob`,304B)变成 Register:8/size304/addrtied/无 uselimit 的 blob 条目,`find_container_entry` [8..311] 命中 rax:160 指针循环高 → ActionNameVars link_symbol→handle_symbol_conflict→attach_symbol_to_vn 把 8B rax varnode 挂 304B `glob` SE → 两个 diff-high 同名 `glob.pattern[3]._0_8_` → 2 个自赋值 junk(oracle 同位为独立局部 __dest/pcVar8:linkSymbols 只查 nameRepresentative 的真实地址,栈条目不匹配 rax)。**双向破坏**:真栈槽读(in_stack_00000130)反查不到条目(oracle 打 `glob.size`)。INTEGER 类参数(filename rdi/8B)碰巧正确。修法=按参数真实存储类落条目(MEMORY→栈槽;寄存器→寄存器尺寸),验收=match_url 自赋值 0+`glob.size` 形态出现+三门禁恒等。证据=/dev/shm/rugra-tests/sb-junk/(JUNKMAP/JUNKSETSE backtrace/JUNKFINAL census) |
| (排队) | `BLOCKACTION-ALIVELIST-GLUE-0001`(P3,Lane DR 新登记) | writer(owner 待派) | `src/blockaction.rs`(ActionFinalStructure 内 8354-8391 unreachable-op 清理)+`docs/api/blockaction.md`+本行 | 登记(2026-09-23 wt/junk):该 glue("Remove unreachable ops after unconditional BRANCH or RETURN",注释自认无 oracle 对应)仅从 obank.alivelist 删 op,不置 DEAD、不清 block 链接——main spill 半 @0x2669(`p_Stack_240 = __stream;`)终态不在 alivelist(is_dead=false,printc 按 block 仍打印),而 copymarker/probe 等 alivelist 消费者对它失明。风险双面:后置 alivelist 驱动 Action 盲区+census 审计失真。需对照 oracle block 切分语义重设计(unreachable op 根本不应进 alivelist/block)。证据=/dev/shm/rugra-tests/sb-junk/JUNK_REPORT.md §4 |
| (排队) | `EXAMPLE-HTTPD-BRACE-0001` | writer(owner 待派) | `examples/httpd_decompile.rs`+本行 | 排队(2026-09-22 sb-copyprop lane 发现):master 77e97b4 起 httpd example 无法编译——`cargo build --example httpd_decompile` 报 examples/httpd_decompile.rs unclosed delimiter(fn worker_memory_image_bytes 起,文件花括号 410开/409闭)。sb-copyprop worktree 工作区 clean 复核=HEAD 自带损坏,非 lane 改动(本 lane write-set 仅 coreaction/merge/printc 探针+docs)。影响=httpd E2E 门禁不可跑;修复=定位缺失 `}`(疑最近 pspec/constbase 提交 df36e78/ea0f7a74 一带)+恢复门禁基线(对照 sb-condreplay 记录 httpd 2459/0/0) |
| (送审) | `MERGE-COPYNOISE-DIFFHIGH-0001` | sb-copynoise@wt/sb-copynoise(2026-09-22) | `src/merge.rs`(compute_varnode_covers)+`docs/api/merge.md`+本行;已完成 | **判决翻转(2026-09-22 sb-copynoise lane,wt/sb-copynoise)**:登记时的嫌疑根因被证伪——successor [0,MAX] 填充在判决基线(77e97b4)早已禁用(dead code),且 `compute_varnode_covers`/`merge_all` 在生产管线**零调用**(coreaction.rs 走逐 Action 细粒度路径,生产 cover 全部经惰性 `update_cover_locked`→`Cover::rebuild` 忠实链)。lane 交付=近似实现换轨为 oracle 惰性机积极物化(calc_cover+COVERDIRTY+update_cover_locked)+删除自创 `propagate_cover_through_cfg`/`op_block_order`+错注更正(merge.hh:83→varnode.cc:233)+形状回归测试(中间块回填,旧实现必败);E2E curl/httpd byte-identical(3654/2459 skeleton,defects=0/numbering=0)。**真根因新证据**:探针(/dev/shm/rugra-tests/sb-copynoise/)测得 CopyMarker 时 1957 个 diff-high 幸存 COPY 中 **1531 个一侧为 implied**(mergeTestBasic 正确拒绝,oracle 同语义)→噪声主杠杆在 **MarkImplied×打印折叠**(implied 标记后 printc 未折叠,归 printc/implied lane);仅 ~312 ok/ok 对未合并(移交 MERGE-COPYNOISE-OKOK-0001);次级分歧(R3 hideShadow 遍历序/R4 PIECE 臂)维持登记。cross-review PENDING(merge 白名单,集成前必须)。**勘误(2026-09-22 CE 实证修正,sb-impliedfold 1250e5d0 待复核)**:本行"真根因新证据"段归因方向被推翻——1531 个单侧 implied COPY 是**本侧 merge bug 制造的噪声**(merge_test_with_list 粗近似相交→ActionMergeRequired main 多打 +8804 trim COPY,oracle +45;mergeTestBasic 拒绝本身正确,但被拒对象不该存在),非"打印侧未折叠"缺口;oracle main 自打 20 个合法 in-implied COPY,printc.cc:2703-2705 只跳 out-implied(Rugra printc.rs:3341 已同语义),"折叠责任在打印侧"不成立。真根因+修复=/dev/shm/rugra-tests/sb-impliedfold/LANE_REPORT.md(commit 1250e5d0:merge_test_with_list 改走 type_test_cache.intersection=merge.cc:1664 字面;curl 3654→3018、自赋值 907→2、defects/numbering 全零);OKOK-0001 的 312 对口径需在修复基线重测 |
| (送审) | `SB-IMPLIEDWAVE-0001`(+48 implied 群体/30d6 创建波) | sb-impliedwave@wt/sb-impliedwave(2026-09-22) | `src/subflow.rs`(splitCopy 四 builder)+`src/merge.rs`(groupPartials 注册表源+registerProtoPartialRoot,wip 4b8ab734)+`src/coreaction.rs`(base_explicit cc:3040 partialRoot 守卫)+`src/ruleaction.rs`(RulePieceStructure cc:7697-7698 注册)+`docs/api/{subflow,merge,coreaction,ruleaction,funcdata}.md`+本行 | **DONE(终态 commits 4b8ab734(wip builder)→b660033b(消费链+守卫+探针清零)→88e32df1(buildInConstants >8B 常量移位饱和,回归测试复原;E2E 复验字节不变),基 ce0b5b81+master e2d3c6b4 合流;单线程 cargo test --lib 失败集与 master 既有 18 项逐字相同=零测试回归)**:创建者=RuleSplitCopy→SplitDatatype::splitCopy @0x30d6(curl main, match_url 按值 URLGlob 参数拷贝,DWARF proto 驱动);**判定=创建后未消**(SIGSIG 公平 oracle 同位同裂,golden 亦有同裂;旧简化 builder 不打 protoPartial/partialRoot/不注册→MarkExplicit 不强制显式→MarkImplied 冻结 20 分片)。修复=忠实消费链(四 builder+setProtoPartial+setPartialRoot+registerProtoPartialRoot+groupPartials 按注册表+cc:3040 守卫)。**验收**:30d6 波成员 oimpl=0/oexpl=1/opp=1(=oracle MarkExplicit 契约),implied-out 波贡献 R=0≤O=0,指纹消失;残差 implied-out R=29 vs O=5(公平口径)=const/junk COPY 域(PRINTC-CONDBLOCK-JUNKOPS-0001,CANDGEN §4 路由)+29dd 同位同判;三门禁:curl defects=0/numbering=0(skeleton 3095 vs 基线 2978,+117 全在 main=分片显式化+CONCAT 语句链,VariableGroup 符号打印 varmap 域落地前不被吸收,已 ## Differential 说明),httpd 2392==基线 defects=0/numbering=0,config 域逐函数零回退;探针清零后重建输出与带探针运行字节一致。**Cross-Review: PENDING(merge 白名单,集成前必须)**;产物 /dev/shm/rugra-tests/sb-impliedwave/(oracle_sig 公平 census 等,重启即丢;结论已录本行+commit message) |
| **DONE(2026-09-23,wt/sb-gptrial,commit 4a86abf6;首分歧 55→65)** | `GETPARAM-ACTIVEPARAM-TRIAL-0001`(P1,fspec 参数 trial 记账) | sb-gptrial@wt/sb-gptrial(fixer) | `src/funcdata.rs`(check_call_double_use cc:1789-1790 接通)+`docs/api/funcdata.md`+本行;实际缺陷点=funcdata 非 fspec | **根因(双探针对拍钉死)**:ord 55 activeparam 的 RSI register:38 trial 镜像判定 ar=1/so=1 相同但 ancestorOpUse 分叉——oracle probe(archive 树 stderr 插桩,投影体与 pin 4464f2ff 逐字节同)示 checkCallDoubleUse `op=46db:2e4 match=4151:20a j=5 ck=0 ac=0 alt=1`→cc:1789-1790 `isAlternatePathValid=true`→return false→onlyOpUse res=false→trial markInactive;Rugra check_call_double_use 对未 checked trial 无条件 return true(旧 RUGRA-GAP 注释)→RSI 误 active→fillinMap markUsed→u:23e00 残留(stack:fa40 trial 亦被 cross-talk 翻转 active→inactive)。修复=cc:1789-1790 忠实接通(`else if is_alternate_path_valid(vn,fl) return false`,fl=BFS cur_flags,已有 16703 行 1:1 端口首次接通 input-active 分支)。**验收**:①getparameter 投影首分歧 **55→65**(新=ord 65 universal:fullloop:mainloop:stackstall:oppool1 `V1_RESULT_COUNT_DIVERGENCE` result/count 740 vs 734,ruleaction/oppool 域,**登记 GETPARAM-OPPOOL-COUNT-0001 待派**;ord 1-64 含 55 activeparam 全 stage/snapshot identical);②curl **2689/0/0==基线**,httpd **2331/0/0==基线**;③next_url+match_url Phase 2 投影**双 MATCH 保持**(335/340 stages);④config 域零回退:getparameter 输出 vs 8fd23706 基线**逐字节相同**(golden 754 不变),main 621/match_url 76/helpf 81 全同基线;⑤funcdata:: 批量测试 17 失败=master 既有(基线 A/B 复核,单跑通过,与本 write-set 无关)。B2 证据=锁 oracle 投影对拍(双函数全 MATCH+本函数 1-64 identical);正式 tests/oracle fixture 归 root 集成挑拣。双侧产物=/dev/shm/rugra-tests/sb-gptrial/(oracle probe+双侧 trace) |
| (排队) | `GETPARAM-OPPOOL-COUNT-0001`(P2,ruleaction/oppool 计数) | writer(owner 待派) | `src/ruleaction.rs`(oppool 计数族)+本行;只读排查可并 | 排队(2026-09-23 sb-gptrial lane 定位):GETPARAM-ACTIVEPARAM-TRIAL-0001 修复后 getparameter Phase 2 投影新首分歧=**ord 65**(universal:fullloop:mainloop:stackstall:oppool1)`V1_RESULT_COUNT_DIVERGENCE` result/count oracle **740** vs rugra **734**(@END 计数差,rugra 少 6 次规则应用);ord 1-64 含 activeparam 族全 stage/snapshot identical(证=非 trial 域下游)。双侧产物=/dev/shm/rugra-tests/sb-gptrial/(oracle pin 4464f2ff+rugra_clean);同类前科=next_url/match_url 车道记录的 oppool1 计数分歧族(CD 车道 oppool 域);验收=ord 65 后移 |
| (排队) | `MERGE-COPYNOISE-OKOK-0001` | writer(owner 待派) | `src/merge.rs`(mergeOpcode 门链只读排查)+`src/coreaction.rs`(ActionMarkImplied);只读探针可并 | 排队(2026-09-22 sb-copynoise lane 登记):312 个 basic 双过(b1=b2=true)但 CopyMarker 时仍 diff-high 的 COPY 对——需钉死去向:req fail?intersect=true(cover 真相交)?merge 后被 resplit(DominantCopy/HideShadow 改写)?探针方法照 sb-copynoise(op ptr 配对 COPYSEE/COPYMARK,/dev/shm)。依赖=MERGE-COPYNOISE-DIFFHIGH-0001 判决翻转证据 |
| (排队) | `VARIABLE-INTCOV-RAWREAD-0001` | writer(owner 待派) | `src/variable.rs`(update_internal_cover)+`docs/api/variable.md` | 排队(2026-09-22 sb-copynoise lane 只读发现):`HighVariable::update_internal_cover`(variable.rs:374-391)直接读 `inst.cover` 字段,而 oracle 语义是 `inst[i]->getCover()`(variable.cc:331,varnode.hh:202 惰性重建 COVERDIRTY 实例)。当前观测路径安全(update_high_cover/aggregate_high_cover_from 调用前已对实例跑 update_cover_locked),但任何未预更新的新调用路径会并入陈旧/空 cover——与 RULE-PROPCOPY-ADDRTIED-0001 同类隐患。修法=实例循环内先 update_cover_locked 再 merge。无已证 E2E 影响(本 lane curl/httpd byte-identical) |
| (排队) | `RULE-PROPAGATECOPY-0001`(P2) | writer(owner 待派) | 差分 fixture(`tests/oracle/` 三件套+runner)+`src/ruleaction.rs`(仅当差分暴露偏差时)+本行 | 勘误登记(2026-09-22 sb-docerrata,源自 sb-impliedfold CE 实证):BY 判决"oracle 12.0.4 无 copyprop 规则"系**错名搜索**(搜 RuleCopyPropagate,该名+ActionCopyPropagation 确实零匹配)——oracle 实有 **RulePropagateCopy**(ruleaction.hh:723 / ruleaction.cc:3926 applyOp,注册 coreaction.cc:5566 oppool1 `actprop->addRule(new RulePropagateCopy("analysis"))`)。Rugra 侧结论=**已有实现待差分**:ruleaction.rs:205-218 `RulePropagateCopy`(// Ghidra: ruleaction.cc:3926)已注册 action.rs:1649(// 5566);前序 RULE-PROPAGATECOPY-DRIFT-0001(历史表,2026-08-17 DONE)已 faithful 重写+5 回归测试,残留=逐函数 oracle fixture 未产生。本条=补 B2 差分证据(真实 oracle 跑 RulePropagateCopy::applyOp 全守卫链:isReturnCopy/isWritten/CPUI_COPY/isHeritageKnown/自引用/marker 三子守卫);注意与 sb-copyprop 删的 ActionCopyPropagate(自创未注册死代码)是两回事。勘误注记=docs/alignment_docs/COPYPROP_LANE_VERDICT_1204.md §0.1 |
| (排队) | `MERGE-UNTEDINTERSECT-0001`(P3) | writer(owner 待派) | `src/merge.rs`(HighIntersectTest::intersection 回退臂+stackAffectingOps 惰性 populate 链,merge.rs:417-425 已有载体)+fixture+本行 | 补登记(2026-09-22 sb-impliedfold CE 开项③,sb-docerrata 落账):Rugra `HighIntersectTest::intersection`(merge.rs:280-310,// Ghidra: variable.cc:1166)缺 addrtied-untied 回退臂——oracle 在 cover 块级判交全失败后还有 variable.cc:1188-1197 `if (!res)` 分支:`aTied != bTied` 时对非 tied 侧调 `testUntiedCallIntersection`(定义 variable.cc:1072;该函数顺带 populate stackAffectingOps,variable.cc:1080-1081=merge.hh:85,Rugra merge.rs:421 文档已自认"No Rust production path populates it yet")。Rugra 从 intersect_list 循环直接写缓存,该臂整体缺失。**触发条件=untied call 场景**(addrtied 高变量 × 跨 CALL 非 addrtied 高变量的合并测试;当前语料调用闭包不可达,无已证 E2E 影响)。修复须连 stackAffectingOps 惰性链一起补;merge 白名单=机制 C 强制独立复核 |
| (done) | `SB-FINALCAST-0001`(VARGROUP-ABSORB §6 残余①归因裁决) | finalcast-agent@wt/sb-finalcast | 只读诊断+`docs/alignment_docs/VARGROUP_ABSORB_MECHANISM_2026-09-22.md` §7+本行(零 src 改动) | **裁决(2026-09-23,基 master 8fd23706):§6 残余①"RHS finalcast=pushPartialSymbol allowCast 臂(printc.cc:2018-2029)"归因证伪,printc.rs 无缺口**。四处行级证据:①cast.cc:414 `offset!=0→false`,残差全部 byteOff=8 必不命中;②cast.cc:419-422 outtype 限 INT/UINT/UNKNOWN/PTR/FLOAT,union 不在集;③finalcast 臂命中不推人工 entry(printc.cc:2024-2027)→不可能 `(cast)sym._off_sz_` 共存;④oracle 控制台 `(ContentUnion)SUB2416(...,8)`(sb-vargroup 工件 oracle_main.c:604)=cast 包裹**函数式** SUBPIECE(pushPartialSymbol 未参与)→cast 是 IR CAST op。Rugra `partial_symbol_walk` allowCast 臂已在位(printc.rs:15382-15401)且正确不点火。真根因→SETCASTS-COPYINPUT-0001(coreaction)+DWARF-ANON-TYPENAME-0001(debugproto);printc 渲染链(op_type_cast printc.rs:12351+emit_inline_expr CPUI_CAST:7490)与 cast_standard_full default 臂(type_system/cast.rs:514,=cast.cc:339-391 Union/Pointer→Some)均验证忠实,无需改。三门禁(本分支=master 零 src delta):curl 124 函数 defects=0/numbering=0 skeleton **2689==基线**;httpd 29 函数 0/0 **2331==基线**;next_url 103/match_url 76(0/0;76 vs DD 时点 79 系 master 8fd23706 前 blockstructure/setcasts 提交自身演化,本 lane 零 delta 由 git diff master 证实)+config 域 6 函数(main/getparameter/parseconfig/glob_set/glob_range/glob_url)逐个 0/0。产物 /dev/shm/rugra-tests/sb-finalcast/(curl_full/httpd_full/逐函数提取,重启即丢;结论已录 §7+本行) |
| (done) | `SETCASTS-COPYINPUT-0001`(P1,COPY 输入 cast 臂) | fixer@wt/sb-copyinput | `src/coreaction.rs`(copy_input_cast+dispatch 臂)+`src/type_system/cast.rs`(cast_standard_full enum 元类型规范化,write-set 经查冲突矩阵无主后扩)+`docs/api/coreaction.md`+`docs/api/type_system/cast.md` 同 commit+本行 | **完成(2026-09-23,基 master 49c303c4)**:①coreaction.rs cast_input dispatch 补 `OpCode::CPUI_COPY` 臂→新 `copy_input_cast`(// Ghidra: typeop.cc:397),逐行镜像 TypeOpCopy::getInputCast:reqtype=out getHighTypeDefFacing(非基臂 inputTypeLocal),curtype=in0 getHighTypeReadFacing(op)(覆写直接读 slot0 忽略 slot 参数,无基臂 annotation 守卫——Rugra 侧派发后统一应用),castStandard(false,true)。②cast.rs cast_standard_full 补 req/cur 元类型规范化 Enum→Int/PartialEnum→Uint(oracle:TypeEnum 全构造路径强制存 TYPE_INT/UINT,type.hh:491-494 内联构造器+type.cc:1475 decode;枚举性由 ENUMTYPE flag 携带,cast.cc:347/363 注释即此呈现)——不规范化则枚举落 default 臂恒过cast(main 9×`.type` 行+`(int)::config.httpreq` 前缀,修复后与 oracle 判定一致地消失;golden 该两处均无 cast)。③验收全过:curl 124 函数 defects=0/numbering=0 skeleton **2689→2665**(-24,逐函数扫描仅 main 621→605/getparameter 754→748/file2string.part.0 125→123 全改善**零回退**);httpd 29 函数 0/0 **2331==基线**(字节级仅 1 行 `puVar10=(uint1 *)uVar2` 新 cast,golden 无对应行=结构分歧域);next_url **103**/match_url **76** 逐字节不变 0/0;config 域 6 函数(main/getparameter/parseconfig/glob_set/glob_range/glob_url)全 0/0 且 main/getparameter 改善;gcc 语法审计 81OK/26FAIL==基线;lib 测试 18 失败∈基线 19-20(失败集严格子集,均为预存 flaky/heritage_creation@master)。④残留登记:`(union_5a7)` vs `anon_union_16_3_...` 名=DWARF-ANON-TYPENAME-0001(既有);新暴露符号/字段类型态残差(3 行)→DWARF-SYMFIELD-TYPESTATE-0001。产物 /dev/shm/rugra-tests/sb-copyinput/(curl_after2/httpd_after/before_after2.diff/perfunc_delta.txt) |
| (部分完成,①落地) | `DWARF-SYMFIELD-TYPESTATE-0001`(P3,符号/字段类型态残差) | fixer@wt/symfield | `src/debugproto.rs`(①已落地)+coreaction/unionresolve 域(②③待认领)+`docs/api/debugproto.md` 同 commit+本行 | **①完成(2026-09-23,基 master 94f3bf58)**:双因素修复——(a)`parse_type_names` 的 `DW_TAG_typedef` 条目改经 `resolve_type` typedef 分支解析(物化为 typedef 拼写,Ghidra TypeTypedef type.hh:522 保名语义),`type_names["FILE"]` 从 `struct _IO_FILE` 改为名为 FILE 的 216B struct;(b)`DebugGlobalDatabase::parse_elf` walk 收集 external 声明(DW_AT_declaration+DW_AT_external+DW_AT_type,首见优先)+walk 后 goblin 枚举 R_X86_64_COPY 重定位目标(`copy_reloc_object_symbols`,STT_OBJECT,@@VERSION 剥离)按地址种入——stdout/stdin/stderr@0x174e0/f0/500 获 DWARF FILE* 类型(typelocked 经既有 seed 路径,ELF 回退跳过),curl 受影响集合恰为 stdio 三符号,httpd 无 DWARF FILE 路径惰性。验收:main `__stream_00 = stdout;`/`__stream = stdin;` 裸、`(FILE *)0x0`、`heads.stream = (FILE *)stdout;` cast 重现(golden 896)、全 `_IO_FILE`→`FILE`;curl defects=0/numbering=0 skeleton **2665→2614**(main 605→583、libc FILE thunk 11→9、my_fwrite 12→4、getparameter 748→743、helpf 81→77,10 改善 0 回退);httpd 2331==基线 0/0;next_url/match_url 字节不变(103/76);gcc 审计 82OK/25FAIL(基线 81/26);lib --test-threads=1 18 失败==基线。**②③未修(已定位,修复域=coreaction/unionresolve store-cast 目标选择,非 debugproto)**:②`pattern[8].content.Set.elements` 行 Rugra 在 STORE 插 `union_a49` 8B PartialUnion cast(`get_exact_piece` union 臂,dump op@0x30d6 `CAST(PartialUnion)=in_stack_fd90`)vs golden `(char **)`——oracle 经 ScoreUnionFields.derefPointer 钻 Set→elements@0 尺寸匹配后 cast 到叶子 char**(typeop.cc:530-554/unionresolve.cc:227);③`glob._296_8_ = (undefined8)uVar32` 同类 PartialStruct 片 vs golden 裸 uVar29。产物 /dev/shm/rugra-tests/sb-symfield/ |
| (排队) | `DWARF-ANON-TYPENAME-0001`(P2,DWARF 匿名 composite 命名) | writer(owner 待派,debugproto/dwarf 域) | `src/debugproto.rs`(resolve_type 匿名命名 1162/1167/1172)+oracle 证据前置+本行 | 排队(2026-09-23 sb-finalcast 登记):Rugra 对匿名 struct/union/enum 用 DIE offset 命名(`struct_{:x}`/`union_{:x}`,debugproto.rs:1162-1172,RUGRA-GLUE),oracle 12.0.4 Java DWARF importer 合成 `anon_union_16_3_e2f18bb4_for_content` 形(binary DWARF 无此串,readelf/strings 双查 0 命中;sparse checkout 无 Java;11.4.2 发行版 stubs 显示旧方案 `anon_<tag>_for_<fields>`,12.0.4 插入 `_<size>_<n>_<hash8>` 段)。**算法 NO_ORACLE——按机制 D 禁猜**,前置=取得 12.0.4 DWARFUtil/DIEAggregate Java 源(完整 distribution 或源码 repo)并多样本验证 hash/序号语义。SETCASTS-COPYINPUT-0001 落地后本缺口变成 `(union_5a7)` vs `(anon_union_16_3_e2f18bb4_for_content)` 的显性文本残差;届时 curl defects/numbering 门禁不受影响(compare 缺陷分类不计 cast 前缀文本),属登记残差 |

> 冲突矩阵:x86_lift.rs=w-sse;subflow.rs 已并;funcdata.rs=w-scopefix;coreaction.rs+fspec.rs=已并(PENDING 复核);
> merge.rs+varmap.rs=w-selfcopy;condexe.rs+fspec 域=w-varargs(printc 如需则报告声明);debugproto.rs=w-debugwarn。
> E2E 输出一律 /tmp/<agent>-*,禁触 result/(root 专属)。W4 后 top:main 1248/getparameter.constprop.0 869/
> parseconfig 199/next_url 147/file2string.part.0 144/match_url 100/myprogress 96/helpf 91/glob_set 91。
> **`CODEGEN-DIVERGENCE-REDFLAG-0001` 新实证(root,2026-09-01)**:getparameter 的 `uVar27=uVar30;`/`uVar33=uVar27;`
> COPY 对顺序在三次语义无关集成间振荡(cur→cur2→cur4:x86_lift 改动不可能语义影响 curl SLEIGH 路径,却使输出
> 翻回旧序)——迭代序/堆布局敏感,已知会 w-selfcopy 定位(其域内)。master 当前验证:curl 3713/0/0、httpd 2344/0/0
> (字节=cur4/cur2 除该一对外稳定)。
> **`HERITAGE-PROMOTE-SYMBOLTAIL-0001`(P1,**FIXED 2026-09-22**,Lane BZ
> `wt/sb-globfield`,owner=fixer,rebase 于 0b737955)**: globalsym typed-global 覆盖
> 扩展到 glob 族(BT 的 RESID-3 归因第二域)。根因:heritage 的 renameRecurse 三处
> 输入提升(heritage.cc:2500/2509/2539)、guardReturns 三处(cc:1628/1670/1687)、
> guardInput 两处(cc:1973/1985)在 oracle 全经 `Funcdata::newVarnode`(带
> queryProperties→setSymbolProperties 符号尾,funcdata_varnode.cc:161-166),Rugra
> 只跑 flags 折叠且 Ram 腿预置 MAPPED 堵死后续 attach——`glob_expand` 读
> (0x17660,DWARF `URLGlob*`)提升后 `t=Int/int8` 无 typelock,RulePtrArith
> (cc:6629 TYPE_PTR 门)不点火,输出 `(int*)glob_expand+…*0x18` 原始算术。
> 修=各点补 `fd.set_varnode_properties(&vn)`(create 后、flags 前)+
> `Funcdata::set_input_varnode` 补 cc:364 属性遍(早退/去重臂不跑)+
> debugproto 类型工厂软驻留(intern_named/pointer_type 走共享 TypeFactory,
> 消双 DWARF 通道 Arc 分裂的伪 cast,glob_url 4 保持)+
> prettyprint WARN-EMIT2 R4 门(签名启发式排除比较运算符,堵本修复引入的
> 多行 if 续行误命中→glob_range 重复声明 numbering 0→6→0)。验证(rebase
> 0b737955 双侧):glob_set 97→94/glob_range 80→76(URLGlob 字段化形态
> `glob_expand->size/->pattern/->type/->content`),**main 1199=1199/
> gp 880=880/parseconfig 199=199 零回退**(config 域 `::config.` 186=186,
> DAT_00117[56]xx=0,`.rodata` witness 4 保留),curl 3645/0/0(基线 3654),
> httpd 29 函数 0/0,gcc 审计 81/26 同基线。b9277490 旧基线上 gp 866→978
> 为 2154919d 已判"净改善"态(SWITCH-BRIDGE-DUP 触发),8e9a98ed/cfd6b40a
> 已修,新基线实测零差。**新登记**:`GLOBFIELD-RESIDUAL-0001`(P2,残差:
> ①glob_url `->size` store 侧未字段化——malloc 结果 void*→CAST int8 挡
> RulePtrArith,TYPEOP-PTRSUB-FIELDCAST-0001 域;②`pattern[i]` 打成
> `(&pattern + i)` 数组下标形;③`.content.Set.elements` 联合成员下钻;
> ④`(URLPatternType)0x1` vs `UPTSet` 枚举名;⑤uVar4 传播类型 vs oracle
> typed pUVar2)。报告=/dev/shm/rugra-tests/sb-globfield/。
> **`FUNCDATA-INDIRECT-SYMBOLTAIL-0001`(P2,CLOSED 2026-09-22,Lane CI
> `wt/sb-indirecttail`,owner=fixer)**: CF 裁决表登记的 funcdata 域子项——
> `new_indirect_op`(cc:689/692)与 `new_indirect_creation_in_space`(cc:719)
> 注释声称带符号尾、实现仅 `apply_new_varnode_flags` 折叠。修=BZ 模式在
> create/def 接线与折叠间插 `set_varnode_properties`(折叠保留)+注释按
> oracle 逐字纠偏(双路径 usepoint:newVarnode cc:162=INVALID `Address()`,
> 仅 addr-tied 项可挂,database.cc:117-119;newVarnodeOut cc:115=定义 op 地址,
> 接线后 `get_use_point` 精确同值)。验收(A/B 同机同树 base=d3fbe924):
> curl 3607/0/0→**3605/0/0**(−2,config 域潜伏位点激活:main 4 行+
> parseconfig_constprop_0 1 行,其中 `(void*)0x0`→`(char*)0x0` 2 行与 golden
> 1008/1017 逐字节一致,char 字面量 `'\0'`→`0` 方向中性仍非 bool 残差);
> httpd 2456/0/0 字节级一致;config 域 6 函数抽验 0/0+函数体 A/B 零回退
> (getparameter_constprop_0/glob_set/glob_range/glob_url 恒等);cargo
> test funcdata/indirect/guard 域 62/17 与 base 逐项相同(17 失败 d3fbe924
> 既有)。funcdata.rs 非白名单,root 快速复核。证据=/dev/shm/rugra-tests/
> sb-indirecttail/。

## 历史 wave：`W-2026-08-29-FLEET10`（2026-08-29~31；goal=函数文本级对齐；会话中断，成果已大宗回收入 master）

> 基线(root 亲测 @ master `3fb97c11`,即 08-28 wave backlog 落库 + 2 个 char 域陈旧测试修复):
> cargo check 绿;`cargo test --lib -- --test-threads=1` = 1626 passed / 17 failed / 5 ignored(已知集:
> 1 normalize_branches CONTINUE 断言 + 16 FFI_TEST_LOCK 级联 FUNCDATA-TESTS-FLAKY-0001,零新增)。
> **fresh formal E2E @ `3fb97c11`**(2026-08-29,stdout sha=`19bba32a62a900c92037253baf4b01292524707329d467f3384f15d490b56da6`,
> 2970 行/97807 bytes,归档 `/dev/shm/rugra-3fb97c11-curl.c`):124/124、skeleton=3528、numbering=0、
> **defects=2**(`EMPTYELSE-REGRESS-0001`:getparameter.constprop.0 非法嵌套 `else{`(函数内~156 行)+
> glob_word 空 `else{}`(~60 行);08-28 快照 defects=0 之后由 backlog 引入;结构域已路由 w-identify,
> printc 侧知会 w-anondecl)。输出体量 60KB→97.8KB(golden 96.2KB),内容恢复大幅推进。
> Top skeleton:getparameter 715 / main 710 / next_url 284 / glob_range 226 / file2string 191 /
> myprogress 186 / match_url 148 / glob_word 135 / parseconfig 129 / glob_set 124。
> **httpd 回归(root 亲测 @ `3fb97c11`)**:仅输出 5/30 函数(08-28 基线 29)——首因
> `ap_parse_vhost_addrs` panic `typefactory.rs:1999 LowlevelError: TypeFactory alignment map not
> initialized`(get_type_pointer enforce=true 打在 align_map 未接线工厂)→锁中毒→23 worker PoisonError
> 级联;归 `HTTPD-TFALIGN-PANIC-0001`(路由 r-plt-httpd);curl corpus 无此 panic。
> 编排:root(主 Agent,唯一 master writer + 集成者)+ **10 个常驻后台子 Agent**(5 writer + 5 reader);
> worktree 隔离（`/home/wirs/.cache/rugra-w2-*`）+ 独占写集租约 + 专属 CARGO_TARGET_DIR + flock 共享资源；
> 子 Agent 交付（branch commit + 报告 `/tmp/rugra-reports/<name>-2026-08-29.md`）→ root 串行 cherry-pick →
> flock 构建 → E2E → 差分门禁（defects 必须 0）→ 更新本板。板面 wave 节 root 维护，子 Agent 不直接改本板。
> **已集成(2026-08-29 21:45)**:`FUNCDATA-NEWVARNODE-SYMBOLTAIL-0001` 实现半(w-newvarnode2 交付 2a55097b:queryProperties/
> setSymbolProperties/setFlags 完整接线,含三处 review 修正;cherry-pick 到 master 编译绿/测试=已知17/E2E 2134·0·1 无回归;
> 双侧 fixture 未交付→状态 PARTIAL,fixture 验收仍开放)。
> **舰队轮换(2026-08-29 21:50,第二轮预算耗尽后)**:myprog(存活)+新派 w-identify3/w-anondecl3/w-registry3(收尾交付)
> +w-nonconverge(NONCONVERGE-P0,merge/action 域);全舰队改用【里程碑即时 commit+报告增量追加】纪律防预算耗尽丢工作;
> 20 分钟周期监控自动化已建立(探活/抢救/验收/补位)。
> Agent 完成即由 root 从队列补位。**并发上限 2026-08-29 下午起调整为 5(用户指令)**;当前 5 活跃:
> w-anondecl2/w-newvarnode2/w-identify2/w-registry2(续作 writer)+w-myprog-cluster(myprogress/glob_range 簇)。
> **机制 C 复核已闭环**:`R-DESCEND-CROSSREVIEW-2026-08-29.md` 对 `55783411` 判 **APPROVE**(五 Rule+簿记链
> 四类语义全 MATCH,复核者独立重跑 E2E 确认 defects=0/numbering=1/skeleton=2134);其 F1 跟进项已修——
> `RULE-NEGATEIDENTITY-CALCMASK-BOUNDARY-0001`(f91eb0265 引入的 `size>=64` bits/bytes 混淆,改用忠实
> calc_mask 8 字节饱和,master `99ae9c35`,Cross-Review: APPROVE 随附)。
> 首批 5 reader 已终止(r-getparam 交付 jumptable 256→88 截断/cmp $0x57 guard/BOOL_AND pullback 情报;
> r-f2s/r-plt 留 diag 分支 agent/reader-*;r-main/r-parseconfig 无报告,结论需重查)。
> **已集成**:`PTRSUB-SWITCH-CAST-RESIDUAL-0001` phase A(w-switchfix2,fixture+runner 三模式绿,
> master `46dd1f02..c6939910`,registry 已登记,overall=MISMATCH 13 条登记差异)。
> 补位队列(按 55783411 基线 diff 排序):
> ①descend 修复的机制 C 独立 Cross-Review(ruleaction.rs,优先);②myprogress(102)+glob_range(93,含 DUPDECL)簇;
> ③glob_word(83)+my_get_line(82);④helpf(73)+my_get_token;⑤REGA-HUGEHELP(heritage.rs);
> ⑥剩余 inrefs 直接写点 sweep(清单=/tmp/rugra-reports/INREFS_SWEEP_INVENTORY_2026-08-29.md,已知生产者已清,剩余点需逐个对照 Ghidra 判定)。
> ②簇 root 预分析(2026-08-29):myprogress 三信号=(a) Rugra 发 `/* WARNING: Type propagation algorithm not settling */`
> 而 golden 无(ActionInferTypes 传播不收敛本身是分歧);(b) golden 有 DWARF 局部名 `format/line/outline`(typed stack 数组),
> Rugra 全 raw `auStack_*`(DWARF 局部符号未接线到 varmap);(c) 多余寄存器 temp 声明(`int8 in_RSP`/`Var8`/`extraout_XMM1_Qa`)。
> glob_range=`int iVar3;` 声明两次(golden 用 iVar4,per-prefix 计数器回退,VARMAP-GLOBRANGE-DUPDECL-0001)。
> **本轮监控循环集成(2026-08-29 23 时,root)**:①anondecl3 匿名声明渲染(7a5b24af/f34b88cf,六函数 typeless 装声明 0)
> +root 间距根治兜底(7c3df2ce:组合名指针 glue,skeleton 2198→2114,DECL-SPACING-NAMEFLOW-0001 登记根治方向=导入器匿名化,已派 w-globword);
> ②identify3 完整交付(f19bac86/bc2db403:identifyInternal 无 f_dead+双侧 fixture MATCH+E2E 逐字节零劣化;whiledo 门保守固定+residual 登记;
> 机制 C 复核=reviewer-identify 在途);③myprog 交付获 R-MYPROG-CROSSREVIEW **APPROVE**(P-FLOW-1 登记欠账见下)。
> **NONCONVERGE-GETPARAM-MATCHURL-0001 已修(root `96e8e1a8`)**:join-block 路径接入 node_join_create_block(含
> structureReset,funcdata_block.cc:816);w-nonconverge 因果证明+fixture 红绿双态;0 panic/timeout,124/124 全反编译。
> **2026-08-29 深夜集成burst**:①anondecl3 根治(e331a5c5 六处构造匿名化)+glue 兜底撤销(46412358)+DECL-SPACING-NAMEFLOW-0001
> 闭环(fixture 撤 residual 回 MATCH);②nodejoin 修复获 R-NODEJOIN-CROSSREVIEW **REJECT**——致命 F1(索引求值序)已整改
> (faea03b0:语句序索引+响亮断言+CFG形状断言+删冗余build_dom_tree),F2-F5 登记如下,待重审;③w-globword 交付:B 部 5 提案
> (C1-C5)登记,A 部匿名化与 e331a5c5 同根被覆盖(其 named_return_pointer twin 洞察记录于报告);④registry:check/strict 双绿
> (2889e048+再生,definitions=9494/raw=15811/rust=10467),lint=2 仅剩 checkpoint 治理推进(10ea407d..HEAD 窗口审查)。
> **基线修正(w-main2)**:main 在当前 master 结构化已完整(finalize 149→1,无 selectGoto exhausted;
> result/curl_cur.err 是 8/27 陈旧 stderr);仍耗尽的是 getparameter(已修)。main 结构段计数:golden if65/while6/do5/for2/goto19/label8/helpf8 vs Rugra if14/while1/do0/for1/goto4/label2/helpf0。
> **2026-08-30 凌晨全绿里程碑**:集成 w-carry 的 guard-lattice+LATTICE-GEN 修复链(c7d1c1a0+8ca7bb57:
> 根因三层=单读 cover 缺 addRefPoint CFG 前驱递归/vn2 def 比较序缺 getUIndex marker 规则/from_op INDIRECT 端点回退;
> "保护格代际"假设被双侧证据否定——console oracle 缺 Java NoReturnAnalyzer 数据造成的假差异)后:
> **defects 0/numbering 0/0 panic/124/124 全反编译**。同窗口集成:RC2(main 体 245→643,51×setopt 级联恢复)+
> C4(INT_NOT token 序,非法形态 2→0)+goto 发射 target_dyn 切换+njf234 复核单门补齐(cc:2076)。
> **当前权威基线(master `c743bf26`,会话中断后大宗回收)**:curl E2E skeleton **3090**/defects **0**/numbering **0**;
> httpd **2278/0/0**。回收集成:shifts 三笔(shl/shr/sar flags 全家族)+collapse4 两笔(findExit 容器臂+checkSwitchSkips
> +双侧 fixture)+w3 两笔(FuncProto-void 投影守卫+P13 退役;P22 词界)。历史:3528→2911→…→3095→3091→**3090(0/0)**。——**三项全零**(hdefects DoNothing 忠实重写-13+ndo 嵌套发射恢复+19;main `do {` 3→5=golden、corpus do 17→23);httpd **2277/0/0**——**双语料 defects 全零里程碑**(livearm:根因=build_blocks_from_ops 的 CBRANCH 出边序反了
> [target,fallthru] vs oracle [fallthru,target](flow.cc:960-967+getFalse/TrueOut 配套)→常量折叠进错误支配侧→空臂;修复后 curl 字节不变);历史链:3528→2911→…→3089→3076→**3095(0/0)**;registry 第七窗已并,dowhile/push88/hdef/ndo 窗例行待批
> (PendingBrace 动态机制 1:1 移植+backfill 签号门修 numbering;+9=8 处裸 else 恢复 golden 括号结构);
> httpd 2325/7/0(iced 链:cmp/test 旧 flag 偏移全错修正+js/jns 曾零 op+c5 零 op 臂;-46 空投影;+2 defects=下游结构化残差已解释)。历史链:2911→…→3110→**3119(0/0)**。(main iVar4 dup,
> PRINTC-STRUCTEMIT 残差簇已登记);CARRY 泄漏 5→**0**(CARRY1 形态×3);main URL-glob 区与 golden 结构 1:1;
> while(true) 头逐字节匹配;双侧 fixture printc_whiledo_body_emit MATCH;httpd 2212/5/0。
> 历史链:2911→2884→3374→3104→3108→3068→3060→**3110**(内容恢复优先)(virtual_list 可变列表
> 语义已修:collapse 消费组件移除+组合块尾接+6 扫描点 shift-skip;do-while 阻塞定缝到 @339e 缺块);httpd 2214/5/0;
> 历史链:2911→2884→3374→3104→3108→3068→**3060**。
> extraout_var 全消(RULEACTION-PULLSUB-SPACE 已修:main 循环守卫与 golden 逐 token 同形);httpd **2214/5/0**(29/29,strcasecmp 收敛;+66 恢复内容;defects 5 含僵尸下游);
> 历史链:2911→2884→3374→3104(0/0)→3108(C5)→**3068**。
> 历史链:2911→2884(F2-F5)→3374(RC2 内容恢复)→**3104(0/0)**。
> 旧基线(master `2889e048`)**:**
> 0 panic/timeout/ABORTED,124/124 全反编译;httpd 仍 5/30(HTTPD-TFALIGN-PANIC 待重派)。
> 历史权威基线(master `96e8e1a8`,诚实口径)**:curl E2E skeleton **2866**(全函数计入)/defects **1**(getparameter
> empty else,GETPARAM-EMPTYELSE-0001 已派 w-nonconverge2)/numbering **0**;registry 138 条目。
> identify3 获 R-IDENTIFY-CROSSREVIEW **APPROVE**;nodejoin 链闭环:REJECT(F1)→整改(faea03b0)→R-NODEJOIN-CROSSREVIEW2 **APPROVE**(语句序等价独立推导+断言双组合验证+门禁全绿 2911/1/0);F2-F5 维持在册。getparameter 两函数 F1 后=678/1/0(残差含 F2/F4 贡献)。
> **fleet 纪律(事故后)**:禁用 git stash(跨 worktree refs 共享碰撞),一律 per-worktree commit;stash@{0}(ifgoto-wip)保留勿动。
> **P-FLOW-1 登记欠账(复核发现,root 待落)**:`SUBFLOAT-TRANSFORM` 残差与 `TRI4-MYPROGRESS-SETTLE-0001` 需补板行。
> 舰队(5):w-nonconverge2/w-globword/w-main2/w-registry4/reviewer-identify。
> **当前权威基线(master HEAD,2026-08-29 22 时)**:curl E2E skeleton **2118**/defects **0**/numbering **0**(DUPDECL 已修)/
> ABORTED 0/raw 命名 16;myprog 交付含 myprogress 警告行消除。历史:2134(99ae9c35 口径)——【口径警告】getparameter+match_url 因 `NONCONVERGE-GETPARAM-MATCHURL-0001`
> 超时无输出,其 ~672+83 diff 未计入,可比口径≈2889(与 2d78b5af 的 2881 相当;descend 修复的真实收益=10 函数命名/结构/defects
> 质量+2 函数转入待修非收敛);httpd 仍 5/30+24 panics(HTTPD-TFALIGN-PANIC-0001 在查)。Top:main 696/next_url 144/file2string 134/
> parseconfig 131/myprogress 102/glob_range 93/glob_set 88/glob_word 83/my_get_line 82/helpf 73;PLT stub 族 ~24×9-11。
> 历史参考:2d78b5af=skeleton 2881/defects 1;3fb97c11=skeleton 3528/defects 2(簿记腐蚀期)。

### W-2026-08-29-FLEET10 认领租约（10 并发；write-set 互斥已核）

| Agent | ID | 类型 | 独占 write-set | 交付物 |
|---|---|---|---|---|
| w-newvarnode2(续作)+root | `VARNODE-DESCEND-BOOKKEEPING-0001` | **FIXED @ master `55783411`**(root 修复;w-newvarnode 审计立功:簿记基础设施本就完备,真凶=ruleaction.rs 五处直接 inrefs 写) | `src/ruleaction.rs`+`docs/api/ruleaction.md`(已落库) | 修复:Piece2Zext/Sext→`fd.op_remove_input(0)`+`op_set_opcode`;TrivialBool 六分支完整移植(仅 slot1 常量+`V&&0→#0`/`V||1→#1`/`V^^1→NEGATE` 补齐);NegateIdentity→`fd.new_constant`+三步簿记;NotDistribute→`op_set_input`+`op_insert_input`。E2E@55783411:ABORTED 10→**0**、raw 命名 1077→**16**、defects 2→**0**(getparameter/glob_word else 缺陷证实为簿记腐蚀下游,随修复消失)、skeleton 3528→**2134**(优于 2d78b5af 健康基线 2881);cargo test 失败集=已知 17 零新增。Cross-Review: PENDING(机制 C,待派);审计证据=/tmp/w-newvarnode-curl-probe.err(49 条悬空);coreaction.rs:1371(ActionCse)核实未注册非生产者不动 |
| w-carry(转向攻坚)+w-nonconverge2(交付) | `GETPARAM-EMPTYELSE-0001`→`HERITAGE-GUARD-LATTICE-0001` | 修复已双侧实证(2e226d95,**阻塞未入 master**) | `src/heritage.rs`(guard_query_properties 补 database.cc:1271-1276 global-scope 尾)+`src/merge.rs`(eliminate_intersect 五守卫 merge.cc:543-562) | 根因:guard INDIRECT 输出无 addrforce→保护格 pass=2 deadcode 全灭→全语料 write-only 全局赋值丢失。修复效果:getparameter defects=0、全语料 2866→**2166**/defects **0**。**阻塞①**:4 函数(glob_range/main/my_get_token/next_url)"Forced merge caused intersection" panic(120/124 受影响)=第一代 vs 二代保护格代际分歧(my_get_token MULTIEQUAL@0x37b4 descend 5v4 双侧实证);工具=RUGRA_MERGE_DIAG=1+oracle 探桩(/tmp/w-nonconverge2-ore/cpp-dbg);阻塞②getparameter uVar11 重复声明;阻塞③stack:delay=1/1 vs Stack=2 冲突。w-carry 已转向攻坚 |
| 已集成(4e78a2fb) | `HTTPD-TFALIGN-PANIC-0001` | **FIXED**(Cross-Review: PENDING) | `src/type_system/typefactory.rs`(已落库) | 根因=shared_default() 工厂停留 raw 态(align_map 空);Ghidra 保证 Architecture::decode 尾 setupSizes 的 guard(type.cc:3164-3165 setDefaultAlignmentMap)使 LowlevelError 仅 raw→decode 间可达;修复=get_or_init 内同 guard。httpd 4/29→**28/29 输出 0 panic**,首个可比基线 2148/4/0;curl 2911/1/0 零劣化;测试失败集逐名一致。 |
| 已集成(484f752c..863c0741) | `HTTPD-STRCASECMP-NONCONVERGE-0001` | **FIXED**(**httpd 29/29 达成**,0 panic/timeout;Cross-Review: PENDING) | `src/coreaction.rs`(root 裁定接受:regB 租约属久亡会话,文件实际空闲) | 三层根因(gdb+BSTRACE):僵尸决策块(CBRANCH<2出边,Rugra独有状态,违反 Ghidra 隐式契约 coreaction.cc:3544)+DeterminedBranch 空转 reset(58877 次)+if_no_exit 每轮 negate 喂 count(24.6万轮);修复=size_out<2 守卫+cc:3546 count 恢复;死循环→5 轮;附带 ap_pregsub 退化语句恢复;26/28 函数字节相同;curl 3068/0/0 精确不变 |
| 已集成(7786a8e0,Cross-Review: PENDING) | `FUNCDATA-ZOMBIE-DECISION-ORIGIN-0001` | **FIXED**(真因=lifter 零-op 目标→边丢弃;Ghidra flow 合成块边界语义;僵尸 260→0;ap_strcasecmp 垃圾常量消失+do-while 骨架恢复;curl 字节级不变) | `src/funcdata.rs`(已落库) | 残余:httpd 2372/5/0(+160=正确 CFG 暴露 lifter 空投影,绑 X86LIFT-ZEROOP-ARMS) |
| 已集成(0c80ad5c..56f77d1b,Cross-Review: PENDING) | `X86LIFT-FLAG-PCODE`+`ZEROOP-ARMS` | **DONE**(c1-c5 五笔;语义 oracle=x86-64.sla 直通 dump 双侧 op-for-op MATCH;curl 字节不变) | `src/disasm/x86_lift.rs`(已落库) | 重大发现:cmp/test 旧 flag 偏移全错(ZF 写 0x201=F1 级);js/jns 此前零 op 丢控制流;cmovcc/setcc/sbb/adc/pop/movzx/movsx/REX 低字节别名全补;CARRY 存活最终 IR(golden CARRY8 函数 1×INT_CARRY+22 flag 写 vs 旧 0) |
| unassigned | `ZF-LOOP-OSCILL-0001` | OPEN(iced 移交,被 c5 掩盖未根治) | 待认领(规则域定位) | ap_strcasestr >5min 不收敛:ZF 写链×jcc 读×循环头触发 rule_repeatapply 震荡;oracle 同 IR 收敛 ⇒ Rugra 规则缺陷 |
| 已集成(01b2bc95) | `X86LIFT-PUSH88-0001` | **DONE**(push 33/33 形态 op-for-op vs sla dump;万能尾 INT_SUB(RSP)→STORE;SIB scale=1 也发射;rip displacement 已解析坑规避;curl 字节不变) | `src/disasm/x86_lift.rs`(已落库) | httpd 2325→2546(+221=暴露既有共享管线缺口,非 lifter 缺陷;defects 7→5 改善) |
| 已集成(0e7acc89) | `HTTPD-STACKSLOT-FOLD-0001` | **FIXED**(根因改判:httpd runner 从未 attach Architecture→RuleStore/LoadVarnode 的 getArch 解引用走 None miss;curl 自带 worker arch 故无泄漏;一行 runner 修复) | `examples/httpd_decompile.rs`(已落库) | httpd 2546→**2230**(−316,逐函数只降不升)、in_RSP 行 148→**0**;curl 3089/0/0 字节一致 |
| 已集成(d02df6f5,证伪+改判+修复) | `PRINTC-LABEL-LEDGER-DISCOVERY-0001` | **FIXED**(真因=discovery 只记 emit_block_ops;emit_flow_basic 补记录器+尾扫描收窄 needs_anchor;自指标 curl 22→3/httpd 13→1;31 函数标签回归 oracle 放置;fixture MATCH) | `src/printc.rs`(已落库) | 残余4处=上游:collapse 形态分歧(getparameter 0x4049/glob_set×2)+shared-tail 族(httpd),登记 BLOCKSTRUCT 上游 |
| w-cmov(新派) | `RULE-PROPCOPY-ADDRTIED-0001` | OPEN(w-w4 定位:6 空 if=CMOVcc 惯用法;phi(X,f(X)) 分支内实例化缺失;RulePropagateCopy 已被双侧 fixture 排除) | `src/{condexe,ruleaction,heritage,merge}.rs`(w-cmov) | fixture=rule_propcopy_1204(b94f0b8b 扩展,11 cases MATCH);P16c 计数 277 待降;验收=空 if 恢复体 |
| unassigned | `BLOCKSTRUCT-COLLAPSE-RESIDUAL-0001` | OPEN(scopeb 残差) | 待认领(结构域) | Rugra 树目标恰为 next-in-flow 而 golden 无该 goto 的 4 处形态分歧 |(原scopeBreak TODO证伪改判) | OPEN(真因=discovery_block_starts 只记 FLAT 路径;结构化路径目标块无记录→锚点误fire+标签错位+目标自身标签被抑) | `src/printc.rs`(w-scopeb 独占)+blockaction/block(原) | 修复=emit_flow_basic 加同款记录器;双侧 fixture 含证伪记录;`goto X; X:` curl 1→0 |
 | OPEN(ndo 残差) | 待认领(`src/blockaction.rs`;已释放) | `goto X; X:` 自指标(next_url/glob_word/getparameter 新暴露)=scopeBreak 的 goto_type 缺失(block.cc:2866-2874) |
| 已集成(9b5056fb+e9f7b8c0) | `FUNCDATA-PUSHMULTIEQUALS-0001` | **FIXED**(C1 闭环:funcdata_block.cc:84-171 完整移植;warning×3→0、滞留op→0、curl -4 纯警告行清除;fixture 3/3 MATCH) | `src/funcdata.rs`(已落库) | httpd +1=ap_fini 声明重排已解释 |:httpd 实测触发 warning×3+滞留 op) | 待认领(`src/funcdata.rs`) | push_multiequals(pre-existing RUGRA-GAP)完整移植(funcdata_block.cc 对应段);触发点=ap_fini_vhost_config×2/ap_pregsub×1;R-DONOTHING 报告 §7 |
| unassigned | `DEGRADE-POLICY-UNIFY-0001` | OPEN(C2,裁决项) | 无(root 裁决) | cc:311/cc:330 LowlevelError 降级与仓内 panic/abort 策略不一致;统一策略裁决+补 TODO ID |
| unassigned | `DONOTHING-C3-SPLICE-FIXTURE-0001` | OPEN(C3) | 待认领(fixture) | splice-phi 分支(cc:283-287)与 unblockedMulti 冗余非空路径缺独立 oracle fixture(现仅语料覆盖) |
| unassigned | `HTTPD-EMPTYELSE-LIVEARM-0001` | OPEN(hdefects 残差,3 defects) | 待认领(ruleaction 域) | else 臂含活 PIECE/COPY(非 do-nothing);oracle 同区域=复合条件链;数据流/implied 时机分歧;探桩复用路径=/tmp/rugra-reports/w-hdef-2026-08-30.md |
| unassigned | `FUNCDATA-CANONICAL-ARCH-0001` | OPEN(stackfold 移交,已原型验证) | 待认领(`src/funcdata.rs`+subflow 测试一行) | Funcdata::new 自动绑 canonical Architecture(oracle 不变式 glb=scope->getArch());原型 E2E 数字相同;翻转 subflow::tests::test_split_datatype_constructs 的过时 arch-less 断言 | 待认领(`src/heritage.rs`空间基域;hdefects 完成后接) | `*RSP-k` STORE 未重索引进 stack 空间→以指针表达式打印(in_RSP 行 52→150);Ghidra 经 spacebase/heritage 折叠+死码消 push/pop 序言 |
| 已集成(e673d88a,复核=reviewer-callin0 在途) | `COREACTION-CALLIN0-CLOBBER-0001` | **FIXED**(FUN_0 63→0;ActionDeadCode 恢复 cc:3846 首操作数全量 consume 保证;防御性=inject 路径无 spec 的成因绑 CALLSPEC-DRIVER-0001;fixture 5/5 MATCH) | `src/coreaction.rs`(已落库) | httpd 2278→2279(+1 调用目标恢复) |
| unassigned(w-scopefix 新派) | `FUNCDATA-SCOPELOCALOVERFLOW-0001` | OPEN(registry9 移交:debug 构建 panic) | 待认领(`src/funcdata.rs`) | scope_local_find_overlap(funcdata.rs:173)debug 构建 i32 符号扩展溢出 panic(release 门禁掩盖);按 Ghidra findOverlap 语义修复 |
| unassigned | `CALLSPEC-DRIVER-0001` | OPEN(1:1 根因,inject 路径补 flow-time callspec 锚定) | 待认领(`src/funcdata.rs`/`src/flow.rs`;pushme 完成后接) | Ghidra flow.cc:683-690 setupCallSpecs 无条件挂 spec+换 in(0) 为 fspec 注解;Rugra inject 出生带 flag 无对象 |
| sb-httpd-agent@wt/sb-httpd(2026-09-22) | `CALLSPEC-DRIVER-0002` | **PARTIAL-FIXED**(门条件已落;解除门条件待尾部移植) | `src/funcdata.rs`+`docs/api/funcdata.md`(已落库);解除需 `src/heritage.rs`/`src/coreaction.rs`(ActionCopyPropagation 缺失) | 79bb0f6 把锚定三步(flow.cc:683-686)移植到 inject 边界但跳过原子尾部(flow.cc:688-694 queryCall/checkForFlowModification);无模型载体的 driver(httpd 裸 Architecture::new)注册半初始化 spec 进 qlst→heritage.cc:362-364 极性翻转激活逐 call 守卫而 FuncLink/ActiveParam 无模型挂实参→httpd skeleton 2344→3576(+379 条 x=x 死拷贝链;curl 不受影响=最终路径走 setup_call_specs+driver link_call_specs 完成尾部)。修复=qlst 注册门 `fd.funcp.has_model()`(annotation swap 无条件保留,CALLSPEC-DRIVER-0001 意图不回退);实测 httpd 2343/0/0,curl 3711/0/0。根因报告=/dev/shm/rugra-tests/sb-httpd/ROOT_CAUSE.md(易失,要点已录入本行与 docs/api/funcdata.md)。解除门条件=(a)queryCall/checkForFlowModification 尾部移植到 driver 边界(driver callee 表+defaultfp 模型,同 curl link_call_specs 架构)+(b)~~补 ActionCopyPropagation~~ **前提纠错(2026-09-22 sb-copyprop lane,docs/alignment_docs/COPYPROP_LANE_VERDICT_1204.md)**:锁定 oracle 12.0.4 无 ActionCopyPropagation/RuleCopyPropagate(全仓 grep+16030 commits pickaxe 零匹配;coreaction.cc:5510-5511 实为 oppool1 ActionPool 块),universal 树无此槽位,门条件(b)按字面不可满足;守卫重载拷贝存活的真修域=merge 相 Cover 模型(见 MERGE-COPYNOISE-DIFFHIGH-0001)+driver 尾部(a)。write-set 更新:解除项仅剩 `src/heritage.rs` driver 边界。**(2026-09-22 勘误,sb-impliedfold CE)**:"无 copyprop 规则"系错名搜索——oracle 实有 RulePropagateCopy(ruleaction.cc:3926,注册 coreaction.cc:5566),Rugra 已实现已注册(ruleaction.rs:205-218/action.rs:1649),差分欠账见 `RULE-PROPAGATECOPY-0001`;ActionCopyPropagation/RuleCopyPropagate 两名字确实零匹配,本行门条件(b)结论不变 |
| 已集成(6c533eba) | `HTTPD-URAM-SYMBOLIZE-0001` | **FIXED**(uRam 调用 87→0;根因=前端导入:.dynsym UND→真实目标为无名 .plt.sec thunk;补 ElfPltImports 解析+.plt.got;82 thunk+5 FUN_ 名与 golden 拼写逐一相同) | `src/debugproto.rs`(已落库) | httpd 2278→2274;残差=main 的 FUN_fff…90 间接调用族+5 处数据侧 uRam(DAT_ 标签族,另案) | 待认领(printc/varmap 域) | httpd 调用打印 uRam<addr>() 而非函数名 | 待认领(`src/coreaction.rs`;f23 完成后接) | "analysis"组 STORE 压力下把 CALL coderef 换 const:0(FUN_0 症状);前缀二分 DECOMPILE prefix 18→19;复现工具 examples/x86push_dbg.rs | | OPEN(爆炸半径大,独立任务) | 待认领(`src/disasm/x86_lift.rs`) | push 仍零-op(push88 语义,1385 处,影响全部函数序言) |
| unassigned | `X86LIFT-SHIFTS-FLAGS-0001` | OPEN(后续 family) | 待认领(同上) | shl/shr/sar 的 flag 语义(~38 op/条)未实现 |
| w-iced(接) | `X86LIFT-ZEROOP-ARMS-0001` | OPEN(P1,已路由 iced) | `src/disasm/x86_lift.rs`(w-iced 租约) | movzx/movsx/pop 等零-op 指令臂补齐(对照 ia.sinc);消解 httpd +160 空投影;golden 在这些地址设 LAB 标签为证 | 僵尸决策块成因=上游断边路径跳过 op-destroy(rounds 27-30 逐轮退化 o2→o1→o0);oracle 由 branchRemoveInternal(funcdata_block.cc:203-204)在 sizeOut==2 时先销毁 cbranch;ap_strcasecmp_match 输出数据流仍退化(垃圾常量 0xbaadef)属其下游 | | OPEN(被掩盖的既有缺陷) | 待认领(blockaction/collapse 域;现租约=maingo) | ap_strcasecmp_match(第29函数)collapse restart 不收敛:orderLoopBodies→finalize 3→1 零进展无限重复(15s 112,865 次);证据=/tmp/w-httpd-fix1.err |
| unassigned | `HTTPD-DEFECTS-QUAD-0001` | OPEN | 待认领 | httpd 4 defects 函数(ap_fini_vhost_config/ap_strcmp_match/ap_getparents/ap_make_dirstr_prefix);首个可比基线后暴露 |
| w-maingo(新派) | `MAIN-RC2-BLOCKGOTO-WRAPPED-0001` | **P0**(w-main2 探针实测:helpf 0→8/8,main 体 251→736 行) | `src/blockaction.rs`(try_rule_goto)+`src/block.rs`(BlockGoto)+fixture;printc.rs 侧(emit_block_goto 消费)经 root 与 GETPARAM-EMPTYELSE 租约协调 | Ghidra `BlockGoto : BlockGraph`(block.hh:546)持有被包装块组件+删边前捕获 gototarget(block.cc:1702-1713),发射 body+goto 完整(printc.cc:2766-2771)。Rugra 构造 `BlockGoto{goto_target:None}` 无 wrapped 字段、get_ops 返回空、goto_prints 硬编码 false→main 14 个包装块(helpf×8/bVar3=false/break)全部蒸发。证据=/tmp/rugra-reports/w-main2-2026-08-29.md |
| unassigned | `MAIN-RC3-STRUCTURED-EMIT-0001` | P0(探针:if 14→55) | `src/block.rs`(BlockIf::get_ops 丢 if/else body)+printc.rs 翻门(依赖 RC2) | whiledo/if 体发射:printc.rs:4285 body_is_dead=true 硬编码 flatten(=identify3 residual 翻门条件);BlockIf::get_ops(block.rs:5119)只返回 condition 丢弃 body→50×curl_easy_setopt 级联+嵌套 if 丢失;oracle cc:3061-3063/2994-2995 structured 递归 |
| unassigned | `MAIN-RC4-DOWHILE-TRACE-0001` | P1(main 0 命中 vs golden 5 do-while) | TraceDAG 回边分类/吸收顺序(需 oracle 运行时定缝;RUGRA_BS_TRACE 已有) | try_rule_do_while 与 cc:1555-1574 逐条对齐无缺陷,但 argv 循环头被 if_else 消费成 `if(1){}else{}`,自环复合体永不出现 |
| 已闭环 | `BATCH1-CROSSREVIEW`(五笔) | **全部 APPROVE**(R-BATCH1:5/5,零REJECT;六消费者逐一/全调用点路由/收敛数学等价核毕) | — | #1 PULLSUB空间/#2双符号尾/#3 httpd守卫(定性=防御性,正当性锚在良构路径逐行+成因TODO)/#4 cc2076门/#5 virtual_list;建议登记 O-2/O-5b 如下 |
| 已闭环(642e6156..9d419f3a) | `PRINTC-STRUCTEMIT-RESIDUALS-0001`(主项) | **DONE**(numbering 1→0;双侧 fixture MATCH;WHILEIF-FOLD 同修) | printc/prettyprint(已落库) | 残差诊断外移:FORHDR→coreaction(switchb 租约);C3 三件→ruleaction`(0-…)`负号形/heritage cast 形 |
| w-minis(新派) | `RULEACTION-NEGATION-FORM-0001`+`VARNODE-GETUSEPOINT-FREELEG-0001` | OPEN(C3 残差+O-2 批) | `src/ruleaction.rs`+`src/varnode.rs`(均空闲) | ①`(0 - x)` vs `-x` 负号形(w-printc2 诊断,ruleaction 域);②get_use_point 自由腿 Address(0) vs varnode.cc:701 fd.getAddress()+-1 |
| unassigned | `PRINTC-WHILEDO-MODS-STACK-0001` | OPEN(O-1,P2,复核发现) | 待认领(`src/printc.rs`) | emit_structured_whiledo 缺 cc:3012-3013/3060 mods 栈操作(db090828 Evidence 对 whiledo 过度声明,仅 for 成立);当前零触发(CPUI_BRANCH 双侧无条件跳过)但属缺失守卫;rc3 bundle 其余 4/4 APPROVE,R-RC3 报告含定性(prettyprint 后处理=GLUE 兜底,终极路径=退役) |
| w-postw0(新派) | `POSTFIX-RETIRE-0001`(W0 波) | OPEN(路线图=/tmp/rugra-reports/w-postfix-2026-08-30.md;架构论证=oracle 无任何文本后处理) | `src/prettyprint.rs`+`src/printc.rs`(W0 波限死代码+伪造注解改正) | W0=死代码清除(~700 行,5 死函数+16 伪造 Ghidra 注解[EmitNoMarkup 无这些方法,机制 D 红旗]改正/删除;字节级零风险);后续 W1 计数器→W2 零突变退役→…→WT(~2700 行可删);风险=跨 pass 掩蔽须尾部先删/挂载点①②同批拆 |
| 已集成(2cae9654) | `POSTFIX-INSTRUMENT-0001`(W1) | **DONE**(33-pass 计数器;21/33 双语料双轮全零;A队列9确认) | prettyprint(已落库) | — |
| 已集成(0d070aea..9777c407) | `POSTFIX-RETIRE-0001`(W2) | **DONE**(A队列9 pass 全退役,净删~534行;九刀字节一致;掩蔽零揭示) | prettyprint(已落库) | B队列(P13/P14/P24/P25/P26等)待上游;C队列(B3/B4/P12/P15/B1);P16c→W4 | 计数器插桩(每 pass 突变计数,退役判定依据) |
| unassigned | `PRINTC-VOIDCALL-0001` | OPEN(P13 正解) | 待认领 | 硬编码 13 个 libc 名的 void-call 判定→FuncProto void 返回正解 |
| unassigned | `DATABASE-RAM-FINALSCOPE0-0001` | OPEN(lminors ②,标准分析不可达) | 待认领(`src/database.rs`/heritage 协同) | oracle database.cc:1263-1281 分支3(地址不在 global 发现范围)仅 getProperty;Rugra 已有 global_scope_id+rangetree 基础设施,补齐形状=分支(3)前查 global 范围;完整 oracle 行号+形状见 /tmp/rugra-reports/w-lminors-2026-08-30.md §② |
| unassigned | `VARNODE-GETUSEPOINT-FREELEG-0001` | OPEN(O-2) | 待认领(`src/varnode.rs`) | get_use_point 自由腿返回 Address(0) vs oracle varnode.cc:701 fd.getAddress()+-1;未登记潜在分歧 |
| unassigned | `BLOCKACTION-CASEFALLTHRU-INTERLEAVE-0001` | OPEN(O-5b,预存) | 待认领(`src/blockaction.rs`) | cc:1844 caseFallthru per-block 交错 vs Rugra 批处理;预存未登记 |
| w-printc2(新派) | `PRINTC-STRUCTEMIT-RESIDUALS-0001`(簇) | OPEN(w-rc3 登记) | `src/printc.rs`(已释放) | ①MAIN-IVAR4-DUP(numbering+1 源根)②MAIN-FORHDR ③WHILEIF-FOLD-PREFIX ④C3 残差三件(cast链/piece-split/typeop push路由)⑤C3-HTTPD-FLAGS |
| 已闭环(诊断改判+fixture MATCH) | `FLOW-339E-OVERLAP-HLT-0001` | **DONE**(根因改判:flow 已对齐;缺块=known-noreturn 数据差,Rugra 149 块=对全分析 golden 正确;fixture oracle 无 Java 分析器才落穿 150 块;双侧 3 块图逐字节同) | fixture 已登记 | 门控 RUGRA_ORACLE_FIXTURE_DATA 供 fixture 对齐 |
| 已集成(b3dd0c3c+4829107c,Cross-Review: PENDING) | `BLOCKACTION-DOWHILE-ABSORB-0001` | **FIXED**(**main do_while 0→5=golden**;126/126 规则事件双侧对齐;真缝=cat 后 force 步缺失(forceOutputNum 复合自环+forceFalseEdge,block.cc:880-889)非 goto 分类;fixture MATCH) | `src/blockaction.rs`(已落库) | 残差:①printc 渲染 do{4 vs 6(嵌套 DoWhile 发射平铺,printc 域);②oracle 二轮 collapseAll(管线中段重结构化,登记) |
| w-f23(新派) | `COREACTION-ISOPIDENTICAL-TYPEDEF-0001`(F2)+`CASTINPUT-ARMORDER-0001`(F3) | OPEN(reviewer-switchb 必办) | `src/coreaction.rs`(已释放) | F2:is_op_identical(coreaction.rs:5038)缺 cc:2476-2479 typedef 链剥离——typedef 输入下 force 误真可构造;F3:castInput else-if 臂序(:4745 合并了两层嵌套)"def=CAST 非 implied+constant"误入常量臂(当前不可达,residual 后分叉);另 Q1-Q10 定性意见见 R-SWITCHB 报告 §4 |
| 已闭环 | `SWITCHB-PHASEB-CROSSREVIEW` | **4/4 APPROVE**(R-SWITCHB:opUndoPtradd/preflight/arithmeticOutputStandard/castOutput+castInput;fixture 17/17 独立复跑;Q1-Q10 定性意见) | — | Phase B 正式放行 |
| unassigned | `PRINTC-NESTED-DOWHILE-EMIT-0001` | OPEN(残差①) | 待认领(`src/printc.rs`) | main 渲染 `do {` 4 vs oracle 6:两个嵌套 DoWhile 复合体被发射层平铺成单次迭代(规则级已对齐) |
| unassigned | `PIPELINE-SECOND-COLLAPSE-0001` | OPEN(残差②,观察) | 无(登记) | oracle 对 main 有第二轮 collapseAll(146 块重启,事件 127-250)=管线中段 CFG 变更触发 structureReset 的重结构化 | | OPEN(do-while 精确位移) | `src/blockaction.rs`(rc4 释放) | w-339e 续缝:事件对齐前缀 53(#15 infloop@147 双侧同位);**首分歧 #54:oracle goto@67 vs Rugra if_goto@67**,其后 oracle #56 dowhile@67/#60 dowhile@70 吸收链全缺;门控下 main 规则计数 53cat/29properif/1infloop/0dowhile(golden 5);续缝命令与证据=/tmp/rugra-reports/w-339e-2026-08-30.md;工具=/tmp/w-rc4-ore+RUGRA_BS_VISIT |
 | OPEN(P1,do-while 最终阻塞) | 待认领(`src/funcdata.rs`/`src/flow.rs`;zombie 完成后接) | main(0x25a0,3531B)/_start(0x3374) 重叠区的 `hlt`:oracle flow-follow 越 noreturn __libc_start_main 落入(oracle 150 vs Rugra 149 块);修复后用 w-rc4 固化的双侧 visit-trace 对拍法可快速续缝 do-while 吸收链(oracle 事件 53-80) |
| unassigned | `MAIN-RC5-LOOPCOND-VARMAP-0001` | P1 | varmap/heritage 域 | 循环条件错接:`extraout_var_00 != 0` 应为 `iVar17 < argc-1` |
| 已集成+复核中 | `NODEJOIN-F2/F3/F4/F5-0001` | **FIXED**(798466b1..92ad5358;双侧 fixture 9/9 MATCH;Cross-Review=reviewer-njf234 在途) | `src/coreaction.rs`(已落库) | F4 findDups 六门序/F3 same-cond 全 join/F2 execute 四步+mergeneed 有序/F5 动态 size;E2E 2911→**2884**(next_url 150→134/getparameter 678→669/my_get_token 57→55,余零变化,全向 golden);移交项 action.rs fixture 改共享 written 条件已修 | ConditionalJoin::execute 四步仅做1(nodeJoinCreateBlock);setupMultiequals/moveCbranch/cutDownMultiequalities(blockaction.cc:2098-2101)全缺,cbranch未迁入join块 |
| 已集成+APPROVED | `GLOBWORD-C5-GLOBAL-TYPEFLOW-0001`+`GETPARAM-EMPTYELSE→LATTICE` | **DONE**(1a5504c1+a4694407;R-LATTICE-CROSSREVIEW **APPROVE**) | debugproto/ruleaction/heritage(已落库) | glob_word 字段化(`->size/->literal`);glob_set/range/url 的 subscript 形态绑 PRINTC-ARRAYDEREF-SUBSCRIPT(rc3 租约);六条 MINOR 跟进项见复核报告 |
| w-rc3(接) | `PRINTC-ARRAYDEREF-SUBSCRIPT-0001` | OPEN(P2,w-typeflow 提案) | `src/printc.rs`(w-rc3 租约) | opStore/opLoad 的 checkArrayDeref→print_store_value mod 设定链(printc.cc:500-515/353-367);症状=`->literal + size/2 =` 应为 `literal[...]=` |
| unassigned | `MAIN-RC5-LOOPCOND-VARMAP-0001` | OPEN(P1) | 待认领(varmap 域) | 循环条件错接:`extraout_var_00 != 0` 应为 `iVar17 < argc-1` |
| unassigned | `GLOBWORD-C1-PUSH-TYPE-STRUCTURAL-0001` | OPEN(w-globword提案) | 待认领(`src/printc.rs`) | push_type 按名渲染改结构化(全匿名化后可收紧;26 返回头保持) |
| unassigned | `GLOBWORD-C2-LOOP-LIFT-0001` | OPEN(w-globword提案) | 待认领(`src/blockaction.rs`) | my_get_line 外层 do-while 整环丢失(语义级)+glob_word 尾块不提升+死 goto;oracle 行号见 w-globword 报告 B.3 |
| w-rc3(接print侧) | `GLOBWORD-C3-CARRY-INJECT-0001` | 根因已双侧证伪定位=**printc发射缺INT_CARRY/SCARRY/SBORROW分支**(提升/折叠双侧逐op一致;RIP偏移0x288修正已集成319862a8) | `src/printc.rs`(w-rc3租约) | 规格=docs/alignment_docs/CARRY-PRINT-ROOTCAUSE-2026-08-30.md §3;判据=泄漏5→0+CARRY1出现+3108/0/0;残差=CARRY参数piece-split(heritage)+CAST链(coreaction) |
| unassigned | `X86LIFT-FLAG-PCODE-0001` | OPEN(iced路径缺口) | 待认领(`src/disasm/x86_lift.rs`) | iced路径(httpd语料)add/sub/logic无flag pcode、sbb/adc/cmovcc/setcc未实现;httpd golden有CARRY1(:39307)/CARRY8(:35777);须按ia.sinc全量补(档案CARRY-PRINT §4) |
| unassigned | `GLOBWORD-C4-INTNOT-TOKEN-0001` | OPEN(w-globword提案) | 待认领(`src/printc.rs`) | INT_NOT token 序:`0xfefefeff~` 非法 C 形,oracle `~*puVar4` |
| unassigned | `GLOBWORD-C5-GLOBAL-TYPEFLOW-0001` | OPEN(w-globword提案) | 待认领(类型流域) | glob_expand 的 URLGlob 字段不传播→`*(int*)(glob_expand+0x128)` 原始偏移 |
| 已闭环(master `2da6a86b`) | `REGISTRY-CHECKPOINT-ADVANCE-0001`+`ORACLE-REGISTRY-IMPACT-CONTINUITY-0001` | **DONE**(w-registry5+root) | tools 钉+生成物 | 全链绿:generate --check 0/lint **0 findings**/select --strict 0/reconcile 幂等(baseline 9639→current 10467/lineages 489/introduced 883/tombstones 55);分支线锚配方(no-ff,first-parent 411)实战固化;重跑序列见 /tmp/rugra-reports/w-registry5-2026-08-29.md | CONTINUITY_CHECKPOINT 治理推进:10ea407d..2889e048 窗口 src delta 的 transition/tombstone/introduced 逐条审查后改钉+reconcile+再生;验收=lint exit 0 三链全绿 |
| unassigned | `SUBFLOAT-TRANSFORM-RESIDUAL-0001` | OPEN(P-FLOW-1 复核发现) | 待认领(`src/subflow.rs`) | RuleSubfloatConvert 非常量路径 defer 为保守降级(Ghidra transform 链只新建 Varnode 不 retype);完整移植 TransformManager 语义后撤销 defer;证据=R-MYPROG-CROSSREVIEW 报告第 2 节 |
| unassigned | `TRI4-MYPROGRESS-SETTLE-0001` | PARTIAL(666afd3b 已修 settle 契约) | — | temp 类型经 TypeFactory 规范化已落地;残差=myprogress 的 DWARF 局部名(format/line/outline)三层环境缺口(DW_OP_fbreg 解析/导入/消费)+ in_RSP/Var8/extraout 多余声明(funcdata/heritage 域) |
| w-myprog-cluster(已集成) | `VARMAP-GLOBRANGE-DUPDECL-0001` | **FIXED**(65d72291;根因=prettyprint 两 GLUE pass:if 条件续行误判签名+uVar 无词界;非 varmap 计数器) | `src/prettyprint.rs`(跨lease最小化已声明) | varmap_dupdecl_1204 双侧 MATCH;numbering 1→0 |
| unassigned(P0) | `NONCONVERGE-GETPARAM-MATCHURL-0001` | OPEN(root 亲证,descend 修复暴露) | 待认领(嫌疑 `src/merge.rs` allocate_copy_trim/trim_op_input 域+循环源待定位) | getparameter+match_url 在 55783411+ 非收敛(180s 仍未完成→watchdog TIMEOUT,无输出)。主仓构建:worker panic 于 `Merge::allocate_copy_trim→op_set_input→add_descend`"Free varnode has multiple descendants"(varnode.rs:2500=Ghidra varnode.cc:336 自身不变量);诊断构建(同源码!):不 panic 但 ~120s 内 19+ 轮完整管线重启/oppool1×77,ops 稳定 1246,mid-pass 被杀——**同一源码两个构建行为分歧(panic vs 不收敛)=codegen/堆布局敏感的迭代序依赖,红旗单独登记**。golden 两函数均无 "Exceeded maximum restarts"/"Type propagation" 警告=Ghidra 干净收敛;Rugra 的 maxrestarts cap 已在位(action.rs:1103 镜像 cc:565-575,universal=1)。诊断工具:worktree /tmp/rugra-freediag(census 钩子 6150cdc8+freevn 探针),`RUGRA_ACTION_CENSUS=1`;证据=/dev/shm/rugra-census2.err+rugra-bt.stderr.log。验收:两函数在 30s 内收敛或按 Ghidra 语义停机+输出与 golden 对比;panic 消除且不引入绕过。 |
| unassigned | `CODEGEN-DIVERGENCE-REDFLAG-0001` | OPEN(登记) | 无(调查项) | 同源码两构建(主仓 target vs /tmp/rugra-freediag-target)对 getparameter 确定性地产 panic vs 不收敛——指向 Arc 指针作键的有序容器/迭代序依赖(堆布局=codegen 相关)。凡"测试偶发/不可复现"类问题先对照此项。 |
| w-anondecl | `PTRSUB-TYPED-DECL-RESIDUAL-0001` | writer | `src/printc.rs`, `docs/api/printc.md`, `tests/oracle/printc_anonymous_pointer_decl_1204.{cc,rs,metadata.json}`, `tools/run_printc_anonymous_pointer_decl_oracle.sh`, branch `agent/anondecl-0001` | 匿名 PTR/ARRAY/CODE 声明 type-token 修复（printc.cc:143-166 buildTypeStack + :264-303 pushTypeStart/genericTypeName）；验收=六函数 typeless decl 清零 + gcc 诊断不劣化 + defects/numbering=0 |
| w-newvarnode | `FUNCDATA-NEWVARNODE-SYMBOLTAIL-0001` | writer | `src/{funcdata,varnode,database}.rs`, 对应 `docs/api`, fixture 三件套+runner+registry, branch `agent/newvarnode-tail` | newVarnode symbol tail 完整移植（create→assignHigh→lane check→localmap queryProperties→setSymbolProperties/setFlags）；验收=双侧 fixture datatype/mapentry/high symbol/flags/异常 MATCH |
| w-identify | `BLOCKSTRUCT-IDENTIFY-BOUNDARY-0001` | writer | `src/{block,blockaction}.rs`, `docs/api/{block,blockaction}.md`, `tests/oracle/block_identify_internal_1204.*`+runner+registry, branch `agent/identify-boundary` | identifyInternal 真实 parent/成对边重写/组件顺序/无 f_dead（block.cc:940-963）；验收=双侧 fixture 完整突变 MATCH + goto fixture 110 行 diff→0 |
| w-registry | `ORACLE-REGISTRY-IMPACT-CONTINUITY-0001` | writer | `tools/{generate_function_ledger,oracle_registry,select_fixtures}.py`, `tests/oracle/fixture_registry.json`, `docs/alignment_audit/{FUNCTION_LEDGER.json,FUNCTION_MAP.md,FUNCTION_PROTOCOL_MAP.json,FUNCTION_DEPENDENCY_DAG.json,FUNCTION_ID_CONTINUITY.json}`, `tools/README.md`, branch `agent/registry-continuity-w3` | 账本/registry 连续性重建：transition/tombstone/introduced 人工审核→一次性重建四生成物→ledger check/lint/selector 全 exit 0 |
| w-switchfix | `PTRSUB-SWITCH-CAST-RESIDUAL-0001`(phase A) | writer(fixture-only) | `tests/oracle/ptrsub_switch_cast_1204.{cc,rs,metadata.json}`, `tools/run_ptrsub_switch_cast_oracle.sh`, branch `agent/switchcast-fixture` | 诊断 fixture：锁定同一 IR/CFG/type state 下 glob_set switch 表达式树双侧观察，定位最早 cast-churn 分叉；实现 write-set 由 fixture 结论另行认领 |
| r-main | `TRI4-MAIN-RESID-0001` | reader | 无（只读+报告） | main() 228 vs 476 行：最早 IR/CFG 分叉、双侧完整函数、最小修复 write-set 提案 |
| r-getparam | `TRI4-GETPARAM-CALLPROTO-0001` | reader | 无 | getparameter callspec/prototype 最早分叉 + oracle fixture 设计 |
| r-parseconfig | `TRI4-PARSECONFIG-STRUCT-0001` | reader | 无 | parseconfig finalize 41→2 塌缩：首个错误组合/父子关系 + 最小 block write-set |
| r-f2s-nexturl | `TRI4-FILE2STRING-IR-0001` + `TRI4-NEXTURL-STATE-0001` | reader | 无 | file2string 缺失语句的最早 Action/Rule 归属 + next_url SSA/merge/varmap 最早状态分叉 |
| r-plt-httpd | `PLTSTUB-WARNLOSS-0001` + `HTTPD-ADDDESCEND-THROW-0001` | reader | 无 | 51→27 PLT 警告丢失首叉（unknown-model+lock 生命周期）+ httpd addDescendant throw 残余 MISMATCH 清算 |

> 冲突矩阵：printc.rs=w-anondecl 独占；funcdata/varnode/database=w-newvarnode；block/blockaction=w-identify；
> heritage/coreaction 本 wave 无人写（留给补位 REGA/REGB）；examples/* 本 wave 冻结（root 集成时统一处理）。
> reader 一律在自有 worktree 构建（`CARGO_TARGET_DIR` 专属），E2E 输出只写 `/tmp/<agent>-*`，禁触 `result/`。

## 历史 wave：`W-2026-08-27-DSH-FLEET`（2026-08-27 10:15 起接续 TRIFUNC-GAP；goal 至周五 16:00）

> 基线（root 亲测 @ master `5462eebd`）：124 函数 / defects 0 / numbering 0 / skeleton 3128 / 0 panic / 0 timeout。
> 三函数严格字节差：hugehelp **12** / progressbarinit **6** / my_fwrite **8**。
> **两个 P0 回归**：A=ed827938 后 hugehelp 字面量退化+progressbarinit `__nptr=` 赋值形丢失；B=c243cdef 后 my_fwrite 双判空塌缩永假合取。
> 编排：DSH 主 Agent（root）+ 10 并发后台子 Agent；worktree 隔离 + 独占写集租约 + `/tmp/rugra-cargo-build.lock` + 专属 CARGO_TARGET_DIR；子 Agent 交付→root 串行 cherry-pick→flock 构建→E2E→差分门禁（defects 必须 0）→更新本板。本节 root 维护。

### 2026-08-28 正式输出快照（当前权威状态；覆盖 2026-08-27 21:35 输出口径）

> 本节覆盖下方 11:05 租约快照；旧 owner 在本会话无存活进程，故不继续占用写锁。fresh baseline HEAD=`933de80b7c7526ae50f61ec75fe66f51787d8f6b`；root 当前独占 PTRSUB wave 精确 write-set，gate health=OK，oracle=`e40ed13014025f82488b1f8f7bca566894ac376b`。
> 2026-08-27 pre-PTRSUB fresh fast-release baseline（stdout sha256=`4404af6da658cc912b84070acb6354073c4649f3b266751e1be6cb81bd1bdc8e`，归档 `/tmp/rugra-de26f21c-curl.c`）：124/124、defects=0、numbering=0、skeleton=2822、0 panic、0 timeout；warning=27/51；gcc syntax=28/123。该 stdout 当时曾回流 `result/curl_cur.c`，但该路径现已由下条 formal 输出覆盖。
> pre-PTRSUB baseline 三函数级 skeleton：hugehelp=19、progressbarinit=15、my_fwrite=20；可见语义损伤包含 synthetic `ramLIT`、`curl_getenv` 错误实参和 `fwrite` 参数链断裂，不能被 defects=0 掩盖。
> root PTRSUB formal release E2E（2026-08-28，两次 stdout byte-identical，且当前 `result/curl_cur.c` 同字节；sha256=`f04dee502dc0131b27b41acb5ec412c0e413515aecf3f29e0e2532b304912a73`，2281 行/59995 bytes）：124/124、76 decompiled、0 empty/timeout/panic/worker/protocol failure；defects=0、numbering=0、skeleton=2820、warning=27/51。相对上条 baseline 的 `diff -U3` 为 12 个 grouped hunks/7 functions；`diff -U0` 为 33 个 atomic hunks、42-/42+。仅 `progressbarinit` 的 golden skeleton 15→13（字段清零恢复 `*(undefined4 *)&bar->field_0x1c = 0;`）；`main/getparameter/glob_word/glob_set/next_url/match_url` 的各自 golden skeleton 数不变，raw 类型/命名 token 发生扩散，不能写成“single-line delta”。这里的确定性结论仅覆盖 stdout，不外推 stderr 或全日志。
> gcc syntax 仍为 28/123 OK、95 FAIL，但诊断类别从 baseline `{other:1101, undeclared:75, syntax:73}` 变为 `{other:1113, undeclared:79, syntax:73}`。六个 concrete-pointer 前缀虽朝 golden 的 `pC/ppu/pc` 族移动，却发射成无类型的 `pCVar29/ppuVar16/pcVar5/pcVar7/pcVar11/pcVar10;`；该可见残差绑定 `PTRSUB-TYPED-DECL-RESIDUAL-0001`，不得用 compare 的 defects=0 掩盖。
> `cargo test --lib -- --test-threads=1` 为 1618 passed / 17 failed / 5 ignored，full suite 仍 FAIL；17 个失败名称与 2026-08-27 归档基线逐项相同，本候选新增失败=0。首个是既有 `test_normalize_branches_break_in_while_loop` CONTINUE 断言，其后 16 个是 `FFI_TEST_LOCK` 中毒后的 `PoisonError` 级联（`FUNCDATA-TESTS-FLAKY-0001`）。

| ID | P | 状态 | owner | 当前 write-set / 依赖 | 验收与证据（2026-08-28） |
|---|---|---|---|---|---|
| `GETSTR-FUNCLINK-SPACE-0001` | P0 | PARTIAL_MATCH（covered projection `MATCH`；`ActionFuncLink` overall `UNTESTED`；evidence=`this commit`） | root + `funclink_oracle_audit`（独立 scoped review） | 实际 write-set=`src/{coreaction,debugproto,fspec,funcdata,grammar,heritage}.rs`, `src/type_system/{datatype,typefactory}.rs`, 对应 8 份 `docs/api`, `examples/curl_decompile.rs`, `tests/oracle/action_funclink_input_1204.{cc,rs,metadata.json}`, `tools/run_action_funclink_input_oracle.sh`, `tests/oracle/fixture_registry.json`, `ALIGNMENT_ROADMAP.md`, 本行；只读 fixture closure=`src/varnode.rs`；先于 `GETSTR-PRINTC-STRUCTCOND-0001` | oracle=`e40ed13014025f82488b1f8f7bca566894ac376b`。`tools/run_action_funclink_input_oracle.sh` exit 0：101 records/9268 bytes/stdout sha=`6cd6ad2e…`/raw diff empty；覆盖 locked 6-register+first-stack、locked register-only、locked varargs、unlocked、完整 trial/placeholder/pass/flags/order 与 `getTrialForInputVarnode`，coarse x86 单 Architecture 投影 MATCH。生产不再合成 callspec/known-return 表，DWARF/libc 复用绑定 ProtoModel 并经 `setPieces→assignParameterStorage`；永久 ParamActive 与独立 active bool 已接通。curl sha=`66097f6d…`，canonical skeleton=2678、defects=0、numbering=0、syntax=28/123 OK；httpd sha=`a4628403…`，canonical skeleton=2188、defects=0、numbering=0、syntax=8/29 OK；GetStr 在该历史 snapshot 恢复 `strdup(value)`；当时“仅余 5 行”的说法已被 fresh 六阶段门禁取代，当前权威状态见顶部 GetStr 两行：五个可比较 stage 仍 MISMATCH、Heritage NO_ORACLE。完整 Address/overlay/cross-Architecture、generic newVarnode symbol tail、funcLinkOutput 与非标量/ModelRules 保持 residual；模块不升 L3。last_updated=2026-08-28。 |
| `GETSTR-CHAR-READFACING-0001` | P0 | PARTIAL_MATCH（最终 GetStr 字符 token/body `MATCH`；阶段状态 overall `MISMATCH`；evidence=`this commit`） | root + `getstr_char_token_audit` / `unknown_type_audit` / `getstr_pretty_path_audit`（只读）+ `getstr_char_chain_review` / `atom_metadata_review` / `ptrsub_commit_evidence_review`（独立 scoped APPROVE；debug-import 增量待 fresh review） | 实际 write-set=`src/{coreaction,debugproto,funcdata,printc,printlanguage,prettyprint,varnode,typeop}.rs`, `src/type_system/{cast,typefactory}.rs`, `docs/api/{coreaction,debugproto,funcdata,printc,printlanguage,prettyprint,varnode,typeop}.md`, `docs/api/type_system/{cast,typefactory}.md`, `examples/getstr_stage_snapshot.rs`, GetStr/Atom fixtures+runners+registry、`ALIGNMENT_ROADMAP.md`、本行；依赖=`DEBUGPROTO-DWARF-CHAR-0001`/`VARNODE-LOCALTYPE-RESOLUTION-0001`/`ACTION-INFERTYPES-DISPATCH-0001`；不得用常量值、函数名或打印后字符串替换特判 | 锁定 oracle=`e40ed13014025f82488b1f8f7bca566894ac376b`。`*value != 0` 与 `*value != '\0'` 运行布尔结果相同，但严格 token、恢复类型与逐字节输出不同。无 DWARF fixture 早已证明 standalone core char→精确 reader slot/read-facing High→`pushCharConstant`；生产首叉后来定位为 `DebugPrototypeDatabase` 把 `name=char,size=1,DW_ATE_signed_char` 构造成 flags=0 的 detached Int。修复 debug-import 字符分类后 fresh release `/dev/shm/rugra-root-getstr-charfixed.c` 的完整 GetStr body 与 golden 一致，`compare_ghidra.py --func GetStr -v` 为 skeleton identical/defects=0/numbering=0，真实行恢复 `if ((value != (char *)0x0) && (*value != '\0')) {`。阶段 runner 的 raw signature 103/103、272 Varnode flags/type 与聚焦 read-facing/token 投影 MATCH；五个可比较 stage overall 仍 `MISMATCH`、Heritage `NO_ORACLE`，不得升级整管线/L3；last_updated=2026-08-28。 |
| `DEBUGPROTO-DWARF-CHAR-0001` | P0 | PARTIAL_MATCH（真实 GetStr 可见投影 `MATCH`；完整 importer `NO_ORACLE`） | root + `getstr_pretty_path_audit`（独立根因审计）；实现完成后另派 current-tree reviewer | 当前 write-set=`src/debugproto.rs`, `docs/api/debugproto.md`, 本行；后续 fixture write-set=`tests/oracle/debugproto_dwarf_char_1204.{java,cc,rs,metadata.json}`, `tools/run_debugproto_dwarf_char_oracle.sh`, registry；依赖 Architecture TypeFactory importer boundary | 输入 SHA=`8af50bca2f812580933fbbf125b66ce8ba4acfe88ef4435c89ac72356f122d41`，GetStr DIE 链=`0x28c6→0x174→0x17f`（char/1/signed_char）。当前实现让 DWARF signed-char/core `char` 与 libc `char` 使用 `CHARTYPE/SUB_INT_CHAR`，并让 typedef/qualifier clone 保留 flags/submeta 与完整 shape；`DW_ATE_unsigned_char` 明确保留 ordinary `uchar` 语义。3 个精确 Rust tests 通过，fresh production GetStr 精确 `MATCH`。残差：Java `DWARFDataTypeManager::getBaseType` 的完整 name-first 表/size/compatibility/cache、Program TypeDef identity、Pcode 编码矩阵和 Architecture TypeFactory `findAdd` canonical identity尚无双侧 fixture，故 importer 继续 `NO_ORACLE`/L2；验收需 signed/unsigned/UTF/non-char/name-first 矩阵、异常与对象突变零差异；last_updated=2026-08-28。 |
| `RULE-PORT-EARLYREMOVAL-0001` | P0 | PARTIAL_MATCH（14/14 covered projection `MATCH`；overall `MISMATCH`；evidence=`this commit`） | root + `earlyremoval_fixture_impl`（fixture）+ `earlyremoval_static_review`（独立 scoped APPROVE） | 实际 write-set=`src/{action,ruleaction,heritage,space,varnode}.rs`, `docs/api/{action,ruleaction,heritage,space,varnode}.md`, `tests/oracle/rule_earlyremoval_1204.{cc,rs,metadata.json}`, `tools/run_rule_earlyremoval_oracle.sh`, `tests/oracle/fixture_registry.json`, `ALIGNMENT_ROADMAP.md`, 本行；依赖现有 `HeritageInfo`/`Funcdata::op_destroy`；是 `GETSTR-PRINTC-STRUCTCOND-0001` 的前置地基 | 锁定 oracle=`e40ed13014025f82488b1f8f7bca566894ac376b`。双侧 runner exit 0：17 raw records，其中 14 covered records 逐字节 MATCH，covered sha=`b8d27bf2181542cad1958344231cab884f954cc5e300e9a920b7c4aeb86d6532`；覆盖六守卫、严格 `pass > deadcodedelay`、成功时 `deadremoved=1`、writemask/autolive 正交、OTHER policy、完整 covered op/block/bank/def-use 突变及 72 typed-opcode production dispatch（68 删除，CALL/CALLIND/CALLOTHER/NEW 四类存活）。独立复核只批准该投影。Ghidra raw bucket 0/45、销毁后的 nullable input arity、FSPEC/overlay/manager-indexed infolist、完整 op-bank 与未动态覆盖的 `reset/clear/propagateCopyAway` 继续 `MISMATCH/UNTESTED`；模块不升 L3。last_updated=2026-08-28。 |
| `FUNCDATA-NEWVARNODE-SYMBOLTAIL-0001` | P0 | OPEN（由 GetStr scoped fixture 明确隔离） | unassigned | 后续 write-set=`src/{funcdata,varnode,database}.rs`, `docs/api/{funcdata,varnode,database}.md`, 独立双侧 fixture；依赖 `ADDRESS-0001`/Architecture-owned Scope/TypeFactory | Ghidra `Funcdata::newVarnode` 固定执行 create→assignHigh→lane check→localmap queryProperties→setSymbolProperties 或 setFlags；当前 generic 路默认 RAM，explicit-space glue 只证明 flags/coarse bank order，拿不到 live SymbolEntry，也未建立 HighVariable symbol 反向连接。验收须比较 datatype/mapentry/high symbol/flags、异常与同址 bank identity完整突变；last_updated=2026-08-28。 |
| `BLOCKSTRUCT-IDENTIFY-BOUNDARY-0001` | P0 | IN_PROGRESS（重开；现有 absorbed-child `DEAD` 模型已独立证伪） | root + `blockidentify_repair_audit`（只读）；实现后另派独立 reviewer | 独占实现租约=`src/{block,blockaction}.rs`, `docs/api/{block,blockaction}.md`, `tests/oracle/block_identify_internal_1204.{cc,rs,metadata.json}`, `tools/run_block_identify_internal_oracle.sh`, `tests/oracle/fixture_registry.json`, 本行；依赖先完成 `GETSTR-FUNCLINK-SPACE-0001`，并复用 `BLOCK-BUILDCOPY-MIRROR-0001` 审计结论 | Ghidra `BlockGraph::identifyInternal`（`block.cc:940-963`）移动同一节点、写真实 parent、保持组件顺序并成对重写边，不设置 `f_dead`；Rugra 当前把仍由 composite 持有的子块置 `DEAD`，这是结构树所有权、后续 Rule 可见性和边重写语义的独立 MISMATCH，但已证实并非 GetStr 漏发 `free` 的最近打印根因。验收=双侧 fixture 的顶层/children 顺序、parent、flags、边 slot/reverse/label、dedup 与完整突变 MATCH；goto/deadregion/parseconfig/main、curl/httpd 主管线无未解释差异；核心白名单须取得新的 `## Cross-Review: APPROVE`；last_updated=2026-08-28。 |
| `GETSTR-PRINTC-STRUCTCOND-0001` | P0 | PARTIAL_MATCH（PrintC 4-case、structured-negate 13-case 与 GetStr 短路条件投影 `MATCH`；GetStr overall `MISMATCH`；evidence=`this commit`） | root + `printc_structcond_audit` + `getstr_condition_polarity_review`（独立 scoped APPROVE） | 实际 write-set=`src/{block,blockaction,printc}.rs`, `docs/api/{block,blockaction,printc}.md`, `examples/getstr_stage_snapshot.rs`, `tests/oracle/{getstr_pipeline_1204.*,printc_structured_if_condition_1204.*,block_structured_negate_1204.*}`, `tools/{run_getstr_pipeline_oracle.sh,run_printc_structured_if_condition_oracle.sh,run_block_structured_negate_oracle.sh}`, `tests/oracle/fixture_registry.json`, `ALIGNMENT_ROADMAP.md`, 本行；依赖=`GETSTR-CHAR-READFACING-0001`/`RULE-PORT-EARLYREMOVAL-0001`；完整 identify/build-copy 闭包继续由既有 ID 跟踪 | fresh 12.0.4 证据：PrintC 4-case runner 5 records/9284 bytes raw diff 空；structured-negate 13 cases/14 lines/3628 bytes 双侧 stdout 逐字节 MATCH（sha=`d47ef9da43…`），覆盖有序双半边/reverse-index/label、`FLIP_PATH`、AND↔OR、children 虚派发与 dataflow count；GetStr 结构树聚焦投影及最终行逐 token 为 `if ((param_1 != (char *)0x0) && (*param_1 != '\0')) {`（oracle 仅参数编号为 `param_2`）。完整 `ruleBlockProperIf` 的 raw indegree 守卫、指针身份自环守卫、`newBlockCondition` 无终端 CBRANCH、`identifyInternal` parent/top/dead/boundary、其他 subtype/markup/异常仍 `MISMATCH/UNTESTED`；GetStr 五个可比较 stage 仍 overall `MISMATCH`、Heritage `NO_ORACLE`，不得宣称完整结构化或 PrintC 对齐；last_updated=2026-08-28。 |
| `BLOCK-STRUCTURED-NEGATE-0001` | P0 | PARTIAL_MATCH（13-case covered projection `MATCH`；overall `MISMATCH`；evidence=`this commit`） | root + `structured_negate_fixture`（fixture）+ `getstr_condition_polarity_review`（独立 scoped APPROVE） | 实际 write-set=`src/{block,blockaction,printc}.rs`, `docs/api/{block,blockaction,printc}.md`, `tests/oracle/block_structured_negate_1204.{cc,rs,metadata.json}`, `tools/run_block_structured_negate_oracle.sh`, `tests/oracle/fixture_registry.json`, `ALIGNMENT_ROADMAP.md`, 本行；依赖=`BLOCK-BUILDCOPY-MIRROR-0001` 的 reciprocal-edge 契约；不得把 full CollapseStructure residual 归一化掉 | 锁定 oracle=`e40ed13014025f82488b1f8f7bca566894ac376b`。runner validate/full 均 exit 0：13 cases、14 lines、3628 bytes，双侧 raw stdout diff 空、sha=`d47ef9da438012d850b3374dddf6d54cf7bdbaaf82b145b5b7c280ab5151c57d`；覆盖 `FlowBlock::swapEdges/negateCondition`、`BlockList::negateCondition`、`BlockCondition::negateCondition`、Rule cat/proper-if 与 collapse count 的指定投影，两次取反恢复完整有序边状态。overall 保持 `MISMATCH`：Rugra 独有 4 行结构诊断 stderr（sha=`03a128b338…`），且 ProperIf raw indegree/身份、`newBlockCondition`、`identifyInternal`、BlockBasic/Copy/错误路径及其他 PrintC 分支未闭合；独立 reviewer 仅 scoped APPROVE，模块不升 L3；last_updated=2026-08-28。 |
| `ORACLE-REGISTRY-IMPACT-CONTINUITY-0001` | P0 | OPEN（全局 ledger/fixture 选择连续性门禁不健康；本波新增 entries 本身结构完整） | unassigned | 精确后续 write-set=`tools/{generate_function_ledger,oracle_registry,select_fixtures}.py`, `tests/oracle/fixture_registry.json`, `docs/alignment_audit/{FUNCTION_LEDGER.json,FUNCTION_MAP.md,FUNCTION_PROTOCOL_MAP.json,FUNCTION_DEPENDENCY_DAG.json,FUNCTION_ID_CONTINUITY.json}`, `tools/README.md`, 本行；不得手改生成物或猜迁移 ID | 当前依赖 ID 已统一为 canonical `BLOCK-BUILDCOPY-MIRROR-0001`，`TMPDIR=/dev/shm python3 tools/generate_function_ledger.py --check` 现 fail-closed 到生成物 stale（exit 1）。完整 dry-run 显示 Rust function 10058→10455、541 introduced/144 removed、82 组疑似签名演进需人工 continuity 复核；checkpoint 仍停在旧 src tree，registry 的 216 个唯一 RG 引用中 22 个不在 live tree。`oracle_registry.py lint` 仍因 continuity 漂移 exit 2，strict selector 还会被 legacy entry 缺 `impact` 阻断。验收=人工审核 transition/tombstone/introduced allowlists 后一次性重建四生成物并更新 checkpoint，所有 legacy entry schema-valid，ledger check/lint/selector 全 exit 0；本波 GetStr/structured/Atom/PTRSUB 的局部 fixture 结论不得外推成全局账本健康；last_updated=2026-08-28。 |
| `EVIDENCE-DRIFT-DE26F21C-0001` | P0 | IN_PROGRESS（只读定位） | root + postf133_regress | 当前无写集；审计 `f44f6415..de26f21c` 后再租约 | commit message 声称 skeleton=2672/progressbarinit=2，但同 commit fresh 实测 2822/15；以 sha=`4404af6d...`、`/tmp/rugra-de26f21c-curl.{c,stderr}` 和 `/tmp/rugra-reports/TRI5-2026-08-27.md` 为证。验收=定位首错提交/函数并产生可复算 A/B。 |
| `POSTF133-REGRESSION-0001` | P0 | IN_PROGRESS（只读函数体审计） | postf133_regress | 当前无写集；候选依赖 Ghidra typeop/coreaction 完整函数审计 | pre-chain artifact skeleton=2678；de26/`4404af…` 历史 baseline=2822，当前 formal=2820、progressbarinit=13。hugehelp/progressbarinit/my_fwrite 的 +19/+13/+12 仅是旧 A/B 对比，未在本轮多函数 PTRSUB overlay 下重算或单因归因。验收=双侧行号、四类语义、首错函数、最小底层修复和 oracle fixture 设计。 |
| `TYPEOP-PTRSUB-FIELDCAST-0001` | P0 | PARTIAL_INTEGRATED（covered projection `MATCH`；overall `MISMATCH`） | root + `ptrsub_commit_evidence_review`（独立 scoped APPROVE） | `src/{typeop,varnode,coreaction,space}.rs`, `src/type_system/{cast,typefactory}.rs`, `docs/api/{typeop,varnode,coreaction,space}.md`, `docs/api/type_system/{cast,typefactory}.md`, `tests/oracle/ptrsub_output_token_1204.{cc,rs,metadata.json}`, `tools/run_ptrsub_output_token_oracle.sh`, `tests/oracle/fixture_registry.json`, `ALIGNMENT_ROADMAP.md`, `CURRENT_STATUS.md`, 本行 | evidence=`this commit`；oracle=`e40ed13014025f82488b1f8f7bca566894ac376b`。24 records/3740 bytes，双侧 raw stdout 逐字节一致（sha=`129d793461…`），registered raw differences 为空。scale、8-byte PcodeOp/direct local identity/core、direct token/pointee/repeat identity、ordinary castOutput selected graph、raw `ActionSetCasts` 的 `result=0,count=1`，以及 selected infer 的 base/out int8 shape、STOP、定义边与 canonical int8 identity=1 均 MATCH。fixture overall 保持 MISMATCH：Rust `take_count_delta` 是 Ghidra 无对应方法的 `NO_ORACLE` adapter，且未执行完整 `Action::perform` lifecycle；full getOutputToken 未覆盖分支绑定本 ID、`TYPEFACTORY-ARC-IDENTITY-0001` 与 `TYPEFACTORY-LOCALTYPE-CACHE-0001`；full castOutput/apply、VarnodeLocSet/SymbolEntry/exact-piece/DFS/repeat/error 闭包仍分别绑定 `PIPE-ACTION-COUNT-0001C`、`VARNODE-LOCALTYPE-RESOLUTION-0001` 与 `ACTION-INFERTYPES-DISPATCH-0001`；full Architecture 状态仍 `NO_ORACLE`。先前 release A/B 的可见残差仍按 `TYPEOP-PTRSUB-FIELDCAST-0001`、`PTRSUB-SWITCH-CAST-RESIDUAL-0001`、`PTRSUB-TYPED-DECL-RESIDUAL-0001` 跟踪，不能由本 selected fixture 宣称 production closure MATCH。验收=`tools/run_ptrsub_output_token_oracle.sh` + targeted unit + fresh curl full/function differential；last_updated=2026-08-28。 |
| `PTRSUB-TYPED-DECL-RESIDUAL-0001` | P0 | MISMATCH（formal E2E 已观察） | unassigned（本轮仅登记） | 本 ID 精确 write-set=`src/printc.rs`, `docs/api/printc.md`, `tests/oracle/printc_anonymous_pointer_decl_1204.{cc,rs,metadata.json}`, `tools/run_printc_anonymous_pointer_decl_oracle.sh`, 本行；syntax repair 无前置 blocker；satisfied foundations=`VARNODE-LOCALTYPE-RESOLUTION-0001`,`FUNCDATA-LINKSYMBOL-TYPED-0001`；reopened output subdomain=`PRINTC-SYMBOL-DECL-0001`；conditional related=`TYPE-UNKNOWN-0001`；上游 golden identity 仍由 `ACTION-INFERTYPES-DISPATCH-0001` 独立跟踪 | Ghidra `PrintC::buildTypeStack`（printc.cc:143-166）沿匿名 PTR/ARRAY/CODE 下钻，`pushTypeStart`（:264-303）对匿名 base 调 `genericTypeName` 并按 typestack 发射 `*`；Rust `push_type_start_opt→emit_type_prefix` 当前直接渲染 `Datatype::get_name()`，而 anonymous `TypePointer` 的 name 可为空。这是六处裸声明的直接语法断点，不要求先修完 VarnodeLocSet/getLocalType；上游 type/symbol identity 仍是达到 golden 的独立闭包。formal A/B 中 `main pCVar29`、`getparameter ppuVar16`、`glob_word pcVar5`、`glob_set pcVar7`、`next_url pcVar11`、`match_url pcVar10` 均缺 type token；focused gcc primary errors 在 main/glob_word/glob_set/match_url 各 +1，getparameter/next_url 失败数不变。验收=锁定双侧匿名 pointer/array/code declarator fixture 的类型栈、token 顺序、spacing、异常与完整突变 MATCH + 上述六个 typeless declarations=0 + gcc pass/fail/诊断不劣于 `4404af…` baseline；golden type/name/symbol identity 由既有上游 TODO 另验。禁止 prettyprint/backfill 字符串补类型；evidence=`this commit`（差异登记），last_updated=2026-08-28 |
| `PTRSUB-SWITCH-CAST-RESIDUAL-0001` | P1 | MISMATCH（formal E2E 已观察；根因未定位） | unassigned（只登记，先建诊断 fixture） | phase A 精确 write-set=`tests/oracle/ptrsub_switch_cast_1204.{cc,rs,metadata.json}`, `tools/run_ptrsub_switch_cast_oracle.sh`, `docs/TODO_BOARD.md`；只读依赖审计=`TYPEOP-PTRSUB-FIELDCAST-0001`、`ACTION-INFERTYPES-DISPATCH-0001`、`RULE-PTRARITH-ADDTREE-0001`、`JUMPTABLE-TABLEAPI-0001`、`PRINTC-SWITCH-EMIT-0001`；实现 write-set 须由 fixture 定位首叉后另行认领 | `glob_set` baseline 为 `switch(unique0x10000119 + (int *)*((int *)(int *)unique0x10000111 + (int *)(int *)(long) register...))`，formal 新增 outer 与 nested `(int *)`，变为 `switch((int *)unique0x10000119 + (int *)*((int *)(int *)(int *)unique0x10000111 + (int *)(int *)(long) register...))`；锁定 golden 是 `switch(cVar3)`，故两版均 MISMATCH，且新增 cast churn 不是收益。验收=锁定同一 IR/CFG/type state 的双侧 fixture，观察 PTRSUB/PTRADD token、cast insertion、switch expression tree 与打印 token，定位最早差异；随后 curl `glob_set --func -v` 必须无新增 cast churn、case/control-flow 不退化且 defects/numbering=0。evidence=`this commit`（差异登记），last_updated=2026-08-28 |
| `TRI4-MAIN-RESID-0001` | P0 | IN_PROGRESS（只读根因分层） | main_resid | 当前无写集；main_diff helper 已交付 raw diff | 当前 main skeleton=677，函数体 228 行 vs oracle 476 行，入口/参数循环/URL 主循环大量缺失；验收=最早 IR/CFG 分叉、双侧完整函数与独占修复切片。 |
| `TRI4-GETPARAM-CALLPROTO-0001` | P0 | IN_PROGRESS（只读根因分层） | getparam_proto + getparam_diff | 当前无写集；与 typeop/coreaction 候选互斥 | 当前 getparameter skeleton=543；验收=最早 callspec/prototype/结构分叉、真实 oracle fixture 和最小 write-set。 |
| `TRI4-FILE2STRING-IR-0001` | P0 | IN_PROGRESS（只读根因分层） | file2string_ir | 当前无写集 | 当前 file2string skeleton=121；验收=缺失语句被删除/未生成的最早 Action/Rule、IR 观察点和 oracle fixture。 |
| `TRI4-PARSECONFIG-STRUCT-0001` | P0 | IN_PROGRESS（只读根因分层） | parseconfig_struct | 当前无写集；skeleton_diff helper 已证明入口 OR/四循环在 finalize 前仍存在 | 当前 parseconfig skeleton=123；raw 41 blocks/4 loops/入口 OR 后 finalize 41→2，验收=首个错误组合/父子关系与最小 block write-set。 |
| `TRI4-NEXTURL-STATE-0001` | P1 | IN_PROGRESS（只读根因分层） | nexturl_state | 当前无写集 | 当前 next_url skeleton=103；验收=SSA/merge/varmap/Rule 最早状态分叉、互斥 write-set 和双侧 fixture。 |
| `PLTSTUB-WARNLOSS-0001` | P1 | IN_PROGRESS（只读复核） | plt_warnloss | 当前无写集；候选 `src/{coreaction,fspec}.rs` 等 postf133 裁决 | fresh warning=27/51，缺失恰为 24 个 PLT thunk；验收=unknown-model+lock 状态生命周期首叉、1 PLT+1 EXTERNAL oracle fixture、51/51 E2E。 |
| `HTTPD-ADDDESCEND-THROW-0001` | P1 | IN_REVIEW（当前 master 只读复核） | httpd_dom | 当前无写集；候选 block/funcdata/heritage 属机制 C | 验收=核清 rootlist/dominator/forceRestructure 已闭合与残余 MISMATCH，给出 httpd+curl 门禁及独立 reviewer 条件。 |

> 并发计数：上述 9 个 work package 由 10 个活跃后台 worker 执行（main/getparameter/parseconfig 各含一个差分 helper，已完成 helper 立即由 POSTF133/httpd 补位）；后台 workers 全部只读，root 是 PTRSUB 精确 write-set 的唯一 writer，当前无 Cargo 竞争。首批审计交付后，root 只给 write-set 无重叠且依赖满足的任务创建隔离 writer，并保持总活跃 worker 数为 10。

### 本 wave 认领租约（2026-08-27 11:05 刷新；前表见 git 历史）

| ID | P | 状态 | owner | write-set（租约） | 备注 |
|---|---|---|---|---|---|
| `REGA-HUGEHELP-LITERAL-0001` | P0 | IN_PROGRESS | regA@rugra-wt-regressfix2 | `src/heritage.rs`+docs/api/heritage.md | 回归 A 根因修复（bumpDeadcodeDelay/restart 链嫌疑）；前任 [DBG] 插桩需清除；验收=hugehelp/progressbarinit 恢复 c10871c3 形态+defects 0 |
| `REGB-MYFWRITE-DUALNULL-0001` | P1 | IN_PROGRESS | regB@rugra-wt-regb | `src/coreaction.rs`+`src/block.rs`（blockaction.rs 尽量少动） | 回归 B 根因修复（fieldsplit live_ops_source/STORE 播种嫌疑）；验收=my_fwrite 双判空恢复+skeleton 8→低 |
| `PRINTC-LINEWRAP-0001` | P1 | IN_PROGRESS | strconst2@rugra-wt-strconst2 | `src/printc.rs`+`src/prettyprint.rs`（**独占 printc 租约**） | hugehelp 最后 12 行折行；交付后释放 printc 租约给 switchemit 队列 |
| `TRI2-STRUCT-IRREDUCIBLE-TRACE-0001` | P0 | IN_PROGRESS(第6轮) | irred@rugra-wt-irreducible | `src/blockaction.rs` | exhausted 8 函数→更低；block.rs 只报告不动 |
| `GOTO-LABEL-UNPRINTED-0001` | P1 | IN_PROGRESS | gotolabel@rugra-wt-gotolabel | `src/flow.rs`+`src/funcdata.rs`+`examples/httpd_decompile.rs` | printc.rs 冻结提交后不再编辑（租约在 strconst2）；等 root 通知 |
| `REVIEW-DEBTS-X3-2026-08-27` | P0 | IN_PROGRESS | reviewer@rugra-wt-rfupreview | 只读+报告+TODO 三行 | R-MAINSPIN/R-ORPHANDECL/R-CALLOUT 三笔机制 C 欠账复核；附回归缩围意见 |
| `RULE-PULLSUB-NEWVNODEOUT-0001` | P2 | IN_PROGRESS | orphanfix@rugra-wt-orphanfix | `src/ruleaction.rs` + `docs/api/ruleaction.md` + 本 TODO 行 | 补齐 RulePullsubMulti::buildSubpiece join 臂：按 ruleaction.cc:793-815 逆序 piece 扫描、skipleft/端序地址计算、renormalize；单片/未命中保持 unique。依赖现有 Architecture::join_db::find_join（space.rs:503-523）。验收：cargo check/test --lib ruleaction + curl_decompile 全量 compare defects=0/numbering=0；oracle e40ed13014025f82488b1f8f7bca566894ac376b；2026-08-27 |
| `PLTSTUB-WARNLOSS-0001` | P1 | IN_PROGRESS(只读 bisect) | pltstub@rugra-wt-pltstub | 只读+报告+TODO 行 | 51→27 警告丢失二分（早于 8474ec13）；交付引入 commit+修复 write-set 建议 |
| `HTTPD-E2E-2026-08-27` | P1 | IN_PROGRESS(只读) | httpde2e@rugra-wt-httpde2e | 只读+报告+TODO 行 | master httpd 全量复测+goto 三症状普查（喂 gotolabel） |
| `PRINTC-SWITCH-EMIT-0001` | P0 | PHASE-A | switchemit@rugra-wt-switchemit | docs 设计+fixture C++ 侧（**printc.rs 禁改**） | 阶段 A=oracle 深读+设计文档+fixture 骨架；printc 租约释放后实施 |
| `TRI2-CALLOUT-ASSIGN-0001` | P0 | INTEGRATING | integrator@主仓 | master 直接操作（唯一例外） | agent/callout 链 19ce09bf+c9f6337e+6230b082 入 master（__nptr 赋值形+(char **)折叠+BlockCopy 活委托）；"回归 A"证伪=集成缺位非回归（regA A/B 实证 c10871c3≡master） |
| `REGB-MYFWRITE-DUALNULL-0001` | P1 | IN_PROGRESS | regB@rugra-wt-regb | `src/coreaction.rs`+`src/block.rs` | 回归 B（c243cdef 双判空塌缩）根因修复；coreaction/block 租约持有中 |
| `TRI2-STRUCT-IRREDUCIBLE-TRACE-0001` | P0 | R7-IN_PROGRESS | irred@rugra-wt-irreducible | `src/blockaction.rs`+`src/jumptable.rs` | R1-6 已集成 master（4a4bd5c6..4ac33d30，exhausted 8→1）；R7=blk127 obvious-exit 拓扑+JumpTable 真实标签通道+checkSwitchSkips；复核=R-IRREDUCIBLE（reviewer 在途） |
| `TRI2-CALLOUT-RESID-0002` | P2 | IN_PROGRESS(varmap 半) | regA@rugra-wt-regressfix2 | `src/varmap.rs` | 死 `int iVar1;` 声明清零；'O'→0x4f printc 半归 markimplied2 |
| `PRINTC-IMPLIED-CHARCONST-0001` | P1 | IN_PROGRESS | markimplied2@rugra-wt-markimplied2 | `src/printc.rs`（**独占 printc 租约**） | implied-def 折叠臂（unique 裸标识/missing-LHS）+ 'O'→0x4f char 常量形（pushCharConstant 通道） |
| `REJECT-MAINSPIN-RESTART-0001` | P0 | IN_PROGRESS | mainspinfix@rugra-wt-mainspinfix | `src/action.rs`+`src/funcdata.rs` | R-MAINSPIN REJECT 返修：有界降级→clearAnalysis+followFlow 忠实重启环；heritage.rs 只报告 |
| `REJECT-CALLOUT-GUARDS-0001` | P1 | IN_PROGRESS | calloutfix@rugra-wt-calloutfix | `src/typeop.rs` | R-CALLOUT REJECT 返修 typeop 域（PTRADD/PTRSUB 规范化、castOutput PTRSUB 臂等）；`TYPEOP-PTRSUB-FIELDCAST-0001` 的 castOutput consumer 由 root 当前 wave 接线，历史 `67355abd` WIP 的播种阶段口径已撤销。本轮 locked fixture + formal curl E2E 已完成；状态/残差见顶部 `TYPEOP-PTRSUB-FIELDCAST-0001`、`PTRSUB-TYPED-DECL-RESIDUAL-0001` 与 `PTRSUB-SWITCH-CAST-RESIDUAL-0001`，本旧租约行不再作为 pending 证据；printc 域只登记 |
| `REJECT-ORPHANDECL-JOIN-0001` | P1 | IN_PROGRESS | orphanfix@rugra-wt-orphanfix | `src/ruleaction.rs` | R-ORPHANDECL REJECT 返修：join 多片段逆序扫描（ruleaction.cc:793-815）；JoinRecord 基础设施缺口评估 |
| `REGISTRY-CONTINUITY-W2-0001` | P1 | IN_PROGRESS | registryw2@rugra-wt-registryw2 | fixture_registry+metadata | append-only 前滚+三件套再生 |
| `REVIEW-RIRRED-RPULLSUB-0001` | P0 | IN_PROGRESS | reviewer@rugra-wt-rfupreview | 只读+报告 | R-IRREDUCIBLE（master 4a4bd5c6..4ac33d30）+ R-PULLSUB（cf5c73b8） |
| `PLTSTUB-WARNLOSS-0001` | P1 | IN_PROGRESS(只读 bisect) | pltstub@rugra-wt-pltstub | 只读+报告 | 51→27 警告丢失二分（早于 8474ec13） |
| `TRI4-TOPN-TRIAGE-0001` | P0 | DONE (read-only TRI4) | tri4@rugra-wt-tri4 | `/tmp/rugra-reports/TRI4-2026-08-27.md` + 本行 | master `9f33b86e`（revert 前快照）：124/124，defects=0，numbering=0，skeleton 2499（tri3 3128，-629）；top-5=main 573/getparameter 525/file2string 116/parseconfig 115/next_url 99，归因与新 TODO `TRI4-MAIN-RESID-0001`/`TRI4-GETPARAM-CALLPROTO-0001`/`TRI4-FILE2STRING-IR-0001`/`TRI4-PARSECONFIG-STRUCT-0001`/`TRI4-NEXTURL-STATE-0001` 见报告；2026-08-27 |
| `REGA-HUGEHELP-LITERAL-0001` | P0 | IN_PROGRESS | regA@rugra-wt-regressfix2 | `src/heritage.rs`+docs/api/heritage.md | 回归 A 根因修复（bumpDeadcodeDelay/restart 链嫌疑）；前任 [DBG] 插桩需清除；验收=hugehelp/progressbarinit 恢复 c10871c3 形态+defects 0 |
| `REGB-MYFWRITE-DUALNULL-0001` | P1 | IN_PROGRESS | regB@rugra-wt-regb | `src/coreaction.rs`+`src/block.rs`（blockaction.rs 尽量少动） | 回归 B 根因修复（fieldsplit live_ops_source/STORE 播种嫌疑）；验收=my_fwrite 双判空恢复+skeleton 8→低 |
| `PRINTC-LINEWRAP-0001` | P1 | IN_PROGRESS | strconst2@rugra-wt-strconst2 | `src/printc.rs`+`src/prettyprint.rs`（**独占 printc 租约**） | hugehelp 最后 12 行折行；交付后释放 printc 租约给 switchemit 队列 |
| `TRI2-STRUCT-IRREDUCIBLE-TRACE-0001` | P0 | IN_PROGRESS(第6轮) | irred@rugra-wt-irreducible | `src/blockaction.rs` | exhausted 8 函数→更低；block.rs 只报告不动 |
| `GOTO-LABEL-UNPRINTED-0001` | P1 | IN_PROGRESS | gotolabel@rugra-wt-gotolabel | `src/flow.rs`+`src/funcdata.rs`+`examples/httpd_decompile.rs` | printc.rs 冻结提交后不再编辑（租约在 strconst2）；等 root 通知 |
| `REVIEW-DEBTS-X3-2026-08-27` | P0 | IN_PROGRESS | reviewer@rugra-wt-rfupreview | 只读+报告+TODO 三行 | R-MAINSPIN/R-ORPHANDECL/R-CALLOUT 三笔机制 C 欠账复核；附回归缩围意见 |
| `RULE-PULLSUB-NEWVNODEOUT-0001` | P2 | IN_PROGRESS | orphanfix@rugra-wt-orphanfix | `src/ruleaction.rs` + `docs/api/ruleaction.md` + 本 TODO 行 | 补齐 RulePullsubMulti::buildSubpiece join 臂：按 ruleaction.cc:793-815 逆序 piece 扫描、skipleft/端序地址计算、renormalize；单片/未命中保持 unique。依赖现有 Architecture::join_db::find_join（space.rs:503-523）。验收：cargo check/test --lib ruleaction + curl_decompile 全量 compare defects=0/numbering=0；oracle e40ed13014025f82488b1f8f7bca566894ac376b；2026-08-27 |
| `PLTSTUB-WARNLOSS-0001` | P1 | IN_PROGRESS(只读 bisect) | pltstub@rugra-wt-pltstub | 只读+报告+TODO 行 | 51→27 警告丢失二分（早于 8474ec13）；交付引入 commit+修复 write-set 建议 |
| `HTTPD-E2E-2026-08-27` | P1 | IN_PROGRESS(只读) | httpde2e@rugra-wt-httpde2e | 只读+报告+TODO 行 | master httpd 全量复测+goto 三症状普查（喂 gotolabel） |
| `PRINTC-SWITCH-EMIT-0001` | P0 | PHASE-A | switchemit@rugra-wt-switchemit | docs 设计+fixture C++ 侧（**printc.rs 禁改**） | 阶段 A=oracle 深读+设计文档+fixture 骨架；printc 租约释放后实施 |
| `TRI2-UNNAMED-VN-IMPLIED` + `TRI2-CALLOUT-RESID-0002` | P1 | DONE（printc+prettyprint 侧；agent/markimplied2） | markimplied2 | `src/printc.rs` + `src/prettyprint.rs` + `docs/api/{printc,prettyprint}.md` + 本行 | Oracle `e40ed13014025f82488b1f8f7bca566894ac376b`; RPN PTRADD/PIECE implied 链复核保持（printc.cc:880-893, 424-442；printlanguage.cc:197-211, 514-540），Const 发射改按 propagated TYPE_INT/UINT + isCharPrint 分派；删除 prettyprint 旧 `0x4f→'O'` workaround，progressbarinit 收敛；验收=fast-release curl E2E + compare defects/numbering 0；2026-08-27 |

已交付集成（本轮）：callout=master `f35f29d7`；orphanfix=`1b93bc7e`+`f27f32b9`；regB snapshot=`9f33b86e` 已由 `9ff63dcb` revert（R-REGB REJECT，不计入有效语义）；LINEWRAP=master `980f0831`；GOTO-LABEL=master `a3cda8b9`；PULLSUB=master `cf5c73b8`；IRREDUCIBLE R1-6=master `4a4bd5c6..4ac33d30`。
> 基线（master `9ff63dcb`）：124 函数 / defects 0 / numbering 0 / skeleton 2639；httpd 29 函数 / defects 0 / numbering 0 / skeleton 2123；三函数 hugehelp/progressbarinit/my_fwrite = 0/4/8。
> 租约队列：printc.rs = markimplied2（持锁）→ switchemit 阶段 B（等 BlockSwitch 结构侧）→ callindptr/microform；coreaction.rs = regB → markimplied/deadstore 排队；空闲 worktree 断点：switchemit（阶段 A 设计完成 0630c160/e64fc295，待结构侧+租约）、markimplied（旧 wip 仅作矿）、callindptr/microform（未启）。
> 机制 C 复核账：R-MAINSPIN/R-ORPHANDECL/R-CALLOUT 三 REJECT（报告 /tmp/rugra-reports/R-*-2026-08-27.md）→ 返修已派；R-IRREDUCIBLE/R-PULLSUB 在途。

## 历史 wave：`W-2026-08-24-TRIFUNC-GAP`（进行中；基线 = fresh `f7b3c31` artifact，三函数严格 0/3）

> 目标：hugehelp/progressbarinit/my_fwrite 严格字节一致 + 函数体差距驱动的根因修复 + TODO 清算。
> root 串行集成；所有 writer 走隔离 worktree + `/tmp/rugra-cargo-build.lock` + 专属 CARGO_TARGET_DIR；本节 root 维护，worker 禁改。
> **集成纪律（2026-08-25 事故教训：A4 运行中 root 集成了同文件 ACTIONPOOL，致池克隆语义重叠需 rebase 弥合）**：root cherry-pick 任何候选前，必须先核对候选 write-set 与**所有在跑 Agent 租约**零重叠；有重叠 → 排队等租约释放。构建/测试命令一律包 `timeout 600`（A13 死锁占锁 43 分钟事故）。
> 审计产出已归档 docs/alignment_docs/：GATE_HEALTH / PTRSUB_DOWNCHAIN / STOP_WIRING / D2_BUILDLOCALTYPES_DESIGN / FRESH_BASELINE_TRIAGE / CONTINUITY_DIRTY_FILES_AUDIT（均 *_2026-08-24.md）。

### 本 wave 认领租约

| ID | P | 状态 | owner | 精确 write-set | 备注 / 证据 |
|---|---|---|---|---|---|
| `COREACTION-FUNCPARAMNAMES-RECOMMEND-0001` | P1 | REVIEW（候选 @ agent/funcparam worktree commit，待 root 集成；write-set 不触核心白名单/不触 coreaction.rs——lookForFuncParamNames 内联实现已存在且门禁齐全，缺口纯在数据层——但主管线 Action 可观测行为变化，按任务规约列 REVIEW 待独立复核） | funcparam-agent@wt-funcparam + root | `src/debugproto.rs`（LibcSignatureTable::locked_proto + DebugPrototypeDatabase::apply 两处 NAME_LOCKED）、`docs/api/debugproto.md`、本 TODO 行 | 三函数目标直接命中：progressbarinit `extraout_RAX`→`__nptr`（=golden，--func defects=0/numbering=0）；根因=ProtoParameter::new 恒 flags:0，NAME_LOCKED 全链缺失致 coreaction.rs lookForFuncParamNames（cc:2858-2897 逐条门禁已镜像）永不触发——修复镜像平台边界 fspec.cc:3503-3506 ATTRIB_NAMELOCK→:3564 setNameLock（libc 表全真名参数一律锁；DWARF 合成名 `param_N` 不锁，Ghidra 无名 DIE 由 buildDefaultName 接管 + cc:2831 param_ 前缀过滤）；全量 124 函数 13 函数获推荐命名（`__s`×16/`__dest`×15/`__ptr`×13/`__n`×6/`__nptr`×4/`__stream`/`__haystack`/`__filename`；file2string `FILE *__s` 与 golden:1110 精确一致，main `FILE *__stream` 同），skeleton 2863→2849（-14），defects=0/numbering=0 保持；main 中 `byte *__haystack` 声明未用为既有孤儿声明形态（基线 pbVar45 同样未用，VARMAP-ORPHAN-DECL-0001 族）；2026-08-25 |
| `MAIN-ARGC-PROTO-0001` | P1 | DONE（agent/main-argc-v2 worktree commit，待 root 集成） | a19@wt-argc2 | `src/prettyprint.rs`, `examples/curl_decompile.rs`, `docs/api/prettyprint.md`, 本 TODO 行 | main 签名 `int main(char *argc,char **argv)`→`int main(int argc,char **argv)`（=golden 560 行）。根因链三层全证：① DWARF→FuncProto 接线本就正确（debugproto.apply argc:int@0x38/argv:char**@0x30，scope 符号与输入 varnode 均锁定正确）；② `symbol_driven_function_line_mask` 在 skip_line 布局（签名行与 `{` 间的空行）上分段退化为两行 → main 未被掩码 → `fix_unary_deref_declarations` 因 body 中 `*argc` 把签名里锁定的 `int argc` 改写成 `char *argc`——修 prettyprint.rs 分段先推进到前瞻已定位的 `{`；③ worker 10s 截止砍掉 main（action ~11.5s）→ FUNCTION_TIMEOUT 10→30s（Ghidra DecompileOptions.java:289 SUGGESTED_DECOMPILE_TIMEOUT_SECS=30）。A/B（124 函数全量）：字节 delta 共 8 处全部向 oracle 收敛（main int argc、glob_set int pos 均=golden；match_url param_1 char*→long；5 处 unique0x 声明回退 long 默认）；defects 0→0、numbering 1→1（match_url per-prefix，基线 A/B 同值预存，绑 VARMAP-LOCALWINDOW-0001 族）；skeleton 2884→2880；`prettyprint::` 4/4 + `printc::` 10/10 绿；result/curl_cur.c 已回流；2026-08-25 |
| `PRINTC-EMPTYELSE-0001` | P1 | DONE（候选 @ agent/f2s-else-v2，待 root 集成；cherry-pick 注意 docs/api/printc.md 末尾追加段与遗留冲突标记分区） | f2s-agent@wt-f2s + root | `src/printc.rs`（is_block_body_empty 单函数）、`docs/api/printc.md` 末段、本 TODO 行 | 根因=printc 判空谓词自创 dead-output 过滤（global_used_outputs+27 纯计算 op 白名单，Ghidra 无对应物；oracle emitBlockBasic printc.cc:2695-2705 仅 notPrinted/branch/implied 三道门，真死计算归 ActionDeadCode 前置）；修复=谓词精确镜像默认发射路径 emit_block_basic_rpn 四道门（is_dead/notPrinted 三 flag/is_branch/implied），删 dead-output+legacy COPY/RIP/stack/inlined skip；E2E：空 if 体 7→6（main glob_url 臂+myprogress else 臂真实语句恢复）、defects 0→0、numbering 1→1、skeleton 2884→2891（+7=恢复语句净增，见 ## Differential）；**残差分层**：剩余 6 处空 if 体与 file2string 主体缺失为 IR 层（worker 环境 callspec/DWARF 下上游 Action 已删 op，单函数 runner 无 DWARF 时输出 80 行含 memcpy 尾）→ 归 FILE2STRING-EMPTYELSE-IRLAYER 登记后续；2026-08-25 |
| `MAIN-PROF-STACKOFFSET-0001` | P0 | DONE（本 commit；heritage.rs 属机制 C 白名单，待独立 Cross-Review 后方可入主管线） | mainprof@wt-mainprof | `src/{fspec,varnode,coreaction,ruleaction,heritage}.rs`, `docs/api/{fspec,varnode,coreaction,ruleaction,heritage}.md`, `docs/TODO_BOARD.md` | main() 超时根因三连：①stackoffset 永为 OFFSET_UNKNOWN（resolveSpacebaseRelative/createPlaceholder/RuleLoadVarnode 尾巴未移植）→ guardCalls 为每个 (stack range × call) 造 unknown-effect INDIRECT（8190 个），mainloop repeatapply 37+ 轮不收敛；②abort_spacebase_relative 的 `if let` scrutinee read guard 跨 opDestroy 存活 → makeFree vn.write() 单线程 RwLock 自死锁（coredump 栈实证，main worker futex 挂起）；③set_def O(n) owns 预检 + LocationMap::add O(n) keys 快照 → 二次方。修复后：main action 4.9s（<5s 达标，原 TIMEOUT/挂起），全语料 124/124 零 TIMEOUT（wall 33.9s），差分 defects=0/numbering=1(match_url，基线既有 parity)，cargo test 18 failed 与基线逐一致（非本次引入）；2026-08-25 |
| `TYPEOP-LOCALTYPE-DISPATCH-0001`(D1) | P0 | INTEGRATED（R3 独立 APPROVE 四类语义 4/4；候选 `8b8b541`+`9c79d96`→main `e3e0053`+`e24cdf1`） | a1@wt-typeop-localtype-d1 + R3 + root | `src/typeop.rs`, `docs/api/typeop.md`, `tests/oracle/typeop_local_type_1204.*`, runner | 双侧 167 records 行为 13/13 字节一致；唯二表示行差异绑 `TYPEOP-FSPEC-SPACE-0001`（a8 在修）；条目整体维持 MISMATCH；D2 排 A2 释放 coreaction 后；报告=/tmp/rugra-reports/R3-D1-REVIEW.md；2026-08-25 |
| `TYPEOP-LOCALBASE-DEFAULTS-0001` | P1 | INTEGRATED（`bfba4b4`+`2c31b16`→main `0f0a060`+`81665b8`） | a15@wt-typeop-localbase + root | `src/typeop.rs`, docs, fixture/runner | 两 R3 缺口闭合（covered projection MATCH）；ZEXT/SEXT 16 行差异=PRINTC-CAST M1 双侧证据，M1 前置完成；D2 的 local_type_factory 钩子就位；2026-08-25 |
| `TYPEFACTORY-EXACTPIECE-CALLERS-0001` | P0 | INTEGRATED（R4 独立复核 APPROVE 含本人复现 runner；候选 `26116cc`+`57d4c0f`→main `f87e4d8`+`51b7357`） | a2@wt-myfwrite-splitdatatype + R4 + root | 同原 write-set | 双侧 20 records 逐字节一致；四调用点 canonical API 化、本地副本删除；残余（dynamic-symbol UNTESTED/update_type 零 caller/arch 接线）如实登记；解锁 SPLITDATATYPE-EXACTPIECE；报告=/tmp/rugra-reports/R4-EXACTPIECE-REVIEW.md；2026-08-25 |
| `TYPEFACTORY-DOWNCHAIN-VIRTUAL-0001` | P0 | INTEGRATED（R6 独立复核 APPROVE；`3fa2802`+`18192f9`） | a7@wt-tf-downchain + R6 + root | `src/type_system/typefactory.rs`, docs, fixture/runner | 25/25 双侧 MATCH；R6 核实 blob 同一性+语义逐行等价；建议（alignSize>size case、B1 do-while 闭环）入后续 re-pin 批；2026-08-25 |
| `E2E-BASELINE-POSTINTEGRATION-0001` | P0 | DONE | a17 + root | 只读+result/curl_cur.c 回流 | master `8b2205a` 与 f7b3c31 基线 C 输出逐字节一致（sha `fc9a33ba…`）；ACTIONPOOL/D1/jt/fspec 集成零 E2E 影响、零回归；报告=docs/alignment_docs/E2E_BASELINE_POSTINTEGRATION_2026-08-25.md；后续基线演进：A9 后 `68d4041a…`（2377/2/1）、varmap 后 `b9f34811…`（2405/2/1，Differential 见 VARMAP 行）；2026-08-25 |
| `ACTION-EXECUTOR-BREAKPOOL-0001` | P0 | INTEGRATED（R7 合并裁决 + R10 标签裁决支持批准；4 提交→main `17e0a63..af045d4`） | a4 + R7 + R10 + root | rebase 系列（action.rs 执行器 + 5 文件窄切口 + 双 runner 重钉） | **Differential**：候选/master 的 uVar 标签两侧都不对（golden uVar=0），凡 Ghidra 规定语义处候选忠实（live 游标/opDeadAndGone/断点续跑）；+7 skeleton=5 孤儿声明（`VARMAP-ORPHAN-DECL-0001`，排队 a30 后）+7 等距 label 交换（PRINTC-UNLINKED-REF 族）；双 runner 复跑=获批投影逐字节；报告=/tmp/rugra-reports/R10-BREAKPOOL-ADJUDICATION.md；2026-08-25 |
| `JT-FIXTURES-REPIN-0001` | P1 | DONE（`f2ca3cd`/`1c83d50`/`07bc2d0`+`09ae980`/`b4fe1a1` 全在 master；三 fixture 全过零行为差异） | a40 + root | 三旧 jt fixture 组+rangeutil 行号窄修 | jt_thunk 24/24 byte-identical @candidate d9171ef；HEAD-drift 两拒为证据窗口并发提交（纪律新增：主仓 captured-evidence runner 执行窗口期 root 暂缓集成提交）；2026-08-25 |
| `E2E-MEASUREMENT-2026-08-25C` | — | MEASURED（master 含 A10+A38+A42+A39+A40+A44+A31+A43+A48 集成，**不含 A36 修复**）：skeleton 1624/defects 0/numbering 0、**0 panic 但 6 TIMEOUT**（my_get_line/file2string/parseconfig+3）、70 反编译、sha `bd436a4e…` | root | 只读测量 | **非干净数字**（超时函数 diff 缺席）；改善：helpf −64/glob_word −58/next_url −41/glob_set −33；回归：getparameter +16/match_url +12；panic→timeout 形态迁移疑与 A36 未集成的双 merge 相关；A36 集成后重测为正式基线；2026-08-25 |
| `BLOCKSTRUCT-TIMEOUT-0001` | P0 | INTEGRATED（`c7674b91`→main `e24e6896`） | a73+a56 + root | `src/{block,varnode,op,printc}.rs` | **全部函数 1-3s 恢复**；main 从不终止→7.3s；0 timeout 0 panic 首次全函数产出；真基线 skeleton 2713/0/1；GetStr 单跑字节一致（组合树 3 行差为后续集成效应） | 2026-08-25 |
| `MERGE-FORCEMERGE-PANIC-0001` | P0 | REVIEW（候选 `8dc17e4`+`2d5fe96`；R16 复核中） | a36 + R16 + root | `src/coreaction.rs` 一处调用点（租约外溢已披露：merge.rs 忠实无需改） | 根因=ActionMergeType 误调遗留 merge_all 双跑 mergeAddrTied（f4be0bb 引入，05b6b44 忠实触发）；三函数恢复 0 panic；fixture 负控制可鉴别；**⚠️ 揭示隐藏交互回归：heritage/breakpool 三连使 my_get_line 83→168、file2string 0→143（A28 干净树曾达 2012/0/0）——合流后需归因（ORPHAN/B1/B2 域候选）**；新登记 MARKIMPLIED-COUNT/BASEEXPLICIT-NUMINST；2026-08-25 |
| `COREACTION-MARKIMPLIED-COUNT-0001` | P1 | DONE（修复候选 `1ff25f92` + 本 commit @ agent/gapd-counters，待 root 集成） | gapd-agent + root | `src/coreaction.rs`（apply count+=1 经 perform 返回 1，coreaction.cc:3434+action.cc:362） | gapd_counters_1204 双侧 stdout 9 行字节一致：`act=markimplied\|res=1`（z1 单候选 implied）；负控制（dc6f0bfa）res=0 鉴别成立；merge_forcepanic_1204 的注册分歧行（同 ID）可随集成退役；2026-08-25 |
| `COREACTION-BASEEXPLICIT-NUMINST-0001` | P1 | PARTIAL（numInstances 规则已移植 `1ff25f92` + 本 commit @ agent/gapd-counters，待 root 集成） | gapd-agent + root | 切片1=`src/coreaction.rs:2866`（base_explicit 多实例规则 cc:3020-3021，已交付）；切片2=addr-tied 细化分支（SUBPIECE/loneDescend/ZEXT/PIECE，cc:3022-3063）+ isMapped/isProtoPartial 属性尾（cc:3061-3066） | gapd_counters_1204 双侧 MATCH：`act=markexplicit\|res=5`、post `m1:ex=1,im=0,at=0,hi=2`（仅 numInstances 规则可标记）；负控制 res=4 且 m1 被错误 implied；切片2 未动（a36 的 mapped-panic 症状可能还差切片2）；2026-08-25 |
| `COREACTION-MARKIMPLIED-COUNT-0001` | P1 | INTEGRATED_PENDING_REVIEW（候选 branch agent/returnfold-upstream；双侧 fixture `returnfold_upstream_1204` 22 行字节一致 MATCH；待独立 Cross-Review） | a24@wt-rfupstream + root | `src/coreaction.rs`（apply count 桥：返回计数增量镜像 cc:3434 的 count+=1，perform 观察 lcount<count→count_apply/status_end） | a36 发现；修复含于 RETURNFOLD-GAPA-UPSTREAM-0001 切片；oracle 实证 mi_count：s1/s4=1、s2/s3=0 双侧一致；2026-08-25 |
| `COREACTION-BASEEXPLICIT-NUMINST-0001` | P1 | INTEGRATED_PENDING_REVIEW（候选 branch agent/returnfold-upstream；同上 fixture MATCH；待独立 Cross-Review） | a24@wt-rfupstream + root | `src/coreaction.rs`（base_explicit 全量 port：cc:3020-3021 numInstances>1 规则 + addr-tied/mapped/protoPartial/PIECE-base 分支 + desccount>maxref→-1 修复 + multipleInteraction/processMultiplier/checkNewToConstructor）；heritage 属性尾（heritage.rs 侧 mapped/addrtied 旗标设置）不在本切片 write-set，残差保留 | a36 发现（panic 变量 flags 无 addrtied 有 mapped 与此相关）；oracle 实证 s2_merged 双 explicit（me_count=2）、s3_dup3 explicit（1 assign+3 var return）；2026-08-25 |
| `VARMAP-ORPHAN-DECL-0001` | P1 | REVIEW（根因修复候选 `270aea52` @ branch agent/orphandecl，待 root 集成 + 机制 C 独立复核——ruleaction.rs 属主管线 Rule 改动；**发射层假设证伪**：oracle printc.cc:2258-2279 emitLocalVarDecls/emitScopeVarDecls 对 no_category 符号**无**存活过滤，golden my_get_token 自身携带 pcVar3 孤儿即证；varmap.rs is_read_active/gather_varnodes 已镜像 varmap.cc:1088/1124——耦合在符号铸造上游而非打印层） | orphandecl-agent@wt-orphandecl + root | 实际 write-set=`src/ruleaction.rs`（RulePullsubMulti::build_subpiece 无条件插入修复 ruleaction.cc:776-839）+ `docs/api/ruleaction.md` + 本 TODO 行；**未触** printc.rs/varmap.rs | 根因链：旧版 build_subpiece 仅 is_written 才 op_insert_after，input 基底 SUBPIECE 滞留 dead 列表；opDeadAndGone=obank.destroy(op)（funcdata.hh:476，与 opDestroy 不同**不销毁输出**）→ 输出 Varnode 带 WRITTEN 滞留 vank → ActionNameVars::linkSymbols（coreaction.cc:2954 isFree 跳过放行）铸零读 ScopeLocal 符号 → 孤儿声明。**Differential**（golden=ghidra_curl_1204.c，基线=HEAD ac6e6795 归档 result/curl_cur.c）：全量 defects 0→0/numbering 0→0/124 函数；skeleton 3057→3055（−2=孤儿行删除）；孤儿计数 progressbarinit iVar1 1→0、my_get_token iVar2 1→0、my_fwrite 0→0（uVar1/uVar2/iVar3 已在此前 HEAD 清零）；六函数 --func defects=0/numbering=0。残留（他族已登记）：glob_word iVar3+in_R8、my_get_token __dest、glob_set/glob_range/my_get_token code_r0x 跳表标签、`RULE-PULLSUB-NEWVNODEOUT-0001`；2026-08-27 |
| `RULE-PULLSUB-NEWVNODEOUT-0001` | P2 | DONE (awaiting independent review) | orphanfix@wt-orphanfix | `src/ruleaction.rs` + `docs/api/ruleaction.md` + 本 TODO 行 | join/non-join buildSubpiece 完成：ruleaction.cc:793-821 的逆序 piece 扫描、skipleft、端序地址计算、renormalize；单片/未命中保持 unique。现有 Architecture::join_db::find_join（space.rs:503-523）足够表达 JoinRecord 查询。验收：cargo check --lib、cargo test --lib ruleaction（209 passed）、curl_decompile 全量 compare（124/124，defects=0，numbering=0）；证据 commit 7c9168b4；oracle e40ed13014025f82488b1f8f7bca566894ac376b；2026-08-27 |
| `MERGE-OVERLAPLOC-FLAGUNION-0001` | P2 | DONE-REFUTED（假设证伪 + 语义钉死；候选 @ agent/merge-overlap 待 root 集成，**白名单复核待独立 agent**） | merge-overlap-agent + root | `src/merge.rs`（first_flags→head_flags 更名+语义注释，行为零变更）+ fixture 三件套 + runner | **插桩证伪原假设**：locked overlapLoc（varnode.cc:1791-1820）gate = 各 run **头**成员 flags 联合（:1798/:1813），endLoc(size,addr,written) 跳跃（:1800/:1815）越过同位置全部后续成员——同 run 非头成员永不参与；原 Rust first_flags 折叠**已正确**（一度误修为 per-member OR，负控制证明其在负例分歧后回退）。merge_overlaploc_1204 双侧字节一致（sha `6c3d97ce…`，16 行 ×2 case：正例跨 run 头联合门控 a+b 合并/c 独立；负例同位置后置成员不门控 hi=1 same=0）；双侧 fixture 在 case 间 reset rule_onceperfunc 子 Action 否则负例退化为 status_end 空转；runner 端到端 `decisive_projection=MATCH registered_mismatch=0 overall=MATCH`。extraout_RAX/__nptr 症状**根因已另行定位**：golden `__nptr`=strtol 首参名 `nptr`（`__needle`=strstr 参数同理），经 `ActionNameVars::lookForFuncParamNames`（coreaction.cc:2858-2897 makeRec）从 callee DWARF 原型参数名推荐；curl_decompile 的 `DebugPrototypeDatabase` 只导入 `(address, parameter_count)`——参数名丢失故推荐永不触发 → 见新登记 `COREACTION-FUNCPARAMNAMES-RECOMMEND-0001`；2026-08-25 |
| `JUMPTABLE-THUNK-CLASSIFY-0001` | P0 | INTEGRATED（R2 独立复核 APPROVE + 完整重跑复现；9 提交 `8e09e59..dd16526` 已入 main；docs 措辞已修 `58ff6b7`） | a3@wt-jt-thunk-classify + R2 + root | `src/jumptable.rs`, docs, fixture/runner | 24-case 双侧字节一致（stdout sha `c4cc2b35…`）；10 双侧组 MATCH、3 审计组 MISMATCH 诚实保留；5 条前任 REJECT 全闭合；残差绑 `JUMPTABLE-PIPELINE/SORT-TOOLCHAIN/EMULFN-0001`；S2（metadata SORT residual 残句需重钉修复）挂 P2 待后续 runner 重钉批；报告=/tmp/rugra-reports/R2-JTTHUNK-REVIEW.md；2026-08-25 |
| `RULEBLOCKOR-ISCOOMPLEX-0001` | P0 | REVIEW（候选在 worktree `agent/myfwrite-cfgloss`；blockaction.rs 属核心算法白名单，并入 main 前需机制 C 独立复核；含前任未提交 5 文件续做：block.rs is_complex 全量移植 + blockaction.rs selfIdentify 外部边改写/dedup/剥离 + coreaction/fspec 锁定输出读插入 + printc DEAD 守卫上移） | a-myfwrite-cfgloss | `src/{block,blockaction,coreaction,fspec,printc}.rs` + docs/api 5 篇 | **Differential**：my_fwrite 空体 `if(…||…){}` 消失、fwrite/return 恢复（基线整函数只余 `lVar1=*stream;`）；curl 全量 defects=0/numbering=1（与基线同 1）；skeleton 1590→1778（+188：此前发射侧过保守丢体，现恢复的函数体大于新增折叠差异，非缺陷形态，逐函数结论以 --func 为准）；剩余 my_fwrite 骨架 17 行绑 `MYFWRITE-TEMPVAR-0001`；cargo test 22 fail 中 3 新增者隔离运行全 PASS（并行污染非回归）、1 修复；2026-08-25 |
| `MYFWRITE-TEMPVAR-0001` | P1 | IN_PROGRESS（依赖 RULEBLOCKOR-ISCOOMPLEX-0001 已在基线 c95b8459；branch agent/myfwrite-tempvar rebase 至 master ef26eb24） | a-tmpvar@wt-tempvar | `src/{action,coreaction,heritage}.rs`（ActionReturnRecovery/ActionReturnSplit 链 coreaction.cc:1908-1955/3270-3345 + RuleCondTerms implied 条件保持） | my_fwrite 剩余 17 行骨架差：Ghidra 经 __s 临时变量链（__s=read_ptr; if(__s==0){__s=fopen(...); store; if(__s==0) return -1;} return fwrite(...,__s)），Rugra 直接条件比较+裸 fopen 嵌套+fwrite 末参仍用 stream（__s 数据流未贯穿）+`return uVar_0` 未接 sVar2；单函数 fixture 待建；2026-08-25 |
| `LANEDIVIDE-EVIDENCE-STATUS-REPAIR-0001` | P0 | INTEGRATED（main `2c9f27c`） | a5@wt-lanedivide-repin + root | `tests/oracle/lanedivide_infra_1204.metadata.json`, `tools/run_lanedivide_infra_oracle.sh` | 候选=`fc8df38`；base 4599b50 双侧真实重跑 3 次（含与提交 tree 一致复跑），11 MATCH + 1 NO_ORACLE（`LANEDIVIDE-INFRA-RESIDUAL-0001` 未缩），doctor 维持 MATCH_WITH_UNMATCHED_RESIDUAL；三件套重钉 + oracle fixture rpath 环境修复；registry 条目维持 MATCH（勿改 UNTESTED，历史混淆系 `action_lanedivide_1204`），落库归 A12 批；master src 树自 4599b50 未变、pin 有效已核；2026-08-24 |
| `GATE-WORKTREE-ROOTMISMATCH-0001` | P0 | INTEGRATED（main `bc2fd2f`） | a6@wt-gate-rootmismatch + root | `.zcode/align_gate.py`, `tools/check_gate_health.py`, CI yml, HOOK_GUIDE | 候选=`d49ccf5`；跨根 toplevel 重锚定 + fail-closed + self-test 4 跨根用例；root 集成后主仓复验 self-test/gate-health/crossroot-payload 三绿；审计=GATE_HEALTH_AUDIT_2026-08-24；2026-08-24 |
| `TYPEFACTORY-DOWNCHAIN-VIRTUAL-0001` | P0 | IN_PROGRESS | a7@wt-tf-downchain | `src/type_system/typefactory.rs`, docs, `tests/oracle/typefactory_downchain_virtual_1204.*`, runner | B1 方案切片1（地基，无 production caller）；2026-08-24 |
| `TYPEOP-FSPEC-SPACE-0001` | P0 | PARTIAL（切片1 已集成 `d75f1bb..2286279`；R5 独立复核 APPROVE `0ebcbae` 归档） | a8@wt-fspec-space + R5 + root | 切片2=`src/{varnode,funcdata}.rs` 消费侧迁移 + D0 fixture 重钉 | 切片1=5/5 双侧 MATCH；R5 建议 S1（pcoderaw.cc:107→33 行号漂移）、S2（decode_with_size 起始 *size=0）、S3（悬空槽直接探针）、S5（fspec.rs:2977+ 死孪生删除）并入切片2 范围；残差归 MARSHAL-XML-TEXT-0001 与 SPACE-IOP-PRINTRAW-0001；2026-08-25 |
| `TYPEOP-FSPEC-CONSUMER-0002` | P0 | BLOCKED（等 A2 集成释放 funcdata.rs；varnode.rs 已空闲） | 待发射 | `src/{varnode,funcdata}.rs`, docs, fixture 重钉 | newVarnodeCallSpecs 迁 fspec 空间地址 + getFspecFromConst 注册表接管（fspec.hh:1733/funcdata_varnode.cc:210）；完成即消 D1 的 fspec_name/fspec_type 两行表示残差；2026-08-25 |
| `JUMPTABLE-SORT-TOOLCHAIN-0001` | P2 | DONE | orphanfix@wt-orphanfix | `tests/oracle/jt_thunk_classify_1204.{cc,rs,metadata.json}`, `tools/run_jt_thunk_classify_oracle.sh` + 本 TODO 行 | 新增 `sort_equal_thirtytwo`：32 个同 Address、size 交替的等键输入，覆盖 >16 sort 阈值并双侧 byte-identical；Ghidra `jumptable.cc:58-111` 确认 sort 比较键仅 Address，等键次序由 libstdc++ 实现而非 C++ 标准规定。runner exit=0，25/25 双侧一致，focused tests 35 passed；最终复跑 candidate tree `a2d50b29`、bundle `jt-thunk-classify-evidence-8268c38faded50cbd0cb16be1c58df1f70aefaba`，stdout 双侧 sha `1c517359…`；证据 commits `164e162d`/`8583bc7f`，oracle e40ed13014025f82488b1f8f7bca566894ac376b；2026-08-27 |
| `FLOW-TAILCALL-OVERTRACE-0001` | P0 | INTEGRATED（`3db1f29`→main `bdc7f34`；root E2E 验证 ✅） | a9@wt-flow-overtrace + root | `src/flow.rs`, docs, fixture/runner | **假设修正**：Ghidra 无函数体边界，真根因=query_call no-op 致 fd 守卫不触发；双侧 61 行 MATCH + pre-fix MISMATCH；master 集成树 E2E 实测：skeleton 2409→**2377**、PIC 5→**0**、glob_word 176→144、defects=2/numbering=1 零新增（本 wave 首次全局改善，result/curl_cur.c 已回流）；flow.rs 释放；2026-08-25 |
| `FLOW-NORETURN-DATA-0001` | P1 | QUEUED（examples/curl_decompile.rs 被 a20 占用；与 a18 的 fspec noreturn 位汇合后做驱动侧名单喂入） | 待 a20/a18 后 | `examples/curl_decompile.rs` + 可选 `src/funcdata.rs` | Ghidra Known No-Return 名单（exit/abort/__stack_chk_fail…）→ followFlow 前锁 callee no_return；预期 glob_word 344→323；2026-08-25 |
| `INFERTYPES-CALLINPUT-D2-0001` | P0 | INTEGRATED（R19 Cross-Review APPROVE；`a95562f`+`dfba5ec`→main `1c6458ee`+`a26bbab9`，丢弃与 b55ef7f 等效的 1f1481c；接缝修复 `94110fe5`；编译绿） | a21 + R19 + root | `src/{coreaction,typeop}.rs`+docs+fixture | R19 判定：无 double-seed（A43 的 varnode get_local_type 零生产消费者，D2 补其 CALLIND 缺口）；调和终局=DISPATCH 收官时以 A43 为唯一播种源退役 coreaction 臂；E2E 差分押后至 A56 合测；2026-08-25 |
| `JUMPTABLE-PIPELINE-0001` | P0 | 段1 INTEGRATED（R12 Cross-Review APPROVE + rangeutil 追认；`88ede48`→main `73b5ef2`，编译绿） | a22 + R12 + root | 段1 `src/jumptable.rs`+`src/rangeutil.rs`（追认：next wrapping+get_size 溢出 lie-by-one 反例验证） | 8/8 双侧 MATCH；SELECTION/EMULFN 关闭；Trivial 删除正当；R12 建议 A（行号 hh:82）随重钉批；段2（funcdata+fspec+coreaction）待认领；旧 jt fixtures 重钉=A40；2026-08-25 |
| `FLOW-NORETURN-DATA-0001` | P1 | **DONE 全链**（a `c1598da`/s2 `ac4691a`/c `dfe87ff`；getstr 迁移随 root） | a18+a25+a28 + root | examples 三段 + getstr_stage_snapshot.rs | **本 wave 首个 defects=0/numbering=0**：skeleton 2405→2012、file2string 全 MATCH、my_get_line 160→83、glob_word 144→81；⚠️ 该测量不含 merge-panic 三函数（A36 修后合测）；残差→`BLOCKSTRUCT-NORETURN-DEADREGION-0001`+`PRINTC-WARNING-COMMENT-0001`；2026-08-25 |
| `BLOCKSTRUCT-NORETURN-DEADREGION-0001` | P0 | INTEGRATED（`7beb78e8`→main `2b4183f8`，编译绿） | a50 + root | `src/blockaction.rs`+docs+fixture | goto-edge 可见性/target 消费/cat-chain 语义；待复核 | 2026-08-25 |
| `PRINTC-WARNING-COMMENT-0001` | P1 | QUEUED（printc.rs 等 a30） | 待 a30 | `src/printc.rs`（commentdb WARNING 注释发射，golden 16 vs 0） | a28 段c 发现；2026-08-25 |
| `PRINTC-IFGOTO-EMIT-0001` | P1 | DONE（候选 @ agent/ipgoto-emit：emit_structured_if goto_target 臂 + goto-cascade 顺序回退臂按 emitBlockIf cc:2894-2916 移植 + emit_goto_statement 去 tagLine；待 root 集成） | a-ifgoto | `src/printc.rs` + `docs/api/printc.md`（printc.rs 独占租约） | E2E（HEAD 干净树对照）：goto 0→22（golden 61）、skeleton 2927→2923、defects 0/numbering 1 不变、多次重跑字节一致；16 处裸 `(cond);`/条件丢弃形态恢复为 `if (cond) goto <label>;`；残差=flat opCbranch 全臂（stash `flatcbr-prev-agent-diff` 的 FLAT mod+op_cbranch_rpn 未含本轮）与 LAB_ 符号化（varmap 域）；2026-08-25 |
| `PRINTC-UNLINKED-REF-FAMILY` | P0 | C=A39 已钉；B1 DONE；**A DONE**（`59fc42bc`→main `a9351d26`，33/35 MATCH+uVar_全族消除+skeleton −29）（分支 `agent/printc-b1-addrsrc`：fallback 地址源统一 high 代表，line 31 碎片化轴闭合，runner 重钉后 covered=29/35 全 registered 无 stale）；B2/A 排队 | a35 + a39 + a-b1 | B1=`src/printc.rs`+`docs/api/printc.md`+`tools/run_printc_unnamed_oracle.sh`（overlay 机制+基线重钉 9bdd5e3）+metadata 重钉；B2=符号化到达主门；A=pushUnnamedLocation 形式+三阶梯合并 | B1 证据：state-1 drift（`uVar_10000008`→`uVar_10000000`）→重钉→PASS；行 7/23/30/31 仅剩形式差异（A 片）；洞察：修 B2 使标签与执行序解耦；交换族 16/16 属创建序域禁追数值一致；报告=/tmp/rugra-reports/A35-PRINTC-UNLINKED-REF.md；2026-08-25 |
| `CALLSPEC-NORETURN-WIRE-0001` | P0 | 段(a)+(b) INTEGRATED（`7b1da21`+`b8da22a`）；段(c) A28 修中（callee_func_protos 表） | a18 + a25 + a28 + root | 段(a) fspec.rs / 段(b) flow.rs（4 接线点+halt dead-insert 缺陷修复）/ 段(c) examples | 段(b) fixture 5/5 MATCH；E2E 零变化=设计预期（数据源待段c）；合流三指标验证（warning/extraout/skeleton）在段c 回收时做；2026-08-25 |
| `B3-COREACTION-CONSTANTPTR-0001` | P0 | 段(a0)+(a1) INTEGRATED（`e20ca72`+`debc817`+`c9daaeb`+`dcead22`）；段(b) INTEGRATED（src=`8474ec13`；双侧 fixture=`cptr_b_1204` 17/17 records MATCH，runner 重跑 exit 0 @HEAD） | a20 + a23 + cptrb@wt-cptrb | 段(a1) `src/{database,funcdata}.rs`：query_container/query_properties 真通道+resolvemap 分裂+map_scope qpoint 修正；18/18 双侧 MATCH | 段(b) 交付=ActionConstantPtr::apply 主体（src 8474ec13）+ 双侧 fixture `tests/oracle/cptr_b_1204.{cc,rs,metadata.json}`+`tools/run_cptr_b_oracle.sh`：六 hugehelp 别名形态（3×无效UTF-8→&DAT PTRSUB、3×字符串→char-ptr PTRSUB）+ needexact/char-array/bounds/bitform 拒绝 + skip/CALL 臂，17 记录双端逐字节 MATCH（锁定 oracle 重建，身份四元组校验）；残差=string_literal_fold_chain/high-attach 两覆盖条目 UNTESTED（登记 PRINTC-PTRCHAR-CONSTANT-0001/FUNCDATA-LINKSYMBOL-TYPED-0001）；段(a0) driver 生产安装待用 add_symbol_mapped 接线；2026-08-26 |
| `BLOCKSTRUCT-GOTOCASCADE-CONDSTMT-0001` | P0 | INTEGRATED（R14 Cross-Review APPROVE；`a38a9b5`+`61eaf89`→main `cddcefd`+`5cf34c1`，编译绿；E2E 测量在途） | a10 + R14 + root | `src/{blockaction,tracedag}.rs`+funcdata 测试断言（披露）+docs+fixture | 11 处根因修复（siblingedge 方向/公共祖先距离/per-loop TraceDAG/collapseAll 无前置标记/ruleBlockGoto 任意 slot/守卫取反…）；双侧 6 case 核心 MATCH + 110 行 registered MISMATCH→`BLOCKSTRUCT-IDENTIFY-BOUNDARY-0001`；附带暴露 `PRINTC-SWITCH-EMIT-0001`；机制 C 白名单；2026-08-25 |
| `BLOCKSTRUCT-IDENTIFY-BOUNDARY-0001` | P1 | QUEUED（等 R14 后串行，blockaction.rs） | 待 R14 | `src/blockaction.rs`（identifyInternal+selfIdentify+dedup，block.cc:893-960） | 验收=goto fixture 110 行 diff→0；2026-08-25 |
| `PRINTC-SWITCH-EMIT-0001` | P1 | QUEUED（依赖 a10 集成；printc.rs 等 a42） | 待 a42+a10 集成 | `src/printc.rs`（emit_structured_switch 首例 case 标签落入换出 buffer）+funcdata 测试断言恢复 | 验收=switch 断言恢复+curl switch case 完整；2026-08-25 |
| `PRINTC-FLAT-CBRANCH-0001` | P1 | DONE（@ agent/flat-cbranch worktree，待 root 集成；与 ifgoto 交付（goto 臂 0→22）相邻域，集成时按 hunk 拆解） | flatcbr-agent + root | `src/{printc,prettyprint}.rs`, `docs/api/{printc,prettyprint}.md`, 本 TODO 行 | flat opCbranch 全臂（cc:536-580：FLAT mod 镜像/booleanflip/checkPrintNegation 折叠/boolean_not token/fallthru_true 取反/FALSEBRANCH transport）+ emitBlockBasic 尾部（cc:2685 emitLabelStatement label 补齐 + cc:2723-2741 nofallthru tail goto，双路径 RPN+legacy）+ prettyprint Pattern 5 排除 code_ 标签；E2E：50 处裸条件语句→if-goto、41/41 goto 有 label（此前 39 无 label）、假 return code_r0x 0、skeleton 2927→2908、audit 53→55 OK、gcc label-undefined 2→0；残差：4 个无引用 orphan label（goto_prints/nextFlowAfter 未对齐，登记 `PRINTC-GOTOPRINTS-0001`）；2026-08-25 |
| `PRINTC-GOTOPRINTS-0001` | P2 | OPEN | unassigned | `src/block.rs`（BlockGoto::goto_prints 现保守 true；block.cc:2881-2890 需 parent nextFlowAfter 对接）+ `src/printc.rs`（label 补齐处消费） | flat tail goto/label 补齐后仍产 4 个无引用 orphan label（curl）；修法=goto_prints 对齐 + flat label 只对真打印的 goto 目标发射；2026-08-25 |
| `MAINDIFF-UNIQLEAK-0001` | P0 | DONE（@ agent/uniqleak worktree，待 root 集成；ruleaction.rs 非机制 C 白名单但包含前任未提交 4 文件续做：space.rs delay 模型 + funcdata.rs linkSymbol 全局半边 + heritage.rs 测试同步 + examples symboltab 播种） | uniqleak-agent + root | `src/{ruleaction,space,funcdata,heritage}.rs`, `examples/curl_decompile.rs`, `docs/api/{ruleaction,space,funcdata,heritage}.md`, 本 TODO 行 | **根因三层**：① space.rs get_delay 硬编码 Ram=0/Stack=1（oracle x86-64.sla: ram=1/stack=2/unique=register=0，stack 由 architecture.cc:566 ram+1 合成）→ heritage 时序错位；② linkSymbol 缺全局符号半边（Scope::queryProperties database.cc:1263 父作用域走查）→ 37 个死 `in_ram_` 声明；③ **RuleSborrow/RuleScarry 仅 trivial 分支**（ruleaction.cc:3376-3410/3447-3492 AddExpression 深形式缺）→ gcc 循环守卫 `sborrow(argc,1) != (argc-1 s< 0)` 不折叠 → implied flag varnode 被 ActionNameVars 正确跳过（hasName=false）但打印侧未内联 → pushUnnamedLocation（printc.cc:1938）裸名 + prettyprint 文本回填声明。**修复**：RuleSborrow/RuleScarry 深形式全量移植（依托已有 expression.rs AddExpression/functional_equality）+ linkSymbol 全局半边（query_global_symbol_hit，仅 Ram 门控）+ delay 模型对齐 + driver symboltab 播种（DWARF+ELF STT_OBJECT）。**验证**（flock + /home/wirs/.cache/uniqleak-target）：main unique0x/register0x 泄漏 2→**0**、main 声明区 163→**96** 行（golden 53）、main skeleton 626→558、`if(argc<2)` 与 golden 字节同形；全量 defects=0、0 panic、skeleton 2863→2761；numbering=1（parseconfig.constprop.0 per-prefix 启发式，隔离证明=前任 4 文件单独引入、与 rule 移植无关，见 ## Differential）；cargo test --lib 单线程 19 失败与 HEAD 集合逐一相同（零回归），sborrow/scarry 3 测试绿（fixture 补 output 镜像 oracle `op->getOut()` 不变量）；result/curl_cur.c 已回流 | 2026-08-25 |
| `FLOW-JUMPTABLE-GETPARAM-0001` | P0 | DIAGNOSIS_DONE（修复并入下行） | a11（只读） | 无写；报告已归档 GETPARAM_JUMPTABLE_DIAG_2026-08-24.md | 根因=恢复跑在 raw Funcdata（无 partial 克隆/策略简化/SSA/基本块）且 flow.rs:2561 静默吞失败；2026-08-24 |
| `MAINDIFF-DEADSTORE-0001` | P0 | DIAGNOSIS_DONE（任务前提证伪：ActionDeadCode 无缺分支；无代码改动，探针已撤） | deadstore-agent@rugra-wt-deadstore | 无写（本 worktree 调试探针全部清除，工作区净） | 锁定 oracle 实证（/tmp/ds_oracle 自建 instrumented 12.0.4 e40ed13 + bfd-2.38 直跑 driver=tools/regen_ghidra_golden.py 内嵌 golden_dump_1204.cc，输出与入库 direct-runner golden 字节一致，104/104 `Stack_260=` 全保留）：①Rugra ActionDeadCode 逐分支对照 coreaction.cc:3925-4064 完整（clear/预存活/主循环/markConsumedParameters/gatherConsumedReturn/lastChanceLoad/消除循环/seenDeadcode）；②双侧 flag 状态 dump 一致——fda0（-0x260）guard-INDIRECT 版本 oracle 也是 af=1 dw=1，保护路径=isAutoLive(addrforce)+DirectWrite 污染 → pushConsumed(~0)（cc:4003-4009），非 Rugra 缺陷；③正典 headless golden 0 死存储=分析器/DWARF 环境输入差（Rugra driver 105 callspec 仅 24 libc 锁定，curl_version 仍 6 试验参数 vs headless `curl_version()`），非库内分支；④Rugra 现存 4 死存储=入口直线路径纯 INDIRECT 链（0x25f7/0x2608/0x276c/0x3327，恰为 direct-runner 前 4 个）；后 ~100 个随 main 结构性丢块一起缺失（strnequal/parseconfig 分支整段不在输出），归 main 结构域非 deadcode。干净树 E2E：defects 0/124、numbering 0、lStack_260=4、skeleton 2863、result/curl_cur.c 已回流。oracle 环境重建配方：cp cpp→/tmp/ds_oracle，`make decomp_opt BFDHOME=/tmp/rugra-ghidra-bfd-2.38/usr`，driver 链接 `-l:libbfd-2.38-system.so` + rpath（系统 bfd 为错配 segfault 源）；2026-08-25 |
| `ACTIONDW-COPYDEF-MARKING-0001` | P1 | DONE（@ agent/actiondw worktree，待 root 集成 + R-agent Cross-Review；源码修复 `e1140912`（coreaction 收集段+phase2 门+fspec FuncProto::possible_input_param+varnode is_stack_store+action.rs 双注册接线），fixture 三件套+runner+registry 登记在后续钉住 commit） | actiondw-agent@rugra-wt-actiondw + root | `src/coreaction.rs` ActionDirectWrite + `src/{fspec,varnode,action}.rs` 新增方法/接线 + `docs/api/{coreaction,fspec,varnode,action}.md` + `tests/oracle/actiondw_copydef_1204.{cc,rs,metadata.json}` + `tools/run_actiondw_copydef_oracle.sh` + `tests/oracle/fixture_registry.json` + 本行 | 四偏差全部修复：①COPY-def 收集不标（isStackStore 追踪例外，单层源解开边界锁定）；②possibleInputParam 分支（FuncProto 前奏完整移植，锁定环 inert 降级与兄弟移植同口径，见 fixture known_residuals）；③isStackStore→marker 源追踪；④marker(INDIRECT) 地址变化/persist 分支 + propagate_indirect 字段（a=true/b=false 四注册点）。fixture：7 case×双注册×before/after=28 records 双侧字节一致 MATCH（含 oracle 深层语义锁定：④的 no-push 标记使 phase-2 `!isDirectWrite` 守卫跳过 mark+push 永久阻断 taint；isIndirectStore 推播腿经等址 0xe0 构造判别）；E2E：defects 0/124、numbering 0、skeleton 2863→2839、lStack_260=4 处不变（0x25f7/0x2608/0x276c/0x3327）、0 panic/timeout，result/curl_cur.c 已回流。残差（fixture known_residuals 已登记）：FuncProto 锁定参数前奏环 UNTESTED（需 ProtoParameter space/size 存储）；Funcdata::setInputVarnode unaffected 派尾巴 MISMATCH（bank 层独立债）；FuncCallSpecs::possible_input_param 仍缺前奏（独立债）；2026-08-26 |
| `JUMPTABLE-PIPELINE-0001` | P0 | BLOCKED（三段串行） | 段1待 a3 释放 jumptable.rs；段3待 a9 释放 flow.rs；段2待 a2 释放 funcdata | 段1 `src/jumptable.rs`（EMULFN loader 桥+失败语义、Assisted→Basic→Basic2 选择、recover_model 改调 find_normalized）；段2 `src/{funcdata,fspec,coreaction}.rs`（truncated_flow+stage_jump_table+jumptable 策略组）；段3 `src/flow.rs`（generate_ops Phase2 接 recover_jump_tables+truncate/RecoveryMode） | 审计=GETPARAM_JUMPTABLE_DIAG_2026-08-24；验收=getparameter.constprop.0 88 case 可达 + A/B/C 三观察面 fixture（报告 §6）；四类语义核对 5 组签名在报告 §8；2026-08-24 |
| `JUMPTABLE-PIPELINE-0001` | P0 | 段2+段3 DONE @ agent/jumptable 待 Cross-Review（前任 a2/a9/a20 死于重启，本 agent 续做收尾：ElfSegmentLoadImage loader 桥 + recoverMultistage/checkForMultistage 补齐 + ActionSwitchNorm 去自创预扫） | a-jt-pipeline（本 worktree）+ 待指派 reviewer | 段2 `src/{funcdata,coreaction}.rs`；段3 `src/flow.rs`；追认 `src/jumptable.rs`（recover_multistage/check_for_multistage）；`examples/curl_decompile.rs`（worker Architecture loader）；docs/api 四件 | 验收达成：getparameter.constprop.0 @0x3fd5 88 条目恢复（flow 665 ops/36 块→1866/138，[JUMPTABLE] 日志 0→出现）+ glob_set @0x4c45 35 条目；124 函数 defects=0/numbering=0；单线程测试失败集与 baseline 恒等（18=18）；根因=worker Architecture 无 loader（EmulateFunction LOAD 读不到跳表字节→"Could not emulate address calculation"→fail_normal→truncate）；残差→`PRINTC-CALLIND-PTR-0001`（PLT CALLIND 打印）+ `JUMPTABLE-TABLEAPI-0001`（switch 渲染）；skeleton 2863→2925（恢复的 88 case 体以 goto 形态出现，须 Differential 解释）；机制 C 白名单——merge 前需独立 reviewer；2026-08-25 |
| `PRINTC-CALLIND-PTR-0001` | P1 | PARTIAL（agent/markimplied2；R-printc 复核待定） | markimplied2 | `src/printc.rs` + `docs/api/printc.md` + 本行 | Oracle `e40ed13014025f82488b1f8f7bca566894ac376b`; legacy inline 与 RPN CALLIND 已分离为 `(*(code *)<in0-expr>)(args)`，PLT 形态切换（如 `PTR_00116e80`）；全量 defects=0/numbering=0/skeleton=2678；嵌套场景 `= PTR_...` 已消失，但返回值调用形仍有约 80 处 audit 失败；audit 28/95；2026-08-27 |
| `REGISTRY-CONTINUITY-W1-0001` | P0 | INTEGRATED（R1 独立复核 7/7 APPROVE；main `8cb8829`+`4e20dc3`+`7b4b29b`） | a12@wt-registry-w1 + R1 + root | 同原 write-set + PROTOCOL_TABLE | checkpoint 9640→9726（+90−4 账目闭合，append-only 逐字节验证）；rc2→rc1；registry 落库后 lint 462→446（余为 schema 存量长线）；报告=/tmp/rugra-reports/W1-REVIEW.md；2026-08-25 |
| `REGISTRY-CONTINUITY-W2-0001` | P1 | QUEUED（`bd8e38e` 后 checkpoint 已滞后；待 R2/R3 批次落库后一并前滚） | 待批次积累 | 同 W1 模式 | append-only 前滚 + 三件套再生 + 独立复核；2026-08-25 |
| `REGISTRY-CONTINUITY-W3-0001` | P1 | INTEGRATED（R21 复核 7/7 全量验证；`188091a8`→main `fb73c881`+`308f9867`；9836→10058 零未解释；W4 节奏启动） | a68 + R21 + root | 同 W2 write-set | 46/46 transitions+25 tombstones 全量验证（非抽样）；两跳链 base 修正独立确认；2026-08-25 |
| `B3-COREACTION-CONSTANTPTR-0001` | P0 | BLOCKED（三段串行） | 段(a)待 a2 释放；段(b)待 D2 释放 coreaction | 段(a) `src/{funcdata,database}.rs` + `examples/curl_decompile.rs`（.rodata DAT 条目 + query 通道接线）；段(b) `src/coreaction.rs` 重写 Action 主体 + `src/funcdata.rs` 修 spacebase_constant；六常量 fixture/runner | 审计=HUGEHELP_CONSTANTPTR_AUDIT_2026-08-24；验收=六常量 oracle fixture MATCH + hugehelp 六行字节对齐；机制 B 白名单需 Differential 块；前置警告=先修 string UTF-8 判定再放行 Rule 折叠（driver string_table 现保留 0xAD 串，反转风险）；2026-08-24 |
| `PRINTC-CAST-OPNAME-0001` | P1 | **M1 DONE**（`4208320`+`cba6003`，16 行翻 MATCH/102 records 全等）+ **M2 DONE**（A43 varnode getLocalType 已集成）+ M4 DONE（9d0674c）；仅剩 M3=coreaction build_localtypes（随 D2 集成） | a38+a43+a30 + root | typeop.rs 宏族 41 对构造器 metatype/varnode.rs/coreaction.rs | 衍生 `TYPEOP-PTRADD-LOCALBASE-0001`（P2）+`PRINTLANGUAGE-SHIFT-EMITTER-0001`（P2，opIntLeft 族 emitter 缺失）；2026-08-25 |
| `STRINGMANAGER-CORE-JAVACONTRACT-0001` | P0 | INTEGRATED（`82770c9`；18/18 双侧 MATCH） | a13@wt-stringmanage-core + root | `src/{stringmanage,arch}.rs`, docs, fixture/runner | Address 键+正/负双缓存+Java 契约（native 2048 界与契约无界两 oracle 行为同 fixture 锁定）；0xAD→-1 判定链；**43 分钟死锁根因=RwLock 重入，已修**；消费侧接线归 D3；后继小项：UTF16/32 manager 级 fixture、XML space-name 恢复（均 P2）；2026-08-25 |
| `PRINTC-PTRCONST-DAT-SYMBOL-0001` | P1 | FIXTURE_MATCH（`59d4402`→main `01712a2`）；生产接线待 D3；M4 由 a30 修中 | a24 + root | `src/printc.rs`（push_ptr_char_constant 全链/print_character_constant/escape/TYPE_SPACEBASE 臂/M5 finalcast）+ docs + fixture | 双侧 14+5 记录 MATCH（TRUNCATED 2098 字符/负缓存零重读/上下文分辨）；E2E 零变化=预期（无生产调用点，接线属 D3）；printc.rs 租约已释放；2026-08-25 |
| `FSPEC-JUSTIFIED-CONTAIN-0001` | P0 | INTEGRATED（`8cd31fd`→main `5a6d13d`；63/63 双侧 MATCH） | a26 + root | `src/fspec.rs`（两条独立卫语句对齐 address.cc:131-141）+ docs + fixture | 旧代码三类几何误判含 dev 构建 panic；fspec_phase0 投影零回归；衍生 `FSPEC-JUSTIFIED-ENDIAN-0002`+`RESOLVER-GATE-0003` 由 a31 合并修中；2026-08-25 |
| `TYPEOP-TEST-BUILD-0004` | P2 | FIXED（main `b55ef7f`；root 亲修） | root | `src/typeop.rs` 测试模块 | E0423（TypeOpBranch 非 unit struct 当值用）曾阻塞全仓 cargo test --lib；改构造实例后恢复；2026-08-25 |
| `HERITAGE-GUARD-SUBPIECE-CONST-0001` | P1 | INTEGRATED（R18 Cross-Review APPROVE；`8d138b92`→main，编译验证在途） | a45 + R18 + root | `src/heritage.rs`+docs+fixture | R13-发现A 修复 + 三同族新发现（insertPoint 游标/opSetOutput 接线/读锁自死锁——首次真实执行即死锁）；before①死锁/before②三差异/after 全等证据链；残差→TRYOUTPUT-STACKGUARD-CONTAINS（cc:1407-1430 第 4 触点）；2026-08-25 |
| `FSPEC-JUSTIFIED-ENDIAN-0002`+`RESOLVER-GATE-0003` | P1 | INTEGRATED（R13 Cross-Review APPROVE；`d32a005`+`b54b24b`→main `3f07f96`） | a31 + R13 + root | `src/{fspec,heritage}.rs`+docs+fixture+justified_contain 重钉 | R13 独立重建 oracle 复现+等价性字面证伪但结果等价验证；发现 A→`HERITAGE-GUARD-SUBPIECE-CONST-0001`（a45 修中）；FINDENTRY-0005→a46 修中；2026-08-25 |
| `FSPEC-SPACELESS-REMAINDER` | P0 | REVIEW（候选分支 `agent/fspec-spaceless-v2`；fspec_spaceless_rem_1204 双侧 31/31 行 byte-identical stdout `485c2b85…`，covered=MATCH/overall=UNTESTED；fspec::tests 31/31；phase0 runner 重钉后复绿 stdout `69c76294…` 与原 pin 逐字节相同=签名变更零行为漂移） | fspec_spaceless_writer + root | `src/fspec.rs`（unjustified_container cc:1411 签名增补 space + fillin_map_fallback cc:1656/cc:1702 两处 trial 查询改 justified_contain_in_space 并删自创 get_space()==守卫）+ `docs/api/fspec.md` + `tests/oracle/fspec_spaceless_rem_1204.{cc,rs,metadata.json}` + `tools/run_fspec_spaceless_rem_oracle.sh` + phase0 重钉 `tests/oracle/fspec_phase0_1204.{rs,metadata.json}` | A46 残差 4 收口（possibleparam metadata residual 登记）；旧形态实证分歧=stack 查询数值巧合命中 register/ram 容器、join output entry 在 fallback 永不可达（bestentry null→全部 markNoUse）；phase0 重钉修复其 55ef9943 起的 fspec_rs/fspec_doc 双 stale pin 并补上同 commit 遗漏的 assumed_extension 签名调用点；衍生 `FSPEC-PIN-STALE-0002`（possibleparam/findentry/endian_resolver pin 滞后）；2026-08-25 |
| `FSPEC-PIN-STALE-0002` | P2 | DONE（a85 主仓重钉批；三 fixture 双侧真跑复绿且 stdout 与原 pin 逐字节相同=`8d9aaf66` fspec.rs 改动零行为漂移：possibleparam `ab31ac37…`/findentry `440d81cd…`/endian_resolver `2d59318a…`，均 OK covered=MATCH overall=UNTESTED） | a85 + root | 三 fixture 的 `metadata.json`（fspec_rs_sha256→`6f6e25c6…`、fspec_doc_sha256→`f0884bd5…`、rust_crate_tree_sha256→`adb6fb46…`/`2e9748d1…`/`73cd850f…`、runner_sha256）+ 三 runner（fixture_target 迁 `/home/wirs/.cache/a85-fspin-target/<name>` 专属 CARGO_TARGET_DIR + cargo 行 `timeout 600`）+ TODO_BOARD | 重钉算法先以 pin 时刻 fspec.rs blob 复现三个旧 crate_tree pin 全中后才采信新值；overlay=旧 base（b8c70e8/3f07f96/d32a005）+live fspec.rs 三组均编译通过（旧树对 A76 改动 API 零调用面：unjustified_container/fillin_map_fallback 无 crate 内 caller）；fixture .cc/.rs 未动；2026-08-25 |
| `VARMAP-LOCALWINDOW-0001` | P0 | INTEGRATED（R8 复审翻 APPROVE；`e7ae84f`+`50e24c1`→main `c042d9a`+`4fcfc1b`） | a14 + R8 + root | `src/varmap.rs`, docs, fixture/runner | **Differential（机制 B）**：E2E skeleton 2377→2405（+28）、defects=2/numbering=1 持平；劣化归因=窗口条目×未修症状2 的交互（负偏移局部进窗后被 extraout/投机实参命名放大，`my_get_line` 151→160、helpf/file2string/parseconfig 各+6）；`match_url` −3、`_start` −1 改善；**修复方向 oracle 级已证（R8+fixture），保留集成，预期 A25+A28（noreturn 链）落地后回收**，届时复测 my_get_line 不恢复则专项定位；新基线 sha `b9f34811…`；2026-08-25 |
| `FLOW-NORETURN-DATA-0001` | P1 | DRIVER_DONE+补齐中（`a8ab0d8`→main `c1598da`；pre-flow callee 标记半边由 a28 追加） | a28 + root | `examples/curl_decompile.rs`（21 项 ELF 名单逐字自 oracle git objects、剥 `_`+大小写敏感匹配、8/8 单测；E2E 零 delta 符合预期） | 数据源 12 函数命中（my_get_line=3/glob_word=5/glob_set=3/helpf=2/match_url=3）；合流架构裁决=analyzer 先标 callee 函数属性（pre-flow 半边补齐中）+flow 期 queryCall 拷贝到 callsite；合流验收项=warning 0→≥1、extraout 下降、skeleton ≤2377；2026-08-25 |
| `VARMAP-CROSSPASS-PERSISTENCE-0001` | P1 | BLOCKED（coreaction.rs 被 D2/a21 占用；依赖 STOP 链） | 待 D2 后 | `src/coreaction.rs`（ScopeLocal 生命周期改 fd 持有跨趟 scope） | 跨趟窄化窗口保持+min/max 累积+符号累积；暴露面=出参栈槽>511B、restrict 临时区第 2+ 趟误映射；R8-R2 发现；2026-08-25 |
| `VARMAP-NAMING-REPIN-0001` | P1 | DONE（`5fde682`；双 fixture MATCH，stdout 与旧 pin 逐字节相同=零行为漂移） | a27 + root | 两 fixture 的 rs/metadata/runner（host 迁移本机 system toolchain） | 实际改动=naming 1 行 proto_local_range+add_symbol 5 参 API 适配、unlinked 1 行 proto_local_range（依据 varmap.cc:555 原型窗判定）；VARMAP-LOCALWINDOW 集成批闭合；2026-08-25 |
| `VARMAP-GATHEROPEN-GUARD-0001` | P1 | UNBLOCKED（依赖 a14 已满足，待发射） | 待发射 | `src/varmap.rs`（addGuard/gatherSymbols 回灌/annotateRawStackPtr/checkUnaliasedReturn） | S3 切片；varmap.rs 单文件（与 REPIN 任务串行）；2026-08-25 |
| `CALLSPEC-NORETURN-WIRE-0001` | P0 | BLOCKED（fspec.rs 部分可先行，flow.rs stub 删除待 a9 释放） | 待 a14 后发射 fspec 段 | `src/fspec.rs`（noreturn 位）+ `src/flow.rs:120-123`（删 is_no_return 恒 false stub，对齐 flow.cc:636-651） | 症状2 extraout 爆炸根因；merge.rs 已核实无需改；2026-08-24 |
| `COREACTION-REGNAMES-FS-0001` | P1 | BLOCKED（coreaction.rs 租约） | 待 D2 链释放 | `src/coreaction.rs:858-875`（register_names 表补 FS 段寄存器） | canary 恒真自比较观察；C1 关联发现；2026-08-24 |
| `FSPEC-JUSTIFIED-CONTAIN-0001` | P0 | QUEUED（fspec.rs 被 a18 占用） | 待 a18 后 | `src/fspec.rs`（justified_contain_range 的 -1 条件：Rust "双侧同时越界" vs Ghidra address.cc:131-141 "任一侧越界"）, docs, heritage fixture case 2 重钉 | a16 审计新发现：低位部分重叠误判 justified → guardReturnsOverlapping 在 Rust 不可达；验收=heritage fixture case 2 翻 MATCH + paramlist 回归；2026-08-25 |
| `TYPEOP-LOCALTYPE-CALLOTHER-0001` | P1 | INTEGRATED-CANDIDATE（A52@wt-callother 分支 `agent/callother-userop-wiring`；src 提交 `5cba7550` + fixture/re-pin 提交；双侧 72 records/2495 bytes 逐字节 MATCH，`callother_userop_closure_1204` runner 真实执行绿；`varnode_localtype_res_1204` 重钉至 `5cba7550` 后 locked capture 零漂移复跑绿） | a52 + root 集成 | `src/varnode.rs`（get_local_type/两派发 helper userops 参数线程 + CPUI_CALLOTHER 臂，typeop.cc:855-873）、`docs/api/varnode.md`、`tests/oracle/callother_userop_closure_1204.{cc,rs,metadata.json}` + `tools/run_callother_userop_closure_oracle.sh`、`tests/oracle/varnode_localtype_res_1204.rs` 调用点 + 其 runner/metadata 重钉 | 方案=userops 参数线程（shared_default 单例 + set_types 先于 Arc 排除 TypeFactory 反链，论证在 docs/api/varnode.md 2026-08-25 节）；登记衍生 `TYPEOP-CALLOTHER-PRINTNAME-0001`（dormant TypeOpManager 打印路径）；给 root 的建议：USEROP-LOCALTYPE-METADATA-0001 的 untested_caller_closure 残差由此关闭（其 pinned metadata 的 overall 推进归 root 重钉批）；2026-08-25 |
| `TYPEOP-CALLOTHER-PRINTNAME-0001` | P2 | OPEN（dormant） | unassigned | `src/typeop.rs`（`callother_userop_name` None-stub 接线，typeop.cc:837-853） | 宿主 `TypeOpManager` 全仓零实例化，打印活性路径在 `PrintC::op_callother`（printc.rs 已持 userops 忠实解析）；待 TypeOpManager 激活时给 TypeOpCallother 挂 userops 句柄；src/typeop.rs 现被 D2/a21 租约占用；2026-08-25 |
| `OPTIONS-SPLITDATATYPE-WIRING-0002` | P0 | CANDIDATE（agent@wt-options-v2 分支 `agent/options-wiring-v2`；双侧 fixture 25 records 逐字节 MATCH，stdout sha `8662fd98…`；机制 C Cross-Review 待做） | agent + root 集成 | `src/{arch,action}.rs`, `docs/api/{arch,action}.md`, `tests/oracle/options_wiring_1204.{cc,rs,metadata.json}`, `tools/run_options_wiring_oracle.sh` | arch.rs allacts 字段（architecture.hh:212，Arc<RwLock> 内嵌）+build_action（cc:582）+resetDefaults 接线（cc:1442）；action.rs toggleAction/addToGroup/removeFromGroup（cc:1036/1090/1103）+ActionGroupList String 键；Action/Rule trait 加 Send+Sync supertrait（Rust glue，Architecture 内嵌所需）；双侧 oracle=真实 OptionSplitDatatypes::apply 驱动（TestArch 最小 Architecture 子类）；identity 用构造序数通道规避分配器地址复用；2026-08-25 |
| `OPTIONS-SPLITDATATYPE-WIRING-0003` | P1 | CANDIDATE（agent@wt-wiring3 分支 `agent/options-wiring3`；apply_case 已换直调生产 apply，runner 复跑 exit 0：B2 covered projection MATCH 25 records，stdout sha `8662fd98…` 与旧 pin 逐字节相同=零行为漂移；options.rs 不在机制 C 白名单，无 Cross-Review 要求） | agent + root 集成 | `src/options.rs`（RUGRA-GLUE 块换 arch.allacts 转发）+ `docs/api/options.md` + fixture/runner/metadata 重钉（验收条款要求的 apply_case 直调化连带重钉） | apply 内以 `split_action_toggles` 结果直调 `arch.allacts…toggle_action(get_current_name(), "splitcopy"/"splitpointer", …)`（options.cc:1008-1015）；getCurrentName 单快照等价依据=action.cc:1024/1049 写入集（toggleAction 只读 currentactname）；None-allacts 跳过=reset_defaults Option 守卫先例；runner 另修 /tmp usrquota：CARGO_TARGET_DIR+TMPDIR 迁 `~/.cache`（cc-rs/g++ 临时文件耗尽共享用户配额）；2026-08-25 |
| `ARCH-PARSEEXTRARULES-0001` | P2 | QUEUED | 待认领 | `src/arch.rs`（parseExtraRules，architecture.cc:586，spec 驱动 extra rules 注入 build_action） | 0002 的 build_action 残差：body 两调用已移植，parseExtraRules 无输入路径前不可观察；2026-08-25 |
| `FUNCDATA-TESTS-FLAKY-0001` | P1 | OPEN（root 诊断） | unassigned（只读观察已归档） | 无写（先诊断共享状态污染源） | 纯净 base `fce01555` 三次同参运行 funcdata::tests 失败数 13/21/25 各不同（并行测试共享全局态污染；非 0002 引入——其 write-set 不触 funcdata）；action::/arch::/options:: 309 测试确定性全绿；建议 --test-threads=1 复核或补隔离；2026-08-25 |
| `RETURNFOLD-GAPA-UPSTREAM-0001` | P1 | INTEGRATED_PENDING_REVIEW（候选 branch agent/returnfold-upstream；双侧 fixture `tests/oracle/returnfold_upstream_1204` + runner `tools/run_returnfold_upstream_oracle.sh`：22 行字节一致 MATCH（stdout sha `c1472b99…`）、stderr 双空、各 2 次确定性；含 GAP-D 两 TODO 的 coreaction.rs 部分；待独立 Cross-Review 后合流） | a24@wt-rfupstream + root | `src/coreaction.rs`（e2e return-value fold 链：MarkExplicit numinstances/multlist + MarkImplied count + PrintC implied inlining 验证）+ `docs/api/coreaction.md` + fixture 三件套/runner | R24 发现1（报告=/tmp/rugra-reports/R24-GAPA-REVIEW.md §6.1）已闭合：本行即板面登记；四场景（s1_fold/s2_merged/s3_dup3/s4_mult2）驱动生产 perform+emitBlockBasic 钉死折叠/显式选择与计数；残差=case_new_constructor/case_addrtied_branches（partialroot 旗标缺）/case_cover_crossing/case_marking_order（metadata coverage UNTESTED）；printc.rs 经核验无需改动（rpn 链路 printc.rs:1585/998/2579 已 1:1，base sha 入 metadata base_source_sha256）；2026-08-25 |
| `FSPEC-GETCONTAINER-SPACELESS-0001` | P3 | OPEN | unassigned | `src/fspec.rs`（`get_container` join piece 选择 space 线程化或复用 `justified_contain_in_space` 已核查询空间守卫，fspec.cc:295-328；`VarnodeData::get_addr()` spaceless 路径 fspec.rs:3799→`Address::overlap` address.rs:194 数值臂）+ `docs/api/fspec.md` + 跨空间同数值区间 piece 病态构型 fixture | R26 发现 B（报告=/tmp/rugra-reports/R26-SPACELESS-REVIEW.md §6）：Ghidra `Address::overlap` 异空间返 -1（address.cc:158），Rugra spaceless 数值臂致 join 低显著 piece 异空间且数值区间含查询时选错 piece（Ghidra 跳过）；存量越界、非 8d9aaf66 引入、双侧 fixture 未 staging 该构型；与 ADDRESS-0001 家族归并跟踪；FSPEC_GAPS_2026-08-23.md:169 对 cc:295 记 M(U)；2026-08-25 |
| `STRUCT-MEMBER-NAV-0001` | P1 | CLOSED（症状已在 `46366461` 修复；本行登记诊断结论+残差归因，src 零改动） | a-structmem3@wt-structmem3（只读诊断） | 无（docs 登记行） | 症状=旧 `result/curl_cur.c` 175 处 `->literal` 全在 next_url(14 行)/glob_url(1 行)；诊断结论：URLGlob 类型来自函数参数 DWARF 原型（debugproto apply→ActionRestructureVarnode 参数符号 typelock 注入 input varnode），**非** ActionInferTypes 推断空壳；多重嵌套 literal 是 PTRSUB 链传播缺陷，已由 `46366461`（STOP seal + PTRSUB downChain，VARNODE-STOPUP-FLAGS-0001/TYPE-PTRSUB-PTRSUB-0001）修复；本轮全量 E2E（124/124，0 timeout/0 panic/0 worker-failure）`->literal`=0，`glob->size`/`pUVar3->size`/`bar->point`/`bar->width` 字段级渲染正确，result/curl_cur.c 已回流（skeleton 2884/defects 0/numbering 1）；**golden 本身对 URLGlob 也打 `glob->literal`**（12.0.4 oracle 对该 offset 打首字段名 literal@0，非 Rugra bug，勿"修复"）；next_url 剩余 diff=120 主体是 `piVar10+0x38` 原始指针算术未重构为 PTRSUB/`pattern` 数组元素指针 → 登记 `RULE-PTRARITH-ADDTREE-0001`；progressbarinit 的 `*bar = 0`（golden 逐字段清零）归 VARMAP/ORPHAN 族既有条目；2026-08-25 |
| `RULE-PTRARITH-ADDTREE-0001` | P2 | FIXED_PENDING_REVIEW（agent/ptrarith 工作树；待 root 集成+独立复核） | a-ptrarith@wt-ptrarith | `src/ruleaction.rs`（AddTreeState::assign_propagated_type + build_tree STOP 闸门 size!=0 + RulePieceStructure partial-root 闸门）、`src/funcdata.rs`（TYPE_RECOVERY_EXCEEDED 旗标 + is/set）、`src/coreaction.rs`（localcount==7 → set_type_recovery_exceeded）、`src/op.rs`（is/set_partial_root）、`examples/curl_decompile.rs`（worker_architecture 装配 TypeFactory + data_organization 解码 + setupSizes）、`docs/api/{ruleaction,funcdata,op,coreaction}.md`、fixture 三件套 `tests/oracle/ptrarith_addtree_1204.{cc,rs,metadata.json}` + `tools/run_ptrarith_addtree_oracle.sh`、本 TODO 行 | 根因三层：①E2E 驱动 Architecture 从未 set_types → ActionInferTypes 的 PTRSUB/PTRADD/INT_ADD 指针臂（TypeOpPtrsub::propagateType→propagateAddIn2Out downChain）在 `type_factory?` 处静默死路 → PTRSUB 输出永远 Int/Unknown → RulePtrArith applyOp 槽位搜索找不到 TYPE_PTR（板上原诊断的『res==2/get_type_read_facing 时机』两项均非根因，eval 与时机在 Rule 侧本就忠实）；②Ghidra buildTree 的 assignPropagatedType+isTypeRecoveryExceeded 链未移植（Funcdata 连旗标都没有）；③RulePieceStructure 缺 isPartialRoot/setPartialRoot 再入闸门——工厂接入后 cleanup 池对同一 CONCAT 根每轮重走返回 change，universal 尾部不收敛致 main>300s（修复后 main 13s、全量 0 timeout）。验证：双侧 fixture 5 用例 10 记录与锁定 12.0.4 oracle 逐字节一致（runner exit=0，stdout sha256=7b632ca1…）；全量 E2E 76/124 反编译 0 timeout/0 panic，defects=0/numbering=0，skeleton 2864→2883（next_url 137→140：PTRADD/PTRSUB 链已出现 ->type/->content 真实字段名，残余 = 新建中间 unique 临时量在 merge/HighVariable 命名层泄漏为 unique0x… 标识，登记 `MERGE-PROTOPARTIAL-UNIQUE-0001`）；cargo test --lib funcdata 域 19-22 失败为 master 既有 flake（pristine master 563264a3 同样 18 failed，与本改动无关）；2026-08-26 |
| `MERGE-PROTOPARTIAL-UNIQUE-0001` | P2 | IN_PROGRESS | mergeproto@rugra-wt-mergeproto | `src/merge.rs` + `src/variable.rs` + `docs/api/merge.md` + 本行（varmap.rs/printc.rs 不动） | 依赖=`RULE-PTRARITH-ADDTREE-0001`（激活链已修）；R4 修复：both-existing groups 按 variable.cc:599-604 无条件 combineGroups，删除 existing_keys duplicate Err；同步清除 group_partials 的静默 duplicate 忽略语义。此前 R3 的 strict compare_order polarity、单测、optree 顺序与输出空间修复保留。扫描 PIECE 根、按 Ghidra 端序收集偏移并调用 groupWith；验收=next_url unique0x 下降、defects=0、全量 skeleton 不劣化；oracle=e40ed13014025f82488b1f8f7bca566894ac376b；2026-08-27 |
| `TOPFUNC-TRIAGE-0001` | P1 | DIAGNOSIS_DONE（只读分诊；报告=/tmp/rugra-reports/TOPFUNC-TRIAGE.md） | toptri@wt-toptri（只读） | 无 src 写；本 TODO 行 | main(894)/getparameter.constprop.0(493)/glob_range(114) 三函数根因分桶：main=A结构化373/G全局162/D死存储105/B命名171/E常量字符串~181(重叠)/F调用原型~20；getparameter 91%=JUMPTABLE-PIPELINE-0001 单因；glob_range 回归二分定案见 GLOBRANGE-REGRESS-ATTR-0001；2026-08-25 |
| `GLOBRANGE-REGRESS-ATTR-0001` | P2 | DIAGNOSIS_DONE（二分实测定案，无 src 改动） | toptri@wt-toptri（只读+构建测量） | 无 src 写；本 TODO 行 | glob_range 111→114(+3,峰值115) 引入点=**1253d177**（base_explicit 多实例规则 coreaction.rs:2888-2896，oracle coreaction.cc:3020 逐字核对=忠实移植，非缺陷）；+7 机制=explicit 化 varnode 物化命名局部致声明膨胀（`piVar12/iVar12/in_RCX` 等），表达式 register 泄漏反减；次级 +1=cac8f60a，−1=5aac3203；处置=残余行归 MAINDIFF-STRCONST-0001（`/* dec */` 注释）与 varmap 声明域（VARMAP-ORPHAN-DECL-0001 同族），不为 glob_range 单开实现桶；测量点 8b387a8d=107/eb8a63f9=107/1253d177=114（--rugra-selected-function 与全量函数体逐字节等价已验证）；2026-08-25 |
| `MAINDIFF-STRCONST-0001` | P1 | **CLOSED**（wip `460ab6a6` + align `e69f2c5e`（VOID/FLOAT 臂补全+正式 Evidence/Differential）；独立复验 @HEAD：hugehelp 6 参数逐字节=golden、defects=0/numbering=0、skeleton 2186） | cptrb@wt-cptrb | `src/printc.rs`+`src/ruleaction.rs`+`src/database.rs`+`src/prettyprint.rs`+`examples/curl_decompile.rs`+docs | 交付=四层链接通：① RPN 常量臂 `constant_leaf_text` 完整移植 pushConstant 分派（printc.cc:1749-1815，含 pushPtrCharConstant cc:1698/pushPtrCodeConstant cc:1730/pushCharConstant cc:1606/pushEnumConstant cc:1666/push_integer cc:1288 文本核），删 `format_constant_value` 及 `/* dec */` 自创注释；② `RulePtrsubCharConstant` 完整守护链（ruleaction.cc:7354-7403：charPrint 守护 cc:7366-7369、is_scope_read_only cc:7372、共享 StringManager isString cc:7375、pushConstFurther 传播半段 cc:7378-7391 + opDestroy/COPY 双臂）；③ `Database/Scope::set_symbol_flag`（database.cc:394 decodeHeader 标志通道）+ driver 对字符串符号置 TYPELOCK、.rodata 全局置 READONLY；④ EmitNoMarkup 空白折叠带引号状态机（字面量内多空格 run 不折叠）。验收=E2E 124 函数：hugehelp 三字面量（0x10ea40/0x111270/0x113ad0）与 golden **逐字节一致**（2132/2105/2139 字节，`\'` 转义+` /* TRUNCATED STRING LITERAL */` 标记），三非法 UTF-8 地址保持 `&DAT_*`（isString 负缓存拒绝，同 golden）；progressbarinit `curl_getenv("COLUMNS")`、my_fwrite `fopen(..., "wb")` 折叠；`/* dec */` 全量清零；defects=0/numbering=0，skeleton 2194→2186（对 HEAD 28a20ca3 实测基线）；print_unicode 逃逸判定纠正（printlanguage.cc:411-487，引号逃逸）；**测量基线警示**：result/curl_cur.c 旧档（51 警告/72 反编译/3 panic）跨多提交陈旧——HEAD 28a20ca3 干净重建实为 27 警告/75/0，已回流新档；2026-08-26 |
| `PLTSTUB-WARNLOSS-0001` | P1 | OPEN（诊断归因部分完成，需向更早提交二分） | unassigned | 待二分定界后定（嫌疑：printc doc_function 评论渲染链 UNKNOWN-PROTOMODEL-WARN-EMIT-0001 域或 PLT 签名装填域） | 症状=24 个 PLT stub（0x22f0-0x2580：free/puts/strcpy/...）丢失头注释 `/* WARNING: Unknown calling convention -- yet parameter storage is locked */`（golden=51 处，现输出=27 处，仅剩 EXTERNAL 块静态段+hugehelp/main_free/main_init）；伴随 `undefined8 in_ram_00016e98` 变 `long ...`（声明类型漂移）；归因实测：a23a6394（8474ec13 父）=27 处 → **引入点早于 8474ec13**，与 STRCONST 无关（HEAD 干净重建与 STRCONST WIP 同为 27，双向验证）；old result/curl_cur.c=51 为跨提交陈旧档不可作二分锚点；需从 a23a6394 向前二分；验收=PLT stub 警告恢复 51/51 且 `undefined8` 声明形态回归；2026-08-26 |
| `PLTSTUB-WARNLOSS-0001` | P1 | IN_PROGRESS（fspec 半边已落地；coreaction 半边排队 regB dual-null） | pltstub-diagnostic | 已改 `src/fspec.rs`；待 regB dual-null 后改 `src/coreaction.rs` 主门控；必要时 `src/ruleaction.rs`；heritage/varnode 只读依赖 | 24 个丢失函数清单及逐 commit 证据见 `/tmp/rugra-reports/PLTSTUB-ROOTCAUSE-2026-08-27.md`：`9a9da74a`/`e0af9d21`/`8b387a8d`/`46366461` 均 51，`4d5e89fd` 与 HEAD 均 27；24 个为 free/strcpy/puts/isatty/fclose/strlen/strchr/strrchr/fputc/fgets/strtol/memcpy/time/fileno/__xstat/malloc/realloc/fopen/strcat/exit/fwrite/strdup/strstr/__ctype_b_loc。fspec 半边已验证：`create_placeholder` 增加重复槽与 tagged locked-stack 参数门控；`cargo check --lib`、32 fspec 测试、E2E defects=0/numbering=0 通过，WARNING 仍 27（coreaction 未修，预期）。最小修复候选=ActionFuncLink 输入链恢复 oracle 的 inputlocked/varargs/stack-parameter placeholder 门控与时序，保留 4d5 的 stackoffset 收敛、死锁修复、LocationMap 性能收益；coreaction 计划=`/tmp/rugra-reports/PLTFIX-COREACTION-PLAN.md`；建议验收=51 warnings + `undefined8 in_ram_00016e98` + main<5s/0 timeout；2026-08-27 |
| `MAIN-TIMEOUT-8474EC13-0001` | P1 | **CLOSED**（由 MAINDIFF-GLOBAL-0001 mapGlobals 真移植附带解决：超时根因=flag-only 桩把 persistent 组暴露给下游不收敛；合并 master 后 main action 6.48s/print 6.58s，全语料 0 TIMEOUT） | mapglobals@wt-mapglobals | 已随 `MAINDIFF-GLOBAL-0001` 落地 | a23a6394 全量=76 反编译/0 timeout vs 8474ec13 起=75/1 timeout（main 3531 字节反编译→TIMEOUT >10s，此后 28a20ca3 与 STRCONST WIP 均保持 75/1）；引入点=**8474ec13**（SLEIGH register-name 通道 + ActionConstantPtr segment (b)）；实测关闭证据=mapGlobals 分支 ee7f8f02 与 merge master 后 E2E 均为 main 6.5s 内完成、0 FUNCTION_TIMEOUT；2026-08-26 |
| `MAINDIFF-GLOBAL-0001` | P1 | **INTEGRATED**（wip `90d0ec88`+`ee7f8f02` + merge master 定稿：in_ram_*=0、defects=0/numbering=0 保持；funcdata.rs 自动合并零冲突，冲突合成=curl_decompile.rs 播种链（分支 8 字节槽宽/undefined * PTR_ 层/DWARF 层 + master TYPELOCK/READONLY flag 通道）+ printc.rs `constant_leaf_text`（master 完整分派 + 分支 `cast_type_string` 结构性 cast 前缀路由进 `default_cast_constant_text`）+ ruleaction.rs/op.rs isPartialRoot 取 master 正式版） | mapglobals@wt-mapglobals | `src/funcdata.rs`（map_globals/cover_varnodes/query_properties_parent_scope/discover_scope_parent_scope）+`src/database.rs`+`src/coreaction.rs`+`examples/curl_decompile.rs`+`src/printc.rs`+docs | oracle=funcdata_varnode.cc:1653 Funcdata::mapGlobals；四类决定性语义全对齐（loc_tree space-major 双下标组循环+跨 space 显式 break n_space!=base_space；index 每次 addSymbol 前置 0；inconsistentuse 函数级单 bool；maxvn 严格更大 size 首个最大者胜；溢出判定 (addr+ct_size)-1>(entry_addr+entry_size)-1 饱和算术）；通道分流=RAM 组走 Database queryProperties 父作用域走查（global scope 只建模默认数据空间）、非 RAM persist 组走 legacy symbol_table 代理臂（登记的 funcdata 缺口）；验收=main 从 TIMEOUT 恢复 6.5s、全文件 in_ram_* 66→0（合并后仍 0）、124 函数 defects=0/numbering=0、skeleton 2683→2772（master jumptable 管线新增恢复内容，defects 不受影响）；2026-08-26 |
| `MAINDIFF-UNIQLEAK-0001` | P1 | OPEN（**heritage.rs 无人在途，可立即认领**） | unassigned | `src/heritage.rs`（free unique/register 空间 varnode SSA 化/消除，使其不存活到命名打印）+ `docs/api/heritage.md` | 证据=main `char *unique0x00008f00` 被解引用 4 处+`register0x0000020b` 入判式、gp `unique0x00009d00`+畸形 switch 表达式 `register0x00000000`、glob_range `register0x00000010`；Ghidra unique=scratch 永不出现在命名（varmap 命名前置过滤+heritage 全 free 读 SSA 化）；族关联=UPSTREAM-OUTVN-DEADWIRE-0001/HERITAGE-FLAGFREE-SSA-0001（setcasts/flag 读域）；单函数直跑 getparameter 在 varnode.rs:2421 panic "Free varnode has multiple descendants"（线性注入 955 ops 路径）=同族显式崩溃证据；验收=三函数 unique/register 命名清零+panic 消除；2026-08-25 |
| `MAINDIFF-DEADSTORE-0001` | P2 | QUEUED（coreaction.rs 在途域 a42/D2） | 待 coreaction.rs 释放 | `src/coreaction.rs`（ActionDeadCode 栈槽连写消除）+ docs | oracle=coreaction.cc:3925 ActionDeadCode::apply（propagateConsumed cc:3556/3576）；证据=main `lStack_260 = 0x25f7 /* 9719 */` 每次 call 前重复 ~40 处（golden 只留 3 处活存储喂 __fprintf_chk），skeleton ~105 行；验收=main lStack_260 连写清零仅留 golden 同位活存储；2026-08-25 |
| `MAINDIFF-CALLPROTO-0001` | P2 | DONE（commit `c126d618`，待机制 C 独立复核；wip 断点 `b8cf5eac`） | callproto-agent@rugra-wt-callproto | `src/coreaction.rs`+`src/fspec.rs`+`examples/curl_decompile.rs`（timeout 诊断钮）+`docs/api/{fspec,coreaction}.md`+本行 | 消费者清单①-⑤ 全落：①build_input_from_trials 补 opSetAllInput 尾+SUBPIECE 截断+markNotMapped+deleteUnusedTrials+unref newVarnode queryProperties 尾（fspec.cc:5668-5741/funcdata_varnode.cc:148-165）②commit_new_inputs 正文修正（锁定 stack 参数占 placeholder 角色、setStackPlaceholderSlot inline，fspec.cc:5172-5181）；零调用方=与 oracle 管线位置一致（唯一调用方 deindirect:5466/forceSet:5497 均在 CALLSPEC-0001 未接线 seam 之后）③ParamList 消费闭环=derive_input/output_map 优先走 cspec ProtoModelFull（ParamListStandard port 含 mark-active-used 尾环）；"ActionLockInputs"在 12.0.4 无此类，实名 ActionInputPrototype/OutputPrototype 已存在（coreaction.rs:7070/7156）④func_link_input locked 路改读锁定原型参数表（coreaction.cc:1474-1513），known_param 表从该函数删除（残留仅 ActionCallParams/ActionInferParams 域）⑤apply 的 known 谓词删除，initActiveInput 按 cc:1482 `(!inputlocked)\|\|varargs`；集中式 trial 重注册环移除（guard_calls 是唯一所有者）；new_for_op 不再合成 caller funcp 克隆（fspec.cc:4926 `: FuncProto()`，parseconfig `__stack_chk_fail(filename,config)` 2 参 bug 根因）；E2E run2（124 全量，含全部修正）：defects=0/numbering=0/skeleton 2305、75 decompiled/1 timeout(main)/0 panic；调用形态收敛：parseconfig `__stack_chk_fail(filename,config)`→`__stack_chk_fail()`（golden 一致）、helpf `__stack_chk_fail()` 一致、glob_url 定义 3 参一致；main 内 curl_version/glob_url/__stack_chk_fail 站点与 getparameter 内 curl_version×2 被**宿主函数预存健康问题**遮挡（main 后 blockstruct 动作环 ≥300s 不收敛=HEAD c75b3195 worktree 复现证明非本改动回归→MAIN-POSTSTRUCT-SPIN-0001；getparameter goto 塌缩=GOTO-LABEL 域）；机制 B 门禁（coreaction.rs 白名单）已跑 defects=0；maprinty 站点在 golden/corpus 均不存在（旧证据过期）；**复核闭环（R-CALLPROTO APPROVE）**：考古证实（ActionLockInputs 12.0.4 零命中）；五项四类语义全 MATCH；三处坏形态（my_get_line 4 参/helpf 14 参/parseconfig 6 参）父基线实测→集成后全消；登记 latent 残差 R1=FuncCallSpecs::is_input_locked 缺 void_input_locked 门+首参检查（fspec.cc:2264 侧，被 characterize 补偿不可观测）、R2=func_link_input 锁定 spacebase 腿未移植（cc:1494-1506，X86_64GccStorage 栈溢出 bail 使当前不可达）——归 FSPEC-CALLPROTO-LATENT-0001 跟踪；R3 init_active_input 缺 setMaxPass（预存）；2026-08-26 |
| `FUNCDATA-MAPGLOBALS-MAXVN-0001` | P2 | **MATCH**（R-MAPGLOBALS REJECT→fix-forward 43af1718+66da7f3d→R2 窄复核 APPROVE；fixture 6/6 records 双侧字节一致，双宽度判别 case 有效排除 iRam/size=1 与 span fallback 两个错误答案；E2E defects=0/in_ram=0） | mapglobals-fix + root | `src/funcdata.rs`（map_globals maxvn 携带，cc:1685-1686 严格更大重指）+ fixture 三件套 | maxvn 类型源取组内最大 varnode 的 high（cc:1692-1693）；生产面仅两 fix hunk 其余在 mod tests；latent high==None 降级归 MAINDIFF-GLOBAL-0001；2026-08-26 |
| `FSPEC-CALLPROTO-LATENT-0001` | P2 | PARTIAL_MATCH（R2 scalar spacebase projection MATCH；full storage identity UNTESTED） | root + funclink_oracle_audit | 证据并入 `GETSTR-FUNCLINK-SPACE-0001`；后续 full Address/ModelRule 写集另认领 | `ACTION-FUNCLINK-INPUT-1204` 已用真实 x86-64 GCC 模型构造第 7 个 scalar stack 参数，并逐字节证明 locked/varargs/unlocked 的 stack LOAD、natural/synthetic placeholder、slotbase 与 fixed-position 投影。此前“stack spill 不可达/无 fixture”已失效；仍不可外推完整 AddrSpace* 身份、aggregate/join/hidden-return 或 funcLinkOutput。2026-08-28 |
| `MAIN-POSTSTRUCT-SPIN-0001` | P1 | DONE（evidence=`92d7c122`，@ agent/mainspin worktree，待机制 C 独立复核） | mainspin-agent | `src/{heritage,action,funcdata,space,coreaction}.rs` + `docs/api/{heritage,action,funcdata,space,coreaction}.md` + 本行 | **根因链闭合**：①旧 `bump_deadcode_delay` 直接 `infolist[i].deadcodedelay += 1` pass 中途变异 + 不设 restart 标志（oracle 契约=经 Override 装 delay、下一遍 startProcessing 生效、cc:2581 setRestartPending）；②旧 `ActionRestartGroup` 在未清空 Funcdata 上 reset+重跑整棵子树（oracle 是 clearAnalysis 后经 startProcessing→followFlow 再生成，Rugra 流生成在驱动侧）→ post-blockstruct 动作环在不健康状态续跑（≥250s 挂起/tryCallPull 连发）。**修复**：bumpDeadcodeDelay 忠实移植（heritage.cc:2571/2573-2582：种类门→全局 delay 门→hasDeadcodeDelay 门→insertDeadcodeDelay(deadcodedelay+1)→setRestartPending(true)，不再动本 pass infolist）+ `Funcdata::start_processing` 接 `applyDeadCodeDelay`（override.cc:217-231→heritage.cc:2815）+ `AddrSpace::get_index/from_index/spec_space_name`（space.hh:332 索引表基础设施）+ ActionPrototypeWarnings 空间名表（coreaction.cc:4885-4892）+ 重启环降级为有界完成（eprintln+return，PIPE-RESTART-0001 保守降级注记）。**验收**：main 独立 7.1s 完成；全量 curl 124/124 processed、76 decompiled、**0 timeout/0 panic**；差分 defects=0/numbering=0（main skeleton 845、match_url 124）；语料内 restart 触发者=match_url 1 处（heritage bump `register`，输出 oracle 同文头注释"Restarted to delay deadcode elimination for space: register"，golden 无此注=oracle 重启后第二遍不再触发的正确形态，真实重启环归 PIPE-RESTART-0001）；引注行号修正 bumpDeadcodeDelay 2572→2571/setDeadCodeDelay 2829→2815/getDeadCodeDelay 2817→2803/seenDeadCode 2805→2791；2026-08-27 |
| `CALLSPEC-ENV-SCOPE-0001` | P1 | INTEGRATED（`360e00d4`，待 root 复核；消费者移交 MAINDIFF-CALLPROTO-0001） | callspecenv-agent@rugra-wt-callspecenv | `examples/curl_decompile.rs`（link_call_specs 增 DWARF 源+计数）、`src/debugproto.rs`（locked_proto 重构+`locked_callsite_proto`）、`docs/api/debugproto.md`、本行 | oracle=flow.cc:656-672 queryCall（queryFunction→setFuncdata→copyFlowEffects）+coreaction.cc:2322-2330 ActionDefaultParams（`fc->copy(otherfunc->getFuncProto())`）+fspec.cc:3789-3804 FuncProto::copy（model+flags+store clone）+fspec.cc:3843-3852 setPieces 锁配方；根因=Rugra 只把 DWARF 签名锁被反编译函数自身（apply），main 105 callspec 仅 24 libc 锁定，其余 81 保持 flow.rs:1390 的调用者 funcp 克隆，而 headless 环境的 callee DWARF 签名经 queryCall→copy 到达调用点（golden main glob_url 3 参/curl_version 0 参的来源）；实现=driver link_call_specs libc 未命中后查 `DebugPrototypeDatabase::locked_callsite_proto(entry,&fd.funcp,storage)`（两源按构造不相交：thunk 地址无 DWARF 定义、DWARF 函数名不在 24 导入表），锁配方与 apply 共享 locked_proto 构建器（SYSV 存储+DW_AT_name 才 NAME_LOCKED+input/output/model 三锁+空参钉 unknown 哨兵）；E2E（124 全量）：main 105 callspec→24 libc+14 DWARF 锁定（match_url 聚合参数 fail-visible 拒绝）、defects=0/numbering=0、skeleton 2863→2820、main extraout_var 声明位次变化=锁定 callee output 改变 funcLinkOutput 路径、**调用实参个数不变**（curl_version 仍 6 试验参——消费者缺口见上行 MAINDIFF-CALLPROTO-0001 清单①-⑤，coreaction/fspec 租约域）、lStack_260=4 不变（死存储消除同依赖消费者）；模块保持 L2（driver 边界无独立 oracle fixture，NO_ORACLE per 机制 B2）；2026-08-25 |
| `BLOCK-RECIPROCAL-OOB-0001` | **P0** | OPEN（httpd 语料 panic，已二分钉死） | unassigned | `src/funcdata.rs`（decrement_for! reciprocal slot 过期 index）+ docs | httpd ap_getword dec→panic 100% 引入=`36633d9d`（该 commit Cross-Review 自声明 remove/replace caller closure 残差未覆盖，正是命中点）；父提交正常、单点复现；报告=/tmp/rugra-reports/HTTPD-GATE-c1e3733d.md §回归1；验收=ap_getword 完成反编译无 panic；2026-08-25 |
| `HTTPD-STRIPPREFIX-ADDDESCEND-0001` | **P0** | OPEN（httpd panic，定界 (0d2252dd..e24e6896]，首选嫌疑 e24e6896 VarnodeBank iterator-erase 半边；cac8f60a/5aac3203/9a9da74a/c1e3733d 已排除） | unassigned | `src/varnode.rs`/`src/funcdata.rs`（addDescend free-with-reader）+ docs | httpd ap_stripprefix dec→panic；报告同上 §回归2；验收=ap_stripprefix 完成；2026-08-25 |
| `MAINDIFF-CALLPROTO-0001` | P2 | QUEUED（coreaction/fspec 域，与 CALLSPEC 族衔接勿重开工） | 待租约 | `src/coreaction.rs`+`src/fspec.rs`（ActionParamList/ActionLockInputs 参数收敛、间接调用 GOT 槽符号化）+ docs | oracle=coreaction.cc ActionLockInputs/ActionParamList+paramid.cc；证据=main `curl_version(6参)` vs 0 参、`glob_url(2参)` vs 3 参、`__stack_chk_fail(lStack_40,0,0,0,0,0)`、`maprinty(0x6f80,piVar52)`；_start `FUN_16fe0(0x5400,…)`=GOT 槽 __libc_start_main 物化（golden `(*(code *)PTR___libc_start_main_00116fe0)`）；验收=上述调用参数个数与 golden 一致；2026-08-25 |
| `BLOCK-RECIPROCAL-OOB-0001` | **P0** | **FIXED**（2026-08-25 agent/httpdpanic；根因非 36633d9d 单点，而是边删除路径系统性单侧化） | agent/httpdpanic | `src/block.rs`+`src/blockaction.rs`+`src/funcdata.rs`+ docs | 修复=halfDelete 上移 trait（全子类型）+ remove_in_edge_from 改双边 removeInEdge + eliminateIn/OutDups 成对移植 + ruleBlockGoto 两臂改 BlockGraph::removeEdge + identify 安装后 resync 边界 + move_out_edge 忠实重写（replaceInEdge 保槽位）+ decrement 记录槽越界跳过（残差见 BLOCK-RECIPROCAL-RESIDUAL-0001）；验证=httpd 29/30 全 0 panic 0 timeout、ap_getword 完成（diff=58 defects=0）、curl 124 函数 defects=0/numbering=0；commit=本轮；2026-08-25 |
| `HTTPD-STRIPPREFIX-ADDDESCEND-0001` | **P0** | **FIXED**（2026-08-25 agent/httpdpanic；根因=removeUnreachableBlocks 两个自创降级，非 e24e6896） | agent/httpdpanic | `src/funcdata.rs`+`src/coreaction.rs`+`src/flow.rs`+`src/block.rs`+ docs | 修复=忠实移植 funcdata_block.cc:346-393（删除 >=5/>5% 保守门禁与"外部后代 op 保活"近似）+ descend2Undef 死块判定改块级 DEAD flag + descendantsOutside 改查父块 + collectReachable 移植 + ActionUnreachable/generateBlocks 参数对齐；不可达块活 op（读 free vn 的 CBRANCH）不再存活到 RuleCondNegate → addDescend panic 全族消失（ap_stripprefix/ap_ht_time/ap_update_vhost_from_headers/ap_getword）；验证=httpd ap_stripprefix 完成（diff=25 defects=0）；commit=本轮；2026-08-25 |
| `TOPFUNC-TRIAGE-0002` | P1 | DIAGNOSIS_DONE（103 项集成后第二轮复诊，只读；素材=/tmp/rugra-reports/tri2-03d2b4c5.{stdout,stderr}+tri2-fullcompare.txt；报告本轮回报内嵌） | tri2@wt-tri2（只读+构建测量） | 无 src 写；本 TODO 行 | **总盘（master 03d2b4c5，124/124，0 panic/0 timeout，defects=0，numbering=0，skeleton 2826；51 函数零差）**。top-15：main 591(↓303)/getparameter 533(↑40)/parseconfig 186(↑34)/file2string 147(↑16)/glob_range 135(↑21)/glob_set 126(↑10)/myprogress 108/next_url 102(↓30)/my_get_line 100/match_url 91/my_get_token 87/helpf 83/glob_word 81/my_fwrite 22/glob_url 16；hugehelp 12、progressbarinit 12。**上轮桶核对**：G-全局=in_ram_* 全语料清零（MAINDIFF-UNIQLEAK d257375a + REGNAME 8474ec13 吸收，桶基本解决，残 DAT_001175xx 4 处+::config 字段化）；E-常量=main /*dec*/ 181→~5、ActionConstantPtr 已产 DAT_ 符号形但字符串字面量仍未解析（STRCONST 仍 QUEUED，三函数直接命中）；D-死存储=main lStack_260 ~40→5 处（缩水未清零）；F-调用原型=curl_version 6 参仍在（CALLSPEC-ENV 只锁签名未收敛实参，消费者清单①-⑤未动）；JT=getparameter 表 88 项@0x3fd5 恢复（bblocks 36→138）、glob_set 35 项@0x4c45 恢复、getparameter 单函数直跑 panic 已消失（httpdpanic 96065ff9 吸收）——但恢复体被结构化塌缩（见 TRI2-STRUCT-SELECTGOTO-SELFLOOP-0001），getparameter 493→533 为恢复体暴露下游缺口的正确性增长（golden 470 行/1 switch/48 case vs Rugra 58 行/0 switch）；A-结构化=新晋跨函数第一桶（selectGoto exhausted 命中 18 函数）；B-命名=extraout_var_00-04 per-prefix 编号族新增+main ` Var46;` 空前缀畸形新增；2026-08-26 |
| `TRI2-STRUCT-SELECTGOTO-SELFLOOP-0001` | **P0** | **PARTIAL-FIXED**（2026-08-26 agent/selectgoto 3a18f319+本轮；机制 C 复核 APPROVE 附条件，R-SELECTGOTO 纠偏：master 实测自环 17→16（非 5→0，agent 为旧基线口径）、code_r 标签 63→63 持平；exhausted 18→8 fns 属实；16 自环中 15 个落在残差 exhausted 8 函数族） | selectgoto@wt-selectgoto | `src/blockaction.rs`+`src/funcdata.rs`(测试搜索层)+docs | 第一轮修复=两处 selectGoto 前分叉：①identify_internal 单侧 retain 去重→复合块出边被 peer 成对 dedup 按陈旧 rev `halfDeleteOutEdge`（-1→usize::MAX 时 pop 尾边）误删，proper-if 复合块 sizeOut==0→TraceDAG 断走→exhausted；修=成对 dedup 协议（block.cc:930/440-501）+边标签继承（cc:172/188）+双射 resync+install==consumed 跳过（newBlockGoto/DoWhile nodes={bl}）。②proper_if/if_no_exit/while_do/do_while 的自创 switch_case_indices/CASE_BODY 级联守卫（a9d68f77 同族）删，回 oracle 守卫（cc:1383/1487/1525/1561 bl isSwitchOut——build_copy 已重建 f_switch_out、cc:1394/1500/1534 子句 isSwitchOut、cc:1501 isDecisionOut、cc:1487-1490 自环/goto-out）。验证=全量 curl 124/124 exit0/0 panic：exhausted 命中函数 18→8（各 1 hit；原 my_get_line×6/file2string×4 族全清）、自环 `code_r: goto code_r;` 5→0、code_r 标签 140→40（main 41）、defects=0/numbering=0、skeleton 2826→3085（+259=main +249/next_url +37/match_url +35 恢复体暴露下游：main 164→413 行真实调用序列 vs golden 478；file2string −26/my_get_line −24/my_get_token −17 等 8 函数改善）；file2string 单函数 exhausted 60→0 defects=0。残差=8 函数各 1 hit（main/getparameter.constprop.0/glob_set/glob_range/glob_word/match_url/next_url/parseconfig.constprop.0，全为 jumptable 邻域不可约环，getparameter switch=0 case=0 不变）→ 续 TRI2-STRUCT-IRREDUCIBLE-TRACE-0001；funcdata test_bool_condition_folding_and_pattern 搜索层扩两级（oracle 序列 ruleBlockOr→IfNoExit 包 D→cat 合 C 终态 List[If[Cond,D],C]，旧断言恰好锚定旧 bug 形态）；测试基线对齐=并行 20→12 fail、单线程 funcdata 17 fail 与基线同集（FUNCDATA-TESTS-FLAKY-0001 既有）；**复核条件登记（R-SELECTGOTO C2/C3）**：既有守卫缺口 TODO=①rule_block_if_else 缺 oracle 守卫组（cc:1422/1423/1424/1434/1437-1440）②rule_block_do_while 缺 isGotoOut（cc:1562-1563）③identify_internal 不继承 f_switch_out 到复合块（cc:925-926，inf_loop 吸收 dispatch 时可观测）——三项均 BLOCKSTRUCT-GUARD-GAPS-0001 跟踪；frame_dummy 残余 1 自环绑 TRI2-STRUCT-IRREDUCIBLE-TRACE-0001；引用行号漂移（cc:1383→1385 等）机制 D 记录在案；2026-08-26 |
| `BLOCKSTRUCT-GUARD-GAPS-0001` | P1 | **DONE**（2026-08-26 agent/guardgaps wip 94337f2d + 收尾 align commit；机制 C 复核=继任 agent 干净重开后全量逐行重读 oracle（回执 blockaction.cc:1416-1444/1555-1574、block.cc:895-963、block.hh:299-400）guard-for-guard 三处全 APPROVE） | agent/guardgaps@wt-guardgaps | `src/blockaction.rs`（if_else 守卫组 cc:1422-1440 / do_while isGotoOut cc:1562-1563 / identify_internal f_switch_out 复合块继承 cc:925-926）+ docs | 三处存量守卫缺口全补齐：①try_rule_if_else（blockaction.rs:3640）补 ruleBlockIfElse 全守卫组（cc:1422 bl isSwitchOut、cc:1423-1424 双出边 isDecisionOut、cc:1433-1434 汇合点≠cond 防 loop、cc:1435 双子句同 exit、cc:1437-1440 子句 isSwitchOut/isGotoOut）+ **tc/fc 槽位修正**：tc=getTrueOut=out[1]、fc=getFalseOut=out[0]（block.hh:299-300 位置性；flow.cc:960-967 fallthru-first 与 flow.rs:1043-1051 同构）——旧端口倒置读 out[0]→tc，把 false 路径子句打印在 `if` 下（oracle 此处从不 negateCondition，cc:1442）；②try_rule_do_while（blockaction.rs:4017）补 cc:1562-1563 isGotoOut(0/1)（经 out_edge_is_goto label 形态，结构块同样可见）；③identify_internal 补 selfIdentify cc:925-926 `if (mybl->isSwitchOut()) setFlag(f_switch_out)` 复合块继承，双插入点=install 块外部出边相（blockaction.rs:2771-2776，install 块在 Ghidra -nodes- 集内，inf_loop 吸收 BRANCHIND dispatch 场景）+ consumed 组件边界相（blockaction.rs:2848-2851），语义=组件 isSwitchOut **且**有≥1 外部出边才继承（全内部 dispatch 不继承，与 oracle else 分支内检查一致）。验证（flock + /home/wirs/.cache/guardgaps-target，fast-release 独立冷构建重跑）：E2E curl 124/124 exit0/0 panic、defects=0/numbering=0、skeleton=**3050**（wip 归档 3051 为文件尾混入杂散 `EXIT=0` 行所致；独立重跑 C 输出与 wip 归档字节级一致=确定性成立）、selectGoto exhausted 9 hits/8 fn（next_url 1→2=同不可约邻域两轮 collapse restart，finalize 29→11/30→11 双双收敛 blocks=11 不破坏）；**8 残差函数未解锁——预期内**（守卫只补拒判语义，不可约环根因在 LoopBody/TraceDAG 精度与 ruleBlockSwitch 恢复体消费）→ 续 TRI2-STRUCT-IRREDUCIBLE-TRACE-0001；cargo test --lib 1613 pass/17 fail 与 FUNCDATA-TESTS-FLAKY-0001 基线同集零回归；R-SELECTGOTO C2 三条件全部关闭；2026-08-26 |
| `TRI2-STRUCT-IRREDUCIBLE-TRACE-0001` | P1 | OPEN（round 6 诊断完成，仍无代码 writer） | unassigned | `src/blockaction.rs`（LoopBody::update/TraceDAG/emitLikelyEdges 精度 + ruleBlockSwitch 恢复体消费）+ docs | 证据=oracle `e40ed13014025f82488b1f8f7bca566894ac376b`、curl 124/124：本树 exhausted **8→1 函数**（仅 getparameter.constprop.0 1 hit；main/glob_set/glob_range/glob_word/match_url/next_url/parseconfig 清零），defects=0/numbering=0，skeleton=2598；Ghidra TraceDAG 唯一实现为 blockaction.cc:499-1014（无 splitDag/duplicate API），pushBranches cc:983-1014 通过 checkRetirement/checkOpen/selectBadEdge 单边拆环。getparameter 耗尽图 138 块/51 非孤立/86 DEAD，仍 0 switch/0 case（golden 1/48）；TraceDAG 未触发 iter cap。根因锁定为 jumptable 恢复后的组合块边界/消费差异，block.rs 由他 agent 持有，本轮不改；验收=exhausted 清零、getparameter switch≥1 且 case≥48；2026-08-27 |
| `TRI2-UNNAMED-VN-IMPLIED-0001` | P1 | OPEN（无人在途；coreaction.rs 租约须核对） | unassigned | `src/coreaction.rs`（ActionMarkImplied→markImplied 链，coreaction.cc:3412-3447+merge.cc:1590 Merge::markImplied；checkImpliedCover cc:3380-3410 LOAD-crossing-store/call 判定）+`src/heritage.rs`（LOAD 输出 HighVariable attach 完备性）+ docs/api/coreaction.md | 证据=my_fwrite `fopen((FILE)unique0x1000007a, DAT_001061c4)` vs golden `fopen(*(char **)stream,"wb")`——`mov (%rbx),%rdi`(0x3498) LOAD 输出 HighVariable 无 symbol 且未 implied → pushUnnamedLocation（printc.cc:1938，oracle 机制本身存在）打 unique0x token；**引入点=8474ec13**（REGNAME-WALKBACK：此前该 varnode 被 varmap 非法命名 `FILE FVar3`，register-name 通道修正后暴露 implied 链缺口=正确性改动暴露下游缺口，非回归；归档二分：e2e-numdecl=FVar3 形 → e2e-rawquar-integrated=unique0x 形）；RUGRA_DISABLE_CALLSPEC_LINK A/B 排除 callspec 因（0x76↔0x7a 偏移漂移=同管线 temp 计数器）；printc 内联侧已就绪（printc.rs:1029-1096 镜像 printlanguage.cc:518-537 + LOAD 在 rpn_def_inline_reachable 集合 printc.rs:1205）→ 根因在上游 ActionMarkImplied 未标 implied（isFree 跳过=heritage SSA 缺口 或 checkImpliedCover 误 false→setExplicit）；同族形态=9 处 missing-LHS ` = strlen(__s);`（myprogress 5/my_get_token 2/glob_word 2/getparameter 1，赋值位 unnamed varnode 丢 token）+main `*unique0x00008f00` 4 处解引用；次生=prettyprint.rs backfill_missing_locals 对 unique token 回填 `long unique0x…;` 声明（RUGRA-GLUE 无 oracle 对应物，主修后自动消失）；验收=my_fwrite fopen 首参 `*(char **)stream` 形态+missing-LHS 清零；2026-08-26 |
| `TRI2-STORESPLIT-WHOLESTRUCT-0001` | P2 | **FIXED**（2026-08-26 agent/fieldsplit wip `2647faac`+`76a46fa2`+align 提交；任务投影 MATCH，fixture=tests/oracle/tri2_storesplit_wholestruct_1204.{ghidra.c,rugra.c,metadata.json}） | fieldsplit@wt-fieldsplit | 实际 write-set=`src/coreaction.rs`+`src/typeop.rs`+`src/block.rs`+`src/blockaction.rs`+docs/api/{coreaction,typeop,block,blockaction}.md（原租约猜的 ruleaction/subflow 无需改——RuleSplitStore 拆分机制本体已忠实，欠拆根因在上游读者局部类型） | 根因=ActionInferTypes 给 STORE 值输入播种的局部类型用 `IntTypes::sized`（16/32 字节常量饱和到 8 字节 long），而 oracle 基默认 `TypeOp::getInputLocal`（typeop.cc:271-275）是 `getBase(size,UNKNOWN)`——>10 字节产 unknown1[N] 数组（type.cc:3652），值类型全宽才能让 `testDatatypeCompatibility` piece 走查（subflow.cc:2319-2334）覆盖全部 outType 分量，RuleSplitStore（subflow.cc:2991-3004）才触发。修=①coreaction LOAD/STORE 读者派发：每个非 annotation 输入以 `merge_min_type_order` 播种 `getBase(ownSize,UNKNOWN)`（varnode.cc:918-932 descendant typeOrder-最小值的 op 中心投影；typeop.hh:269/279 两 opcode 均无 override）；②typeop 移除自创 `TypeOpStore::get_input_local` slot-2 pointee 回查（oracle 无此 override）；③block/blockaction `live_ops_source` 镜像委托（BlockCopy 契约 block.hh:520-535），使 ActionBlockStructure 之后 cleanup 池插入的 RuleSplit* op 对打印可见。验证=E2E curl 124/124 defects=0 numbering=0，progressbarinit skeleton 12→9、5 行逐字段清零（bar->total/prev/point/width/field_0x1c）逐行对上 golden，my_fwrite `stream->_IO_read_ptr` FILE*+8 单 STORE 不误拆；基线因果=真基线 50353b90 重建对比 12 函数变化净 +8（match_url/glob_set/next_url typing/naming churn 绑 RULE-PTRARITH-ADDTREE-0001）；测试失败集与基线 byte-identical；残差（printc cast 形 `*(undefined4 *)&bar->field_0x1c`、curl_getenv 赋值形、iVar1 孤儿声明、'O' vs 0x4f）分属 PRINTC/MAINDIFF-CALLPROTO/VARMAP-ORPHAN-DECL 族，非本任务域；2026-08-26 |
| `BLOCK-BUILDCOPY-MIRROR-0001` | P0 | PARTIAL_MATCH（5-case covered projection `MATCH`；overall `MISMATCH`；evidence=`this commit`） | root（生产与串行集成）；`block_buildcopy_fixture`（fixture/registry）；`block_buildcopy_review`（独立 scoped APPROVE） | 实际 write-set=`src/{block,blockaction,coreaction,funcdata,printc}.rs`, `docs/api/{block,blockaction,coreaction,funcdata,printc}.md`, `docs/TODO_BOARD.md`, `ALIGNMENT_ROADMAP.md`, `tests/oracle/block_buildcopy_1204.{cc,rs,metadata.json}`, `tools/run_block_buildcopy_oracle.sh`, `tests/oracle/fixture_registry.json`, `tests/oracle/block_calcloop_1204.metadata.json`；锁定 oracle=`e40ed13014025f82488b1f8f7bca566894ac376b`；后继=`ruleBlockOr/isComplex/identifyInternal` GetStr 折叠 | append-only 真实 BlockCopy、原序全边向量/label/reverse slot、mapped idom/source copymap/index/numdesc/flags/visit=0/>2 switch、live delegation、default-switch 双半边、graph-owned Basic end-insert parent/order/BRANCHIND flag、swap 后平行边精确配对删除、Copy 入口地址与 StructureTransform child-first/direct-Copy 路由已实现。双侧 fixture 五个 case 均 MATCH：37 records/5577 bytes，stdout sha=`650c8aa6bc4e15ee6e57cdbed07268117d446a676a88b0bee46634e9249bfe21`，raw diff sha=`e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`；writer 与独立 reviewer 均完整重跑，`--validate-only` PASS。复核仅批准这五个投影；Action count bridge 静态符合但 Action::perform 未双侧覆盖。完整 parent、内建 structured source 状态、BlockGoto/MultiGoto、Copy negate/swap/self-loop、non-end insert、MULTIEQUAL/branchRemove、print/marshal、StructureTransform finalTransform、Action/loop/collapse/identify/PrintC/GetStr 闭包继续 `MISMATCH/UNTESTED`，不升 L 级。`cargo check --lib` PASS；目标 block/blockaction/StructureTransform/switch 测试全过；整库为 1620 passed/17 failed/5 ignored，17 失败与既有基线同集（首个 normalize-branches 断言，后 16 个为 `FFI_TEST_LOCK` poison 级联），新增失败=0。release curl sha=`0fcd3815…`，124/124、skeleton=2881/defects=1/numbering=0/GetStr diff=4/parseconfig diff=118/syntax=30/123；httpd sha=`6f57448e…`，27 完成/2 timeout、skeleton=2001/defects=3/numbering=0/syntax=7/27。last_updated=2026-08-28。 |
| `TRI2-CALLOUT-ASSIGN-0001` | P2 | OPEN（无人在途；认领时建议与 MYFWRITE-TEMPVAR-0001 合并调度——同 coreaction 调用输出/return 值数据流域） | unassigned | `src/coreaction.rs`（call output→局部赋值链，coreaction.cc ActionReturnRecovery 域）+ docs | 证据=progressbarinit `curl_getenv(DAT_0010626a);` 裸调用语句+下一行直接 `if (__nptr != 0x0)` 使用，golden 为 `__nptr = (char *)curl_getenv("COLUMNS");`——call 返回值 varnode 未接赋值（数据流断链但 varnode 存活）；同族=my_fwrite fwrite 末参 stream 而非 __s（MYFWRITE-TEMPVAR 已登记）；验收=progressbarinit __nptr 赋值形恢复；2026-08-26 |
| `TRI2-STORESPLIT-WHOLESTRUCT-0001` | P2 | OPEN（无人在途） | unassigned | `src/ruleaction.rs`/`src/subflow.rs`（RuleSplitStore 整结构 STORE 拆字段，subflow.cc:2910-2938/2244-2274 getValueDatatype 门控）+ docs | 证据=progressbarinit `*bar = 0;` 单条整结构 STORE vs golden 5 条字段清零（`bar->total=0; bar->prev=0; bar->point=0; bar->width=0; *(undefined4 *)&bar->field_0x1c=0;`）；ProgressData DWARF 类型已到参数（bar->point/bar->width 字段访问正常工作=类型链就绪），仅 32 字节整 STORE 未拆；历史注=splitgate 域曾修反向过拆（getValueDatatype 门禁），本条为欠拆侧；验收=progressbarinit 清零序列与 golden 逐字段一致；2026-08-26 |
| `TRI2-CALLOUT-ASSIGN-0001` | P2 | FIXED（agent/callout `19ce09bf`+`c9f6337e`+`6230b082`，2026-08-27；待 root 集成+机制 C 独立复核后闭） | callout agent | `src/coreaction.rs`+`src/printc.rs`+`src/debugproto.rs`+`src/block.rs`+`src/blockaction.rs`+ docs/api 五件套 | 修复链=（a）BlockCopy 活委托：结构图副本 get_ops 经 source_basic 回指源块当前 op（block.hh:533-534 语义），结构化后 ActionSetCasts 插入的 CAST 对 printer 可见；（b）cast_output CALL/CALLIND token：callspec LOCKED 非 void 输出，否则 getBase(size,TYPE_UNKNOWN)（typeop.cc:720-735/776-789）——unlocked curl_getenv token undefined8 vs 输出 high char* → CAST 插 CALL 后；（c）printc 拼写：cast_type_string 单 type_expr_space+spacing0 ptr_expr 粘着（printc.cc:73/75）+ rpn_op_type_cast get_name 直读改结构化折叠（消 `char * *`）。验收=progressbarinit `__nptr = (char *)curl_getenv("COLUMNS");` 与 golden 逐字符一致；defects=0 numbering=0；skeleton 3304→3300；`char * *` 全语料 3→0；fopen(*(char **)stream,"wb") 逐字符一致。残余拆出：progressbarinit 剩 8 行=store-split（TRI2-STORESPLIT-WHOLESTRUCT-0001 域）+`'O'` 字面量（0x4f char 常量形）+死 `int iVar1;` 声明（P2 新登记 TRI2-CALLOUT-RESID-0002）；同族 MYFWRITE-TEMPVAR-0001 的 fopen 首参形态已由 (c) 顺带收敛（`*(char **)stream`），其 fwrite 末参残差不变 |
| `TRI2-CALLOUT-RESID-0002` | P2 | OPEN（2026-08-27 由 TRI2-CALLOUT-ASSIGN-0001 收敛后拆出；progressbarinit 剩余 8 行） | unassigned | `src/printc.rs`（int 家族 char 常量形：0x4f→'O'，pushCharConstant/pushConstant 通道）+ `src/varmap.rs`（死 `int iVar1;` 局部声明：call 输出 CAST 链收敛后残存的未用局部清理，Ghidra 侧无此声明=varmap 映射多产出）+ docs | 证据=progressbarinit 残差 diff 8 行=`bar->width = 'O';` vs golden `bar->width = 0x4f;`（2 行）+ 死声明 `int iVar1;`（1 行）+ 整结构 STORE 欠拆 4 行（后者归 TRI2-STORESPLIT-WHOLESTRUCT-0001 不重复计）；验收='O'→0x4f 十六进制形（或证明 oracle 在此 metatype 下确打 char 形）+死声明清零；2026-08-27 |
| `GOTO-LABEL-UNPRINTED-0001` | P1 | OPEN（httpd 首暴露；curl 0 goto 路径零覆盖） | unassigned | `src/printc.rs`（goto 目标 label 发射缺失）+ docs | httpd ap_matches_request_vhost gcc 报未定义标号、`goto code_r0x00000000` 零地址、`if (( = ...)` 畸形（ap_getparents；curl my_get_token 同族 1 例）、4 函数 goto 超发（ap_update_vhost_given_ip 3 vs golden 0）；报告 §回归3；验收=httpd goto 家族三症状清零；2026-08-25 |

| `GOTO-LABEL-UNPRINTED-0001` | P1 | DONE（本 worktree `0f5e8177..e67fce60` + `cb39d8c3` 冻结；httpd 实测） | gotolabel-agent@wt-gotolabel；root 集成 | `examples/httpd_decompile.rs`, `src/{flow,funcdata}.rs`, `docs/api/{flow,funcdata}.md`, 本 TODO 行；`src/printc.rs` 由 strconst2 持有 | 症状清零：前任报告 §4/R3 的 label 未定义 **1→0**、零地址 **1→0**、病态 `if (( =` **1→0**；当前 httpd 29/29 functions、goto refs=15、labels=18、undefined=0、orphan=3、`LAB_00000000`=0、conditional-goto=16；compare defects=0/29、numbering=0。根因链：Ghidra `BlockGoto::gotoPrints` block.cc:2881-2890 以 parent `nextFlowAfter` 与 target front leaf 比较；`PrintC::emitBlockGoto` printc.cc:2766-2778 只在该谓词为真发 goto；`FlowInfo::overrideFlow` funcdata_op.cc:969-1009 要求 dead primary branch 后改 opcode；Rugra 已由 label ledger/backpatch、front-leaf discovery、raw override transport 和 tail-call seeding 补齐。剩余 3 orphan labels 属结构化 over-emission，另受 `PRINTC-GOTOPRINTS-0001` 跟踪；httpd gcc 11/29 OK（其余为既有隐式声明/类型残差），curl 124/124 defects=0/numbering=0；2026-08-26 |
| `TYPEOP-PTRSUB-FIELDCAST-0001` | P2 | SUPERSEDED/REJECTED（由顶部权威行接管） | calloutfix historical | 无有效租约 | 原“让 `op_output_type_local`/buildLocaltypes 消费 output token”的阶段诊断被锁定 oracle 否决；PTRSUB local 必须保持 INT，token 只在 `ActionSetCasts::castOutput` 消费。不得作为 FIXED/MATCH 证据；2026-08-27 |
| `BLOCK-RECIPROCAL-RESIDUAL-0001` | P1 | OPEN（受控降级，2026-08-25 登记） | unassigned | `src/block.rs`（decrement_reciprocal_reverse_index 越界跳过）+ `src/blockaction.rs`（identify_internal）+ docs | 残差=identify 捕获式建边模型仍可留下过期 reverse_index（如结构块边界边列表被重建而对侧半边存活）；当前 decrement 记录槽越界时跳过并打 `[BLOCKSTRUCT] WARN`（httpd 全量 28 次），非 oracle 行为（Ghidra 的 replace*Edge 协议保证槽位恒存在）；修复路径=selfIdentify 完整移植（block.cc:160-191 replaceIn/OutEdge + 895-931 selfIdentify 经成对重定向，替换 rewrite_*_to_idx 单侧翻指 + resync 补救）；验收=httpd 该 WARN 清零且 30/30 完成；2026-08-25 |
| `HTTPD-NUMBERING-STRCASECMP-0001` | P2 | OPEN（2026-08-25 由 BLOCK 移除真实化触发） | unassigned | `src/varmap.rs`（per-prefix 计数器共享不变量）+ docs | httpd ap_strcasecmp_match numbering=1（7 局部变量最大编号仅 4，Ghidra 单共享 base 应 ~7；181538f 同族）；会话前该函数 numbering=0，BLOCK-RECIPROCAL/ADDDESCEND 修复使真实不可达块删除落地→局部计数进入触发区；defects=0 非阻塞；验收=numbering=0；2026-08-25 |
| `HTTPD-TIMEOUT-UNIFY-0001` | P2 | OPEN | root | `examples/httpd_decompile.rs`（15s→30s，与 curl 及 Ghidra SUGGESTED_DECOMPILE_TIMEOUT_SECS=30 统一） | httpdgate 发现口径不一致；2026-08-25 |
| `REGNAME-WALKBACK-DOUBLESTEP-0001` | P0 | REWORK-CANDIDATE（agent@wt-rawquar 分支 `agent/raw-varnode-quar`；复核 REJECT 报告=/tmp/rugra-reports/R-RAWQUAR.md） | rawquar agent + root 集成 | `src/{arch,coreaction,printc}.rs` + `docs/api/{arch,coreaction,printc}.md` + ALIGNMENT_ROADMAP.md + cptr fixture 重钉（`tests/oracle/cptr_query_channel_1204.metadata.json`+`tools/run_cptr_query_channel_oracle.sh`） | F1 主修=arch.rs 回走循环删多余 `next_back()` 丢弃（隔一取一）：`range(..current)` 已排除端点，首个 `next_back()` 即 oracle cc:161 `--iter` 落点；单测期望 (0x1,1) RAX→EAX（复核 C++ 复刻实证）+补 S8 两步回走/Q8 假性漏查判别；F2=补实现（`break 'chase` 替代 `?`，lone_descend=None 时 cc:984 break 入 epilogue 扫描后代）；F3=两 PRINTC 登记（下行）；F4=cptr comparand 重钉；F5=新 commit Evidence 块修正"回走单步"不实陈述；2026-08-25 |
| `REGNAME-WALKBACK-DOUBLESTEP-0001` | P0 | REWORK-DONE（待机制 C 重审；rework commit `a5446455`（amend fb1181a0）+ fixture repin commit（分支 `agent/raw-varnode-quar` HEAD）；复核 REJECT 报告=/tmp/rugra-reports/R-RAWQUAR.md） | rawquar agent + root 集成 | `src/{arch,coreaction,printc}.rs` + `docs/api/{arch,coreaction,printc}.md` + ALIGNMENT_ROADMAP.md + cptr fixture 重钉（`tests/oracle/cptr_query_channel_1204.metadata.json`+`tools/run_cptr_query_channel_oracle.sh`） | 五项全毕（2026-08-25 实测）：F1=arch.rs 回走循环删多余 `next_back()` 丢弃（隔一取一）：`range(..current)` 已排除端点，首个 `next_back()` 即 oracle cc:161 `--iter` 落点；单测期望 (0x1,1) RAX→EAX（复核 C++ 复刻实证）+补 S8 两步回走/Q8 假性漏查判别——`test_get_register_name_boundaries` 绿；F2=补实现（`break 'chase` 替代 `?`，lone_descend=None 时先 vn=outvn 再 break 入 epilogue 扫描后代）；F3=两 PRINTC 登记（下行两行）；F4=cptr comparand 重钉三件套（runner shell 钉 c4a89fa8→a5446455 commit/tree/src_tree/database/funcdata blob + metadata comparand api_database/api_funcdata/runner sha256 + rugra_source 块），`run_cptr_query_channel_oracle.sh` **exit 0**，双侧 18 记录 stdout 逐字节一致，covered_projection=14/14 MATCH；F5=amend 后 Evidence 块"回走单步"陈述已与代码一致。验证数字：非 funcdata 单测 1585/1585 绿（funcdata 17-21 失败为 FUNCDATA-TESTS-FLAKY-0001 既有并行污染，隔离全过）；E2E curl exit 0/0 panic，defects=0/numbering=1/skeleton=2710，裸 in_register_* 1→0（0x3c probe 双侧 oracle 正解 in_RDI，双步 bug 曾跳过 RDI 得 ""），in_RSP 21/in_R8 7/in_AL 1 通道不变。残余（诚实声明）：REGNAME/CPTR(b) 映射函数（get_register_name 六边界/is_pointer/spacebase_constant）仍无专属双侧 fixture（机制 B2 NO_ORACLE，metadata overall_status=UNTESTED 残差 B3-COREACTION-CONSTANTPTR-0001/B3-HUGEHELP-CONSTANTPTR 未变），需 root 另排 fixture wave；arch=None flat 表回退与 entry_addr 只比 offset 两受控残差沿 R-RAWQUAR 附注(a)(b) 跟踪。2026-08-25 |
| `PRINTC-SPACEBASE-TYPECODE-0001` | P2 | OPEN（条件性缺口，现管线不可达） | unassigned | `src/printc.rs`（TYPE_SPACEBASE 臂补 cc:1068-1069 `TYPE_CODE → valueon=true`：函数符号不打 `&`）+ `docs/api/printc.md` | R-RAWQUAR F3 发现：program-DB 命中只携带 type_metatype、driver DAT 层无 CODE 条目，分支在现管线不可观测；解锁依赖 `FUNCDATA-LINKSYMBOL` 的 high→symbol 附着通道带出 TYPE_CODE 符号；已同步登记 ALIGNMENT_ROADMAP printc 行；2026-08-25 |
| `PRINTC-SPACEBASE-PARTIALSYM-0001` | P2 | OPEN（条件性缺口，现管线不可达） | unassigned | `src/printc.rs`（TYPE_SPACEBASE 臂补 cc:1084-1093 `symbolOffset≠0 → pushPartialSymbol`：mid-symbol 命中打子字段而非整符号名）+ `docs/api/printc.md` | R-RAWQUAR F3 发现：容器查询 stand-in 无 symbol-offset 通道且 ConstantPtr 路径构造的 newconst 恒落符号起点（off==0），现管线不可观测；解锁依赖 `FUNCDATA-LINKSYMBOL`（symbol-offset 通道）；已同步登记 ALIGNMENT_ROADMAP printc 行；2026-08-25 |

## 历史 wave：`W-2026-08-23-PARALLEL-FOUNDATION`（未完租约由 W-2026-08-24-TRIFUNC-GAP 接管续跑；保留认领记录）

> 2026-08-23 证据基线三批已原子提交，停止继续扩张同一加固批。当前 17-thread runtime
> 动态保持至少 10 个后台子 Agent：互斥 `/tmp` 算法 worktree writer、fixture writer 与独立只读 reviewer 并行，其余线程
> 只读拆分 locked Ghidra/Rugra 函数体、fixture 观察面与调用闭包；root 仅写中央 TODO/DAG 并
> 串行 cherry-pick。Flow/FSPEC 使用独立 `CARGO_TARGET_DIR`，只读 Agent 禁止编译；实现完成后
> 释放 writer，再由另一 Agent 独立读取 locked oracle 复核。

### 本 wave 认领租约

| ID | P | 状态 | owner | 精确 write-set | 依赖 / 验收 / 证据 / 更新 |
|---|---|---|---|---|---|
| `FUNCTION-LEDGER-REFRESH-0001` | P0 | DONE | root + ledger_refresh_audit（独立只读复核） | `tools/generate_function_ledger.py`, `docs/alignment_audit/FUNCTION_LEDGER.json`, `docs/alignment_audit/FUNCTION_MAP.generated.md`, `docs/alignment_audit/PROTOCOL_TABLE.json`, `docs/alignment_audit/DEPENDENCY_DAG.json`, 本 TODO 行 | evidence=`this commit`；锁定 Ghidra definitions/raw 保持 9494/15811，当前 Rust 8644→9639（production 7965/test 1674），exact 3142→3586、body-ref 379→353，9494 definitions 仍全部 `UNTESTED`；Rust DAG 先 mask comment/literal，边 484→531 且 9 条假边清零；TODO 表按表头和显式依赖段解析，状态伪节点清零，未知 target/cycle fail-closed；验收=`py_compile`、generator self-test、generate+`--check` 全绿；2026-08-23 |
| `FLOW-TRUNCATED-0001` | P0 | INTEGRATED（main `3be7b24`+`0d2252d`；continuity/registry 已随 `8cb8829`/`7b4b29b` 闭合） | flow_writer + heritage_free_ssa_status_review + flow_squash_verify + blockrange_cross_review（独立APPROVE）+ root | `src/funcdata.rs`, `src/flow.rs`, `src/op.rs`, `src/block.rs`, `docs/api/funcdata.md`, `docs/api/flow.md`, `docs/api/op.md`, `docs/api/block.md`, `tests/oracle/truncated_flow_1204.cc`, `tests/oracle/truncated_flow_1204.rs`, `tests/oracle/truncated_flow_1204.metadata.json`, `tools/run_truncated_flow_oracle.sh` | A=`3be7b24` 已集成 reviewed raw-clone/LowlevelError/op-deadlist 地基；B=`0d2252d` 已集成完整 Address block range、块内 max-stop 与 synthetic front range，独立双侧17/17逐字一致、stderr/diff空，overall仍MISMATCH并新增 `BLOCKBASIC-COVER-0001` residual；当前仅等 post-checkpoint continuity 扩展和中央registry登记，不提升Flow/Block模块到L3；2026-08-24 |
| `JUMPTABLE-THUNK-CLASSIFY-0001` | P0 | REWORK（candidate `58aa109` REJECT） | jt_thunk_writer + address_final_review（独立REJECT） | `src/jumptable.rs`, `docs/api/jumptable.md`, `tests/oracle/jt_thunk_classify_1204.cc`, `tests/oracle/jt_thunk_classify_1204.rs`, `tests/oracle/jt_thunk_classify_1204.metadata.json`, `tools/run_jt_thunk_classify_oracle.sh` | `58aa1096b2129ebe37301b2ff7d3d86f2e20e1ee` 已补GCC16等价key/Address wrap并使focused 31/31通过，但完整runner在Rust fixture链接前因artifact selector退出，24-case current Rust从未执行；metadata若干子项仍误标MATCH，runner又有live metadata/overlay TOCTOU、Ghidra archive/toolchain/libstdc++未冻结，且buildAddresses/EmulateFunction Lowlevel错误通道缺失未登记。作者已按新hash重做immutable runner并将未跑分支降UNTESTED，完整新门禁与重审前不得集成；2026-08-24 |
| `ACTIONPOOL-CLONE-FILTER-0001` | P0 | INTEGRATED（candidate `7f0d67a`→main `bd8e38e`） | block_halfdelete_writer + actionpool_clone_review（独立APPROVE）+ root | `src/action.rs`, `docs/api/action.md`, `tests/oracle/action_pool_clone_filter_1204.cc`, `tests/oracle/action_pool_clone_filter_1204.rs`, `tests/oracle/action_pool_clone_filter_1204.metadata.json`, `tools/run_action_pool_clone_filter_oracle.sh` | 依赖=`PIPE-DERIVED-TREE-0001` 已满足；final candidate=`7f0d67a6efe8eabd3d921f58485b7ceb4f08137b` 仅6文件，cargo check/build、action定向236/236与两次独立locked双侧27 records逐字MATCH（SHA=`60044190…7fe`）全过；完整Evidence/Cross-Review已入message，overall仍MISMATCH且factory/ActionState/executor/IdentityEl行为残差未缩。等待当前continuity/registry原子批收口后再集成并重跑IdentityEl后继；2026-08-24 |
| `ACTION-EXECUTOR-BREAKPOOL-0001` | P0 | REWORK（candidate `48675b4` Cross-Review REJECT） | action_executor_writer + breakpool_oracle_review（独立只读） | `src/action.rs`, `docs/api/action.md`, `tests/oracle/action_break_pool_1204.cc`, `tests/oracle/action_break_pool_1204.rs`, `tests/oracle/action_break_pool_1204.metadata.json`, `tools/run_action_break_pool_oracle.sh` | candidate=`48675b4fb621eb2f1e18919699dcd2273a11722d` 已原子冻结但不得集成；Action/Rule断点、Group/Pool cursor、SeqNum successor、first-colon local-unique lookup 的双侧记录逐字一致，静态门禁与 cargo check 通过；独立复核拒绝生产闭包：RuleSubvarSext/RuleDoubleIn 等 derived reset 未接 virtual seam、非 decompile root 未按 grouplist clone、raw Rule 名仍漂移，Restart/OPBANK lifecycle 也未闭合；metadata 已降 overall=MISMATCH 并绑定 `PIPE-BREAK-0001`、`PIPE-POOL-0001`、`PIPE-RESTART-0001`、`OPBANK-0001`；最终 Group direct-apply 修正后作者按队列指令未重跑完整 runner，返修必须先补真实复跑再重新独立复核；2026-08-24 |
| `PCODE-OPBANK-LOCKED-AUDIT-0001` | P0 | DONE | audit_pcode_ops（独立只读）+ root | `ALIGNMENT_ROADMAP.md`, `docs/alignment_audit/DEPENDENCY_DAG.json`, 本 TODO 行 | evidence=`this commit`；完整读取 locked op.cc/.hh、funcdata_op.cc 与 Rust 调用闭包，确认 opcode 1..73/保留洞本身完整，但 PcodeOp/Bank 的 nullable slot、DEAD→alive lifecycle、optree/list identity、deadandgone/IOP、SeqNum、special_prop、遍历、CSE、异常/out-state 均有静态差异，撤销 op 旧 L3；2026-08-23 |
| `OPBANK-LIFECYCLE-0001` | P0 | IN_PROGRESS（`propagateCopyAway→opDestroy` 子投影；root 独占 writer） | root + `getstr_opbank_lifecycle_audit` / `getstr_copy_lifecycle_callers`（只读）；实现后另派独立 reviewer | 当前独占 write-set=`src/{heritage,op,funcdata}.rs`, `docs/api/{heritage,op,funcdata}.md`, `tests/oracle/opbank_lifecycle_1204.{cc,rs,metadata.json}`, `tools/run_opbank_lifecycle_oracle.sh`, `tests/oracle/fixture_registry.json`, `ALIGNMENT_ROADMAP.md`, 本行；只读闭包=`src/{action,coreaction,ruleaction,flow,block}.rs`；依赖 `FLOW-TRUNCATED-0001` 已完成，完整 nullable-opcode/input-slot 阶段仍受 `ADDRESS-PHASE2-CLOSURE-0001` 约束 | 已纠正 GetStr 因果：结构化时的 LOAD 后 COPY 是正常 alive/optree/block member，根因归 `RULE-PORT-EARLYREMOVAL-0001`，不再把它登记为 orphan。独立存在的确定 mismatch 是 Ghidra `Heritage::propagateCopyAway` 在 `totalReplace` 后调用 `fd->opDestroy(op)`，而 Rugra 只手工置 `DEAD`，会制造 bank/block/parent 失配；该函数现改走完整 `op_destroy`，仍待双侧 lifecycle fixture。当前 Rust `PcodeOpBank::create` 还反向从 alive 起步，`destroy` 又允许 integrated op；Ghidra `create` 固定 dead，只有 `opInsert→markAlive` 才集成，`destroy` 对非 dead 先抛错且零突变。fixture 必须固定 create/insert/uninsert/destroy/destroyDead 的 flags、三表顺序、parent/block membership、异常前后全状态；完整 nullable opcode/input slots、canonical changeOpcode、deadandgone/IOP、u32 wrapping uniq 与 duplicate key 保持显式 residual，未覆盖不得升级 L3；last_updated=2026-08-28。 |
| `PRINT-SYMBOL-PIPELINE-AUDIT-0001` | P0 | DONE | audit_print_varmap（独立只读）+ root | `ALIGNMENT_ROADMAP.md`, `docs/alignment_audit/DEPENDENCY_DAG.json`, 本 TODO 行 | evidence=`this commit`；完整读取 locked PrintC/PrintLanguage/Scope naming/constants/markup 主链，确认三套 Symbol 状态、ScopeLocal 每 pass 重建、mapGlobals 平面表、DWARF aggregate/fbreg 缺口与 typed constant dispatch 绕过是上游根因；当前 result/curl_cur.c 无 current commit/options metadata，只作诊断信号；2026-08-23 |
| `PRINT-TYPED-CONSTANT-CLOSURE-0001` | P1 | BLOCKED | unassigned | `src/printc.rs`, `src/printlanguage.rs`, `docs/api/printc.md`, `docs/api/printlanguage.md`, `tests/oracle/constant_typed_1204.cc`, `tests/oracle/constant_typed_1204.rs`, `tests/oracle/constant_typed_1204.metadata.json`, `tools/run_constant_typed_oracle.sh` | 依赖=`DATABASE-SCOPE-OWNERSHIP-FIXTURE-0001`、`TYPEFACTORY-POINTER-CANONICAL-0001`；production leaf 必须携带同一 vn/op/read-facing type/Symbol/readonly/StringManager，按 equate→display format→High type→modifier→natural base 顺序覆盖组合/补码/shift enum、char/null/string/code pointer/void异常；上游闭合前禁止扩张打印期推断；2026-08-23 |
| `CONDEXE-RULE-LOCKED-AUDIT-0001` | P0 | DONE | audit_rule_condexe + ruleaction_readonly（独立只读）+ root | `ALIGNMENT_ROADMAP.md`, `docs/alignment_audit/DEPENDENCY_DAG.json`, 本 TODO 行 | evidence=`this commit`；完整读取 locked condexe.cc/.hh 与 ruleaction 136 classes；确认 condexe per-space heritage/count/live graph/reciprocal edge 与 5 个 Rule opcode 集、IdentityEl/BooleanDedup/SignMod/ConditionalMove/PieceStructure 等真实缺口，同时勘误旧“缺约11条 Rule”为当前 decompile 池成员完整；2026-08-23 |
| `BLOCK-HALFDELETE-REVIDX-0001` | P0 | INTEGRATED（main `36633d9`；registry/continuity 已随 `7b4b29b`/`8cb8829` 闭合） | block_halfdelete_writer + halfdelete_cross_review（独立APPROVE）+ root | `src/block.rs`, `docs/api/block.md`, `tests/oracle/block_halfdelete_revidx_1204.cc`, `tests/oracle/block_halfdelete_revidx_1204.rs`, `tests/oracle/block_halfdelete_revidx_1204.metadata.json`, `tools/run_block_halfdelete_revidx_oracle.sh`, `tests/oracle/fixture_registry.json` 对应条目 | 依赖=`CONDEXE-RULE-LOCKED-AUDIT-0001` 已满足；reviewed tree=`ae7c49834036266438719805d49e049d83fcfc08` 已集成 main=`36633d9`，19 records MATCH、SHA=`e0cf6143…b650`、39/39与门禁全绿，block保持L2；中央registry条目已准备但发现参数binding `slot→mut slot` 触发 scheme2 ID漂移并使migration loader rc2，必须先由 `FUNCTION-ID-POSTBASE-CONTINUITY-0001` 闭合再提交registry/新ledger；2026-08-24 |
| `CONDEXE-SUCCESS-STATE-0001` | P0 | INTEGRATED（R17 Cross-Review APPROVE；`2743440`+`e5db503f`→main `e956e963`+`ea31d7fc`） | a47 + R17 + root | `src/condexe.rs`+docs+fixture | 13/13 MATCH、6/6 投影、零残差；per-space 数组/live 遍历/count 时机/RETURN 空间保持四修复；三旧 fixture 行为保持（overlay 重钉随集成批）；2026-08-25 |
| `RULE-IDENTITYEL-OPCODESET-0001` | P0 | INTEGRATED_CONTINUITY_REGISTRY_PENDING（main `5261aaa`） | rule_identityel_writer + identityel_review（独立APPROVE）+ root | `src/ruleaction.rs`, `docs/api/ruleaction.md`, `tests/oracle/rule_identityel_opcodeset_1204.cc`, `tests/oracle/rule_identityel_opcodeset_1204.rs`, `tests/oracle/rule_identityel_opcodeset_1204.metadata.json`, `tools/run_rule_identityel_opcodeset_oracle.sh`, `tests/oracle/fixture_registry.json` 对应条目 | 依赖=`CONDEXE-RULE-LOCKED-AUDIT-0001` 已满足；reviewed candidate已集成main=`5261aaa`，精确六opcode与raw name=`identityel`，真实ActionPool covered 20/20逐字一致、overall=MISMATCH，生产getSubRule及其余111 raw-name残差未缩；等待本wave continuity扩展和中央registry登记，ActionPool后继不得倒置覆盖本证据；2026-08-24 |
| `RULE-IDENTITYEL-DISPATCH-0001` | P0 | BLOCKED | unassigned | `src/ruleaction.rs`, `docs/api/ruleaction.md`, `tests/oracle/rule_identityel_dispatch_1204.cc`, `tests/oracle/rule_identityel_dispatch_1204.rs`, `tests/oracle/rule_identityel_dispatch_1204.metadata.json`, `tools/run_rule_identityel_dispatch_oracle.sh` | 依赖=`RULE-IDENTITYEL-OPCODESET-0001`、`ACTION-EXECUTOR-BREAKPOOL-0001`、`OPBANK-LIFECYCLE-0001`；在地基闭合后补完整 Rule tests/applies/break/resume 与 alive/dead/optree 调用闭包证据；同一 ruleaction writer 后续串行承接 BooleanDedup/SignMod/ConditionalMove；2026-08-23 |
| `FIXTURE-CURRENT-COVERAGE-AUDIT-0001` | P0 | DONE | audit_e2e_coverage（独立只读）+ root | `docs/alignment_audit/DEPENDENCY_DAG.json`, 本 TODO 行 | evidence=`this commit`；119 metadata/runner/C++、116 Rust、registry 118；raw registry 53 MATCH/32 PARTIAL/21 MISMATCH/12 UNTESTED，但 doctor=157/schema=274。53 raw MATCH 仅10个 current-source，26 stale、15不可证明、2无可靠路径；5,458 production exact-mapped 函数全 UNTESTED，registry 仅引用64个，其中 current comparand 14，逐 fixture 合规候选 MATCH仅2个，严格全局因 schema/registry 未闭合仍为0；2026-08-23 |
| `LANEDIVIDE-EVIDENCE-STATUS-REPAIR-0001` | P0 | READY（metadata-only，源码冻结） | unassigned | `tests/oracle/lanedivide_infra_1204.metadata.json`, `tools/run_lanedivide_infra_oracle.sh` | 依赖=`FIXTURE-CURRENT-COVERAGE-AUDIT-0001`；TODO 历史称 registry UNTESTED，实际 registry+metadata 为 MATCH 且 metadata 同时含 NO_ORACLE UB residual，doctor 正确报 MATCH_WITH_UNMATCHED_RESIDUAL；不得缩 residual，须以当前源码真实重跑后保守迁移 B2 状态并重钉，registry 由 root 集成时串行更新；2026-08-23 |
| `PROGRAM-FLOW-METADATA-AUDIT-0001` | P0 | DONE | audit_program_flow_metadata + java_sharedreturn/cmd_semantics（独立只读）+ root | `ALIGNMENT_ROADMAP.md`, `docs/alignment_audit/DEPENDENCY_DAG.json`, 本 TODO 行 | evidence=`this commit`；锁定 Java/C++ 全链确认 SharedReturn/NoReturn 先突变 Program Function/Instruction/Reference，live override 由 Java packed P-code消费，C++ restore XML是另一入口；callee可在 followFlow内 lazy query并 copy inline/no-return/injectid。Rugra缺 Program body/ref/override/function flags/callfixup、稳定 callee Funcdata 与及时消费，不能靠 ELF size/count-only post-link 替代；撤销 override L3并纠正 flow L1；2026-08-23 |
| `PROGRAM-FLOW-METADATA-FIXTURE-0001` | P0 | INTEGRATED（main `4e86624`+`beb5bf6`；registry/continuity 已随 `7b4b29b`/`8cb8829` 闭合） | program_flow_fixture_writer + fspec_output_review（独立APPROVE）+ root | `tests/oracle/program_flow_metadata_1204/`, `tests/oracle/program_flow_metadata_1204.metadata.json`, `tools/run_program_flow_metadata_oracle.sh`, `tests/oracle/fixture_registry.json` 对应条目 | 依赖=`PROGRAM-FLOW-METADATA-AUDIT-0001` 已满足；approved producer/provenance series 已集成 main=`4e86624`+`beb5bf6`：cache只保留官方zip SHA=`c3b45866…a120`，每次mktemp fresh extract并核12.0.4/PUBLIC/revision=`e40ed130…`，metadata顶层且决定性字段精确绑定；两lane×两fresh与三类坏cache故障注入均过，overall=UNTESTED、projection=ORACLE_CAPTURED；中央registry条目已准备，待 `FUNCTION-ID-POSTBASE-CONTINUITY-0001` 恢复全局doctor后提交；2026-08-24 |
| `FLOW-SHAREDRETURN-0001` | P0 | INTEGRATED（main `33793c1`，overall MISMATCH） | threefunc_ir_trace + root | `src/flow.rs`, `src/funcdata.rs`, `src/override_rs.rs`, `examples/curl_decompile.rs`, `docs/api/{flow,funcdata,override_rs}.md`, 专属双侧 fixture/runner；中央 registry 仅 root | 证据源=`aab77f7cd42cf66e351f3737cfad10ccf5389694`、集成=`33793c130fed5a7d0ecb858293dfcfd7bc23b88e`，独立 Cross-Review APPROVE、runner exit0；immutable All A/B：hugehelp skeleton 19→18并恢复第6个puts，progressbarinit 24→21并恢复free，my_fwrite逐字节零漂移；与Merge组合的main `e9a0b7a` 保持hugehelp同字节，并让progressbarinit使用分离后的`extraout_RAX`执行`strtol/free`。raw fixture 保留 RAM/null Address 与 callspec pointer/address lookup 差异，残差=`ADDRESS-PHASE2-CLOSURE-0001`/`CALLSPEC-0001`/完整 Java producer与instruction-limit；不得升L3；2026-08-24 |
| `FLOW-PROGRAM-METADATA-INGEST-0001` | P0 | BLOCKED | unassigned | `src/database.rs`, `src/fspec.rs`, `src/override_rs.rs`, `src/funcdata.rs`, `src/arch.rs`, `src/debugproto.rs`, `src/flow.rs` 及对应 API；实际实现时须按文件拆串行子任务 | 依赖=`PROGRAM-FLOW-METADATA-FIXTURE-0001`、`DATABASE-SCOPE-OWNERSHIP-FIXTURE-0001`、`FSPEC-MODEL-UNIFY-0001`；按 Database stable callee→fspec inject/copyFlowEffects→local override ownership→Architecture resolver→debug snapshot→Flow override-before-xref/queryCall 顺序接入，最终删除 driver post-flow parameter-count linking；禁止一个 writer 同时占全部文件；2026-08-23 |
| `ADDRESS-SCOPE-OWNERSHIP-AUDIT-0001` | P0 | DONE | audit_address_scope（独立只读）+ root | `ALIGNMENT_ROADMAP.md`, `docs/alignment_audit/DEPENDENCY_DAG.json`, 本 TODO 行 | evidence=`this commit`；重新完整读取 locked address/translate/op/block/flow/database/funcdata/varmap ownership 主链，确认 legacy Address/Range 与 space-aware 模型、Database Scope 与 Funcdata ScopeLocal 两组双模型尚未汇合；静态反例含 create 进 alive、target 不调用 op target、Flow u64 跨空间折叠、Block 无 cover、Scope clone/镜像 entry/每 pass 重建 local scope；历史局部 fixture 均不足以证明 current-tree closure；2026-08-23 |
| `ADDRESS-PHASE2-CLOSURE-0001` | P0 | INTEGRATED_PENDING_REGISTRY（main `8d9b8ba`+`35edd74`） | address_phase2_fixture_writer + address_final_review（独立APPROVE）+ root | `tests/oracle/address_phase2_closure_1204.cc`, `tests/oracle/address_phase2_closure_1204.rs`, `tests/oracle/address_phase2_closure_1204.metadata.json`, `tools/run_address_phase2_closure_oracle.sh`, `tests/oracle/fixture_registry.json` 对应条目 | 依赖=`ADDRESS-SCOPE-OWNERSHIP-AUDIT-0001`；approved series 已集成：commit/tree真实解析、固定asset key→path、OID→blob bytes→snapshot SHA、FD clean-env与最终字节回读均闭合；独立 reviewer 重读 Address/Bank/Flow/Block 后确认四类语义及runner provenance，完整runner rc0，overall=MISMATCH、cross-space visited=UNTESTED且边界残差未缩；中央registry条目待continuity恢复doctor后同批提交；2026-08-24 |
| `DATABASE-SCOPE-OWNERSHIP-FIXTURE-0001` | P0 | INTEGRATED_PENDING_REGISTRY（main `c45f1b1`+`ee3789a`+`ba4f54a`+`e77a17c`） | database_scope_fixture_writer + fspec_output_review（独立APPROVE）+ root | `tests/oracle/database_scope_ownership_1204.cc`, `tests/oracle/database_scope_ownership_1204.rs`, `tests/oracle/database_scope_ownership_1204.metadata.json`, `tools/run_database_scope_ownership_oracle.sh`, `tests/oracle/fixture_registry.json` 对应条目 | 依赖=`ADDRESS-SCOPE-OWNERSHIP-AUDIT-0001` 已满足；最终 series 已集成：compile-time production type helper真实区分Ghidra pointer/pointer/pointer与Rugra value/value/value，direct identity与lookup-slot identity分离，next_scope_id真实观察；full=MISMATCH、selected=MATCH、multi-child order=UNTESTED，runner前后双阶段锁状态/residual；独立reviewer重读locked Database/Scope并复验pins后APPROVE，现仅待中央registry登记；2026-08-24 |
| `HERITAGE-MERGE-LOCKED-AUDIT-0001` | P0 | DONE | audit_heritage_merge（独立只读）+ root | `ALIGNMENT_ROADMAP.md`, `docs/alignment_audit/DEPENDENCY_DAG.json`, 本 TODO 行 | evidence=`this commit`；完整读取 locked heritage.cc/.hh 与 merge.cc/.hh，当前源码 hash 与所有相关 metadata comparand 漂移；Heritage guard/returns/indexed/load/join 与 Merge ADDRTIED/persistent/groupPartial/mergeOp/relink/trim-order 存在可定位 MISSING/STUB/MISMATCH，撤销 Merge L3；2026-08-23 |
| `HERITAGE-FREE-SSA-FIXTURE-0001` | P0 | INTEGRATED_PENDING_REGISTRY（main `ad05f4a`） | heritage_free_ssa_fixture_writer + heritage_checkpoint_review（独立APPROVE）+ root | `tests/oracle/heritage_free_ssa_1204.cc`, `tests/oracle/heritage_free_ssa_1204.rs`, `tests/oracle/heritage_free_ssa_1204.metadata.json`, `tools/run_heritage_free_ssa_oracle.sh`, `tests/oracle/fixture_registry.json` 对应条目 | 依赖=`HERITAGE-MERGE-LOCKED-AUDIT-0001` 已满足；checkpoint返修已集成main=`ad05f4a`：source固定 `36633d9`，103项版本化closure、canonical fingerprint与单Cargo flock均经独立复核；完整runner五行PASS，covered=MATCH/overall=MISMATCH、两项异常MISMATCH和其余UNTESTED均未缩；中央registry可在continuity扩展后启用；2026-08-24 |
| `MERGE-ADDRTIED-FIXTURE-0001` | P0 | REWORK（follow-up `500c083` REJECT） | merge_addrtied_fixture_writer + address_final_review（独立REJECT） | `tests/oracle/merge_addrtied_current_1204.cc`, `tests/oracle/merge_addrtied_current_1204.rs`, `tests/oracle/merge_addrtied_current_1204.metadata.json`, `tools/run_merge_addrtied_current_oracle.sh` | 依赖=`HERITAGE-MERGE-LOCKED-AUDIT-0001`；follow-up虽补真实copy-trim diamond/updateIntersections并保留overall=MISMATCH，独立复核仍命中两项B2阻断：Ghidra在真实G30/G31调用groupWith，而Rust超时探针使用无instance/size0 synthetic High且无启动握手，不是同输入；C++直接遍历真实copyTrims，Rust却扫描IR反推trim_ops，不能把pending-vector顺序标MATCH。须改等价隔离图/真实channel observer或将两项降UNTESTED后重跑重审；2026-08-24 |
| `HERITAGE-GUARD-NORMALIZE-0001` | P0 | INTEGRATED（R9 附条件 APPROVE 全闭合；`f95369a`+`05b6b44`+`e2e0612`+`f914ac3`+`e76fa01`） | a16 + R9 + root | `src/{heritage,varnode}.rs`, docs, `tests/oracle/heritage_guard_normalize_1204.*`, runner | 10 项差异表闭合；F1 BE overlap（varnode.cc:221-226）/F2 两处属性尾已修，funcdata 两处→`FUNCDATA-NEWVARNODE-FLAGS-TAIL-0001`（a33 修中）；case 2 翻 MATCH 由 a34 重钉验证（与 5a6d13d 合流）；剩余=indexed/join 切片；2026-08-25 |
| `MERGE-ADDRTIED-CLOSURE-0001` | P0 | BLOCKED | unassigned | `src/merge.rs`, `docs/api/merge.md`, `tests/oracle/merge_addrtied_1204.cc`, `tests/oracle/merge_addrtied_1204.rs`, `tests/oracle/merge_addrtied_1204.metadata.json`, `tools/run_merge_addrtied_oracle.sh` | 依赖=`ADDRESS-PHASE2-CLOSURE-0001`、`MERGE-PERSISTENT-STATE-0001`；补 space/ADDRTIED/free/max-overlap/partial groupWith 与 StackAffectingOps/testCache/protoPartial 通道，观察 High/VariablePiece 身份、cache/copyTrims 首次出现序、异常和完整 IR；同一 merge.rs writer 串行，独立 Cross-Review 后才可集成；2026-08-23 |
| `MERGE-ADDRTIED-GATES-0001` | P0 | INTEGRATED（main `e9a0b7a`，scoped 8/8，overall MISMATCH） | threefunc_history_audit + root | `src/merge.rs`, `docs/api/merge.md`, 专属双侧 fixture/runner；中央 registry/roadmap 由 root 承接 | 证据源=`c8001f507131d0be5da527c1756971925dfbff85`、集成=`e9a0b7a`；冷启动双侧8/8逐字节相同且scoped Cross-Review APPROVE。my_fwrite isolated A/B仅 skeleton45→44、decl30→29、implied3→1；与Flow组合后progressbarinit由错误`lVar1`分离为`extraout_RAX`并正确传给`strtol/free`，全语料flow5→1、implied259→136且可加和无回退。它仍是split后的合并放大器而非首因。整体MISMATCH及三项残差不变；2026-08-24 |
| `MERGE-ADDRTIED-ERROR-CHANNEL-0001` | P0 | MISMATCH | unassigned | `src/merge.rs`, `src/coreaction.rs`, `docs/api/{merge,coreaction}.md`, paired exception fixture | 依赖=`MERGE-ADDRTIED-GATES-0001`；让 production caller 传播 locked `LowlevelError` 等价通道与异常前部分状态，删除 `Result`→panic 适配；2026-08-24 |
| `MERGE-ADDRSPACE-RUNTIME-TYPE-0001` | P0 | MISMATCH_SOURCE_PROVEN | unassigned | `src/space.rs`, `src/merge.rs`, `docs/api/{space,merge}.md`, paired custom-space fixture | 恢复任意 index 的 runtime `SpaceType`，覆盖多个 PROCESSOR/SPACEBASE；当前 `Other(non-1)` fail-closed 不等价；2026-08-24 |
| `MERGE-GROUPED-REQUIRED-CLOSURE-0001` | P0 | MISMATCH | unassigned | `src/merge.rs`, `src/variable.rs`, `docs/api/{merge,variable}.md`, paired grouped fixture | 对齐双方均 grouped 与单边 grouped 的 required merge、dirty/symbol/piece-transfer 顺序及异常前状态；2026-08-24 |
| `MERGE-ADDRTIED-ERROR-BRANCHES-0001` | P1 | UNTESTED | unassigned | `src/merge.rs`, `docs/api/merge.md`, `tests/oracle/merge_addrtied_gates_1204.{cc,rs,metadata.json}`, `tools/run_merge_addrtied_gates_oracle.sh` | 补 cover intersection、既有 group owner、duplicate-piece、speculative merge-class 的同输入异常 fixture；2026-08-24 |
| `MERGE-ADDRTIED-ORDER-DISAMBIGUATION-0001` | P1 | UNTESTED | unassigned | `tests/oracle/merge_addrtied_gates_1204.{cc,rs,metadata.json}`, `tools/run_merge_addrtied_gates_oracle.sh` | 构造 creation order 与 input/written、SeqNum comparator order 相反的判别输入，不得排序抹平；2026-08-24 |
| `MERGE-ADDRTIED-PROJECTION-CLOSURE-0001` | P1 | UNTESTED | unassigned | `tests/oracle/merge_addrtied_gates_1204.{cc,rs,metadata.json}`, `tools/run_merge_addrtied_gates_oracle.sh` | 真实观察 native pieceSet 顺序、High/Varnode/group cover/type/symbol/cache/copyTrims 与 op-edge 完整突变；2026-08-24 |
| `MERGE-ADDRTIED-BOUNDARY-SPACE-COVERAGE-0001` | P1 | UNTESTED | unassigned | `src/merge.rs`, `tests/oracle/merge_addrtied_gates_1204.{cc,rs,metadata.json}`, `tools/run_merge_addrtied_gates_oracle.sh` | 判别 next-run begin==inclusive max 的 `<=` 边界，并覆盖 Overlay/Iop/Fspec/Const；2026-08-24 |
| `MERGE-ADDRTIED-CALLER-CLOSURE-0001` | P0 | MISMATCH | unassigned | `src/coreaction.rs`, `src/merge.rs`, `docs/api/{coreaction,merge}.md`, paired Action fixture | surrounding Action 仍构造局部 Merge 且不能传播 Result/LowlevelError；需闭合真实 caller ownership/异常通道；2026-08-24 |
| `MERGE-ADDRTIED-DEAD-WRITTEN-PRECONDITION-0001` | P1 | UNTESTED_CALLER_PRECONDITION | unassigned | `src/funcdata.rs`, `src/merge.rs`, paired caller fixture | 判定 opUninsert 后无 parent 的 written output 在合法 merge-stage caller 中是否可达；禁止在 merge 层私加 live_set 过滤；2026-08-24 |
| `TYPE-PTRWIDTH-PTRSUB-0001` | P0 | PARTIAL_INTEGRATED（Action width main `92daed3`；overall MISMATCH） | progressbar_rootcause + review_action_width_slice（独立APPROVE）+ root | 已集成=`src/typeop.rs`, `docs/api/typeop.md`, `src/coreaction.rs`, `docs/api/coreaction.md`, 两组专属双侧 fixture/metadata/runner；后继 PTRSUB/downChain/STOP 另租约 | TypeOp地基=`577e54b`+`6e5099c`+`172582b`；生产 Action source=`ae3a668`、main=`92daed3`。外部 candidate OID 冷快照 runner exit0：LOAD/STORE 以真实16/4/32B访问宽度判定32B pointee传播，selected projection MATCH；Ghidra/Rust stdout SHA=`f20f3c8a…b75f`/`9b73e923…a701`，stderr双空，raw diff仅6个已声明`array→unknown` token。immutable curl A/B：progressbarinit skeleton 22→20、my_fwrite 44→16、hugehelp逐字节零漂移，证明宽度缺门是前两者真实根因之一；整体仍受 `ACTION-INFERTYPES-DISPATCH-0001`、`TYPE-UNKNOWN-0001`、`TYPEFACTORY-EXACTPIECE-0001`、`TYPEFIELD-IDENT-REPRESENTATION-0001` 约束，PTRSUB downChain/PointerRel与getLocalType STOP尚未闭合，不得升L3；2026-08-24 |
| `SPLITDATATYPE-EXACTPIECE-0001` | P0 | INTEGRATED（R15 REJECT→返修→APPROVE；四提交 `a5552de`..`2a43884`→main `26d675e`..`c509a6c`，25/25 MATCH 含 M-1 判别 case） | a41 + R15 + root | | `src/subflow.rs`（946 行）+docs+fixture | 20/20 双侧 MATCH；FILE*+8 不误拆/ProgressData partial/identity/二轮稳定四验收全过；误拆根因（collect_components+双计）已删；E2E 留 A36 合流（预期 my_fwrite 45→~16）；剩余 5 项；2026-08-25 |
| `SUBFLOW-ROOTPOINTER-PORT-0001` | P1 | INTEGRATED（`28c15cae`+`330e6631`→main `3b6e3139`+`1f26a807`，31/31 MATCH，编译绿） | a58 + root | `src/subflow.rs`+docs+fixture | RootPointer find 多跳/addrTied duplicateToTemp/freePointerChain+两回溯腿；flat_array UNTESTED 翻 MATCH；待复核 | 2026-08-25 |
| `BLOCK-STOPADDR-FIXTURE-REGRESSION-0001` | P1 | INTEGRATED（`8d59b77`+`2e2e5a2`→main；38/38 逐字节+comment:: 16/16） | a54 + root | `src/block.rs`（一行可见性）+comment.rs 测试+fixture 重钉 | 方向判定=fixture 非法状态（Ghidra C++ fixture 自装 cover），生产语义零改动；同源风险（graph/merge/funcdata 的 start_addr 回退）归 BLOCKBASIC-COVER-0001 存量；2026-08-25 |
| `HERITAGE-TRYOUTPUT-STACKGUARD-CONTAINS` | P2 | INTEGRATED（`3a5563ea`→main `3dc4b2ae`，8/8 MATCH；R22 复核 APPROVE） | a78 + root | `src/heritage.rs`+docs+fixture | 第 4 触点 cc:1420 移植+前任死锁修复；FSPEC-OUTPUT-STORAGE 残差由 `FSPEC-OUTPUT-STORAGE-0001` 承接闭合 | 2026-08-25 |
| `FSPEC-OUTPUT-STORAGE-0001` | P1 | CANDIDATE（agent/fspec-outstore worktree；权威 runner 11/12 MATCH 待 root 集成） | a79@wt-outstore | `src/{fspec,coreaction,heritage}.rs`, `docs/api/{fspec,coreaction,heritage}.md`, `tests/oracle/heritage_tryoutput_1204.{cc,rs,metadata.json}`, `tools/run_heritage_tryoutput_oracle.sh` | FuncProto proto-store 输出存储（`output_storage`+`set_output_parameter` 全量记录）+ characterize/getBiggest 锁定分支（fspec.cc:4339-4353/4495-4506）+ funcLinkOutput cc:1538-1553 生产者（setStackOutputLock/记录地址 newVarnodeOut）+ guardCalls 真实读取（try_output_stack_guard 删 None 暂存参数、自读 cc:1407/1410 getOutput()）；fixture 扩为四 case（含 production_entry_guardcalls 全链与 register 控制 INDIRECT 对照）；BE 栈空间 UNTESTED 保持 | 2026-08-25 |
| `OPTIONS-SPLITDATATYPE-SEMANTICS-0001` | P1 | QUEUED（options.rs 自由） | 待发射 | `src/options.rs`（OptionSplitDatatypes 位语义与 12.0.4 不符） | a41 剩余差异 4；2026-08-25 |
| `PRINTC-PTRCHAR-CONSTANT-0001` | P1 | **CLOSED**（生产接线已交付：MAINDIFF-STRCONST-0001，wip `460ab6a6` + align `e69f2c5e`） | cptrb@wt-cptrb | `src/printc.rs`, `docs/api/printc.md` | 旧证据=`fbf326600ed31fcbfbf593020bbab6accb096124`（双端14条MATCH，当时生产路径未接）；2026-08-26 生产链闭环：RPN 叶片 `constant_leaf_text` 完整 pushConstant 分派（printc.cc:1749-1815）+ RulePtrsubCharConstant 真守护链（ruleaction.cc:7354-7403）+ driver TYPELOCK/READONLY 旗标 + 引号感知空白折叠；E2E 验收= hugehelp 6 个 puts 参数与 golden 12.0.4 逐字节一致（3×&DAT + 3×字面量 2130/2103/2137B）、`curl_getenv("COLUMNS")`/`fopen(...,"wb")` 折叠、defects=0/numbering=0（skeleton 2186）；残差=折链专属双侧 unit fixture（cptr_b_1204 metadata `string_literal_fold_chain`=UNTESTED，E2E 差分已覆盖行为面）；2026-08-26 |
| `FSPEC-PARAMLIST-OUTPUT-DISPATCH-0001` | P0 | INTEGRATED_CONTINUITY_REGISTRY_PENDING（main `87f9309`+`805cf88`+`7224fb1`） | fspec_writer + fspec_b_review（独立APPROVE）+ root | 原批：`src/fspec.rs`, `docs/api/fspec.md`, `tests/oracle/fspec_paramlist_output_1204.{cc,rs,metadata.json}`, `tools/run_fspec_paramlist_output_oracle.sh`；返修A：`tests/oracle/fspec_phase0_1204.{rs,metadata.json}`, `tools/run_fspec_phase0_oracle.sh`；返修B：`src/coreaction.rs`, `docs/api/coreaction.md`, `src/heritage.rs`, `docs/api/heritage.md`, `src/type_system/protomodel.rs`, `docs/api/type_system/protomodel.md` | 完整三提交series已集成：17个spaceless caller迁至真实Varnode/range/model-entry space，无名称猜测/默认Register，查询/注册次数、trial顺序、slotbase与size时机不变；ActionFuncLink1/1、ProtoModel5/5、Heritage12/12、cargo check通过。B caller闭包仍UNTESTED，phase0投影MATCH/overall UNTESTED，output保持真实MISMATCH；等待本wave continuity扩展与两条中央registry登记；2026-08-24 |
| `FSPEC-MODEL-UNIFY-0001` | P0 | BLOCKED（同 worktree 串行后继） | fspec_writer | `src/fspec.rs`, `src/coreaction.rs`, `src/arch.rs`, `docs/api/fspec.md`, `docs/api/coreaction.md`, `docs/api/arch.md`, `tests/oracle/fspec_model_unify_1204.cc`, `tests/oracle/fspec_model_unify_1204.rs`, `tests/oracle/fspec_model_unify_1204.metadata.json`, `tools/run_fspec_model_unify_oracle.sh` | 依赖=`FSPEC-PARAMLIST-OUTPUT-DISPATCH-0001`；oracle `FuncCallSpecs` 继承 `FuncProto` 且只有一个 `model`，Rugra 当前有 calling-convention 字符串、`FuncProto.model` 与 callspec legacy SysV 三个可漂移来源；前置完成后统一绑定共享 full model 并删除 coreaction SysV 回退；独立 target=`/tmp/rugra-target-fspec-writer`；验收=同 cspec/同输入 trial 终态与所有状态突变双侧 MATCH + cargo check/lib tests；`src/coreaction.rs` 变更必须独立 Cross-Review；evidence=pending；2026-08-23 |
| `FUNCTION-ID-MIGRATE-REKEY-0001` | P0 | DONE（main `61631f2`） | function_id_rekey_writer；root 串行集成 | `tools/generate_function_ledger.py`, `tools/oracle_registry.py`, `docs/alignment_audit/FUNCTION_ID_MIGRATION.json`, `tools/README.md`, 本 TODO 行 | 依赖=`FUNCTION-LEDGER-REFRESH-0001` 已满足；main=`61631f2` 已锁定历史闭包：24323 live+47 tombstone=24370 origins，146 live lineage/149 alias、132严格自动/6签名演进/8跨名 successor/3 two-hop；两个工具self-test、reconcile --check、gate与确定性全绿，plan=`auto9/manual0/family0/tombstoned0/collateral5/unmappable21(14 unique)`；后续源码身份连续性由独立 `FUNCTION-ID-POSTBASE-CONTINUITY-0001` 承接，不改写本历史基线；2026-08-24 |
| `FUNCTION-ID-POSTBASE-CONTINUITY-0001` | P0 | DONE_INITIAL_CHECKPOINT（main `450806f`+`970d77e`+`cd61a61`） | function_id_rekey_writer + halfdelete_cross_review（独立APPROVE）+ root | `tools/generate_function_ledger.py`, `tools/oracle_registry.py`, `docs/alignment_audit/FUNCTION_ID_CONTINUITY.json`（新；历史 migration 保持不可变）, `docs/alignment_audit/FUNCTION_LEDGER.json`, `docs/alignment_audit/FUNCTION_MAP.generated.md`, `docs/alignment_audit/PROTOCOL_TABLE.json`, `docs/alignment_audit/DEPENDENCY_DAG.json`, `tools/README.md`, 本 TODO 行 | 依赖=`FUNCTION-ID-MIGRATE-REKEY-0001` 已满足；最终三提交栈已集成，checkpoint `36633d9` 闭合9639→9640、2 lineages+1 introduced；exact reviewed allowlist、used==declared、tomb alias/event链、跨层/collision/断链均fail-closed，恶意half in/out交换实测rc2；introduced后删除在schema1显式rc2。Flow A/B后续源码已启动append-only continuity扩展，历史 migration SHA保持不变；2026-08-24 |
| `ORACLE-METADATA-MIGRATE-0001` | P0 | BLOCKED | root | 本 TODO 行；解锁后再为每批 metadata/runner 登记互斥精确 write-set | 依赖=`ORACLE-REGISTRY-PROVENANCE-HARDEN-0003`、`FUNCTION-ID-MIGRATE-REKEY-0001`；逐 fixture 按 B2 四态迁移，禁止把部分覆盖升级为 `MATCH`，禁止缩窄 residual；最终验收=`python3 tools/oracle_registry.py migration-status --strict`；evidence=pending；2026-08-23 |
| `ORACLE-REGISTRY-HARDEN-0002` | P0 | DONE | root + registry_repair_audit（实现）+ ledger_refresh_audit（独立只读复核 APPROVE） | `tools/oracle_registry.py`, `tools/README.md`, `docs/alignment_audit/DEPENDENCY_DAG.json`, 本 TODO 行 | evidence=`this commit`；nested/shared/paired output pin 只认精确 64hex 白名单且任一已声明 side 缺边即失败，消除 11 个假 `SINGLE_SIDE_MATCH`；递归 coverage/residual 重新抓出 10 个错误 `MATCH`；状态完整分隔解析，runner/metadata 多 owner/多链接 fail-closed；新增 deterministic `migration-status --strict` 合并 doctor/schema/pre-B2 与未引用 rekey family；31 项 self-test 全绿，坏 schema/$ref/migration 输入 rc=2 无 traceback；真实树 doctor=157、schema=274、migration blockers=459，text/json 双跑逐字一致，plan auto/manual=0/0、unmappable=30、rekey family=193；2026-08-23 |
| `ORACLE-REGISTRY-PROVENANCE-HARDEN-0003` | P0 | READY | root | `tools/oracle_registry.py`, `tools/README.md`, `docs/alignment_audit/DEPENDENCY_DAG.json`, 本 TODO 行 | 依赖=`ORACLE-REGISTRY-HARDEN-0002`；当前 input hash 只检查非空、architecture/compiler spec/options 只检查 truthy、空 comparand dict 可绕过 `MATCH`，MiniSchemaValidator 对未来未知 keyword 也须 fail-closed；须按现存 metadata 形态建立显式 hash/type/comparand 合同与 synthetic negative matrix，禁止把旧脏数据静默解释为合规；验收=坏长度/前缀 hash、错误 JSON 类型、空 comparand、未知 schema keyword 全部稳定 finding 或 rc=2；evidence=pending；2026-08-23 |
| `ORACLE-RUNNER-PIN-0001` | P0 | DONE | root + runner_pin_audit（独立只读复核 APPROVE） | `tools/run_op_insert_oracle.sh`, `tests/oracle/op_insert_1204.metadata.json`, `tests/oracle/fixture_registry.json` 中 `op_insert_1204` 条目, `docs/alignment_audit/DEPENDENCY_DAG.json`, 本 TODO 行 | evidence=`this commit`；固定 Rugra commit=`183d2a1b8b62b865b177ece7001dd18dd816f82d`/tree=`85636ac61b6ec5b4140b660fc8587f6eccb3d7ff`/curl blob=`4e26a362f92ac1961bab63000215a84b4d7212dd`（162544 B，sha256=`4ee4002b...`），不再读取 `HEAD`；direct runner 对 runner/fixture/BFD header+SO 全部 comparand pin，input manifest 指纹=`c73d1915...`，同时拒绝 Ghidra cpp/x86 的 worktree/index 漂移；显式锁定 BFD 2.38 双侧完整运行 rc=0，covered projection=`MATCH`、stdout sha256=`adff0e77...` 不变；overall 由非法 `PARTIAL_MATCH` 保守迁为 `MISMATCH`，绑定 `OPBANK-0001` MISMATCH + `SEQNUM-0001` UNTESTED residual；doctor 对 `op_insert_1204` 专属 finding=0；cache 与 hermetic 构建闭包分别由后续 P0 承接；2026-08-23 |
| `ORACLE-BFD-CACHE-PROVENANCE-0001` | P0 | REWORK（`agent/bfd-cache-provenance`，candidate rejected） | bfd_cache_writer + bfd_cache_review（独立只读 REJECT） | `tools/oracle_cache.py`, `tools/rugra_gate.py`, `tools/select_fixtures.py`, `tests/oracle/schema/fixture-v1.schema.json`, `tests/oracle/fixture_registry.json` 中BFD fixture cache项, `tools/README.md` | 依赖=`ORACLE-RUNNER-PIN-0001`、`ORACLE-REGISTRY-HARDEN-0002` 已满足；candidate `f76361c5...` 不得集成：原子替换、同 inode 写回、parent symlink `A→B→A` 均实证 child 读 EVIL、manifest 记 OLD，随后 OLD provenance HIT 回放 EVIL；相对 ENV/chdir、final-check 后换档、`lstat→resolve` 窗口、mode 与非 UTF-8 错误也未闭合，且只 pin bfd.h/SO 会漏 include tree 与 DT_NEEDED。`op_insert_1204`、`database_scope_ownership_1204`、`fspec_paramlist_output_1204` 当前均必须 `--no-cache`，后两项中央registry暂不列always tiers；正确修复须从已打开 fd 物化 content-addressed 私有只读 snapshot，把 child ENV 重写到同一 snapshot root，并包含完整 include/动态依赖闭包，禁止轮询/advisory-lock 假闭合；2026-08-24 |
| `TYPE-IDENTITY-TREE-AUDIT-0001` | P0 | DONE | audit_typefactory_datatype + ghidra_typefactory（独立只读复核）+ root | `ALIGNMENT_ROADMAP.md`, `docs/alignment_audit/DEPENDENCY_DAG.json`, 本 TODO 行 | evidence=`this commit`；完整读取 locked type.cc/.hh、typefactory/typeop/cast 对应实现，确认现有 182/220-record fixture 只是限定投影；stable identity、structural tree、多 ID 名称索引、aggregate/code decode、local-type canonical dispatch 仍未闭合，并撤销 cast 旧 L3；2026-08-23 |
| `TYPEFACTORY-STABLE-IDENTITY-FIXTURE-0001` | P0 | REWORK（candidate `d706174` REJECT） | type_identity_fixture_writer + fspec_output_review（独立REJECT） | `tests/oracle/typefactory_identity_1204.cc`, `tests/oracle/typefactory_identity_1204.rs`, `tests/oracle/typefactory_identity_1204.metadata.json`, `tools/run_typefactory_identity_oracle.sh` | 依赖=`TYPE-IDENTITY-TREE-AUDIT-0001`；`d706174` 的253 records=187 MATCH+66真实MISMATCH获确认，但runner仍从live四文件/CARGO_HOME取证、无captured commit执行FD/verified vendor闭包；residual误绑已DONE codeflags而非 `FUNCPROTO-DECODE-0001`，identity分裂后的return/lookup/typedef-target完整shape未观察。作者正改为单一captured OID物化、四owned pre/post readback、Cargo.lock checksum vendor/fresh CARGO_HOME，并补完整状态或降UNTESTED；新commit必须重跑重审，root后继registry/CI提供外部Git OID锚；2026-08-24 |
| `CAST-CANONICAL-IDENTITY-0001` | P1 | BLOCKED | unassigned | `src/type_system/cast.rs`, `docs/api/type_system/cast.md`, `tests/oracle/cast_identity_1204.cc`, `tests/oracle/cast_identity_1204.rs`, `tests/oracle/cast_identity_1204.metadata.json`, `tools/run_cast_identity_oracle.sh` | 依赖=`TYPEFACTORY-CORE-PROMOTION-IDENTITY-0001`、`TYPEFACTORY-POINTER-CANONICAL-0001`；删除每次新建 Arc 的伪 factory/恒 false identity 比较，覆盖同 handle、结构相等但不同 identity、typedef、pointer space、返回 alias class 与最终 token；2026-08-23 |
| `ORACLE-RUNNER-HERMETIC-0001` | P0 | READY | root | `tools/run_op_insert_oracle.sh`, `tests/oracle/op_insert_1204.metadata.json`, `tests/oracle/fixture_registry.json` 中 `op_insert_1204` cache/comparand, `tools/README.md`, `docs/alignment_audit/DEPENDENCY_DAG.json`, 本 TODO 行 | 依赖=`ORACLE-RUNNER-PIN-0001`；当前 direct runner 仍可复用 oracle 源树中的共享 `libdecomp.a`/`com_opt`，ambient `g++`/`make`/`cargo`/`rustc` 与 SLEIGH specs 尚未形成完整构建 provenance；须改为隔离构建并记录可执行文件、版本、参数与全部 spec/input 哈希，任一漂移 fail-closed；验收=冷/热两次隔离运行 raw stdout hash 相同，注入共享 archive/toolchain/spec 漂移均被拒绝；evidence=pending；2026-08-23 |
| `ORACLE-BFD-RUNNER-MIGRATE-0001` | P0 | BLOCKED（协调父任务） | root | 本 TODO 行；激活后为每个 runner 单独登记 metadata/registry/runner 精确 write-set | 依赖=`ORACLE-BFD-CACHE-PROVENANCE-0001`、`ORACLE-RUNNER-HERMETIC-0001`；除 `op_insert` 外另有 5 个读取 BFD env 的 runner，以及约 50 个硬编码 BFD 2.38 路径的 runner，部分缺 header/SO hash 或 `LD_LIBRARY_PATH`；须逐 fixture 重跑双侧并重钉，禁止批量字符串替换后沿用旧证据；验收=runner 中 ambient/hardcoded BFD 路径为 0、每个 BFD fixture 的外部资产进入 cache/direct-run provenance 且真实 rerun；evidence=pending；2026-08-23 |

## 历史 wave：`W-2026-08-22-ORCH`（已过截止窗口，保留交付记录）

> 2026-08-22 06:15 启动，截止窗口=当日 12:00。root 只做串行集成/门禁/看板登记，全程不被阻塞；
> ≥10 个后台子 Agent 并行（writer + 只读审计混合）。每个 writer 在独立 git worktree
> （`wt/<agent>` 分支）工作，worktree 内默认独立 `target/`，禁止共享构建目录；
> worker 禁改本文件、禁 `git add -A`、禁碰他人 write-set、禁 restore/checkout 他人文件；
> writer 交付=自己分支上的原子 commit + 验证输出，由 root cherry-pick 串行集成后跑全量门禁。
> 只读审计 Agent 交付=`docs/alignment_audit/<NAME>_2026-08-22.md` + 拟登记 TODO 清单（root 代登记）。
> `GATE-WORKTREE-GITDIR-0001`（P0 基础设施）：DONE=`973c052`（worktree GIT_DIR 劫持 gate health 已修，
> 修复已同步全部 10 个 worktree + ghidra symlink；writer 提交不再需要 --no-verify）。

### 本 wave 认领租约（root 串行登记；worker 禁改本文件）

| ID | P | 类型 | owner（worktree 分支） | write-set（互斥） | 目标 |
|---|---|---|---|---|---|
| `GETSTR-ZERODIFF-0001` | P1 | HISTORICAL / SUPERSEDED（旧 scoped snapshot，不是当前状态） | historical `wt/getstr` | 历史 write-set 保留在 git；当前任务统一见顶部 `GETSTR-CHAR-READFACING-0001`、`GETSTR-PRINTC-STRUCTCOND-0001`、`BLOCK-STRUCTURED-NEGATE-0001` | 旧提交组曾在当时 snapshot 声称 GetStr final C 逐字节一致，并使用现已删除的 `BlockIf.negated`/拓扑补偿。该结论不得作为当前证据：fresh 六阶段门禁为五个可比较 stage `MISMATCH`、Heritage `NO_ORACLE`；只有顶部登记的 char read-facing/token、272 raw Varnode state、Atom metadata、structured-negate 与最终短路字符字面量等 scoped projection 为 MATCH。overall 保持 MISMATCH，无 L3；last_updated=2026-08-28。 |
| `LANEDIVIDE-INFRA-0001` | P0 | writer | lanedivide_finish (`wt/lanedivide`) | `src/{arch,funcdata,subflow}.rs`, `docs/api/{arch,funcdata,subflow}.md`, `tests/oracle/lanedivide_infra_1204.*`, `tools/run_lanedivide_infra_oracle.sh` | 续作：完成 fixture metadata + oracle 对拍（lookup/min/ordered map、失败零突变、PIECE/SUBPIECE/MULTIEQUAL 投影） |
| `SCOPELOCAL-QUERY-0001` | P1 | writer | scopelocal_query_r2 (`wt/scopelocal`) | `src/varmap.rs`, `docs/api/varmap.md`, `tests/oracle/scopelocal_query_1204.*`, `tools/run_scopelocal_query_oracle.sh`（本轮不含 funcdata.rs） | r2：修 r1 REJECT（stable Vec/min_by_key、queryProperties flags/parent/space）+ 双侧 fixture |
| `PIPE-DERIVED-TREE-0001` | P0 | writer | pipe_tree_agent (`wt/pipetree`) | `src/action.rs`, `docs/api/action.md`, `tests/oracle/pipeline_tree_1204.*`, `tools/run_pipeline_tree_oracle.sh`（完成后同 agent 续 `PIPE-STACKSTALL-COUNT-0001` 的 `src/coreaction.rs`） | 完成 derive tree fixture（Ghidra resetDefaults/getCurrent vs Rust default database，DFS path/ordinal/kind/name/flags） |
| `TYPEFACTORY-LOCALTYPE-CACHE-0001` | P0 | writer | typefactory_rework (`wt/typefactory`) | `src/type_system/typefactory.rs`, `docs/api/type_system/typefactory.md`, `tests/oracle/typefactory_local_cache_1204.*`, `tools/run_typefactory_local_cache_oracle.sh` | 续作 r1 REJECT 项：noncore promotion 别名、同名冲突异常、core enum tree、char cache-miss、空 ctor/bootstrap、decodeCoreTypes |
| `DATATYPE-TYPEORDER-RESIDUAL-0001` | P1 | writer | datatype_residual (`wt/datatype`) | `src/type_system/datatype.rs`, `docs/api/type_system/datatype.md`, `tests/oracle/datatype_type_order_1204.*` | 解除阻塞（R3 已冻结 `5fb36f0`）：TypeCode varargs/递归 compare、Array/Union/Partial、Struct field、24 SubMetatype |
| `COMMENT-SORTER-ITERATORS-0001` | P1 | writer | comment_sorter (`wt/comment`) | `src/comment.rs`, `docs/api/comment.md`, `tests/oracle/comment_sorter_iterators_1204.*`, `tools/run_comment_sorter_iterators_oracle.sh` | setupBlockList/setupOpList/setupHeader 共享 start/stop/opstop 迭代器状态移植+fixture |
| `BLOCK-FINDIRREDUCIBLE-0001` | P2 | writer | block_findirred (`wt/blockirred`) | `src/block.rs`, `docs/api/block.md`, `tests/oracle/block_findirreducible_1204.*`, `tools/run_block_findirreducible_oracle.sh` | block.cc:1178+ findIrreducible 移植（FIND 复用+wipe-first 交互）+fixture |
| `RULE-GAPS-2026-08-22` | P1 | 审计 | rule_gaps_audit（只读） | 仅 `docs/alignment_audit/RULE_GAPS_2026-08-22.md` | ruleaction.cc vs ruleaction.rs 缺失/未注册 Rule 全量清单+拟登记 TODO |
| `COREACTION-GAPS-2026-08-22` | P1 | 审计 | coreaction_gaps_audit（只读） | 仅 `docs/alignment_audit/COREACTION_GAPS_2026-08-22.md` | coreaction.cc 剩余 ~20 Action（FuncCallSpecs/HighVariable 依赖型）逐个差距+依赖 DAG |
| `CONDEXE-GAPS-2026-08-22` | P2 | 审计 | condexe_gaps_audit（只读） | 仅 `docs/alignment_audit/CONDEXE_GAPS_2026-08-22.md` | condexe.cc findInitPre/forceSpecific/removeBlockEdges/setOut 图重写缺失清单+移植方案 |
| `JUMPTABLE-GAPS-2026-08-22` | P2 | 审计 | jumptable_gaps_audit（只读） | 仅 `docs/alignment_audit/JUMPTABLE_GAPS_2026-08-22.md` | jumptable.cc JumpBasicOverride/PathMeld/EmulateFunction 差距+依赖链 |

> wave-2 预留：各 writer 完成后由 root 触发机制 C 独立复核 Agent（varmap 核心算法层/
> subflow 属核心算法白名单的按 AGENTS.md 处理）；复核通过才集成。

### 审计产出登记（root 代登记；writer 释放后作为补员池）

来自 `docs/alignment_audit/JUMPTABLE_GAPS_2026-08-22.md`（128 函数对照：43 MATCH/29 PARTIAL/
21 MISMATCH/33 MISSING/14 新缺口；结论=守卫/range 比 ROADMAP #21 记载严重一个量级，
最重地基是 Funcdata 部分克隆 + "jumptable" action 策略链）：

| ID | P | 标题 | write-set | 依赖 |
|---|---|---|---|---|
| `JUMPTABLE-EMULFN-0001` | P0 | EmulateFunction 1:1 重写（LOAD/MULTIEQUAL/CALL 忽略/BRANCH 抛错/loader fallback/lastOp） | `src/jumptable.rs`(EmulateFunction)、`src/funcdata.rs`(loader 访问器)、`docs/api/jumptable.md` | loader 桥（loadimage.rs trait 已有） |
| `JUMPTABLE-CALCRANGE-0001` | P0 | calcRange 守卫交集写回 + isBoolOutput | `src/jumptable.rs`、API 文档 | RANGE-0001 |
| `JUMPTABLE-GUARDS-0001` | P0 | analyzeGuards 完整移植 + checkUnrolledGuard 接线 + value_match 补全 | `src/jumptable.rs`、API 文档 | —（可与上并行） |
| `JUMPTABLE-SELECTION-0001` | P0 | recover_model 选择顺序（Assisted→Basic→Basic2，去 Trivial 回退，maxtablesize 读 arch） | `src/jumptable.rs`、`src/arch.rs`、API 文档 | JUMPTABLE-EMULFN-0001 |
| `JUMPTABLE-OVERRIDE-0001` | P1 | Override 闭包（findStartOp/trialNorm/…/encode/decode） | `src/jumptable.rs`、`src/dynamic.rs`、API 文档 | JUMPTABLE-EMULFN-0001 |
| `JUMPTABLE-ASSISTED-0001` | P1 | JumpAssisted 全方法 + JumpAssistOp 接线 | `src/jumptable.rs`、`src/userop.rs`、`src/pcodeinject.rs`、API 文档 | INJECT-0001 |
| `JUMPTABLE-TABLEAPI-0001` | P0 | label 管道半部 DONE @ wt/sb-jtlabel（BO，rebase 于 31bc1e0，待 Cross-Review——blockaction.rs 属机制 C 白名单）；表级剩余（matchModel 恢复侧已有 recover_labels 真身） | lane BO（wt/sb-jtlabel，commits 724c7c1/8c8cd1e/dc16179/d8fbe54/b5f6f07/e8f0497）+ 待指派 reviewer | write-set=`src/{jumptable,block,blockaction,funcdata,flow}.rs`、`tests/oracle/printc_switch_emit_1204.{rs,metadata.json}`（重钉）、`docs/api/{jumptable,block,blockaction,funcdata,flow}.md`；fixture 三件套（blockstruct_switch_label_1204）在 /dev/shm/rugra-tests/sb-jtlabel/fixture/ 待 root 集成入库 | 落地六件：①switch_over（jumptable.cc:2528，block2addr 构建，此前全缺）+block2_position/num_indices_by_block/get_index_by_block（cc:2337/2438/2485）②BlockSwitch 持 JumpTable（ctor cc:3488）+case_order（caseblocks 平行数组，CaseOrder=block.hh:755-767）③finalize_case_labels（cc:3556-3592：depth 两 pass+链根 label+CaseOrder::compare 稳定排序+case_values 物化=block.hh:780/787 活查询值）④BlockGraph::finalize_printing（cc:1364）+dispatcher（Switch 递归 control+非 goto case=cc:1913 消费集；其余复合块走 component_list_dyn）⑤ActionFinalStructure 补 finalize_printing 调用点（cc:2192 位；cc:2191 orderBlocks 仍缺=登记）⑥常规 default 边路由 default_case 槽（addCase cc:3515 isdefault 消费，switchOver maxcount→installSwitchDefaults 链此前无消费者）；gp 证据：RUGRA_BS_DUMP 见证 48 臂 label 集合+逐臂顺序均与 golden ghidra_curl_1204.c 的 switch 逐项相等（88→48 修正 default 40 假臂）；B2 双侧 fixture 16 行字节 MATCH（slot_desc/multi_shared[多条目 addressIndex 序]/fallthru_chain[chain/depth/label 下传]/default_edge）；三门禁：curl 全量 124/124 defects=0 numbering=0（与档存基线 3304 skeleton 等值 3305±，glob_set panic 修复后 0 PANICK）；httpd 29/29 defects=0 numbering=0；cargo test --lib 单线程 18 failed=已知 FUNCDATA-TESTS-FLAKY-0001 集，block/jumptable 81/81；残项：BL（InfLoop 递归发射）合入前 gp 文本级 switch( 不出现（结构层已对齐）；非 default 零条目 case 的 printc 跳体（'Should never happen' 域）；default 打印位置（Rugra 末尾 vs oracle 排序位）归 PRINTC-SWITCH-EMIT-0001；evidence=wt/sb-jtlabel e8f0497，2026-09-22 | PATHMELD-0001 |
| `JUMPTABLE-PIPELINE-0001` | P0 | stageJumpTable partial 克隆简化链 + ActionSwitchNorm 闭包 + 去自创预扫 | `src/{funcdata,fspec,coreaction,flow}.rs`、API 文档 | Layer1-3 全部 |
| `JUMPTABLE-HYGIENE-0001` | P1 | 卫生项（is_load_in_path 漂移/buildLabels/clone/foldInNormalization/buildAddresses/Basic2/setup_trivial）；**minimalmask 消费者轴已函数级双侧锁定（2026-09-22,wt/sb-mmfixture）**：`tests/oracle/minimalmask_ladder_1204.{cc,rs,metadata.json}`+`tools/run_minimalmask_ladder_oracle.sh`（MINIMALMASK-LADDER-CONSUMERS-0001,CR-CN 条件②）——runner 实跑 `minimalmask_ladder_1204: MATCH`（55 行字节恒等）：阶梯 12 边界（严格 `>`：0xff→0xff/0xffff→0xffff/0xffffffff→0xffffffff/0x100000000→~0）+ markConsumedParameters（coreaction.cc:3840:名义/bytes 门/autolive/inputlock 四形态）+ gatherConsumedReturn（:3871:OR 累积/dead/单输入/outputlock/bytes 门）+ foldInNormalization（jumptable.cc:2574:1 字节 switchvar ≥full-mask 恒真 gate=1/SEXT 截断/COPY 不截断/8 字节不覆盖/2 字节恰等边界，含 JumpBasic in(0) 改写断言）；其余卫生项仍开放 | `src/jumptable.rs`、API 文档 | 局部 |

来自 `docs/alignment_audit/COREACTION_GAPS_2026-08-22.md`（62 Action 对照：18 PARTIAL + 12 STUB
+ 2 未注册 DynamicMapping/ForceGoto + 7 注册/顺序偏差；关键发现=ActionFuncLink 硬编码 libc ABI 表
替代数据库 FuncProto 查询、8 处 Merge::new() 非持久、Heritage::guard_returns 缺失、
analyze_extra_pop 死代码）。write-set/验收详见报告 §5，按 wave 认领：

- **CA-1（R1 小地基，解锁 6 Action）**: COREACTION-PORT-LIKELYTRASH-0001(P0)、
  COREACTION-PORT-EXTRAPOP-0001(P0)、COREACTION-PORT-STACKPTRFLOW-0001(P0)、
  COREACTION-PORT-INTERNALSTORAGE-0001(P0)、COREACTION-PORT-PARAMDOUBLE-0001(P1)、
  HERITAGE-GUARDRETURNS-0001(P0)
- **CA-2（protorecovery 收口+删硬编码表）**: COREACTION-PORT-FUNCLINK-0001(P0)、
  COREACTION-PORT-PROTOTYPES-TYPES-0001(P1)、COREACTION-PORT-ACTIVERETURN-0001(P1)、
  COREACTION-PORT-INPUTPROTOTYPE-0001(P1)、COREACTION-PORT-OUTPUTPROTOTYPE-0001(P1)、
  COREACTION-PORT-UNJUSTIFIEDPARAMS-0001(P1)、COREACTION-PORT-DEINDIRECT-0001(P1)、
  COREACTION-PORT-RESTRICTLOCAL-0001(P2)、COREACTION-DEINFUNCPARAMS-0001(P2)
- **CA-3（HighVariable/符号）**: COREACTION-STATEFUL-MERGE-0001(P0)、
  COREACTION-PORT-DOMINANTCOPY-0001(P1)、COREACTION-PORT-GROUPPARTIALS-0001(P1)、
  COREACTION-PORT-MARKEXPLICIT-0001(P1)、COREACTION-PORT-MARKIMPLIED-0001(P1)、
  COREACTION-PORT-MAPGLOBALS-0001(P1)、COREACTION-PORT-DYNAMICSYMBOLS-0001(P1)、
  COREACTION-PORT-DYNAMICMAPPING-0001(P0，含 :5504 注册)、
  COREACTION-PORT-RESTRUCTUREVARNODE-0001(P0)
- **CA-4（专项/接线）**: COREACTION-PORT-FORCEGOTO-0001(P1)、
  COREACTION-PORT-SEGMENTIZE-0001(P2)、COREACTION-PORT-STARTCLEANUP-0001(P2)、
  COREACTION-PORT-PROTOTYPEWARNINGS-0001(P2)
- 复用既有 TODO 12 个（见报告 §5 末尾），不重开。

### W-2026-08-22-ORCH 交付流水（root 维护；集成需复核 APPROVE + 白名单差分门禁）

| ID | 状态 | 交付→集成 | 备注 |
|---|---|---|---|
| BLOCK-FINDIRREDUCIBLE-0001 | DONE | a494832→636daa1 | 机制C APPROVE，fixture 90 |
| LANEDIVIDE-INFRA-0001 | DONE | 3fb7be7→a8bd8d0 | 机制C APPROVE，fixture 91 |
| DATATYPE-TYPEORDER-RESIDUAL-0001 | DONE | c14dfa9→1180a72 | 机制C APPROVE（证据型） |
| COMMENT-SORTER-PRINTC-0001 | DONE | 57f39db→c5e685c | 纯重构：E2E 输出与基线逐字节一致（sha 9e6dfd51…）；None 分支补 setupOpList(NULL) 语义；fixture 重钉后 exit0；残差仅剩 block-cover/db-comment-clone 两表示层 |
| COMMENT-SORTER-ITERATORS-0001 | DONE | dc6eb2c→d7a9cae | root 验证，fixture 92 |
| TRANSFORM-MULTIEQUAL-INSERT-RESIDUAL-0001 | DONE | 7163dec→a5ceae7 |
| PIPE-STAGE-BISECT-0001 | DONE | 9513f07→d915255 | 纯工具（stage_bisect.py+selftest 13/13+atf 骨架），root selftest 复验通过 |
| MERGE-CLEAR-LIFECYCLE-0001 | DONE | c3b9843+c330dbc→3c2db52+2234c5f | 机制C APPROVE，fixture 95 |
| PIPE-DERIVED-TREE-0001 + PIPE-STACKSTALL-COUNT-0001 | DONE | b40a315+6fe013a+c265a2a→eb8ad57+c66f442+a5a1973 | 机制C APPROVE（78 节点独立推导复核、NormalizeBranches 论断=真、双 runner 复跑 MATCH、action:: 233/233）；全量 lib 1495/2 已知；E2E v5=defects 1/numbering 0（唯一 defect 绑定 PIPE-ORDER-EMPTYELSE-0001，skeleton 10757 归因管线序涟漪）；fixture 96/97；残差 4+4 项登记（PIPE-RULE-CLONE-FILTER/PIPE-POOL-LOCAL-RULES/PIPE-DEINDIRECT-CHANGE/PIPE-STACKSOLVER-WRITEBACK + checkClog/repair/ShadowVar 链式/analyzeExtraPop 守卫形态项） | 机制C APPROVE，fixture 4→11 记录升 MATCH |
| PRINTC-UNLINKED-REF-0001（printc 域） | DONE | b155b8a→fcc20d2 | 机制 B 门禁通过：E2E defects=0/numbering=0（skeleton 8679，注入组 117→45 行，glob_url 恢复丢失语句与 golden 同形）；68反编译/8超时=共享负载（前后自测一致），wave 收尾低负载复验；残差两件归 varmap 域（explicit 符号化 + 命名期类型前缀）；B2 fixture 待建 |
| TYPEFACTORY-LOCALTYPE-CACHE-0001 | DONE | f0a7829+9ee59ab→bda7dab | 机制C APPROVE（8 通道穷尽枚举、oracle 手重建验证前缀 sha、排序键逐值核对 24 submeta 表）；type_system:: 106/106；复核域外 3 项登记 TYPEFACTORY-DECODE-SIDENOTES-0001 |
| RULE-PORT-COLLAPSECONSTANTS-0001 | DONE | e9a850d+6399c08→b94e7f4+01aafa2 | 机制C APPROVE（72-live-opcode 独立验证、EquateSymbol 真符号公证、复核者重读全部引用行）；ruleaction:: 180/180；fixture 93；组合树 E2E 门禁通过（defects=0/numbering=0，skeleton 8674）；残差=VARNODE-COPYSYMBOL-EQUATE/FLOAT-OPINT2FLOAT-SIGN/OPBEHAVIOR-SRIGHT-SIZEMASK |
| JUMPTABLE-GUARDS-0001 | DONE | 07be5cb+2f84e7e→696916d |
| FSPEC-SPACEFILTER-0002 + FSPEC-TRIALCMP-0003 | DONE | c1cebf3→集成 |
| JUMPTABLE-CALCRANGE-0001 | DONE | dbcc9cb→集成 |
| TYPEFACTORY-CODEFLAGS-DECODE-0001 | DONE | 1ffa567→集成 |
| FLOAT-OPINT2FLOAT-SIGN-0001 | DONE | 2d85fa8+1eaf0b1→集成 | 36/36 MATCH 残差清零；关键发现=oracle 分数码 64 位顶对齐约定；fixture 103；结构级残差 FLOAT-FMT-STRUCT-0001 登记 |
| TYPEFACTORY-CODEFLAGS-DECODE-0001 | DONE | 1ffa567→集成 | 89/89 MATCH；关键发现=oracle 自身死路径实证（嵌套 pointer→code 恒 throw，fixture 字节级证 flag 无关）；fixture 102；live prototype 解码登记 FUNCPROTO-DECODE |
| JUMPTABLE-CALCRANGE-0001 | DONE | dbcc9cb→集成 | 机制C APPROVE（18 ctor 全量比对、交集写回/布尔/截断逐语句、markModel skip 链路独立核实）；fixture 101；复核域外 4 处注释行号漂移当场修正 |
| LANEDIVIDE-INFRA-RESIDUAL-0001（六分支） | DONE | f38e514→集成+重钉 | 5/6 支 MATCH；indirect 支 MISMATCH 精确定位新残差 TRANSFORM-CONSTANT-IOP-SPACE-0001；oracle UB corner 保持 NO_ORACLE；runner 复验 exit0 |
| PRINTC-UNLINKED-REF-0001（varmap 域） | DONE | c600945→集成 | 6/6 MATCH 零源码改动=证据型；租约前提被推翻（链路已存在且忠实）；45 行注入组真实根因=MERGE-MIXEDHIGH-0001；fixture 104/105 |
| PIPE-POOL-LOCAL-RULES-0001 | DONE | afe2a08→79a8faa |
| ACTION-LANEDIVIDE-0001 | DONE | 98c3a9b+a9eec91→集成 | 机制C APPROVE（快照等价双侧独立验证、生产惰性论断成立、count 通道/ONCEPERFUNC 逐字）；fixture 106；LaneDivide INFRA→ACTION 链完整；E2E 零影响（lane_records 仅测试可达） |
| BLOCK-CALCLOOP-0001 | DONE |
| CONDEXE-PULLBACK-0005 | DONE |
| TYPEFACTORY-LEGACY-CALLER-MIGRATION-0001 | DONE |
| TRANSFORM-CONSTANT-IOP-SPACE-0001 | DONE |
| FLOW-CONTAINEDCALL-0001 | DONE |
| DATABASE-EQUATE-VALUE-REGISTRY-0001 | DONE |
| VARNODE-COPYSYMBOL-HIGHBRANCH-0001 | DONE |
| FUNCDATA-CALCNZM-0001 | DONE |
| CONDEXE-ERROR-0006 | DONE |
| FLOAT-FMT-STRUCT-0001 | DONE |
| PIPE-ORDER-EMPTYELSE-0001 | DONE |
| FLOAT-OPTRUNC-OOB-0001 | DONE |
| CONDEXE-UNREACHGUARD-0001 | DONE |
| RULE-BEHAVIORAL-FIVE-0001 | DONE（r2） |
| FLOW-INJECT-WIRING-0001 | DONE |
| PANIC-SWAPEDGE-OOB-0001 + CONDEXE-CFG-0001 | DONE |
| DB-LOCALSCOPE-MAP-0001 | DONE | 813a30e+fa408da→集成（机制C APPROVE：PartMap 分区语义/四分支交互/victim 等价独立验证） | 13/13 MATCH；scopelocal fixture 六层重钉后 11/11 复验；fixture 119；复核 3 观察项登记 | 6e4e8f8→集成（机制C APPROVE：swap 序列逐字+parentless⟹dead 不变量链 grep 亲证） | 4→0 panic、71→75 反编译、skeleton 2762→2621；复核域外 3 处过时注释登记清理 | 835456b+afd61ed→集成 | 3/3 MATCH（cpuid 真生产路径+xref 臂 MATCH）；flow 审计第 5 子系统接线完成；fixture 118；containedcall fixture 标 STALE（API 适配 TODO） | 分支快照→集成（Cross-Review APPROVE r2：四修正方向全闭合、反例双侧 apply=1 目击） | 40/40 MATCH；五条规则 oracle 语义；fixture 117 | a55147f→集成 | root 亲验守卫位置（condexe.cc:485-486 逐字）；4/4 MATCH+负对照；fixture 115 |
| VARIABLE-GETTYPE-LAZY-UPDATETYPE-0001 | DONE（收窄） | 56c00e2+4c6b4a3→集成 | 4/4 MATCH；TypeCell 方案（autoref 不可行性经 rustc 样例证实）；raw-bit 残差+两个 varnode 域腿登记；fixture 116 | efb68d4→集成 | 153/153 MATCH；cvttsd2si 实效语义（INT64_MIN）+ sizeout mask；registry 描述更新 | 2b6ec5b→集成 | 根因=已销毁 op 滞留结构快照（build_copy 克隆 vs oracle BlockCopy 包装）；defects 1→**0**，28 函数只降不升；连带登记 BUILDCOPY-SNAPSHOT-DEADOP-0001 与 DoNothing/RedundBranch count 恒 0 |
| FUNCDATA-CALCNZM-0002 | DONE | 65a2492+06cb4a0→e684a9b | switch 合并回 op.rs（-270 行暂存副本），6/6 复验；发现并修复 master lib-test 编译断裂（925e668） | d3a7353+80fab84→集成 | 134/134 MATCH 结构级 1:1（位阶梯+顶对齐+RNE+次正规中间态）；顺带修两值缺陷（denorm 偏大 2^frac、NaN 透传）；int2float runner 重钉后 36/36 复验；fixture 114 | ab15811→集成 | 机制C APPROVE（消息逐字/前进不变量/传播协议/UB 区分；E1 触发独立推演）；死循环消灭（负向对照）；fixture 113；pullback pin 刷新后 5/5 复验；复核域外观察挂 TRUEOUT-RESIDUAL（hasUnreachableBlocks 前置未接线） | d752ce0+f763ff0→集成 | 6/6 MATCH（两阶段 DFS+worklist、INT_DIV 族、MULTIEQUAL 裁剪）；sc6 根因修复；主管线接线核实（NonzeroMask 槽位已在）；fixture 112；residual CALCNZM-0002/0003 登记（0003=rulebehav ②） |
| RULE-BEHAVIORAL-FIVE-0001 | REWORK-r2 复核中 | fe2f5e6+875e1d5（②③④ 完成：字段读+fixture 传播用例含 r1 反例本体 40/40 MATCH+Evidence 更正） | r2 独立复核进行中；fe2f5e6 携带的 database.rs 编译修复与 root 925e668 同因（cherry-pick 时择一） | a214945+4dc8cf8（wt/rulebehav，37/37 fixture MATCH 但公证前提被证伪） | 复核 REJECT 主因=RuleShiftBitops 的 nz_mask_exact 声称『nzm 仅构造函数写入』被证伪：calcNZMask（funcdata_varnode.cc:856-927）另有 5 写点，且主管线 NonzeroMask（cc:5507）先于规则池运行→规则读传播值；最小反例=ADD(X,W&0x80)<<7。四条通过（ZextEliminate/SignForm/SignNearMult/Shift2Mult）。修正方向：①calcNZMask 忠实移植（=calcnzm writer 在制任务，依赖汇合）②ShiftBitops 改读 nzm 字段删 nz_mask_exact ③fixture 加 written-稀疏-nzm 正例+metadata 语义描述更正 | ff63a18→集成 | 5/5 MATCH（typeDirty/setSymbol 簿记完整）；连带残差 RULEACTION-ADDUNSIGNED-COPYSYMBOL-HIGH-0001 登记（rulebehav 租约释放后一行适配）；fixture 111 | 922209b+a8735a2+f905730→集成 | 4/4 MATCH；运行时+XML 解码双腿接通；buildDynamicSymbol 腿域外登记；fixture 110 | 00833ff+150b332+4d9b05f+5804a03→集成 | 11/11 MATCH 残差清零；fixture 抓到首版 erase 后继双自增缺陷并修正；发现 .sla 零位移 call→goto 规则；fixture 109 | 149527d→集成 | lanedivide fixture 11/12 overall MATCH；iop 物化路由 1:1（transform.cc:211-215）；fixture 去规范化恢复检测力；唯一残差=oracle 自身 UB NO_ORACLE | 0f7b96a→f43de75 | cpool/merge/grammar/internal 全迁移；两 runner 重 pin 后 PASS（220/220 + 16/16，输出中性证明）；merge 域机制C APPROVE（归因链/nochar 门/panic 文本/中性证明全闭合）；引用勘误 3131→3128 已修 | 3e096ae→集成 | 机制C APPROVE（5 腿同序、null 槽可表示性分析、负向对照自洽）；fixture 108；新_varnode_out 硬编码 Register 的系统性缺口登记（writer 已私有复刻） | 76bdf1b+725ad27+0f0862d→集成 | 机制C APPROVE（18/18 语句对齐、add_loop_edge 镜像论证证实、build_copy 标签丢弃论证成立、next_url 归因闭合）；fixture 107；E2E v6 门禁：defects=1（仅已知 PIPE-ORDER-EMPTYELSE）/numbering=0，skeleton 10757→9956 如预言收敛 | 机制C APPROVE；池投影 308 规则双根 MATCH；E2E 字节级零影响；复核附带 PIPE-MAINLOOP-REPEATAPPLY 等 TODO 补登 |
| VARNODE-COPYSYMBOL-EQUATE-0001 | DONE | f7f8c39+5578ae0→集成 | 3/3 MATCH；root 修连带 ruleaction 测试（equate 值须与 collapse 输出 value-close，非原始输入）；fixture 100；残差 HIGHBRANCH/DATABASE-REGISTRY 两项登记 | 6/6 MATCH；审计漏报发现（justifiedContain 端序）并入 ADDRESS-0001 族；fixture 99；残差 FSPEC-PHASE0-RESIDUAL-0001（trial slot 基数跨域/比较器等值序） |
| CONDEXE-TRUEOUT-0002 | DONE | e804193+63ab17e+9d632f5→bed6ef5+6c2d6b9+93f78ba | 极性修复 4/4 MATCH；swap_edges 反向索引新缺陷修复；root 重断言陈旧极性测试（7a49877，全量 1495/2 基线）；fixture 98；跨域 2 项登记（CONDEXE-CONSTBASE-FLIP-0001/废弃形参清理） | 机制C APPROVE（六改动逐项 PASS、3/6 MISMATCH 归因独立证实、readOp 遮蔽语义独立验证、复跑 exit0 27/27）；fixture 94；复核域外发现登记 JUMPTABLE-MARKMODEL-0001 |
| SCOPELOCAL-QUERY-0001 r2 | DONE | 3bea8db+31f4046→c258bb3 | 机制C APPROVE（包夹怪癖双向独立推演+libstdc++ 源码级验证）；root 微修 varmap.rs:3053 相邻合并死分支（297ba6c）+fixture 重钉链（5df1a34+），runner 复跑 exit0 11/11；E2E v4 门禁通过（defects=0/numbering=0，skeleton 8674=查询层重构无可观察输出影响） |

来自 `docs/alignment_audit/CONDEXE_GAPS_2026-08-22.md`（27 函数对照：20 结构 MATCH 全 UNTESTED、
1 MISSING（buildHeritageArray）、6 MISMATCH；**勘误 roadmap #19**：PathMeld 归因错误（condexe.cc 零引用）、
forceSpecific/removeBlockEdges/setOut 在锁定 oracle 不存在、"238 行只检测"已过时（现 1555 行）；
两个 Phase-0 共享缺陷=remove_from_flow_split 调用序列反+越界 panic、get_true/false_out 极性反+重复翻转）：

| ID | P | 标题 | write-set | 依赖 |
|---|---|---|---|---|
| `CONDEXE-CFG-0001` | P0 | DONE(wt/panicfix)：removeFromFlowSplit 忠实化（swap 序列+越界 panic×3）+ op_destroy 无 parent 路径 mark_dead（glob_word RuleSubvarSubpiece panic） | `src/block.rs`(新增)、`src/funcdata.rs:2728-2787`、docs/api/{block,funcdata}.md | 无 |
| `CONDEXE-TRUEOUT-0002` | P0 | getTrue/FalseOut 纯位置化+消费方迁移 | `src/block.rs:453-485`、`src/condexe.rs`、全部消费方、docs/api/block.md | 无（与 0001 并行需分文件租约） |
| `CONDEXE-ACTION-0003` | P1 | apply guard/count/活列表遍历/管线移位 | `src/condexe.rs:1346-1389`、`src/action.rs:1053-1076` | 0001,0002 |
| `CONDEXE-HERITAGE-0004` | P1 | buildHeritageArray + per-space numHeritagePasses 接线 | `src/funcdata.rs:6958`、`src/condexe.rs:104-127/357-396`、`src/heritage.rs` | 无 |
| `CONDEXE-PULLBACK-0005` | P1 | pullbackOp storage+插入位置 | `src/condexe.rs:450-500` | 0002 |
| `CONDEXE-ERROR-0006` | P1 | resolve 链 Result 化+死循环消灭 | `src/condexe.rs:546-746` | 0001 |
| `CONDEXE-FIXTURE-0007` | P2 | 27 函数 locked-oracle fixture 补全 | tests/ + tools/ | 0001-0006 |

| `BLOCKBASIC-COVER-0001` | P2 | 部分闭合（2026-09-22 SB-HERITAGE50-BLOCKCOVER-0001：多范围 cover+copyRange/mergeRange/getEntryAddr/getStart/getStop 已接线；残余=contains(cover.inRange) 的 comment 域消费+RangeList marshal+空 cover invalid-Address() 精确化） | comment 域后续认领 | `src/block.rs`、fixture | 排队 |
| `COMMENT-DB-EMITTED-0001` | P2 | comment 审计新增：setEmitted(false) 不回写 CommentDatabaseInternal（Ghidra mutable 直改库内对象） | `src/comment.rs`、`src/database_*.rs` | 排队 |
| `COMMENT-SORTER-PRINTC-0001` | P2 | comment 审计新增：printc 消费端迁移到直接协议（消除 Vec 快照与 printc.rs:6651 警告） | `src/printc.rs`、`src/comment.rs` | printc 租约释放后认领 |

来自 `docs/alignment_audit/FLOW_GAPS_2026-08-23.md`（40+ 函数对照；**勘误 ROADMAP 〇节**：可达性流追踪
已移植并接线（flow.rs 2756 行），『线性扫描替代 L1』声明过时；truncateFlow/JunctionSplit 在锁定 oracle
不存在，真实机制=Funcdata::truncatedFlow）。10 项 TODO 详见报告 §Wave（FLOW-ENTRY/ERRSEM/TRUNCATED/
STAGEJT/SWITCHOVER/CONTAINEDCALL/OVERRIDE/INLINE/INJECT-WIRING/ROADMAP-STALE），最重链=
truncatedFlow partial 克隆 → stageJumpTable 简化链（=JUMPTABLE-PIPELINE-0001 根因）→ 内联/注入共同地基；
httpd 门禁仍走线性扫描（examples/httpd_decompile.rs:261）。

| `VARNODE-COPYSYMBOL-EQUATE-0001` | P2 | rulefound 交付发现：copy_symbol_if_valid 不区分 EquateSymbol/isValueClose（varnode.rs 保守近似） | `src/varnode.rs`、fixture | varmap/varnode 租约释放后 |
| `FLOAT-OPINT2FLOAT-SIGN-0001` | P2 | rulefound 交付发现：float_emulate op_int2float 无符号 vs Ghidra sign_extend（float.cc:614） | `src/float_emulate.rs`、fixture | 无 |
| `OPBEHAVIOR-SRIGHT-SIZEMASK-0001` | P3 | rulefound 交付发现：混合尺寸 SRIGHT mask 语义（SLEIGH 不产，不可观测） | `src/opbehavior.rs` | 排队 |
| `PIPE-RULE-CLONE-FILTER-0001` | P2 | 非 decompile 根（jumptable/normalize/paramid/register/firstpass）的 rule 级 clone 过滤无 oracle 观察 | `src/action.rs`、fixture | 排队 |
| `PIPE-POOL-LOCAL-RULES-0001` | P1 | oppool1 的 RuleSextEliminate/RuleEquality 与 cleanup 重复 RuleTrivialArith 为 Rugra-local（铁律 1.4 存量违例）；移除的 E2E 影响需门禁数据 | `src/action.rs`、`src/ruleaction.rs` | 排队 |
| `PIPE-DEINDIRECT-CHANGE-0001` | P2 | deindirect 的 CALLIND+persist/externalref 分支未触发（仅零通道观察） | `src/coreaction.rs`、fixture | 排队 |
| `PIPE-STACKSOLVER-WRITEBACK-0001` | P2 | analyzeExtraPop 未知 extrapop 的 StackSolver 回写未接线（已数解未写回） | `src/coreaction.rs`、`src/fspec.rs` | fspec 租约释放 |
| `PIPE-ORDER-EMPTYELSE-0001` | P1 | 管线序集成（eb8ad57/c66f442）后 E2E v5 唯一 defect：getparameter.constprop.0 输出 `if(c){((bool)(x==0));}else{}` ——真臂为无副作用表达式语句+空 else 未坍缩（疑 DeterminedBranch/RedundBranch 与新 DeadCode 位置交互）；skeleton 8674→10757 归因为管线序涟漪（normalizebranches 移除/deadcode 移位/unreachable 提前），随后续 Action 对齐收敛 | `src/{coreaction,blockaction}.rs`、printc 侧 | E2E 复现为据，P1 优先 |
| `PIPE-STALL-SHAPE-0001` | P3 | MISMATCH/UNTESTED | unassigned | `src/coreaction.rs`, `src/heritage.rs`, `docs/api/{coreaction,heritage}.md`, paired StackSolver/error fixture | 预存形态偏差：checkClog 全量扫 vs loc-range 迭代（cc:440-448）、repair 全局正向扫+缺 stack-traced 弃修（cc:382-419）、ShadowVar 首匹配 break vs 链式（cc:927-942）、analyzeExtraPop 守卫读 fd.funcp（cc:264-267）；D0 callspec 审计又确认 StackSolver initial-input 失败在 Ghidra 为 `LowlevelError`→caller `warningHeader`，Rust 为 stderr+return，须对拍异常前状态与告警通道，不得归入 callspec identity MATCH；2026-08-24 |
| `MERGE-CLEAR-LIFECYCLE-RESIDUAL-0001` | P1 | localmap_typelock_survival（需 varmap.rs 忠实 clearUnlocked=database.cc:2042-2069，typelocked/equate 存活）；funcproto_unlocked_output（需 fspec returnBytesConsumed=0）；reset_local_window 重派生/clean_up 索引/merge 生产填充/localoverride 沉积 4 项 UNTESTED | `src/{varmap,fspec,funcdata,merge}.rs` | 分域认领 |
| `SCOPELOCAL-QUERY-RESIDUAL-0002` | P1 | 生产消费者接线：funcdata.rs:153 第二份手写 scope_local_find_overlap（等值 tie 答先插入者，oracle 答后插入者）须改委托 varmap 条目 API；mapGlobals/linkSymbol 线程 parent+space；wholeCount/multiEntrySet 无查询可观察未建模 | `src/funcdata.rs`、`src/varmap.rs` | funcdata 租约释放后 |
| `TYPEFACTORY-DECODE-SIDENOTES-0001` | P3 | typefactory 复核域外发现：decodeEnum 重复值 warning 丢弃（oracle 经 ghidra_process 可见）；<void> 元素臂沿用 panic 版 get_type_void（clear 后遇 <void> 会 panic）；decode_basic size<0 静默归 0 vs oracle throw | `src/type_system/typefactory.rs`、`src/type_system/datatype.rs` | 排队 |
| `JUMPTABLE-MARKMODEL-0001` | P2 | DONE（dbcc9cb 修复）｜原 jtguards 复核域外发现：mark_model 用 get_read_op() 判 skip，oracle markModel（cc:1258-1264）用 getBranch() 判 null——已 clear 的 guard 在 Rugra 仍被 mark；另 value_match/check_unrolled_guard 内联注释行号漂移 | `src/jumptable.rs`、docs | 无 |
| `RANGE-PULLBACK-SLESS-0001` | P1 | jtguards 交付发现：CircleRange::pull_back_binary 缺 CPUI_INT_SLESS/INT_SLESSEQUAL（rangeutil.cc:882-917），signed 比较守卫拉回失败→守卫记录缺失（jt_guards sc1/sc3 MISMATCH 的根因）；RANGE-0001 家族 | `src/rangeutil.rs`、docs/api/rangeutil.md、fixture | 无 |
| `FUNCDATA-CALCNZM-0001` | P1 | jtguards 交付发现：calc_nz_mask 简化单遍，缺 unwritten 输入 nzm 初始化（funcdata_varnode.cc:889-893）与 DFS/worklist；主管线未调用（sc6 根因） | `src/funcdata.rs`、`src/heritage.rs`、docs | mergeclear 租约释放后认领 |
| `DATATYPE-SPACEBASE-SPACEID-0001` | P2 | datatype 审计新增：Ghidra AddrSpace 裸指针身份 vs Rust AddressSpace enum 同 index 折叠；TypePointer::compare 同 index 空间恒返 1 的 Ghidra quirk（type.cc:944）；长期解=ADDRESS-0001 空间注册表迁移 | `src/type_system/datatype.rs`、`src/space.rs` | ADDRESS-0001 方案定稿后认领 |

来自 `docs/alignment_audit/FSPEC_GAPS_2026-08-23.md`（26 类对照：10 类完全缺失
（ParamListRegister/Merged、ProtoModelMerged、ScoreProtoModel、UnknownProtoModel、ProtoParameter、
ParameterBasic/Symbol、ProtoStoreSymbol、ParamEntryRange）；~88 MISSING/11 MISMATCH/6 STUB；
**最重发现=双模型栈分裂**：FuncCallSpecs.proto_model 绑硬编码 SysV stub（protomodel.rs 315 行），
忠实的 ParamListStandard::fillin_map（fspec.rs:4932 对齐 cc:1285）在主管线是死代码；
另 findEntry/unjustifiedContainer/assumedExtension 三处 Ram-only 过滤、ParamTrial 排序键错、
split_lo 地址 `+sz` 应为 `+(size-sz)` 且丢 flags、likelytrash 折叠污染 has_effect）。15 项 TODO 详见报告 §6：

| ID | P | 标题 | write-set | 依赖 |
|---|---|---|---|---|
| `FSPEC-MODEL-UNIFY-0001` | P0 | callspec 改绑 Arc<ProtoModelFull>，忠实 fillinMap 接入主管线 | `src/fspec.rs`、`src/coreaction.rs:6030/6451`、docs | coreaction 租约释放 |
| `FSPEC-SPACEFILTER-0002` | P0 | 删 findEntry/unjustifiedContainer/assumedExtension 三处 Ram-only 过滤 | `src/fspec.rs`、docs | 无 |
| `FSPEC-TRIALCMP-0003` | P0 | ParamTrial 排序键 + split_lo 地址/flags 修正 | `src/fspec.rs`、docs | 无 |
| `FSPEC-PHASE1-*`（trashset/effective_extrapop/internal-storage 等 12 项） | P0/P1 | 见报告 §6（解锁 CA-1 的 LikelyTrash/ExtraPopSetup/InternalStorage/StackPtrFlow） | 以 `src/fspec.rs` 为主 | Phase 0 |

| `BLOCK-RWLOCK-RECURSIVE-READ-0001` | P3 | blockirred 复核 NOTE：区间测试快照同时持 x 与 yprime 读锁，yprime==x 时对同一 RwLock 递归 read()（std 标注 might panic，当前实现安全）；建议 ptr_eq 预判分支消除标准库实现依赖 | `src/block.rs` | 排队 |
| `BLOCK-CALCLOOP-0001` | P2 | BLOCK-FINDIRREDUCIBLE 残差：calcLoop（block.cc:2104-2147）空 stub，Rugra 已在 cc:2213 对应点接线，落地后不可归约端到端闭环；另 structure_loops 生产接线（blockaction.rs structureReset 路径）+ 管线差分 | `src/block.rs`、`src/blockaction.rs` | a494832 集成后认领 |
| `LANEDIVIDE-INFRA-RESIDUAL-0001` | P2 | lanedivide fixture 8 分支残差（terminator/store/load/shift/zext/indirect/restricted-window/typelock 中 6 UNTESTED + subflow.cc:3942 负索引 UB corner NO_ORACLE） | `src/{arch,funcdata,subflow}.rs`、fixture/runner 扩展 | LANEDIVIDE-INFRA-0001 集成后认领 |

来自 `docs/alignment_audit/RULE_GAPS_2026-08-22.md`（155 条 Rule 全量对照：struct 级 MISSING=0、
三池注册齐全且顺序一致；行为级 MISSING=5：ZextEliminate/SignForm/SignNearMult/ShiftBitops/Shift2Mult
——同名规则实现的是不同算法，Ghidra 会做的变换在 Rugra 永不发生且反之；PARTIAL=24 新发现；
注册纯净度偏差=3：Rugra 注册了 oracle 不存在的 RuleSextEliminate、从未实例化的 RuleEquality、
cleanup 池重复的 RuleTrivialArith）。26 项拟登记 TODO 详见报告 §5（`RULE-PORT-<NAME>-0001` 系列），
最重两项：
- `RULE-PORT-COLLAPSECONSTANTS-0001`（P0）：全 opcode 常量折叠（基类 getOpList→action.cc:705-714、
  isCollapsible+PcodeOp::collapse→op.cc:115/450、TypeOp evaluateUnary/Binary 全表、
  collapseConstantSymbol、opMarkNocollapse 错误路径）；write-set=`src/ruleaction.rs`+`src/op.rs`+docs
- `RULE-PORT-EARLYREMOVAL-0001`（P0）：14 条 covered projection 已 `MATCH`；raw 0/45、nullable
  input、完整 Heritage manager/op-bank 仍由顶部活动行跟踪为 overall `MISMATCH`。

### 函数体差距攻坚 wave（W-2026-08-23-GAP，至周二 16:00；基线=wt/getstr 头 d73ee60，52/68 body 一致）

**进度（root 维护）**：
- GETSTR-ZERODIFF-0001 六提交集成完毕（机制C APPROVE 附 2 必改已落：行号勘误 b6c1430）；master 基线升级为 GetStr 树。
- SUBFLOW-SPLITFLOW-SIGABRT-0001 集成完毕（f216b8e，双层 UB：空指针构造+共享 iop 占位符，oracle NULL 语义逐字对齐）。
- FLOW-FIXTURE-API-ADAPT-0001 集成完毕（86a6bb9，根因更正为 runner pin 配对失谐，双 overlay 防复发）。
- 差距组六 Agent 转入分析优先模式（用户三步走指令），等分析报告回收。
- **globstruct 分析回收（5 根因）**：RC-A shared-return/FlowOverride 整块缺失（决定性证据=direct-runner 纯核心同样吸收 tail call，golden 干净形态由 Java SharedReturnAnalyzer 提供）｜RC-B follow_flow 下界=入口 vs oracle 0（flow.rs:3063，5 函数受害，但需与 A 同修否则吸 PLT）｜RC-C opPtrsub 无 union/findTruncation/field_0x 回退（printc.rs:1338）｜RC-D 符号声明链（已登记域）｜RC-E blockaction 循环体丢失（direct-runner 对照证独立存在，需 A/B 后定位）。派发：RC-A+B 一个修复 Agent（wt/sharedret）；C/E 待其他分析回收后统一排。
- **truncat 分析回收（6 根因）**：RC-A 与 globstruct 交叉验证一致（已给 sharedret 补 fail_thunk/PLT 规格）｜RC-B RETURN 槽位（42/42 裸 return）→ 派发 wt/retstr（含 RC-C 字符串/DAT_ 渲染）｜RC-D DWARF 聚合参数被拒 → 派发 wt/dwarfagg｜RC-E 规则非幂等 → 派发 wt/rulebisect（分析，覆盖 glob_set/range 去重）｜RC-F match_url 特有毁损待 RC-D 后重归因。
- **myfwrite 分析回收（3 层根因，主因推翻初始假设）**：主因=RuleSplitLoad/RuleSplitStore 缺 getValueDatatype 门禁（subflow.cc:2970-2983/2910-2938/2244-2274；一条 mov 拆 44 字段 load，门禁模拟实证 45→16 行+全量 2467→2433 零回归）→ 派发 wt/splitgate｜次因=merge_addr_tied 全空间分组（oracle merge.cc:609-648 双门：仅 IPTR_PROCESSOR/SPACEBASE+addrtied 标志）→ 派发 wt/addrtied｜MERGE-MIXEDHIGH 部分成立（次要，等前两修后重triage）｜次生登记：unique 偏移碰撞、reassemble_via_piece 自环。
- **myprog 分析回收**：myprogress/progressbarinit 差异主体是 RC-A 第三形态（吞 GetStr 函数体=扫过尾+jmp PLT 尾调用丢，已补 sharedret 规格）｜find_partial_field 原型修复已 stash（patch=/tmp/myprog_find_partial_field.patch，A/B 验证零回归），归 RC-C opPtrsub 派发时合并。
- **派发 wt/recovered**：恢复组重triage（原 panic/未产出 11 函数在新基线的形态）。
- **myprog 详细分析回收（P1-P7/M1-M8 根因表）**：P6/M1=RC-A（已入 sharedret）；P1 splitstore 越窗=splitgate 域；P2/M5=RuleStructOffset0 offset 硬编码+STOP_TYPE_PROPAGATION 未消费+type_infer 自创传播器（已补 rulebisect 输入）；M4=DWARF 局部变量导入缺失（已补 dwarfagg 规格）；M3/P7a/P5+find_partial_field 原型=myprog 租约内续修（已指令）；P4=动态符号映射缺口（待 merge/varmap 域认领）；M6=RPN 残留族（PRINTC-CAST-EXPR-0001）。

| 组 | owner | write-set | 目标函数（rugra vs golden 字符） |
|---|---|---|---|
| GetStr 六提交复核 | getstr_reviewer | 只读复核 | 五域零差交付的机制C |
| 产出不足 | truncat (`wt/truncat`) | funcdata/heritage/flow | hugehelp 166/6645、match_url 760/2544、SetHTTPrequest 78/212、glob_url 423/332 |
| 小幅差异 | smalldelta (`wt/smalldelta`) | printc/prettyprint/varmap命名 | _start 79/336、frame_dummy 28/62、main_init 40/49、__libc_csu_init 355/306、FUN_00102020 |
| 过度展开-结构 | globstruct (`wt/globstruct`) | blockaction/结构Rule | glob_word 6384/1489、next_url 7602/3137 |
| 展开+死代码 | globvars (`wt/globvars`) | ruleaction/varmap符号 | glob_set 2776/2326、glob_range 1861/2713；先做 ADDUNSIGNED 一行适配 |
| 命名域 | myprog (`wt/myprog`) | varmap命名/printc类型 | myprogress 1772/1716、progressbarinit 441/400 |
| 合并域 | myfwrite (`wt/myfwrite`) | merge/merge族Action | my_fwrite 803/368（疑 MERGE-MIXEDHIGH 受害者） |
| SIGABRT P0 | sigabrt (`wt/sigabrt`) | subflow/transform | test_split_flow_full_transform |
| fixture 适配 | ccadapt (`wt/ccadapt`) | containedcall fixture | FLOW-FIXTURE-API-ADAPT |
| SLESS 拉回（续） | rangepull (`wt/rangepull`) | rangeutil | RANGE-PULLBACK-SLESS |

## 历史 wave：`W-2026-08-20-CLOSEOUT`（收尾中，租约归 W-2026-08-22-ORCH 接管的以新 wave 为准）

> 2026-08-20 启动，root 编排、显式文件 staging 与串行集成；实现 write-set 互斥，核心算法由独立
> reviewer 只读复核。目标是先把遗留工作树拆成 MERGE / PIPE / VARMAP-query /
> VARMAP-window-error 四个原子闭包，再恢复新功能 wave。

### 本 wave 认领租约（root 串行登记；worker 禁改本文件）

| ID | P | 状态 | owner | write-set（互斥） | 依赖 / 验收 / 证据 / 更新 |
|---|---|---|---|---|---|
| `BLOCK-DOMROOT-0001`（在途收尾） | P1 | DONE | block_domroot_wave + 独立复核 agent | `src/{block,funcdata,heritage}.rs` + fixture 三件套 + runner | evidence=`9825db1`（root 集成）。权威 runner MATCH stdout_sha256=080948a5…（四 case：rootlist swap/入口回环/多根 VRoot 别名/单根）；机制 C 独立复核 APPROVE（四类语义零 MISMATCH + 独立复跑 MATCH）；registry 登记 block_domroot_1204（第 79 个）。残差：BLOCK-FINDIRREDUCIBLE-0001（findIrreducible/calcLoop 域）+ removeUnreachableBlocks 消费者未接线 |
| `PIPE-HEAD-FLAT-ACTIONS-0001` | P0 | REWORK | root（实现）+ pipeline_cross_review（REJECT r1） | `src/{action,coreaction}.rs`, `docs/api/{action,coreaction}.md`, pipeline-tree fixture/runner | r1 REJECT：raw universal 与 derived decompile 混合、错误保留 noproto FuncLinkOutOnly、DeadCode 实际顺序未移动、stackstall count 丢失、LaneDivide no-op。依赖 `PIPE-DERIVED-TREE-0001`、`PIPE-STACKSTALL-COUNT-0001`、`LANEDIVIDE-INFRA-0001`、`ACTION-LANEDIVIDE-0001`；验收=真实 default derive tree + leaf count/state + LaneDivide IR fixture + canonical curl + Cross-Review；evidence=pending；2026-08-20 |
| `PIPE-DERIVED-TREE-0001` | P0 | DONE | pipe_tree_agent + 机制C APPROVE + root 集成=`eb8ad57`（78/78；见交付流水） | `src/action.rs`, `docs/api/action.md`, `tests/oracle/pipeline_tree_1204.{cc,rs,metadata.json}`, `tools/run_pipeline_tree_oracle.sh` | WIP 已锁定=`dd76d37`（嵌套树镜像+树寻址原语）。复用 action_merge_order FixtureArchitecture；Ghidra 必须走 `universalAction→resetDefaults→getCurrent`，Rust 走实际 default action database；DFS 输出 path/ordinal/kind/name/basegroup/flags，保留重复且不 sort/dedup；修 noproto filtering、root key/name、exact action names、DeadCode :5682 顺序；evidence=pending；2026-08-22 |
| `PIPE-STACKSTALL-COUNT-0001` | P0 | DONE | pipe_tree_agent（任务 B）+ 机制C APPROVE + root 集成=`c66f442`（3+1 passes MATCH） | `src/{action,coreaction}.rs`, `docs/api/{action,coreaction}.md`, paired leaf-count fixture | 依赖 `PIPE-DERIVED-TREE-0001`；MultiCse/ShadowVar/Deindirect/StackPtrFlow 的 change/count/reset/analysis_finished 必须逐层反馈 stackstall fixed point；验收=两侧每 pass count/state/IR 完整观察；evidence=pending；2026-08-20 |
| `LANEDIVIDE-INFRA-0001` | P0 | DONE | lanedivide_finish + 独立复核（机制 C APPROVE：四类语义独立验证、type_metatype 降序枚举核对、UB 反例独立构造、fixture 五要素+fail-closed 公证通过、复跑 exit0 sha 一致）+ root 集成 | 交付=`3fb7be7`→集成=`a8bd8d0`（subflow:: 47/47，registry 第 91 个 fixture，evidence_status=UNTESTED 整体）。残差：`LANEDIVIDE-INFRA-RESIDUAL-0001`（6 UNTESTED+1 NO_ORACLE UB corner）；后续 `ACTION-LANEDIVIDE-0001` 依赖就绪但 coreaction.rs 租约未释放 | `src/{arch,funcdata,subflow}.rs`, `docs/api/{arch,funcdata,subflow}.md`, `tests/oracle/lanedivide_infra_1204.{cc,rs,metadata.json}`, `tools/run_lanedivide_infra_oracle.sh` | WIP 已锁定=`d5df444`（build/trace 层 + laned 查询/生命周期，subflow:: 47/47 绿）；剩余=fixture metadata/runner、oracle 对拍、独立复核；`TRANSFORM-MULTIEQUAL-INSERT-0001` 前置已完成（`b6af5be`）。完整补 Architecture::{getLanedRegister,getMinimumLanedRegisterSize}、Funcdata::checkForLanedRegister/minLanedSize/typed ordered lanedMap 生命周期，以及 subflow.cc:3518-4119 `LaneDivide` 全类 doTrace/apply 所需生产地基；不得在 Action 层模拟。最终验收=lookup/min/ordered map、失败零突变、成功 PIECE/SUBPIECE/MULTIEQUAL 完整 IR/identity/order/count 投影，targeted+caller closure，独立复核；evidence=pending；2026-08-22 |
| `TRANSFORM-MULTIEQUAL-INSERT-0001` | P0 | UNTESTED | dynamic_gatherops_alive + pipeline_cross_review（R1 REJECT→R2 scoped APPROVE）+ root | `src/transform.rs`, `docs/api/transform.md`, `tests/oracle/transform_multiequal_insert_1204.{cc,rs,metadata.json}`, `tools/run_transform_multiequal_insert_oracle.sh` | evidence=`b6af5be`（root 集成，runner 复验 exit0 covered_projection=5/5 MATCH）；locked transform.cc:242-246 createReplacement 与 :257-263 attemptInsertion 的 MULTIEQUAL `opInsertBegin` 特判已替换 Rust 无条件 `op_insert_before`。双侧 fixture 4 records/5覆盖项逐字一致：双 PHI 逆序头插、follow PHI、COPY创建序、完整 identity/SeqNum/order/def-use/bank count；targeted=1/1，R2 scoped Cross-Review APPROVE。schema2 runner fail-closed 校验 5 MATCH+6 UNTESTED coverage、四类语义与 residual union；整体 UNTESTED/L2，未覆盖面绑定 `TRANSFORM-MULTIEQUAL-INSERT-RESIDUAL-0001`；2026-08-22 |
| `TRANSFORM-MULTIEQUAL-INSERT-RESIDUAL-0001` | P2 | DONE | tmresid + 独立复核（机制 C APPROVE：六项全 PASS、setOrder 阶梯手算验证、哨兵无副作用验证、复跑 11/11 MATCH sha 6524ac83…）+ root 集成 | 交付=`7163dec`→集成=`a5ceae7`（transform:: 20/20；registry 条目升级 MATCH）。out_of_scope 5 项登记：FUNCDATA-MARKINDIRECT-STRICT-0001、OP-BANK-CREATE-NULL-SLOTS-0001、TRANSFORM-PARSESIZES-STRICT-0001、TRANSFORM-GETPIECE-DUP-THROW-0001、CREATEOPS-RETRY-UNREACHABLE-0001 | `src/transform.rs`, `docs/api/transform.md`, 扩展 `transform_multiequal_insert_1204` fixture/runner | 依赖 `TRANSFORM-MULTIEQUAL-INSERT-0001` 释放同文件；覆盖 op_preexisting、nested follow、INDIRECT special handling、异常前部分状态、`TransformOp::createReplacement` 的 `output==nullptr` 分支，以及 SeqNum midpoint 不足触发 `BlockBasic::setOrder()` 重排边界。当前目标 createReplacement/attemptInsertion 的 PHI/COPY 投影可为 MATCH，但这些路径完成前整体必须为 UNTESTED；验收=locked 12.0.4 同输入完整 identity/SeqNum/order/def-use/bank mutation/exception 对拍；evidence=pending；2026-08-21 |
| `ACTION-LANEDIVIDE-0001` | P0 | BLOCKED | unassigned | `src/coreaction.rs`, `docs/api/coreaction.md`, `tests/oracle/action_lanedivide_1204.{cc,rs,metadata.json}`, `tools/run_action_lanedivide_oracle.sh` | 依赖 `LANEDIVIDE-INFRA-0001` 与 coreaction 租约释放；完整移植 collect/process/apply、RULE_ONCEPERFUNC、generated flag、lane map clear、成功拆分 count/IR；完成前不得在默认树以 no-op 注册；evidence=pending；2026-08-20 |
| `CSPEC-TEXT-INGEST-0001` | P0 | READY | unassigned（从 handoff 恢复） | `src/arch.rs`, `docs/api/arch.md`, `tests/oracle/cspec_text_ingest_1204.{cc,rs,metadata.json}`, `tools/run_cspec_text_ingest_oracle.sh` | 当前 Wave 0 四闭包完成后认领；归档补丁=`docs/alignment_docs/handoff/cspec_slices_w19_partial_arch_rs.patch`，使用前必须重读 locked oracle、重放现有 fixture，禁止直接信任未验证 patch；evidence=pending；2026-08-20 |
| `BLOCK-INDEX-WIRE-0001` | P1 | DONE（历史补偿已由 BLOCK-BUILDCOPY-MIRROR-0001 替换） | block_index_wire_w19 | `src/blockaction.rs`, `docs/api/blockaction.md`，curl E2E 差分 | 历史 evidence=`this commit`：当时因假 BlockCopy 丢标签而在 order_loop_bodies 重算 spanning tree。2026-08-28 真实 buildCopy 已复制 basic CFG 标签，故该补偿删除；当前 authoritative 行为是 clearVisitCount 后直接消费已复制标签。旧 `EDGEFLAG-BIT7-COLLISION-0001` 结论也已被常量复核否定。 |
| `MERGE-PERSISTENT-STATE-0001` | P1 | MISMATCH | root + merge_cross_review（r1 REJECT→r2 scoped APPROVE） | `src/merge.rs`, `docs/api/merge.md`, `tests/oracle/merge_persistent_1204.{cc,rs,metadata.json}`, `tools/run_merge_persistent_oracle.sh` | evidence=`this commit`；r1 fixed-name blocker 已由 `a4a2fe9` canonical cache + actual `Arc::ptr_eq` + custom-name fixture 关闭，r2 scoped Cross-Review APPROVE。29/29 投影 MATCH；Java/CPOOL/CALLOTHER/INDIRECT TypeOp 与 testCache/copyTrims/protoPartial/marker/clear 持久通道残差仍使整体 MISMATCH/L2，分别绑定 `TYPEOP-LOCALTYPE-DISPATCH-0001`、`MERGE-PERSISTENCE-CHANNELS-0001` 及其子项；2026-08-20 |
| `TYPEFACTORY-LOCALTYPE-CACHE-0001` | P0 | REWORK | typefactory_local_cache + typefactory_cache_review（REJECT r1）+ typefactory_rework（W-2026-08-22-ORCH 续作） | `src/type_system/typefactory.rs`, `docs/api/type_system/typefactory.md`, `tests/oracle/typefactory_local_cache_1204.{cc,rs,metadata.json}`, `tools/run_typefactory_local_cache_oracle.sh` | 99条 cache/nochar 定向投影 MATCH，但整体 MISMATCH：existing noncore promotion 别名、同名冲突异常类型、core enum 不入 tree、char cache-miss、空 ctor/bootstrap、large-base、decodeCoreTypes MISSING、poison/宽字符/float10/16。依赖 `TYPEFACTORY-CORE-PROMOTION-IDENTITY-0001`、`TYPEFACTORY-CODEFLAGS-DECODE-0001`、`TYPE-0001`；不得解锁 TypeOp；evidence=`this commit`（99条投影MATCH；Cross-Review总体REJECT，故保持REWORK/L2且不解锁TypeOp）；2026-08-20 |
| `TYPEFACTORY-CORE-PROMOTION-IDENTITY-0001` | P0 | READY | unassigned | `src/type_system/{datatype,typefactory}.rs`, paired docs/fixture | Ghidra setCoreType 对既有 canonical Datatype 原地 OR core flag，所有外部别名立即可见；当前 immutable `Arc<Datatype>` 只能 clone/replace，旧 handle 状态与身份分叉。须建立可共享原位 flag/identity 模型，并对拍已有 noncore→core、同名冲突异常前后状态；evidence=pending；2026-08-20 |
| `TYPEOP-LOCALTYPE-DISPATCH-0001` | P0 | PARTIAL_INTEGRATED（D0 stable callspec identity；整体MISMATCH） | current_exact_function_count + d0_oracle_review + d0_identity_audit（独立APPROVE）+ root | D0 identity=`src/{fspec,varnode,funcdata,flow,coreaction,heritage,ruleaction,unionresolve,signature,subflow}.rs`及对应docs/9组fixture runner；D1 getter=`src/typeop.rs`, `docs/api/typeop.md`；D2 caller=`src/coreaction.rs`, `docs/api/coreaction.md`；D3 producer/string=`src/{flow,arch,stringmanage,coreaction,ruleaction,printc}.rs`, `examples/curl_decompile.rs` | oracle capture=`305dbc1`、D0 source=`15757de79fc88b3c3b6c886c76e7121e35685000`、root集成=`cad41c2`。Funcdata现以stable Arc强拥有callspec，Varnode typed Weak与spec→exact op Weak闭环；same-address identity、两键sort、exact delete、clone新owner/op/annotation+active reset的7项双侧投影逐字节MATCH，三路独立终审APPROVE。整体仍受`TYPEOP-FSPEC-SPACE-0001`、`CALLSPEC-0001`、`OPBANK-0001`、`PIPE-STALL-SHAPE-0001`约束；D1必须从input0 typed handle读取prototype并由Architecture-owned TypeFactory返回canonical fallback，禁止地址扫描、字符串硬编码或prototype snapshot旁路；2026-08-24 |
| `TYPEOP-FSPEC-SPACE-0001` | P0 | MISMATCH | unassigned | `src/space.rs`, `src/address.rs`, `src/varnode.rs`, `src/funcdata.rs`, paired annotation fixture/docs | Rust legacy `AddressSpace` 尚无真正FSPEC，D0只能暂以Iop地址+typed Weak handle保存对象身份；须接通`SpaceType::Fspec`、完整Address空间身份、encode/decode/print/lookup并对拍FSPEC与同offset Iop/Const判别，完成前TypeOpCall整体不得标MATCH；依赖=`TYPEOP-LOCALTYPE-DISPATCH-0001` D0；2026-08-24 |
| `TYPEFACTORY-POINTER-CANONICAL-0001` | P0 | BLOCKED | unassigned | `src/type_system/typefactory.rs`, `docs/api/type_system/typefactory.md`, paired fixture | 依赖 `TYPEFACTORY-LOCALTYPE-CACHE-0001` 释放同文件；TypePointer canonical identity 必须包含 size、ptrto、wordsize/space语义，不能按 `"code *"` 名称复用；为 INDIRECT/CBRANCH/CALLIND code pointer local type 提供真实 Arc；evidence=pending；2026-08-20 |
| `TYPEFACTORY-EXACTPIECE-0001` | P0 | FOUNDATION_INTEGRATED（A-D+fixture；projection/overall MISMATCH） | myfwrite_rootcause + b_frozen_review + d_scope_audit + fixture_final_review（独立APPROVE）+ root | `src/type_system/{typefactory,datatype,grammar}.rs`, `docs/api/type_system/{typefactory,datatype,grammar}.md`, `tests/oracle/typefactory_exactpiece_1204.{cc,rs,metadata.json}`, `tools/run_typefactory_exactpiece_oracle.sh` | source series=`342d86d`→`ab34598`→`bbbd208`→`5f10b94`→fixture `4d405ad`；main=`594ed4f`→`874e81f`→`23ef9c9`→`651045a`→`cf3b10b`。32条双侧记录同构，30条逐字节一致；actual diff恰46个allowlist字段，仅explicit-align struct/union各23，Ghidra/Rust SHA=`e1f3fd…91f6`/`fff1c6…79e2`，故状态保持MISMATCH。已闭合array/partial/pointer canonical tree与Arc-preserving composite `getExactPiece`顺序；残差=`TYPEFIELD-IDENT-REPRESENTATION-0001`、`TYPEFACTORY-ARC-IDENTITY-0001`、Pointer truncate、TypeCode、Spacebase/API域。当前仍零production caller，下一步迁移四处本地exact-piece fallback并让SplitDatatype消费唯一canonical API；不得升L3；2026-08-24 |
| `TYPEFACTORY-EXACTPIECE-CALLERS-0001` | P0 | IN_PROGRESS | myfwrite_rootcause + root | `src/{variable,database,funcdata,ruleaction}.rs`, `docs/api/{variable,database,funcdata,ruleaction}.md`；`src/coreaction.rs`/`docs/api/coreaction.md`仅限ActionNameVars传Architecture-owned TypeFactory；`tests/oracle/exactpiece_callers_1204.{cc,rs,metadata.json}`, `tools/run_exactpiece_callers_oracle.sh` | 依赖=`TYPEFACTORY-EXACTPIECE-0001`已满足；移除四处本地subtype/clone替代路径，全部显式消费同一Architecture TypeFactory canonical API：HighVariable::finalizeDatatype、SymbolEntry::getSizedType、Funcdata::syncVarnodesWithSymbols、RulePieceStructure::getExactPiece。禁止shared-default factory与局部复制helper；验收=同输入别名/返回Arc identity、offset/size边界、异常与完整突变双侧门禁，独立review后再解锁`SPLITDATATYPE-EXACTPIECE-0001`；evidence=pending；2026-08-24 |
| `TYPEFACTORY-ARC-IDENTITY-0001` | P0 | MISMATCH | unassigned | `src/type_system/{datatype,typefactory}.rs`, `docs/api/type_system/{datatype,typefactory}.md`, paired identity fixture | Ghidra对incomplete composite原指针erase→原位mutate→reinsert；Rust immutable`Arc<Datatype>`只能受控replacement，导致外部pre handle及先前依赖array/pointer/partial/typedef/cache保留旧对象。本轮 `ptrsub_output_token_1204` 仅证明 ordinary Struct component 的 canonical Arc/pointer/repeat identity 窄投影 MATCH；array/partial/spacebase、旧 handle 与原位突变仍 MISMATCH/UNTESTED。要求先证明old-key exact identity、拒绝new-key/name冲突，并完整观察pre/post/factory/dependent identity；在全局interior-mutability迁移前不得标MATCH；2026-08-27 |
| `TYPEFACTORY-ARCH-ALIGNMAP-WIRING-0001` | P0 | MISMATCH | unassigned | `src/arch.rs`, `src/type_system/typefactory.rs`, `docs/api/{arch,type_system/typefactory}.md`, paired TypeFactory bootstrap/decode fixture | Architecture 必须把同一 processor/compiler data-organization alignment map、enum size/meta与max-basetype-size注入唯一 TypeFactory；当前多条 raw/default factory 路径可缺map或使用不同默认值，导致 odd-size composite/array stride 与双侧同输入漂移。A/B只保证已显式写入layout的对象传播该状态，不得冒充Architecture生产闭包；依赖=`TYPEFACTORY-EXACTPIECE-0001`整栈与`CSPEC-TEXT-INGEST-0001`；2026-08-24 |
| `TYPEFIELD-IDENT-REPRESENTATION-0001` | P1 | MISMATCH | unassigned | `src/type_system/datatype.rs`, `docs/api/type_system/datatype.md`, paired TypeField encode/decode/order fixture | Ghidra `TypeField` 持有独立 `ident` 并参与字段身份/序列化，Rust 当前仅有 name/offset/type；在补齐 ident 的构造、decode/encode、compare与字段列表生命周期前，依赖完整 TypeStruct/TypeUnion 同输入身份的 fixture须标UNTESTED或MISMATCH，不得从name推断ident；2026-08-24 |
| `TYPEFACTORY-CODEFLAGS-DECODE-0001` | P0 | BLOCKED | unassigned | `src/type_system/typefactory.rs`, `docs/api/type_system/typefactory.md`, `tests/oracle/typefactory_codeflags_decode_1204.{cc,rs,metadata.json}`, `tools/run_typefactory_codeflags_decode_oracle.sh` | 依赖 `TYPEFACTORY-LOCALTYPE-CACHE-0001` 释放同文件；完整移植 type.cc:4193 `decodeTypeWithCodeFlags` 与 TypePointer→TypeCode→FuncProto ctor/dtor flags、XmlDecode cursor/异常部分状态，禁止 CPOOL 侧复制 decoder；evidence=pending；2026-08-20 |
| `CPOOL-TYPED-RECORD-0001` | P0 | MISMATCH | cpool_typed_record + root | `src/cpool.rs`, `docs/api/cpool.md`, `tests/oracle/cpool_typed_record_1204.{cc,rs,metadata.json}`, `tools/run_cpool_typed_record_oracle.sh` | evidence=`this commit`；CPoolRecord持有canonical Arc，完整tag/value/token/data/ref/map顺序、duplicate/clear与两类异常部分状态；clean pinned base+cpool sole overlay tests15/15、16/16投影MATCH。整体MISMATCH：packed IDs/raw byte受 `MARSHAL-ID-0001`/`MARSHAL-PACKED-0001`，ctor/dtor codeflags受 `TYPEFACTORY-CODEFLAGS-DECODE-0001`；roadmap降至L2；2026-08-20 |
| `USEROP-LOCALTYPE-METADATA-0001` | P0 | UNTESTED（闭包精确化：varnode.rs CALLOTHER arm + userops 接线边，**与 D2 无关**（A48 复核：typeop.cc:855-873 不经 CALLIND）；61/61 宿主迁移重钉 MATCH；a52 修中） | userop_localtype_metadata + a48 + a52 | fixture 组（runner 已迁 Arch 主机 02dfd46） | 投影侧全齐（userop.rs==overlay 字节同）；接线后配 caller-closure fixture 方可重议 MATCH；2026-08-25 |
| `MERGE-PERSISTENCE-CHANNELS-0001` | P1 | BLOCKED | unassigned | `src/merge.rs`, `docs/api/merge.md`, `tests/oracle/merge_persistent_1204.{cc,rs,metadata.json}`, `tools/run_merge_persistent_oracle.sh` | 依赖 `TYPEOP-LOCALTYPE-DISPATCH-0001`；双侧观察 testCache/copyTrims/protoPartial/marker trim/Funcdata::clear 生命周期，不得用 Rust 单边 assert 冒充差分；子缺口=`MERGE-PROTOPARTIAL-GROUP-0001`、`MERGE-COPYTRIMS-CACHE-0001`、`MERGE-MARKER-TRIM-0001`、`MERGE-CLEAR-LIFECYCLE-0001`；evidence=pending；2026-08-20 |
| `MERGE-PROTOPARTIAL-GROUP-0001` | P1 | BLOCKED | unassigned | `src/merge.rs`, `docs/api/merge.md`, paired fixture | 依赖 PieceNode/VariablePiece/partial-root 地基；当前 `group_partials` 为已知 MISSING/no-op，须移植 merge.cc:965-975 与 variable.cc mergeGroups 闭包；evidence=pending；2026-08-20 |
| `MERGE-COPYTRIMS-CACHE-0001` | P1 | BLOCKED | unassigned | `src/merge.rs`, `docs/api/merge.md`, paired fixture | 依赖 forced/address-tied trim 生产路径；完整移植 copyTrims population、testCache mutation/purge、processCopyTrims 顺序与计数观察；evidence=pending；2026-08-20 |
| `MERGE-MARKER-TRIM-0001` | P1 | BLOCKED | unassigned | `src/merge.rs`, `docs/api/merge.md`, paired fixture | 依赖 `MERGE-COPYTRIMS-CACHE-0001`；当前 marker 的粗粒度 `merge_test_with_list` 对 MULTIEQUAL/INDIRECT trim 已知 MISMATCH；evidence=pending；2026-08-20 |
| `MERGE-CLEAR-LIFECYCLE-0001` | P2 | DONE | mergeclear + 独立复核（机制C APPROVE：clear 域序逐行、JumpTable 永久域、lanedMap/localoverride 持久、四通道、2 MISMATCH 归因独立证实）+ root 集成 | 交付=`c3b9843+c330dbc`→`3c2db52+2234c5f`（merge:: 6/6、funcdata:: 42+2 已知；fixture 95）。复核簿记：commit message 行号漂移 3 处（heritage.cc:2882→2868、op.cc:1209→1210、varnode.cc:1239→1240）已此处登记 | `src/funcdata.rs`, `src/merge.rs`, `docs/api/{funcdata,merge}.md`, paired fixture | 对拍 Funcdata::clear 前后持久 Merge/testCache/copyTrims/protoPartial/attach-depth 全状态与外部 handle；同文件租约释放后认领；evidence=pending；2026-08-20 |
| `RULE-PROPCOPY-BOOKKEEP-0001` | P1 | DONE | propcopy_w19 | `src/ruleaction.rs`, `docs/api/ruleaction.md`，补 fixture | evidence=`this commit`（root 集成）。核实：核心重写已在 HEAD `1cc588d` 落地，本环交付 oracle fixture `rule_propcopy_1204`（19 records / 9 目标 case 双侧逐字节一致：读者触发/六层守卫序/opSetInput 四段记账含常量去重 g0 与多读者 descend 擦除/self-defined throw 双侧复现）；root 隔离 worktree 复跑 runner exit0 同哈希。残差（预登记跨域，非本环引入）：OPBANK-0001（NULL 输入槽哨兵不可表示）、ARCH-0001（BfdArchitecture vs 合成 Funcdata）、TYPE-UNKNOWN-0001（copySymbol typelock/namelock 未投影）+ 5 条 UNTESTED 分支（metadata 已登记）。E2E（12.0.4 golden）：defects=0/numbering=0/Matched 122 |
| `SCOPELOCAL-QUERY-0001` | P1 | REWORK-r2 | root（待地基）+ varmap_split_review（REJECT r1）+ scopelocal_query_r2（W-2026-08-22-ORCH，本轮仅 varmap.rs） | `src/{varmap,funcdata}.rs`, `docs/api/{varmap,funcdata}.md`, `tests/oracle/scopelocal_query_1204.{cc,rs,metadata.json}`, `tools/run_scopelocal_query_oracle.sh` | r1 REJECT：stable Vec/min_by_key 不等价；另 queryProperties 缺 flags/parent/stackContainer/multi-entry/use-space/partial offset，hasOverlap 缺 space，生产消费者仍走第二份 funcdata helper。依赖 `RANGEMAP-COMMON-REFINEMENT-0001`、`DB-LOCALSCOPE-MAP-0001`、`ADDRESS-0001`；验收含 equal-subsort、wide/narrow 双序、跨空间/多 uselimit/多映射 + Cross-Review；evidence=pending；2026-08-20 |
| `RANGEMAP-COMMON-REFINEMENT-0001` | P0 | DONE | rangemap_common_refinement + varmap_split_review + root | `src/rangemap.rs`, `docs/api/rangemap.md`, `tests/oracle/rangemap_common_refinement_1204.{cc,rs,metadata.json}`, `tools/run_rangemap_common_refinement_oracle.sh` | evidence=`this commit`；完整公共细分/equal bucket/hinted-unhinted/zip-unzip-erase/find/walk与stable cursor identity；r1 index-cursor REJECT反例已修为 owner_generation+part_serial，unzip保原serial、erase/zip精确失效、end/cross-map安全。r2 Cross-Review APPROVE；runner 43/43 MATCH，tests 19/19；生产 SymbolEntry/ParamEntryRange/ScopeMapper接线留各自TODO；2026-08-20 |
| `SCOPELOCAL-WINDOW-ERROR-0001` | P1 | BLOCKED | unassigned | 协调项，不直接拥有代码 | r1 后拆为已有 `SCOPELOCAL-RANGE-STATE-0001`（须追加 DB local-scope/ownership 依赖）与 `RANGEHINT-MERGE-ERROR-0001`；RestrictLocal 状态当前会被 RestructureVarnode 新建 ScopeLocal 覆盖，pending warning/error 无生产消费者；evidence=pending；2026-08-20 |
| `RANGEHINT-MERGE-ERROR-0001` | P1 | BLOCKED | unassigned | `src/{varmap,coreaction}.rs`, `docs/api/{varmap,coreaction}.md`, `tests/oracle/rangehint_merge_error_1204.{cc,rs,metadata.json}`, `tools/run_rangehint_merge_error_oracle.sh` | 依赖 `RANGEMAP-COMMON-REFINEMENT-0001`、`FUNCDATA-LOCALSCOPE-OWNERSHIP-0001` 与 coreaction/varmap 租约释放；RangeHint::merge LowlevelError 必须穿透 restructure/Action，观察异常前部分状态、scope identity、count/numpass，并证明后续 sync 不执行；最终删除 pending error 旁路；evidence=pending；2026-08-20 |
| `ACTIONTYPEINFER-VTYPE-0001` | P0 | BLOCKED | unassigned | 协调项；最终 write-set=`src/{coreaction,varnode}.rs` + paired docs/fixture | 审计结论：canonical `Some(undefinedN)` 是 Ghidra 不变量；旧 Rugra-local ActionTypeInfer/InferParams 测试 IR 非法且不对应 oracle，不能改回 None或把 `is_none` 粗换 UNKNOWN。依赖 `DATATYPE-TYPEORDER-0001`→`TYPEOP-LOCALTYPE-DISPATCH-0001`→`VARNODE-LOCALTYPE-RESOLUTION-0001`→`ACTION-INFERTYPES-DISPATCH-0001`；验收迁移为真实 ActionStartTypes/InferTypes/OutputPrototype fixture，并删除/隔离无 Ghidra 对应的旧 Action；evidence=pending；2026-08-20 |
| `DATATYPE-TYPEORDER-0001` | P0 | MISMATCH | datatype_typeorder + datatype_typeorder_review（R1/R2 REJECT→R3 scoped APPROVE）+ root | `src/type_system/datatype.rs`, `docs/api/type_system/datatype.md`, `tests/oracle/datatype_type_order_1204.{cc,rs,metadata.json}`, `tools/run_datatype_type_order_oracle.sh` | evidence=`this commit`；datatype 层 public virtual dispatch、direct Pointer/PointerRel state、TypeCode model/flags、Spacebase invalid/identity、Unicode1 submeta 已闭合；R3 同输入 fixture 78 records=72 MATCH+6 declared TypeFactory MISMATCH，tests=57/57。schema2 runner fail-closed 核对四类语义、coverage/expected mismatch/顶层 residual 集合；独立 Cross-Review scoped APPROVE。整体保持 MISMATCH/L2：6项生产 TypeFactory 分歧绑定 `TYPEFACTORY-POINTER-CANONICAL-0001`/`TYPEFACTORY-SUBMETA-RECLASS-0001`，完整 compare 矩阵 UNTESTED 绑定 `DATATYPE-TYPEORDER-RESIDUAL-0001`；2026-08-21 |
| `DATATYPE-TYPEORDER-RESIDUAL-0001` | P1 | DONE | datatype_residual + 独立复核（机制 C APPROVE：15 record 抽查独立推演零偏差、2 条 quirk MISMATCH 描述核实、runner 复跑 exit0 sha 一致、scope=证据型交付）+ root 集成 | 交付=`c14dfa9`→集成=`1180a72`（type_system::datatype 57/57）。残差：6 条 TYPEFACTORY-* + 2 条 `DATATYPE-SPACEBASE-SPACEID-0001`；UNTESTED 2 项（TypeCode null-output、struct 递归环）| `src/type_system/datatype.rs`, `docs/api/type_system/datatype.md`, 扩展 `datatype_type_order_1204` fixture/runner | 依赖 `DATATYPE-TYPEORDER-0001` R3 冻结并释放同文件。补齐 TypeCode varargs/model-name/参数与返回递归/dependency/level-id，Array/Union/Partial 全 compare/dependency/id cutoff，Struct field offset/dependency，same-kind AddrSpace identity，以及全部 24 个 SubMetatype 双侧值；未完成前状态为 UNTESTED，模块保持 L2；验收=新增分支完整状态/identity/order 观察、locked 12.0.4 零未解释差异、独立复核 APPROVE；evidence=pending；2026-08-21 |
| `VARNODE-LOCALTYPE-RESOLUTION-0001` | P0 | INTEGRATED（`8c26662`+`3879a8b`→main `b3e1056`；getLocalType core projection 62/62 双侧 MATCH，含 def-STOP 早退与真平局先遇；0x800/0x1000 常量+访问器就位） | a43 + root | `src/varnode.rs`+docs+fixture | STOP 的 coreaction 窄路径已接线（`set_stop_up_propagation` 写入、`propagateTypeEdge` 读取）；该 62/62 结论不覆盖 production `ActionInferTypes::buildLocaltypes` 消费、SymbolEntry/exact-piece 与 CALLIND/RETURN/CALLOTHER/CPOOLREF 特殊派发闭包，后者仍 MISMATCH/UNTESTED；2026-08-27 |
| `ACTION-INFERTYPES-DISPATCH-0001` | P0 | PARTIAL_INTEGRATED（PTRSUB selected projection `MATCH`；overall MISMATCH） | unassigned（本轮窄纠偏=root） | 本轮窄 slice=`src/coreaction.rs`, `docs/api/coreaction.md`, `tests/oracle/ptrsub_output_token_1204.{cc,rs,metadata.json}`, `tools/run_ptrsub_output_token_oracle.sh`；未来 closure=`tests/oracle/action_infertypes_vtype_1204.{cc,rs,metadata.json}`, `tools/run_action_infertypes_vtype_oracle.sh` | 前置 `VARNODE-LOCALTYPE-RESOLUTION-0001` 已集成；24-record PTRSUB fixture 的 unlocked SPACEBASE PTRSUB production apply 在双侧都把 base/out 变为 canonical int8 identity=1，并匹配 STOP、定义边与 `result=0`。`build_localtypes` 已改为 loc-set 顺序并调用映射的 `Varnode::get_local_type`，旧 hybrid op-walk 删除；但完整 VarnodeLocSet/SymbolEntry/exact-piece、descendant competition、非 PTRSUB 派发、DFS/repeat/error 状态仍为 MISMATCH/UNTESTED；evidence=`this commit`；last_updated=2026-08-28。 |
| `COMMENT-WARNING-CODEC-0001` | P1 | MISMATCH | comment_warning_codec + root | `src/comment.rs`, `docs/api/comment.md`, `tests/oracle/comment_warning_codec_1204.{cc,rs,metadata.json}`, `tools/run_comment_warning_codec_oracle.sh` | evidence=`this commit`；修复 space/offset 属性混写、XMLcontent 通道、unknown type/missing offset 错误与部分状态；locked projection 6行 MATCH、comment tests 11/11。整体保持 MISMATCH：Decoder 缺 AddrSpaceManager/readSpace，绑定 `ADDRESS-0001`/`MARSHAL-PACKED-0001`；CommentSorter iterator 另属 `COMMENT-SORTER-ITERATORS-0001`；2026-08-20 |
| `COMMENT-SORTER-ITERATORS-0001` | P1 | DONE | comment_sorter + root 集成验证（runner 复跑 exit0 38 行 MATCH、comment:: 15/15、全量 lib 1470/2 已知失败）| 交付=`dc6eb2c`→集成=`d7a9cae`，registry 第 92 个 fixture。残差：`BLOCKBASIC-COVER-0001`、`COMMENT-DB-EMITTED-0001`、`COMMENT-SORTER-PRINTC-0001` | `src/comment.rs`, `docs/api/comment.md`, paired iterator-state fixture | 完整移植 setupBlockList/setupOpList/setupHeader 对共享 start/stop/opstop 的 lower/upper_bound 状态，观察 hasMore/header/basic/op-stop 交错消费；当前 Vec-return adapter 与 setup_header no-op 不得算对齐；evidence=pending；2026-08-20 |
| `DYNAMIC-GATHEROPS-ALIVE-0001` | P1 | DONE | dynamic_gatherops_alive + root | `src/dynamic.rs`, `docs/api/dynamic.md`, `tests/oracle/dynamic_gatherops_alive_1204.{cc,rs,metadata.json}`, `tools/run_dynamic_gatherops_alive_oracle.sh` | evidence=`this commit`；根因双闭包：旧单测 newOp 未 opInsert（两侧均 dead，不应收集）；生产旧实现扫 alivelist 导致同地址结果按 alive 插入序，locked Ghidra beginOp/endOp 实为 optree SeqNum 地址闭区间并显式过滤 dead。修复走 `obank.begin_addr`，首个地址大于 target 即停，保持输出 append。runner 7/7 MATCH；targeted 1/1；canonical curl defects=0/numbering=0/skeleton=2926；2026-08-20 |
| `RULE-ADDUNSIGNED-TYPEPRECOND-0001` | P1 | MISMATCH | rule_addunsigned_precond + root | `src/ruleaction.rs`, `docs/api/ruleaction.md`, `tests/oracle/rule_addunsigned_typeprecond_1204.{cc,rs,metadata.json}`, `tools/run_rule_addunsigned_typeprecond_oracle.sh` | evidence=`this commit`；旧 fire/no-fire tests 显式安装同factory canonical UINT，新增默认 UNKNOWN拒绝；生产 apply零改动。fixture unknown_ff/uint_ff/uint_7f 7/7投影MATCH；整体 MISMATCH：None、op-aware union、equate、enum、rule name已知分歧，char/pool-repeat未测，均绑定本ID；2026-08-20 |

### 恢复队列（按优先级，未认领）

1. `PRINTC-UNLINKED-REF-0001`（17 函数 backfill 注入组残差）。
2. `BASE-EXPLICIT-GAPS-0001`（P1 READY）— `coreaction.rs` 租约冲突，PIPE 与
   `ACTIONTYPEINFER-VTYPE-0001` 串行完成后认领。

### 下一 wave 候选（未认领）
PIPE-MERGETYPE-ORDER 已 DONE；BLOCK-INDEX-WIRE / RAW-OPINSERT / SETALLINPUT /
PRINTRAW-WORDSIZE / UNLINKED-REF / MAKEREC-CALLIND / HERITAGE-COLLECT-WRAPAROUND 等见下方表格。

**锁定 oracle**：Ghidra 12.0.4 / `Ghidra_12.0.4_build` /
`e40ed13014025f82488b1f8f7bca566894ac376b`。

**状态机**：`BACKLOG → READY → IN_PROGRESS → REVIEW → DONE`。`BLOCKED` 必须指向一个更底层 TODO ID；
`DONE` 必须同时有 commit、四类语义、验收命令和 oracle 状态。

**工具链串行集成规则**：下列任务中的 `docs/TODO_BOARD.md`、`docs/VERIFICATION_GUIDE.md`、
本轮计划文档、`tools/README.md` 及 `tests/oracle/fixture_registry.json` 均为 root 串行集成文件，不作为并行 agent 的
共享写租约。协调父任务只拆分并登记明确的子任务；禁止把 metadata/runner 通配路径交给单个 writer。

| ID | P | 状态 | owner | Ghidra ↔ Rugra | write-set | 依赖 / 验收 | 更新 |
|---|---:|---|---|---|---|---|---|
| `ORACLE-0001` | P0 | DONE | root | 本地参考树 | 忽略的 `ghidra/` | HEAD=`e40ed13`, 114 `.cc` | 2026-08-11 |
| `ORACLE-0002` | P0 | DONE | golden_regen_wave | 12.0.4 curl/httpd golden | `tools/build_ghidra_1204_headless.sh`, `tools/regen_ghidra_golden.py`, `tests/golden/ghidra_{curl,httpd}_1204.{c,provenance.json}` + direct-runner 补充 | evidence=`pending root commit`；Path 1 成功：`build_ghidra_1204_headless.sh` 从锁定 e40ed130 源码构建**真 12.0.4 headless dist**（便携 JDK21/Gradle 到 /tmp，未用 sudo；proxychains loopback 劫持与沙箱阻断已修，约 40min 可重建）。canonical golden 经 analyzeHeadless 默认分析 + postScript 产出：**curl 124 函数/0 FAILED/3318 行（双跑 byte-identical）**、httpd 2010 函数/0 FAILED/66637 行；provenance 全字段（oracle/arch `x86:LE:64:default`/cspec `gcc`/options/输入 sha=8af50bca/逐函数 ledger）。vs 11.3.2：123/124 matched、62 identical、61 changed（261 行，含输入血缘漂移——现 curl 为 2026-08-13 DWARF 版）。direct-runner 补充 golden（74/790 函数）与 headless 对比 2/74、3/790 相同，等价性鸿沟量化入 provenance。`--check` rc=0（root 复验 124+2010 ledger 全 OK）。dist 在 /tmp（易失，脚本可重建）。**新基准已就位：Rugra 对 12.0.4 的端到端差分从现在起可量测** | 2026-08-15 |
| `LEDGER-0001` | P0 | DONE | root | 114 `.cc/.hh` 全函数 ↔ `src/**/*.rs` + 协议表/依赖 DAG | `tools/generate_function_ledger.py`, `docs/alignment_audit/{FUNCTION_LEDGER.json,FUNCTION_MAP.md,FUNCTION_MAP.generated.md,PROTOCOL_TABLE.json,DEPENDENCY_DAG.json}`, `docs/TODO_BOARD.md` | evidence=`this commit`；locked HEAD/clean tree + Universal Ctags 6.2.0；9494 behavior definitions（5691 `.cc` + 3803 `.hh`）/6317 declarations/15811 raw records，Rugra=8551（7164 production + 1387 test）；3116 exact definition markers、377 body refs，其余不伪报；481 Rust + 396 include + 180 TODO dependency edges；隔离 HEAD+expression overlay 生成、self-test、py_compile、`--check` 与 strict selector 零漂移 | 2026-08-14 |
| `PERF-BUILD-0001` | P0 | DONE | root | Rust/C++ 可复现快速构建 | `.gitignore`, `Cargo.toml`, `Cargo.lock`, `build.rs`, `tools/rugra_build.py`, `tools/README.md`, `docs/TODO_BOARD.md` | evidence=`this commit`；versioned lockfile + `cc/parallel`；受控 offline/locked 环境、430-file input digest、工具链/dirty-state/report；自动探测 cache（本机无 sccache/ccache，正确回退 none）；fast-release all-targets cold=43.17s、warm=0.162s，release lib=35.79s；self-test/py_compile/tree/profile checks 全绿，canonical release 配置未改 | 2026-08-12 |
| `PERF-GATE-0001` | P0 | DONE | root | edit/commit/wave/nightly 四级门禁 | `tools/rugra_gate.py`, `tests/oracle/fixture_registry.json`, `tools/README.md`, `docs/VERIFICATION_GUIDE.md`, `docs/TODO_BOARD.md` | evidence=`this commit`；每级命令、超时、输入/输出 hash、exit 状态与工作树指纹结构化记录；commit 级使用稳定函数账本选择 changed fixture，未覆盖源码改动 fail-closed；wave/nightly 保留全 fixture、端到端与冷 release 门禁；self-test、dry-run、真实 edit tier 全绿（health=0.04s，fast lib check=17.99s） | 2026-08-12 |
| `IMPACT-0001` | P0 | DONE | root | changed Rust function/path → oracle fixture/门禁 | `tools/select_fixtures.py`, `tests/oracle/fixture_registry.json`, `tools/README.md`, `docs/VERIFICATION_GUIDE.md`, `docs/TODO_BOARD.md` | evidence=`this commit`；依赖 `LEDGER-0001`（已满足）；5 个真实 runner 注册；git hunk→当前 Rust span→稳定 ID，支持 staged/base/显式 path/function/tier；新增/删除/rename-as-D+A/顶层/未登记函数 fail-closed 选择全量，`--strict` rc=2；registry stale-ID/runner/commit/timeout 自检；目标选择约 0.4s | 2026-08-12 |
| `ORACLE-CACHE-0001` | P0 | DONE | root | 内容寻址 oracle 产物与阶段快照 | `.gitignore`, `tools/oracle_cache.py`, `tools/{rugra_gate,select_fixtures}.py`, `tests/oracle/{fixture_registry.json,preferred_zext_1204.metadata.json}`, `tools/README.md`, `docs/VERIFICATION_GUIDE.md`, `docs/TODO_BOARD.md` | evidence=`this commit`；5 个 registry fixture 默认走 cache capture；key 覆盖锁定 commit、arch/cspec/options、metadata、input/tool/comparand、command/executable/env hash；成功可重放，失败/超时不缓存，`--refresh-fixtures` 检测同 key 非确定输出；原子并发 store、逐 artifact/结果交叉校验、防 symlink/path traversal、只读 restore、超时清理进程组；命中重新计算 provenance；self-test + real miss/store/hit/verify/restore 全绿，preferred fixture 4.34s→0.128s | 2026-08-12 |
| `DIFF-BISECT-0001` | P1 | DONE | root | pipeline stage hash/首差异定位 | `tools/stage_diff.py`, `tools/README.md`, `docs/VERIFICATION_GUIDE.md`, `docs/TODO_BOARD.md` | evidence=`this commit`；依赖 `ORACLE-CACHE-0001`（已满足）；snapshot 固定 oracle/arch/cspec/options/metadata/context 与有序原始 artifact；compare 报首个 provenance/stage ID/order/schema/state/hash/size/缺失差异，rc=0/1/2；不规范化顺序/别名/CFG；self-test 覆盖零差异/内容/重排/缺失/schema | 2026-08-12 |
| `ORACLE-CACHE-HARDEN-0002` | P0 | DONE | oracle_cache_harden_wave | RUGRA-GLUE：oracle cache 的实际执行环境、TOCTOU 与 artifact bundle 闭包 | `tools/oracle_cache.py`; root 串行更新`tools/README.md`及验证/TODO/计划文档 | evidence=`pending root commit`；capture 执行环境与 provenance 快照字节一致（`Popen(env=...)`=declared snapshot，未声明变量不可见，EXEC_ENV_KEYS 常声明，argv0 按 child-PATH 解析）；PATH 值/PATH 目录内容/declared 值变化必换 key；命令后 env post-readback 漂移与全量 provenance 重推导不一致均拒绝入库（rc=2）；restore 逐 artifact hash/size/结构交叉校验+复制后源重校验+闭包比对，缺失/多余/篡改/中途换源全拒且无 partial output；HIT 回放字节级 hash 校验；`--self-test` 矩阵（env/TOCTOU/恢复/CLI 端到端）root 复验全绿，/tmp 合成三攻击（环境注入/执行期漂移/恢复篡改）全部拒绝；preferred_zext_1204 真实 fixture 冷 STORE→HIT 逐字回放不回归，select_fixtures/rugra_gate/stage_diff/reduce_fixture importer 自检通过。遗留：旧 `.rugra-cache` 条目因 env 语义变化一次性重捕（预期）；ORACLE-RESULT-0001 的三层缓存 lineage 归该 TODO | 2026-08-15 |
| `DIFF-REDUCE-HARDEN-0002` | P0 | DONE | reduce_harden_wave | RUGRA-GLUE：通用 deterministic reducer 的原始输入、fresh final 与失败签名合同 | `tools/reduce_fixture.py`; root 串行更新`tools/README.md`及验证/TODO/计划文档 | evidence=`pending root commit`；`--self-test` 矩阵（原始/drift/签名强制/invalid 跳过/fresh 假阳性/resume 身份门禁）+ py_compile 全绿；/tmp 合成演示：签名强制下 bug-a 缩至 `[2,4]` 而无签名对照缩至 `[5]`（bug B），first-sighting 谓词的 memo 假阳性被 final-fresh 捕获 rc=2；schema 1→2 增量、CLI 调用面兼容（新增 `--invalid-exit`/`--resume` 带默认值）。遗留：schema 1 旧 trace 不可 resume（有意 fail-closed）；observation 级签名扩展归 `PCODE-CFG-REDUCE-0001` | 2026-08-15 |
| `FUNCTION-ID-0001` | P0 | DONE | function_id_wave | locked Ghidra/Rust 定义 ↔ 不依赖行号或同名 ordinal 的稳定函数 ID | `tools/generate_function_ledger.py`, `docs/alignment_audit/{FUNCTION_LEDGER.json,FUNCTION_MAP.md,FUNCTION_MAP.generated.md,FUNCTION_ID_MIGRATION.json}`; root 串行更新TODO/计划文档 | evidence=`pending root commit`；ID scheme 2 落地：Ghidra=path+类别+限定签名（碰撞用预处理守卫消歧，12 条显式消歧符），Rust=module+owner（`Type as Trait`/`macro:NAME`/`fn:` 域）+规范化签名，行号与 name_ordinal 不参与；旧 ID 歧义/新碰撞双 fail-closed（实测 0/0）。`--self-test` 覆盖同名插删移动/staged-base-dirty 四态/守卫消歧/双歧义拒绝；root 串行重生产物：`--migrate`（24370 条 0 歧义 12 消歧符）→ `generate`（definitions=9494 raw=15811 rust=8644）→ `--check` verified；select_fixtures self-test / gate health 全兼容。**已知有界缺口**（→`FUNCTION-ID-MIGRATE-REKEY-0001`）：`--migrate` 对 22 个签名已变函数（cover.rs rebuild 族等）的 rekey new_id 与 `--generate` 重扫不一致（表内 22 个 new_id 不在当前账本）；`--migrate` 在账本已迁移后拒绝重跑（防重入，设计如此）。registry 45 RG-F/7 处硬编码 GH12-F 迁移归 `ORACLE-REGISTRY-0001`（依赖已满足），3 个 cover.rs registry 项需按新账本重选 | 2026-08-15 |
| `ORACLE-REGISTRY-0001` | P0 | DONE | oracle_registry_wave | RUGRA-GLUE：fixture schema、registry doctor 与迁移清单生成 | `tests/oracle/schema/fixture-v1.schema.json`, `tools/oracle_registry.py`, `tests/oracle/fixture_registry.json`, `tools/README.md`; root 串行更新验证/TODO/计划文档 | 依赖 `FUNCTION-ID-0001`；反向发现全部tracked metadata/runner/comparand，orphan/重复ID/缺provenance/状态冲突/stale function ID均fail-closed；self-test覆盖单侧假MATCH；只输出确定性迁移计划，本任务不批量改metadata且不提前启用strict CI；验收=`python3 tools/oracle_registry.py self-test && python3 tools/oracle_registry.py plan --check-determinism`，evidence=`pending` | 2026-08-14 |
| `ORACLE-REGISTRY-ENFORCE-0001` | P0 | BLOCKED | unassigned | fixture registry/schema → commit gate 与 CI fail-closed enforcement | `tools/{check_gate_health,rugra_gate,select_fixtures}.py`, `.github/workflows/alignment-gates.yml`, `tests/oracle/fixture_registry.json`, `tools/README.md`; root 串行更新验证/TODO/计划文档 | 依赖 `ORACLE-METADATA-MIGRATE-0001`；strict lint进入本地门禁和CI，要求orphan metadata/runner、schema/status冲突、stale function ID、`PENDING_*` pin全部为0；验收=`python3 tools/oracle_registry.py lint --strict && python3 tools/check_gate_health.py && python3 tools/rugra_gate.py commit --staged --dry-run`，evidence=`pending` | 2026-08-14 |
| `FUNCTION-EVIDENCE-0001` | P0 | BLOCKED | unassigned | locked Ghidra/Rust 稳定函数 ID ↔ fixture case/observation/residual/caller 证据索引 | `tools/{generate_function_ledger,select_fixtures,evidence_query}.py`, `docs/alignment_audit/{FUNCTION_LEDGER.json,FUNCTION_MAP.md,FUNCTION_MAP.generated.md,FUNCTION_EVIDENCE.json}`; root 串行更新`tools/README.md`及验证/TODO/计划文档 | 依赖 `ORACLE-REGISTRY-ENFORCE-0001`；`select_on_paths`仅触发运行，只有`proves_*_functions`+observation可消除coverage gap；staged/base读取对应Git blob，删除hunk取pre-image，dirty不得污染，stale ledger与同名歧义拒绝；验收A=`python3 tools/select_fixtures.py --self-test --matrix staged,base,delete,dirty,stale,ambiguous` rc=0且每case候选集合等于fixture；验收B=`python3 tools/select_fixtures.py --path src/action.rs --strict --pretty` rc=2且`coverage_gap`非空；验收C=`python3 tools/evidence_query.py check --strict` rc=0；~2055/~5200+/9494分母冲突关闭前全局完成度未证明，evidence=`pending` | 2026-08-14 |
| `ORACLE-RESULT-0001` | P0 | READY | unassigned | RUGRA-GLUE：统一 oracle runner/result、层内结构 diff 与分层 artifact cache | `tests/oracle/schema/oracle-result-v1.schema.json`, `tools/{oracle_harness,observation_diff,stage_diff,oracle_cache}.py`, `tools/run_getstr_pipeline_oracle.sh`, `tests/oracle/getstr_pipeline_1204.metadata.json`; root 串行更新`tools/README.md`及验证/TODO/计划文档 | 依赖 `ORACLE-REGISTRY-0001`、`ORACLE-CACHE-HARDEN-0002`、`DIFF-BISECT-0001`；schema分离`execution_status/comparison_status/expected_comparison_status/status_drift`，gate rc=0/1/2分别表示expected一致/状态漂移/harness失败；`00_effective_configuration`不同即`NO_ORACLE`；建立RFC6901诊断位置与site/predicate/observation三级签名，保留slot/alias/CFG顺序；历史重放/current诊断/审核accept分离，三层缓存原子发布并记录run lineage；验收=`python3 tools/oracle_harness.py self-test`覆盖preflight漂移、已知MISMATCH rc0、状态漂移rc1、相同ERROR rc2、全MATCH/尾部增删，evidence=`pending` | 2026-08-14 |
| `ORACLE-RUNNER-MIGRATE-0001` | P1 | BLOCKED | root | 现有 locked oracle runners → `oracle-result-v1` 通用 harness | 协调父任务仅写 root 串行 TODO/计划文档；激活后逐批生成明确 runner+metadata+comparand 子任务 | 依赖 `ORACLE-RESULT-0001`、`ORACLE-REGISTRY-ENFORCE-0001`；子任务禁止通配write-set，迁移前后两侧raw输出hash、观察域和整体B2状态必须完全一致；全部registry runner产schema-valid result并可由cache恢复完整artifact，任何观察域缩窄立即停止；核心fixture保留独立review边界；验收=`python3 tools/oracle_registry.py runner-status --schema oracle-result-v1 --strict`，evidence=`pending` | 2026-08-14 |
| `IR-INVARIANT-0001` | P0 | BLOCKED | unassigned | `VarnodeBank/PcodeOpBank/Funcdata/BlockGraph` 对象图 ↔ RUGRA-GLUE mutation journal/invariant checker | `src/align/{ir_invariant,mod}.rs`, `docs/api/align/{ir_invariant,mod}.md`, `src/{funcdata,varnode,op,block}.rs`, `docs/api/{funcdata,varnode,op,block}.md`, `tests/oracle/ir_mutation_journal_1204.{cc,rs,metadata.json}`, `tools/run_ir_mutation_journal_oracle.sh`, `tests/oracle/fixture_registry.json`; root 串行更新TODO | 依赖 `ORACLE-RESULT-0001`且四个src文件活跃writer租约全部释放；检查input↔descend occurrence、output↔def、Loc/Def key、alive/dead/opcode list、parent/block order、CFG reverse slot/PHI顺序、Cover/High identity；提供mutation/rule/action开销级，默认关闭且关闭时现有oracle输出hash不变；逐项注入corruption须在首个非法mutation报稳定path/signature，任何映射函数插桩前重读locked全函数；验收=`cargo test --lib align::ir_invariant::tests:: && tools/run_ir_mutation_journal_oracle.sh`，evidence=`pending` | 2026-08-14 |
| `ACTION-TRACE-0001` | P1 | BLOCKED | unassigned | Ghidra `Action::perform`/`ActionPool::processOp`/`Funcdata::debugMod*` ↔ Rugra Action/Rule JSONL observer | `src/align/{action_trace,mod}.rs`, `docs/api/align/{action_trace,mod}.md`, `src/action.rs`, `docs/api/action.md`, `tools/{run_action_trace_oracle,trace_diff}.py`, `tests/oracle/schema/action-trace-v1.schema.json`, `tests/oracle/action_trace_1204.{cc,rs,metadata.json}`, `tests/oracle/fixture_registry.json`; root 串行更新TODO | 依赖 `ORACLE-RESULT-0001`、`IR-INVARIANT-0001`、`PIPE-TREE-0001`；记录tree path、Rule、计数/reset、SeqNum、alias、before/after mutation、返回/异常并比较最长共同前缀；opcode redispatch/DeadCode/merge-order已知case须定位到首个具体Rule/字段；无trace模式输出hash不变，触及主管线必须独立Cross-Review，debug trace不得冒充B2 MATCH；验收=`python3 tools/run_action_trace_oracle.py --fixture action_opcode_redispatch_1204 && python3 tools/trace_diff.py --self-test`，evidence=`pending` | 2026-08-14 |
| `HANG-TRIAGE-0001` | P1 | BLOCKED | unassigned | RUGRA-GLUE：隔离 worker 超时前线程/锁/progress/CPU 诊断与分类 | `tools/hang_triage.py`, `tools/run_curl_timeout_isolation.sh`, `examples/curl_decompile.rs`; root 串行更新TODO/计划文档 | 依赖 `RUNTIME-TIMEOUT-0001`、`ORACLE-RESULT-0001`、`ACTION-TRACE-0001`；终止前保存全线程栈、`/proc` wchan/stat/stack、进程树、最后稳定Action/Rule event+progress hash、CPU与IR cardinality，分类`PANIC/SELF_LOCK/NO_PROGRESS_LOOP/CPU_SCALE/IO_WAIT/PROTOCOL_FAILURE`后仍kill/reap PGID；synthetic自锁/CPU loop/I/O/child进程矩阵通过且诊断关闭时driver输出不变；验收=`python3 tools/hang_triage.py --self-test && tools/run_curl_timeout_isolation.sh --self-test`，evidence=`pending` | 2026-08-14 |
| `PCODE-CASE-DSL-0001` | P1 | BLOCKED | unassigned | locked Ghidra/Rugra production P-code/CFG mutation API ↔ shared `pcode_cfg_case_v1` 输入协议 | `tests/oracle/pcode_cfg_case_v1.md`, `tests/oracle/pcode_cfg_fuzz_1204.{cc,rs,metadata.json}`, `tools/{pcode_cfg_case.py,run_pcode_cfg_fuzz_oracle.sh}`, `tests/oracle/fixture_registry.json`; root 串行更新TODO | 依赖 `FUNCTION-EVIDENCE-0001`、`ORACLE-RESULT-0001`；严格行协议仅调用生产API并维护ID/alias、def-use、parent/block order、CFG reverse slot、PHI incoming、IOP及LOAD/STORE space前置条件；每步输出完整对象图且顺序/alias不规范化；验收=`python3 tools/pcode_cfg_case.py --self-test && tools/run_pcode_cfg_fuzz_oracle.sh --profile replay-smoke`覆盖valid/invalid/重复slot/PHI/IOP，evidence=`pending` | 2026-08-14 |
| `PCODE-CFG-FUZZ-0001` | P1 | BLOCKED | unassigned | deterministic valid-case generator ↔ locked oracle/Rugra generic replay | `tools/fuzz_pcode_cfg.py`, `tests/oracle/corpus/pcode_cfg_v1/manifest.json`, `tools/run_pcode_cfg_corpus_oracle.sh`, `tests/oracle/fixture_registry.json`; root 串行更新TODO | 依赖 `PCODE-CASE-DSL-0001`、`IR-INVARIANT-0001`；profile顺序固定为`opbank_mutation_v1→rule_local_v1→small_ssa_cfg_v1→action_tree_v1`，暂不以完整二进制fuzz制造已知residual噪音；验收=`python3 tools/fuzz_pcode_cfg.py --profile opbank_mutation_v1 --seed 1 --cases 100 --check-determinism`，要求两次report/corpus hash相同、100% schema-valid；同bug缩减前后predicate相同，而不同slot/opcode/alias role/左右方向predicate不同，evidence=`pending` | 2026-08-14 |
| `PCODE-CFG-REDUCE-0001` | P1 | BLOCKED | unassigned | grammar-aware CFG/def-use/PHI reducer ↔ 同一稳定语义 `predicate_signature` oracle mismatch | `tools/{reduce_pcode_cfg,triage_oracle_failures}.py`, `tests/oracle/pcode_cfg_case_v1.md`; root 串行更新验证/TODO/计划文档 | 依赖 `DIFF-REDUCE-HARDEN-0002`、`PCODE-CFG-FUZZ-0001`、`ORACLE-RESULT-0001`；六态`SAME_FAILURE/MATCH/DIFFERENT_FAILURE/INVALID_CASE/HARNESS_ERROR/NONDETERMINISTIC`；删操作/依赖闭包/块边/op/VN后同步修PHI/def-use，再缩slot/size/address/flags；event/具体JSON Pointer可变，语义operand/edge/alias role不可丢；最终fresh三次同predicate且所有transform不可再缩；验收=`python3 tools/reduce_pcode_cfg.py --self-test && python3 tools/triage_oracle_failures.py --check`，evidence=`pending` | 2026-08-14 |
| `ORACLE-CORPUS-PROMOTE-0001` | P1 | BLOCKED | unassigned | minimized failure → 去重、provenance 完整的正式 oracle corpus/registry entry | `tools/promote_oracle_case.py`, `tests/oracle/corpus/pcode_cfg_v1/manifest.json`, `tools/run_pcode_cfg_corpus_oracle.sh`; root 串行更新registry/验证/TODO文档 | 依赖 `PCODE-CFG-REDUCE-0001`、`ORACLE-REGISTRY-ENFORCE-0001`；按`(site_signature,predicate_signature)`去重并保留`(steps,blocks,edges,ops,varnodes,bytes)`最小case；缺oracle commit/arch/cspec/options/input hash/稳定函数ID/TODO/双边observation hash/reducer trace任一字段即拒绝promotion；验收=`python3 tools/promote_oracle_case.py --self-test && tools/run_pcode_cfg_corpus_oracle.sh`且registry strict lint全绿，evidence=`pending` | 2026-08-14 |
| `PIPE-SNAPSHOT-0001` | P0 | DONE | root | locked 12.0.4 `GetStr` ↔ Rugra 六层 pipeline snapshot | `examples/getstr_stage_snapshot.rs`, `tests/oracle/getstr_pipeline_1204.{cc,metadata.json}`, `tools/run_getstr_pipeline_oracle.sh`, `tests/oracle/fixture_registry.json`, `docs/{TODO_BOARD,VERIFICATION_GUIDE}.md` | evidence=`this commit`；同一 curl SHA/entry/x86-64 gcc/BFD-symbol-only 输入保存 P-code、CFG、Heritage/SSA、Action IR、结构树、C；真实 locked runner exit0 并产 stage manifests/comparison；首差异 raw op 103↔105、index77 为 Ghidra 可达 `0x3708` ↔ Rugra 不可达 NOP `0x3702`；两次 Rugra 00-02逐字相同但03起非确定，完整保留不规范化；Heritage 边界仍 `NO_ORACLE`，overall=`MISMATCH`、模块不升级 | 2026-08-13 |
| `PIPE-REACH-0001` | P0 | DONE | root | `Funcdata::followFlow` reachable-instruction policy ↔ canonical curl/GetStr paths | `src/{flow,funcdata}.rs`, `src/disasm/sleigh_lift.rs`, `src/bin/rugra.rs`, `docs/api/{flow,funcdata}.md`, `docs/api/{disasm/sleigh_lift,bin/rugra}.md`, `examples/{getstr_stage_snapshot,curl_decompile}.rs`, `tests/oracle/{getstr_pipeline_1204.metadata.json,fixture_registry.json}`, `tools/run_getstr_pipeline_oracle.sh`, `docs/{TODO_BOARD,VERIFICATION_GUIDE}.md` | evidence=`this commit`；locked runner exit0：不可达 `0x3702` 消失、raw 103 ops/272 VNs与 Ghidra同数、103项 numeric op signature 顺序 MATCH、CFG 6块同数、两次隔离 release 六层逐字稳定；overall 仍 MISMATCH（首 storage diff=Fspec5↔Iop7；首 Varnode state diff=type/cover flags；Heritage/Action/C差距大）。最新 curl 仍仅 2/23 函数通过 gcc 审计；11.3.2 诊断 golden 为 defects=0、numbering=3、skeleton=3690，分别继续绑定 Print/Type/Address/FSPEC 与 `VARMAP-NAMING-0001`，不得把 raw-op 改善解释成最终 C 对齐 | 2026-08-13 |
| `OP-INSERT-0001` | P0 | MISMATCH | op_insert_wave | `Funcdata::opInsert*` / `PcodeOpBank` alive + `BlockBasic` parent/ordered list 原子更新 | `src/funcdata.rs`, `docs/api/funcdata.md`, `tests/oracle/op_insert_1204.{cc,rs,metadata.json}`, `tools/run_op_insert_oracle.sh` | evidence=`this commit`；锁定 12.0.4 同输入 direct diff 的 covered projection=`MATCH`：有效 `BlockBasic` 域的 before/after/begin/end/uninsert、parent、block 顺序、alive/dead、def/use、SeqNum order、`SWITCH_OUT` 与 `setOpcode` 派生 flags；独立 reviewer APPROVE。overall=`MISMATCH`：parentless compatibility、nullable slot/destroy/dead-IOP/order 边界归 `OPBANK-0001`/`SEQNUM-0001`，分别为 `MISMATCH`/`UNTESTED`，模块保持 L2 | 2026-08-23 |
| `BLOCK-ENTRY-0001` | P0 | DONE | block_entry_wave | `FlowInfo::generateBlocks` entry block 标志与首块身份 | `src/flow.rs`, `docs/api/flow.md`, `tests/oracle/block_entry_1204.{cc,rs,metadata.json}`, `tools/run_block_entry_oracle.sh` | evidence=`this commit`；锁定 12.0.4 ret/self-loop direct runner `MATCH`：BlockGraph list、真实/合成 entry 对象、唯一 `ENTRY_POINT=0x200`、地址范围、有序双向边与 reverse slots 完整零差异；独立 `Cross-Review: APPROVE`。不扩为一般 FlowInfo/L3 | 2026-08-13 |
| `FLOW-TARGET-BOUND-0001` | P0 | DONE | block_entry_wave | `FlowInfo::{setFallthruBound,fallthru}` visited join `STARTBASIC` | `src/flow.rs`, `docs/api/flow.md`, `tests/oracle/flow_target_boundary_1204.{cc,rs,metadata.json}`, `tools/run_flow_target_boundary_oracle.sh`, `docs/TODO_BOARD.md` | evidence=`this commit`；锁定 12.0.4 minimal join + immutable Git-blob GetStr runner `MATCH`：3块 `0→[1,2],1→2`；GetStr 6块完整 flags/op/range/order/reverse-slot，CALL block1 `out=[2:1]`、join block2 `in=[0:1,1:0]`。清除 provisional edges 后仅按 collectEdges 原序重放；独立 `Cross-Review: APPROVE`。offcut/OOB/relative/BRANCHIND/multiroot 等仍 UNTESTED，模块保持 L2 | 2026-08-13 |
| `VARNODE-INIT-0001` | P0 | PARTIAL_MATCH | varnode_init_wave | `Varnode` constructor + `VarnodeBank` xref/ordering/cover/canonical caller closure + `termOrder` | `src/{varnode,funcdata,heritage,double_precis}.rs`, `docs/api/{varnode,funcdata,heritage,double_precis}.md`, `tests/oracle/varnode_init_1204.{cc,rs,metadata.json}`, `tools/run_varnode_init_oracle.sh`, `docs/TODO_BOARD.md`, `ALIGNMENT_ROADMAP.md` | evidence=`this commit`；locked 12.0.4 runner exit0：22行，其中 constructor/setdef/xref/guard/destroy/ordering/combine covered域一致，唯一Cover semantic endpoint为已登记MISMATCH；17 Varnode +2 combine +9 Heritage回归通过，完整crate pin=`ddcd1175…`，独立 `Cross-Review: APPROVE`。canonical Arc传播、重复slot重接、checked makeFree/destroy、Loc/Def排序与termOrder已闭合；Heritage caller graph仅Rust回归仍UNTESTED，TypeFactory/IOP-FSPEC/SeqNum/nullable/BE/ProtoModel及Cover残差保留，模块不升L3 | 2026-08-13 |
| `TYPE-UNKNOWN-0001` | P0 | IN_PROGRESS | type_unknown_wave | `TypeFactory::getBase(size, TYPE_UNKNOWN)` canonical unknown types | `src/type_system/typefactory.rs`, `docs/api/type_system/typefactory.md`, `tests/oracle/type_unknown_1204.{cc,rs,metadata.json}`, `tools/run_type_unknown_oracle.sh`, `docs/TODO_BOARD.md` | 与 VARNODE write-set 不重叠；锁定 `type.cc/.hh` 与 TypeFactory cache/compare/name路径，完整对拍 size/metatype/name/id/flags及同factory repeated-call pointer identity、异size排序；先闭合 factory，再由后续任务移除 VarnodeBank 临时 unknown cache | 2026-08-13 |
| `PIPE-LIFECYCLE-0001` | P0 | PARTIAL_MATCH | pipeline_lifecycle_wave | `ActionStart` / `ActionStop` / `ActionPrototypeTypes` locked-input creation | `src/coreaction.rs`, `docs/api/coreaction.md`, `tests/oracle/pipeline_lifecycle_1204.{cc,rs,metadata.json}`, `tools/run_pipeline_lifecycle_oracle.sh` | evidence=`30ecbee`；ActionStart/Stop wrapper fields locked fixture MATCH且独立 reviewer APPROVE；完整 lifecycle仍 MISMATCH：Ghidra GetStr followFlow=103 ops/272 VN/6 blocks/2 calls，Rugra=0；ActionPrototypeTypes locked-input 因 Address/ProtoModel/ParamList缺口仍UNTESTED，模块保持L2；runner+2 scoped tests+cargo check+annotations/refs/bash/json/diff-check通过。MODELBIND r2 备注：cc:4620-4621 prepareThisPointer 仍未实现（r2 只落 4615-4619 绑定尾） | 2026-08-13 |
| `HERITAGE-DRIVER-0001` | P0 | AUDITED | heritage_driver_audit | `Heritage::heritage` canonical driver ↔ `ActionHeritage` | 只读审计：`src/{heritage,coreaction,funcdata}.rs`、locked oracle；输出 `docs/alignment_audit/HERITAGE_DRIVER_2026-08-13.md` | evidence=`5b9d177`；确认生产路径错误执行 direct place/rename 两遍并夹 DeadCode，绕过 canonical single-pass/guard/ADT/refinement；GetStr 缺 20 call INDIRECT + 23 MULTIEQUAL 已按 2 calls×10 ABI ranges 与 block2/5 的 11+12 phi 精确归因；直接切换 nominal driver 会重入 RwLock 写锁，实施须按文档 DAG 自底向上，模块仍 L2/MISMATCH | 2026-08-13 |
| `HERITAGE-OWNERSHIP-0001` | P0 | DONE | heritage_ownership_wave | `Funcdata::opHeritage` + `Heritage` explicit-borrow ownership boundary | `src/{heritage,funcdata}.rs`, `docs/api/{heritage,funcdata}.md`, `tests/oracle/heritage_ownership_1204.{cc,rs,metadata.json}`, `tools/run_heritage_ownership_oracle.sh`, `tests/oracle/fixture_registry.json`, `docs/TODO_BOARD.md` | evidence=`pending root commit`；Heritage.fd Weak 字段删除，pass 方法显式 `&mut/&Funcdata`；`Funcdata::op_heritage`（funcdata.hh:462 1:1 桥）mem::take→单 pass→回写，零 Weak 升级零嵌套锁；driver 逐行对齐 heritage.cc:2663-2758（无 pass>=2 早退、无内嵌 DeadCode、pass+=1 仅末行、maxdepth=-1 哨兵 cc:223/2868 单次重建、buildADT 根深度1/死桶/z[0]=-1/augment 严格 <、driver 不调 buildInfoList(funcdata.cc:166 归 startProcessing)/不调 guardCalls(cc:2629 在 placeMultiequals 内)、错位的 driver 级 guard_calls_range_with_space 已删）。权威 runner（pinned base=e034f80+2文件overlay）root 复验 exit0：`MATCH covered_projection=3/3 overall=PARTIAL_MATCH stdout_sha256=7df41b64…`（register_free_promote 投影经复核者从 renameRecurse cc:2479-2562 手工推演三方一致；phi 环三连 pass 无死锁）。独立 `Cross-Review: APPROVE`（机制 C，8/8 逐项独立验证）。单测 heritage:: 11/11（+2 新增看门狗测试），全库 6 域外失败零新增（pinned-base 对照）。**未切生产 Action**（ActionHeritage 仍走 direct 双 pass，切换归 HERITAGE-DRIVER-SWITCH，依赖 CALLGUARD+ADT-RENAME）。复核观察项：O1 既有注释行号漂移（clear 引 2869 实际 2855 起/get_store_guard 引 2776 实际 2762）；O2 size==0 早退 maxdepth 差异（无可观测面）；O3 place_multiequals 无条件重建 dominator（ADT-RENAME 邻域）；O4 augment 路径压缩防御分支 | 2026-08-15 |
| `HERITAGE-CALLGUARD-0001` | P0 | DONE| heritage_callguard2_wave | callspec effect/guard → 20 `INDIRECT` | `tests/oracle/heritage_callguard2_1204.{cc,rs,metadata.json}`, `tools/run_heritage_callguard2_oracle.sh`, `examples/getstr_stage_snapshot.rs`（02b 生产观察层）, TODO 行；src 零改动（guard 链已由 OWNERSHIP/DRIVER-SWITCH/MODELBIND 落地，本环为 base=HEAD 验证性钉死） | 依赖全就绪（OWNERSHIP DONE、PROTO-EFFECT-MODEL、CALLSPEC a4dcfdf、SPACE/ADDRESS、OPBANK、MODELBIND fb49998、CSPEC-TEXT-INGEST 7721b0d）。证据：①新 fixture `heritage_callguard2_1204`（base=102c476，oracle=e40ed13 锁定）权威 runner exit0 `MATCH stdout_sha256=da554d46…`——GetStr 形态（2 calls×10 ABI ranges）经**生产入口 ActionHeritage::apply**（coreaction.hh:284-290）与 canonical `opHeritage` 双入口逐对象投影 20 guards 全等（identical=1）：block+position、ind/ic 形态、Iop alias（getOpFromConst 往返）、in0 source（RAX killedbycall→const-zero creation{ic}；8B ranges→seed COPY op1；1B free-read→promoted input；chained call2 in0=op61=call1 guard out）、out flags（{ic}/{ra}）、use set（call1 outs 被 call2 guards 恰好一次 INDIRECT 读取、call2 outs 无读者）、生产二遍惰性（pass2new=0 stable=1）、spacebase 平移（S0x18→S0x8、offset_unknown→tryregister=false）；`guard_def_use` 单 call 逐对象隔离 rename 接线。②既有 `heritage_callguard_1204` runner 在当前 HEAD 复跑仍 MATCH（7/7）。③生产 GetStr 证据（examples/getstr_stage_snapshot.rs 新增 02b 层：真实 curl ELF + 真实 x86-64-gcc cspec arch（set_arch 生产绑定）+ 生产 pre-heritage action 前缀 + 生产 ActionHeritage，即 oracle getstr_pipeline_1204 的 post-heritage 断言同界）：guard 结构与 oracle `02_heritage_ssa` 逐对象一致——block1 pos2-10 / block5 pos3-11 连续组紧贴各自 call（0x36e4/0x370b）之前、IOP alias 正确、顺序 0,30,38,200,202,206,207,20b,288 与 oracle（0,30,38,200,202,206,207,20a,20b,288）相对序一致、RAX=const-zero+ic 创建形态、call2 in0 全为前值 W。**残差（绑定 `ARCH-CONTEXT-TRACKED-0001`）：每 call 缺第 10 个 range 0x20a（DF tracked-context）**——oracle 侧由 ActionConstbase 的 tracked COPY@entry（coreaction.cc:692-704，x86-64.pspec `<tracked_set><set name="DF" val="0"/>`→register:20a:1）制造该 range；Rugra arch.rs 未 ingest context_data（CompilerConfigReport skipped）+ ActionConstbase stub → 生产 18 vs 20；guardCalls 本体零缺陷（fixture 已覆盖含 0x20a 的全部 10 range 形态）。E2E 基线（本环实测）：defects=0/numbering=0/Matched=123，GetStr skeleton diff=24，src 零改动故 delta=0。未 commit（按工单，留 WIP+交接报告）。Cross-Review: APPROVE（复核方六项独立验证：guardCalls 四类清单/newIndirect 形态/lookupEffect 链/生产 18/20 独立复跑（自跑 02b 层逐对象 diff）/0x20a 归因五环节逐一实证（pspec:16-18→coreaction.cc:692-704 COPY→oracle block0 实存→heritage.cc:2704/2619→Rugra skip 链）/gen-1 复跑 MATCH 7/7；注记 N1 unique 空间判别 case 缺、N2 tryregister=false 半边未隔离、N3 assignment-output 早退+wrapOffset 回卷边界未覆盖——均非 metadata 宣称项） | 2026-08-17 |
| `ARCH-CONTEXT-TRACKED-0001` | P1 | DONE| arch_context_tracked_wave | pspec `context_data`/`tracked_set` ingest → Architecture tracked partitions → ActionConstbase tracked COPY | 前半（arch.rs 摄取链 + 权威 fixture）已于 a596110 落地。本轮（后半，WIP 未 commit，禁自行 add/commit）已交付：`src/coreaction.rs`（ActionConstbase::apply cc:678-706 逐行移植替换 stub）、`examples/curl_decompile.rs` + `examples/getstr_stage_snapshot.rs`（worker_architecture pspec `<context_data>` 喂 decode_context_data，acceptance 例的镜像 worker 同步接线）、`docs/api/coreaction.md`（2026-08-17 条目）、`src/coreaction.rs` 新增单侧回归测试、TODO 本行。| 后半完成（本 agent，2026-08-17）：①apply 逐行映射——cc:681 空块早退/cc:684 getBlock(0)/cc:686-690 injectid≥0 腿门控（FuncProto 不存 injection id（set_inject_id=INJECT-0001 no-op），按 flow.rs get_inject_id 同款兼容回退读 -1，doLiveInject 本体=INJECT-0001 BLOCKED）/cc:692 get_tracked_set(Ram, fd.get_address().as_u64())（Address 无空间维→ram 显式传参；to_vec() 快照结束不可变借用，Ghidra 引用指向全局 context 循环不触变异）/cc:697-703 Address::new(ctx.loc.offset)（new_varnode_out 钉 Register 空间，DF reg:20a:1 正确；非 register tracked loc 登记残差）+new_op(1,bb start)+new_varnode_out+new_constant+COPY+set_input(0)+insert_begin（多 ctx 逆序居块头=opInsertBegin 语义）/cc:705 无条件 return 0。②worker 接线=restoreFromSpec 顺序（architecture.cc:639 parseProcessorConfig→:641 parseCompilerConfig）→ 读 sleigh_specs/x86-64.pspec→DocumentStorage 解析→<context_data> 子元素经 TreeDecoder 喂 decode_context_data（ARCH-0001 最小接线，其余 pspec 子元素=残差）。③验收（核心）达成：getstr_stage_snapshot 02b [CALLGUARD] **18→20**（每 call 第 10 个 0x20a range，相对序 0,30,38,200,202,206,207,20a,20b,288 与 oracle 全等）；block0 头部 COPY out=register:0x20a:1 in0=const0（被两 INDIRECT guard 读）=CALLGUARD 复核 golden 形态。④E2E 归因（HEAD 004816a 干净 worktree 基线 vs 本 tree）：skeleton 3155→3151——my_get_line/helpf/file2string.part.0/getparameter.constprop.0 各 -1 行幽灵 `in_register_0000020a` 声明（golden 无此行，方向朝 oracle）；defects=0/numbering=0/Matched=123 不降；gcc audit 16 FAIL（≤16，前值 17）；cargo test --lib 1416 pass + 5 fail=全部预存（comment/dynamic/funcdata×2/ruleaction）+0 新增（constbase 回归测试 1/1 绿）。⑤残差登记：a) 无双侧 constbase fixture——单侧断言 test_action_constbase_inserts_tracked_copy_at_entry_head（机制 B2：手写 expected 不升 MATCH；oracle 侧证据=getstr_pipeline_1204 02b 逐对象投影）；b) `heritage_callguard2_1204` runner heritage_rs_sha256 钉 base 102c476，committed 漂移（heritage.rs+14/funcdata.rs+195）致干净 HEAD 亦 mismatch（本改动前已破，非本 write-set；需 fixture owner 重钉）；c) 非 register 空间 tracked loc（new_varnode_out 钉 Register）；d) INJECT-0001 doLiveInject 腿；e) worker pspec 只接 context_data（ARCH-0001）。root 复核后可 DONE | 2026-08-17 |`HERITAGE-ADT-RENAME-0001` | P0 | REWORK (二轮返工交付，待第三轮复核) | heritage_adt_rename_wave | `buildADT/placeMultiequals/rename` → 23 `MULTIEQUAL` | `src/heritage.rs`, `docs/api/heritage.md`, `tests/oracle/heritage_adt_rename_1204.{cc,rs,metadata.json}`, `tools/run_heritage_adt_rename_oracle.sh`, TODO 行 | evidence=`pending root commit（二轮复核返工后重交，待第三轮）`；首轮 REJECT（M1-M4）已修并经二轮确认；二轮 REJECT 项已修：M5(a) warning 改 printRaw 原形（`0x`+2*addrsize 零填充+高位缩短、无空间名，space.cc:206-221）；M5(b) removeRevisitedMarkers 重插改元素锚（opUninsert 前于移除前列表解析：dead-target→INDIRECT 自身后继 cc:268-269 / alive target→target 后继 cc:270-272 / ME→组后首个非 ME cc:275-280，uninsert 后 op_insert_before、尾锚追加）——原数值索引在移除后列表平移一位且块尾越界 panic（生产可达）。canonical place_multiequals（cc:2599-2645）逐段消费 disjoint TaskList；rename/rename_recurse（cc:2479-2593）block-0 根+单趟 op 序+cc:2538 oldmark；refine_read 非自由路径 panic 保留原文（M4，标签 UNTESTED）。fixture 判别力升级：常量投影含值（C4:0 vs C4:2）、block-order 逐 op 含输入描述符（组内置换可见）；5 case 双侧逐字节=diamond 3 phi（块首倒序+reverse pred slot）/oldmark（ownership 受限案例升格全投影）/two-join `seq=6,3`（pq 序≠块索引序）/refine_order（3-PIECE 链+2-SUBPIECE 组创建序，C4:0→C4:2 组内序断言）/revisit_positions（[a2,S,f2]/[a3,S]/[m2,S,x] 三重插形态，数值法块尾必 panic）。authoritative runner（pinned base=194901d+src/heritage.rs overlay）exit0：`MATCH covered_projection=5/5 overall=PARTIAL_MATCH stdout_sha256=563749be…`。验收：E2E 124/124 processed/76 decompiled/0 fail；20×All sha 一致（隔离 target 二进制 03d97945…；共享 target 曾被并发 agent 中途重建致一次假 drift，已隔离复验）；20×compare-main 全 MATCH；lib 1352 pass+5 域外预存（pinned-base+overlay worktree A/B）；heritage:: 12/12；annotations/refs strict/Evidence 4/4 过。残差：guardInput concat 路径 UNTESTED、removeRevisited warning 文本源对齐未投影（deadremoved>0 未驱动）、collect 全 bank 偏移窗口（跨空间碰撞→DRIVER-SWITCH 硬前置）、heritage_ownership/callguard fixture pin 待 root 重钉、fixture_registry 登记归 root  | | 2026-08-16（二轮返工） | |
| `HERITAGE-DRIVER-SWITCH-0001` | P0 | REWORK-DONE r2 (复核 M1/M2 已修，待窄面复审) | heritage_driver_switch_wave | `ActionHeritage -> Funcdata::opHeritage -> Heritage::heritage` 单pass生产路径 | `src/heritage.rs`, `src/coreaction.rs`, `src/funcdata.rs`, `docs/api/{heritage,coreaction,funcdata}.md`, `tests/oracle/heritage_driver_switch_1204.{cc,rs,metadata.json}`, `tools/run_heritage_driver_switch_oracle.sh`, TODO 行 | evidence=`pending root commit（write-set 就绪，本 agent 禁 add/commit）`。实施五件：①coreaction.rs apply 逐字对齐 coreaction.hh:289 `{fd.op_heritage();Ok(0)}`，删 pass>=2 guard/global_struct_ptrs 预戳/direct 双 pass/内嵌 DeadCode/discover 夹层；②**LocationMap 空间键硬前置已修**：themap 键 (AddressSpace,Address)（对齐 map<Address,SizePass> + Address::overlap 跨空间 -1），add/find_pass/entry_containing 子区间化，新增 `test_location_map_cross_space_keys_are_disjoint` 看门狗；③**normalize_read_size 修复**（ping-pong 根因：原直接 `newop.output=` 绕过 op_set_output，被归一 vn def 永不置位→每 pass 重归一→main 800+ 迭代/WARN 21630；现 op_set_output+cc:398 set_write_mask）；④collect 改 beginLoc/endLoc 语义窗口——**复核 M1 修正后为探针驱动活迭代器**：合成探针 varnode（size 0 同 offset 排最前）构造 `loc_tree.range(probe..)`，O(log V+hits) 只走本空间窗口，兼修跨空间同 offset 误收；早先入口冻结快照实现被复核 REJECT（refinement cc:1902-1906 pieces 对本 pass 全部 collect 不可见→下一 pass 范围已 OLD、INDIRECT 永不补建）；⑤direct 族移出生产（无调用者 insert_multiequal 删除；FLAGFREE 发现的 insert_multiequal_direct phi 尺寸 .unwrap_or(4) 回退无生产可达路径）；run_heritage_direct 注释改 RUGRA-GLUE（原误引 funcdata.cc:34=setSelfRef）。**验证**：权威 runner（pinned base=2a0010a+3 文件 overlay，immutable-fd）exit0：`MATCH covered_projection=5/5 overall=PARTIAL_MATCH stdout_sha256=325f2286…`——单 pass 写读链接/3×重入幂等（op 集合稳定 pass 0→3）/跨空间 hp_reg=0+hp_stack=1 各自链接/prev==2 老范围晚期 free 吸收零新增 op/**refinement 活可见性（case E：部分宽写触发 cc:2610-2616 细分，第二 piece 经活 re-collect 获独立 input 提升 R54:4:I、PIECE 重组全宽读 U8:W+PIECE；/tmp 冻结快照反证：该实现下 case E free_with_reader=0 断言失败无输出=判别力实证）**，双侧逐字节一致。E2E（r2 复跑，master=a4dcfdf+03ec065 后含 CALLSIGN 落地）：124/124 processed，74 decompiled/1 timeout/**1 panic=HELPF-NONFREE-NORMALIZE-0001（复核 M2，独立于 M1——r2 修复后仍复现，非快照症状）**；`multiple descendants` WARN 351=HEAD 基线（FLAGFREE 审计之 44 于 HEAD 不可复现——基线与切换态均 351，如实登记，44 归因实验出自非 HEAD 插桩树）；not settling 5=基线（type-propagation 族，FUNC-TYPEPROP-SETTLE-0001 域）；**差分 defects=0 硬底线过（r2 主树复跑 defects=0，numbering=20 系并发在途改动窗口、pinned overlay 隔离态 numbering=0；总 skeleton 4400→2048 含 8 超时函数缺席不可直比，11 个文本变化函数 +1..+277 归因 INDIRECT 结构膨胀）；确定性：隔离二进制 3×+3×+5× sha 一致（436606f8 主树窗口 / 8bfe730d 冻结副本；共享 target 被并发 agent 重建曾致假 drift，隔离复验为准）。测试：heritage:: 12/12（+2 新看门狗）、coreaction:: 44/44、funcdata 41+2 败=base 预存、全库 5 败=base 同集零新增（comment/dynamic/funcdata×2/ruleaction）；annotations --all/refs --strict/Evidence 4/4 ✅。**残差（绑定后继）**：①8 函数超 10s worker（decompiled 76→68，TIMEOUT-8FN-0001 已审计）——生产 callspec 无 model→has_effect 恒 UnknownEffect（fspec.rs 保守分支）→canonical guardCalls 每 call×range 建 INDIRECT（main pass0=13,462 INDIRECT+4,098 phi，ops 674→22,186）+ varnode bank O(descend) Weak-upgrade has_no_descend/eraseDescend×共享 free varnode（pass0 rename 30s），绑定 FUNCPROTO-MODEL-BIND-0001/CSPEC-TEXT-INGEST-0001+varnode-bank 性能 TODO；②skeleton +1..+277 同因，defects=0 不变；③新 WARN 族 `overlapping input varnodes`=124（Ghidra 此处 throw LowlevelError、Rugra 保守继续=既有分歧现被 canonical 生产路径暴露，guardInput 合并覆盖缺口）；④AFTER dead removal×12=canonical 检测器正常触发（bumpDeadcodeDelay 未 setRestartPending——Ghidra 以 ActionRestartGroup 重启破环，Rugra 收敛靠 ranges 归零，机制差异如实）；⑤**helpf panic 已登记 `HELPF-NONFREE-NORMALIZE-0001`**；⑥fixture_registry 登记归 root。模块保持 L2 | 2026-08-16（r2：复核 M1/M2 修正完成） |
| `HELPF-NONFREE-NORMALIZE-0001` | P1 | DONE| funcdata_wave | canonical guard→normalize_read_size 对非自由 vn 的 opSetOutput fail-fast panic | `src/subflow.rs`（根因域）, `src/funcdata.rs`（inject_raw_ops 输入去重）, `docs/api/funcdata.md`, 本 TODO 行 | evidence=WIP。根因定位+修复（2026-08-17，HEAD=9ea2a78 worktree）：**根因=`SubvariableFlow` 两处 raw `set_flags(INPUT)`**（subflow.rs 旧 `replace_input`/`get_replace_varnode`，自述"setInputVarnode is not ported"——实为当时未移植，现基础设施已在）——Ghidra `getReplaceVarnode` cc:1343/`replaceInput` cc:1262 走 `fd->setInputVarnode`→`vbank.setInput`→`xref` 置 INSERT；raw 写产生 oracle 不可能的 INPUT-without-INSERT varnode（Ghidra: input⇒xref⇒insert，varnode.cc:1358-1374），被 `Heritage::collect` 按 read 分支收下（该分支只查 !written&&!heritage_known，与 oracle 分支序一致），`normalizeReadSize`→`opSetOutput`→`setDef !is_free` fail-fast——即登记的 0x39a5 panic（vn=Register:0:1 flags=0x1000008，reader=INT_EQUAL@0x39a5，watchpoint 背栈直指 `get_replace_varnode`）。**修法（自底向上）**：两处改 `fd.set_input_varnode(...)`（canonical transition_input→xref）+ `replace_input` 补 cc:1264 `fd.delete_varnode(&oldvn)`（totalReplace 已断全部读者）。**连带第二缺陷**：`Funcdata::inject_raw_ops`（funcdata.rs batch lift GLUE）对非常量输入 `find_or_create_input_space` 去重共享 free varnode→多读者 free（Ghidra `Varnode::addDescend` varnode.cc:331-340 对此 throw）→下游 destroy_varnode slot-miss+`Refining non-free varnode` panic 家族——改为 `PcodeEmitFd::dump`（funcdata.cc:878-908）的 per-input-reference `newVarnode` 语义（与 `inject_raw_ops_single` 一致，读-写链接归 Heritage）。**验收**：E2E curl 124 函数 **74 decompiled/1 panic → 75 decompiled/0 panic**（helpf 0x3980 恢复出体），defects=0/numbering=0/skeleton 4403=基线，gcc 19 FAIL=基线；cargo test --lib 1367/5=已知域零新增；annotations+refs 绿。main churn 复验：main timeout 计数与基线一致（1 timeout），无新搅拌。残差：`Varnode::add_descend` 的 WARN 软化（Ghidra throw "Free varnode has multiple descendants"）保留为登记残差——两生产者已除，恢复 throw 属独立小步（VARNODE-ADDDESCEND-THROW-0001 建议） | 2026-08-17 |
| `PRINTC-CAST-EXPR-0001` | P1 | DONE| printc_cast_expr_wave | printc 表达式层畸形 cast/赋值发射（`= (uVar28;` / `))))` 连串 / `* = uVar20)))) = 0x3489;`，1168 处） | `src/printc.rs`, `docs/api/printc.md`, 本 TODO 行 | evidence=`this commit`（root 集成树复测：`= (` 1262→0、`))))` 0、skeleton −1424、gcc FAIL 20→18（单件隔离口径）；四缺陷全发射层：notPrinted 过滤缺失/RPN 栈跨语句泄漏/ZEXT-SEXT-SUBPIECE 缺臂/recurse no-op 丢操作数）（禁止本 agent 自行 commit）。**根因=纯发射层（非上游 IR）**，四个叠加缺陷：①`emit_block_basic_rpn` 缺 Ghidra `notPrinted()` 过滤（printc.cc:2696 + op.hh:182 marker\|nonprinting\|noreturn）→ MULTIEQUAL/INDIRECT（TypeOp ctor 带 marker，typeop.cc:1947/1988）作为独立语句进入 `emit_statement_rpn`，而 `opMultiequal` 是空实现（printc.hh:331）→ dispatch 后 revpol 残留 assignment(visited=1<stage=2) 不完整条目，泄漏到下一语句在错误位置吐 ` = `/`(`/`)`——即全部 `= (` 行与 `))))` 连串；②`dispatch_op_rpn` 对 INT_ZEXT/INT_SEXT/SUBPIECE 落 `_ => {}` no-op（Ghidra 为 printc.cc:786/799/843 的 cast-or-opFunc 双臂），同栈泄漏类；③`rpn_push_op`/`rpn_push_atom` wrapper 把 printlanguage.cc:132-133/165-166 的 `recurse()` 委托给 printlanguage.rs 无 arena 的 no-op 自由函数——mid-expression push 时 pending 输入被静默丢弃；④`hidden` token 表字段错（printc.cc:23 = stage 1/prec 70；便捷 ctor 写死 stage 2/prec 0，又留 incomplete 条目）。**修法**：①notPrinted 过滤（is_marker + NONPRINTING + NORETURN，flags 由 opcode_flags 初始化已核实）；②ZEXT/SEXT/SUBPIECE 三臂按 oracle（is_zext_cast/is_sext_cast/is_subpiece_cast → rpn_op_type_cast；else rpn_op_func，getOperatorName="ZEXT/SEXT/SUB"+insize+outsize，typeop.cc:1122/1148/2127）+ 新增 function_call(postsurround,66,bump10)/comma(binary,2,assoc) token（printc.cc:28/55）+ readOp 线穿 dispatch（emitExpression 传 None=printc.cc:2493 字面 0；recurse 传读 op=printlanguage.cc:532；isExtensionCastImplied readOp==null→false 与 cast.cc:257 一致）；③wrapper 先走真 `self.rpn_recurse()` 再进自由函数（单一 `if (pending < nodepend.size())` 语义不变）；④hidden token 表内直构聚合 `{ "", "", 1, 70, … }`。CAST 臂重构为 `rpn_op_type_cast` 共享。**验收（干净 worktree=HEAD a301036+仅本 printc.rs overlay，避开主树并发 varnode.rs WIP 的 20-panic 窗口）**：`= (` 计数 **1262→6 且 6 处全为合法 C**（3×`uVar_10000419 = (undefined4)in_R12;` cast 赋值 + 3×`== (bool)` 比较，真畸形 0）；`))))` 连串 0；my_fwrite/GetStr/myprogress 复检干净（`*uVar20 = 0x3489;`/`fwrite(uVar28, uVara0, nmemb, stream);`）；ZEXT/SEXT 以 oracle 的 opFunc 形态发射（`ZEXT48(x)`/`SEXT18(bVar1)`）；差分 skeleton **4724→3300（−1424）**、defects 0→0、numbering 0→0；gcc 审计 **103 OK/20 FAIL → 105 OK/18 FAIL**（GetStr+hugehelp 修复=本缺陷；其余 18 FAIL 逐函数 gcc 复跑证实与基线同错同位——`uVara0 undeclared`=varmap 声明域、`URLGlob` typedef 缺失=类型域、`_start` 括号/my_get_token `!` 形=既有残差；audit 15 行打印截断曾掩盖 glob_url/glob_range 基线即在）；124/124 75 decompiled/0 panic/1 timeout=基线；cargo test --lib 1373/5 失败集逐名一致（comment/dynamic/funcdata×2/ruleaction，HEAD 同集）。annotations --all ✅ / refs --all --strict ✅。残差：①二进制算术仍 `emit.tag_op(" + ")` 直发不经 RPN token（嵌套优先级括号缺失，3 处 `== … + 0 - … < 0` 形）——binary token 化建议 PRINTC-BINARY-RPN-0001；②`uVara0` 类 use-registered 但声明缺失=varmap 域既有；③SUBPIECE doesSpecialPrinting 字段抽取分支（printc.cc:846-871）依赖 piece-structured 类型+SPECIAL flag，Rugra 均无，不可达分支如实省略 | 2026-08-17 |
| `PRINTC-SUBPIECE-FIELDEXTRACT-0001` | P1 | REWORK-DONE r1 (审计三 MISMATCH 已修 + fixture 26/26 MATCH，待审计方快速重验) | printc_audit_fix_wave | SUBPIECE `doesSpecialPrinting` 三缺口实现 + 审计返工（WIP 未提交）：(c) `is_subpiece_cast`（type_system/cast.rs:102）inmeta 补 PartialStruct/PartialUnion 臂（cast.cc:416-418）+ Enum/**PartialEnum** 输入/输出/PTR→int 特例映射（TypeEnum 构造器 type.hh:489-494 归一化；REWORK #2——TypePartialEnum type.cc:2255-2262 经同一构造器落 TYPE_UINT，真 oracle 实测 partialenum in/out 均=1）；(b) `is_piece_structured`（type_system/datatype.rs:462）= {Struct,Union,Array,PartialStruct,PartialUnion}（oracle 实测 enum/partialenum=0 证实归一化）；(a) RPN `opSubpiece` 两臂（printc.rs:1496）：`compute_byte_offset_for_composite`（typeop.cc:2195 含 BE 臂）+ `rpn_push_partial_symbol`（printc.cc:1947-2065：struct find_truncation 下降/**REWORK #1 findResolve 查 union_resolutions 快照（doc_function 从 fd.union_map 克隆），无缓存回退 field[0].type，仅 ==ct 才 break（TypeStruct::findResolve override type.cc:1944-1951）**/array 元素下降/**REWORK #3 allowCast 实参=SUBPIECE 输出 vn（printc.cc:859/2019/2022）**/合成 `_off_sz_`）+ findTruncation/object_member 字段 atom 臂；token 表增 subscript（printc.cc:27）；公开 `op_subpiece_rpn`；legacy `op_subpiece` 同步并补 loop-top TYPE_PTR 豁免（printc.cc:1962）+ unnamedField 格式修正。 | `src/printc.rs`、`src/type_system/datatype.rs`、`src/type_system/cast.rs`、`docs/api/{printc,type_system/cast,type_system/datatype}.md`、`tests/oracle/printc_subpiece_fieldextract_1204.{cc,rs,metadata.json}`、`tools/run_printc_subpiece_fieldextract_oracle.sh`、`docs/TODO_BOARD.md`（本行 + TYPEFACTORY-NEEDSRES-SINGLEFIELD-0001 行） | 2026-08-17 返工 session：`bash tools/run_printc_subpiece_fieldextract_oracle.sh` → records=**26** MATCH（原 22 + armA.nested=N.in.x / cast.int_partialenum_0=1 / cast.partialenum_out_0=1 / armA.allowcast=(uint2)C.lo，cast sweep 8→10；C++ 侧含真 setFields 置位的单字段 needsResolution inner + 基本块插入使 findResolve 父链可达）；E2E curl 输出与改前逐字节不变（diff 0 行），门禁 defects=0/numbering=0；域内测试 printc 9 + type_system 92 全绿（cast 含 partialenum 单测）；残余：union 缓存命中 break 臂 UNTESTED（读侧已接线、写侧管线无该路径条目）、suboff>0、BE byteOff UNTESTED，pipeline needsResolution 产出 MISSING→TYPEFACTORY-NEEDSRES-SINGLEFIELD-0001。 | 2026-08-17 |
| `TYPEFACTORY-NEEDSRES-SINGLEFIELD-0001` | P1 | DONE| printc_audit_fix_wave (PRINTC-SUBPIECE-FIELDEXTRACT-0001 审计返工衍生) | 铁律 1.5 基础设施缺口已闭合：needs_resolution 完整置位矩阵落地 `src/type_system/typefactory.rs`（+276 行 WIP）。六处置位镜像：①`set_fields` 单字段臂（type.cc:1569-1571，OR 不清除，比较字段类型 `getSize` 与 struct size）；②新增 `set_fields_sized`（type.cc:3479 显式 newSize/newAlign 形态——唯一能表达"单字段不填满=0"与"offset 不参与比较（单字段@4 且 type size==newSize → 1）"两格的 API）；③`decode_struct` 尾臂（type.cc:1875-1877，对 decode 的 size 属性比较；与并发协作者合入的 decodeFields 校验循环共存）；④`TypePointer::calcSubmeta` 继承臂（type.cc:1051-1052）落 `get_ptr`/`get_type_pointer`/`get_type_pointer_rel`/`resize_pointer`/decode 指针臂。**同 session 并发协作者事件**：decode_struct 被并行合入 Ghidra decodeFields 校验循环（乱序 throw/overlap 丢弃/fit throw/void throw/incomplete 残留——与本文档同文件，未回退，fixture 27 记录含其全部 cell 且 oracle 实测含 size8 空字段 incomplete=1 证实其尾块正确）；.cc/.rs fixture 亦被其扩展（本 agent 补齐 char 碰撞修复——inline `char` 与工厂核心 char id 不同触发 "Trying to alter definition of type: char"，改匿名 1 字节基——并去重 .rs helper、统一 27 记录、锁哈希）（指针继承 flag 但绝不穿过第二层指针；消费侧全部 TYPE_PTR 豁免故无解析行为变化，豁免变 load-bearing 与 Ghidra 一致）；⑤`get_array` 内联 TypeArray ctor size-1 臂（oracle 实测 factory 路径=1）；⑥decode 数组臂 arraysize==1（type.cc:1341-1342）+**真 bug 修复**：数组 decode 分支补 `rewind_attributes()`（type.cc:1329）——此前 decode_basic 属性循环耗尽属性致 arraysize 永远读不到、一切 XML 数组 decode 报 "Bad size for array of type"。scope 外残差（已注释）：SUB_PTR/SUB_PTR_STRUCT 重分类、ctor `flags=ptrto->getInheritable()`（coretype 继承）未建模。 | `src/type_system/typefactory.rs`、`docs/api/type_system/typefactory.md`、`tests/oracle/typefactory_needsres_1204.{cc,rs,metadata.json}`、`tools/run_typefactory_needsres_oracle.sh` | 验收证据（2026-08-18 实测）：`bash tools/run_typefactory_needsres_oracle.sh` → **records=27 MATCH**（矩阵：set 单字段填满=1/不填满=0/offset@4=1/多字段=0；grammar 派生 size 单=1/多=0/**overfire 未取整 size5 字段=0（派生 newSize=calcAlignSize(8,4)=8≠5，逼 Rust grammar cell 走 set_fields_sized(derived)——grammar.cc:2798 逐字形态）**/嵌套 inner:1,outer:1；dec 填满=1/不填满=0/多=0/数组 size1=1/**overlap 丢弃留单字段=1/keepfirst=1/空字段 incomplete=1（size8 与旧式 size0 两种）**/**5 条 dec.err LowlevelError 文本逐字节**；arr.factory=1；ptr.inner=1/ptrptr=0/plain=0/**ordering stub:0,cached:0,new:1（继承仅构造期）**；union.setfields=1；oracle=e40ed130 锁定 commit，指纹全录 metadata）；E2E curl 与改前基线**逐字节不变**（md5 f846dcc…，diff 0 行；门禁 defects=0/numbering=0 双 golden——归因：指针消费侧全 TYPE_PTR 豁免 + curl 语料无单字段 struct 触达 SUBPIECE walk，flag 路径激活无输出面变化）；`cargo test --lib` 1427 pass/5 fail（5 个均为他域租约 WIP 预存：comment/dynamic/funcdata×2/ruleaction），type_system 93/0。后续（root 域）：printc_subpiece fixture Rust 侧手动 flag 可回收（root 已示不急）；set_fields_sized 尚无主管线调用方（setStructDecl 移植时接线） | 2026-08-18（27 记录终版） |
| `TYPEFACTORY-SUBMETA-RECLASS-0001` | P3 | REGISTERED | unassigned | calcSubmeta 的 SUB_PTR/SUB_PTR_STRUCT 重分类与 TypePointer ctor getInheritable 继承未建模 | `src/type_system/typefactory.rs`, fixture | NEEDSRES 交付残差：无 sub_metatype 字段故未建模；与 grammar setStructDecl 移植一并接线（set_fields_sized 同批） | 2026-08-17 |
| `GRAMMAR-SETFIELDS-SIZED-0001` | P3 | READY | unassigned | grammar.rs:1617 仍调自派生 set_fields——未取整字段与 Ghidra 派生 newSize=calcAlignSize 分叉 | `src/grammar.rs`, fixture | NEEDSRES overfire cell 证实分叉面；1 行改 set_fields_sized(derived)（grammar.cc:2798 派生语义） | 2026-08-17 |
| `TYPEFACTORY-ARRAYSIZE-ALIGN-0001` | P3 | READY | unassigned | get_array size 用 get_size()*n，Ghidra ctor 用 n*getAlignSize()（type.hh:936） | `src/type_system/typefactory.rs`, fixture | NEEDSRES 复核补遗 #4：未取整元素（size3/align4）分叉；与 ctor 同行的既有代码 | 2026-08-17 |
| `TYPEOP-PROPAGATE-BYPASS-0001` | P3 | REGISTERED | unassigned | typeop.rs propagate_to_pointer 绕工厂建指针（Ghidra typeop.cc:197 走 getTypePointer→calcSubmeta） | `src/typeop.rs`, fixture | NEEDSRES 复核补遗 #5：现 pointee 受限（unknown/base）臂为 no-op；pointee 扩展前接线 | 2026-08-17 |
| `CALLSPEC-DRIVER-0001` | P0 | REVIEW | callspec_driver_wave | driver 侧 CALL/CALLIND callspec 目标→导入符号名/签名接线（queryCall 边界） | `examples/curl_decompile.rs`, `src/fspec.rs`, `src/debugproto.rs`, `docs/api/{fspec,debugproto}.md`, 本 TODO 行 | evidence=`pending root commit`（禁止本 agent 自行 commit）；oracle: flow.cc:656-723 queryCall/setupCallSpecs/setupCallindSpecs + fspec.cc:4924-4960 ctor/setFuncdata + fspec.cc:3830 setPieces + coreaction.cc:2311-2337 ActionDefaultParams + printc.cc:589-612 opCall `fc->getName()` + golden 0x102320 `int puts(char *__s)` 锁定签名 thunk。实现三件：①`LibcSignatureTable`（debugproto.rs，24 条 glibc ABI 单一事实源，stub 渲染同源委托）+`locked_proto()`（SYSV 存储 assign + input/output lock + model 不锁=golden "Unknown calling convention" 组合）；②`FuncCallSpecs::set_funcdata(name, entry)`（fspec.rs，无 callee Funcdata 的可观测 (name,entry) 对，替换无调用者的 `Option<&Funcdata>` 形）；③driver `decompile_request`：lift 后逐 spec `entry→fd.symbol_table` 命名（queryCall 等价）、导入签名装 locked proto（ActionDefaultParams copy 等价）、CALL in(0) fspec varnode offset 由 spec 索引改键 entry 地址（printc 既有 symbol_table 消费=fc->getName() 可观测等价；ActionCallParams 的 inrefs[0]→param-count 消费同步解锁）、PLT-stub 目标函数装同表签名（golden `int puts(char *__s)`）、`.plt.got` GLOB_DAT thunk 槽解析（0x22e0→__cxa_finalize，`.rela.dyn`+slot 反汇编 disp32）。**干净 A/B（同一 immutable v1 二进制，RUGRA_DISABLE_CALLSPEC_LINK 双跑）**：FUN_ 64→6、具名 libc 调用 46→55、skeleton 2043→1988、defects 0→0、numbering 25→23、124/124 处理数 68/8/0 完全一致；最终树（含并发 printc/prettyprint 修复+.plt.got）：FUN_ 5、skeleton 2010、defects 0、numbering 7、gcc 98/18、result/curl_cur.c sha=`0943a422…`。GetStr 侧 `FUN_0))(lVar1)`→`free))(lVar3)`（名字+void-锁 output 生效；表达式层 `(a;=(b;=free))` 残差属 PRINT-RESIDUAL-0001 上游分叉）。`cargo test --lib -- fspec:: debugproto`=21/21、annotations --all/refs --strict ✅。残差（非 callspec 域）：剩 4 处 FUN_=FUN_00102020（golden 同名 parity）/FUN_16fe0（golden 为 GOT 指针间接调用 PTR___libc_start_main，symbol-space 域）/FUN_23e00×2（golden 为 init_array 具名间接调用，printc 数据符号域）；`exit` no-return 流效果（golden halt+"does not return"）未随签名表携带（checkForFlowModification/flow.rs 租约域）；numbering 7 处 uVar 重复声明=printc 双声明源+prettyprint backfill（PRINTC-SYMBOL-DECL-0001 邻域）。模块保持 L2（driver 边界无独立 oracle fixture，NO_ORACLE per 机制 B2） | 2026-08-16（交付待 root 复核） |
| `PIPE-TREE-0001` | P0 | DONE | pipeline_tree_audit | 12.0.4 universal/default action tree 与 Rugra 实际 perform 顺序 | 只读审计：`src/{action,coreaction}.rs`、locked oracle；输出 `docs/alignment_audit/PIPELINE_TREE_2026-08-13.md` | evidence=`this commit`；报告 SHA=`336bc223…`，锁定 action/coreaction snapshot=`e1b191fe…/79364848…`；完整记录 raw/effective/Rugra ordered tree、154/157 Rule 序、27项 Heritage前死/提前分类、executor PARTIAL_MATCH、修复DAG与可执行tree/trace schema；独立 reviewer APPROVE。总体结论仍 REJECT/MISMATCH，后续分解到 `PIPE-ACTION-COUNT-0001`/`PIPE-POOL-0001`/`PIPE-RESTART-0001`/`PIPE-BREAK-0001` 等 | 2026-08-13 |
| `PARAM-BIND-0001` | P0 | AUDITED | param_bind_audit | locked `FuncProto` input storage → SSA Varnode → Symbol/PrintC 参数名 | 只读审计：`src/{debugproto,fspec,funcdata,coreaction,varmap,printc}.rs`、locked oracle；输出 `docs/alignment_audit/PARAM_BIND_2026-08-13.md` | evidence=`73e5c79`；首差异在 Funcdata 构造缺 Scope+ProtoStoreSymbol，参数导入再丢 AddrSpace/category-0 Symbol，ActionPrototypeTypes 无 locked-input loop，Heritage 绕过 property/effect；RBP/RBX 因缺 UNAFFECTED 被 Merge input-register guard 错命名 `param_35/param_33`；按文档层级 fixtures 自底向上修，禁止 printer 文本替换 | 2026-08-13 |
| `PROTO-EFFECT-MODEL-0001` | P0 | PARTIAL_MATCH | param_scope_next_wave | `FuncProto::{setModel,hasEffect}` resolved `ProtoModelFull` identity/fallback | `src/fspec.rs`, `docs/api/fspec.md`, `tests/oracle/funcproto_effect_model_1204.{cc,rs,metadata.json}`, `tools/run_funcproto_effect_model_oracle.sh` | evidence=`5599050`；locked immutable runner exit0且独立`Cross-Review: APPROVE`：FuncProto持同一resolved model引用，空local effect委托model，非空local完整覆盖；copy/model identity、range/space/effect优先级与setModel状态投影MATCH。malformed-unsorted输入与null-model前置条件仍UNTESTED；loader/Funcdata model attachment不在本原子，不能冒充已修GetStr | 2026-08-14 |
| `SCOPE-CAT0-0001` | P0 | PARTIAL_MATCH | scope_cat0_wave | `Scope::{getCategorySize,getCategorySymbol,setCategory}` category-0 storage | `src/database.rs`, `docs/api/database.md`, `tests/oracle/scope_category_1204.{cc,rs,metadata.json}`, `tools/run_scope_category_oracle.sh` | evidence=`1a32f59`；locked direct runner MATCH且独立`Cross-Review: APPROVE`：category-0 slot/holes、cat>0 append、NO_CATEGORY、replace/delete/order/Arc identity均闭合。非法enum、corrupt prestate、并发/categorySanity/clearCategory仍UNTESTED；仅解锁ProtoStoreSymbol/ScopeLocal后继，database整体保持L2 | 2026-08-14 |
| `CSPEC-PARAMMODEL-0001` | P0 | MISMATCH | cspec_parammodel_wave | `ParamListStandard` `<input>/<pentry>` decode、参数RangeList与Architecture默认`ProtoModelFull` | `src/{fspec,arch}.rs`, `docs/api/{fspec,arch}.md`, `tests/oracle/cspec_param_model_1204.{cc,rs,metadata.json}`, `tools/run_cspec_param_model_oracle.sh`, `docs/TODO_BOARD.md` | evidence=`this commit`；locked full-cspec fixture的structured-DOM目标投影逐字节MATCH（stdout SHA=`a7d29c1e…`），map/default/decode返回及旧新external handle保持同一model identity，独立`Cross-Review: APPROVE`。生产文本ingestion、ModelRule、alias/error/unknown-name adapter仍MISMATCH，resolveprototype/join/default-return/异常部分状态/reverse-BE/register strategy仍UNTESTED；整体不升L3 | 2026-08-14 |
| `CSPEC-TEXT-INGEST-0001` | P0 | READY | unassigned（W-2026-08-20-CLOSEOUT 后恢复） | production `.cspec` bytes → DOM/TreeDecoder → `Architecture::parseCompilerConfig` | `src/arch.rs`, `docs/api/arch.md`, `tests/oracle/cspec_text_ingest_1204.{cc,rs,metadata.json}`, `tools/run_cspec_text_ingest_oracle.sh`, `tests/oracle/fixture_registry.json`, `docs/TODO_BOARD.md` | evidence=`this commit（write-set 就绪，本 agent 禁 add/commit）`。`Architecture::parse_compiler_config`(cc:1239-1351) 落地：真实 x86-64-gcc.cspec 字节→marshal DocumentStorage::parse_document 文本 ingest→逐 child decode→published 状态。前置 MARSHAL-XML-TEXT/TRANSLATE-DOCSTORE/SPACE/RANGEPROPS/PARAMMODEL 全就位；GLOBAL-APPLY/DEFAULT-RETURN/CALLFIXUP/UNIVERSAL-CHILD 四切片 IN_PROGRESS（见各自行）。复核返工后权威 runner（F1 参数名错误传播/F2 空 varnode null 哨兵/F3 marshal hex 整数 = 新增 5 探针记录）：72 行投影逐字节一致 stdout_sha256=a1391a1d…（space 表/global 三元组/RET stack:0:8/STACK/MODELRET×5/THISCALL/16 FIXUP+TPL/3 callother 探针/2 volatile 探针）。**未全量通过，不发布**：data_organization+enum(MISMATCH, CSPEC-TYPEORG-STATE-0001)、spacebase/deadcodedelay/inferptrbounds(space-manager 接线)、readonly(DB property)、context_data(context spec)、resolveprototype(PARAMMODEL)、setupSizes/PreferSplitManager 全局安装均登记残差（parse_compiler_config 返回 CompilerConfigReport 不静默 skip）；`<body>` ATTRIB_CONTENT 经配对 DOM handle（MARSHAL 域 TreeDecoder 缺口）。完整成功前不原子发布 Architecture。模块保持 L2 | 2026-08-16 |
| `CSPEC-JUMPDEST-INSTSYM-0001` | P2 | DONE| jumpdest_instsym_wave | PcodeSnippet JUMPSYM 语言符号（pcodeparse.cc:3215-3265 lex + slghsymbol.cc:1090/1155/1220/1276/1308 getVarnode + slgh_compile.cc:1986-1991 predefinedSymbols）↔ `SleightSymbolKind::JumpTarget(JumpTargetKind)`/`ConstTpl::{JStart,JNext,JNext2,JFlowRef,JFlowDest}`/`PredefinedJumpSymbols` | `src/pcodeparse.rs`, `docs/api/pcodeparse.md`, `tests/oracle/jumpdest_instsym_1204.{cc,rs,metadata.json}`, `tools/run_jumpdest_instsym_oracle.sh`, `docs/TODO_BOARD.md`（本行） | evidence=`pending root commit（write-set 就绪，本 agent 禁 add/commit）`。CSPEC-TEXT-INGEST 复核登记的 UNTESTED 缺口：snippet 编译器 JUMPSYM 表面（callfixup/jumpassist pcode 语义用 inst_start/inst_next/inst_dest/inst_ref 引用指令地址）。修复三件：①`specific_symbol_varnode`（varnode/lhsvarnode 规则）JumpTarget 分支由错误形态 `(j_curspace,real 0,j_curspace_size)` 改为逐字 `(spaceid const, j_xxx, sz_zero)`（pcodeparse.y:202）；②`parse_jumpdest` JumpSym 分支 offset 由硬编码 `Real(0)` 改为重包装符号动态 offset（pcodeparse.y:195 逐字 `sym->getOffset()`）；③Host 侧 `PredefinedJumpSymbols<L>` 组合器——inst_start/inst_next/inst_next2 是每语言必然序列化符号（predefinedSymbols + purge default 臂放行），Rugra 无 SLEIGH 引擎故经语言 lookup hook 分层注入（inner 优先）。fixture：裸 SLEIGH 引擎（x86-64.sla，即 parseInject 的 `const SleighBase*` 句柄，inject_sleigh.cc:387-416 同构生命周期）对拍 10 个 JUMPSYM snippet + 6 个 findSymbol 观察。**权威 runner exit 0：records=18 bytes=2444 stdout_sha256=a74cdae2…，jumpsym_symbol_status=MATCH overall=MATCH**（SYM：inst_start=start_symbol(9)/inst_next=end_symbol(10)/inst_next2=next2_symbol(11)、inst_dest/inst_ref/epsilon 语言表 ABSENT——前二者 snippet 本地 pcodeparse.y:693-694、EpsilonSymbol 被 purge 删除；全部 jumpdest/varnode 模板 XML 逐字节一致；两条 lexer 回落错误消息一致）。单元测试 +4（73/73 pcodeparse::）。E2E curl byte-identical（x86-64-gcc.cspec 无 JUMPSYM 用法，sha256 04c77c1b 前后一致）；cargo test --lib 1383 过/5 败=预存同集。metadata comparand 为 WIP 工作树模型，root 集成 commit 后需 re-pin 指纹。fixture_registry 登记归 root 串行 | 2026-08-17 |
| `FUNCPROTO-MODEL-BIND-0001` | P0 | DONE| funcproto_model_bind_wave | `Funcdata`/DWARF prototype绑定Architecture resolved default model | `src/{funcdata,fspec,coreaction}.rs`, `docs/api/{funcdata,fspec,coreaction}.md`, `examples/curl_decompile.rs`, `tests/oracle/funcproto_model_bind_1204.{cc,rs,metadata.json}`, `tools/run_funcproto_model_bind_oracle.sh`, `docs/TODO_BOARD.md`（write-set 扩入 fspec/coreaction；fixture_registry.json 登记归 root 串行） | evidence=`this commit`（reviewer 两轮 APPROVE：r1 七项 + r2 窄面四项含锁联动 fspec.cc:3921/3942 独立验证与 Ghidra 不可达态证明；root 集成树亲测 __stdcall=0/Matched 123/skeleton 3097/defects 0/numbering 0/gcc 17 FAIL；runner 22 records MATCH；行号漂移 cc:48/55-56/69 已修 11 处+重钉）。r1 落地五件：①`Funcdata::set_arch` 镜像 named-ctor 绑定链（funcdata.cc:48-69→fspec.cc:3879-3884）；②`FuncProto::set_internal` 签名改 `Option<Arc<ProtoModelFull>>`+cc:3891-3898 model-None 守卫；③`ActionDefaultParams::apply` 镜像 cc:2311-2337；④`print_model_in_decl` 修复为 fspec.hh:1395 `model->printInDecl()` 委托；⑤example 两 worker 经 `worker_architecture()`（真实 cspec 字节→parse_compiler_config，OnceLock；cwd 读=CURL-CSPEC-SNAPSHOT-0001 残差）在 DWARF/PLT overlay 前 set_arch。**r2 返工（root 集成复测两回归）**：回归1 `__stdcall` 打爆声明——根因是 r1 中段窗口（print 修复前）的 result/curl_cur.c 被复测；`set_default_model` 的 cc:323-330 翻转在 arch.rs:590-605 **本就存在且生效**（隔离复测 ee71076±WIP 双侧 __stdcall=0），fixture 补 PRINTFLAG×4 观察点把该副作用 oracle 钉死（默认=0、非默认=1、换默认后旧=1/新=0）；回归2 locked 签名被覆盖——**真缺陷**：r1 的 ActionDefaultParams 对 modelless+model-locked callspec（libc/DWARF 锁定，Ghidra 对应 UnknownProtoModel 克隆 cc:1155-1166）跑 setInternal 会以 void 覆写锁定返回类型——修复为 locked 只 `set_model`（行为克隆等价，storage 保全），unlocked 走 cc:2327-2328；并补 `ActionPrototypeTypes::apply` 的 cc:4615-4619 绑定尾（`!isModelLocked() && !hasMatchingModel(evalfp)` 逐字守卫）+ `FuncProto::has_matching_model`（fspec.hh:1391）+ 单测 `test_action_default_params_locked_callspec_keeps_storage`。fixture 22 记录（+PRINTFLAG×4+CALLSPEC_REBIND：绑定后锁定的 callspec 二过 ActionDefaultParams 不被触碰），**权威 runner（pinned base=ee71076）exit 0：records=22 bytes=460 stdout_sha256=7c7be18e…，model_bind=MATCH**。E2E 受控归因（worktree 隔离 ee71076±WIP）：双侧 75 decompiled/1 timeout/0 panic、__stdcall=0、Matched 123/124、audit FAIL 17≤基线、defects=0/numbering=0；UnknownEffect 28,394→7,764（-73%，余量=cspec 未列名寄存器，Ghidra 同答）；351 WARN 与超时与 model 绑定无关（前提证伪，varnode add_descend 域另案）。r1 数字：skeleton 4724→4205、19 函数文本变化（16 改善/2 微增）。残差：FUNCDATA-LOCALSCOPE-OWNERSHIP-0001、UNKNOWN-PROTOMODEL-0001（含 locked-set_model 语义=行为克隆）、CALLSPEC-COPY-0001、CALLFIXUP-INJECT-0001、PROTOSTORE-SYMBOL-0001。模块保持 L2 | 2026-08-17（r2） |
| `DB-LOCALSCOPE-MAP-0001` | P0 | BLOCKED | unassigned | Database-owned local Scope、per-space rangemap、usepoint query/addMap/clear闭包 | `src/{database,rangemap}.rs`, `docs/api/{database,rangemap}.md`, `tests/oracle/database_local_scope_1204.{cc,rs,metadata.json}`, `tools/run_database_local_scope_oracle.sh`, `docs/TODO_BOARD.md` | 依赖`ADDRESS-0001`、`RANGEADDR-0001`、`DATABASE-0001`的symbol flags/type地基；必须保留Scope parent/id与Symbol/SymbolEntry共享identity、space-index rangemap顺序、最小有效container、addMap溢出异常后的部分状态及clearUnlocked副作用，禁止用独立Vec/deep clone替代 | 2026-08-14 |
| `PROTOSTORE-SYMBOL-0001` | P0 | BLOCKED | unassigned | `ProtoStoreSymbol` 与同一 Database-owned Scope/category-0 Symbol 图 | `src/fspec.rs`, `docs/api/fspec.md`, `tests/oracle/protostore_symbol_1204.{cc,rs,metadata.json}`, `tools/run_protostore_symbol_oracle.sh`, `docs/TODO_BOARD.md` | 依赖`CSPEC-PARAMMODEL-0001`、`DB-LOCALSCOPE-MAP-0001`与`SCOPE-CAT0-0001`；storage相同原位更新并保持Symbol identity，storage变化才remove/recreate；与当前fspec writer严格串行 | 2026-08-14 |
| `FUNCDATA-LOCALSCOPE-OWNERSHIP-0001` | P0 | BLOCKED | unassigned | `Funcdata`构造/clear/startProcessing持久local-scope ownership与默认model/store绑定 | `src/{funcdata,arch}.rs`, `docs/api/{funcdata,arch}.md`, production constructor/driver fixture, `docs/TODO_BOARD.md` | 依赖`FUNCPROTO-MODEL-BIND-0001`、`DB-LOCALSCOPE-MAP-0001`、`PROTOSTORE-SYMBOL-0001`；Database唯一拥有Scope，Funcdata/FuncProto/Action共享同一handle，构造attach失败不得提交半成品，clear不得换Scope identity；当前funcdata租约释放后才能认领 | 2026-08-14 |
| `SCOPELOCAL-RANGE-STATE-0001` | P0 | BLOCKED | unassigned | `ScopeLocal::{resetLocalWindow,markNotMapped}` space-aware range/minmax/locked-state | `src/varmap.rs`, `docs/api/varmap.md`, `tests/oracle/scope_local_range_1204.{cc,rs,metadata.json}`, `tools/run_scope_local_range_oracle.sh`, `docs/TODO_BOARD.md` | 依赖`RANGEADDR-0001`、`DB-LOCALSCOPE-MAP-0001`与`FUNCDATA-LOCALSCOPE-OWNERSHIP-0001`；Range仅合并重叠不合并相邻，按space index遍历，mark参数min/max先突变再处理locked/fake early-return与warning；warning 必须即时进入 Funcdata::warningHeader，删除须保持 Symbol/entry identity 并触发 Database resolve 副作用；禁止硬编码`[0,0x100000]` | 2026-08-14 |
| `FUNCDATA-SCOPE-SYNC-0001` | P0 | REVIEW | scope_sync_wave | `Funcdata::{setVarnodeProperties,syncVarnodesWithSymbols}` 与共享Scope/entry调用闭包 | `src/funcdata.rs`, `src/coreaction.rs`（ActionMappedLocalSync 接线）, `docs/api/{funcdata,coreaction}.md`, `tests/oracle/scope_sync_1204.{cc,rs,metadata.json}`, `tools/run_scope_sync_oracle.sh`, `docs/TODO_BOARD.md` | 依赖`FUNCDATA-LOCALSCOPE-OWNERSHIP-0001`与VARNODE/ADDRESS基础；必须消费queryProperties强输出、同一SymbolEntry identity和DefLoc同址组迭代，不能clone ScopeLocal或用任意overlap近似 | 2026-08-15: scope_sync_wave 交付（待独立复核——coreaction 主管线接线属机制C白名单）。syncVarnodesWithSymbols+per-set 重载忠实移植（funcdata_varnode.cc:938-1093：updateDatatypes 全路径 getSizedType→updateType、unmappedAliasCheck/inScope、mask 不对称=addrtied只可清/nolocalalias只可设/addrforce联动清、per-set 同址组迭代、isFree skip、mapentry 局部掩码保持 mapped、typelock 不被覆盖、flagsDirty/typeDirty 补偿）；DIRECT_WRITE 代理删除（消费者核对：varnode.rs:1433 is_illegal_input/coreaction.rs:471 DeadCode cc:3944/funcdata.rs:12180 descendPath 均为 Ghidra 真实 directwrite 语义的忠实移植，代理消失=向 Ghidra 收敛，E2E 零回归）。coreaction 接线：RestructureVarnode cc:2281-2282（false/aliasyes+count）、MappedLocalSync cc:2302-2303（true/true+count，no-op stub→真实现）。oracle fixture scope_sync_1204（cover_rebuild 模式，pinned base=609981d+{funcdata,coreaction}.rs overlay）：runner exit0，8/8 记录双侧逐字节 MATCH（type_projection/unmapped_alias/mask_asymmetry/typelock_mapentry）。E2E：curl 4070 行与 base 逐字节一致，defects=0/skeleton=4425/numbering=20 前后不变（GetStr 的声明类型改善需链①已修中止+链④ PrintC Symbol 驱动落地后可见——本环仅作用域内行为已由 oracle 证明）。cargo test --lib：1357 通过、5 失败与 base 完全一致（funcdata×2=38af1fd TypeFactory 路由后 ActionTypeInfer is_none 门预置失败；comment/dynamic/ruleaction 同），零新增回归。annotations/refs/evidence 门禁全过。残差：①getExactPiece partial-struct/array/enum/union 构造缺失→ct=None；②Varnode::set_flags 本体无 flagsDirty（调用点补偿）；③未知类型命名 undefined* vs xunknown*（fixture 层规范化）；④fixture_registry.json 登记+root 复验 pending。未 commit（按工单）。 |原语义清单:  2026-08-14TYPED-DECL-GAP 语义清单（2026-08-15 补）：updateDatatypes=true 全路径（getSizedType→updateType，funcdata_varnode.cc:953-960）、unmappedAliasCheck（:971-983）、mask 不对称规则（:1056-1067）；删除 funcdata.rs:1896 DIRECT_WRITE 代理（现两参数全 _ 忽略、从不 updateType）。  |
| `COREACTION-SCOPE-CALLER-0001` | P0 | BLOCKED | unassigned | `ActionRestrictLocal/ActionRestructureVarnode` 持久Scope、reset/count/JT调用闭包 | `src/coreaction.rs`, `docs/api/coreaction.md`, `tests/oracle/coreaction_scope_caller_1204.{cc,rs,metadata.json}`, `tools/run_coreaction_scope_caller_oracle.sh`, `docs/TODO_BOARD.md` | 依赖`SCOPELOCAL-RANGE-STATE-0001`与`FUNCDATA-SCOPE-SYNC-0001`；两次pass与reset始终突变同一Scope，保留effect/descend遍历顺序、aliasyes/numpass/count与protectSwitchPaths异常时机；禁止每pass`ScopeLocal::new()` | 2026-08-14 |
| `SCOPELOCAL-STATE-0001` | P0 | BLOCKED | scope_local_state_audit | persistent `ScopeLocal/ScopeInternal` range、category、query/addSymbol综合闭包 | `tests/oracle/scope_local_state_1204.{cc,rs,metadata.json}`, `tools/run_scope_local_state_oracle.sh`, `docs/TODO_BOARD.md` | 只读审计结论为当前`MISMATCH`：依赖`ADDRESS-0001`、`RANGEADDR-0001`、`DB-LOCALSCOPE-MAP-0001`、`PROTOSTORE-SYMBOL-0001`、`FUNCDATA-LOCALSCOPE-OWNERSHIP-0001`、`SCOPELOCAL-RANGE-STATE-0001`、`FUNCDATA-SCOPE-SYNC-0001`与`COREACTION-SCOPE-CALLER-0001`。综合fixture须证明ctor→两次Action→reset→clear始终同一scope graph/category-0 Symbol identity及异常残留；PrintC明确留给后继 | 2026-08-14 |
| `VARMAP-FAKEINPUT-0001` | P0 | DONE | agent-fakeinput-w1 | `ScopeLocal::fakeInputSymbols` 参数范围过滤、重叠分组与uintb回绕 | `src/varmap.rs`, `docs/api/varmap.md`, `tests/oracle/scope_fake_input_symbols_1204.{cc,rs,metadata.json}`, `tools/run_scope_fake_input_symbols_oracle.sh`, `docs/TODO_BOARD.md` | 实现完成待复核：`fake_input_symbols` 完整 1:1（def_tree input 前缀遍历/首地址 1 byte paramrange 过滤/重叠吸收不合并相邻/typelock 整组跳过/breaker queryProperties 探测/uintb 回绕/addSymbol LowlevelError→warningHeader 继续），新增 `get_category_size`(database.cc:2806)/`find_container_invalid_usepoint`(database.cc:2250+114)/`add_fake_input_symbol`(database.cc:1810/1843 异常面)/`func_proto_param_range`(fspec.hh:1540+fspec.cc:3879)/`param_range_in_range`(address.cc:468)。fixture `tools/run_scope_fake_input_symbols_oracle.sh` 全 10 记录 MATCH（锁定 oracle e40ed130，base 9ea2a78e+varmap overlay）；E2E 差分 skeleton 4403/defects 0/numbering 0（输出与 base 提交 byte 一致——语料无 stack input varnode，管线输出不变）；cargo test --lib 1368 过/5 已知失败（+3 新 varmap 单测）。解锁 `PRINTC-SCOPE-RESTRUCT-0001`。残差：queryProperties 父作用域上溯（varmap ScopeLocal 无父链，database-scope 统一前登记）；无 Architecture 时 paramrange 以 `ProtoModelFull::new` 默认 [0,511] 占位（FUNCPROTO-MODEL-BIND-0001） | 2026-08-16 |
| `PRINTC-SCOPE-RESTRUCT-0001` | P1 | DONE | printc_scope_restruct_wave | 移除PrintC emit阶段`ScopeLocal::restructureVarnode`兜底 | `src/printc.rs`, `docs/api/printc.md`, `docs/TODO_BOARD.md`（本行） | 依赖`VARMAP-FAKEINPUT-0001`及Action caller闭包；Ghidra在Action阶段建立持久scope，printer只消费，禁止emit时再次发现/清空/重构。必须以Action后scope、PrintC前后状态与最终C direct diff证明无副作用 | 2026-08-16: 两个解锁条件核实成立（VARMAP-FAKEINPUT-0001 DONE WIP 在树 + HERITAGE-DRIVER-SWITCH c309130 已提交）。**核心移除已随 PRINTC-SYMBOL-DECL-0001 落地（commit df0da85，吸收本 TODO）**：删除 doc_function 的 `scope.restructure_varnode(fd)` 打印期兜底与二次 `assign_default_names`，换 `snapshot_local_scope`（纯 `fd.scope.clone()`）。本 session 补齐验收证据：①oracle 只读消费面核实——`PrintC::docFunction`（printc.cc:2641）`const Funcdata*` 全链只读（cc:2597 pushScope/cc:2260-2279 emitLocalVarDecls/cc:2518-2575 emitScopeVarDecls），`restructureVarnode` 全库唯一调用点=`ActionRestructureVarnode::apply`（coreaction.cc:2280）；Rugra 生产 actionlist 接线确认（action.rs:999 mainloop RestructureVarnode + action.rs:1077 NameVars→assign_default_names coreaction.rs:4670）；printc.rs 全文审计无残留 scope 突变（10329 的 ScopeLocal::new 在 #[cfg(test)]）。②**PrintC 前后状态 direct diff**：新增看门狗 `test_doc_function_leaves_action_scope_unchanged`（printc.rs）——真实 ActionRestructureVarnode 构建 scope + ActionNameVars 输出形态符号（name_dedup/typelock/register/unique/dynamic+hash），完整 doc_function 后断言 `fd.scope` Debug 指纹逐位一致 + printer 私有快照与 Action 后状态一致 + 声明确从快照发射；突变实验（快照改名 `_x` 后缀）确认测试有牙。③**最终 C 文本 direct diff**：E2E curl 124/124（74 decompiled/1 timeout/1 panic=HELPF-NONFREE 基线域），`result/curl_cur.c` sha256 ab25f148 前后逐字节一致；差分 4403/defects 0/numbering 0 持平（基线=动手前实测值，与本任务参考基线 4403/0/0 一致）。④方向观察：GetStr 声明 `char *in_RBX;` 证实 SCOPE-SYNC updateType 投影已显形（GetStr diff=29 残差归因上游 IR/结构化域，非打印期 scope）。cargo test --lib 1367 过/5 已知失败（+1 看门狗，基线 1366/5 同集）。annotations --all ✅ / refs --all --strict ✅。 | 2026-08-16 |
| `PARSECONFIG-RULE-PAIRS-0001` | P0 | MISMATCH | root | `functionalEqualityLevel` raw out-buffer/positive-prefix contract | `src/expression.rs`, `docs/api/expression.md`, `tests/oracle/functional_equality_level_1204.{cc,rs,metadata.json}`, `tools/run_functional_equality_level_oracle.sh`, `tests/oracle/fixture_registry.json`, `docs/alignment_audit/{FUNCTION_LEDGER.json,FUNCTION_MAP.generated.md,PROTOCOL_TABLE.json,DEPENDENCY_DAG.json}`, `docs/TODO_BOARD.md` | evidence=`this commit`；独立`Cross-Review: APPROVE`仅限metadata覆盖投影。locked direct matrix 25 records逐字节MATCH：包括两槽raw buffer、code0/-1已写状态、四个positive-code分支、原顺序优先、commutative交换与完整wrapper/单一AddExpression调用；真实RulePushMulti返回1且不再panic。整体保持MISMATCH：Rule输出SeqNum order与dead nullable slots仍有差异，bb/earliest/CSE/insert/shared-storage caller闭包仍未对齐；address重复helper/其他callers与边界保持UNTESTED。immutable fixed-base+仅expression overlay runner exit0，outer stdout=`4646e893…`、stderr空；15项Rust定向测试通过；四个稳定函数ID已入机器账本并绑定fixture，strict path/function selector uncovered=[] | 2026-08-14 |
| `PRINT-RESIDUAL-0001` | P1 | AUDITED | print_residual_audit | upstream 修复后 `PrintC` 未声明变量/类型/表达式残差分类 | 只读审计：`src/{printc,printlanguage,prettyprint,varmap}.rs`、locked oracle；输出 `docs/alignment_audit/PRINT_RESIDUAL_2026-08-13.md` | evidence=`5b9d177`；GetStr 在 raw Fspec↔Iop 与 VN flags/type 即分叉，Heritage 156/273↔105/287、Action 14/23↔131/439、structure 1↔3，故先修 upstream；真实 emitter residual 为 callspec消费、声明/命名、RPN/CBRANCH、类型写回及 semantic postprocess；2/23 仅 sink诊断，非对齐率 | 2026-08-13 |
| `VARMAP-GATHEROFFSET-0001` | P0 | DONE | varmap_mask_wave | `AliasChecker::gatherOffset` 8-byte `calc_mask` boundary | `src/varmap.rs`, `docs/api/varmap.md`, `tests/oracle/varmap_gather_offset_1204.{cc,rs,metadata.json}`, `tools/run_varmap_gather_offset_oracle.sh`, `docs/TODO_BOARD.md` | evidence=`this commit`；locked direct 9行完整before/result/after byte MATCH，stdout SHA=`daf47afc…`：8-byte COPY保留64位、8-byte ADD回绕=`0x25`、相邻7-byte mask=`0x25`，含shared Datatype identity、def/output/slot/descendant/flags/SeqNum。runner由e40ed130 archive重建Ghidra，并以d972d7f完整103-file Rust tree+唯一varmap overlay隔离构建；独立核心reviewer APPROVE。更深递归、其他opcode、UINTB4与AliasChecker外围仍UNTESTED，模块保持L2 | 2026-08-13 |
| `VARMAP-NAMING-0001` | P0 | DONE | varmap_naming_wave | `Scope::buildDefaultName` / `ScopeInternal::assignDefaultNames` / `ScopeLocal::buildVariableName` authoritative naming | `src/{varmap,printc}.rs`, `docs/api/{varmap,printc}.md`, `tests/oracle/varmap_naming_1204.{cc,rs,metadata.json}`, `tools/run_varmap_naming_oracle.sh`, `tests/oracle/fixture_registry.json`（root串行登记）, `docs/TODO_BOARD.md` | evidence=`pending root commit`；权威 runner（pinned base=ee29a32+2文件overlay，锁定 oracle 重建）root 复验 exit0：`MATCH covered_projection=6/6 overall=PARTIAL_MATCH stdout_sha256=1a903906…`；独立 `Cross-Review: APPROVE`（varmap 白名单机制 C：复核者自读 database.cc:1756-2865/varmap.cc:548-581/database.hh:358-373/coreaction.cc:2974-2999，四类语义逐函数独立重推导；独立重跑 runner、手工推演 sequential/bump case、净 A/B（base+仅 varmap/printc overlay）：numbering 126→126、defects 0、skeleton +7（全为死声明行）、$$undef 泄漏 0）。共享 `&mut i32` base 跨 assignDefaultNames→printc compact_base 续接；SymbolNameTree=(name,nameDedup) BTreeMap；makeNameUnique `_NN`/`_xNNNNN` 回扫逐行对齐。varmap:: 26/26、printc:: 8/8。残差：vn 分支/Translate 寄存器表 UNTESTED；**复核提示**——base 消耗顺序反转（Rugra assignDefaultNames 先于 lazy 寄存器重编号，Ghidra namerec 先）仅在两源同函数同时耗号时可观察（curl 未触发，Stack 路径不耗 base）；死声明发射与 startProcessing symbols/nametree 不同步归 printc/`FUNCDATA-SCOPE-SYNC-0001` 既有缺口。模块保持 L2 | 2026-08-15 |
| `TYPED-DECL-GAP-0001` | P0 | AUDITED | typed_decl_diag_wave | 12.0.4 golden 差分三大声明症状只读诊断：`xunknown8 local_XX` 未类型化 / `long auStackX_30` 栈偏移名 / 57 处重复 `uVarN` 声明（占 4530 行 skeleton diff 主体，glob_word 1246 / glob_set 843 / main 485） | 只读诊断完成（未改任何 src/，探针在 /tmp/rugra_diag）：**上游根因=3/21 函数管线在 simplifypool 中止**（`RuleMultiCollapse functional branch has no definition`，ruleaction.rs:7519 vs ghidra ruleaction.cc:3306 无 null 情形）——glob_word/glob_set/main 精确对应 top-3 diff（2574/4530=56.8%），中止后 fd.scope=None、HighVariable=0、sblocks=0；三症状全为中止+RUGRA-GLUE 兜底（printc.rs:2903 GLUE 声明器读 per-Varnode used_varnode_types 而非 Ghidra printc.cc:2260/2518 Symbol 驱动 emitScopeVarDecls；printc.rs:4970 发现阶段重建 scope）。修复链=①修 RuleMultiCollapse func_eq def 缺失（P0 最高杠杆）→②`Funcdata::linkSymbol` 建带 high->getType() 的 ScopeLocal Symbol（funcdata_varnode.cc:1177 ↔ funcdata.rs:758 现为 local_XX HashMap）→③`sync_varnodes_with_symbols` 实现 updateDatatypes/nolocalalias（funcdata_varnode.cc:938-1093 ↔ funcdata.rs:1866 现仅设 DIRECT_WRITE 代理 flag）→④PrintC 声明改为 Symbol 驱动并删 print 阶段 restructure 兜底（PRINTC-SCOPE-RESTRUCT-0001）。后继派工原子建议：RULE-MULTICOLLAPSE-ABORT-0001（新）、FUNCDATA-LINKSYMBOL-TYPED-0001（新，落 FUNCDATA-SCOPE-SYNC-0001 链）、保留既有 SCOPELOCAL-RANGE-STATE/COREACTION-SCOPE-CALLER 依赖序 | 2026-08-15 |
| `RULE-COLLECTTERMS-0001` | P0 | PARTIAL_MATCH | op_insert_wave | `RuleCollectTerms::applyOp` + `AddTreeState/TermOrder` 调用闭包 | `src/{ruleaction,expression}.rs`, `docs/api/{ruleaction,expression}.md`, `tests/oracle/rule_collect_terms_1204.{cc,rs,metadata.json}`, `tools/run_rule_collect_terms_oracle.sh`, `docs/TODO_BOARD.md` | evidence=`this commit`；locked 7 cases/14 records direct byte-match，stdout=`c45835fd…`，合法BlockBasic定向测试1/1；稳定crate pin=`7ddaabac…`且跑后无漂移，独立 `Cross-Review: APPROVE`。闭合u64回绕/storage mask、constant phase倒序、ADD-output lone-descend与`termOrder`调用链；仅批准metadata目标投影，OPBANK/ARCH为MISMATCH，TYPE/tie/shared-ADD/root/distribute及RulePieceStructure传播为UNTESTED，模块不升L3 | 2026-08-13 |
| `RULE-MULTICOLLAPSE-0001` | P0 | MISMATCH | rule_multicollapse_wave | `RuleMultiCollapse::applyOp` 根/嵌套 `MULTIEQUAL` collapse + `Funcdata` op mutation调用闭包 | `src/{ruleaction,funcdata,unify}.rs`, `docs/api/{ruleaction,funcdata,unify}.md`, `tests/oracle/rule_multi_collapse_1204.{cc,rs,metadata.json}`, `tools/run_rule_multi_collapse_oracle.sh`, `docs/TODO_BOARD.md` | evidence=`this commit`；锁定 12.0.4 immutable runner exit0，5个合法目标结构/10 records 的root、self-loop、nested、functional CSE、functional LOAD原位改写均`TARGET_BEHAVIOR_MATCH`，Rust stderr为空，独立 `Cross-Review: APPROVE`。根output与全skiplist实际突变后才返回CHANGE，修复main等simplifypool假改动不收敛；但 Ghidra dead op保留NULL input-slot数而Rust清空Vec，raw diff未规范化且继续绑定`OPBANK-0001`，因此整体保持`MISMATCH`/L2 | 2026-08-13 |
| `RULE-MULTICOLLAPSE-ABORT-0001` | P0 | DONE | multicollapse_abort_wave | `RuleMultiCollapse::applyOp` functional-equality 分支 `defining_branch.get_def()` 为 None 的管线中止（ghidra ruleaction.cc:3306 `defcopyr->getDef()` 无 null 情形） | `src/ruleaction.rs`, `docs/api/ruleaction.md`, `tests/oracle/rule_multi_collapse_abort_1204.{cc,rs,metadata.json}`, `tools/run_rule_multi_collapse_abort_oracle.sh`, `docs/TODO_BOARD.md` 该行 | TYPED-DECL-GAP-0001 诊断：curl 差分 top-3（main/glob_word/glob_set=2574/4530=56.8% skeleton diff）管线中止于 ruleaction.rs:7519-7523 → scope=None/High=0/sblocks=0。疑似 skiplist 处理中 op 生命周期差异丢 def op（与 RULE-MULTICOLLAPSE-0001 的 OPBANK-0001 dead-op NULL-slot 差异同族）。验收：三函数 perform_action ok + curl 24/24 不劣化 + 12.0.4 golden skeleton diff 从 4530 大幅下降；fixture cover_rebuild 模式 pinned base=启动 HEAD(a51e0c5)+ruleaction.rs overlay。evidence=`pending root commit`；根因实证=defining branch 是未 written 常量 `const 0:1`（phi 0x4d64 t=2441 = MULTIEQUAL(c0a,c0b) 两个同值不同对象常量分支），functional_equality_level 对 const-const 同值返回 0 置 func_eq，而 cc:3306 `defcopyr->getDef()` 无条件解引用——**锁定 oracle 同形状实测 SIGSEGV**（fixture fork 子进程复现），证明该形状在 Ghidra 真实管线不可达、其意图语义=cc:3279/cc:3334 absolute 折叠。修复（ruleaction.rs:7455+29 行）：const-value identity（同 space/size/offset 双常量）在 func_eq 之前按 cc:3279 指针同一性的值语义等价走 absolute continue，恢复「func_eq ⇒ defcopyr written 且 def 存活」不变量；7519 错误保留为真不变量守卫。验收全过：probe main/glob_word/glob_set perform_action ok（High 509/290/229，sblocks 110/47/…，scope≠None，修复前 3 函数全部 scope=None/High=0/sblocks=0）；ruleaction:: 159/160（唯一失败 test_rule_add_unsigned_fires 在 pristine base 同败=预存），全 lib 6 失败=基线不增；curl 24/24；12.0.4 差分 pinned-base 8140→4062（numbering 223→6；main 3404→1073、glob_word 1383→279、glob_set 842→199）、live tree 4530→4233，defects=0；runner exit0：6 records=4 MATCH+1 MATCH_MASKED_OPBANK_0001+1 ORACLE_CRASH_VS_RUGRA_COLLAPSE（SIGSEGV vs absolute 折叠，intentional）；refs strict OK。上游残差（不越租约，交 root）：heritage rename 直写 phi 槽（heritage.rs:3981/4064 绕过 op_set_input 常量 clone）等路径可造出该形状，Ghidra 靠管线序使 RuleMultiCollapse 先见 COPY 分支——HERITAGE 侧是否需对齐待 root 定夺；printc/prettyprint 各 1 个并发 agent 未注解新函数致 annotations --all 红（非本 write-set） | 2026-08-15 |
| `RUNTIME-TIMEOUT-0001` | P0 | DONE | timeout_driver_wave | curl逐函数分析超时隔离与进程回收 | `examples/curl_decompile.rs`, `tools/run_curl_timeout_isolation.sh`, `docs/TODO_BOARD.md` | evidence=`this commit`；prototype pre-pass与正式反编译均进入self-exec独立进程组，deadline后终止整个PGID并wait/reap；有界bincode协议区分timeout/panic/nonzero/input/output/monitor/wait/cleanup/spawn错误。私有release runner验证双Hang、超时后后续worker、全部故障分类、PID/PGID/子孙进程归零、snapshot回读、`main_init`/`main_free` direct-vs-isolated字节MATCH、typedef全文仅1次；独立reviewer `Cross-Review: APPROVE`。仅修测量器；wallclock-only无RLIMIT与direct baseline非Ghidra golden保留 | 2026-08-13 |
| `ACTION-OPCODE-REDISPATCH-0001` | P0 | MISMATCH | next_url_panic | `ActionPool::processOp` 每条 Rule 后 opcode/dead 检查、新 opcode重派发与 Architecture诊断闭包 | `src/{action,arch}.rs`, `docs/api/{action,arch}.md`, `tests/oracle/action_opcode_redispatch_1204.{cc,rs,metadata.json}`, `tools/run_action_opcode_redispatch_oracle.sh`, `docs/TODO_BOARD.md` | evidence=`this commit`；locked immutable runner exit0：enabled/live/single-op/no-break投影为`target_projection=MATCH raw_diagnostic=MATCH`，正返回与零返回均弃旧SUBPIECE规则尾并从INT_ZEXT index0重派，零返回错误原文逐字一致；独立`Cross-Review: APPROVE`。完整ActionPool因apply/count载体、disabled/Rule stats/warning/break/resume、动态PcodeOpTree与dead cleanup残差保持`overall=MISMATCH`/L2。隔离`next_url`已越过原RuleSubvarSubpiece崩溃，随后独立暴露`ScopeLocal::fakeInputSymbols`溢出 | 2026-08-14 |
| `DEADCODE-SELFLOOP-0001` | P0 | MISMATCH | remaining_timeout_small | `ActionDeadCode::{apply,pushConsumed,propagateConsumed}` + Varnode consume状态机 + `Funcdata` output生命周期闭包 | `src/{coreaction,varnode,funcdata}.rs`, `docs/api/{coreaction,varnode,funcdata}.md`, `tests/oracle/action_deadcode_selfloop_1204.{cc,rs,metadata.json}`, `tools/run_action_deadcode_selfloop_oracle.sh`, `docs/TODO_BOARD.md` | evidence=`this commit`；locked immutable runner exit0：36条target projection逐字节MATCH、raw_diff=0，独立`Cross-Review: APPROVE`。闭合call/nonassignment/autolive seed、VAC/LIS/LIFO、self-loop/two-phi、covered return/load及合法opSet/Unset/newVarnodeOut BTree生命周期；但neverConsumed/全opcode/return-jumptable-space-callspec、minimalmask/NZMask、nullable slot/opcode-list、错误契约与High/lane/localmap仍MISMATCH/UNTESTED，模块不升L3。实测glob_word/glob_set从永久自锁降至<1s；parseconfig越过DeadCode/BTree崩溃后暴露独立`PARSECONFIG-RULE-PAIRS-0001` | 2026-08-14 |
| `MERGE-DATATYPE-SCALE-0001` | P0 | MISMATCH | my_get_line_diag | `Merge::{mergeByDatatype,mergeLinear,mergeTestBasic,mergeTestSpeculative}` 精确候选/排序/cache闭包 | `src/merge.rs`, `docs/api/merge.md`, `tests/oracle/merge_datatype_scale_1204.{cc,rs,metadata.json}`, `tools/run_merge_datatype_scale_oracle.sh`, `tests/oracle/fixture_registry.json`, `docs/TODO_BOARD.md` | evidence=`this commit`；locked immutable runner exit0，4行direct full-loc投影逐字节MATCH（stdout SHA=`06e92447…`），独立`Cross-Review: APPROVE`。闭合free/Basic过滤、exact Datatype identity、space-index/storage/def顺序、首个候选、mergegroup/High反向引用和枚举态cache突变；但任意subrange、ActionMergeType持久caller/cache生命周期、overlap/shadow/piece、spec guards、完整comparator/raw High状态仍MISMATCH/UNTESTED，模块不升L3。`my_get_line`不再卡旧10s Merge点，3.95s后暴露独立`varmap.rs:1675`溢出，尚非端到端成功 | 2026-08-14 |
| `COVER-REBUILD-SELFLOCK-0001` | P0 | DONE | cover_rebuild_wave | `Varnode::updateCover` → `Cover::rebuild/addRefPoint` root identity与worklist闭包 | `src/{cover,varnode,merge}.rs`, `docs/api/{cover,varnode,merge}.md`, `tests/oracle/cover_rebuild_1204.{cc,rs,metadata.json}`, `tools/run_cover_rebuild_oracle.sh`, `tests/oracle/fixture_registry.json`（root串行登记）, `docs/TODO_BOARD.md` | evidence=`ee29a32`；authoritative runner exit0：`MATCH covered_projection=8/8 overall=PARTIAL_MATCH stdout_sha256=00dba3d1…`（pinned base=235b91b+3文件overlay，完整重建锁定Ghidra），独立 `Cross-Review: APPROVE`（自读 cover.cc:29-612/cover.hh/varnode.cc:233/merge.cc:1572，无新 MISMATCH，独立复验字节一致）。E2E 独立验证：parseconfig.constprop.0（0x3c80 含 0x3d26）从 TIMEOUT 变数秒完成（`normal output MATCH bytes=4127`），waypoint 链与旧 panic 日志逐项一致后正常完成；curl 全量 21/25 成功且 glob_word/glob_set/next_url/my_get_line 相对 08-14 改善；11 个单测失败 A/B 验证与本改动无关。修复：`update_cover_locked` 持锁窗口快照+`Arc::ptr_eq` 槽匹配+无写锁重入；input sentinel uindex 域 0。后继：`COVER-TWOPIECE-RESIDUAL-0001`/`COVER-REBUILD-COVERAGE-0002`/`BLOCK-INDEX-ASSIGN-0001`；新发现 `SUBFLOW-OUTVN-UNWRAP-0001`/`FUNC-TYPEPROP-SETTLE-0001`/`FUNC-GLOBRANGE-HANG-0001` | 2026-08-15 |
| `BLOCK-INDEX-ASSIGN-0001` | P1 | DONE | block_index_wave | `BlockGraph::findSpanningTree` reverse-post-order `FlowBlock::index` assignment | `src/block.rs`, `docs/api/block.md`, `tests/oracle/block_index_assign_1204.{cc,rs,metadata.json}`, `tools/run_block_index_assign_oracle.sh`, `tests/oracle/fixture_registry.json`（root串行登记）, `docs/TODO_BOARD.md` | evidence=`pending root commit`；authoritative runner（pinned base=07efbff+src/block.rs/docs overlay，锁定 oracle 重建）`MATCH covered_projection=8/8 overall=MATCH stdout_sha256=57a9e8d6…`；root 复验 exit0。公共 `BlockGraph::find_spanning_tree`（block.rs:1647）全副作用对齐 block.cc:1009-1135（index/visitcount/copymap/numdesc/RPO 重排/rootlist swap/两遍 extraroots/wipe-first 清 label）；`set_out_edge_flag_mirrored` 自环单锁修复；wipe-first 语义发现（cc:1045 每遍清全部边 label，外部预置 f_irreducible 不存活→stale-root/irreducible 跳过分支对可构造输入不可达，metadata 记 UNTESTED）。block:: 测试 2/2；6 个 lib 失败 pristine-base A/B 证实预存。后继登记：`BLOCK-INDEX-WIRE-0001`/`EDGEFLAG-BIT7-COLLISION-0001`/`BLOCK-ADDGRAPH-SEMANTICS-0001`/`BLOCK-FINDIRREDUCIBLE-0001` | 2026-08-15 |
| `BLOCK-INDEX-WIRE-0001` | P1 | DONE | block_index_wire_w19 | blockaction.rs 私有 `find_spanning_tree` 迁移至公共 port 并统一 index 域 | `src/blockaction.rs`, `docs/api/blockaction.md`, 主管线差分门禁 | `BLOCK-INDEX-ASSIGN-0001` 后继：公共 port（block.rs:1647）已落地但 blockaction.rs 私有变体（:2369 起）保留未接线（注释已注明位置与前提）；迁移须保证 TraceDAG 行为零变化并跑 curl E2E 差分；主管线接线后本闭包才可升 L3 | 2026-08-15 |
| `EDGEFLAG-BIT7-COLLISION-0001` | P2 | CLOSED（旧诊断失效） | root | `src/block.rs`, `docs/api/block.md` | 2026-08-28 锁定 oracle/当前常量复核：`F_DEFAULTSWITCH_EDGE=0x04`、`F_TREE_EDGE=0x10`，pairwise-unique 单测覆盖；不存在 bit-7 共用。default-switch 另由双半边镜像测试覆盖。旧 READY 任务作废，不计函数 MATCH。 | 2026-08-28 |
| `BLOCKCOPY-FIDELITY-0001` | P2 | MERGED（canonical=`BLOCK-BUILDCOPY-MIRROR-0001`） | root | 无独立 write-set；证据、实现、夹具与验收统一归 canonical ID，避免对同一 `src/{block,blockaction}.rs` 重复租约 | 旧审计的 label/immed_dom/index/numdesc、rootlist 与补偿性树重算观察全部保留在 canonical 任务；2026-08-28 |
| `TYPEFACTORY-CHAR-REGISTER-0001` | P1 | READY | unassigned | E2E/shared_default TypeFactory 注册 `int1` 但无 `char`（Ghidra 生产 sleigh_arch.cc:204-241 + cspec data_organization 俱全） | `src/type_system/typefactory.rs`, `src/arch.rs`, `docs/api/type_system/typefactory.md`, `docs/api/arch.md`, `tests/oracle/typefactory_char_register_1204.{cc,rs,metadata.json}`, `tools/run_typefactory_char_register_oracle.sh` | 依赖 `CSPEC-TYPEORG-STATE-0001`（DONE），与 `CSPEC-TEXT-INGEST-0001` 的 `arch.rs` 租约串行；验收=真实 cacheCoreTypes char identity/type_nochar 门 + x86 CL E2E 差分。MERGE-PERSISTENT r2 复核链发现（2026-08-19）：BaseNoChar 等值语义现已按工厂注册态忠实（nochar_distinct），但 Rugra E2E 工厂处于 type_nochar==null 世界 → 1 字节移位量通过 merge.cc:1001 门而 Ghidra 生产拒绝（x86 CL 移位常见，curl 输出可变）。修复=按 cacheCoreTypes（type.cc:3200-3248）与 cspec data_organization 注册 char，与 CSPEC-TEXT-INGEST-0001 dataorg 切片同域可协同；evidence=pending | 2026-08-20 |
| `BLOCK-ADDGRAPH-SEMANTICS-0001` | P3 | PARTIAL（graph-index-min 已实现；真实 parent 仍 MISMATCH） | unassigned | `BlockGraph::addBlock` 的稳定 graph parent 身份 | `src/block.rs`, `docs/api/block.md` | 2026-08-28 `add_block` 已维护 graph index 最小值，并给 Basic/Copy 安装 weak self handle、补齐预置 op 的 block parent；但 inline-owned Rust BlockGraph 仍无法把稳定 graph Arc 写入 `FlowBlock::parent`，与 block.cc:862-874 不等价。buildCopy fixture 故保持 overall MISMATCH。 | 2026-08-28 |
| `BLOCK-FINDIRREDUCIBLE-0001` | P2 | DONE | block_findirred + 独立复核（机制 C APPROVE，四类语义独立验证+needrebuild 不可达独立重推）+ root 集成 | 交付=`a494832`→集成=`636daa1`（block:: 2/2，registry 第 90 个 fixture）。残差：`BLOCK-CALCLOOP-0001`（calcLoop stub+生产接线）+ `BLOCK-RWLOCK-RECURSIVE-READ-0001`（yprime==x 时同锁递归 read() 加固建议，复核 NOTE） | `BlockGraph::findIrreducible`（block.cc:1147-1199）移植+structureLoops 完整重建环 | `src/block.rs`, `docs/api/block.md`, fixture | 唯一 f_irreducible 写者，运行于 findSpanningTree 之后（copymap FIND 复用 + wipe-first 交互）；BLOCK-INDEX fixture 已证外部预置不存活，本任务补真实 findIrreducible 路径 | 2026-08-15 |
| `COVER-TWOPIECE-RESIDUAL-0001` | P1 | DONE| cover_twopiece_wave | `CoverBlock` 指针身份域 + two-piece 回绕全方法对齐（merge/contain/boundary/intersect 四象限/addRefPoint wrap fall-through/addRefRecurse 精确 tip） | `src/{cover,merge}.rs`, `docs/api/{cover,merge}.md`, `tests/oracle/cover_twopiece_1204.{cc,rs,metadata.json}`, `tools/run_cover_twopiece_oracle.sh`, `docs/TODO_BOARD.md` | evidence=`this commit`（Cross-Review APPROVE：14 项全一致 + 复核方独立 4096/6561 对抗矩阵 byte-identical；root 复跑 runner 13/13 MATCH；E2E skeleton 4781→4742）（禁 commit，工作区交付）；authoritative runner exit0：`MATCH covered_projection=13/13 overall=MATCH stdout_sha256=65cf1b92…`（pinned base=a301036+cover.rs/merge.rs overlay，完整重建锁定 Ghidra，13 行双侧字节一致）。表示法：新增 `CoverEndpoint`（Begin/EndMark/InputMark/Op{order,multiequal}）建模 Ghidra `PcodeOp*` 哨兵+marker 身份，`empty()` 改指针级 `(Begin,Begin)` 释放 `end<start` 值域给回绕区间；merge 全 internal1..4、intersect 四象限、contain/boundary 回绕分支、addRefPoint `ustop<ustart` wrap fall-through、addRefPoint/addRefRecurse 的 old-stop MULTIEQUAL-tip 精确判别（旧保守近似与 metadata 登记 MISMATCH 消除）。fixture：Part A 八个 CoverBlock 单元级 case（含 a3 指针级 boundary 判别、a4/a5 merge 回绕产生、a7 四象限）+ Part B 四个 production rebuild/merge case（b1/b2 join 块 ME 读者回绕 `d-0m`、b3 tip 精确判别填未触前驱、b4 one-piece+two-piece Cover::merge 并集回绕）。E2E（隔离 worktree，HEAD+本改动两文件 A/B）：cargo test --lib 1373/5 与基线完全一致（主工作区 8 个失败经 pristine A/B 证实属并发 agent WIP）；curl 124 函数 75 decompiled/0 panic/1 timeout 保持，defects=0/numbering=0 保持，skeleton 4781→4742 净改善 39（myprogress/helpf/glob_word 等下降，无函数恶化>5）。残留：INDIRECT 目标 order fallback（限制族第 2 实例，metadata MISMATCH 登记）；fixture_registry 登记留给 root。后继：`COVER-REBUILD-COVERAGE-0002` 扩展、INDIRECT-order 解析 TODO | 2026-08-17 |
| `COVER-REBUILD-COVERAGE-0002` | P2 | DONE | cover_coverage_wave | cover_rebuild_1204 补 "MULTIEQUAL 读 implied 中间变量" 用例 | `tests/oracle/cover_rebuild_1204.{cc,rs,metadata.json}`, `tools/run_cover_rebuild_oracle.sh` | evidence=`pending root commit`（禁 commit，工作区交付）；新 case `implied_multiequal_reader`（b0: d→root；b1: r1=INT_AND(root)→implied t1；b2: r2=INT_XOR(root)；b3: m=MULTIEQUAL(t1,c4,c4)，无槽持 root）推演：rebuild 恒以 root 调 addRefPoint（cover.cc:490），MULTIEQUAL 槽匹配（cover.cc:606 `ref->getIn(j)==vn`）对 root 无命中→零前驱递归，oracle 实测 cover=`[0:8388610-e,1:b-8388610,2:b-8388610,3:b-b]`，block1 保持 reader-tail 端点而 current-identity 错配会递归 b1 产生 `1:b-e`——判别场景已被观察；coverage `multiequal_reader_of_implied_intermediate` UNTESTED→MATCH。authoritative runner exit0：`MATCH covered_projection=9/9 overall=PARTIAL_MATCH stdout_sha256=3387f7d3…`（10 行 stdout 双侧字节一致）。**重 pin 说明**：SELFLOCK 后 varnode.rs（91b4774/763d564/e87ebfc）与 merge.rs（7046998）及其 docs 已领先旧 base 235b91b（live≠pin，runner 旧态必红），按 BLKACT 先例重 pin base=194901d+live 3 文件 overlay，全部 comparand 哈希按 runner 算法重算（Cargo/registry closure 恰未变）；expected stdout 先独立复刻 oracle 构建取新值 3387f7d3 回填后全跑绿；未动任何 src/。fixture_registry 该行 description 未列新 case（root 串行登记域，无 hash pin 不阻塞）。⚠ 集成注意：本 runner 全绿后并发 agent 又改了 src/merge.rs+type_system/*（非本 write-set），若 root 提交时 live merge.rs≠pin 01b03dab 需按同算法再重 pin 重跑。TWOPIECE 复核补充 UNTESTED：(i) addRefRecurse 到达已有 two-piece 块的不填底分支未驱动；(ii) addRefPoint contained-MULTIEQUAL fall-through（cc:580-583）未驱动 | 2026-08-15 |
| `SUBFLOW-OUTVN-UNWRAP-0001` | P0 | DONE | subflow_outvn_wave | `SubvariableFlow` create_link 路径 `outvn.unwrap()` 无守卫 | `src/subflow.rs`, `docs/api/subflow.md`, `tests/oracle/subflow_outvn_1204.{cc,rs,metadata.json}`, `tools/run_subflow_outvn_oracle.sh`, `tests/oracle/fixture_registry.json`（root串行登记）, `docs/TODO_BOARD.md` | evidence=`pending root commit`；oracle 判定：Ghidra 对 COPY/MULTIEQUAL/INT_* 的 null outvn 不可达（subflow.cc:883 null 守卫仅包 mark-skip，:895 裸传 createLink，:70 直接解引用→null 即 UB；一致性 IR 由 funcdata_op.cc:52-66/203-217 保证）。Rugra E2E 命中该状态=上游 IR 分歧（`UPSTREAM-OUTVN-DEADWIRE-0001`）。修复：17 个消费 getOut 的 case 统一 None→该 case 失败路径 `return false`（非 unwrap_or 造值）；附带修出 CopyPatch/ExtensionPatch 读锁死锁（guard 提升，对齐 subflow.cc:1486-1487）、`get_replace_varnode` 硬编码 Register space→继承原空间（subflow.cc:1338）、`use_same` 恒真条件→`bitsize>=8`+补 aggressive（1281/1282）+consume 位宽 `1<<bitsize`。权威 runner（pinned base=07efbff+2文件overlay）root 复验 exit0：`MATCH covered_projection=15/15 overall=PARTIAL_MATCH stdout_sha256=eeb58785…`；E2E my_get_token panic 消除（normal output MATCH）；subflow:: 45/45。None 前置如实 NO_ORACLE（Ghidra 该点必崩，无法产生可比观测） | 2026-08-15 |
| `UPSTREAM-OUTVN-DEADWIRE-0001` | P0 | IN_PROGRESS/AUDITED | deadwire_diag | alive 无输出 op 滞留 descend 列表 + `free varnode gets multiple descendants` WARN（E2E 当前树 351 条）归因 | `src/{varnode,funcdata}.rs`, paired docs, E2E 复现 | 只读审计完成（2026-08-15，/tmp 插桩副本 6 轮动态取证）。**结论：351/351 WARN 全部在 `setcasts` 内触发**（ActionSetCasts::cast_input coreaction.rs:3151 `op_set_input(cast, in_vn, 0)` 对 free vn 加第二读者，对齐 Ghidra coreaction.cc:2702-2712 同一行）；`refs_vn=true` 全量证实旧读者真实存在（非 descend 陈旧漂移）；违规 vn=1 字节 free flag/字节寄存器 vn（Register 0x200/0x206/0x20b 共 150）与 SLEIGH unique 临时（0x58800 等共 201），唯一读者=翻译期创建的 BOOL_NEGATE(279)/BOOL_OR(46)/BOOL_AND(16)/BOOL_XOR(10)（SLEIGH `!ZF`/`&&`/`||` 语义；OP-CREATE-BOOL 316 个=280+36*2，输入数 352≈351 完全对账）。Ghidra oracle：varnode.cc:330-338 addDescend 对该状态 throw LowlevelError；不可达保证=Heritage 在 setcasts 前将全部此类 free 读 SSA 化（guard/MULTIEQUAL/input 标记）+ funcdata_varnode.cc:148 newVarnode→varnode.cc:1250 create 每读新 free 对象。Rugra 根因=**heritage 未对 SLEIGH flag 读 SSA 化**（拆 `HERITAGE-FLAGFREE-SSA-0001`）。已排除制造者：RulePropagateCopy 裸 push（探针 0 命中，另拆 `RULE-PROPCOPY-BOOKKEEP-0001`）、make_free 带读者（关联 0 命中）、就地 clear WRITTEN（DIAG3≈DIAG2 全为 make_free 内部）、`VARNODE-INPLACE-MUTATION-SITES-0001` 5 处（无一是本现象制造者）、ActionCse/ActionCopyPropagate（未接主管线，拆 `COREACTION-RAWWIRE-DEADCODE-0001`）。glob_range 本轮 17 条。SUBFLOW 移交原始描述（alive 无输出 op 滞留 descend）为同族对称问题，续查 | 2026-08-15 |
| `HERITAGE-FLAGFREE-SSA-0001` | P0 | DONE| flagfree_ssa_wave | Heritage 未将 SLEIGH flag/byte 读（register/unique 空间 1 字节 free-with-reader）SSA 化，free 读存活到 setcasts 触发 add_descend 警报 | `src/heritage.rs`, `docs/api/heritage.md`, `tests/oracle/heritage_flagfree_1204.{cc,rs,metadata.json}`, `tools/run_heritage_flagfree_oracle.sh` | **2026-08-17 独立复核（flagfree_ssa_wave 第二轮，/tmp/rugra_ffdiag2+ffdiag3 受控实验）——推翻前序"rules/merge 换回"结论**：① activeheritage 线索复核：Ghidra guard() cc:1174/1181 置位、renameRecurse cc:2496-2497 消费（isHeritageKnown=insert|const|annot cc:298），Rugra guard_range(heritage.rs:1689/1698)/rename_recurse(4462/4466) 逐行镜像，无缺口——**非根因**。② census 轨迹铁证（my_get_token，每 action 边界 bank 全量 free-with-descend 差分+WATCH vn152 全状态轨迹）：vn152(Register:0x206:1) 翻译期 ADD_DESCEND(pre-action) 后一直 free-with-1-reader 在 bank；**stage 序列 = decompile/funclink→prototypetypes→[extras 平铺 22 个 action]→…→decompile/setcasts(早期 CAST=第二读者→WARN)→…→mainloop/heritage(SEEN pass0，ERASE 1→0，vn 出 bank=真实 heritage 正确 SSA 化并删除)→…→mergerequired/mergetype→decompile/setcasts(终版，无该 vn 事件)**——**不存在任何换回**：早期 setcasts 跑在 heritage 之前的 pre-SSA IR 上；无 SWAPBACK-NEW、无 DEADVN 复活、vn 在真 heritage 后永不出 bank。③ 根因=coreaction.rs `build_full_pipeline_actions()`(:10178-10239) 返回 22 个 legacy 平铺 action（含 VarnodeProps/Segmentize/…/HideShadow/OutputPrototype/InputPrototype/**SetCasts**/PrototypeWarnings）注册为 root 子节点，在 fullloop/mainloop(heritage) 之前执行；oracle coreaction.cc:5477-5486 root head 仅 Start/Constbase/NormalizeSetup/DefaultParams/ExtraPopSetup/PrototypeTypes/FuncLink/FuncLinkOutOnly 八个，无此平铺清单（RUGRA-GLUE 增殖，无 oracle 对应物）。④ 受控实验（/tmp/rugra_ffdiag3，pristine 副本单行改动）：**A2=仅删 extras 中 ActionSetCasts 一行（coreaction.rs:10234）→ E2E WARN 351→0、124/124、75 decompiled/0 panic/1 timeout（=基线）、defects=0、skeleton 3128→3182(+54)、numbering 0→3（glob_url piVar1/uVar0/uVar20 重复声明块=中段二次局部声明发射，printc/varmap 域待修）**；A(删 5 个 post-merge extras)输出与 A2 逐字节相同；B(extras→仅 FuncLinkOutOnly，Ghidra 全对齐头) WARN=0/同 numbering=3。方向核对：A2 的 glob_url `piVar1=strlen(url); if(piVar1<=0x1000)` 比 baseline `strlen(url); if((bool!)uVar)` **更接近 golden** `sVar2=strlen(url); if(sVar2<0x1001)`。⑤ 修复归属=coreaction.rs build_full_pipeline_actions（本 TODO 原判定"根因在 coreaction.rs"方向正确但机制不同：非 pass guard，是早期 setcasts 平铺重复）；测试 test_build_full_pipeline_actions_nonempty(coreaction.rs:10818) 断言 setcasts 在 extras 内须同步改写。⑥ 可恢复 throw 判据（VARNODE-ADDDESCEND-THROW-0001 landing gate）：A2 后全语料 `multiple descendants` 计数=0（已达成）→ stash@{0} throw 恢复后 E2E 预期 75/0 无 panic。⑦ ruleaction.rs(1cc588d 新 RulePropagateCopy)本轮无涉案证据。⑧ numbering=3 精确位点（/tmp/ffdiag3_A2_out.log，A2 副本）：全部在 glob_url（输出行 1929 起）——首块局部声明 1943-1947（piVar1/uVar0/uVar20/uVar_1000004a/uVar_9100），if((bool)piVar1<=0x1000){ 体首（1953 开括号后）**整块重发 1954-1958**，checker 判 piVar1/uVar0/uVar20 declared twice（简单名键）；`*uVar20=in_R12/RBP/RBX` 无初始化指针 store 基线与 A2 均有=预存缺口非本回归；printc/varmap 域：嵌套 scope 边界二次发射局部声明节。⑨ 集成顺序确认（root 2026-08-17 指示）：coreaction setcasts 单行修复（排队等 WARN-通道 agent 落地后，落地前需在新树上复测 WARN=0）→ glob_url numbering 3 处归零（含 skeleton 复测）→ 恢复 stash@{0} throw → 全量验收（75/0、WARN=0、defects=0/numbering=0、cargo test 基线）。**补充前提（throw 恢复阻塞项，VARNODE-ADDDESCEND-THROW-0001 ⑥）**：throw WIP 在 cargo test 有 8 个 harness 构造 free-with-reader 的预存失败（funcdata split_uses/unlink_then_destroy、heritage op_heritage_leaves_deadcode、ruleaction sub_commute/sub_right、subflow dumpty_hump×2/split_copy）——须随 throw 恢复同批修 harness（标 WRITTEN/INPUT 或分立 free 对象），跨文件租约需 root 预派。**⑩ 2026-08-17 终局交付（flagfree_ssa_wave 第三轮，WIP 未提交禁 commit）**：(a) 复核 coreaction.cc:5462-5738 全文——root head 8 action（:5477-5485，:5481 ParamShiftStart 被注释）+ ActionSetCasts 全文唯一出现 :5735（fullloop :5690 之后、NameVars :5734 与 FinalStructure :5736 之间），结构判断成立。(b) 执行修复：coreaction.rs build_full_pipeline_actions extras 删除 ActionSetCasts 一行（原 :10322）+ 注释更新 + test_build_full_pipeline_actions_nonempty 改写（断言 extras 无 setcasts 且 build_default_pipeline root 恰 1 个 setcasts）+ docs/api/coreaction.md 同步。ActionSetCasts 本体零改动，唯一注册=action.rs set_default_actions :5735 位。(c) E2E 前后（当前树含 WARN-EMIT2 WIP）：修复前基线 WARN=351/75-0-1/defects 0/numbering 0/skeleton 3147/Matched 123 → 修复后 **WARN=0/75-0-1 保持/defects=0/Matched 123/skeleton 3155**；numbering 0→3 复现后**已归零**——(d) numbering-3 根因独立定位（/tmp/rugra_numdiag 44GB 副本三轮动态插桩：raw-dump/回溯/片段 in-out 对拍）：FRAG IN 铁证=符号驱动声明块仅 8 输入符号（piVar1/uVar0/uVar20/uVar_1000004a/uVar_9100 不在 fd.scope=varmap 侧缺口，归 PRINTC-UNLINKED-REF-0001 族），prettyprint.rs backfill_missing_locals 的 sig_shape `contains(" *")` 臂把 `if ((bool)(piVar1 <= 0x1000 /* 4096 */)) {` 误判为函数签名（` /* 4096 */` 注释含 " *"）→ if 体当嵌套函数整块重注入声明 → 双块；该修复（control_flow_opener 门控，WARN-EMIT2 R2）已由并发 printc agent 落入工作树 src/prettyprint.rs:2485-2496（我 rsync 后落盘，副本复现即修复前形态），最终树 numbering=0 证实。(e) cargo test --lib 1410 过/7 失败=5 跨域基线（comment/dynamic/funcdata×2/ruleaction，与 e3e7e53 记录一致）+2 个 action.rs 过时断言（test_prototype_warnings_registered_once 断 setcasts 计数=2、test_base_group_order_matches_ghidra_5477_5485 断前缀含 setcasts——均编码本任务移除的 DELIBERATE RESIDUAL，须随 action.rs WIP 落地同步改为 1/删行，action.rs 租约归 WARN-EMIT2 agent）。(f) gcc 审计 107 OK/16 FAIL（WARN-EMIT2 记录 19 FAIL，改善）。(g) 后续=本行 coreaction 修复集成 commit（含 action.rs 2 测试同步）→ stash@{0} throw 恢复判据已满足（WARN=0）按 ⑨ 顺序走。工作树 coreaction.rs+docs/api/coreaction.md+本行为本 agent write-set；本行由 flagfree_ssa_wave 更新 | 2026-08-17
| `RULE-PROPCOPY-BOOKKEEP-0001` | P1 | DONE | propcopy_w19 | RulePropagateCopy（ruleaction.rs:178-224）与 Ghidra ruleaction.cc:3926-3959 三重偏离：触发方向（对 COPY 触发 vs 对读者 op 全 opcode 触发）、缺 `invn->isHeritageKnown()` 守卫（Ghidra :3936-3937 "Don't propagate free's away from their first use"）、手工 inrefs 直写+裸 descend.push（无 erase 旧输出、无 add_descend free 检查） | `src/ruleaction.rs`, `docs/api/ruleaction.md` | UPSTREAM-OUTVN-DEADWIRE-0001 审计拆分（2026-08-15）。本轮探针 0 命中=未参与当前 351 WARN，但一旦 COPY 输入为 free vn 即静默制造 free-with-N-readers（绕过 add_descend 警报），COPY 输出 descend 残留另造陈旧条目。修复=按 3926-3959 重写（读者触发+守卫+opSetInput 全记账）并补 fixture | 2026-08-15 |
| `COREACTION-RAWWIRE-DEADCODE-0001` | P2 | READY | unassigned | ActionCse（coreaction.rs:745-805）/ActionCopyPropagate（coreaction.rs:1085-1159）未接入主管线但含同型 `user.inrefs[slot]=src` 直写+裸 `descend.push`；ActionCse 注释自认 Ghidra 已删的历史 ActionCse（coreaction.cc:708） | `src/coreaction.rs`, `docs/api/coreaction.md` | UPSTREAM-OUTVN-DEADWIRE-0001 审计拆分（2026-08-15）。Ghidra 12.0.4 无对应物（CSE 由 RuleSelectCse/RuleMultiCse 承担、copy 传播由 RulePropagateCopy 承担）：删除或改 totalReplace/opSetInput 语义并注明对应 Rule，防止误接入主管线重演 free-with-reader | 2026-08-15 |
| `FUNC-TYPEPROP-SETTLE-0001` | P0 | DONE | typeprop_settle_wave | `match_url`(0x5220)/`__libc_csu_init`(0x5400) 类型传播不收敛 | `src/coreaction.rs`（根因域）, `docs/api/coreaction.md`, 后继 fixture | 只读审计完成（2026-08-15）：根因**不在**类型传播本身，而在 `ActionReturnRecovery::apply` 三处自创偏离——①mainloop 内 `active_output.is_none() → init_active_output()` 重建容器（ABA 复活：每周期 seed 全新未 checked trials）；②未 fully-checked 分支自创 `else { count += 1 }`；③`ActionPrototypeTypes` 自创 void 门控 init（Ghidra coreaction.cc:4649-4651 output 未锁即无条件，onceperfunc）。锁定 oracle 语义：cc:1908-1955 整体以 `active != 0` 为门（NULL 即 return 0），count 仅两处（cc:1935 每未 checked trial 一次 + cc:1951 clear 后一次），clearActiveOutput 置 NULL 后永不重建。已验证修复 diff（5 hunks，临时应用实测：match_url 120s→101ms、csu_init→10ms、curl 全量 24/0、1337/6 不变）交 root——**等 `PIPE-MERGETYPE-ORDER-0001` 释放 coreaction.rs 租约后由 root 应用**；应用时按机制 B2 严格记 UNTESTED（源级对齐+E2E 恢复，无逐函数 oracle fixture），建议后补 returnrecovery 生命周期 fixture。次生确认：`not settling` 警告在 my_get_token/next_url 等仍各出现一次=对齐 Ghidra localcount>=7 仅警告语义，非挂起 | 2026-08-15 |
| `TYPE-RECOVERY-EXCEEDED-0001` | P2 | READY | unassigned | `Funcdata::setTypeRecoveryExceeded` 未移植 | `src/funcdata.rs`, `src/ruleaction.rs`, paired docs | TYPEPROP 审计移交：Ghidra funcdata.hh:72/182 的标志位（消费点仅 RulePtrArith assignPropagatedType，ruleaction.cc:6502/6514）在 Rust 无对应物；移植时连带 localcount>=7 警告路径置位 | 2026-08-15 |
| `FUNC-GLOBRANGE-HANG-0001` | P0 | DONE | glob_range_hang_wave | `glob_range`(0x4d60) 挂起（最后 trace：erase_descend 循环） | `src/varnode.rs`, `src/funcdata.rs`, `docs/api/{varnode,funcdata}.md`, timeout isolation 日志 | evidence=`pending root commit`；根因=旧 `total_replace` 重扫循环在 `newvn==vn` 早退/op_set_input 无法收缩时永续自旋（A/B：旧实现回归测试 30s SIGTERM；Ghidra funcdata_varnode.cc:1478-1486 迭代器单程恰访问每条目一次）；WARN 风暴=opUnsetInput 无 NULL-slot 表示（Ghidra op.hh:136 clearInput 原地置 NULL + funcdata_op.cc:213 opDestroy 逐槽守卫），Rust `Vec<Arc>` 留 stale 槽 + op_destroy 无守卫重 erase → 1454 WARN/函数；死 Weak 工厂=destroy_varnode 的 `op_get_slot` -1→`as usize`→usize::MAX 静默 no-op。修复=total_replace 快照迭代（死 Weak 跳过、应用时求槽、漂移清理）+ op_unset_input/op_set_input 以 descend-membership 为链路存活性判据（重复 unset 幂等）+ destroy_varnode slot>=0 守卫 + add_descend free 检查只计 live。E2E **24/24 完成 0 失败**（glob_range 1.1s），erase_descend WARN 1454→0；单测 1337 pass/6 域外既有（root 隔离 worktree 复验 staged 树编译 + 3 回归测试通过）。DEADWIRE 关系=同根因家族风暴已消，340 条 free-varnode WARN 为揭出的 Action 层违规残余（归 `UPSTREAM-OUTVN-DEADWIRE-0001` 续查，验收更新为归因/清零）。同型隐患：funcdata.rs:7270/7459 两处 `as usize` -1→MAX（登记 `FUNCDATA-SLOT-CAST-0001`） | 2026-08-15 |
| `FUNCDATA-SLOT-CAST-0001` | P2 | READY | unassigned | `funcdata.rs:7270/7459` 两处 `op_get_slot(...) as usize` -1→usize::MAX 隐患 | `src/funcdata.rs`, `docs/api/funcdata.md` | GLOBRANGE wave 移交同型隐患（非本 hang 路径，低危）：`op_get_slot` 返回 -1 时 `as usize` 成 usize::MAX 静默越界 no-op；须改为显式 `>= 0` 守卫并补回归测试 | 2026-08-15 |
| `VARNODE-BANK-KEY-LIVE-0001` | P0 | DONE | setcasts_output_bank_wave | `ActionSetCasts::castOutput` → `Funcdata::opSetOutput/opUnsetOutput` → `VarnodeBank::makeFree` 动态key生命周期 | `src/{coreaction,funcdata,varnode}.rs`, `docs/api/{coreaction,funcdata,varnode}.md`, `tests/oracle/setcasts_output_bank_1204.{cc,rs,metadata.json}`, `tools/run_setcasts_output_bank_oracle.sh`, `tests/oracle/fixture_registry.json`（root串行登记）, `docs/TODO_BOARD.md` | evidence=`pending root commit`；根因=makeFree/setInput/setDef 的 key 重算删除对「树驻留期间原地突变 key 字段」的合法路径误断言（Ghidra 用 Varnode 内 lociter/defiter 删除，varnode.cc:1319-1320/1366-1367/1396-1397，从不重算比较键、无 ownership 预检）。修复=`erase_loc/def_identity` 身份删除（take(live key)+Arc::ptr_eq 校验、误中放回、miss 时 identity retain 精确删除）接入 make_free_prevalidated/transition_input/transition_def 三转换，断言改为真实所有权判据。单测 11→6 失败（5 个 varnode.rs:2489 panic 全消，余 6 个域外既有）；authoritative runner（pinned base=07efbff，overlay src/{varnode,coreaction}.rs）root 复验 exit0：`MATCH covered_projection=4/4 overall=MATCH stdout_sha256=4a584dc5…`；A/B 还原旧实现 fixture 即 panic。生产同型隐患移交 `VARNODE-INPLACE-MUTATION-SITES-0001`；glob_range 根因线索已并入 `FUNC-GLOBRANGE-HANG-0001` | 2026-08-15 |
| `VARNODE-INPLACE-MUTATION-SITES-0001` | P1 | DONE(awaiting root commit) | inplace_mutation_wave | 生产代码 5 处「树驻留期间原地突变 key 字段」同型隐患 | `src/{funcdata,ruleaction,transform,heritage}.rs`, paired docs, 差分验证 | 完成（2026-08-15，未提交待 root）。逐处 Ghidra→bank 转换：①`split_uses`(funcdata.rs:~4490)=cc:1556-1557 `newVarnode`+`opSetOutput`→`vbank.create_with_space`+`op_set_output`（删除 create 后原地改 address_space/WRITTEN/def），**附带修复循环边界**：Ghidra 迭代器先推进后重写处理**全部** descendant（cc:1549-1565），旧 Rust `last_idx` break 跳过最后一个是移植缺陷；②RuleSubCommute 尾部(ruleaction.rs:~7055)=cc:4650-4651 `opSetOutput(longform,outvn)`+`opDestroy`→`fd.op_set_output`（旧代码 old_out.def=None 不还树+outvn 原地 WRITTEN/def）；③RuleStoreVarnode(ruleaction.rs:~14790)=cc:4331-4332 `newVarnodeOut`→`vbank.create_def_with_space`+output 接线+`set_varnode_properties`（对应 varnode.cc:1411-1418 createDef，最终键直接创建）；④transform.rs `transform_input_varnodes`=cc:734-736 `fd->deleteVarnode(rvn->vn)`+`rvn->replacement=fd->setInputVarnode(...)`→`fd.delete_varnode`+`fd.set_input_varnode`（canonical 存回 replacement 供 placeInputs；删除原地 set_flags(INPUT)）；⑤heritage refineInput 已由 ADT-RENAME c96f699 修复（heritage.rs:3481-3501 对照 cc:1837-1844 无 flag 突变，本轮重读 oracle 确认）。heritage.rs 本轮零改动（同型 grep 审计 0 残留；ruleaction.rs:16782+/transform.rs:1407 均为 cfg(test)）。验证：单测 1352 pass/5 fail——HEAD worktree 基线复跑证明同 5 失败（comment/dynamic/funcdata×2/ruleaction 域外既有，零新增）；E2E 124/124（76 decompiled, 0 panic）；修改树 stdout 5 连跑 sha256 全等 `79357d5b…`；A/B HEAD 基线 3 连跑 `03d97945…` 亦全等；差分（真实捕获 stdout）defects=0/numbering=0 双侧持平，skeleton diff 4678→4400（-278）；`multiple descendants` WARN=351 与基线持平；`[TRANSFORM]` WARN=0；annotations --all ✅ + refs --strict ✅。⚠️ 注意：`result/curl_cur.c` 不由 example 写出，须 `cargo run … > result/curl_cur.c` 重定向捕获（曾以此文件做证据会测到 stale HEAD 输出）；audit_syntax 0/120 为 HEAD 既有状态（A/B 相同）。文本 A/B 1763 行差异全部来自 split_uses 循环边界修复的下游效应（变量声明序/stack 偏移命名），方向朝 Ghidra | 2026-08-15 |
| `PIPE-MERGETYPE-ORDER-0001` | P0 | DONE | pipe_mergetype_order_wave | cleanup后 `PreferComplement→StructureTransform→NormalizeBranches→AssignHigh→Merge*` 顺序 | `src/{action,coreaction}.rs`, `docs/api/{action,coreaction}.md`, `tests/oracle/action_merge_order_1204.{cc,rs,metadata.json}`, `tools/run_action_merge_order_oracle.sh`, `tests/oracle/fixture_registry.json`, `docs/TODO_BOARD.md` | evidence=`pending root commit`；post-cleanup 25 leaf 序对齐 coreaction.cc:5714-5738（结构变换先行/MergeType 唯一一份/PrototypeWarnings↔FinalStructure 恢复 oracle 先后/AssignHigh+DominantCopy+CopyMarker 迁入精确位/14 onceperfunc/MarkExplicit count 桥/base_explicit 补 hasNoDescend→-1 cc:3064）；runner（pinned base=91b4774+2文件overlay）**29 行观察 28 行逐字节一致**，唯一差异=finalstructure count 桥（→`BLKACT-FINALSTRUCT-COUNT-0001`）；root 复跑同果。独立 `Cross-Review: APPROVE`（复核者自读 oracle 逐行核对 25/25 位置、14/14 flags、count 桥、hasNoDescend 语义，复跑 runner 与 E2E 1337/6）；git 考古证明过早 MergeType 为历史层积。残差：merge 空-cover 前提（→`MERGE-PERSISTENT-STATE-0001`）、decompile group 过滤 UNTESTED（PIPE-TREE T5）、flatten 顶部 5 过早重复（PIPE-TREE T6）、`BASE-EXPLICIT-GAPS-0001`（复核发现的既有缺口） | 2026-08-15 |
| `BASE-EXPLICIT-GAPS-0001` | P1 | READY | unassigned | `base_explicit` 与锁定 `baseExplicit`（coreaction.cc:3007-3082）的 5 处既有缺口 | `src/coreaction.rs`, `docs/api/coreaction.md`, 后继 fixture | PIPE 复核（2026-08-15）发现的既有缺口（非本次引入，机制 D 登记）：①缺 cc:3020-3021 `getHigh()->numInstances()>1→-1`；②addr-tied 无条件 -1，缺 cc:3022-3048 SUBPIECE/ZEXT/PIECE loneDescend 例外；③缺 cc:3050-3072 isMapped/isProtoPartial/PIECE-base/PTRSUB-spacebase `maxref=1000000`；④缺 cc:3076 marker-op 检查；⑤**行为差**：cc:3078 `desccount>maxref→-1`（标 explicit）而 Rust 返回 desc_count（修正：coreaction.rs:2764 改 return -1）。另 apply 缺 checkNewToConstructor（cc:3253）与 multlist 链（cc:3256-3268） | 2026-08-15 |
| `BLKACT-FINALSTRUCT-COUNT-0001` | P1 | DONE | blkact_count_wave | `ActionFinalStructure::apply` 硬编码 `changed += 1` | `src/blockaction.rs`, `docs/api/blockaction.md`, `tests/oracle/action_merge_order_1204.metadata.json`, `tools/run_action_merge_order_oracle.sh`, `tests/oracle/fixture_registry.json`, `docs/TODO_BOARD.md` | evidence=`9fbb194`+re-pin commit；oracle=blockaction.cc:2186-2197（五步图调用、零 count、无条件 return 0，ctor flags=0）。修复：删三处自增（mark_unstructured 后/GOTO 打标臂/死 op 删除循环）+ changed>0→Ok(1) 尾部→无条件 NO_CHANGE；IR 突变零改动。root 重 pin（base=9fbb194、overlay+src/blockaction.rs、门禁 MISMATCH→MATCH 翻转、全 comparand 哈希重算）后权威 runner exit0：**`MATCH covered_projection=29/29 overall=MATCH stdout_sha256=13963dfb…`**——action_merge_order_1204 成为第 9 个全投影 MATCH fixture；blockaction:: 8/8、全库 1337/6 不变 | 2026-08-15 |
| `MERGE-PERSISTENT-STATE-0001` | P1 | MISMATCH | root + merge_cross_review (r2 scoped APPROVE) | 独立 merge Action 的 cover/testCache 前提 | `src/merge.rs`, `src/funcdata.rs`（仅持久 Merge 挂载点 4 hunks）, `docs/api/{merge,funcdata}.md`, …（2026-08-20 r1 REJECT：29/29 仅投影 MATCH；nochar 名称启发式、Java ZEXT/CPOOLREF 与持久通道仍分歧，依赖 TYPEFACTORY-LOCALTYPE-CACHE/TYPEOP-LOCALTYPE-DISPATCH/MERGE-PERSISTENCE-CHANNELS；三轮：M1/M3 实证通过——负例 canary 真炸、groups 反例证方向判别力、13 入口深度对称、runner 13/13、E2E 124/124、defects=0；仅剩 R2-M2 两表条目：FLOAT_TRUNC 应 (Int,Float) 误为 (Int,Int)（merge.rs:2824 vs typeop.cc:1913）；BaseNoChar 等值语义反了（type.cc:3619-3626 仅 s==1&&Int 时异于 Base），修后补 shift/float-trunc fixture case 重 pin）| 2026-08-20 |
| `MERGE-PREEXISTING-GATES-0001` | P2 | REVIEW | merge_gates_wave | 复核确认的 merge 家族预存在门/方向缺口（不在 MERGE-PERSISTENT diff 内） | `src/merge.rs`, `src/variable.rs`（仅 compare_just_loc 单函数）, `docs/api/{merge,variable}.md`, `tests/oracle/merge_gates_1204.{cc,rs,metadata.json}`, `tools/run_merge_gates_oracle.sh`, `tests/oracle/merge_persistent_1204.metadata.json`（仅 merge_rs_sha256 重 pin 1 行）, TODO 行 | evidence=`pending root commit`；五项全修：①mergeIndirect 全路径（isAddrForce 门 merge.cc:850、输入侧存活 merge :857/:865/:879、忠实 collectInputs :783-802+snipOutputInterference :811-839、allocateCopyTrim 剪断兜底 :871-881）；②buildDominantCopy 尾合并改 :1236 null-testCache 直调 merge_highs（speculative 类 u:1 由 fixture 钉死）；③merge_multi_entry 补 :936 门+setMergeProblems/setUnmerged+精确 warningHeader :950-961+Symbol (name,nameDedup) 排序遍历；④compare_just_loc 补 space 维（Address::operator< 全序，address.hh:375-393，variable.rs:463——物理位置在 variable.rs，nominal write-set 外最小单函数 diff+docs/api/variable.md 同步，无冲突租约，须 root 复核）；⑤merge_highs (Some,Some) 臂 debug_assert+如实注释（NO_ORACLE：Ghidra 侧经 Merge::merge 调用者不可达，variable.cc:699-711）。附带 RUGRA-GLUE wire_unique_high：Ghidra newUnique 的 assignHigh 半边（funcdata_varnode.cc:89），修 trim unique 无 High 的静默 no-op 与 mergeOp phase-2 过度剪枝——funcdata.rs 侧 latent 缺口建议另行登记。验收全过：merge_gates runner exit0 **14/14 MATCH**（双侧 sha 9878946d…，stderr 双空）；merge_persistent_1204 复跑 **17/17 MATCH**（仅 merge_rs_sha256 重 pin）；merge:: 4/4、variable:: 16/16、全库 1352/5（=既有域外基线，净 HEAD worktree 验证）；annotations/strict-refs 门禁绿。E2E **124/124**（76 反编译、0 timeout/panic）。差分（11.3.2 回归信号）：defects=0；隔离 A/B（净 HEAD=83360fc vs +merge.rs+variable.rs）：numbering 25→**23**（改善 2）、skeleton 4201→4635、492 hunks 集中于 main(162)/glob_word/my_get_line 等大函数——门修复+trim-High 接线改变合并/数据流决策的可见输出位移，缺陷零增长（## Differential 块见交付报告）。待 root：commit + 核心算法白名单 Cross-Review | 2026-08-16 |
| `COVER-MIDBLOCK-FILL-0001` | P2 | READY | unassigned | `compute_varnode_covers` 不做 def→use 中间块回填 | `src/merge.rs`, `docs/api/merge.md` | MERGE-PERSISTENT 二轮复核登记（2026-08-16）：merge.rs `compute_varnode_covers` 只按 def/读者事件建块，缺 Ghidra `Cover::rebuild` 的中间块回填（cover.cc:524-558 addRefRecurse/setAll——def 块到 use 块之间的路径块应获得 [begin,end] 覆盖）；当前用「更晚块读者→[def,MAX]」近似 live-out，线性 CFG 等价、一般 CFG 有界偏差。忠实的 `Cover::rebuild` 已在 cover.rs:690 可用（COVER-REBUILD-SELFLOCK fixture 已 MATCH）——修复=compute_varnode_covers 改为消费它或逐块回填对齐 cover.cc | 2026-08-16 |
| `TYPE-WIRING-0001` | P0 | DONE(待 root commit+复核) | type_wiring_wave | VarnodeBank bank-local `xunknown{size}` adapter 与 Architecture TypeFactory `undefined{size}`（TYPE-UNKNOWN-0001）双轨并存——生产 vn.v_type 走前者，`xunknown8 in_RBX` vs golden `char *pcVar1` 的类型面残差；DWARF/ActionInferTypes 类型未汇入符号链 | `src/{varnode,type_system/typefactory,arch,varmap}.rs`, `docs/api/{varnode,type_system/typefactory,arch,varmap}.md`, TODO 行 | evidence=`pending root commit（禁 commit，工作区交付）`；**双轨消除**：删 bank-local `unknown_datatype`/`unknown_types` cache；`VarnodeBank::allocate`/`Varnode::new_with_space` 经 `default_unknown_type`（注入句柄优先→`TypeFactory::shared_default()` DataOrg 进程级单例模拟 headless 单 Architecture，type.cc:3106）取 `getBase(size,UNKNOWN)`（对齐 funcdata_varnode.cc:69/87/107/132/154 全家族经 glb->types）；varmap 侧 `restructure_varnode` 解析 `fd.arch.types`（无则 canonical）并贯穿 `gather_spacebase`/`merge_with`(varmap.cc:309)/`create_entry`(concretize 走工厂 getBase,type.cc:4147)/`fake_input_symbols`(varmap.cc:1438)；`concretize` 不再自铸 undefined1；`Architecture::ensure_types` 供 root 接线。**E2E A/B（76 函数同树隔离）**：`xunknown` 137→**0**、`xVar` 65→**0**、uVar 604→667（UNDEFNAME 残差闭）、undefined 拼写 2→156、typed-ptr 声明 31→37、行数 2397→2422；12.0.4 差分 **defects=0**（硬底线）、skeleton 2018→2041、numbering 0→**23 已逐个归因**（同一机制：prettyprint.rs:3043-3052 legacy 十一趟声明白名单不识别 `undefinedN`→missing 合成重复 `int uVarN;`（:3097），prettyprint.rs 非 write-set，一行修法=`parts[0].starts_with("undefined")` 交 root；23=parseconfig9/myprogress4/match_url5/GetStr2/my_fwrite1/glob_url1/csu_init1，gcc "old-style param decl" 11 错同源）；**gcc 审计 53→98 OK**（xunknown 非法类型名 63 fail→18）；受影响 7 函数子集两跑 sha 一致（72c5c228…），全量指纹 ce98b164…。域测试 varnode::19/type_system::80/varmap::26 全绿，全库 1354 过/5 败=既有域外集（comment/dynamic/funcdata×2/ruleaction）零新增；annotations --all + refs --strict 过。**需 root 重 pin**：varnode_init_1204 fixture Rust 侧 `type=/type_id=/type_core=` 投影（xunknownN/0/0→undefinedN/hashName/1，expected_rugra_stdout_sha256=3b0bdfbf… 过期）；printc TYPEDEFS 块缺 undefined2（1 处 decl）。**残差**：createEntry 数组壳仍本地构造（无 TypeFactory::getTypeArray，varmap.cc:625）；shared_default 的 max_basetype_size=size>8 数组化路径未接（type.cc:3652-3656） | 2026-08-16 |
| `MERGE-INFLATE-TEST-STATE-0001` | P2 | READY | unassigned | `Merge::inflate_test` 无状态 vs Ghidra 经持久 testCache | `src/merge.rs`, `docs/api/merge.md` | MERGE-PERSISTENT 复核 N3（2026-08-16）：Ghidra ActionMarkImplied 经 `data.getMerge().inflateTest` 消费持久 testCache（merge.cc:1620 `testCache.updateHigh(high)` 副作用——惰性重建 high cover 并 purge 该 high 的缓存对）；Rugra `Merge::inflate_test`（静态、coreaction.rs MarkImplied 直调）无任何 testCache 状态，每次全量重算。覆盖面缺口：markImplied 群组与 merge 家族间无缓存复用；修复=inflate_test 接 Funcdata mount（走 type_test_cache.update_high）并按 merge.cc:1616-1645 全文核对 | 2026-08-16 |
| `VARNODE-COPYSHADOW-ARC-0001` | P1 | PARTIAL_MATCH | copyshadow_wave | `Varnode::copy_shadow` 恒 false：`collect_copy_sources` 起始 Arc 解析断裂（已修复，残差移交 merge.rs） | `src/varnode.rs`, `docs/api/varnode.md`, `tests/oracle/merge_persistent_1204.*` | evidence=`pending root commit`；修复=删除断裂的 `collect_copy_sources`（首迭代 `Arc::as_ptr(&out_arc)`=RwLock 分配基址 vs `vn as *const Varnode`=payload 地址，永不相等→sources 恒空→copy_shadow 恒 false；且无 def 起始节点返回空集，丢 Ghidra cc:989-993 vn=this 分支），`copy_shadow` 重写为忠实两循环（varnode.cc:982-993）：`copy_chain_hits(self,op2)`=cc:982+cc:984-988，`copy_chain_source_def(self)` 解析链源（written=true→def.output，funcdata_op.cc:78-82 def↔output 不变量；written=false→终端 COPY inrefs[0]；无 def→vn=this），再 `copy_chain_hits(op2,&链源)`=cc:990-993；身份比较统一 payload↔payload `std::ptr::eq`（同文件 copy_chain_hits 既有范式）。验证（pinned base=ec03e2f+merge.rs+重建 funcdata.rs+varnode.rs overlay 双侧全量重建，oracle 侧 stdout sha=634736cb… 与 metadata 钉值一致）：ladder 图 6 varnode 36/36 有序对 copyShadow 逐对比对 oracle 全一致（含 P/Q1/Q2 传递族双向、TA/TB/TC 非影子）；merge_persistent_1204 mergecopy 行 MISMATCH→MATCH（3/3/3 same_high 全 1），covered_projection 6/8→7/8；varnode:: 18/18、全 lib 1352/5（=既有域外集）；E2E 124/124 零 timeout/panic，12.0.4 差分 defects=0、numbering 26→6（7 基线再收 1）、skeleton 4188→4181→3996。**新残差（越租约移交 root）**：mergeadjacent 行仍 MISMATCH（Ghidra 4/4/4={P,Q1,Q2,TC} vs Rugra 5/5/5={P,Q1,Q2,TB,TC}）——双侧进入状态字节一致、gdb+diag 判定 trace 证明根因=merge.rs 侧 mergecopy 并入 Q1/Q2 后 high 的内部 cover 未并集重建（Rugra 行为恰等价于用 P 单实例 cover [b..op4] 判相交：TA✗/TB✓/TC✓；Ghidra merge.cc:1571-1572 `high1->merge(...); high1->updateCover();`+testCache purge），需 merge.rs HighVariable::merge cover 并集修复后 fixture 方可 8/8 且需 root 重 pin（runner 从 pinned base 构建，不取工作区 varnode.rs；expected_rugra_stdout_sha256=e71b6b59… 亦需随新输出重算） | 2026-08-15 |
| `REDUCE-0001` | P1 | DONE | root | oracle mismatch 输入 delta reduction | `tools/reduce_fixture.py`, `tools/README.md`, `docs/VERIFICATION_GUIDE.md`, `docs/TODO_BOARD.md` | evidence=`this commit`；JSON list/JSON Pointer object-field/byte-hex deterministic ddmin；predicate argv 无 shell、interesting/boring/harness exit 严格分型、进程组 timeout、重复判定、candidate mutation guard、内容 hash memoization；输出原始/最小指纹与完整 evaluation trace，最终重跑并证明 1-minimal；self-test + 三格式 CLI fixture 全绿 | 2026-08-12 |
| `PERF-VALIDATE-0001` | P1 | DONE | root | verification acceleration wave 冷闭包验收 | `docs/TODO_BOARD.md` | evidence=`753cfd3`；全部新增 Python 工具 py_compile/self-test、ledger `--check`、gate health/doc/annotations/strict refs/Evidence 全绿；`cargo test --offline --locked --all-targets` exit 0（既有 warnings）；fresh-target canonical `release --all-targets` exit 0，430 inputs / 112 jobs / cache=none / 174.31s，临时 target 自动清理；nightly dry-run 13 checks | 2026-08-12 |
| `GATE-0001` | P0 | DONE | root | `pre-commit` / `commit-msg` / ZCode events / CI | `.githooks/{pre-commit,commit-msg}`, `.zcode/config.json`, `tools/check_alignment_evidence.py`, `.github/workflows/alignment-gates.yml`, `docs/alignment_docs/HOOK_GUIDE.md` | `da4d93a`；hooksPath=`.githooks`，两 hook index mode=100755；health/doc/annotations/strict refs/Evidence 全绿；ZCode `startup|resume`；独立终审 APPROVE | 2026-08-12 |
| `GATE-SCANNER-0001` | P0 | DONE | annotation_checker_review | Rust fn item scanner + commit/PreToolUse 共用 scope | `tools/rust_fn_scanner.py`, `tools/check_ghidra_annotations.py`, `.zcode/align_gate.py` | `ce1d32c`；共享 scanner 覆盖 const/extern/restricted visibility/单行 impl、item macro、精确 cfg(test) scope、trait `;` 终点、多编辑重叠与逐次读取 freshness；94 文件零违规；独立终审 APPROVE | 2026-08-12 |
| `SLEIGH-0001` | P0 | DONE | root | Linux SLEIGH source build + FFI link | `build.rs`, `sleigh_shim/`, `examples/sleigh_test.rs`, build/verification docs | `fcc5142`；22 TU + CLI/example 链接，SLEIGH smoke 3-byte/COPY，`cargo test --offline --all-targets` 1297/0/3，独立复核 APPROVE | 2026-08-11 |
| `SLEIGH-0002` | P0 | IN_PROGRESS | root | raw SLEIGH 资产/ABI/context/lifecycle 闭包 | 由 `SLEIGH-0002A`…`D`、`SLEIGH-0002B-COVERAGE` 与 `SLEIGH-0003` 精确租约组成 | 依赖链 `2A → 2B → {2B-COVERAGE,2C,0003}`，`2C → 2D → SLEIGH-FLOW-0001`；禁止以单次 `load_pspec` 冒充修复；完整 raw fixture 前保持 MISMATCH/NO_ORACLE | 2026-08-12 |
| `SLEIGH-0002A` | P0 | DONE | root | 锁定 x86-64 spec 资产与可重现生成 | `.gitattributes`, `tools/build_locked_x86_64_sla.sh`, `sleigh_specs/{x86-64.sla,x86-64-gcc.cspec,x86-64.spec-metadata.json}`, `docs/TODO_BOARD.md` | evidence commit=`87aaef2`；从 `e40ed130` Git 对象临时提取 cpp+language tree；双编译 SHA `d5adc314…`/487659；pspec/cspec/ldefs hash 固定；example 3-byte/COPY；Cross-Review APPROVE；仅资产 provenance，仍 NO_ORACLE | 2026-08-12 |
| `SLEIGH-0002B` | P0 | DONE | root + sleigh_abi_fixture | 动态无截断 C ABI + owned image + typed result/error | `sleigh_shim/rugra_sleigh.cpp`, `src/sleigh_ffi.rs`, `docs/api/sleigh_ffi.md`, `examples/sleigh_test.rs`, `tests/oracle/sleigh_decode_1204.{cc,rs,metadata.json}`, `tools/run_sleigh_decode_oracle.sh`, `docs/TODO_BOARD.md` | evidence=`this commit`；锁定 12.0.4 同 schema stdout direct diff：12 个已覆盖 case `MATCH`（CPUID 78 ops/134 inputs），fixture overall=`PARTIAL_MATCH`，`Unimpl`=`UNTESTED`；四类语义 4/4：owned result/输出与 pointer alias、issued/input 顺序、per-instruction identity 计数、pointer/space whitelist 比较键；runner + `cargo test --offline --all-targets` + example 全绿；独立 fixture/provenance 与 C++ semantic review APPROVE；余项绑定 `SLEIGH-0002B-COVERAGE`/`SLEIGH-0002C`/`SLEIGH-0002D`/`SLEIGH-0003`，模块保持 L2 | 2026-08-12 |
| `SLEIGH-0002B-COVERAGE` | P1 | READY | unassigned | `Sleigh::oneInstruction` 动态 ABI 未覆盖分支闭包 | `tests/oracle/assets/sleigh_decode_coverage_1204/`, `tests/oracle/sleigh_decode_coverage_1204.{cc,rs,metadata.json}`, `tools/run_sleigh_decode_coverage_oracle.sh`, `docs/TODO_BOARD.md` | 依赖 `SLEIGH-0002B`（已满足）；用锁定官方语言资产触发 `Unimpl`，并覆盖单 op >16 inputs、delay-slot step、STORE space-id、OOM/std/unknown 与析构 fault injection；完整 result/state direct diff；lifecycle/InvalidState 归 `SLEIGH-0002D` | 2026-08-12 |
| `SLEIGH-0002C` | P0 | READY | unassigned | `ContextInternal::decodeFromSpec` 完整 pspec context/tracked ranges | `sleigh_shim/rugra_sleigh.cpp`, `src/sleigh_ffi.rs`, `docs/api/sleigh_ffi.md`, `tests/oracle/sleigh_context_1204.{cc,rs,metadata.json}`, `tools/run_sleigh_context_oracle.sh`, `tests/oracle/sleigh_decode_1204.metadata.json`, `docs/TODO_BOARD.md` | 依赖 `SLEIGH-0002B`（已满足）；`.sla initialize` 后走真实 XmlDecode，逐项对拍 range/mask/order/tracked DF/异常与全部 state mutation，删除字符串扫 `<set>`；与 `SLEIGH-0003` write-set 互斥 | 2026-08-12 |
| `SLEIGH-0002D` | P0 | BLOCKED | unassigned | 长生命周期 `SleighLifter` / parser cache | `src/disasm/sleigh_lift.rs`, `src/sleigh_ffi.rs`, paired API/oracle | `PIPE-REACH-0001` 已在配置后单函数流内复用一个 translator；完整项仍依赖 `SLEIGH-0002C`，需覆盖首次失败后 mutable retry、reset/cache/image/context replacement、prefix 不泄漏与 branch constant-space 保真 | 2026-08-13 |
| `SLEIGH-0003` | P1 | READY | unassigned | `Sleigh::initialize` + space/register catalog ↔ lossless construction/metadata ABI | `sleigh_shim/rugra_sleigh.cpp`, `src/sleigh_ffi.rs`, `docs/api/sleigh_ffi.md`, `examples/sleigh_test.rs`, `tests/oracle/sleigh_decode_1204.{rs,metadata.json}`, `tests/oracle/sleigh_create_metadata_1204.{cc,rs,metadata.json}`, `tools/run_sleigh_create_metadata_oracle.sh`, `docs/TODO_BOARD.md` | 依赖 `SLEIGH-0002B`（已满足）；用 typed owned result 替代 `new()->Option`/固定 64-byte name 缓冲的证据入口；同输入对拍创建异常与完整 space/register 字段；与 `SLEIGH-0002C` 串行 | 2026-08-12 |
| `SLEIGH-FLOW-0001` | P0 | IN_PROGRESS | root | Flow 使用 SLEIGH step/error/xref | `src/{flow,disasm/sleigh_lift}.rs`, `src/bin/rugra.rs`, paired API/examples/oracle/differential | `SLEIGH-0002B` 已解锁单 translator + typed atomic result，本轮先移除 iced 长度/逐指令 ctx/线性扫描；完整 pspec 依赖 `SLEIGH-0002C`，失败后 mutable retry 依赖 `SLEIGH-0002D`，Architecture/storage/callspec/injection 仍依赖 `ARCH-0001`；锁定 GetStr 六层差分非零时保持 L2/MISMATCH | 2026-08-13 |
| `SLEIGH-FLOW-REL-0001` | P0 | DONE | sleigh_relative_flow_wave | const-space relative `BRANCH/CBRANCH` → `FlowInfo` internal p-code edges | `src/{disasm/sleigh_lift,flow,funcdata}.rs`, paired `docs/api`, `tests/oracle/sleigh_flow_relative_1204.{cc,rs,metadata.json}`, `tools/run_sleigh_flow_relative_oracle.sh`, `tests/oracle/fixture_registry.json`（root串行登记）, `docs/TODO_BOARD.md` | evidence=`pending root commit`；authoritative runner（pinned base=07efbff+3文件overlay，锁定 oracle capture sha `7490edf5…`）`MATCH records=388 ops=81 varnodes=186 relatives=33 blocks=34 edges=49 visited=2`——正中目标 81 ops/33 Const relatives/34 blocks/完整双向边（旧 Rust 为 78 ops/0 edges/漏 RET）；Const 空间 relative branch 保留 VN space、按 immutable SeqNum time 解析 target、仅非 Const 目标入机器 worklist；`finish_process_instruction` 四参重构。root 复验 runner exit0；flow::tests 45/45、disasm 13/13；全库 1332/6（域外既有）；annotations/refs/机制A/机制F 通过。SeqNum 模型残差按 metadata 显式保留 | 2026-08-15 |
| `ANN-0001` | P0 | DONE | root | 旧 scanner 基线 247 项 + expanded scanner 新发现 85 项 | `ANN-A`…`ANN-P` 的互斥 annotation 租约 | `d78f3d0`…`8a3e13d` + `ce1d32c`；全部先读锁定 `.cc/.hh` 后分类；`annotations --all` 94 文件零违规，strict refs/doc-sync 全绿；纯 provenance 不升级模块状态 | 2026-08-12 |
| `ANN-A` | P0 | DONE | ann_options | `options.rs` 44 个（37×`name` + parse/collection/default helpers） | `src/options.rs`, `docs/api/options.md` | `d78f3d0`；锁定 oracle 全文重读；纯 provenance、四类行为语义未变、B2 仍 NO_ORACLE；scoped annotation+refs+doc-sync+check 通过 | 2026-08-11 |
| `ANN-B` | P0 | DONE | pipeline_reachability | `flow.rs` 18 个 + `callgraph.rs` 2 个 accessor/helper | `src/{flow,callgraph}.rs`, `docs/api/{flow,callgraph}.md` | `d2526ee`；锁定 oracle 全文重读；纯 provenance、四类行为语义未变、B2 不升级；scoped annotation+refs+doc-sync+check 通过 | 2026-08-11 |
| `ANN-C` | P0 | DONE | database_symbol_1204 | `fspec.rs` 18 个 parameter/default/helper 函数 | `src/fspec.rs`, `docs/api/fspec.md` | `0857dec`；锁定 oracle 函数体重读；纯 provenance、四类行为语义未变、B2 不升级；scoped annotation+refs+doc-sync+check 通过 | 2026-08-11 |
| `ANN-D` | P0 | DONE | small_foundation_1204 | `signature.rs` 12 个 + `memstate.rs` 14 个 container/accessor 函数 | `src/{signature,memstate}.rs`, `docs/api/{signature,memstate}.md` | `609e89a`；锁定 oracle 两模块全文重读；纯 provenance、四类行为语义未变、B2 不升级；scoped annotation+refs+doc-sync+check 通过 | 2026-08-11 |
| `ANN-E` | P0 | DONE | cover_merge_foundation_1204 | `cover.rs` 12 个 + `funcdata.rs` 7 个 CFG/helper 函数 | `src/{cover,funcdata}.rs`, `docs/api/{cover,funcdata}.md` | `0cfe1a4`；锁定 oracle 函数体重读；纯 provenance、四类行为语义未变、已知 CFG/identity 差异保留；scoped gates 通过 | 2026-08-11 |
| `ANN-F` | P0 | DONE | heritage_ssa_1204 | `coreaction.rs::{newparam_push_unique,seed_output_trials,derive_func_output_map}` + `heritage.rs::guard_calls_range_with_space` | `src/{coreaction,heritage}.rs`, `docs/api/{coreaction,heritage}.md` | `0864a47`；锁定 oracle 函数体重读；纯 provenance、四类行为语义未变，缺口绑定 OPBANK/FSPEC/HERITAGE/ADDRESS；scoped gates 通过 | 2026-08-11 |
| `ANN-G` | P0 | DONE | parser_ruleparse_1204 | `pcodeparse.rs` 22 个 lexer/decoder helper + `grammar.rs::new_impl` | `src/{pcodeparse,grammar}.rs`, `docs/api/{pcodeparse,grammar}.md` | `64eda57`；锁定 oracle parser/decoder 函数体重读；纯 provenance、四类行为语义未变，wire/parser gaps 保留；scoped gates 通过 | 2026-08-12 |
| `ANN-H` | P0 | DONE | print_pipeline_1204 | `printc.rs` 6 个 + `opbehavior.rs` 36 个 output/behavior helper | `src/{printc,opbehavior}.rs`, `docs/api/{printc,opbehavior}.md` | `d7e7a37`；锁定 oracle 相关文件/函数体重读；纯 provenance、四类行为语义未变，output/behavior gaps 保留；scoped annotation/doc/check 通过 | 2026-08-12 |
| `ANN-I` | P0 | DONE | transform_subflow_1204 | `variable.rs` 14 个 + `dynamic.rs::default` + `unionresolve.rs` 6 个 helper | `src/{variable,dynamic,unionresolve}.rs`, `docs/api/{variable,dynamic,unionresolve}.md` | `10a1929`；锁定 oracle 三模块全文重读；纯 provenance、四类行为语义未变，legacy/ownership gaps 保留；scoped gates 通过 | 2026-08-12 |
| `ANN-J` | P0 | DONE | space_address_1204 | `translate.rs` 6 + `constseq.rs` 5 + `typefactory.rs::insert` + `datatype.rs` 18 个 helper | `src/{translate,constseq}.rs`, `src/type_system/{typefactory,datatype}.rs`, paired `docs/api/*` | `40fa6f9`；锁定 oracle 函数体重读；纯 provenance、四类行为语义未变，TypeFactory/space/wire gaps 明确保留；scoped gates 通过 | 2026-08-12 |
| `ANN-K` | P0 | DONE | print_pipeline_1204 | expanded scanner: `opbehavior.rs` 42 个 const constructors | `src/opbehavior.rs`, `docs/api/opbehavior.md` | `6540688` + `3122d5f`；40 个锁定 inline constructor + 2 个 macro GLUE；Translate 缺口不伪装成 GLUE；scoped gates 通过 | 2026-08-12 |
| `ANN-L` | P0 | DONE | isolated_candidate_b | expanded scanner: `ffi.rs` 10 个 extern ABI 函数 | `src/ffi.rs`, `docs/api/ffi.md` | `3deef90`；10 个 C/Python comparison ABI 均为具体 GLUE；纯 provenance；expanded scanner/refs/doc/check 通过 | 2026-08-12 |
| `ANN-M` | P0 | DONE | cover_merge_foundation_1204 | expanded scanner: `blockaction.rs` 2 个 const helpers | `src/blockaction.rs`, `docs/api/blockaction.md` | `7dd5246`；锁定 12.0.4 constructors/functions 重读；纯 provenance，既有 collapse 差异保留；scoped gates/check 通过 | 2026-08-12 |
| `ANN-N` | P0 | DONE | small_foundation_1204 | expanded scanner: address 2 + marshal 1 + signature 2 + types 13 | `src/{address,marshal,signature,types}.rs`, paired `docs/api/{address,marshal,signature,types}.md` | `7029011`；1 个 locked inline 映射 + 17 个 scalar/const-holder compatibility GLUE；纯 provenance；scoped gates/check 通过 | 2026-08-12 |
| `ANN-O` | P0 | DONE | parser_ruleparse_1204 | expanded scanner: `pcodeparse.rs` 3 个 const token helpers | `src/pcodeparse.rs`, `docs/api/pcodeparse.md` | `1ab0429`；锁定 parser declarations/consumers 重读；3 个具体 typed-token/Pratt GLUE，纯 provenance；scoped gates/check 通过 | 2026-08-12 |
| `ANN-P` | P0 | DONE | transform_subflow_1204 | expanded scanner: `unify.rs` 10 个单行 impl functions | `src/unify.rs`, `docs/api/unify.md` | `8a3e13d`；9 个锁定 constructor + 1 个 Default GLUE；uniqid/maxnum 差异不伪装成 GLUE；scoped gates/tests 通过 | 2026-08-12 |
| `GATE-REF-PRINTC` | P0 | DONE | printc_wire_impl | `printc.rs` 9 个越界 refs（含 integer format wire 值） | `src/printc.rs`, `docs/api/printc.md` | `d9bb491`；9 refs strict 全绿；display wire 窄域 oracle MATCH；完整 push_integer 保持 MISMATCH/L2；Cross-Review APPROVE | 2026-08-12 |
| `GATE-REF-OPBEHAVIOR` | P0 | DONE | print_pipeline_1204 | `opbehavior.rs` 9 个 PTRADD/PTRSUB/POPCOUNT/LZCOUNT refs | `src/opbehavior.rs`, `docs/api/opbehavior.md` | `46752a8`；locked 12.0.4 定义起始行已核；另修 PIECE/SUBPIECE strict false-negative；零行为改动；scoped refs/annotations/doc/check 通过 | 2026-08-12 |
| `GATE-REF-RULEACTION` | P0 | DONE | isolated_candidate_b | `ruleaction.rs` 1 个 `RuleExpandLoad::applyOp` ref | `src/ruleaction.rs`, `docs/api/ruleaction.md` | `a64752b`；锁定 12.0.4 两个完整函数体重读；零行为改动；strict refs 绿，11.3.2 回归 defects/numbering=0 | 2026-08-12 |
| `PCODE-0001` | P0 | DONE | root | `TypeOpFloatInt2Float::preferredZextSize` | `src/typeop.rs`, `src/ruleaction.rs`, `src/subflow.rs`, 对应 API 文档 | `3762e22`；oracle MATCH；1294/0/3；Cross-Review APPROVE | 2026-08-11 |
| `PCODE-0002` | P0 | READY | unassigned | `TypeOp` flags + `OpBehavior` + PcodeOpBank opcode mutation | `src/typeop.rs`, `src/opbehavior.rs`, `src/op.rs`, `src/funcdata.rs`, API/路线图 | 72 descriptors + 72x72 mutation + code-list oracle；当前 clear mask `0x2008084e` vs `0x200fc8de`，仅 1/72 COPY-origin 结果正确 | 2026-08-11 |
| `FFI-0001` | P0 | READY | unassigned | constant evaluator/test IR/comparator ABI 真实 oracle 化 | `src/ffi.rs`, `tools/pcode_compare_test.py`, `docs/api/ffi.md`, locked oracle fixture | 依赖 `PCODE-0002`；修正 5/6 参数 ABI 漂移，区分合法 0/unsupported/div0/error，禁止 RuntimeVerifier 自比较；binary buffer 必须有真实所有权/LoadImage 语义；完整同输入 stdout/state diff | 2026-08-12 |
| `OPCODE-0001` | P0 | READY | unassigned | OpCode 文本/wire 表 + reverse + reserved raw values | `src/opcodes.rs`, `src/marshal.rs`, `src/ffi.rs`, `src/unify.rs`, API/路线图 | 10 个协议名不符；缺 reverse；packed 0/45 raw round-trip；72 正式 discriminants 保持不变 | 2026-08-11 |
| `OPTIONS-0001` | P0 | READY | unassigned | `ArchOption` validation/registry/wire IDs/state mutation | `src/options.rs`, `docs/api/options.md`, 路线图, oracle fixture | 依赖 `MARSHAL-ID-0001` + `ARCH-0001`；精确 name/ID/注册顺序、异常与 numeric 边界、alias/split/nan 参数、所有 state mutation；当前 L2/NO_ORACLE | 2026-08-11 |
| `PARSER-0001` | P0 | READY | root | `PcodeSnippet` 必选标点与失败状态 | `src/pcodeparse.rs`, tests, API/审计文档 | 当前 27 个 `expect_punct` 结果被丢弃且 3 个 local 分支静默漏 `;`；标点删除矩阵 + 失败时无 result + Ghidra parser oracle | 2026-08-11 |
| `PARSER-0002` | P0 | DONE | root | `UserOpSymbol::getIndex` → `CPUI_CALLOTHER` input 0 | `src/pcodeparse.rs`, API/路线图, oracle fixture | `c1e799a`；锁定 runtime oracle MATCH；statement/expression+两参数顺序；1295/0/3；Cross-Review APPROVE | 2026-08-11 |
| `MULTI-0001` | P0 | READY | unassigned | `multiprecision.cc` 16 functions / 7 public operations → RuleDiv* | 新 `src/multiprecision.rs`, `src/ruleaction.rs`, API/路线图 | 完整 334 LoC limb engine；先修 extended PIECE 布局再替换 native `u128`；cross-review+differential | 2026-08-11 |
| `COMP-0001` | P1 | IN_PROGRESS | root | `Decompress` 五函数闭包 | `src/compression.rs`, error/API/oracle fixture | system z_stream + 同 libz/stdout 直接差分；normal/replace/mutate/same-address alias/data-error MATCH；尚缺 init/NEED_DICT/MEM/STREAM/Drop fault injection，整个 compression 保持 L2 | 2026-08-11 |
| `RANGE-0001` | P0 | READY | unassigned | `CircleRange::{intersect,circleUnion,translate2Op}` + `RuleRangeMeld` | `src/rangeutil.rs`, `src/ruleaction.rs`, API/路线图 | `PCODE-0001` 已完成并释放租约；8-bit exhaustive + cross-review + differential | 2026-08-11 |
| `PIPE-0000` | P0 | DONE | root | `Action::{reset,perform}` + group/root execution | `src/action.rs`, `src/bin/rugra.rs`, `examples/{curl_decompile,httpd_decompile,getstr_stage_snapshot,debug_my_fwrite,rugra_decompile_func,debug_cfg,decompile_demo}.rs`, `docs/api/{action,bin/rugra}.md`, `tests/oracle/action_perform_1204.{cc,rs,metadata.json}`, `tools/run_action_perform_oracle.sh`, `docs/TODO_BOARD.md` | evidence=`this commit`；锁定 12.0.4 direct runner `MATCH cases=5 repeat_changes=3 group_children=3 group_reset=1 partial_resume=2`，完整观察 status/count/lcount/tests/apply、插入序、partial cursor、reset 后 iterator 保持 end 且下次从 begin；独立 `Cross-Review: APPROVE`。根入口统一 `reset→perform`；curl 旧 11.3.2 回归 `defects=0, numbering=3, skeleton=3690`，gcc audit `2/23`，残差已绑定 `PIPE-ACTION-COUNT-0001`/`PIPE-POOL-0001`/`PIPE-RESTART-0001`/`PIPE-BREAK-0001`、`VARNODE-INIT-0001`、`HERITAGE-DRIVER-0001` 与打印 DAG；模块保持 L2/PARTIAL_MATCH | 2026-08-13 |
| `PIPE-ACTION-COUNT-0001` | P0 | IN_PROGRESS | block_entry_wave | core Action 叶节点的 protected `count` 与 rule flags（分A/B/C原子） | 各子项精确租约，禁止跨批共享writer | 依赖 `PIPE-0000`；首批A进行中，DoNothing/SetCasts因算法与计数闭包更深拆B/C；禁止按返回值猜变化，每批真实fixture+独立复核+differential | 2026-08-13 |
| `PIPE-ACTION-COUNT-0001A` | P0 | PARTIAL_MATCH | block_entry_wave | `ActionStartTypes` count bridge + 6个prototype/call leaves once flags | `src/coreaction.rs`, `docs/api/coreaction.md`, `tests/oracle/action_leaf_count_1204.{cc,rs,metadata.json}`, `tools/run_action_leaf_count_oracle.sh`, `docs/TODO_BOARD.md` | evidence=`this commit`；locked runner exact `MATCH cases=2 starttypes_passes=2 once_actions=6 reset_reapply=6`，stdout=`f53a1970…`，独立 `Cross-Review: APPROVE`。StartTypes count bridge与六leaf `RULE_ONCEPERFUNC`状态机已闭合；非零calls/ops/blocks/VN、nonnull ExtraPop、warning/break/debug仍UNTESTED，DoNothing/SetCasts归B/C，模块保持L2/PARTIAL_MATCH | 2026-08-13 |
| `PIPE-ACTION-COUNT-0001B` | P0 | READY | unassigned | `ActionDoNothing` repeat/count + exact block removal | `src/{coreaction,block,funcdata}.rs`, paired docs, locked block-state fixture | 依赖A；覆盖N=2变化序`[1,1,0]`、switch target、phi/unblockedMulti、自环warning与完整双向边/identity；当前错误splice successor，禁止仅补flag冒充MATCH | 2026-08-13 |
| `PIPE-ACTION-COUNT-0001C` | P0 | PARTIAL_MATCH（selected raw apply/count MATCH；full lifecycle MISMATCH/UNTESTED） | unassigned | `ActionSetCasts` once/count + cast phase/order | `src/{coreaction,funcdata}.rs`, paired docs/type依赖, locked cast fixture | `ptrsub_output_token_1204` 真实观察：双侧两次 raw apply 都是 `result=0,count=1`；旧 Ghidra0/Rust1 差异已关闭。Rust-only `take_count_delta` 读 1、清零后读 0，但 Ghidra 无对应方法，故 adapter 为 `NO_ORACLE`，且 fixture 未执行 `Action::perform`。仍需覆盖 startCastPhase、完整 block/dominance/op input→output、union/pointer checks、repeat/perform lifecycle、异常与完整突变；禁止把 bridge 当作映射函数 MATCH；last_updated=2026-08-28。 |
| `PIPE-POOL-0001` | P0 | READY | unassigned | `ActionPool::{apply,processOp}` 动态 PcodeOpTree/Rule 状态机 | `src/action.rs`, `docs/api/action.md`, locked pool fixture, `docs/TODO_BOARD.md` | 依赖 `OP-INSERT-0001`；按 SeqNum ordered alive tree 动态迭代，每条 Rule 后立即按新 opcode 换 rule list，dead op走 deadAndGone；补 Rule reset/disable/tests/apply/breakpoint，禁止 `alivelist.clone()` 快照；完整 IR/order/stats diff + 独立复核 | 2026-08-13 |
| `PIPE-RESTART-0001` | P0 | READY（前置半边已落地，见 MAIN-POSTSTRUCT-SPIN-0001：bumpDeadcodeDelay/applyDeadCodeDelay/get_index 基础设施 + 有界完成降级；剩余=真实重启环） | unassigned | `ActionRestartGroup::apply` → `Architecture::clearAnalysis` | `src/action.rs`, `src/arch.rs`, `src/funcdata.rs`, `examples/*_decompile.rs`（流再生成闭包注入）, paired `docs/api`, locked restart fixture, `docs/TODO_BOARD.md` | 依赖 `PIPE-0000` 与 processing lifecycle；对拍 restart pending、jumptable guard、max warning、architecture/cache/symbol/context/Funcdata全部 mutation 与 child reset order；~~当前仅 reset subtree 会在旧分析状态重跑，明确 MISMATCH~~→已改为有界完成（pending 保持置位、curstart 递增后直接 return，不重跑子树；旧 reset-subtree 形态即 MAIN-POSTSTRUCT-SPIN-0001 自旋根因②）；剩余缺口=clearAnalysis+in-Funcdata followFlow 再生成（Rugra 流生成在驱动侧 follow_flow*，start_processing 无法重入）+Funcdata::clear 残差（localmap typelock 存活/funcp unlocked output 简化，MERGE-CLEAR-LIFECYCLE-RESIDUAL-0001）；语料内观察点=match_url（golden 无 restart 警告=Rugra 降级侧多 2 行头注释+skeleton 124）；2026-08-27 |
| `PIPE-BREAK-0001` | P1 | READY | unassigned | Action/Rule breakpoint + warning + statistics executor state | `src/action.rs`, paired docs, locked breakpoint fixture, `docs/TODO_BOARD.md` | 依赖 `PIPE-0000`；补 checkStartBreak/checkActionBreak、temporary flag clearing、status_actionbreak 不重调 apply、warning once、resetStats/printState；对拍完整 status/count/lcount/tests/apply/break flags，不能只测无 breakpoint happy path | 2026-08-13 |
| `PIPE-0001` | P0 | IN_PROGRESS | root | `ActionDatabase::universalAction` + six default groups | `src/action.rs`, `src/coreaction.rs`, `docs/api/{action,coreaction}.md`, ordered-tree oracle/路线图 | evidence=`this commit`；按 `coreaction.cc:5421-5441,5479` 从 default decompile 排除 group=`normalanalysis` 的 `ActionNormalizeSetup`，保住外部 FuncProto locks；locked-void Rust regression PASS，curl 24/24 完成且签名已消费锁定原型。仍依赖 `PIPE-0000`，oracle universal=76 leaves/default decompile=71 与当前剩余平铺/重复项须有序树 fixture + 独立 cross-review+differential 后闭环；本次因用户要求串行而未取得独立 APPROVE，不得升 L3 | 2026-08-12 |
| `CALLSPEC-0001` | P0 | READY | unassigned | stable Fspec/callspec identity → normal CALL/CALLIND flow | `src/space.rs`, `src/fspec.rs`, `src/funcdata.rs`, `src/flow.rs`, x86 lift, API/路线图 | 禁止按机器地址去重；direct/indirect/inject/inline/truncate 全路径 oracle；再统一入口和消费者 | 2026-08-11 |
| `PRINTC-0001` | P1 | DONE | printc_wire_impl | integer display wire values | `src/printc.rs`, `docs/api/printc.md`, `tests/oracle/printc_display_1204.*`, runner | `d9bb491`；0..5 wire + valid codec 1..5 + resolved scalar matrix direct diff MATCH；完整 object/alias/tag/suffix/主管线仍 MISMATCH/UNTESTED | 2026-08-12 |
| `DISPLAYFMT-0002` | P0 | READY | unassigned | Symbol/Datatype display-format state + typedef marshal 错误闭包 | `src/database.rs`, `src/type_system/{datatype,typefactory}.rs`, paired API/oracle | 依赖 `MARSHAL-ID-0001`；set 应 OR 完整值、invalid name/value 抛同类错误、重复属性/round-trip、typedef 持久化 format；当前 masks/静默清零/丢弃 typedef format | 2026-08-12 |
| `SSA-0001` | P0 | BLOCKED | unassigned | `ValueSetSolver` constraints → Heritage guards | `src/rangeutil.rs`, `src/heritage.rs`, API/路线图 | 依赖 `RANGE-0001`；先底层 constraints 再 guard integration | 2026-08-11 |
| `MARSHAL-ID-0001` | P0 | MISMATCH | root | 12.0.4 `AttributeId`/`ElementId` 固定表、reverse lookup 与结束哨兵 | `src/marshal.rs`, `docs/api/marshal.md`, `tests/oracle/marshal_id_1204.{cc,rs,metadata.json}`, `tools/run_marshal_id_oracle.sh`, `tests/oracle/fixture_registry.json`（root串行登记）, `docs/alignment_audit/{FUNCTION_LEDGER.json,FUNCTION_MAP.generated.md,PROTOCOL_TABLE.json,DEPENDENCY_DAG.json}`, `docs/TODO_BOARD.md` | evidence=`this commit`；锁定 runner exit 0，424 records / 12645 bytes / stdout SHA=`61738956…`，source-manifest 146 attribute + 274 element 的 name↔id、unknown 159/289、重复初始化、SPACE=20/SIZE=19 及 Tree end/unknown/order 投影逐字 MATCH。标准 `libdecomp` 链接闭包仅注册379项，而Rust固定装载420项，TreeDecoder严格错误通道仍MISMATCH，非零scope UNTESTED；整体保持MISMATCH、不得升L3 | 2026-08-15 |
| `MARSHAL-XML-TEXT-0001` | P0 | DONE | marshal_xml_text_wave | XML bytes → ordered DOM / `DocumentStorage::{registerTag,getTag}` | `src/marshal.rs`, `docs/api/marshal.md`, 条件性`Cargo.{toml,lock}`, `tests/oracle/xml_text_dom_1204.{cc,rs,metadata.json}`, `tools/run_xml_text_dom_oracle.sh`, `tests/oracle/fixture_registry.json`（root串行登记）, `docs/TODO_BOARD.md` | 依赖`MARSHAL-ID-0001`；保持属性/child源序、显著content与纯空白规则、entity/CDATA、同名tag后注册覆盖及精确parse/missing-attribute错误。malformed parse先追加null document slot等部分状态必须同输入观察，禁止用手工DOM冒充文本ingestion。owner=marshal_xml_text_wave，write-set=`src/marshal.rs`+`docs/api/marshal.md`+`tests/oracle/xml_text_dom_1204.{cc,rs,metadata.json}`+`tools/run_xml_text_dom_oracle.sh`+本行；evidence=pending root commit（write-set 就绪，本 agent 禁 add/commit）。落地内容：`XmlScan`（xml.cc:111-177/2080-2375）1:1 移植（4字节环形lookahead/EOF合成'\n'/NUL终止/9种one-shot模式/scanSName空白物化' '）+ xml.y 全语法递归下降（mode 动作按 bison default-reduction 时序：shift 末 token 后、读下一 token 前，惰性单 lookahead）+ `TreeHandler` DOM 构建 + `print_content` 纯空白丢弃 + `DocumentStorage`（null-slot 部分状态/register同名覆盖/getTag/openDocument错误）+ `DecoderError` 逐字消息。已用真实 oracle 探针钉死语义：尾随 root 注释必 syntax error（document 仅一个 Misc+合成'\n'）、`<!DOCTYPE` 首位不可达报 plain syntax error 而 prologpre 非空后报 `DTD's not supported`、`<?` 一律 PI 错误、end tag 名不校验、`]]&gt;` 为合法 content。权威 runner（pinned base=9ea2a78+overlay，immutable fd，vendored 离线构建）exit 0：`records=94 bytes=1926 stdout_sha256=7ce5daf5…`，dom=MATCH storage=MATCH errors=MATCH overall=MATCH（45 parse 用例：26 成功 DOM 投影含属性源序/实体/CDATA/注释/prolog，19 精确错误消息，+DocumentStorage 闭包）。单元测试 marshal:: 21/21（+5 新）。残余（metadata 已登记）：①≥0x80 charref/未知实体 0xFF——Ghidra 单字节 vs Rust String UTF-8（MISMATCH，fixture 不触及）；②bison YYMAXDEPTH=10000 `memory exhausted` 深嵌套（UNTESTED）。独立 Cross-Review 未安排（非核心算法白名单模块，但 root 集成时建议补窄面复核）。解锁 `TRANSLATE-DOCSTORE-UNIFY-0001`/`CSPEC-PCODEINJECT-CALLFIXUP-0001`/`CSPEC-UNIVERSAL-CHILD-0001` 及 CSPEC-RANGEPROPS 的 production XML ingestion | 2026-08-16 |
| `TRANSLATE-DOCSTORE-UNIFY-0001` | P0 | DONE | translate_docstore_wave | `Translate::initialize` 复用真实 marshal `DocumentStorage` | `src/translate.rs`, `docs/api/translate.md`, `tests/oracle/translate_docstore_1204.{cc,rs,metadata.json}`, `tools/run_translate_docstore_oracle.sh`, `tests/oracle/fixture_registry.json`（root串行登记）, `docs/TODO_BOARD.md` | 依赖`MARSHAL-XML-TEXT-0001`（DONE）；owner=translate_docstore_wave，evidence=pending root commit（write-set 就绪，本 agent 禁 add/commit）。落地：删除 translate.rs 自创 `pub trait DocumentStorage{next_document}`（无 Ghidra 对应物，全仓零实现者/零调用者），`Translate::initialize` 签名改为 `&mut crate::marshal::DocumentStorage`（对齐 translate.hh:332 `virtual void initialize(DocumentStorage &store)=0` 的具体类非 const 引用传参；消费面=sleigh.cc:558-561 `getTag("sleigh")` miss→`LowlevelError("Could not find sleigh tag")`、sleigh.cc:563-565 content 作 .sla 路径、ghidra_translate.cc:35-41 同构、architecture.cc:624-641 restoreFromSpec 调用序、sleigh_arch.cc:409-417 buildSpecFile 注册 `<sleigh>` tag）。fixture `translate_docstore_1204`：真 oracle（锁定 commit 干净 archive 构建 libdecomp.a + 真 Sleigh 引擎）vs Rugra probe（fixture 本地 Translate 实现者镜像 sleigh.cc 消费序），6+1 记录：空 store/异名 tag/注册 tag 的 content 逐字进 open 错误/同名 registerTag 覆盖后注册者胜/兄弟 tag 查找隔离——双侧逐字节一致。权威 runner（pinned base=d64ef9c+overlay，immutable fd，vendored 离线构建）exit 0：`records=7 bytes=336 stdout_sha256=27cd2f07…`，initialize_contract=MATCH overall=MATCH。残余（metadata 登记）：成功 .sla ingest（sla::FormatDecode 域，UNTESTED）；其余 Translate 方法仍无实现者 NO_ORACLE。register/get ownership 与解析顺序由 marshal DocumentStorage（tagmap operator[] 后写覆盖 + doclist 顺序 append）承载，next_document 概念按 oracle 不存在而移除。不触及 sleigh 引擎文件；CSPEC-TEXT-INGEST-0001 的 .cspec 解码接线未越界，显式留给该域 | 2026-08-16 |
| `MARSHAL-PACKED-0001` | P0 | BLOCKED | unassigned | Decoder error channel + four-position packed state machine | `src/marshal.rs`, all codec callers, API/oracle | 依赖 `MARSHAL-ID-0001` + `SPACE-0001`；strict close/skip/type/EOF/raw bytes/space/opcode，禁止 self-roundtrip 冒充 wire parity | 2026-08-11 |
| `SPACE-0001` | P0 | DONE | space_registry_wave | architecture-owned AddrSpace registry/handle | `src/{space,translate}.rs`, `docs/api/{space,translate}.md`, `tests/oracle/space_registry_1204.{cc,rs,metadata.json}`, `tools/run_space_registry_oracle.sh`, `tests/oracle/fixture_registry.json`（root串行登记）, `docs/TODO_BOARD.md` | evidence=`pending root commit`；registry 全量移植（SpaceType/space_flags 12 位/calc_scale_mask/calc_mask wrapping/7 种 new_* 构造含 overlay decode 体语义/内联访问器族/truncate_space）+ AddrSpaceManager 16 方法桥（insert_space 失败路径部分增长与逐字 LowlevelError、assign_shortcut 碰撞上限 26、copy_spaces refcount、set_base_register BE 截断、set_deadcode_delay 等）；旧枚举保留为过渡 adapter 零破坏。权威 runner（pinned base=c6f5c48a+2文件overlay）`MATCH covered_projection=8/8 overall=PARTIAL_MATCH stdout_sha256=03d310ab…`。**勘误**：TODO 原文 setName/set_DEAD 经 grep 核实不存在于锁定 12.0.4 oracle（setDeadcodeDelay 为最接近物，已覆盖）。残差：per-space decode/read/printRaw 属性编解码（归 MARSHAL-XML-TEXT/TRANSLATE-DOCSTORE）、resolver/join 半部（归 ADDRESS-0001 统一）、Rc<RefCell> 单线程句柄；消费方迁移顺序（ADDRESS-0001→varnode 键→resolver/join→CSPEC→删旧枚举）已写入报告。stable id `GH12-F-0de8bbaa1979f78d01a9` 经新账本核实。高扇出地基，独立 Cross-Review 建议在 ADDRESS-0001 消费迁移前补做 | 2026-08-15 |
| `CSPEC-RANGEPROPS-0001` | P0 | IN_PROGRESS | cspec_rangeprops_wave | `RangeProperties::decode` 的space/range/register状态 | `src/address.rs`, `docs/api/address.md`, `tests/oracle/range_properties_1204.{cc,rs,metadata.json}`, `tools/run_range_properties_oracle.sh`, `tests/oracle/fixture_registry.json`（root串行登记）, `docs/TODO_BOARD.md` | 锁定`GH12-F-87faea0d5fa6006d5091`；补`spaceName,first,last,isRegister,seenLast`及ordered decode/error状态。`MARSHAL-ID-0001`(235b91b)落地后已完成marshal重pin收尾：runner重pin至base 07efbff6/tree c3ea7960并权威重跑 exit 0，covered_projection=11/11，stdout SHA=`a62206f9…`；新增tree_decoder_unregistered投影（真实XmlDecode/TreeDecoder将未注册属性名xmlunknown-attr解析为id 159并继续遍历space/first/last，由本fixture直接观察）。仅批准字符串space投影；剩余MISMATCH：动态AddrSpace应用绑`SPACE-0001`/`ADDRESS-0001`/`CSPEC-GLOBAL-APPLY-0001`，production XML文本ingestion绑`MARSHAL-XML-TEXT-0001`，decoder错误传输绑`MARSHAL-PACKED-0001`；register名字解析UNTESTED。重跑后独立复核尚未安排，保持IN_PROGRESS不得升L3 | 2026-08-15 |
| `CSPEC-GLOBAL-APPLY-0001` | P0 | DONE| cspec_text_ingest_wave | compiler/ext `<global>` 延迟应用到全局Scope | `src/{arch,database}.rs`, `docs/api/{arch,database}.md`, `tests/oracle/cspec_global_apply_1204.{cc,rs,metadata.json}`, `tools/run_cspec_global_apply_oracle.sh`, `tests/oracle/fixture_registry.json`（root串行登记）, `docs/TODO_BOARD.md` | evidence=`this commit（write-set 就绪，本 agent 禁 add/commit）`。arch.rs 落地 `decode_global`(cc:812)+`add_to_global_scope`(cc:826, 含 inferPtrSpaces push 与 overlay 复制循环)+`add_other_space`(cc:847)+`range_from_properties`(address.cc:236, register 早返回/highest 默认/Illegal range tag)；延迟应用时序=主循环+specextensions 收集 RangeProperties→循环应用→addOtherSpace→default 回退→__thiscall 克隆(cc:1329-1350)。权威 runner 对拍 x86-64-gcc.cspec 真链：全局 scope 三元组投影 (OTHER,0,max)/(ram,0,max)/(register,0x1094,0x1097) MATCH（sorted (space index,first) 同 printBounds）。残差：Database 侧 space-keyed property/range 应用与 inferPtrSpaces post-init 过滤（cacheAddrSpaceProperties 域）不可观察——applied 三元组记录于 `Architecture::global_scope_ranges`（源序），DB 写入待 DB space-keyed range 树。模块保持 L2 | 2026-08-16 |
| `CSPEC-TYPEORG-STATE-0001` | P0 | DONE| typeorg_state_wave | `TypeFactory::{decodeDataOrganization,decodeAlignmentMap,setupSizes}` 持久状态 | `src/type_system/typefactory.rs`, `docs/api/type_system/typefactory.md`, `tests/oracle/cspec_typeorg_state_1204.{cc,rs,metadata.json}`, `tools/run_cspec_typeorg_state_oracle.sh`, `tests/oracle/fixture_registry.json`（root串行登记）, `docs/TODO_BOARD.md` | fixture 补证完成（NO_ORACLE→真实 oracle 对拍，本 agent 禁 add/commit，WIP 待 root 集成）：锁定函数 `GH12-F-6fc3cab5839c1a780f39`/`GH12-F-244dfa99723925255478`/`GH12-F-2e085b8e6ed5feb23fdb` 只消费 integer/long/pointer/char/wchar 与 size_alignment_map、其余 child 跳过、空 map 由 setupSizes 安装默认（未自创异常）——全部按此落地并修复三处既有偏差：`align_map[0]` 保持 -1 哨兵（原强置 1）、空 `<size_alignment_map>` decode 后保持空（原返回默认表）、默认表 `[0,1,2,2,4,4,4,4,8]`（原 index0=1）。TypeFactory 新增持久字段（sizeOf*/enumsize/enumtype/alignMap, type.hh:763-771）+ getSizeOf* 快照 getter + getAlignment/getPrimitiveAlignSize 读端（含 verbatim LowlevelError 文本）；setup_sizes 参数化 SizeArchInputs（RUGRA-GLUE，glb 查询外置）。runner `tools/run_cspec_typeorg_state_oracle.sh` 双侧 MATCH：真实 BfdArchitecture::init 链 + raw_decode + 8 个合成 data_organization 用例（稀疏前向填充/空表错误文本/显式 size=0/重复 entry 后者胜/0 对齐/跳过 child/int≠4 long 分支/负值 int4/sam 后 child 消费），期望 stdout sha256=6158e6ab… 锁定于 metadata；残差 5 项登记于 metadata coverage（enum 私有不可观测 UNTESTED、Rust 侧活体 Architecture 输入推导 UNTESTED、非 entry 畸形 child MISMATCH（保守登记：仅 debug-assert 可诊断，release 未证等价，良构不可达）、0 对齐 primitive 查询双侧中止 UNTESTED、生产文本 ingest 归 CSPEC-TEXT-INGEST-0001 MISMATCH）。**复核返工已修**（窄面 REJECT 1 项）：sam 分支原 `continue` 跳过统一 close（Ghidra type.cc:4604 sam 不 continue、落 4612 closeElement）→ 删 continue；新增 c8 观察点钉死"sam 后 char_size=3 双侧一致" + 单元回归 `test_data_organization_consumes_children_after_alignment_map`；illformed 残差措辞改保守；coverage 增 `post_sam_child_consumption` MATCH。回归：隔离 snapshot（base+仅本域）cargo test --lib 1398/5（=基线同 5 域外失败 + 新增 7 用例）；E2E 逐字节中性保持，defects=0/numbering=0；runner 复跑 exit=0。待 root 转复核方窄面重审（仅 sam 分支 + c8，其余不重开） | 2026-08-17 |
| `CSPEC-DEFAULT-RETURN-0001` | P0 | DONE| cspec_text_ingest_wave | Architecture `defaultReturnAddr` 与 ProtoModel缺省return-address注入 | `src/{arch,fspec}.rs`, `docs/api/{arch,fspec}.md`, `tests/oracle/cspec_default_return_1204.{cc,rs,metadata.json}`, `tools/run_cspec_default_return_oracle.sh`, `tests/oracle/fixture_registry.json`（root串行登记）, `docs/TODO_BOARD.md` | evidence=`this commit（write-set 就绪，本 agent 禁 add/commit）`。arch.rs `default_return_addr: Option<VarnodeData>`（hh:194；None=ctor null-space 哨兵 cc:159）+`decode_return_address`(cc:898, 子元素可选/二次标签抛 `Multiple <returnaddress> tags in .cspec`)；注入点 `ProtoModelFull::decode_with_defaults`（fspec.cc:2689-2691 仅 !sawretaddr 追加 ReturnAddress EffectRecord，push 在 effectlist 排序前）经 `Architecture::decode_proto_spec`/`decode_default_proto_spec` 于文档序消费。权威 runner：真实 asset `stack:0,size8` MATCH；per-model MODELRET 投影 __stdcall/MSABI/syscall=__thiscall=默认 stack:0:8，processEntry=自带 RBP register:40:8（sawretaddr 不注入）——同 Ghidra。残差：reverse-BE/异常部分状态 UNTESTED。模块保持 L2 | 2026-08-16 |
| `CSPEC-MODELRULE-DECODE-0001` | P0 | BLOCKED | unassigned | ParamList `<rule>` / model assignment strategy decode | `src/{modelrules,fspec}.rs`, `docs/api/{modelrules,fspec}.md`, `tests/oracle/cspec_modelrule_1204.{cc,rs,metadata.json}`, `tools/run_cspec_modelrule_oracle.sh`, `tests/oracle/fixture_registry.json`（root串行登记）, `docs/TODO_BOARD.md` | 依赖`MARSHAL-ID-0001`、`SPACE-0001`与`CSPEC-PARAMMODEL-0001`；当前ParamList吞rule会使真实default model storage错误。fixture必须覆盖document order、resource/rule phase边界、join_dual_class/goto_stack/hidden_return状态和异常后的部分mutation | 2026-08-14 |
| `CSPEC-PCODEINJECT-CALLFIXUP-0001` | P0 | DONE| cspec_text_ingest_wave | 16个真实 `<callfixup>` decode/register/snippet compile闭包 | `src/{pcodeinject,pcodeparse}.rs`, `docs/api/{pcodeinject,pcodeparse}.md`, `tests/oracle/cspec_callfixup_1204.{cc,rs,metadata.json}`, `tools/run_cspec_callfixup_oracle.sh`, `tests/oracle/fixture_registry.json`（root串行登记）, `docs/TODO_BOARD.md` | evidence=`this commit（write-set 就绪，本 agent 禁 add/commit）`。pcodeinject.rs 重构为 Ghidra 结构：id 索引 `injection` 向量+callFixupMap/callFixupNames(及 callother/callmech/script 四对)，`register_call_fixup` 等 Duplicate 错误逐字(cc:220-278)；`allocate_inject`(inject_sleigh.cc:418, CALLFIXUP 占位名 unknown)/`decode_inject`(cc:352=allocate→decode→register)→`InjectPayloadCallfixup::decode`(cc:171: name/target/pcode 子序+缺 pcode 错误)/`decodePayloadAttributes`(cc:83)/`decodePayloadParams`(cc:111+orderParameters cc:67)/`decodeBody`(cc:72, 空串抛 `Missing <body> subtag in <pcode>:`)→`register_inject`(cc:433: map 注册先于编译=非事务)→`parse_inject`(cc:373: addOperand 输入后输出、setUniqueBase(tempbase)、parseStream 失败抛 `<src>: Unable to compile pcode: <msg>`、成功后 tpl 替换 parsestring)。pcodeparse.rs 补 `PcodeSnippet::lex` sleigh fallback(pcodeparse.cc:3223)/STRING 声明语句(y:108,110)/ConstTpl::handle v_field selector(semantics.cc:425)/delayslot=0(semantics.hh:174)。权威 runner：16 真实 callfixup 注册顺序+参数+**编译模板 XML 逐字节 MATCH**（LOAD/INT_ADD/RETURN、COPY unique 0x364420→0x364430 递进、13×CALLIND）；合成 callother 探针观察编译失败未注册残留(count 16→17/residue id)、duplicate 未触及。残差：dynamic callfixup 的 InjectPayloadDynamic addrMap/debug-decode 面、registerInject 默认分支 UNTESTED。模块保持 L2 | 2026-08-16 |
| `CSPEC-UNIVERSAL-CHILD-0001` | P0 | DONE| cspec_text_ingest_wave | compiler spec其余userop/context/range/eval/resolve/alias子节点闭包 | 按子原子精确租`src/{arch,userop,context,database}.rs`与paired docs/oracle，`tests/oracle/fixture_registry.json`由root串行登记 | evidence=`this commit（write-set 就绪，本 agent 禁 add/commit）`。userop.rs 补 5 个 decode：`register_user_op`(cc:490 同名异 index/同 index 异名/segmentop 同空间错误逐字)、`decode_call_other_fixup`(cc:589+InjectedUserOp::decode cc:85: decodeInject 先注册 payload 再查 userop——库注册为非事务残留)、`decode_segment_op`(cc:533+SegmentOp::decode cc:225: space 必填/unknown userop/Redefining/constresolve/pcode EXECUTABLEPCODE+output=1,input 1-2 校验)、`decode_jump_assist`(cc:606+cc:302 四 pcode dup/缺 addr_pcode、default_pcode 错误逐字)、`decode_volatile`(cc:551 inputop/outputop 必填+functional flags+重复注册错误逐字)。arch.rs 主循环 dispatch 同步接入 callotherfixup/segmentop。权威 runner 合成探针：unknown-target（编译后注册残留）、segment(真 userop index 0) 定制 type=2/index=0、volatile 注册+二次重复错误全 MATCH。残差：segmentop/jumpassist 已移植无 oracle 观察(UNTESTED)；context_data/readonly/spacebase/deadcodedelay/inferptrbounds/resolveprototype 仍按子原子拆分（本切片登记在 parse_compiler_config 报告，不静默 skip）。模块保持 L2 | 2026-08-16 |
| `CURL-CSPEC-SNAPSHOT-0001` | P0 | READY (依赖已解锁，deliverable 未做，unassigned) | unassigned | curl两个worker携带并验证immutable cspec/pspec/SLA/catalog快照 | `examples/curl_decompile.rs`, paired driver文档, `tests/oracle/curl_cspec_snapshot_1204.metadata.json`, `tools/run_curl_cspec_snapshot_oracle.sh`, `docs/TODO_BOARD.md` | 依赖`CSPEC-TEXT-INGEST-0001`核心链已于 7721b0d 落地（真实 cspec 字节→parse_compiler_config→defaultfp registry，cspec_text_ingest 权威 runner MATCH）→解锁。**注意 FUNCPROTO-MODEL-BIND-0001 已先行接入过渡态**：两 worker 经 `worker_architecture()` 从 cwd 读 `sleigh_specs/x86-64-gcc.cspec`（OnceLock 每进程一次）构造 local Architecture——本行 deliverable 仍是把 bytes+SHA256+language/compiler/options/catalog 指纹放进 request、worker 重算后构造 local candidate、全部成功才发布，并消除 cwd 读；GENERAL/FLOAT/RSP 硬编码禁令不变（现有 `WorkerSpecHost` 空间表/寄存器来自真 .sla，无 cspec 硬编码）。跨 worker Arc 注意：Arc 身份域=单 worker Architecture；未来跨 worker FuncProto model 比较须按 worker 域隔离（Arc::ptr_eq 跨 worker 恒 false 假阴性） | 2026-08-17 |
| `ADDRESS-0001` | P0 | PHASE1-DONE| address_phase1_wave | Address 空间维奠基（分三阶段）：阶段一=legacy `Address` 兼容空间字段+核心比较链+桥接 API | `src/address.rs`, `docs/api/address.md`, `tests/oracle/address_compat_order_1204.{cc,rs,metadata.json}`, `tools/run_address_compat_order_oracle.sh`, `tools/run_address_space_phase1_oracle.sh`, `tests/oracle/address_space_phase1_1204.{cc,rs,metadata.json}`, `docs/alignment_docs/ADDRESS_SPACE_PHASE_PLAN.md`, 本 TODO 行（消费方迁移全部留阶段二登记，不越权触碰） | evidence=`pending root commit（本 agent 禁 commit）`。阶段一完成（2026-08-17，WIP 在树）：①代码——`Address{offset,space:Option<SpaceTag>}`（Copy intern tag→thread-local 强句柄表，保 Copy=55 消费文件零改动；强句柄防 identity_ptr 复用；thread-local 对齐 SPACE-0001 的 Rc 单线程残差，跨线程 resolve panic=响亮失败）；比较链 address.hh:356/375——`Eq`=(tag,offset)、`Ord`=None 先行[null-base 规则 :377]→index[:389]→offset[:391]→tag tiebreak、`Hash` 随 `Eq`、None↔None offset-only=存量行为零变化；`Some` 时 offset/next/prev 经 wrap_offset（:423/433）、overlap 同空间+constant 排除+wrap（cc:153-165）、Display 走 printRaw（None 保持历史 `0x{:x}`）；桥接 with_space/get_space/from_space_address（m_maximal panic，静默映射会翻转排序）/to_space_address。②fixture——`address_compat_order_1204` 6 case 36 行（null-base None 模型等/序、跨空间 sorted walk null 首位、同空间 offset 序、wrapOffset 算术、overlap 门、SeqNum 委托序）权威 runner（pinned base 197b34c+工作树 address.rs overlay+离线 vendored 构建+读回哈希校验）`MATCH covered_projection=6/6 overall=PARTIAL_MATCH stdout_sha256=ecf18075…`；None↔None 兼容无 oracle 对应物（Ghidra 无无空间地址）=4 个单侧回归测试，不升 MATCH。③零回归——冻结树 A/B：E2E stdout sha `fdd216f7…` 逐字节相同；lib tests 1419→1423（+4 新增，5 预存失败不变）；当前混合树（含他人已集成 WIP）defects=0/numbering=0/Matched=123、gcc audit 16 FAIL（≤已知基线）；annotations/refs --strict 过。④设计——消费面清单+迁移纪律（**按比较域整体迁移，禁逐铸造点混铸**：None≠Some 同 offset 不等，混域=HashMap/BTreeMap 查找 miss）+阶段二域表（①SeqNum+BlockBasic[解 SPACE-IOP-PRINTRAW 两终态渲染，铸造点 funcdata.rs/flow.rs]②context partition/trackbase[解 SLEIGH-0002C 跨空间交错]③VarnodeBank loc+VarnodeData[VARNODE-0001]④comment/override/jumptable/flow ⑤database 符号域[DB-LOCALSCOPE-MAP]）+阶段三合一消亡+R1-R7 风险。**直接受益接线点偏离登记**：任务简报示例（IopSpace printRaw op 序号分支）未接线——铸造点在 funcdata.rs（他人租约禁域），无 Some-space SeqNum 时接通只渲染 legacy hex=伪收益；op.rs stub 已就位两形式逐行注释，留阶段二 wave ①。残差：fixture_registry.json 登记=root；他方衍生同主题未跟踪文件 `ADDRESS_SPACE_PHASES.md` 与本设计文档并存，请 root 二选一；thread 域 tag 约束写入阶段二纪律。**接续 agent 补充（2026-08-18，address_space_phase1 交付）**：①阶段一补完——`is_invalid`（address.hh:285 null-base 镜像，`None`≡Ghidra invalid，`Address::new(0)`≡`m_minimal`）入 address.rs+address.md；②独立第二组 fixture `tests/oracle/address_space_phase1_1204.{cc,rs,metadata.json}`+`tools/run_address_space_phase1_oracle.sh`（4 case 35 行：none_compat_fallback[含 m_minimal≡null-base-0、null vs 真空间 meeting 规则]/space_ordering[10 元素含 null 混入 walk]/tag_identity[re-fetched handle clone re-intern=指针身份、HashSet/去重、双 8 字节空间同 offset printRaw 同形不等]/wrap_overlap_tagged）权威 runner exit0 `MATCH covered_projection=4/4 overall=PARTIAL_MATCH stdout_sha256=87f5aa15…`（pinned base 197b34c+address.rs overlay，私有槽构造=C++ null-base+offset 直写、观察全走公共算子）；③隔离 A/B 零回归复证（共享树被并发 printc.rs/datatype.rs 中间态阻断编译）：pinned-base worktree 197b34c vs +address.rs overlay——curl E2E stdout 逐字节相同、lib 1419→1423（+4，5 预存失败两侧同）——与本行既有 A/B 独立复现；④设计文档 `docs/alignment_docs/ADDRESS_SPACE_PHASES.md` 独立成文，另含 §1.3 哨兵破坏面清单（heritage `range(..=Address::new(u64::MAX))` 上界/funcdata symbol_table miss 哨兵/funcdata vbank `Address::new(u64::MAX)` loc 哨兵/`comment_sort_key` u64 元组/SeqNum 冒号串 encode 歧义）与 §6 决策记录（None 排最前的 null-base 依据/tiebreak 保 Ord-Eq 一致性）。与 `address_compat_order_1204`/`ADDRESS_SPACE_PHASE_PLAN.md` 并存=root 去重决策（两组 runner 可独立复跑；write-set 重叠=src/address.rs+docs/api/address.md，集成时串行重钉） | 2026-08-17 |
| `SEQNUM-0001` | P0 | PARTIAL_MATCH | seqnum_foundation_wave | immutable uniq/time identity + mutable block order及调用闭包 | `src/{address,op,varnode,graph,unionresolve,variable,funcdata}.rs`, `docs/api/{address,op,varnode,graph,unionresolve,variable,funcdata}.md`, `tests/oracle/seqnum_identity_1204.{cc,rs,metadata.json}`, `tools/run_seqnum_identity_oracle.sh`, `docs/TODO_BOARD.md` | evidence=`this commit`；锁定12.0.4目标投影stdout=`1b05e4d1…`逐字节MATCH，覆盖70次block order重排后identity/bank lookup不变、异Address同time的显式`same_identity`、graph time、ResolveEdge键与HighVariable naming time；immutable closure独立 `Cross-Review: APPROVE`。Ghidra copy ctor不复制order而Rust `Copy`复制全字段、OPBANK lifecycle、Flow `VisitStat`、align/FFI wire identity、AddressSpace及overflow均精确保留`MISMATCH/UNTESTED`，模块保持L2 | 2026-08-13 |
| `RANGEADDR-0001` | P0 | BLOCKED | unassigned | space-aware Range/RangeList | `src/address.rs`, API/oracle | 依赖 `ADDRESS-0001`；相邻不合并、跨 space 分离、完整区间 inRange | 2026-08-11 |
| `BLOCK-0001` | P0 | BLOCKED | unassigned | FlowBlock core + atomic bidirectional edges | `src/block.rs`, `src/funcdata.rs`, API/oracle | 依赖 `ADDRESS-0001` + `SEQNUM-0001`；exact flags/parent/reverse-index/false-first/RPO/loop/dom/marshal | 2026-08-11 |
| `VARNODE-0001` | P0 | BLOCKED | unassigned | VarnodeBank keys/canonicalization/query | `src/varnode.rs`, API/oracle | 依赖 `ADDRESS-0001` + `SEQNUM-0001`；unique base、constructor、loc/def comparator、xref/makeFree/destroy | 2026-08-11 |
| `OPBANK-0001` | P0 | BLOCKED | unassigned | nullable slots/occurrence reads/op lifecycle/IOP | `src/op.rs`, `src/funcdata.rs`, `src/varnode.rs`, API/oracle | 依赖 `VARNODE-0001`；transactional set/unset/output/destroy + deadandgone，移除 Arc::from_raw UAF | 2026-08-11 |
| `CONSTSEQ-0001` | P1 | BLOCKED | unassigned | `Array/String/HeapSequence` storage/order/transform closure | `src/constseq.rs`, Rule/action consumers, API/路线图/oracle | 依赖 `SPACE-0001` + `OPBANK-0001` + `USEROP-0001`；wordsize 转换、encoded space identity、previousOp 顺序、dead outputs 与 CALLOTHER transform 同输入对拍 | 2026-08-12 |
| `MEMSTATE-0001` | P0 | BLOCKED | unassigned | `MemoryBank/MemoryState` exact space/Translate/error state | `src/memstate.rs`, emulate/loadimage consumers, API/路线图/oracle | 依赖 `SPACE-0001` + `ADDRESS-0001` + `ARCH-0001`；命名寄存器必须经 Translate 而非 hash，动态 space/wordsize/endian/异常/overlay mutation 同输入对拍 | 2026-08-11 |
| `VARIABLE-0001` | P0 | BLOCKED | unassigned | HighVariable attach/detach/ownership/order | `src/variable.rs`, `src/varnode.rs`, API/oracle | 依赖 `OPBANK-0001`；Weak ownership、annotation/later VN、sorted instances、dirty propagation | 2026-08-11 |
| `COVER-0001` | P0 | BLOCKED | unassigned | endpoint identity + CFG cover rebuild | `src/cover.rs`, `src/varnode.rs`, `src/merge.rs`, API/oracle | 依赖 `BLOCK-0001` + `VARIABLE-0001`；wrapping cover、predecessor recursion、PcodeOpSet/HighIntersect | 2026-08-11 |
| `DBSYM-FLAGS` | P0 | READY | unassigned | Symbol/Varnode canonical flag namespace | `src/database.rs`, `src/varnode.rs`, API/oracle | NAMELOCK|READONLY 必须是 0x2200，当前 0x6 被解释为 CONSTANT|ANNOTATION | 2026-08-11 |
| `DATABASE-0001` | P0 | BLOCKED | unassigned | SymbolKind identity + addMap/rangemap/query + pipeline | `src/database.rs`, `src/rangemap.rs`, `src/funcdata.rs`, actions/API/oracle | 依赖 `DBSYM-FLAGS` + `ADDRESS-0001` + `TYPE-0001`；最后接 ScopeLocal/newVarnode/Dynamic Actions | 2026-08-11 |
| `FSPEC-0001` | P0 | IN_PROGRESS | root | FuncProto lock/void/model state | `src/fspec.rs`, `docs/api/fspec.md`, locked oracle fixture, `docs/TODO_BOARD.md` | evidence=`this commit`；`tools/run_funcproto_lock_oracle.sh` 对锁定 12.0.4 同输入 stdout direct diff=`MATCH`，覆盖 fresh/zero-input lock/clearUnlocked/copy/clearInput/output lock-unlock 及 model lock；全库 1303 tests PASS。`compat_model` identity 等其余 FuncProto 状态仍留后续闭环，模块保持 L2 | 2026-08-12 |
| `FSPEC-0002` | P0 | BLOCKED（部分旧缺口已闭合） | unassigned | ParamEntry/ParamList 非标量、join、hidden-return 与错误部分状态 | `src/fspec.rs`, `src/type_system/protomodel.rs`, API/oracle | slotbase=1、永久 ParamActive、piece/protoparam flags `this=1/hidden=2/indirect=4/name=8/type=16` 与 selected scalar assignment 已修。剩余：Architecture-owned Address 身份、ModelRules、reverse-justified/endian、join/overlap group 顺序、hidden-return canonical pointer/TypeFactory identity、decode/assign 异常后的完整突变；依赖 `FSPEC-0001` + `ADDRESS-0001` + `CSPEC-MODELRULE-DECODE-0001`。2026-08-28 |
| `PARAM-RECOVERY-0001` | P0 | BLOCKED | unassigned | `ActionInputPrototype` / `ActionUnjustifiedParams` / own-function `FuncProto` recovery | `src/{coreaction,action,fspec,funcdata}.rs`, `src/type_system/protomodel.rs`, paired API, locked oracle fixture, curl differential | 依赖 `SPACE-0001` + `ADDRESS-0001` + `ARCH-0001` + `FSPEC-0001/0002`；本轮已让兼容 `ActionInferParams` 尊重 imported locks，DWARF 已知原型不再膨胀；但 stripped/unlocked 路径仍以 live-in heuristic 代替 Ghidra 的 `possibleInputParam→ParamActive→resolve/derive→updateInputTypes`，必须对拍完整 prototype/IR mutation并删除硬编码 count/type/ABI 表；诊断见 `FUNCTION_SIGNATURE_PARAMETERS_2026-08-12.md`，B2=`NO_ORACLE` | 2026-08-12 |
| `DWARF-PROTO-0001` | P0 | IN_PROGRESS | root | ELF DWARF subprogram/formal-parameter/varargs → locked `FuncProto` | `Cargo.{toml,lock}`, `src/debugproto.rs`, `src/lib.rs`, `docs/api/{debugproto,lib}.md`, `examples/curl_decompile.rs`, importer regression, `docs/TODO_BOARD.md` | evidence=`this commit`；实现 Ghidra 的“Program DB 已知原型”前端边界：解析 definition/abstract-origin/specification、name/type/order/varargs，以锁定 x86-64 gcc cspec 的资源顺序赋寄存器 storage，再在 Action 前锁定 `FuncProto`。curl input SHA256=`4ee4002b...c6b5d1a`；Rust regression 覆盖 `GetStr=2`、`myprogress=5`、`helpf=1+...`、`getparameter=4`、`hugehelp=0 locked`；由于尚无真实 Java DWARF analyzer 同输入 fixture，importer B2=`NO_ORACLE`。跨 CU/location-list/stack/aggregate/split-DWARF/non-x86 与 stripped recovery 分别留本项 residual/`PARAM-RECOVERY-0001`，不得升 L3 | 2026-08-12 |
| `PRINT-SIGNATURE-0001` | P0 | IN_PROGRESS | root | `PrintC::docFunction` → canonical `emitFunctionDeclaration` | `src/printc.rs`, `docs/api/printc.md`, locked prototype/text fixture, curl differential, `docs/TODO_BOARD.md` | evidence=`this commit`；删除 production `main` 特判、任意 RAX-write return heuristic 和 empty-proto 六寄存器 fallback，直接消费 finalized `FuncProto`。最新 `result/curl_cur.c` SHA256=`01b5eabe...d7aa68`，24/24 反编译、11.3.2 诊断 golden `defects=0,numbering=0,skeleton=2735`；但 12.0.4 prototype/text fixture 尚缺，B2=`NO_ORACLE`。gcc audit 仅 5/23 syntax OK，剩余正文/类型声明问题与 `match_url` aggregate-stack 参数分别绑定既有 Print/Type/Address/FSPEC 缺口，模块保持 L2 | 2026-08-12 |
| `TYPE-0001` | P0 | BLOCKED | unassigned | exact Datatype model + stable TypeFactory identity | `src/type_system/datatype.rs`, `typefactory.rs`, consumers/API/oracle | 依赖 `SPACE-0001`；submeta/virtual compare、arena、dual indices、findAdd、recursive completion、exact-piece、codec | 2026-08-11 |
| `HERITAGE-0001` | P0 | BLOCKED | unassigned | canonical single-pass SSA driver + ActionStart/ActionHeritage | `src/heritage.rs`, `src/coreaction.rs`, `src/funcdata.rs`, API/oracle/differential | 依赖 `ADDRESS/BLOCK/VARNODE/OPBANK/COVER`；先消除 canonical 锁重入，再补 collect/refinement/guards/ADT，最后删除 direct 双 pass | 2026-08-11 |
| `CONDEXE-0001` | P0 | BLOCKED | unassigned | ConditionalExecution/RuleOrPredicate exact CFG rewrite | `src/condexe.rs`, `src/expression.rs`, Funcdata/Block API/oracle/differential | 依赖 `OPBANK-0001` + `BLOCK-0001` + `HERITAGE-0001`；true/false slots、异常、storage/order、Action stage/count | 2026-08-11 |
| `PATHMELD-0001` | P0 | BLOCKED | unassigned | parent/SeqNum ordered path meld + cutoff/truncate | `src/jumptable.rs`, op/block API/oracle | 依赖 `BLOCK-0001` + `OPBANK-0001`；MARK 必须进 PcodeOp flags，删除自创 LOAD 例外 | 2026-08-11 |
| `JUMPTABLE-0001` | P0 | BLOCKED | unassigned | Override/Basic/Basic2/Assisted model recovery closure | `src/jumptable.rs`, flow/action/emulate/userop API/oracle/differential | 依赖 `RANGE-0001` + `PATHMELD-0001` + `ADDRESS/BLOCK/OPBANK` + `INJECT-0001`；先 trialNorm/findStartOp，再 model selection/guards/normalization | 2026-08-11 |
| `PRINT-RPN-0001` | P0 | IN_PROGRESS | root | invisible groups + complete OpToken/RPN/CFG emission | 由 `PRINT-RPN-0001A` 起按表达式分组 → terminal mask → CFG 单次发射拆分原子闭环 | exact group IDs；全部算术/CALL/subscript/STORE走 RPN；terminal mask 生效；删除 fresh-loop 重发；主管线 cross-review | 2026-08-12 |
| `PRINT-RPN-0001A` | P0 | DONE | root | `PrintLanguage::pushOp/pushAtom` invisible group 文本语义 | `src/prettyprint.rs`, `src/printlanguage.rs`, `docs/api/{prettyprint,printlanguage}.md`, `tests/oracle/printlanguage_group_1204.{cc,rs,metadata.json}`, `tools/run_printlanguage_group_oracle.sh`, `tests/oracle/fixture_registry.json`, `docs/TODO_BOARD.md` | evidence=`this commit`；锁定 12.0.4 `pushOp/pushAtom` 同输入 stdout direct diff：root unary/binary、无括号嵌套、必须括号嵌套 visible text 全部 `MATCH`；fixture overall=`UNTESTED`（exact TokenSplit group ID/queue）；curl 24 函数回归输出无变化，证明剩余 116 个 `(bVar);` 来自 terminal-branch 重发而非 invisible group | 2026-08-12 |
| `PRINT-RPN-0001B` | P0 | DONE | root | `PrintC::emitBlockBasic` terminal/no-branch 选择语义 | `src/printc.rs`, `docs/api/printc.md`, `tests/oracle/printc_terminal_1204.{cc,rs,metadata.json}`, `tools/run_printc_terminal_oracle.sh`, `tests/oracle/fixture_registry.json`, `docs/TODO_BOARD.md` | evidence=`this commit`；锁定 12.0.4 actual `PrintC::emitBlockBasic` 六场景 statement-selection/order direct diff=`MATCH`，raw text=`MISMATCH`（`(true)` vs `(vn_1)`），overall=`MISMATCH`；curl 24 函数中孤立 `(bVar);` 116→27，11.3.2 诊断 skeleton 2874→2747、defects/numbering 均0；三次新输出均27条但 SHA 不同且 gcc=8/10/10，非确定性另立 `PRINT-DETERMINISM-0001`；完整表达式/markup/CFG 单次发射仍归父项，模块保持 L2 | 2026-08-12 |
| `PRINT-RPN-0001C` | P0 | DONE | root | `PrintC::docFunction` → `PrintC::emitBlockGraph` 单次顶层遍历 | `src/printc.rs`, `docs/api/printc.md`, `tests/oracle/printc_blockgraph_1204.{cc,rs,metadata.json}`, `tests/oracle/printc_terminal_1204.metadata.json`, `tools/run_printc_blockgraph_oracle.sh`, `tests/oracle/fixture_registry.json`, `docs/TODO_BOARD.md` | evidence=`this commit`；锁定 12.0.4 actual `docFunction` 仅调用一次 `emitBlockGraph`，后者按 `getList()` 顺序逐项一次 virtual emit；fixture list-order/single-dispatch=`MATCH`、overall=`MISMATCH`（完整 docFunction 对象图未闭合）；curl 顶层 `do {` 16→8，`__libc_csu_init` 的 return 后重复 loop 消失，孤立 `(bVarN);` 27→26，11.3.2 诊断 skeleton 2747→2729–2730、defects/numbering=0；三次 SHA 仍不同且 GCC=10/9/10，继续绑定 `PRINT-DETERMINISM-0001`，模块保持 L2 | 2026-08-12 |
| `PRINT-DETERMINISM-0001` | P0 | READY | unassigned | PrintC 跨进程名称/调用目标输出非确定性 | `src/printc.rs`, `src/printlanguage.rs`, symbols/variable naming dependencies, paired API, locked oracle fixture, curl determinism runner, `docs/TODO_BOARD.md` | 由 `PRINT-RPN-0001B` 回归发现：同一 release binary、输入和选项连续三次 SHA 为 `9a1b43cf…/be4d5889…/7fb9b064…`，`hugehelp` 在 `FUN_35/puts` 与错误 `puts/local_35` 分配间漂移，gcc 8/10/10；先锁定 Ghidra `assignDefaultNames`/symbol traversal 的容器顺序与单一计数器，再对拍完整名称、调用目标、声明顺序和重复运行哈希；不得用 HashMap seed 或文本后处理稳定化 | 2026-08-12 |
| `PRINTC-FORMAT-0001` | P1 | REVIEW | printc_format_wave | `PrintC::docFunction` 头部/花括号/参数空格 + `Emit::openBraceIndent/closeBraceIndent` 纯格式层 ↔ `src/{printc,prettyprint}.rs` | `src/printc.rs`, `src/prettyprint.rs`, `docs/api/{printc,prettyprint}.md`, `tests/oracle/printc_format_1204.{cc,rs,metadata.json}`, `tools/run_printc_format_oracle.sh`, 本 TODO 行 | evidence=`pending root commit`（write-set 未 commit，禁止本 agent 执行 git add/commit）；实现：`BraceStyle`+`Emit::open/close_brace_indent`（prettyprint.cc:61-76/prettyprint.hh:481-483）、`option_brace_func=skip_line`（printc.cc:1590/2655/2662）、参数 comma spacing=0（printc.cc:57/2233/2252）、type-OpToken join（printc.cc:73-77，`char *pattern`/`char **argv`/`int argc`）+`normalize_pointer_run`、post_process 第八趟分隔行保真+两行式签名检测（pass 11/17/18/19/orphan-breaks/backfill）+指针模板 join 修正。fixture `tools/run_printc_format_oracle.sh`（cover_rebuild，pinned base=a51e0c5）root 复验 `MATCH covered_projection=6/6 overall=PARTIAL_MATCH stdout_sha256=de0fd94e…`（六 case 函数头花括号/参数 join/逗号/缩进双侧逐字节一致）。差分门禁：`cargo run --release --example curl_decompile` 重生 + `compare_ghidra.py result/curl_cur.c tests/golden/ghidra_curl_1204.c --summary-only` = **skeleton 4530→3996（−534）、numbering 126→6、defects 0→0**（`__libc_csu_fini` diff=0；main 485→1072 系并发语义层变更——bool 声明爆炸/参数类型推断归 VARMAP/Type 域，非格式层）；numbering 大幅改善系修复 backfill 在 skip_line 布局下 declared 集为空导致的全量重复声明。`cargo test --lib printc::`=8/8 绿、`prettyprint::` 无测试但全库 1350 passed/6 failed 全部位于 comment/dynamic/funcdata/merge/ruleaction（并发 agent 域，与 printc/prettyprint 无代码依赖，pinned-base 对照见报告）；annotations `--all` ✅、refs `--all --strict` ✅。剩余格式残差：函数间双空行需 examples harness（`getC()` 尾 `\n` 归脚本层）、`/* WARNING */` 头注释归 comment-db 域、`char *N` 类型改写本身归 fix_unary_deref 停通行 | 2026-08-15 |
| `PRETTY-0001` | P1 | BLOCKED | unassigned | TokenSplit/Oppen queue + semantic markup payload | `src/prettyprint.rs`, `src/printlanguage.rs`, API/oracle/differential | 依赖 `PRINT-RPN-0001`；保留 op/vn/type/field/case identity、spaces+bump、line-width break/indent | 2026-08-11 |
| `ARCH-0001` | P0 | BLOCKED | unassigned | production Architecture ownership/init/decode | `src/arch.rs`, Funcdata/Flow/CLI/examples/API/oracle | 依赖 `SPACE-0001` + `SLEIGH-0001` + `TYPE-0001`；必须安装 loader/types/userops/cpool/pcodeinjectlib，禁止裸 None 容器冒充接线 | 2026-08-11 |
| `USEROP-0001` | P0 | BLOCKED | unassigned | exact derived userop registry + Segment/JumpAssist consumers | `src/userop.rs`, `src/coreaction.rs`, API/oracle/differential | 依赖 `ARCH-0001`；全局 selector/name冲突、builtin flags、架构特定 segment execute、真实 ActionSegmentize | 2026-08-11 |
| `INJECT-0001` | P0 | BLOCKED | unassigned | payload decode/registry/temp allocation + Flow injection | `src/pcodeinject.rs`, `src/flow.rs`, `src/arch.rs`, API/oracle | 依赖 `ARCH-0001` + `USEROP-0001` + `OPBANK-0001`；CALL/CALLOTHER/entry/return 全路径有序注入，禁止 HashMap 首项 | 2026-08-11 |
| `TIMEOUT-8FN-0001` | P0 | AUDITED | timeout8_diag | 全量 E2E 8 函数 >10s worker 隔离超时（my_get_line 0x3840 / helpf / file2string.part.0 / parseconfig 族等，历史上能完成）只读诊断与归因 | 只读：`examples/curl_decompile.rs`、HEAD git 历史、`src/**` 现状；实验全部在 /tmp 副本（临时补丁/gdb 栈采样/A-B 单文件回退）；write-set 仅本 TODO 行 | **诊断完成（2026-08-16，/tmp 快照+AB1/AB2 因果实验）**。8 函数=main(0x25a0)/my_get_line(0x3840)/helpf(0x3980)/file2string.part.0(0x3a90)/getparameter.constprop.0(0x3f00)/glob_word(0x4a60)/glob_set(0x4bc0)/glob_range(0x4d60)。**因果链：HEAD 2a0010a（含 LINKSYMBOL 0e4c6f4/PRINTC df0da85/ADT c96f699/INPLACE 3cee3ac/EXTERNAL 83360fc 全部 5 提交）全量 E2E=124/124、0 超时**；冻结工作区快照（DRIVER-SWITCH 未提交改动）=8 超时/68 反编译；AB1=快照+仅回退 coreaction.rs 到 HEAD → **0 超时/76 反编译** ⇒ 引入者=未提交的 ActionHeritage→`fd.op_heritage()` 每 mainloop 迭代重跑切换，5 个近期提交全部无罪。**7/8 函数同一卡点（gdb 栈采样×7 全一致）**：`RuleSubCancel::applyOp` INT_AND 分支 ruleaction.rs:5867 在块作用域 `let op=op_arc.read()` 读锁仍存活时调 `fd.op_set_input`（funcdata.rs:1194 取同 op 写锁）→ RwLock 非重入 futex 永久自死锁（Ghidra ruleaction.cc:5137 无锁、cc:5186-5189 直接 opSetInput；该缺陷 HEAD 即存在=预存潜伏，被新 canonical heritage 产生的 SUBPIECE(INT_AND mask, thru non-free) IR 形状揭出）。AB2=快照+单行 `drop(op)`（5866 前）→ **8 超时→1 超时+1 panic、74 反编译**；6 函数直接达标（my_get_line 3.23s/file2string 1.35s/getparameter 1.69s/glob_word 4.86s/glob_set 2.86s/glob_range 1.79s，全 <5s、direct/isolated MATCH，12.0.4 golden defects=0/numbering=0）。**main=不同机制**：非死锁，CPU churn——栈采样 10/22/30s 全在 `coreaction.rs:35 ActionHeritage→op_heritage→heritage.rs:3983 heritage→rename(4380)→rename_recurse(4551)→delete_varnode→destroy_varnode_prevalidated(varnode.rs:2471 BTreeSet::retain)`、45s 在 ActionDeadCode op_destroy 同族；direct 50.6s 收敛（HEAD 1.47s，34×）=每次 mainloop 迭代重跑 heritage 的 rename/destroy O(N) retain churn。**helpf 在 AB2 解锁后暴露下一层**：`normalize_read_size`（heritage.rs:1684→1559 `fd.op_set_output`）对非 free vn r0x39a5 触发 funcdata.rs:1317 opSetOutput 前置断言 panic（前置 WARNING "Heritage AFTER dead removal" register:0:1(i)）——位于 DRIVER-SWITCH 自己的 write-set（canonical heritage 收敛链）。修复拆分：①`RULE-SUBCANCEL-RWLOCK-0001`（ruleaction.rs:5842-5868 读锁存活期禁同 op 写锁，5866 前 drop(op) 或重构先取 owned 值；验收=7 函数 <5s+defects=0；ruleaction.rs 不在 DRIVER-SWITCH 租约，须独立认领或租约协调）；②main churn 归 `HERITAGE-DRIVER-SWITCH-0001` 收敛验收（其 124/124 底线已覆盖）；③helpf panic 归 `HERITAGE-DRIVER-SWITCH-0001`（heritage.rs write-set 内）。**与 DRIVER-SWITCH 关系：8 超时全部由其在途未提交改动引入/揭出；其中 6 函数经 ①后立即恢复，②③归其自身验收**。同类隐患族：ruleaction.rs 137 个 apply_op 中 117 处粗扫命中"guard 后 fd.op_*"模式（多数为块结束释放/目标不同 op 的误报，如 RuleDumptyHump 安全），建议 ①时附带机械审计（debug 断言 fd.op_* 不在 op 读锁存活期执行） | 2026-08-16 |
| `RULE-SUBCANCEL-RWLOCK-0001` | P0 | DONE | rule_subcancel_wave | `RuleSubCancel::applyOp`（ruleaction.cc:5119-5181 无锁直接 opSetInput）↔ `RuleSubCancel::apply_op`（ruleaction.rs:5842-5868 INT_AND 分支读锁存活期调 `fd.op_set_input` 同 op 写锁）自死锁修复 + ruleaction.rs 137 个 apply_op 同类模式机械审计 | `src/ruleaction.rs`, `docs/api/ruleaction.md`, 本 TODO 行 | evidence=`pending root commit`；修复=两阶段重构（读锁作用域仅收集 owned 值/AND-cancel 决策，锁全释放后 mutate，判定顺序同 cc:5136-5146）；机械审计（named read guard 存活期 × fd.op_* 同 op 写目标，守卫生命周期=块末−drop/shadow，PcodeOpRef/clone 别名归一，`.0.read()` 守卫含入）在 137 个 apply_op/276 个 named guard 上 **唯一真实命中=本处**（对 HEAD 旧文件跑同一分类器精确复现 :5867 命中=self-test），修复后 0 命中；`total_replace` 4 处隐藏写目标（PushMulti:5059/MultiCollapse:7584,7599/IndirectCollapse:12121，写锁 vn 全部后代 op）逐一人工核实无存活守卫并加注释守卫；6 处未解析目标全为 fd.new_op 新 op/block/Vec 参数=安全。验收：6/8 隔离函数=my_get_line 4.20s/file2string.part.0 1.37s/getparameter.constprop.0 1.78s/glob_word 4.83s/glob_set 3.73s/glob_range 1.95s 全 <5s 全 direct/isolated MATCH；helpf 如预告 panic 于 funcdata.rs:1317 opSetOutput 断言（前置 "Heritage AFTER dead removal" WARNING r0x39a5）——**不越租约修，移交 HERITAGE-DRIVER-SWITCH-0001**；main 超时同属 DRIVER-SWITCH churn 域不要求。全量 E2E 43s：68→74 反编译（+6=诊断 6 函数逐一确认、零丢失），12.0.4 golden 差分 defects=0（0/122）、numbering 7→20 增量全部位于 6 个新解锁函数（既有函数 1/2/2/2 逐字不变）、skeleton 2010→4368 增量=新函数自身 diff（2365/2358）；`cargo test --lib`=1357 pass/5 fail 与 HEAD-ruleaction 基线**逐条同集**（comment/dynamic/funcdata×2 并发域 + ruleaction::test_rule_add_unsigned_fires 在 HEAD 同失败=预存）；annotations `--all` ✅、refs `--all --strict` ✅ | 2026-08-16 |

### 已完成发现审计（本 wave）

- `cargo test --lib`：1292 passed / 0 failed / 3 ignored；`cargo check --lib` 通过。该证据只证明 Rugra 回归基线，不是 oracle parity。
- 注释门禁已收口：旧 scanner 初检 22 文件/249 项，fcc5142 后基线为 21 文件/247 项；
  修复 scanner 后又发现 85 项 const/extern/restricted/单行函数。当前 94 文件零违规。
- 门禁健康已修复：`core.hooksPath=.githooks`，两 hook 为 100755，ZCode 使用项目相对
  `python3` + `startup|resume`，Evidence 严格要求 4/4，CI 复跑锁定 oracle 全套门禁。
- 路线图至少 9 个模块存在“代码自承 stub/简化，但文档标 L3”的反证；未复核前不得沿用这些 L3 声明。
- `PcodeSnippet` 实测会接受缺失 `]`/`;` 的非法语句并返回成功；反例和完整修复边界见
  `docs/alignment_audit/PCODEPARSE_SYNTAX_2026-08-11.md`。
- Action executor/tree、callspec、Pcode flags/opcode protocol、SLEIGH、compression、multiprecision、ledger 与 gate 的
  确定性反例见 `docs/alignment_audit/FOUNDATION_PIPELINE_2026-08-11.md`；未带 runtime fixture 的项目保持
  `MISMATCH / NO_ORACLE`，不冒充 MATCH。
- Space/Address/SeqNum、Block、Varnode/PcodeOp、Cover、Database/Fspec、Marshal 与 TypeFactory 的底向上依赖图见
  `docs/alignment_audit/CORE_FOUNDATIONS_2026-08-11.md`；这些模块的历史 L3 已撤回。
- Heritage/CondExe/PathMeld/JumpTable、Print RPN/PrettyPrint、Architecture/UserOp/PcodeInject 的生产可达性与输出链审计见
  `docs/alignment_audit/CONTROL_OUTPUT_PIPELINES_2026-08-11.md`；无锁定同输入 fixture 的结论统一保持 `NO_ORACLE`。
- PackedDecode 的四位置状态机、嵌套 close/skip、typed error/EOF 与 raw string 反例见
  `docs/alignment_audit/MARSHAL_PACKED_2026-08-11.md`；现有 happy-path self-roundtrip 测试不构成 wire 证据。

---

## 当前阶段目标

### Phase A：文档可信化与索引修复

- [x] 重写根目录与 `rugra/` 子项目之间的文档边界说明，避免把工作区描述成不存在的主工程。
- [x] 为 `docs/` 建立清晰的文档索引，说明每个目录/文件的作用、适用范围、维护责任和更新触发条件。
- [x] 清理 README、状态报告、路线图、对齐报告中的夸大结论、未验证能力和过期叙述。
- [x] 统一“已实现 / 部分实现 / 计划中 / 未验证”的措辞标准，禁止混用。
- [x] 修复 API 文档总索引
中的失真描述，移除“与源码 1:1 完全同步”之类无法保证的表述。

### Phase B：事实核验与结论分级

- [x] 对照 `src/` 实际模块，逐项核对 README、`docs/PROJECT_STRUCTURE.md`、`docs/api/README.md` 中的模块清单。
- [x] 对照 `src/bin/rugra.rs` 真实状态，修正所有关于 CLI 已完整可用的描述。
- [x] 对照 `src/lib.rs` 当前公开接口，修正库入口、示例代码、API 文档中的过期接口说明。
- [x] 对照 `src/align/runtime_verify.rs` 当前实现情况，修正“已完成验证能力”与“仅有框架/占位实现”的界限。
- [x] 对照现有测试与示例，重写“生产可用”“完全一致”“端到端已解决”等高风险结论。

### Phase C：维护流程固化

- [x] 建立"修改代码时必须同步检查哪些文档"的最小清单。
  - [x] 已新增 `docs/workflow/CODE_CHANGE_CHECKLIST.md`（2026-04-24）
- [x] 为新增/删除/重命名模块制定文档更新流程。
  - [x] 已纳入 `CODE_CHANGE_CHECKLIST.md` 的"必查项"表格
- [x] 为状态类文档增加"证据来源"要求：每个关键结论必须能回溯到代码、测试、示例或验证记录。
  - [x] 已在 `CODE_CHANGE_CHECKLIST.md` 建立证据来源规则和格式模板
  - [~] 为 `CURRENT_STATUS.md` 增加"证据来源 / Evidence Sources"段落或统一模板
  - [~] 为 `ALIGNMENT_PROGRESS.md` 增加"证据来源 / Evidence Sources"段落或统一模板
  - [~] 为 `docs/VERIFICATION_GUIDE.md` 增加"证据来源 / Evidence Sources"段落或统一模板
  - [x] 统一高风险结论的引用格式（代码 / 测试 / 示例 /日志 / 实验记录）
- [x] 为后续会话建立统一的文档审计模板，避免再次出现系统性失真。
  - [x] 已纳入 `CODE_CHANGE_CHECKLIST.md` 的"会话结束检查清单"
- [~] 为恢复 Ghidra 对齐工作建立“最小 P-code 对拍重入计划”，优先从单条指令 / 小型指令序列开始，而不是直接回到端到端大样本。
  - [x] 已确认最小闭环入口：`x86_lift.rs -> pcoderaw.rs -> funcdata.rs -> runtime_verify.rs -> ffi.rs`
  - [x] 已确定第一批最小指令样本优先级：
    - [x] `mov`
    - [x] `add`
    - [x] `sub`
    - [x] `and/or/xor`
    - [x] 简单 `cmp/jcc`
    - [x] 简单位移
  - [~] 为第一批样本建立可复现的输入/输出记录模板
    - [x] 定义统一字段：指令/序列、原始字节、Rugra 入口、Rugra 结果、参考结果、差异分类、当前层级、下一步修复点
    - [x] 已为 3 条首批样本建立模板化记录草案：`mov rbx, rax`、`add rax, 1`、`sub rax, 8`
    - [x] 已把 `mov rbx, rax` 推进成第一条最小可执行对拍记录说明
    - [x] 为 `mov rbx, rax` 补首条真实运行记录填写清单
    - [x] 继续把 `mov rbx, rax` 补成第一条真实运行记录
      - [x] 已新增最小测试：`funcdata::tests::test_mov_reg_reg_minimal_alignment_path`
      - [x] 已确认样本输入：机器码 `48 89 c3`，起始地址 `0x1000`
      - [x] 已确认 Rugra 路径：`disasm -> x86_lift -> inject_raw_ops -> verify_pcode_generation`
      - [x] 已确认当前结果：测试执行通过，`CPUI_COPY`、`rbx <- rax`、单 op、单 block
      - [x] 已确认当前限制：`verify_pcode_generation(...)` 仍使用框架级占位比较，执行时出现 `[RUGRA DIFF] 0x1000: Opcode mismatch. Ghidra Op: 0, Rugra has 1 ops here`
      - [x] 已把当前 Rugra op 的真实 opcode / 输出 varnode / 输入 varnode 列表传入比较入口，不再使用 `null inputs + 仅 input_count` 的占位调用
      - [x] 已让 `verify_pcode_generation(...)` 基于结构化局部比较结果返回 `Match` / `Mismatch(...)`，不再无条件返回 `VerifyResult::Match`
      - [x] 已让 FFI 比较入口返回结构化状态码，至少区分：`match`、`opcode mismatch`、`output mismatch`、`input count mismatch`、`input mismatch`、`missing Rugra op`
      - [x] 已让 `verify_pcode_generation(...)` 消费 FFI 结构化状态，不再只依赖 stdout 日志副作用
      - [x] 已开始第二条最小样本：`add rax, 1`
      - [x] 已新增最小测试：`funcdata::tests::test_add_rax_imm_minimal_alignment_path`
      - [x] 已确认样本输入：机器码 `48 83 c0 01`，起始地址 `0x1000`
      - [x] 已确认当前 Rugra lifting 结果：`INT_ADD` + `COPY`，共 `2` 条 op
      - [x] 已确认当前失败现象：最小测试执行失败，日志出现 `Opcode mismatch. Ghidra Op: 4` 与 unique 输入比较失败
      - [x] 已让 raw P-code 在 lifting 后补齐 `SeqNum(order)`，为同地址多 op 比较建立顺序基础
      - [x] 已把 FFI 比较入口从“仅按地址匹配”推进到“按地址 + `SeqNum.order` 匹配”
      - [x] 已开始函数级批量语义对齐骨架：新增 `src/align/function_snapshot.rs`
      - [x] 已为函数级语义快照建立核心结构：
        - [x] `FunctionSemanticSnapshot`
        - [x] `PcodeSnapshot`
        - [x] `CfgSnapshot`
        - [x] `SsaSnapshot`
        - [x] `BatchSemanticCompareReport`
      - [x] 已支持从 `Funcdata` 导出 Rugra 侧函数语义快照
      - [x] 已将 `function_snapshot` 接入 `align` 模块
      - [x] 已确认函数级快照基础测试通过：
        - [x] `align::function_snapshot::tests::test_snapshot_from_funcdata_basic`
        - [x] `align::function_snapshot::tests::test_batch_report_counts`
      - [~] 下一步：
        - [x] 已修正 `add rax, 1` 参考 opcode 来源错误（新增 `ffi::to_ghidra_opcode()`，修复 `verify_opcode()` 使用 `map_ghidra_opcode`）
        - [x] 已修正 unique 临时值输入的比较策略（`align/varnode.rs`、`ffi.rs`、`runtime_verify.rs` 均跳过 unique offset 精确比较）
        - [x] 已修正 space_id FFI 映射不一致（新增 `ffi::space_to_ffi_id()`）
        - [x] `add rax, 1` 测试已通过
        - [x] 已新增第三条最小样本 `sub rax, 8` 并验证通过
        - [x] 全部 138 测试通过，0 失败（2026-04-24）
        - [x] 在函数级快照骨架上补批量 runner 与导出流程
          - [x] 已补单函数快照 compare 入口
          - [x] 已补按函数入口地址配对的批量 compare runner
          - [x] 已补缺失 Rugra / 参考函数快照的批量差异记录
          - [x] 已细化各语义层差异明细：新增 `diff_pcode_ops()` / `diff_cfg_blocks()` / `diff_ssa_varnodes()`（2026-04-25）
        - [~] 真实 Ghidra 参考侧接入
          - [x] 已设计 Ghidra 侧 JSON 快照导出格式规范：`docs/method/ghidra_snapshot_format.md`
          - [x] 已创建手工示例 JSON：`docs/method/impl/ghidra_export_example.json`
          - [ ] 需要真实 Ghidra 环境验证导出脚本
        - [~] 内存指令（LOAD/STORE）对拍
          - [x] `mov rax, [rbx]` — LOAD + COPY（2026-04-25）
          - [x] `mov [rbx], rax` — STORE（2026-04-25）
          - [x] `mov rax, [rbx+0x10]` — INT_ADD + LOAD + COPY（2026-04-25）
          - [x] `add [rbx], rax` — LOAD + INT_ADD + STORE（2026-04-25）
        - [~] SSA 对拍
          - [x] SSA 单块线性测试 — 无 Phi 节点验证（2026-04-25）
          - [x] 修复 `Heritage` 的 `place_multiequals` 在处理不同 AddressSpace 但 offset 相同的 Varnode 时错误合并的问题（使用 `(AddressSpace, Address)` 复合键）
          - [x] 发现 heritage() deadlock，并已新增 `Funcdata::run_heritage_direct()` API 从架构层规避
          - [x] SSA 双块 Phi 测试（dominator tree 已就绪，已验证 `MULTIEQUAL` 节点按预期生成并落入 `Register` 空间）
          - [x] 修复 `rename_direct` 的 AddressSpace 碰撞 bug：将 stack key 从 `Address` 改为 `(AddressSpace, Address)` 复合键，与 `place_multiequals_direct` 保持一致（2026-05-09）
          - [x] SSA renaming 验证测试完成（2026-05-09）：
            - [x] 单块线性 renaming：验证 op 输入被正确重写为前序定义的 Varnode（`test_ssa_rename_single_block_linear`）
            - [x] 多块 Phi 输入填充：验证 MULTIEQUAL 输入从前驱块定义正确填充（`test_ssa_rename_multi_block_phi_inputs`）
            - [x] 菱形 CFG（if-then-else 合并）：验证 Phi 输入引用正确分支定义（`test_ssa_rename_diamond_pattern`）
            - [x] INPUT varnode 回退：未定义寄存器读取使用 INPUT varnode（`test_ssa_rename_input_varnode_for_undefined_read`）
          - [~] 下一步：SSA renaming 跨图验证（Ghidra 侧参考数据接入后）
          - [x] ActionNormalizeBranches 实现（2026-05-09）：
            - [x] 新增 `edge_flags` 模块（block.rs）：F_BREAK_EDGE / F_CONTINUE_EDGE / F_GOTO_EDGE
            - [x] 新增 `branch_type` 模块（op.rs）：PcodeOp.branch_type 字段，支持 NONE/BREAK/CONTINUE
            - [x] 增强 `collapse_loops`（blockaction.rs）：新增自然循环检测，支持多块循环体
            - [x] 实现 ActionNormalizeBranches（blockaction.rs）：标记 BRANCH/CBRANCH 为 break/continue
            - [x] 更新 PrintC（printc.rs）：根据 branch_type 输出 break/continue 而非 goto
            - [x] 新增 2 项测试，全部 159 测试通过（原 157 + 新增 2），0 回归
          - [x] Boolean Condition Folding 实现（2026-05-09）：
            - [x] 新增 `BlockCondition` 结构体（block.rs）：BoolOp::And / BoolOp::Or，first/second 子块
            - [x] 新增 `collapse_bool_conditions` pass（blockaction.rs）：Ghidra ruleBlockOr 等价实现
            - [x] 集成到 CollapseStructure.collapse_all（Pass 3），介于 collapse_conditions 和 collapse_sequences 之间
            - [x] PrintC 支持 BlockCondition 输出（printc.rs）：emit_block_condition 递归发射 (a) && (b) / (a) || (b)
            - [x] 新增 2 项测试，全部 161 测试通过（原 159 + 新增 2），0 回归
          - [x] HighVariable 寄存器命名初步实现（2026-05-09）：
            - [x] merge.rs 新增 `register_name()` 函数：根据 Varnode 的 offset 和 size 返回 x86-64 寄存器名（RAX, RDI, RSI 等）
            - [x] merge.rs `assign_names()` 使用 register_name 为 Register 空间的 HighVariable 生成人类可读名称
            - [x] curl 反编译输出从 uVarN 改为 RAX、RDI、RSI 等真实寄存器名
          - [x] CBRANCH-latch 循环检测初步实现（2026-05-09）：
            - [x] blockaction.rs `detect_cbranch_loops()` 新增：检测以 CBRANCH 结尾的 latch 块回边模式
            - [x] 真实 curl 二进制中成功检测出 do-while 循环
          - [x] ActionFinalStructure 初步实现（2026-05-09）：
            - [x] op.rs 新增 GOTO 常量（PcodeOp 操作码）
            - [x] blockaction.rs 新增 `tag_gotos()` 和 `remove_dead_code()`
            - [x] tag_gotos：将无法结构化的 BRANCH/CBRANCH 标记为 GOTO
            - [x] remove_dead_code：清除 BRANCH 后的不可达代码
            - [x] 全部 161 测试通过，0 回归
          - [x] Expression inlining for Unique-space temps（2026-05-09）：
            - [x] printc.rs: 新增 `inline_candidates` map，在 `doc_function` 中通过 use-count 分析构建
            - [x] printc.rs: 新增 `emit_inline_expr` 方法，递归发射 RHS 表达式
            - [x] printc.rs: `push_varnode` 检查 `inline_candidates`，命中则内联表达式而非输出 `uVar_xxx`
            - [x] printc.rs: `get_varnode_display_name` 对 inline candidates 返回空（不生成声明）
            - [x] 结果：curl 输出中 0 个 Unique-space `uVar_xxx` 残留（全部内联为表达式）
            - [x] 161 测试通过，0 回归
          - [x] Pointer type inference improvements（2026-05-09）：
            - [x] coreaction.rs: ActionTypeInfer LOAD/STORE 地址输入现在覆写非指针类型
            - [x] printc.rs: 声明类型解析优先选择指针类型而非 int
            - [x] 结果：curl 输出声明中可见 `void *` 和 `int *` 类型
          - [x] While-do loop detection verified（2026-05-09）：
            - [x] 基础设施已存在（3-phase collapse_loops）
            - [x] curl main loop 过于复杂，当前模式无法匹配（需要 interval analysis — 后续工作）
- [x] 为 `docs/api/` 建立统一文档状态标签规则。
- [x] 为历史会话日志建立复核提示策略与使用口径。
- [~] 将复核提示策略逐步回填到历史日志文件。
  - [x] 已为首批高风险历史日志补充或统一复核提示：
    - [x] `docs/archive/agentlog/engineering_progress_2026-03-07_api_docs_completion.md`
    - [x] `docs/archive/agentlog/engineering_progress_2026-03-08_zero_codegen_pipeline_resolved.md`
    - [x] `docs/archive/agentlog/engineering_progress_2026-03-08_output_refinement_and_quality.md`
  - [ ] 继续覆盖剩余高风险历史日志

---

## P0：最高优先级任务

- [x] **修复 `README.md` 的现状描述**
  - [x] 删除与当前代码不符的完整产品化叙述
  - [x] 明确区分“项目目标”和“当前仓库状态”
  - [x] 标注 CLI 当前受限/临时禁用事实
  - [x] 重新整理“已实现能力”与“开发中能力”

- [x] **修复 `docs/PROJECT_STRUCTURE.md`**
  - [x] 基于当前真实目录树重写模块说明
  - [x] 去除不存在或过时的 `analysis/`、`codegen/`、`pcode/`、`translator/` 主体叙述（若当前源码目录并不直接对应）
  - [x] 补齐 `align/`、`disasm/`、`type_system/`、`bin/` 等现存目录的职责说明
  - [x] 增加文档索引入口，说明 `docs/` 下各目录用途

- [x] **修复 `CURRENT_STATUS.md`**
  - [x] 去除未经证实的完成度与可靠性数字
  - [x] 去除与日期、版本、阶段不匹配的乐观结论
  - [x] 重新定义“已验证”“仅静态对齐”“仅框架存在”“待实测”四级状态
  - [x] 将“当前能做什么、不能保证什么”写清楚

- [x] **修复 `ALIGNMENT_PROGRESS.md`**
  - [x] 清理自相矛盾内容，例如一边写“运行时验证 0%”，一边又写“已保障一致性”
  - [x] 将静态结构对齐、运行时验证框架、真实 FFI 集成、端到端语义比对分层记录
  - [x] 所有勾选项必须以代码或测试证据为基础

- [x] **修复 `docs/api/README.md`**
  - [x] 改为“API 文档目标与当前维护状态说明”
  - [x] 不再宣称自动完整同步
  - [x] 明确哪些目录/文档可能已经过期，哪些应优先校正
  - [x] 增补统一状态标签规则

- [x] **建立 `docs/README.md` 总索引**
  - [x] 为 `docs/` 提供统一入口
  - [x] 说明阅读顺序与使用场景
  - [x] 区分总控文档、API 文档、对齐文档、实验文档、工程日志

---

## P1：高优先级任务

- [ ] **全面审计 `docs/api/`**
  - [ ] 核对每个 API 文档是否仍对应真实源文件
  - [x] 将高风险旧目录页统一降级为“历史遗留 / 待复核”口径
  - [x] 修复 `lib.md` 中对 `Decompiler` 等已注释/已停用接口的描述
  - [x] 为 `docs/api/README.md` 补充统一状态标签规则：
    - `已核对（当前有效）`
    - `部分有效（需对照源码）`
    - `历史遗留（仅供参考）`
    - `明显过期（待重写）`
  - [~] 按新标签体系逐页补齐状态标记
    - [x] 当前主线高风险 API 页面已基本补齐显式状态标签
    - [ ] 继续检查并补齐剩余低风险或边缘页面
  - [ ] 清点并标记仍未处理的遗留目录与子页
  - [~] 建立主线文档清单与历史文档清单两份索引视图
    - [x] 已完成双索引结构设计与清单范围梳理
    - [x] 已将双索引正式写入 `docs/api/README.md`
  - [~] 设计最小 P-code 对拍重入路径
    - [x] 已明确 `x86_lift.rs -> pcoderaw.rs -> funcdata.rs -> runtime_verify.rs -> ffi.rs` 的最小闭环
    - [x] 已固定第一批“单条指令 / 小型指令序列”样本方向，不直接扩大到端到端样本
    - [~] 为第一批对拍结果约定记录格式：输入、Rugra 结果、参考结果、差异分类
      - [ ] 将记录格式补成统一模板
      - [~] 先落首批 3 条样本的模板化记录
        - [x] `mov rbx, rax` 已从草案推进为首条真实运行记录，并补到结构化局部比较返回与 FFI 状态消费
        - [~] `add rax, 1`
          - [x] 已新增最小测试与失败记录
          - [x] 已确认当前 Rugra 侧为 `INT_ADD` + `COPY`
          - [x] 已确认当前仍未通过最小局部验证
          - [x] 已补 `SeqNum(order)` 并切换到按地址 + order 的比较路径
          - [x] 当前问题已修复（2026-04-24）：
            - [x] 参考 opcode 来源错误 → 新增 `ffi::to_ghidra_opcode()`
            - [x] unique 输入比较策略不合理 → 三处跳过 unique offset
            - [x] space_id FFI 映射不一致 → 新增 `ffi::space_to_ffi_id()`
        - [x] `sub rax, 8` — 测试已通过（2026-04-24）
      - [~] 建立函数级批量语义对齐骨架
        - [x] 已新增函数级语义快照模块：`src/align/function_snapshot.rs`
        - [x] 已定义函数级批量对齐所需的核心快照/报告结构
        - [x] 已支持从 `Funcdata` 导出 Rugra 侧函数语义快照
        - [x] 已补基础测试并确认通过
        - [x] 下一步补批量 runner
        - [x] 已设计 Ghidra 侧数据导出约定（2026-04-25）：`docs/method/ghidra_snapshot_format.md`

- [x] **修复 `docs/VERIFICATION_GUIDE.md`**
  - [x] 区分“可以直接运行的验证”“需要环境准备但尚未打通的验证”“仅为计划/示例的验证”
  - [x] 删除将伪代码/示意脚本写成现成流程的表述
  - [x] 明确当前验证链路的真实阻塞点

- [x] **补作文档索引页**
  - [x] 在 `docs/` 下新增总索引文件
  - [x] 汇总：
    - [x] 核心状态文档
    - [x] API 文档
    - [x] 对齐文档
    - [x] 实验与方法文档
    - [x] 工程日志
  - [x] 说明阅读顺序与使用场景

- [ ] **统一术语**
  - [ ] 统一使用：
    - [ ] 静态对齐
    - [ ] 运行时验证框架
    - [ ] FFI 集成
    - [ ] 端到端示例
    - [ ] 生产可用性
    - [ ] 历史遗留
    - [ ] 待复核
    - [ ] 已核对（当前有效）
  - [ ] 避免“打通”“完成”“可用”在不同文档中表达不同含义

---

## P2：中优先级任务

- [x] 审核 `docs/data_contract.md`，补充最基础的数据契约说明，至少覆盖：
  - [x] `Address`
  - [x] `Varnode`
  - [x] `PcodeOp`
  - [x] `Funcdata`
  - [x] `PrintC` 输出阶段的输入约束

- [~] 审核 `docs/archive/agentlog/` 历史日志，给明显失真的日志增加“按当时记录，尚待复核”提示。
  - [x] 已完成首批高风险日志复核提示落地
  - [ ] 继续扩展到剩余历史日志
- [ ] 为 `docs/branches/` 建立最小状态记录规范，避免该目录空转。
- [ ] 为 `docs/method/`、`docs/experiments/` 补充“引用真实实验或代码依据”的要求。
- [ ] 清理仓库中明显属于调试残留但被文档错误当作正式产物引用的文件说明。
- [ ] 把历史日志复核策略沉淀到单独规范文档或模板补充说明中。
- [ ] 为状态类文档建立统一的“证据来源 / Evidence Sources”写作规则，并沉淀到规范文档中。

---

## 文档修复判定标准

以下条件全部满足，才能认为“文档失真修复”阶段基本完成：

- [x] 根目录文档与 `rugra/` 实际工程定位一致
- [x] README 不再把目标态写成现状
- [x] 项目结构文档与真实目录结构一致
- [x] 状态/进度文档不再自相矛盾
- [x] API 文档索引不再声称绝对同步
- [x] 验证文档明确区分“已实现”“可运行”“待集成”“计划中”
- [x] 至少建立一份清晰可用的 `docs/` 总索引
- [~] 所有高风险结论都能指出代码或测试依据
  - [ ] `CURRENT_STATUS.md` 已建立证据来源回链
  - [ ] `ALIGNMENT_PROGRESS.md` 已建立证据来源回链
  - [ ] `docs/VERIFICATION_GUIDE.md` 已建立证据来源回链
- [~] API 文档状态标签已覆盖主要高风险页面
- [ ] 旧架构文档已系统性标注为历史说明或完成重写
- [~] 历史日志已具备统一复核提示机制

---

## 暂缓事项

以下事项不是当前阶段的第一优先级，应在文档可信化完成后再系统推进：

- [ ] 新功能扩展（除非为修正文档失真必须先核实代码）
- [ ] 新架构支持
- [ ] 大规模重构
- [ ] 对外宣传材料撰写
- [ ] 新一轮“与 Ghidra 完全一致”的结果宣称

---

## 历史日志复核提示策略

为避免旧日志继续被误当成当前事实，后续审阅 `docs/archive/agentlog/` 时应遵守以下口径：

### 适用条件
如果历史日志中出现以下情况之一，应补“复核提示”：

- 把阶段性结果写成最终完成
- 把局部样例成功写成整体能力成熟
- 把静态结构对齐写成运行时一致
- 把旧架构写成当前主线
- 把 CLI/验证/输出质量写得明显超出当前可证实范围

### 推荐提示语
建议使用类似表述：

- “按当时记录，尚待按当前代码状态复核”
- “该结论反映当时阶段性判断，不应直接视为当前事实”
- “该条目涉及旧架构口径，需结合当前总控文档重新理解”

### 使用原则
- 不删除历史日志原文
- 不事后篡改历史过程结论
- 通过补充说明降低误导风险
- 以当前总控文档和源码状态为准重新解释历史记录

---

## 最近完成

- [x] 变量声明重构与反编译质量质量提升（2026-05-23）：(1) **变量声明重构** — 废弃原本基于 AST op-scanning 的局部变量声明机制，改为在 Discovery Pass（Pass 1）中使用 `used_varnode_types` 映射收集所有实际被打印的变量名、空间、偏移及类型，并在函数开头进行声明；(2) **未声明 Register/Unique 变量修复** — 允许声明变量化了的寄存器变量（如 `lVar_a8`），支持声明由于 DCE 消除定义后悬空使用的 Unique 临时变量（如 `uVar_a0`）；(3) **非标识符声明过滤** — 过滤包含 `->`、`.` 等非合法 C 标识符字符的名字（如 `struct2->field_8`），确保仅声明基址；(4) **Puts 字符串解析** — 改用 lossy UTF-8 扫描跳过对齐 NULL padding 恢复 3 处 hugehelp 字符串；(5) **puts()) 多余括号修复** — 增加 pass 15 消除多余右括号。所有 168 个测试通过，未声明变量全部消除，C 语法正确性获得飞跃。

- [x] 反编译出力品質改善 v36→v39（2026-05-22 夜）：(1) **位运算常量修复** — push_varnode 在 XOR/AND/OR/SHIFT 上下文中跳过字符串解析，`*DAT ^ "huge_string"` → `*DAT ^ 0x400000`；(2) **未使用变量清除** — prettyprint.rs 第十一趟 pass 扫描声明行变量名是否在函数体引用，uVar 声明 75→30（-60%）；(3) **空 else 空行清除** — 第十二趟 pass 删除 `} else {` 后空行，17→0；(4) **return void() 拆分** — 第十三趟 pass 将 `return free()` 拆为 `free(); return;`，2→0；(5) **连续 goto 死代码** — 第十四趟 pass 删除 goto 后的 goto，1→0；(6) **fwrite 签名修正** — 3→4 参数，显示 `*stderr` 第四参数；(7) **字符串扫描改进** — .rodata 扫描接受 \n\t\r 开头字符串，部分 hugehelp puts 恢复；(8) **字符字面量格式** — ASCII 字母/常用标点显示为 `'/'`、`'='`，数字/符号保持 hex；(9) **长字符串截断** — 超过 80 字符的字符串截断为 60+`(continues)`。总行数 926→865（-7%），声明 75→30。168 测试通过。
- [x] 反编译输出三大结构性问题修复 v32→v35（2026-05-22）：(1) **if-no-body 修复** — `BlockType::Condition` 顶层处理器从错误的 `if(compound_cond)` 无 body 输出改为递归展开子块，0/4 处消除；(2) **函数参数缺失修复** — 恢复 CBRANCH 作为后向搜索停止条件（避免跨分支路径错误拾取寄存器写入），新增首调用入口回退搜索（函数入口参数传递），扩展签名数据库（fputc/curl_slist_free_all/_init 等），无参调用 15→9；(3) **getparameter 死代码消除** — prettyprint.rs 新增第十趟 pass，检测 `return;`/`break;`/`continue;` 后相同缩进级别的不可达代码（排除标签/case），死代码 11→0、getparameter 行数 295→251、return 数 41→33。无回归（fclose 等不出现错误参数），168/168 测试通过，24/24 函数反编译成功。
- [x] 反编译输出质量综合改进 v24→v28（2026-05-22）：(1) 返回类型推断——扫描 RAX 写入推断 int/long，23→7 void（-70%）；(2) 字符串常量解析——.rodata 子串查找，`0x62f8`→`"--"`；(3) switch 条件变量——CBRANCH cascade 比较操作数提取，5→1 空 switch（-80%）；(4) case 字符格式——`case 45:`→`case '-':`；(5) 变量内联扩展——Register 空间单次使用 varnode 内联，uVar 引用 582→502（-14%）；(6) goto 消除——"exit label"检测+替换为 break/return，goto 135→66（-51%）。所有 168 测试通过，24/24 函数反编译成功。
- [x] ActionCallParams 函数签名数据库 + 常量显示改进 + 作弊代码清除：(1) `known_param_count()` 标准 libc 类型库映射约 80 个常见 C/libc 函数的已知参数数量，未知函数默认 6（SysV AMD64 全部参数寄存器）；(2) CALL ops 按签名裁剪参数——0 参数函数无参数、1 参数函数精确 1 个参数；(3) 间隙处理从遇首个间隙停止改为收集到最后找到的参数并插入占位 Register varnode；(4) 常量 >= 256 显示 `0x2726 /* 10022 */` 格式；(5) 高位置位常量显示有符号值（`-1`）；(6) 删除 `curlopt_name()`、`known_global_name()`、`curl_setopt_arg_index` 等二进制特定硬编码。全部 168 测试通过。（2026-05-21 深夜）
- [x] 反编译质量批量修复 (10 项中 7 项完全解决, 2 项改善, 1 项未动)：(1) 空 if/else 死代码块消除 — `is_block_body_empty()` + `seen_return` 守卫 + 空 true 分支条件取反；(2) continue 外循环抑制 — `loop_depth` 跟踪；(3) 函数签名回退检测 — 扫描 SysV ABI 寄存器读取；(4) 大函数栈帧检测 — INT_ADD 二进制补码 + alivelist 回退；(5) RSP STORE → `local_XX`；(6) 全局符号解析 → `*config`；(7) 参数命名 → `param_N`；(8) SSA def chain 回退到 `try_fold_bool_comparison`；(9) getparameter 声明 100→41。效果：998→920 行, 0 空块, 0 非法 continue, 18/18 函数有签名。168 测试通过。（2026-05-21）
- [x] SSA def-use chain 完整集成 + 结构体字段聚合：(1) `get_defining_op` 使用 `Varnode.def` 追踪 SSA 定义链，作为 `emit_condition` 的首选策略（Strategy 0）；(2) 修复 legacy if/else 代码路径，改用 `emit_block_condition` 确保与 BlockIf 一致的条件解析；(3) BOOL_OR/BOOL_AND 的操作数改为递归 `emit_condition`，配合 `try_fold_bool_comparison` 恢复 `argc <= 1`；(4) 新增 `StackStruct` 模型，从 `INT_ADD(RSP, const)` → COPY → Register 模式检测函数参数中的栈结构体基址；(5) `get_stack_variable_name` 优先匹配检测到的结构体范围，发射 `config.field_XX`。效果：`if(uVar99)` → `if(uVar194 == 0)`，`if(uVar156)` → `if(uVar194 != 0)`，`&local_1f0` → `&config`。全部 168 测试通过。（2026-05-21）
- [x] 栈帧分析模型 + 栈变量命名 + 帧基址解析 + SSA-aware 条件解析：(1) `stack_frame_size` 从 `INT_SUB(RSP, const)` 检测栈帧大小；(2) `get_stack_variable_name` 将 `RSP + offset` 转换为 `&local_XX`（Ghidra 风格命名）；(3) `stack_frame_base_key` 追踪帧基址 varnode，将 `uVar107 + 0x218` 解析为 `local_10`；(4) `comparison_def_map` 仅存储比较/布尔 ops，不被后续非比较 ops 覆盖；(5) do-while 条件从 `push_varnode` 改为 `emit_condition`；(6) `def_map` 扩展到 block-level ops。效果：`RSP + 0x38` → `&local_1f0`，`uVar107 = RSP - 0x228` 消除，`while(uVar99)` → `while(uVar194 == 0)`，变量 8 → 7 个。全部 168 测试通过。（2026-05-21）
- [x] 布尔表达式简化 + 跨块 DCE + 寄存器名消除 + CALL 参数深度解析：(1) `try_fold_bool_comparison` 将 `BOOL_OR(INT_EQUAL(A,B), INT_LESS(A,B))` 折叠为 `A <= B`，用 pointer-based def_map 精确匹配；(2) 全局 `global_used_outputs` 跨所有基本块检测死输出；(3) RIP-relative INT_ADD 在 emit_block_ops 级别跳过；(4) `is_raw_register_name` 检测 HighVariable 中的 x86-64 寄存器名，转换为 `lVar_b0`/`iVar_0` 风格的局部变量名（RSP/RBP 保留）；(5) op_call 的 COPY 源增加 RIP-relative 深度解析。效果：`uVar99 || uVar100` → `argc <= 1`，`R14` → `lVar_b0`，`EAX` → `iVar_0`，声明 17 → 8 个。全部 168 测试通过。（2026-05-21）
- [x] 常量折叠 + Def Chain Resolution + 扩展 DCE + argc/argv 映射：(1) 跳过 Const 空间 HighVariable 命名，常量直接显示数值；(2) Priority 1.5 通过 value_def_map 追踪 Register/Unique uVar 的定义链，解析 COPY(Const)、COPY(Unique → RIP-relative)、直接 RIP INT_ADD；(3) is_lhs guard 防止输出端折叠；(4) main() 自动映射 argc/argv；(5) 扩展 DCE 到所有纯计算 opcode；(6) uVar 声明裁剪只从输入端触发。效果：变量声明 75 → 17 个，代码行数 ~93 → ~62 行，`EDI == 1` → `argc == 1`。全部 168 测试通过。（2026-05-21）
- [x] CALL 参数解析 (P2)：在 op_call 中添加 Register 参数的 def chain 追踪，通过 value_def_map 找到 COPY 源并 inline。将 Register-space COPY 纳入 value_def_map。`curl_version(RDI)` → `curl_version(uVar193)` 现在展示实际计算值而非寄存器名。全部 168 测试通过。（2026-05-21）
- [x] 变量声明精简 (P1)：排除所有 Register 空间和 Ram/Const 空间（全局符号）的 varnode 从变量声明。75 → 35 个变量声明。全部 168 测试通过。（2026-05-21）
- [x] RIP-relative 地址折叠 (P0)：新增 `get_rip_relative_operand` 辅助方法检测 `INT_ADD(RIP, x)` 模式，在 `op_binary`、`emit_inline_expr`、`op_store` 三处添加 RIP 折叠。全部 168 测试通过。（2026-05-21）
- [x] If-else 结构化 + 反编译质量优化：(1) 参数名优先级提升（param_names > HighVariable），(2) CALL 从 block terminator 移除，(3) skip_terminal 无条件跳过 branch ops，(4) BlockIf body 使用 skip_terminal=true，(5) E2E 测试重构为 14-op 4-block diamond CFG。输出从 `goto LAB_xxx` 提升到完整 `if/else` 结构。全部 168 测试通过。（2026-05-21）
- [x] INPUT varnode 标记修复 + Pipeline 重排序：`inject_raw_ops` 新增 Phase 3 自动标记未定义的寄存器读取为 INPUT；将 `ActionInferParams` 移到 `ActionHeritage` 后、`ActionCopyPropagate` 前，避免 COPY 传播消除参数寄存器。curl_easy_setopt 现在输出 `long curl_easy_setopt(long param_1, int param_2, long param_3)`。全部 168 测试通过。（2026-05-21）
- [x] 参数名称传播：在 PrintC 中实现了寄存器偏移→参数名称映射（`param_names`），表达式中使用 `param_1` 而非 `RDI`，变量声明中跳过已在签名中声明的参数。全部 167 测试通过。（2026-05-21）
- [x] 函数签名恢复（ActionInferParams）：实现了从 INPUT varnodes 和 RETURN ops 中自动检测函数参数和返回类型。将 FuncProto 集成到 Funcdata，升级 PrintC 的函数头部发射从硬编码改为原型驱动。全部 166 测试通过。（2026-05-21）
- [x] PrintC 类型转换集成：将 `CastStrategyC` 集成到 `PrintC`，实现 LOAD/STORE 类型感知解引用（`*(type *)addr`）、ZEXT/SEXT 类型感知 cast（使用推断出的实际类型名替代硬编码 `(uint)`/`(int)`）。修复了 `test_type_propagation` 的 RwLock 死锁。更新了 `coreaction.md` API 文档。全部 163 测试通过。（2026-05-21）
- [x] 迭代类型恢复引擎与测试对齐验证：重构了 `ActionTypeInfer` 为基于收敛性数据流的双向类型传播引擎，引入基础 Opcode、COPY 传播、ADD/SUB 偏移、LOAD/STORE 解引用以及 Phi 节点等规则，配合 post-pass size 兜底，完美通过了 `test_type_propagation` 单元测试及全局 163 个单元测试。（2026-05-21）
- [x] switch-case 检测与生成初步实现：在 `block.rs` 实现了 `BlockSwitch`，在 `blockaction.rs` 引入了 `collapse_switches` 折叠 Pass，在 `printc.rs` 实现了 `BlockType::Switch` 的 C 语言打印支持，并在 `funcdata.rs` 补齐了闭环单元测试。（2026-05-21）
- [x] HighVariable 寄存器命名初步实现：merge.rs 新增 `register_name()` + `assign_names()` 使用寄存器名，curl 输出从 uVarN 改为 RAX/RDI/RSI 等（2026-05-09）
- [x] CBRANCH-latch 循环检测初步实现：blockaction.rs 新增 `detect_cbranch_loops()`，真实 curl 二进制检测出 do-while 循环（2026-05-09）
- [x] ActionFinalStructure 初步实现：op.rs 新增 GOTO 常量，blockaction.rs 新增 `tag_gotos()` + `remove_dead_code()`，清除 BRANCH 后不可达代码（2026-05-09）
- [x] 修复 `heritage.rs` 中 `rename_direct` 的 AddressSpace 碰撞 bug，stack key 从 `Address` 改为 `(AddressSpace, Address)`（2026-05-09）
- [x] 完成 SSA renaming 验证：4 项测试覆盖单块 renaming、多块 Phi 输入填充、菱形 CFG Phi 验证、INPUT varnode 回退（2026-05-09）
- [x] 更正根目录 `AGENTS.md`，将错误的 RustVSR 项目描述改为与 `rugra/` 实际工程一致的说明。
- [x] 明确后续文档维护默认以 `rugra/` 子项目为核心，避免继续围绕不存在的顶层主工程写作。
- [x] 为 `mov rbx, rax` 补出第一条最小真实运行记录：已新增 `funcdata::tests::test_mov_reg_reg_minimal_alignment_path`，确认 `48 89 c3 -> mov rbx, rax -> CPUI_COPY -> inject_raw_ops(...)` 的最小链路可执行。
- [x] 已把该样本从“框架级占位比较”推进到“Rugra 侧结构化局部比较”：`verify_pcode_generation(...)` 现会传入真实 opcode、输出 varnode、输入 varnode 列表，并按结构化比较结果返回 `VerifyResult::Match` / `VerifyResult::Mismatch(...)`。
- [x] 已让 `rugra_compare_pcode(...)` 从 stdout-only 入口升级为“stdout + 结构化状态码返回”，并把该状态回传给 `verify_pcode_generation(...)` 参与最终判定。
- [~] `mov rbx, rax` 仍未达到“完整 Ghidra 局部对拍通过”：当前结构化比较和 FFI 状态仍主要围绕 Rugra 侧本地构造数据，尚未接入真实 Ghidra 参考 opcode / 输出 / 输入明细。
- [~] `add rax, 1` 已进入最小样本验证，但当前属于“失败样本记录”而非“局部对拍通过”：测试已新增，当前日志显示 opcode 与输入比对仍存在错位；在补齐 `SeqNum(order)` 和按地址 + order 比较后，问题已进一步收敛到“参考 opcode 来源错误”和“unique 输入比较策略”两个点。
- [x] 已开始函数级批量语义对齐骨架建设：新增 `src/align/function_snapshot.rs`，并已支持从 `Funcdata` 导出函数级 `P-code / CFG / SSA` 语义快照。
- [x] 已确认函数级快照骨架基础测试通过，说明 Rugra 侧函数语义摘要结构已经具备最小可用性。
- [x] 将文档修复目标重新聚焦到“事实校验、索引建设、去失真”，而不是继续堆叠未经验证的完成度表述。
- [x] 重写 `rugra/README.md`，纠正项目定位、CLI 状态和当前能力边界。
- [x] 重写 `rugra/docs/PROJECT_STRUCTURE.md`，建立与真实目录结构一致的结构索引。
- [x] 重写 `rugra/CURRENT_STATUS.md`，按可信边界重述当前状态。
- [x] 重写 `rugra/ALIGNMENT_PROGRESS.md`，明确区分静态对齐、运行时验证和端到端验证。
- [x] 重写 `rugra/docs/VERIFICATION_GUIDE.md`，修复把验证框架写成已完成闭环的问题。
- [x] 重写 `rugra/docs/data_contract.md`，从占位
文档补成主链路数据契约文档。
- [x] 重写 `rugra/docs/api/README.md`，改为基于当前源码状态的 API 索引，并加入统一状态标签规则。
- [x] 新增 `rugra/docs/README.md`，作为 `docs/` 统一入口。
- [x] 修复并重写一批当前主线高风险 API 文档入口，包括：
  - [x] `docs/api/lib.md`
  - [x] `docs/api/funcdata.md`
  - [x] `docs/api/op.md`
  - [x] `docs/api/varnode.md`
  - [x] `docs/api/printc.md`
  - [x] `docs/api/block.md`
  - [x] `docs/api/heritage.md`
  - [x] `docs/api/action.md`
  - [x] `docs/api/pcoderaw.md`
  - [x] `docs/api/binary/mod.md`
  - [x] `docs/api/disasm/mod.md`
  - [x] `docs/api/align/runtime_verify.md`
  - [x] `docs/api/bin/rugra.md`
- [x] 将旧架构目录索引统一降级为历史说明，包括：
  - [x] `docs/api/analysis/mod.md`
  - [x] `docs/api/pcode/mod.md`
  - [x] `docs/api/codegen/mod.md`
  - [x] `docs/api/translator/mod.md`
- [x] 将旧架构高风险子页继续降级为“历史遗留 / 待复核”说明，包括：
  - [x] `docs/api/analysis/calls.md`
  - [x] `docs/api/analysis/dataflow.md`
  - [x] `docs/api/analysis/high_variable.md`
  - [x] `docs/api/analysis/liveness.md`
  - [x] `docs/api/analysis/optimization.md`
  - [x] `docs/api/analysis/ssa.md`
  - [x] `docs/api/analysis/type_inference.md`
  - [x] `docs/api/analysis/type_propagation.md`
  - [x] `docs/api/analysis/variables.md`
  - [x] `docs/api/analysis/api/mod.md`
  - [x] `docs/api/analysis/rules/mod.md`
  - [x] `docs/api/analysis/rules/algebra.md`
  - [x] `docs/api/analysis/rules/constants.md`
  - [x] `docs/api/analysis/rules/dataflow.md`
  - [x] `docs/api/pcode/program.md`
  - [x] `docs/api/translator/x86_64.md`
  - [x] `docs/api/translator/registers.md`
- [x] 开始将历史日志复核提示策略回填到高风险 `AgentLog`，包括：
  - [x] `docs/archive/agentlog/engineering_progress_2026-03-07_api_docs_completion.md`
  - [x] `docs/archive/agentlog/engineering_progress_2026-03-08_zero_codegen_pipeline_resolved.md`
  - [x] `docs/archive/agentlog/engineering_progress_2026-03-08_output_refinement_and_quality.md`
- [x] 继续为当前主线 API 文档补齐显式状态标签，已覆盖的高风险页面包括：
  - [x] `docs/api/coreaction.md`
  - [x] `docs/api/ruleaction.md`
  - [x] `docs/api/blockaction.md`
  - [x] `docs/api/fspec.md`
  - [x] `docs/api/typeop.md`
  - [x] `docs/api/merge.md`
  - [x] `docs/api/cover.md`
  - [x] `docs/api/space.md`
  - [x] `docs/api/address.md`
  - [x] `docs/api/opcodes.md`
  - [x] `docs/api/error.md`
  - [x] `docs/api/ffi.md`
  - [x] `docs/api/disasm/x86_64.md`
  - [x] `docs/api/disasm/x86_lift.md`
  - [x] `docs/api/align/mod.md`
  - [x] `docs/api/align/action.md`
  - [x] `docs/api/align/address.md`
  - [x] `docs/api/align/block.md`
  - [x] `docs/api/align/datatype.md`
  - [x] `docs/api/align/heritage.md`
  - [x] `docs/api/align/pcodeop.md`
  - [x] `docs/api/align/range.md`
  - [x] `docs/api/align/varnode.md`
- [x] 已在 `docs/api/README.md` 建立“当前主线文档清单 / 历史文档清单”双索引视图。
- [x] 已完成最小 P-code 对拍闭环首批样本规划，当前优先级明确为：
  - [x] `mov`
  - [x] `add`
  - [x] `sub`
  - [x] `and/or/xor`
  - [x] 简单 `cmp/jcc`
  - [x] 简单位移
- [~] 已开始为首批最小 P-code 对拍样本设计统一记录模板
  - [x] 已把统一字段固化到实验记录模板中
  - [x] 已先落 3 条样本的模板化记录草案
  - [x] 已把 `mov rbx, rax` 推进成第一条最小可执行对拍记录说明
  - [x] 已把 `add rax, 1` 推进为第二条最小样本失败记录
  - [x] 已修正 `add rax, 1` 的局部比较错位，测试通过（2026-04-24）
  - [x] 已新增 `sub rax, 8` 第三条最小样本并通过（2026-04-24）
  - [x] 已新增第四批 6 条样本并全部通过（2026-04-24）：
    - [x] `and rax, 0xf` — INT_AND + COPY ✅
    - [x] `or rax, 0x10` — INT_OR + COPY ✅
    - [x] `xor rax, 0x7` — INT_XOR + COPY ✅
    - [x] `shl rax, 4` — INT_LEFT + COPY ✅
    - [x] `shr rax, 4` — INT_RIGHT + COPY ✅
    - [x] `cmp rax, rbx` — INT_EQUAL + INT_LESS + INT_SLESS (3 ops) ✅
  - [x] 修复多线程测试竞争：新增 FFI_TEST_LOCK 序列化锁
  - [x] 全部 144 测试通过，0 失败（2026-04-24）

---


## 下一会话建议切入点

1. 推进多指令序列对拍（2-5 条指令组成的小基本块）
2. 在函数级快照骨架基础上补批量 runner 与导出流程
3. 完成 Phase C 维护流程固化（证据来源规则、审计模板）
4. 检查剩余 API 文档页面并补齐状态标签
5. 设计 Ghidra 侧快照导出约定
6. ~~Unique 空间临时变量命名改进（当前仍为 uVarNN）~~ → 已通过 Cover-based merge 大幅缓解（uVar 引用 129→72，2026-06-21）
7. 结合类型恢复引擎开始在高层 C 代码发射（PrintC）中处理必要的类型转换（Type Casts）打印逻辑。
8. 探索结构体（Struct）成员和偏移量的类型传播恢复方案。
9. [x] 迭代类型恢复引擎与测试对齐验证：设计并实现基于固定点迭代的多趟数据流类型传播分析（ActionTypeInfer），支持 COPY、ADD/SUB 偏移、LOAD/STORE 解引用及 Phi 等双向传播规则，测试 `test_type_propagation` 完美验证闭环。（2026-05-21）
10. [x] switch-case 检测（BlockSwitch + jump table） （2026-05-21）
11. [x] **Cover-based HighVariable merging**（2026-06-21）：实现 `Merge::merge_by_cover` + `Cover::intersects_except_at`，对齐 Ghidra `Merge::mergeByCopy`。curl `uVar` 引用 -44%。修正 `ActionMergeType` 在 `ActionCopyPropagate` 之前的流水线顺序。173 测试通过。详见 `docs/archive/agentlog/engineering_progress_2026-06-21_cover_based_merge.md`。
12. [ ] 区域化控制流结构化（基于 Ghidra `blockaction.cc`）— 当前 5 个 goto、5 个 switch 仍可改进
13. [ ] for 循环恢复（当前 `for_count=0`，所有循环都是 `while`）
14. [ ] Cover 跨块传递性扩展（当前 Cover 仅覆盖 def/use 块，未做 CFG 传播，导致部分安全合并被错过）
15. [ ] 填补 `merge_adjacent` / `merge_multi_entry` / `merge_marker` / `merge_by_datatype` 桩函数

---
| `FUNCDATA-LINKSYMBOL-TYPED-0001` | P0 | REWORK | linksymbol_wave | `Funcdata::linkSymbol` 忠实化：SSA 寄存器临时也建带 HighVariable 类型的 ScopeLocal Symbol | `src/{funcdata,coreaction,varmap,merge}.rs`, paired docs, fixture | evidence=`pending root commit`（禁止本 agent 自行 commit）。实现：①funcdata.rs `link_symbol`（cc:1156-1184 逐行）——proto-partial 走真 `PieceNode::findRoot`（op.cc:824-852 移植为模块级 piece_node_find_root）、`queryProperties(addr,1,usepoint)` 重叠查询（usepoint=def op 地址，input=函数基址-1 对齐 varnode.cc:696-703）、无重叠且非 persist 时 `ScopeLocal::add_symbol("", high.get_type(), addr, usepoint)`——`local_XX` HashMap 自创名删除；`handle_symbol_conflict`（cc:997-1029）+`build_dynamic_symbol`（cc:1283-1305，DynamicHash unique_hash_vn）改走符号模型，HighVariable→Symbol 关联由 `Funcdata::high_symbols` 侧表建模（variable.rs 域外）。②varmap.rs LocalSymbol 增 space/is_dynamic/hash；add_symbol 带空间；新增 add_dynamic_symbol（database.cc:1690）/query_properties（database.cc:1263）；find_symbol/funcdata 各扫描按空间过滤；buildDefaultName vn 路径 isInput 前置 update_flags（variable.hh:200 惰性语义）。③coreaction.rs ActionNameVars：hasName 修复为 variable.cc:718-747 可命名谓词（原 name.is_empty() 提前 continue 删除）；apply 按 cc:2978-3000 顺序（lookForFuncParamNames 改 cc:2858-2897 符号 renameSymbol(makeNameUnique)，namerec buildDefaultName 循环+assignDefaultNames 共享 base）；RUGRA-GLUE 桥把符号 display_name 发布到 high.name（printc 仍读 high.name，退役归 PRINTC-SYMBOL-DECL-0001）；ActionRestructureVarnode 安装 x86-64 寄存器名表（translate.hh:380 站位）。④merge.rs 自创 per-High assign_names+register_name 删除（Ghidra merge 序列无命名步骤）。**fixture 权威 runner（pinned base=9001bbd+4 文件 overlay）exit0：4 records 全 MATCH（typed_temporaries→bVar5/cVar6/iVar7/pcVar8 带类型/空间/entry/usepoint；irregular_input→in_RCX usept=36cf）**。单测：coreaction 44/44、varmap 26/26、merge 4/4、funcdata 41/43（2 失败=已知 xunknown8 型别域基线，与本改动无关）。E2E curl 124/124、defects=0；差分（12.0.4 golden）：**local_XX 106→0（目标达成）**、声明块出现 golden 族的 in_R12/in_RBP/long */int * 类型化符号声明；skeleton 4585→5280、numbering 23→259——**中间态回归，机制已定位**：printc GLUE 双声明源（doc_variable_decls 的 compact 重编号 vs scope-symbol 安全网的原始符号名）+ compact_base 从 1 重启与符号名相撞，全部在 printc.rs（本租约域外，PRINTC-SYMBOL-DECL-0001 的既定范围）。残差：conflict→dynamic 路径 oracle 级 UNTESTED（锁定 oracle 的 uniqueHash 对最小同址双 High 形状抛 RecovError 'Unable to find unique hash'，中止 ActionNameVars；Rugra 镜像返回 None 并保持原 entry 关联而非中止 Action——需专设 hash-unique 数据流 fixture）；high_symbols 侧表待 variable.rs 收编；sync_varnodes updateDatatypes 未做（③链下一环）。**机制 C 复核 REJECT→4 项 MISMATCH 已修**（2026-08-16）：①link_symbols spacebase 分支删 continue（Ghidra cc:2957-2958 落入 nameRep/hasName/linkSymbol）；②attach_symbol_to_vn 经 Funcdata::symbol_entry_cache（LocalSymbol→database.rs SymbolEntry 身份稳定桥）调用忠实 Varnode::set_symbol_entry+HighVariable::set_symbol（variable.cc:245-275 symboloffset 四分支），cc:2965 getSymbolOffset()<0 namerec 门与 Ghidra 同判，桥接名命名期后刷新；③内联 makeRec 对照 cc:2815-2850 补 nameLocked/尺寸门/CAST 展开/type_order tie-break；④findRoot 平手改用忠实 PcodeOp::compare_order（op.cc:778-791）。fixture 增 partial_coverage case（4B 临时@0x70 def pc=基址-1 + 1B input@0x71 → soff=1 被 namerec 门排除、共享整符号）。重跑 runner exit0：6/6 MATCH；E2E 124/124、defects=0；差分（与并发 printc.rs 改动联合树）：local_=0 保持、**numbering 23→0**、skeleton 4585→4624（+39）、in_ 族/类型化声明在位。待 root 窄面复核（仅 4 函数） | 2026-08-16 |
| `PRINTC-SYMBOL-DECL-0001` | P1 | REVIEW | printc_symbol_decl_wave | PrintC 声明改 Symbol 驱动（吸收 PRINTC-SCOPE-RESTRUCT-0001） | `src/printc.rs`, `docs/api/printc.md`, `tests/oracle/printc_symbol_decl_1204.{cc,rs,metadata.json}`, `tools/run_printc_symbol_decl_oracle.sh`, 本 TODO 行 | evidence=`pending root commit`（禁止本 agent 自行 commit）。实现：①移植 `emit_local_var_decls`（printc.cc:2260-2279；cc:2267-2275 子 scope 遍历在 Rugra ScopeLocal 无子 scope 时等价空遍）、`emit_scope_local_var_decls`（cc:2518-2575：cat>=0 类别分支对局部声明不可达——printc.cc:2265/2272/2612 全传 no_category；cat<0 全 map 遍历=非动态符号 stable sort by（`local_maptable_space_rank` Unique<Register<Stack——锁定 golden helpf/main/myprogress decl 块实测序+database.cc:1889-1919 maptable 空间索引序，start 偏移，usepoint Option 最小序=addrtied EntrySubsort）+dynamic 列表插入序后置；cc:2539-2550 过滤器如实：piece 不可达、category!=no_category 跳过（cc:2541）、空名跳过（cc:2542）、**map 分支无 $$undef 过滤的不对称如实保留**（类别分支 cc:2529 才有；生产中 assignDefaultNames 保证不存活到打印））、`emit_local_symbol_decl`/`_statement`（cc:2497-2516：begin_var_decl+push_type_start(sym.dtype 逐字)+display_name+push_type_end+end_var_decl，tag_line+';'）；`snapshot_local_scope` 暴露 doc_function 的 fd.scope 快照入口。②删除：`compact_name_for`+全部调用点（3 处回退链保留 raw 名）、`preallocate_register_compact_names`、`doc_variable_decls_from_funcdata`（~170 行 GLUE）、`compact_rename`/`compact_base`/`scope_naming_base`/`declaration_order`/`used_scope_symbols` 字段、打印期 restructure_varnode 兜底与二次 assign_default_names（oracle 无任何打印期重编号路径，判断=删除）。③`test_compact_name_for`→`test_emit_local_var_decls_symbol_driven`（断言 map 序/类别跳过/空名跳过/$$undef 原样发射）。**fixture 权威 runner（cover_rebuild，pinned base=b6b61d5+LINKSYMBOL 4 文件 live+printc.rs live overlay）exit0：4/4 MATCH**（typed_temporaries=8 符号 xunknownN xVar1-4(unique)+bool bVar5/char cVar6/int4 iVar7/char *pcVar8(register) 序/拼写逐字节；irregular_input=in_RCX；dynamic_symbol=静态先 dynamic 后；undef_and_category=$$undef0000000a 原样发射+function_parameter 类别跳过）。验收：cargo test printc:: 8/8 绿；全库 1352 过/5 失败=已知 5 域（comment/dynamic/funcdata×2/ruleaction，本改动前基线一致）；E2E curl 124/124（76 decompiled 0 失败）。**差分（12.0.4 golden，同一上游 A/B：HEAD printc.rs vs 本改动，funcdata/coreaction/varmap/merge WIP live）**：numbering **258→0**（目标<23）、local_ 0→0 保持、defects 0→0、skeleton 5356→4624（−732）；另有同树联合测量（与 LINKSYMBOL REWORK 修复合树）numbering 0/local_=0 一致。机制 B ## Differential：numbering=0+defects=0 可提交；skeleton 残差归因：xunknownN/unknown 符号类型拼写（TYPE-UNKNOWN-0001 域，按铁律 1.4 不在打印层改名）、extern long 全局 GLUE 块、prettyprint backfill pass-23（committed 前置）、param 类型 char *pos vs int pos（funcproto 域）。annotations --all ✅、refs --all --strict ✅、## Alignment Evidence 四类语义 dry-run ✅。残差：①未链接符号 body 引用仍走 uVar_<offset> 地址回退（LINKSYMBOL 桥覆盖缺口，variable.rs 侧表收编后消除）；②examples/curl_decompile.rs TYPEDEF_PREAMBLE 前缀契约锁死 typedef latch 形状（域外文件）；③tools/audit_syntax.py 对 skip_line 布局的函数提取已失效（锁定 golden 自身 116/116 FAIL——`depth` 从签名行计数，不兼容 `

{` 布局），需 root 登记 P1 工具 TODO | 2026-08-16 |
| `TYPEFACTORY-UNDEFNAME-0001` | P2 | REVIEW | undefname_wave | `xunknownN`→Ghidra 数据组织命名 `undefinedN` | `src/type_system/typefactory.rs`, `src/type_system/datatype.rs`, paired docs | evidence=`pending root commit`；已实施：init_core_types 核心未知类型更名 undefined1/2/4/8（对齐 ghidra_arch.cc:349-352 ArchitectureGhidra::buildCoreTypes 数据组织注册；id 仍=hash_name，type.cc:3671 同步），get_ptr_to_from_parent 兜底与单测同步，print_name_base（datatype.rs:649）函数体零改动（取名字首字符，undefined8→'u'，doc 已注明）。oracle 审计：(a) type_unknown_1204 记录的 MATCH 被**如实降级 MISMATCH**——其 Ghidra 侧走 BfdArchitecture→SleighArchitecture::buildCoreTypes standalone fallback（sleigh_arch.cc:229-232，xunknown*），与数据组织 undefined* 是 Ghidra 自身两条合法注册路径的 inter-flavor 差异（Rugra 单一工厂默认 vs Ghidra 按架构注册），结构性 identity/ordering/clear 行为不受影响，绑定本 TODO；(b) 其余 *_1204.cc fixture 的 setCoreType("xunknownN") 均为 fixture 局部 scope-zero 安装，Rust 侧对应物走 VarnodeBank 局部 adapter，不触发工厂路径；(c) varnode.rs unknown_datatype adapter（xunknown{size}，TYPE-UNKNOWN-0001）不在本 write-set，保留。验收：cargo test --lib type_system:: 80/80 绿；全库 1352 过/5 失败=已知 5 域（comment/dynamic/funcdata×2/ruleaction，均与本改动无关）；grep xunknown src/ 仅剩 varnode.rs adapter（TYPE-0001 域外文件）+typefactory.rs:107 Ghidra 行注释；同树 A/B（仅翻转工厂命名）E2E：xVar 918→918、uVar 191→191、差分 skeleton 4202→4201/defects 0/0/numbering 25/25=**中性**（±1 为 RUN-NONDETERM-0001 漂移）——xVar 消除被 TYPE-0001 阻塞（打印期类型来自 VarnodeBank adapter 而非工厂），确认本 TODO 的既记依赖；annotations/refs strict 全绿。等待 root commit | 2026-08-15 |
| `DWARF-TYPE-IMPORT-0001` | P1 | REVIEW | dwarf_corpus_wave | DWARF 全局变量类型导入（golden 的 `URLGlob *pUVar2` 来源） | `examples/curl_decompile.rs`, `src/debugproto.rs`, `docs/api/debugproto.md`, docs | evidence=`pending root commit`；`DebugGlobalDatabase::parse_elf` 读全 5 个 DW_OP_addr 全局（config/save/beenhere/glob_buffer/glob_expand），`resolve_type` 扩为字段化 struct/union/enum/array+typedef 物化+递归类型（FILE→_IO_FILE）back-edge 浅投影；`address_pointer_map()` 产出 &global 语义（0x17660→`URLGlob **`，单测验证 URLGlob 304B 布局 literal@0/pattern[9]@80/size@296）；命名去 `struct ` 前缀使 parseconfig/getparameter/progressbarinit 签名与 golden 逐字一致（`Configurable *config`）。**验收按「如实报告差距」分支**：输出中 `URLGlob *` 仍只来自原型路径（glob_url/next_url 签名，改动前后一致）；A/B 实测把 `URLGlob **` 接入 global_struct_ptrs 会把 printc 的全局名渲染改道成未类型化局部（`extern long glob_expand`→`extern long xVar69`）且无 `->size` 收益——LOAD 输出→PTRSUB 字段渲染链与 Symbol 驱动声明缺失，故 driver 仅接线 config（DWARF 字段化 Configurable），glob_expand 等留待 `PRINTC-SYMBOL-DECL-0001`/`FUNCDATA-LINKSYMBOL-TYPED-0001` 后解除；debugproto 单测 3/3、A/B 同 24 函数子集 skeleton 3544→3542、defects 0；NO_ORACLE（无 Ghidra DWARF-analyzer fixture） | 2026-08-15 |
| `FULL-CORPUS-0001` | P1 | REVIEW | dwarf_corpus_wave | curl example 喂全 124 函数语料 | `examples/curl_decompile.rs`, docs | evidence=`pending root commit`；driver 以锁定 12.0.4 ledger（provenance.json 124 项，Ghidra 基址 0x100000 重归一）为语料清单，ELF 符号同名同址优先（保留原子集输入不变）；WorkerTarget 增 symbol_backed 区分 ELF 符号验证与 ledger-only（PLT/init/fini/零大小符号/0x19000+ EXTERNAL）目标，预 pass 仅 ELF 符号函数参与（防 PLT 空壳注入伪 CALL 参数计数）；移除 `_start`/size<5 跳过。验收：`Summary: 124/124 processed: 76 decompiled, 0 timeout, 0 panic, 48 external-stub(no ELF code), 0 worker-failure`（EXTERNAL 项 Ghidra 在 EXTERNAL 空间合成 halt_baddata 桩，Rugra 如实分类为无代码）；新全量基线 skeleton 3929/defects 0/numbering 6（matched 24→76，+52 新函数贡献 ~+387 行=语料扩大非质量下降；同 24 子集 3544→3542 反而 -2）；timeout isolation self-test 8/8 PASS、annotations 全绿 | 2026-08-15 |
| `EXTERNAL-STUB-SUPPORT-0001` | P2 | REVIEW | external_stub_wave | 48 个 EXTERNAL 空间桩函数的签名库支持 | `src/{space,translate}.rs`, `examples/curl_decompile.rs`, `docs/api/{space,translate}.md`, `tests/oracle/external_stub_1204.{cc,rs,metadata.json}`, `tools/run_external_stub_oracle.sh` | evidence=`this commit`；**oracle 语义考证纠偏**：12.0.4 decompiler 无 IPTR_EXTERNAL（space.hh:30-38 止于 IPTR_JOIN，packed 协议拒编组 Java TYPE_EXTERNAL）；48 桩实为 Ghidra ELF importer 的**默认空间人工 EXTERNAL 内存块**（ElfProgramBuilder.java:1496-1556：0x1000 对齐 linkage 块、每 UND .dynsym import 8 字节槽，curl 输入 .bss 尽头 0x18680→块基 0x19000=Ghidra 0x119000），反编译在块内必然 BadDataError（DecompileCallback.java:417 拒编组→flow.cc:446-456：step=1+artificialHalt(badinstruction)+两条 warning；printc.cc:770-772 渲染 halt_baddata()）。已落地：(1) space 层构造期注册——`SpaceRegistry::decode_space/decode_spaces`（translate.cc:254/281 移植，TreeDecoder 属性包协议）+ AddrSpace decode 族（space.cc:87/304/339 部分构造器/decodeBasicAttributes/decode）+ `new_external_space`（Java AddressSpace.java:80 定义镜像，RUGRA-GLUE）；(2) driver 侧 48 桩投影——UND dynsym 槽序+`.gnu.version(_r)` 版本注释+24 条公开 libc ABI 签名表，输出与 golden 逐字节一致（含 `(void)` 空参、`(void)` 警告行、20 空格缩进注释、`}


` 分节符）。验收：`external_stub_1204` fixture **7/7 MATCH**（锁定 oracle 真跑双对照，runner 快照隔离+锁基 194901d）；124 语料 Summary `76 decompiled, 0 external-stub(no ELF code), 48 external-stub(import-signature declared)`（跳过数 48→0）；差分 defects=0、EXTERNAL 48 段 body 逐字节=golden（47/48 含分节符，末段差在 driver Summary 尾巴）；skeleton 3929→4585=48 段新增全配对；numbering 23 为既有非确定（RUN-NONDETERM-0001）+并行 WIP（同一失败集在 HEAD 194901d 干树上复现）。残留：printc halt_baddata 臂（PRINT-RPN-0001）、flow BadDataError 泵入管线、FID 级签名库覆盖面、fixture_registry 注册（root 串行） | 2026-08-16 || `STATIC-LOCAL-NAMING-0001` | P3 | READY | unassigned | 静态局部作用域命名（golden `my_get_token::save`） | `src/{varmap,printc}.rs`, docs | DWARF wave：类型已导入、`作用域名::静态局部` 命名法未接（Ghidra Scope 作用域链）；依赖 symbol 驱动声明链 | 2026-08-15 |
| `RUN-NONDETERM-0001` | P1 | READY | unassigned（诊断完成，修复待派工） | run-to-run 非确定已根因定位：heritage phi 放置顺序 | `src/{heritage,block}.rs`（修复），诊断只读 | 诊断完成（HEAD 194901d，只读 + /tmp 因果验证）：全语料 All 模式 6/6 次输出互异（100% 漂移）；compare 模式 main 单函数 5/8 次直接↔隔离不一致。根因链：block.rs:979 `dom_frontier: HashSet<i32>` 在 heritage.rs:3633 `for y_idx in df` 迭代（std SipHash 每进程随机种子）→ MULTIEQUAL 创建序随机 → alivelist 序漂移 → mergerequired 的 merge_marker trim 分配 size 序 (4,8,8)↔(8,8,4) → VarnodeBank::uniqid 顺延（e3b↔e23 差 0x18）→ printc.rs:879 vn_{offset} 漂移 + xVar/param 编号 +1。Ghidra oracle 用深度优先队列+有序 augment 表（heritage.cc:2439 calcMultiequals / 2394 visitIncr / 2631 placeMultiequals）本就确定。/tmp 因果验证：df 排序一行补丁后 20/20 全语料 + 10/10 main-compare 字节一致、skeleton 4201/defects 0/numbering 25 与基线同质。派工拆分（待主 agent 注册）：NONDETERM-DOMFRONT-0001（P0 根修，heritage.rs:3633 df 排序最小修或忠实移植 pq/augment；⚠ src/heritage.rs 现被 external_stub_wave CALLGUARD WIP 持有，须等租约释放）、NONDETERM-SYMLOOKUP-0002（P2 funcdata.rs:3535 values().next() 取任意符号名，潜伏不触发 curl）、NONDETERM-MERGEGROUP-0003（P3 merge.rs:1754 by_symbol.drain 指针序）、NONDETERM-DRIVERSNAP-0004（P3 examples/curl_decompile.rs:1847-1858 快照排序）、DETERMINISM-GATE-CI-0006（P1 CI 双跑 sha256 门禁）。不建议全局 -- Deterministic 编译开关（std HashMap 无法全局关随机；Ghidra 靠数据结构有序，开关会掩盖失配） | 2026-08-16 |
| `MERGE-HIGHCOVER-UNION-0001` | P1 | DONE | merge_persistent_wave | merge 合并后 high cover 未按 oracle 并集重建 | `src/merge.rs`, `docs/api/merge.md`, merge_persistent_1204 fixture 扩展 | evidence=`pending root commit`；由 MERGE-PERSISTENT-STATE-0001 REWORK 吸收（复核 M2 指令）：`merge_force` 三分支吸收实例后置吸收方 HighVariable `COVERDIRTY`（= variable.cc:660-663 HighVariable::mergeInternal 的 cover 副作用——两侧 cover 干净则 fold tv2 internalCover、否则置 dirty；Rugra 统一走 dirty+`update_internal_cover` 全量并集，结果等价），后续 `update_high_cover`（merge.cc:1572 对应物）重建并集。fixture ladder mergeadjacent 行 MISMATCH→MATCH（Ghidra 4/4/4={P,Q1,Q2,tC}，tB 经 Q1 内点 cover 正确拒绝）；merge_persistent_1204 达 **13/13 overall=MATCH**（含 pipeline_tail 新 case），root 重 pin 已完成（base=e87ebfc54444+expected sha=a02246eb…） | 2026-08-16 |
| `HERITAGE-STACKOUT-LOCK-STUB-0001` | P3 | READY | unassigned | `is_stack_output_lock()` 恒 false stub 使 guard_output_overlap_stack 半边不可达 | `src/fspec.rs`, `docs/api/fspec.md` | CALLGUARD 复核（2026-08-16）移交：cc:1487-1494 的 stack output 锁路径整体死代码；须按 oracle 实现 stack 输出锁判定后复跑 callguard fixture 观察面扩展 | 2026-08-16 |
| `OP-PREVIOUS-BLOCK-ORDER-0001` | P3 | DONE| op_previous_blockorder_wave | `previous_op_in_block` 按 bank 序而非块内序 | `src/op.rs`, `docs/api/op.md` | evidence=`this commit`（root 复跑 runner MATCH：guard1/guard2/uninsert 块序≠alivelist 序判别 + sizeOut 2 穿越/3 拒绝；E2E A/B 2842 行字节一致；调用点 merge/heritage/funcdata 影响面清单随行）（禁止本 agent 自行 commit）。已与 OP-PREVIOUSOP-ALIVELIST-0001 合并交付：previousOp/nextOp 均改为父块 `BlockBasic::ops` 下标序（basiciter 等价，按 PcodeOp 对象地址定位），块首 null、alivelist 语义（mark_alive 追加序）保持不动；oracle fixture `op_previous_block_order_1204` runner MATCH（含 reprocessFreeStores 前置 INDIRECT 链 + nextOp sizeOut∈{1,2} 穿越 / sizeOut=3 拒绝判别 case） | 2026-08-17 |
| `NONDETERM-DOMFRONT-0001` | P0 | IN_PROGRESS (交付完成待 root 集成/复核) | heritage_adt_rename_wave（并入） | heritage df 迭代确定性 | `src/heritage.rs`, `docs/api/heritage.md` | evidence=`pending root commit`；两层均落地：①最小修=place_multiequals_direct 的 df 迭代前 `collect+sort_unstable`（块索引定序）；②第二层=canonical place_multiequals/calc_multiequals 不再走 dom_frontier——merge 块由深度序 PriorityQueue（同深 LIFO）+有序 augment 向量镜像推导（cc:2448-2463/2394-2428），fixture `seq=6,3` witness 证明 merge 序为 pq 序而非块索引序。验收全过：20×All-mode sha256 全一致（6ddd813335f11316…）、20×compare-mode main 全 MATCH（normal output MATCH bytes=20195）、E2E All 124/124 processed/0 fail。生产 direct 路径的最终删除归 HERITAGE-DRIVER-SWITCH-0001 | 2026-08-16（ADT-RENAME 返工后复验） |
| `NONDETERM-SYMLOOKUP-0002` | P2 | READY | unassigned | funcdata.rs:3535 `symbol_table.values().next()` 任意符号名（不确定且语义错误） | `src/funcdata.rs`, `docs/api/funcdata.md` | 潜伏（curl 语料不触发，20/20 证明）；应取该 varnode 自身 high 的 symbol 名；触发路径单测+fixture | 2026-08-16 |
| `NONDETERM-MERGEGROUP-0003` | P3 | READY | unassigned | merge.rs:1754 `by_symbol.drain()` 按 Arc 指针序 | `src/merge.rs`, `docs/api/merge.md` | Ghidra 走 SymbolNameTree 序；当前对 C 输出良性；merge_persistent 全 MATCH 保持+20 连跑验收 | 2026-08-16 |
| `NONDETERM-DRIVERSNAP-0004` | P3 | READY | unassigned | driver 三张 HashMap 快照 stderr 序漂移 | `examples/curl_decompile.rs` | [PREPASS] 列表序逐次不同（C 输出良性）；排序后 stderr 稳定 | 2026-08-16 |
| `DETERMINISM-GATE-CI-0006` | P1 | REVIEW | tools_double_wave | 确定性双跑门禁 | `tools/check_determinism.py`（新）+ `tools/README.md` + TODO 行 | evidence=`pending root commit`（禁止本 agent 自行 commit）。已交付 `tools/check_determinism.py`：①All 模式 `curl_decompile` 无参全量 `--runs` 次（默认 2）stdout sha256 相等断言 + 退出码 0；②compare 模式 `--rugra-timeout-isolation-compare-function main`（`--compare-fn` 可重复）不得报 `isolated output changed`；③`--runs N`/`--timeout SEC`（默认 900，全量单跑实测 ~110s）；④失败输出两 sha+字节数+输出文件路径+首个差异行（行号+两侧内容），forensics 文件保留、成功才清理，退出码 0/1/2；⑤二进制快照隔离——build 一次后 copy 产物到临时目录执行整组运行（多 agent 工作区实测并发 cargo build 会在两次运行间替换共享二进制 30679B↔50966B，快照后漂移只可能来自进程内部）；⑥`--self-test`：真实跑一次 + sed 注入中间行漂移标记，断言 sha 不等且 first_diff=注入行。验证：py_compile OK；`--self-test --no-build` PASS（sha 8bfe730d12ed↔dd4270edbcbb，first diff line 1208=注入行）；完整门禁 `--no-build`（HEAD 二进制）**PASS**：all 2 跑 sha ce98b164… 一致、compare main 2 跑无 isolated-changed、sha 10c858d3… 一致；默认 build 路径实测一次正确响亮失败（exit 2，并发 WIP examples/curl_decompile.rs:492 E0502，域外文件）。CI 接线归 root | 2026-08-16 |
| `OP-PREVIOUSOP-ALIVELIST-0001` | P1 | DONE| op_previous_blockorder_wave | `previous_op_in_block` 按插入序 alivelist 而非块内序（op.cc:344） | `src/op.rs`, `docs/api/op.md`, `tests/oracle/op_previous_block_order_1204.{cc,rs,metadata.json}`, `tools/run_op_previous_block_order_oracle.sh` | evidence=`this commit`（root 复跑 runner MATCH：guard1/guard2/uninsert 块序≠alivelist 序判别 + sizeOut 2 穿越/3 拒绝；E2E A/B 2842 行字节一致；调用点 merge/heritage/funcdata 影响面清单随行）（禁止本 agent 自行 commit）。已交付（WIP 在工作树，未 add/commit）：①`previous_op_in_block`/`next_op_in_flow` 改走 parent `BlockBasic::ops` 块内序（op.cc:344/323 逐行：块首 null / `basiciter--`；`basiciter++`+endOp 时 sizeOut∈{1,2} 经 getOut(0) 穿越后继块、否则 null），`bank` 参数保留仅为既有调用点（merge.rs:2347/funcdata.rs:6197,6229/heritage.rs:2100,2127）签名兼容；alivelist 保持 mark_alive 追加序不破坏；②判别 fixture `op_previous_block_order_1204`：guard1/guard2/uninsert 三阶段块序≠alive 序（前置 INDIRECT 在 alivelist 尾），oracle 真跑双对照 runner 输出 **MATCH**（`op_previous_block_order_1204: MATCH`，expected sha 42deb93b…）；③E2E A/B（HEAD op.rs vs 修复，同 worktree 其他 WIP）：curl 全语料输出 2842 行字节一致；compare 门禁 defects=0/numbering=0（修复前后同值）；`cargo test --lib` 1388 pass，失败 5 项与 HEAD 基线相同集合（comment/dynamic/funcdata×2/ruleaction，他人 WIP 文件域）。已核 constseq.rs:1334 本地 previous_op_in_block 是独立 order 键实现（域外，未动） | 2026-08-17 |
| `FUNCDATA-NEWUNIQUE-ASSIGNHIGH-0001` | P1 | DONE| funcdata_assignhigh_wave | `Funcdata::new_unique` 缺 newUnique 的 assignHigh 半边 | `src/funcdata.rs`, `docs/api/funcdata.md`, `tests/oracle/funcdata_assign_high_1204.{cc,rs,metadata.json}`, `tools/run_funcdata_assign_high_oracle.sh` | evidence=`pending root commit + cross-review`（funcdata 核心域，禁本 agent 自行 commit）。已交付（WIP 在工作树）：①`assign_high`（funcdata_varnode.cc:48-59）补全 `new HighVariable(vn)` ctor 全副作用（variable.cc:220-235：add_instance/vn.high+mergegroup=0/setSymbol 腿）；②newVarnode 族十处接线（oracle 核实全在 **funcdata_varnode.cc** 非 funcdata.cc：:72 newConstant/:89 newUnique/:110 newVarnodeOut/:135 newUniqueOut/:157 newVarnode/:182 newVarnodeIop/:196 newVarnodeSpace(已有)/:212 newVarnodeCallSpecs(已有)/:231 newCodeRef(已有)/:604 setHighLevel 重写经 assign_high 单一真源+补 cc:600 high_level_index 字段+annotation 拒绝门）；③merge.rs wire_unique_high 自动退化 no-op（未改 merge.rs）；④fixture 20 行双侧逐字节 MATCH（hermetic runner 全绿：oracle 身份/快照/stderr 空/pinned 行/sha256 全校验）；dedup_high 行=VARNODE-COPYSYMBOL-FIELDS-0001 残差 R1 关闭观察（opSetInput dedup 副本带 high+单 instance）；⑤E2E：curl+httpd stdout 与接线前逐字节一致，Matched 123 不降，defects=0/numbering=0；`cargo test --lib` 0 新增失败（7 失败=HEAD 预存 5 + 并发 coreaction 域 2）。UNTESTED 登记：assignHigh/copySymbol 的 setSymbol mapentry≠0 腿（需活 SymbolEntry，双侧同构 guard）。注意 metadata rust_crate_tree_sha256 锚定生成时刻全 src 快照，并发 agent 提交后需 CI 再生 | 2026-08-17 |
| `MAKEREC-CALLIND-0001` | P3 | READY | unassigned | makeRec call-op 查找仅 CPUI_CALL，oracle fc->getOp() 覆盖 CALLINDIRECT | `src/coreaction.rs`, `docs/api/coreaction.md` | LINKSYMBOL 二轮复核侧注：UNTESTED 路径；CALLIND 目标时 param 名收集漏记；登记待 fixture | 2026-08-16 |
| `AUDIT-SYNTAX-SKIPLINE-0001` | P1 | REVIEW | tools_double_wave | `audit_syntax.py` 函数提取不兼容 skip_line 布局 | `tools/audit_syntax.py` + `tools/README.md` + TODO 行 | evidence=`pending root commit`（禁止本 agent 自行 commit）。已修：①skip_line depth——签名后跳过空行/少量外提局部声明行至独立 `{` 行才起算 depth（找不到则签名行单独成 body，gcc `expected '{'` 响亮失败）；②字面量安全括号计数（`cVar3 != '{'` 不再吞函数边界，glob_set 原被吞进 glob_word）；③签名词表补 Ghidra 返回类型（undefinedN/ulong/uint/ushort/byte/time_t/FILE/CURLcode…）与双词名（`processEntry _start`），golden 提取 116→124；④STUB_HEADERS 增 Ghidra 基础 typedef+不透明类型+stdarg.h+PTR_/DAT_ 约定 extern，与被审文件 inline 同名声明逐函数去重（Rugra 自产拼写保持）。验证：golden **0/116 → 104/124 OK**（20 残余 FAIL 均如实分类：golden 固有非 C 构造 `::` 域限定/`processEntry _start`/`stack0x…`/`._0_4_` 位选择器；DWARF 域结构体成员访问 URLGlob/Configurable/ProgressData/FILE/myprogress DAT_ 算术；DWARF 枚举常量 CURLE_OK/HTTPREQ_UNSPEC——非提取缺陷）；curl_cur **0/112 → 53/116 OK**，残余 63 FAIL 为当前 WIP 输出真实发现（`xunknown8` 拼写=TYPE-UNKNOWN-0001 域、`{` 前外提声明 old-style parameter、`unknown` 类型名等）。py_compile OK | 2026-08-16 |
| `PRINTC-UNLINKED-REF-0001` | P2 | DONE(diagnosis)| varmap_unlinked_locals_wave | 未链接符号 body 引用的 uVar_offset 回退（glob_url 5 名不在 fd.scope） | `src/varmap.rs`, `src/variable.rs`, `tests/oracle/varmap_unlinked_locals_1204.{cc,rs,metadata.json}`, `tools/run_varmap_unlinked_locals_oracle.sh`, 本 TODO 行 | **2026-08-17 根因诊断完成（WIP 未提交禁 commit）——原假设「varmap/variable 域入 scope 链分叉」被推翻**：① 逐行核对 ActionNameVars 局部入 scope 链（coreaction.cc:2961-2963 hasName 门 → funcdata_varnode.cc:1156 linkSymbol → database.cc:1263/1530 queryProperties/addSymbol → cc:2988-2997 buildDefaultName 命名环），variable.rs has_name（variable.cc:718-747 逐行镜像）、varmap.rs add_symbol/query_properties/build_default_name、funcdata.rs handle_symbol_conflict（cc:997-1029 镜像）全部忠实。② 单函数复现（curl_decompile --rugra-timeout-isolation-compare-function glob_url，插桩树 /tmp/rugra_vul）：5 名中 4 名的 highs 全部 implied=true（Unique+0x9100=malloc 结果 temp、Unique+0x1000004a=bool temp、Register+0x20=RSP 写实例、EAX-4 CAST 影子）→ hasName=false → **Ghidra 同判拒绝**（golden 中 malloc 内联打印 `glob_expand = malloc(0x130)`、`if (sVar2<0x1001)` 无 bool temp、无 RSP store——Rugra 的 implied 标记与 Ghidra 一致）；第 5 名 piVar1 实为已符号化的 uVar1 high（strlen 结果，Register+0x0），printc maybe_apply_type_prefix（RUGRA-GLUE，printc.rs ~4066）按 print 期 vn 类型把符号名 uVar1 改写为 piVar1 → body/decl 名分裂触发 backfill。③ 全语料 census（124 函数 E2E 插桩）：nameable∧无符号 high 计数=**0**——varmap/variable 符号覆盖无洞。④ helpf 对照证实链路健康：显式 temp 照常入 scope（Rugra sVar1/uVar2/uVar3 ↔ golden lVar1）。⑤ **结论：把 5 名强推入 scope 违反 oracle hasName 门（variable.cc:729-733/743），8→13 验收不可忠实达成**；可见现象（backfill 注入组）是 printc 侧 GLUE 兜底命名（get_varnode_display_name_inner Unique/Register fallback printc.rs ~3942-3952）+ prettyprint backfill_missing_locals 对 implied 名的注入，修复归 printc/prettyprint 租约（implied 表达式内联缺失）。⑥ 顺带定位 3 个跨域缺口供 root 派工：(a) coreaction.rs:795 register_names 硬编码 25 项 GPR 表（机制 D 红旗——Ghidra 用 translate getRegisterName 动态派发）→ golden in_XMM0_Qa vs Rugra in_register_00000110（helpf），需接 SLEIGH 注册名目录（coreaction/varmap 接线）；(b) Rugra FuncProto 无 ProtoStoreSymbol 等价（fspec.rs:1241 set_input_parameter 只改向量；Ghidra funcdata.cc:69 ctor 即 setScope→ProtoStoreSymbol，fspec.cc:3157 setInput 建 category-0 符号）→ 形参不入 ScopeLocal，未影响输入符号化为 in_XXX（golden glob_url decl 块无 in_RDX——形参在签名），funcdata/fspec 租约；(c) ActionNameVars apply 的 recoverNameRecommendationsForSymbols/lookForBadJumpTables 为登记 no-op（coreaction 租约，已有 RUGRA-GAP 注释）。⑦ **交付 fixture：`tests/oracle/varmap_unlinked_locals_1204.{cc,rs,metadata.json}` + `tools/run_varmap_unlinked_locals_oracle.sh`（不可变 fd runner，派生自 varmap_naming）——6 案例双侧逐字节 MATCH（explicit register local→lVar1 入 scope、implied unique temp→拒、spacebase 输入→拒（variable.cc:743）、illegal input→in_SREG1、addrtied stack local→lStack_b8、形参 category-0 attach 无新符号），authoritative runner 全绿 `MATCH covered_projection=6/6 overall=MATCH stdout_sha256=a46e0263…`。注意：runner pin=7644a13（编写时 HEAD），root 集成 commit 后需同步 runner 的 rugra_base_commit/tree 两行 + metadata comparand 各 sha（fixture 输出 sha 不变——oracle 侧已锁）。⑧ 无 src 改动（无可忠实修改项）→ E2E defects/numbering/Matched/gcc 基线见交接报告 | 2026-08-17 |
| `PRINTC-INPUTREG-DEADSTORE-0001` | P1 | REVIEW | ruleaction_spacebase_wave（实现: 本行⑤⑥全项落地） | `RuleLoadVarnode::correctSpacebase` 非常量 spacebase 腿（ruleaction.cc:4193-4204）→ Rust stub `None`（ruleaction.rs `correct_spacebase`，14662-14697） | `src/ruleaction.rs`, `src/arch.rs`（新增 `get_space_by_spacebase`，architecture.cc:262-281）, `docs/api/{ruleaction,arch}.md`, `tests/oracle/rule_store_varnode_spacebase_1204.{cc,rs,metadata.json}` + runner, TODO 行 | **根因诊断完成（2026-08-17，只读）**：①量化——curl E2E 输出 `= in_R[A-Z0-9]+;` store 29 处/11 函数（myprogress/GetStr/my_get_token/my_get_line/helpf/progressbarinit/glob_word/glob_set/glob_range/glob_url/next_url），外加 `= in_register_00xxxxx;` XMM 变体 44 处；全部为翻译期忠实 lift 的序言 STORE（`push rbp/rbx/r12-r15` + varargs 寄存器保存 `mov [rsp+X],r??/xmm?` + call 返回地址 push），最终 IR 地址= peelable 形态 `INT_ADD(RSP_input, const)`（03_action_ir 快照核实，RSP 全系含 input 已置 SPACEBASE flag=0x20000，ActionSpacebase 本体无恙）；②Ghidra 路径——oppool2 `RuleStoreVarnode`（coreaction.cc:5669，Rugra action.rs:796 同位注册）调 `checkSpacebase→vnSpacebase→correctSpacebase`（ruleaction.cc:4173-4204）：`getSpaceBySpacebase(reg loc,size)`（architecture.cc:262-281 逐 space×spacebase 记录匹配）+ `assoc->getContain()==ram` → STORE 重写为 stack 空间 COPY（setStackStore），随后 push/pop 对经 heritage 数据流互消、ActionDeadCode（coreaction.cc:3925 consume 分析）删 stack 空间死 varnode → golden GetStr/my_get_token 零残留；varargs 保存形如 golden my_get_line:1396-1400 `local_b0=in_RSI;…local_90=in_R9;` 与 1386-1393 `local_88=in_XMM0_Qa;…`；③Rugra 缺失者——`correct_spacebase` 非常量分支 `// Rugra lacks the spacebase→space registry … Bail out. None`（TODO(spacebase-registry) 无 TODO ID=违铁律 3 未登记），`get_contain`/`get_spacebase`/`num_spacebase` 在 space.rs:1887-1950 已有，arch.rs:568-583 已有 stack_pointer_{space,offset,size}，仅缺 Arch 级查询+接线；④修复方案——arch.rs 新增 `get_space_by_spacebase(&self, loc, size)->Option<AddressSpace>`（逐 space 调 `num_spacebase/get_spacebase` 匹配 point.size/space/offset，oracle 262-281；无匹配返回 None 而非 throw，调用侧 correct_spacebase 按 oracle 语义短路），`correct_spacebase` 非常量腿接 `get_space_by_spacebase`+`get_contain()==spc`（from_id 解 ram 已工作：const 地址 STORE 正常归一）；⑤风险项（修复时验证）——@36e4 call 返回地址 push 的地址链含 heritage seed `COPY(INT_ADD(RSP,const))`（CALLGUARD-0001 证实 seed COPY=oracle 形态），`vn_spacebase` 不剥 COPY，须核对 oppool2 与 ActionHeritage 在 mainloop 的相对时序（oracle coreaction.cc:5492 heritage 在前——首次机会仍来自未包 COPY 的原始链）与 oracle 同型函数输出；⑥验收——`python tools/compare_ghidra.py result/curl_cur.c tests/golden/ghidra_curl_1204.c`（GetStr/my_get_line/helpf 三函数 `*uVar20=`/`*uVar_9d00=`/`*uVar_a600=` 族归零，my_get_line 显形 `local_XX = in_RSI/in_XMM0_Qa` 组）+ 双侧 fixture（stack COPY/setStackStore/offset 投影 MATCH）+ gcc audit 复跑；**边界**：write-set 与 `PRINTC-UNLINKED-REF-0001`（printc.rs/variable.rs）及 SUBPIECE 域（printc.rs/type_system）零重叠；修复后 varmap local_XX 符号化/声明依赖 SCOPE-SYNC 与 UNLINKED-REF 域既有缺口，非本租约  **实现完成送审（2026-08-18，ruleaction_spacebase_wave）**：⑦落地——arch.rs 新增 `stack_base_space`（=cspec `<stackpointer space=..>` basespace，SpacebaseSpace::contain，architecture.cc:559-570/1013）+ `get_space_by_spacebase(loc_space,loc_offset,size)->Option<AddressSpace>`（architecture.cc:264-282 逐记录 size/space/offset 匹配；enum-space 注册表当前=单一 stack 记录；无匹配返回 None 非 throw——预登记偏离，isSpacebase+isInput 前置下 oracle throw 不可达）+ `get_contain(spc)->Option<AddressSpace>`（space.hh:505/159 家族迁移）；ruleaction.rs `correct_spacebase` 非常量腿接 `get_space_by_spacebase`+`get_contain()==Some(spc)`（cc:4180-4183），三 helper+两 applyOp 穿传 `Option<&Architecture>` glb（cc:4285/4326 `data.getArch()`）；引用行号修正至函数定义起始行（4173/4194/4236/4277/4319/4313/4271）；⑧fixture——`tests/oracle/rule_store_varnode_spacebase_1204.{cc,rs,metadata.json}`+`tools/run_rule_store_varnode_spacebase_oracle.sh`：锁定 oracle BfdArchitecture(GetStr@0x36d0, x86:LE:64:default:gcc) 8 case（off0/-8/swap 命中、contain 不匹配、written base、COPY wrap、LOAD、非 spacebase）+ 注册表观察行，双侧 stdout 逐字节一致（diff clean，sha256=bd259cea…，runner 端到端通过）；⑨E2E 验收（混合树实测）——`= in_R[A-Z0-9]+;` store 29→7（my_get_line 4/glob_word 2/glob_set 1；myprogress/GetStr/my_get_token/helpf/progressbarinit/glob_range/glob_url/next_url push 家族全消），`= in_(R|XMM|register)` 50→15；defects=0/numbering=0/Matched 123 全保持，skeleton 3088→2926，gcc audit 107OK/16FAIL（=基线），throw/panic 0；⑩残余归因（探针实测后已删）——剩余 7 处地址链均为 `INT_ADD(MULTIEQUAL_written_RSP, const)`/`CALL-def`（sb=true 但 written≠input，cc:4179 守卫；或 def≠INT_ADD，cc:4208 守卫），oracle 同款算法同样拒绝，golden 消除依赖上游 phi 折叠/stackptrflow 归一化（多 agent 域，非本租约 write-set）→ 建议新 TODO 登记 RSP-phi 折叠缺口；@0x36e4 call-push 家族同因（COPY-wrap 变体），风险项⑤结论：Rugra mainloop 顺序=oracle（action.rs:1004 heritage :5492 → oppool1 → oppool2），首机会确来自未包装原始链；⑪测试——ruleaction 单测 9/9（含新增 3：注册表命中/contain 不匹配/spacebase 输入链+swap）；cargo test --lib 1422 通过/5 失败（comment/dynamic/funcdata×2/rule_add_unsigned 经 HEAD-only worktree 复验均为先存失败，非本次引入）。待 root 复核 + 集成 commit（机制 B/C：ruleaction 白名单+主管线 Rule 域）。**接续 agent 复核+验收（2026-08-18）**：①逐 hunk 对照 oracle（ruleaction.cc:4165-4341/architecture.cc:264-282/translate.hh:172-189/translate.cc:86-124）确认实现忠实，修正引用漂移——get_contain/stack_base_space 注释由 space.hh:159/174 改为 SpacebaseSpace 真实位置 translate.hh:187/174（源码 arch.rs/ruleaction.rs + docs/api 两处）；②官方 runner tools/run_rule_store_varnode_spacebase_oracle.sh 端到端全绿（RUNNER_EXIT=0，输出 `MATCH cases=9 records=18 dependencies=ARCH-0001 untested=CALLSPECS-0001,SCOPELOCAL-0001`），metadata 补齐（BFD library sha 抄写笔误修正、runner_sha 锚定自引用版 runner、crate 树哈希——**集成时若 src/ 再变需重算 rust_crate_tree_sha256 并重跑 runner**）；③E2E 复测：skeleton 2926、defects=0/numbering=0、Matched 123、gcc 107 OK/16 FAIL、E2E stderr throw/panic=0；push 家族 `*uVar20 = in_RBP` 归零，残余 `= in_R` GPR 7 处+XMM 变体 7 处=14 行/3 函数（my_get_line 11/glob_word 2/glob_set 1，全部 `*uVar20`/`*uVar_9d00` 指针链 STORE，上游 phi/stackptrflow 域=RULE-RSPPHI-FOLD-0001）；④5 个失败测试经 HEAD-only worktree 复验均为先存（与 WIP 无关），混合树 1426 通过（+7 全为 WIP 新增）。 | 2026-08-18（27 记录终版） |
| `RULE-RSPPHI-FOLD-0001` | P2 | READY | unassigned | RSP phi 折叠/stackptrflow 归一化缺口（残余 7 处 `= in_R` 死存储的上游） | `src/{ruleaction,coreaction}.rs` 或 transform 域, fixture | INPUTREG 修复复核归因：残余 7 处（my_get_line 4/glob_word 2/glob_set 1）地址链为 MULTIEQUAL-written RSP 或 CALL-def，被 oracle 同款 !isInput(cc:4179)/!=INT_ADD(cc:4208) 守卫拒绝——golden 的消除依赖上游 phi 折叠/stackptrflow（RulePushMulti/ActionStackPtrFlow 族），Rugra 侧缺口待定位 | 2026-08-17 |
| `CALLSPECS-0001` | P3 | REGISTERED | unassigned | callspec 尾部 TODO 账目补登（fixture metadata 引用而无行） | 见各 fixture metadata | INPUTREG 复核发现：rule_store_varnode_spacebase_1204 metadata 引用本 ID 但 TODO_BOARD 无条目——账目缺口补登，实质内容见引用处 | 2026-08-17 |
| `SCOPELOCAL-0001` | P3 | REGISTERED | unassigned | scope-local 尾部 TODO 账目补登（同上） | 见各 fixture metadata | 同上 | 2026-08-17 |
| `HERITAGE-RAW-OPINSERT-0001` | P2 | READY | unassigned | 三处 heritage 插入点经 op_insert_before 的 INDIRECT 回退 vs oracle 裸 opInsert | `src/heritage.rs`, `src/funcdata.rs`, docs | ADT 三轮复核 R1：cc:294/546/602 全用裸 opInsert（funcdata_op.cc:150-159），Rust 走 opInsertBefore 的前导 INDIRECT 组回退（funcdata_op.cc:351-362）——锚前驱为存活 INDIRECT 时（混合尺寸 guard 组+死 target）插入位分叉；UNTESTED 角落；修法=元素定位+裸 op_insert 原语（约 4 行）+判别 fixture | 2026-08-16 |
| `OP-SETALLINPUT-TRUNCATE-0001` | P3 | READY | unassigned | opSetAllInput 截断为 2 输入 vs Rust stale inrefs[2..] | `src/funcdata.rs`, `src/op.rs`, docs | ADT 三轮复核 R2：funcdata_op.cc:280 setNumInputs(2)；>2 输入 ME 标记转换后 Rust 残留 stale 输入；fixture ME 均 2 输入未暴露 | 2026-08-16 |
| `SPACE-PRINTRAW-WORDSIZE-0001` | P3 | DONE| space_printraw_wave | printRaw wordsize>1 分支 + addr_size 硬编码 8 | `src/space.rs`, `docs/api/space.md`, `tests/oracle/space_printraw_wordsize_1204.{cc,rs,metadata.json}`, `tools/run_space_printraw_wordsize_oracle.sh`, 本 TODO 行 | evidence=WIP（本 agent 禁 commit，root 串行集成）。已实施（2026-08-17）：可达性复核=wordsize>1 在当前 x86-64 闭包不可达（x86-64.sla ram/register 及全部硬编码空间 wordsize=1）；registry `AddrSpace::print_raw`（space.rs，ff3f6c8 已入）的 byteToAddress 缩放+`+cut` 后缀经锁定 oracle 逐字节验证等价；legacy enum `AddressSpace::addr_size()`（原恒 8）按构造器真值建模：unique=UniqueSpace::SIZE=4（space.cc:418/428）、join=sizeof(uintm)=4（types.h:27、space.cc:447）、const/OTHER/iop/overlay=8、x86-64 ram/register/stack=8；受影响面（heritage.rs:1918 警告宽度、translate.rs addr_mask_for/resolveConstant、pcodeparse.rs addressOf unique 角落）随枚举自动对齐。fixture `space_printraw_wordsize_1204`（5 case 61 行：addrsize 投影 9 空间+legacy 9 映射、wordsize 1/2/4 打印含收缩规则/+cut/setw 最小宽度、const/OTHER 覆盖）：runner 全绿 `space_printraw_wordsize_1204: MATCH`（锁 oracle 重建+pin base dce02f78+space.rs overlay+期望 sha256）。E2E（隔离快照 HEAD+仅本 overlay，并发 agent 活树编译受阻）：Matched 123 不降、defects=0、numbering=0，与纯 HEAD **逐字节相同**（sha 1dcbad59… 双侧一致=零漂移）。cargo test --lib 1388 过/5 失败=已知 5 域（comment/dynamic/funcdata×2/ruleaction）基线一致。annotations --all ✅、refs --all --strict ✅。残差（建议新原子，非本 TODO write-set）：JoinSpace::printRaw（space.cc:590）pieces 形式与 IopSpace::printRaw（op.cc:41）op 形式未派发（需 JoinDB/manager 回链与 PcodeOp 下钻）；heritage.rs:1918 内联复刻未含 wordsize>1 缩放（不可达，触发前应迁 space.print_raw 调用） | 2026-08-17 |
| `SPACE-PRINTRAW-SPECIAL-0001` | P3 | DONE | space_printraw_wave | JoinSpace::printRaw 特化派发（space.cc:590）+ manager join 半部（translate.cc:671-762） | `src/space.rs`, `docs/api/space.md`, `tests/oracle/space_printraw_special_1204.{cc,rs,metadata.json}`, `tools/run_space_printraw_special_oracle.sh`, 本 TODO 行 | evidence=WIP（本 agent 禁 commit，root 串行集成）。已实施（2026-08-17）：registry 侧新增 `space::manager_join` 模块=AddrSpaceManager join 半部 1:1 对应物（JoinRecord translate.hh:196 + operator< translate.cc:172 含 VarnodeData::operator< pcoderaw.hh:64 的 space-index/offset/size-降序/BIG-first 与短前缀更小；JoinRecordTables=joinallocate+splitset+splitlist translate.hh:233-235；find_add_join translate.cc:671-715 四条逐字校验+去重+16 字节 roundsize；find_join translate.cc:746-762 二分+`Unlinked join address`）；`AddrSpaceInner.manager_join_tables`=space.hh:118 `AddrSpace::manage` 的 join 半，insert_space 注册 Join 空间时接线（校验前，与 Ghidra 构造期持有等价）；`print_raw` 派发新增 Join→`print_raw_join`（逐 piece 递归 `vdat.space->printRaw`，num==1 的 szsum 被统一逻辑尺寸覆盖后 `:szsum` 追加的怪癖逐字保留）。fixture `space_printraw_special_1204`（3 case 11 行：2/3/1-piece pieces 形式、wordsize-2 piece 递归含 `+cut`、去重、分配序列 0x0..0x40、未链接异常文本）：双侧逐字节 MATCH（Ghidra 侧 ~AddrSpaceManager 堆分配空间防析构 double-free）；runner `tools/run_space_printraw_special_oracle.sh`（pin base 102c476 + space.rs/op.rs overlay + 期望 sha256=08cbbe56…）。旧 space.rs JoinDatabase/arch.join_db 与 translate.rs 枚举 join 半不动（他人域，ADDRESS-0001 统一时迁移）。空间耦合处理：space.rs 的 Iop 派发臂内联残差回落、不引用 op.rs 新符号，经 base ae2385a8+当前 space.rs 独立编译验证零破坏（address_space_handle/space_registry/external_stub 等仅 overlay space.rs 的既有 runner 不受影响）；连带重 pin `space_printraw_wordsize_1204.metadata.json`（rugra_space_source+api_document 两 hash，CSPEC 重 pin 先例）并权威重跑 `space_printraw_wordsize_1204: MATCH`。E2E（强制全量重编）defects=0/numbering=0、输出与改前 sha 1dcbad59… 逐字节相同；cargo test --lib 1412 过/5 失败=已知基线（comment/dynamic/funcdata×2/ruleaction）；annotations --all ✅、refs --all --strict ✅ | 2026-08-17 |
| `SPACE-IOP-PRINTRAW-0001` | P3 | BLOCKED | unassigned | IopSpace::printRaw 特化终态渲染（op.cc:41-59 两种形式） | `src/op.rs`（派发拓扑已入）, `src/address.rs`+`src/block.rs`+flow.rs（阻塞面，非本 write-set）, `docs/api/op.md` | 残差登记（2026-08-17）：op.cc:46 offset→PcodeOp 下钻在 Rugra 为 `Arc::as_ptr` 指针编码（new_varnode_iop 同源，get_op_from_const 可逆）；非分支形式需 `SeqNum.addr` 的 `pc.printRaw`（address.cc:32 operator<<：pc.printRaw+':'+uniq 粘滞 hex），分支形式需父块 `sizeOut()==2 ? isFallthruTrue?getOut(0):getOut(1) : getOut(0)` 目标块起始地址的 `code_`+shortcut+printRaw——但 legacy `SeqNum.addr`（address.rs:127）与 `BlockBasic::start_addr`（block.rs，flow.rs:1918 赋 `Address::new(start)` 标量）均无空间句柄，宽度/wordsize 缩放与 getShortcut 不可导出。阻塞链 ADDRESS-0001（address.rs 现由 CSPEC-RANGEPROPS-0001 租约）。已落地可判定部分：`SpaceType::Iop` 派发分支 + `IopSpace::print_raw`（residual stub，两形式 None 回落 base 形式=特化引入前可观察行为）+ `PcodeOp::is_fallthru_true`（op.hh:193）。解除阻塞后：下钻按 funcdata.rs get_op_from_const 的 Arc::from_raw/clone/forget 模式（或只读裸引用），fixture 扩展 op 序号与 block 形式双侧逐字节 | 2026-08-17 |
| `PRINTC-LEGACY-DECL-DUP-0001` | P1 | DONE | legacy_decl_dup_wave | 打印层遗留声明组与符号驱动声明重复（uVar20 等 20 处） | `src/prettyprint.rs`, `docs/api/prettyprint.md`, 本 TODO 行 | evidence=`this commit`（root 亲测复核：numbering=0/defects=0/gcc 19 FAIL/annotations+refs 绿）。已实施（2026-08-16）：根因=flush_func_remove_unused 的 type_ok 表识别不了 oracle ptr_expr join 的 2-token 形 `char *uVarN;`（printc.cc:73-77 spacing=0；前次"三 token 指针形"白名单扩展从不命中）→ 重复注入 `int uVarN;` → fix_unary_deref 重写为 `char *uVarN;` 掩盖重复；last_decl_idx=0 时注入落 sig 与 `{` 之间（K&R old-style）。修法（oracle：printc.cc:2656 docFunction 声明唯一源=emitLocalVarDecls，prettyprint.hh:547 EmitNoMarkup 零 post-process）：新增 `has_symbol_driven_decls`（prettyprint.rs:3014，保守判据=声明块含 undefinedN 类型 token 或 in_ 名字 token，两拼写仅符号驱动发射器可产）+ `symbol_driven_function_line_mask`（:3051）；`flush_func_remove_unused`（:3157）对符号驱动函数整块直通（不删 unused 不注入——oracle 无条件发射所有符号）；`fix_unary_deref_declarations`（:2634）跳过符号驱动函数行（不重写符号标量声明）；`backfill_missing_locals`（:2461）**保留**（declared 收集本就处理两种指针拼写，只补符号块外真缺名=未链接符号引用 PRINTC-UNLINKED-REF-0001 域——任务要求的保留语义）。验收（12.0.4 golden，result/curl_cur.c）：**numbering 20→0**（8 函数 uVar20/18/206/0/28 declared twice 全消）、defects 0→0、gcc 审计 **24→19 FAIL**（old-style 7 错→0、redeclaration→0；_init/FUN_00102020/SetHTTPrequest_part_0/deregister_tm_clones/register_tm_clones 恢复 OK 零新增）；skeleton 4368→4403（+35：backfill 注入组按指令保留，17 函数仍走 backfill 路径：_start/__do_global_dtors_aux/myprogress/GetStr/my_get_token/my_get_line/file2string_part_0/parseconfig/getparameter/SetHTTPrequest/glob_word/glob_set/glob_range/glob_url/next_url/match_url/__libc_csu_init；对照实验三 pass 全旁路 skeleton 4052 但 gcc 25 FAIL）。单测：cargo test --lib 1357 过/5 失败=已知 5 域（comment/dynamic/funcdata×2/ruleaction，改动前基线一致，prettyprint 无测试模块）。annotations --all ✅、refs --all --strict ✅、机制 A 块 dry-run ✅。## Differential：numbering=0+defects=0 可提交；skeleton +35 归因 backfill 保留（见上）。残差：17 函数的 backfill 注入组行本身不在 golden（终极消除依赖 PRINTC-UNLINKED-REF-0001 的 printc/variable 侧符号覆盖修复） | 2026-08-16 |
| `NUMDECL-DOUBLE-V-0001` | P1 | DONE | numdecl_double_v_wave | 同名异类型双声明（`FILE *V; int V;` 形态：SUB-A 符号背书名 print 期前缀重写分裂 + SUB-B backfill 子串幻影注入） | `src/prettyprint.rs`, `src/printc.rs`, `docs/api/{prettyprint,printc}.md`, 本 TODO 行 | evidence=`this commit`。已实施（2026-08-25，worktree agent/numdecl）：①症状量化（committed-tree 基线 E2E defects=0/numbering=0 下）——SUB-B 18 处 backfill 幻影声明/8 函数（`plVar64` 内部 `lVar` 前缀命中→注入 `long lVar64;` 与符号声明 `long *plVar64;` 并存）+ SUB-A 9 对/7 函数（main `int iVar37;` 符号声明 + body 被 maybe_apply_type_prefix 重写为 `piVar37` → backfill 注入 `char *piVar37;`）；②SUB-B 修复=prettyprint.rs backfill_missing_locals 扫描器左词边界（p==0 或前字节非 [A-Za-z0-9_]；C 标识符集）+ 修正误引注解 `Ghidra: prettyprint.hh:547 EmitNoMarkup::backfillMissingLocals`→RUGRA-GLUE（锁定 oracle prettyprint.hh:547 为 EmitNoMarkup 类声明，oracle 零 backfill pass）；③SUB-A 修复=printc.rs get_varnode_display_name_inner + push_varnode Priority 1 两处镜像 printlanguage.cc:238-262 pushSymbolDetail 分支：high.symbol.is_some() → high.get_name() 逐字（oracle printc.cc:1905-1936 pushSymbol 打印 sym->getDisplayName() 无类型前缀重写），symbol-less 保留 GLUE 前缀重写（未链接引用域不变）；④验收（12.0.4 golden，flock+共享 target E2E）：幻影 27→0（SUB-B 18+SUB-A 9 全消）、defects=0/numbering=0 保持、124/124 处理 76 反编译/0 超时/0 panic 与基线一致、语句结构零丢失（前缀归一化后逐语句 multiset 相同，11 函数仅声明行减少）、audit 错误 15→15（file2string 1 处形态变化=符号类型 int vs 实例 int* 的 varmap 域既登记残差）；⑤18 个 funcdata::tests 失败经 pristine HEAD worktree 复验为分支预存（blockstruct 域，与本改动无关）。残差：符号声明类型陈旧（命名期类型前缀）归 varmap 域 PRINTC-UNLINKED-REF-0001 诊断⑤；真未链接引用（glob_set piVar1、unique0x/register0x token）注入组保留至该域关闭 | 2026-08-25 |
| `HERITAGE-COLLECT-WRAPAROUND-0001` | P3 | DONE| heritage_collect_wraparound_wave | collect end 地址回绕钳位（heritage.cc:317-320）未镜像 | `src/heritage.rs`, `docs/api/heritage.md`, `tests/oracle/heritage_collect_wraparound_1204.{cc,rs,metadata.json}`, `tools/run_heritage_collect_wraparound_oracle.sh`, 本 TODO 行 | evidence=WIP（本 agent 禁 commit，root 串行集成；机制 C 白名单，待独立复核）。已实施（2026-08-17）：collect 镜像 oracle 钳位——`endaddr = wrapOffset(addr+size)`（u64 wrapping add=Rugra 全空间 8 字节寻址下的 oracle 算术，`size as u64` 符号扩展=int4→int8）落到 start 之下时窗口终点不用 beginLoc(endaddr)（回绕小偏移会立刻截断成空窗）而钳 `endLoc(space,getHighest())`=下一空间边界（varnode.cc:1596-1602），即从 start 扫到本空间末尾：拆分为「异空间成员 break」+「仅非回绕时偏移上界 break」。可达性判定（fixture 反证构造）：MemRange 跨越空间顶端=offset 0xffffffffffffffff 且 size>1 的 varnode 进入 disjoint cover（cc:2708-2710）或恰顶字节（endaddr 回绕到 0）；真实 loader 不产出，预存非 r2 引入。fixture `heritage_collect_wraparound_1204` 三案例（straddling_write 跨顶 SUBPIECE 链接 / nonwrap_control 同形 0x206 对照 / exact_top 恰顶直连含闭区间上边缘）：锁定 oracle 逐字节 MATCH（runner 全绿 `heritage_collect_wraparound_1204: MATCH covered_projection=3/3 overall=MATCH stdout_sha256=41fcb441…`，pin base c484715+heritage.rs overlay）；判别力实证=去掉钳位后 Rust comparand case A 断言中止（exit 101，free read 不解析）。E2E 新鲜捕获：defects=0/numbering=0/Matched 123 不降；cargo test --lib 1391 过/5 失败=已知 5 域基线一致、heritage:: 12/12 绿；annotations --all ✅ refs --all --strict ✅。注意：5 个旧 heritage fixture runner 的 `heritage_rs_sha256` pin 因本改动失效（各 pin 历史快照，root 需按序 re-pin 或按惯例保留历史态） | 2026-08-17 |
| `SCOPE-FINDOVERLAP-KEY-0001` | P2 | DONE| funcdata_wave | `scope_local_find_overlap` min-start vs oracle 分区单元 owner+EntrySubsort | `src/funcdata.rs`, `docs/api/funcdata.md`, `tests/oracle/scope_find_overlap_1204.{cc,rs,metadata.json}`, `tools/run_scope_find_overlap_oracle.sh`, 本 TODO 行 | evidence=WIP（本 agent 禁 commit，root 串行集成）。已实施（2026-08-17）：`scope_local_find_overlap` 重写为 rangemap 分区语义——同空间非动态记录中取与 `[offset,offset+size)` 相交的最左分区单元（= min over 相交记录的 max(start,offset)，即 `find_overlap` `lower_bound(AddrRange(point))` 首单元，rangemap.hh:411-423），单元内按 `entry_subsort_key`（`getSubsort` database.cc:97-107：usepoint=None→addrtied 最小 (0,0)，否则 (1,首用点)；同二进制代码空间 index 一致故常量 1 建模）取最小，等值按 Vec 序（=multiset 等价键插入序）。判别 fixture `scope_find_overlap_1204`（7 记录）：oracle 实测 `partition_usepoint` 查询 0x309:2 答 **narrow**（usepoint 0x1000）而旧 min-start 实现答 wide（start 0x300）——分歧实证；另有 addrtied_wins（addrtied 胜过先插入的 use-limited）/gap_query（未覆盖起点由最左相交单元答）/gap_query_short（null）。runner（pinned base=9ea2a78+funcdata.rs overlay、oracle commit/tag 校验、expected stdout sha256 比对）：`scope_find_overlap_1204: MATCH`。E2E：defects=0/numbering=0/skeleton 4403=基线零漂移。待机制 C 独立复核 | 2026-08-17 |
| `SCOPE-FINDOVERLAP-DYNAMIC-0001` | P3 | DONE| funcdata_wave | find_overlap 缺 is_dynamic 守卫 | 同 F1 write-set | evidence=WIP。已实施（2026-08-17）：`scope_local_find_overlap` 候选集先 `.filter(|sym| !sym.is_dynamic)`——对齐 `addDynamicMapInternal`（database.cc:1866-1876）只入 dynamicentry 不入 maptable，Ghidra findOverlap 永不见动态条目；Rugra 动态符号（Stack/start=0）不再可能压过静态答案。fixture `dynamic_null`（0x0:8→null）与 `dynamic_no_shadow`（staticfar 不被遮蔽）双侧 MATCH | 2026-08-17 |
| `SCOPE-USEPOINT-WARNING-0001` | P3 | DONE| funcdata_wave | funcdata.rs:2279 unused usepoint warning | `src/funcdata.rs`, `docs/api/funcdata.md`, 本 TODO 行 | evidence=WIP。已实施（2026-08-17）：Ghidra `syncVarnodesWithSymbols` cc:974 将 `vnexemplar->getUsePoint(*this)` 作为第三实参传入 `Scope::inScope`（database.hh:597，虚签名保留、基类实现忽略）；Rugra `scope_local_in_scope` 增 `_usepoint: Option<u64>` 参数并在调用点喂 `varnode_use_point_offset(self,&vnexemplar)`——调用形态与 oracle 一致，语义零变化（基类只查 rangetree），warning 消除且 `varnode_use_point_offset` 不再死代码 | 2026-08-17 |
| `VARNODE-ADDDESCEND-THROW-0001` | P2 | DONE| varnode_adddescend_wave | `Varnode::addDescend` 恢复 Ghidra throw 语义；op_insert_input raw push 收编（**✅ 已交付 2026-08-17**，funcdata 域子项，见 ⑧） | `src/varnode.rs`, `docs/api/varnode.md`, `tests/oracle/varnode_add_descend_1204.{cc,rs,metadata.json}`, `tools/run_varnode_add_descend_oracle.sh`, 本 TODO 行 | **2026-08-17 交付（WIP 未提交，禁 commit）**：①`add_descend`（varnode.rs:1788）WARN 软化→`panic!("Free varnode has multiple descendants")` 逐字对齐 varnode.cc:336 LowlevelError，panic 在 push/coverdirty 前（状态不变语义一致）；错误通道=panic 非 Result：Ghidra void+throw，E2E worker per-function 隔离把 panic 归入函数失败桶=Ghidra LowlevelError 单函数中止模型（memstate.rs:510 先例）。②新增单元测试 `test_add_descend_throws_on_free_multi_descendant`（throw 文本+状态不变+常量无豁免）。③fixture `varnode_add_descend_1204`：权威 immutable runner exit0，**MATCH 7/7 行双侧逐字节一致**（free 首后继 OK/第二后继 throw 文本逐字+状态不变/spacebase 豁免/written/input 累积/常量 throw/push 顺序 getTime 序）；coverage 登记 dead-Weak 过滤与 high coverdirty 传播=UNTESTED-in-oracle。④生产者全仓扫描：直接 add_descend 调用点全为镜像或 fresh-per-read（varnode.rs:2219 replace=cc:1347 镜像、funcdata op_set_input=cc:123 镜像、inject fresh、heritage rename vnnew 恒非 free、其余测试单后继）；raw descend.push 旁路（**不改 throw 语义但留登记**）：funcdata.rs:1487 op_insert_input（Ghidra funcdata_op.cc:308-318 走 opSetInput→addDescend，收编留待 funcdata 租约释放）、coreaction.rs:730/1112、funcdata.rs:11696、ruleaction.rs:214。**⑤E2E 门禁结果（如实登记，root 决策集成时序）**：恢复 throw 后 curl 124 函数 **56 decompiled/20 panic**（基线 75/0）——20 函数同一根因家族：SLEIGH BOOL 1 字节 flag 读（Register 0x206 / Unique temp，flags=COVERDIRTY-only=从未 SSA 化）以 free-with-reader 存活到 ActionSetCasts，cast_input（coreaction.rs:3124=cc:2702-2712 忠实镜像）的 CAST 成第二读者→throw；Ghidra 同形状同 throw，但 Ghidra 管线 fullloop 后 merge/MapGlobals/NameVars 不会把输入换回 free 翻译期 vn（HERITAGE-FLAGFREE-SSA-0001 已 ptr-eq 证实 Rugra rules/merge 存在换回，残差 44 家族）——**修复属 ruleaction/merge/coreaction 域，本 agent 禁越界**。⑥`cargo test --lib` 1371/13（基线 5+8 新 throw 触发：funcdata split_uses/unlink_then_destroy、heritage op_heritage_leaves_deadcode、ruleaction sub_commute/sub_right、subflow dumpty_hump×2/split_copy——全部为测试 wiring 构造 Ghidra 不可达状态：free-with-reader 喂给 rule 相代码/共享 free 对象模拟多次读；Ghidra dump 每次 read 造独立 free vn，修复=测试 harness 标 WRITTEN/INPUT 或分立 free 对象，域归各文件 owner）。⑦annotations --all/refs --strict 绿。**残差绑定**：上游 BOOL-swap-back 家族（HERITAGE-FLAGFREE-SSA-0001 域）修复前 E2E 20 panic 不归零——throw 落地 mainline 需 root 排序（先修换回 or 保持 WIP）；op_insert_input 收编=下轮 funcdata 租约释放后。root 时序决策（2026-08-17）：函数级已 oracle 证明（varnode_add_descend_1204 runner 7/7 MATCH）；但 throw 单独落地使 E2E 75/0→56/20 panic——20 panic 全部同源 FLAGFREE 家族（rules/merge 把输入换回 free 翻译期 vn，Ghidra 管线不产生该状态=同输入 Ghidra 成功），按机制 B2 单独落地是端到端倒退。src/varnode.rs WIP 已 stash（stash@{0}），landing gate=HERITAGE-FLAGFREE-SSA 家族修复（同 wave 或先行）；fixture/runner/docs 留工作树待随家族落地一并集成。**⑧op_insert_input 收编子项交付（2026-08-17，funcdata 域 agent，WIP 未提交，禁 commit）**：(a) `op_insert_input`（funcdata.rs:~1561，`&self`→`&mut self`）改为镜像 `Funcdata::opInsertInput`（funcdata_op.cc:308-317）两步：`PcodeOp::insertInput(slot)`（op.cc:311-318 扩槽右移——Rugra Vec 无法物化瞬态 NULL，以 split_off 尾部 + 委托 `op_set_input` 追加进新槽实现，两步间全部 Ghidra 语句对 NULL 槽为 no-op：cc:107 vn≠NULL、cc:118-121 opUnsetInput 有 NULL guard，无 sentinel 分配）→ **完整 opSetInput 路径**（常量去重 cc:108-115、`add_descend` free 检查 panic 通道+coverdirty varnode.cc:330-340、descend/簿记）——消除最后一个 addDescend 同族生产者旁路（④所列 funcdata.rs:1487）。调用面 20 处全 `&mut` 可达（transform/heritage/coreaction/ruleaction/funcdata 内部+测试），零调用点改动。(b) fixture `funcdata_op_insert_input_1204`（新 .cc/.rs/metadata + `tools/run_funcdata_op_insert_input_oracle.sh`，run_op_insert 简式 runner）：**MATCH 6/6 行双侧逐字节一致**——slot 前缀移位（shift [zero,reg] regdesc=1）/尾部追加+中位右移（[a,c,b] bdesc=1）/同 vn 双槽双 descend（cc:107 只比新 NULL 槽）/常量去重触发（clone distinct=1 offset 同 origdesc=1 clonedesc=1 numVarnodes+1）/free 首后继 OK/free 第二后继 throw 文本逐字+状态不变（throw 先于 push/flags）；declared_divergence 登记 throw 后 abandoned op 槽态（C++ 留扩出的 NULL 槽 vs Rust 未物化，生产 LowlevelError 中止整函数不可观察）与 newOp(N) NULL 槽表征差（fixture 全 newOp(0) 规避，见 op_insert_1204）。(c) E2E A/B（/tmp 受控 worktree，HEAD=004816a 单文件隔离）：curl 输出 **byte-identical**，defects=0/numbering=0/**Matched 123** 不降，throw/panic **0 触发**（124/124，75 decompiled/0 panic/1 timeout）；httpd 输出 byte-identical，6 throw=HEAD 预存同函数集（ap_matches_request_vhost/ap_update_vhost_from_headers/ap_strcasestr 等，经既有 op_set_input 路径，非本收编新增）；gcc 审计 107 OK/16 FAIL=基线。(d) `cargo test --lib` 1413→1419 过（HEAD 间 42653f5 增测试），失败恒 5=预存（comment/dynamic/funcdata×2/ruleaction）零新增。annotations --all/refs --strict 绿。(e) 连带编译兼容：6 个 oracle fixture 的 `fn set_input(&self`→`&mut self`（action_merge_order/cover_rebuild/cover_twopiece/merge_persistent/merge_gates/subflow_outvn，纯机械 receiver 修正，rustc 编译验证 7/7 OK）+ 对应 6 个 metadata `rust_fixture_sha256` 重钉；其 heavy runner 的 crate-tree pin 在 585bdd7 后本已 stale，重验属 root 集成批。write-set：src/funcdata.rs、docs/api/funcdata.md、新 fixture 四件、6 fixture+6 metadata 编译兼容、本行。机制 C 复核待 root（funcdata 核心域）。 | 2026-08-17 |
| `HTTPD-ADDDESCEND-THROW-0001` | P1 | READY | unassigned | httpd 语料 6 函数触发 addDescend throw（根因已定位=支配树根选择错误，非 setcasts 同族） | `src/block.rs`（calc_rpo/build_dom_tree）, `src/funcdata.rs`（structure_reset 接 force_restructure）, `docs/api/block.md`, `docs/api/funcdata.md`, oracle fixture, 本 TODO 行 | **根因诊断完成（2026-08-17，只读诊断 agent，/tmp/rugra_httpd_diag 插桩 9 轮）**。①6 触发点（默认 30 函数口径，200 口径 22 个同族 ap_* 函数）：ap_matches_request_vhost/ap_update_vhost_from_headers/ap_strcasestr/ap_pregsub/ap_getparents/ap_getword，全部同一形态：`rule=cond_negate` 对 BOOL_NEGATE@X slot0 挂 free 1 字节 vn（register:513:1=SLEIGH flag 读 或 unique:N:1），旧读者=同地址 CBRANCH，action=mainloop/stackstall/simplifypool。②census（watch register:513:1 idx=43，ap_matches_request_vhost）：翻译期创建、CBRANCH@0x2d69f 首读者 → heritage pass0 全链正常（SPACE-COLLECT→COLLECT-READ→GUARD-ACTIVE，collect/guard 无缺口）但 **RENAME-VISIT 只有 block 0 的 idx=30 被消费**；RENAME-PROBE：nb=9 中 8 块 uncovered（6 块从块 0 可达），DOMCHILD 全部进 dead bucket `9->[0..8]`，IDOM 全 None。③根因链（行级）：Rugra `build_dom_tree`（block.rs:2134）根=RPO[0]，而 `calc_rpo`（block.rs:2412）入口候选（size_in==0||ENTRY_POINT）按**向量序** DFS，尾部孤儿块（[7 in0 out0 r0][8 in0 out0 r0]）最后完成 DFS → reverse 后 RPO[0]=孤儿块 8 → CHK 以孤儿为根、其无出边 → 全函数 idom=None → build_adt（heritage.rs:679，镜像 cc:2334-2351）把所有块塞 domchild[size] dead bucket → rename_recurse（heritage.rs:4459，镜像 cc:2479）只访问块 0 → 块 1+ 的 free flag 读永不 SSA 化（Ghidra 对应步 cc:2499-2502 setInputVarnode 替换+cc:2519-2520 删旧 free vn 从不发生）。④第二层缺口：结构化删孤儿后（nb 9→6）后续 build_dom_tree 已能算出正确 idom（census pass1 idom=[0:0 1:0 2:1...]），但 oracle structureReset（funcdata_block.cc:712-720：structureLoops→calcForwardDominator→heritage.forceRestructure）的 **forceRestructure 在 Rugra 无任何调用点**（全仓 grep 0 命中）→ maxdepth 不回 -1 → domchild 永远 stale。⑤终点链（全部忠实镜像非缺陷）：blockstructure negate_condition（block.rs:1321=block.cc:2351）在该 CBRANCH 置 BOOLEAN_FLIP → RuleCondNegate（ruleaction.rs:6644 镜像 ruleaction.cc:5476-5494，**12.0.4 oracle 确无 isWritten 守卫**）op_bool_negate 挂第二读者 → add_descend panic（varnode.rs:1809=varnode.cc:336 同形状同 throw，Ghidra 因 heritage 已 SSA 化条件读而不可达该状态）。⑥curl 0 触发原因：curl 函数 CFG 无"向量序晚于入口的 in0 孤儿"，RPO[0] 碰巧=块 0——潜在 bug 被 httpd CFG 形态暴露；非 HERITAGE-FLAGFREE setcasts 早跑同族。⑦A/B 验证（/tmp 副本 2 行近似 oracle 根语义：root=list[0]+入口候选重排）：httpd throw 6→2、RENAME-PROBE reach-but-uncovered 6→0——因果链证实；但剩余 2 throw（ap_update_vhost_from_headers: 59 块中 26 块不可达、free-cond CBRANCH 在不可达块；新出现 ap_parse_vhost_addrs）+ timeout 4→7 漂移证明最小补丁不够，须整体移植 oracle：**修复方案=1) `calc_rpo`/`build_dom_tree` 移植 findSpanningTree rootlist 语义（block.cc:1009-1056：收集 sizeIn()==0 候选，>1 时交换使 list[0]/orighead 最后访问→RPO[0]=真入口；候选空时 rootlist=[list[0]] cc:1034-1036）+ calcForwardDominator 根=postorder.back()=list[0]（block.cc:1962-2028，含 root 带入边 createVirtualRoot 分支 cc:1988-1996）+ virtualroot excise（cc:2013-2024）；2) structure_reset 等价点接 heritage.force_restructure()（funcdata_block.cc:720）；3) 不可达块路径按 oracle blocks_unreachable 语义复核（Ghidra bblocks 流跟随构造通常无不可达块，Rugra lifter 产生孤儿/尾块是深层差异，A/B 后仍在）**。⑧回归门禁：httpd 6 throw→0、timeout 不增（基线 4：main/ap_fini_vhost_config/ap_strcasecmp_match/ap_make_dirstr_prefix）、19 decompiled 不降；curl E2E 持平（124 函数 75/0/1、defects=0、numbering=0）；cargo test --lib 基线；新增 oracle fixture（多孤儿块图：rootlist 交换/virtualroot/idom 断言，锁 block.cc:1028-1036+1962-2028）。calc_rpo 另有 build_dom_depth/其他调用方，改动须全调用面回归。⑨机制 C：block.rs/heritage 域改动落地需独立复核。**⑩第二 session 独立复核（2026-08-17，重启只读诊断 agent，干净 HEAD 197b34c worktree /tmp/rugra_baseline 独立插桩 12 轮：stage-path 跟踪+WATCH vn 全轨迹+HTRACE collect/renameVisit/DOM dump+受控 A/B）——①-⑤全链独立复现**：(a) 6 throw 同函数同形态（throw dump：vn=register:0x201:1(=513,ZF,flags=COVERDIRTY-only) 5/6 + unique:0x1188:1 1/6，旧读者 CBRANCH，stage=fullloop/mainloop/stackstall/simplifypool/cond_negate）；300 口径（900s 截断时已处理 263 函数）=30 throw 同族（ap_* 各 1 次，全 cond_negate）。(b) 决定性铁证（ap_matches_request_vhost）：pass0 collect(ZF range)=reads[30,43,54,78,94,121]+writes[23,33,47,71,87,111]（12 个独立 vn 对象，6 写 6 读），**rename 只 visit block#0**（RPO=[8,7,0,1,...6]，孤儿块 7/8 in0 排最前）→ CHK 以孤儿 8 为根 → 0 个 idom（HTRACE idom 全 None）→ build_adt domchild 全进 dead bucket → 只有块 0 内的 read30 被 SSA 化，43/78 存活到 pass2。(c) A/B（单行近似 oracle findSpanningTree block.cc:1036-1040 根交换，calc_rpo 内）：RPO=[0,1,...8]、renameVisit 2→67 块、httpd throw **6→5**（修复 ap_matches_request_vhost+ap_pregsub）、decompiled 19→**20**、timeout 4→4 无漂移；**curl 回归零变化**（124 函数 75/0/1、throw=0）。与 ⑦ 的 6→2 差异=我的近似更弱（只处理 in==0 根交换），方向一致，两者均证实因果。(d) 残差 4+1 形态证实：ap_strcasestr/ap_getword=不可达链/环（in≥1，如 strcasestr 块 7-10 成环 10→7）经 coverage 循环向量序追加→仍排入口前，swap 规则不覆盖；ap_update_vhost_from_headers/ap_getparents=RPO 已对（入口在前）但 **throwing CBRANCH 位于不可达块**（pass0 rename 只 visit 33/59 块，CBRANCH@0x2d9fe 在未访问块；Ghidra 经 removeUnreachableBlocks+ActionUnreachable 移除不可达块=oracle 无此状态）；新暴露 ap_parse_vhost_addrs（throw@mergerequired：free ram:0x2a980:8 读者 MULTIEQUAL——同"free-with-reader 存活"producer 家族、不同 consumer，须随主修一并归零或另立残差 TODO）。**⑪对 ④ 的现状澄清（2026-08-17 晚，本行 ④ 对 HEAD 4493e4a 仍成立——"forceRestructure 无调用点"在 HEAD 为真；但工作树已有并发执行者未提交 WIP）**：structure_reset（funcdata.rs:2206-2269，WIP 未提交态）正在引入 oracle port——structure_loops(rootlist)+calc_forward_dominator(:2227)+blocks_unreachable 旗标(:2231)+heritage.force_restructure(:2261)，block.rs WIP 同步移植 findSpanningTree（block.cc:1009-1136）；即本行 ⑦ 修复方案正在被执行中。执行者注意双实现并存风险：ActionHeritage（heritage.rs:3889 maxdepth==-1 路径）与 ActionBlockStructure（blockaction.rs:73）历史上调自制 build_dom_tree+calc_rpo（HEAD 行号 block.rs:2626/2833；calc_forward_dominator_impl=block.rs:2177/2191/2644），须确认全部收敛到 oracle 通道、无残留坏根序覆盖路径。**⑫遗留污染物**：funcdata.rs:2207-2215 残留 `TEMP DIAGNOSTIC (HTTPD-ADDDESCEND-THROW-0001) — REMOVE`（[DOMCTR]/RUGRA_DOM_CENSUS 块），执行者落地时必须删除。⑬归因澄清：当前工作树 ruleaction/arch INPUTREG WIP（spacebase 接线）与本家族无关（WIP 树与干净 HEAD 同 6 throw 同函数集）。执行者回归门禁沿用 ⑧，另加：httpd 残差 throw 逐个绑定本行或新 TODO、funcdata.rs TEMP DIAGNOSTIC 删除、双 dominator 实现收敛 | 2026-08-17 |
| `RULE-PROPAGATECOPY-DRIFT-0001` | P2 | DONE| rule_propcopy_wave | `RulePropagateCopy`（ruleaction.rs:178）与 ruleaction.cc:3926-3956 语义完全不同 | `src/ruleaction.rs`, `docs/api/ruleaction.md`, 差分窗口 | 完成（2026-08-17，WIP 未提交）。faithful 重写：读者视角单槽重定向（fd.op_set_input，funcdata_op.cc:104 四步语义）+ 全守卫链（isReturnCopy/isWritten/CPUI_COPY/isHeritageKnown/自引用 LowlevelError/marker 三子守卫）+ 全 opcode 注册（基类 getOpList，action.cc:706）；raw inref/descend 写与 stale descend 全部消除；新增 5 个回归测试（守卫各 1 + 单槽 + descend 簿记）。差分窗口（同树 A/B，基线已被并行 VARNODE-ADDDESCEND-THROW WIP 移至 56/20）：skeleton 427→424（−3）、defects 0/numbering 0 持平、Summary 56/20 与 panic 集合逐字节不变；唯一输出变化=progressbarinit@0x49a0 断裂 extraout 级联（`extraout_EAX;=(extraout_var;=curl_getenv)(…)`）收敛为直接调用，方向朝 Ghidra，commit 草稿（/tmp/rpc_commit_msg_draft.txt，机制 A 4/4 通过）内逐处归因。cargo test --lib 13 既有失败零新增（后出现的 varnode 域 1 失败为并行 WIP 中途态，与本改动无关）。残留：逐函数 oracle fixture 未产生（registry 禁改），机制 C 复核待 root 派 | 2026-08-17 |
| `ACTIONCSE-DEAD-0001` | P3 | READY | unassigned | `ActionCse`（coreaction.rs）手写 CSE 死代码处置 | `src/coreaction.rs`, `docs/api/coreaction.md` | Ghidra 12.0.4 的 ActionCse apply 体已注释删除；Rugra 手写 CSE（含同类 raw 重定向）未注册进管线=死代码。二选一：删除，或登记保留理由；触及主管线文件须差分门禁 | 2026-08-17 |
| `VARNODE-COPYSYMBOL-FIELDS-0001` | P3 | DONE| copysymbol_fields_wave | op_set_input 常量去重分支的 copySymbol 部分移植 | `src/funcdata.rs`, `docs/api/funcdata.md`, `tests/oracle/varnode_copy_symbol_1204.{cc,rs,metadata.json}`, `tools/run_varnode_copy_symbol_oracle.sh`, 本 TODO 行 | **已交付（WIP 未 commit，root 复核）**：funcdata.rs op_set_input 去重分支 cc:112 由内联 mapentry-only 拷贝改为完整 `Varnode::copy_symbol`（varnode.cc:493-505 字段腿 cc:496-499：Datatype 指针、mapentry 指针、typelock|namelock 清空重继承——mapped/insert 不拷贝）+ cc:500-504 high 簿记按 attach_symbol_to_vn 房式接调用侧（copy_symbol 的 &mut self 拿不到 set_symbol 所需 Arc-to-self；当前不可达，见残差 R1）。oracle fixture（pinned base dce02f7 + src/funcdata.rs 单文件 overlay，避开并发 WIP）authoritative runner exit0：locks_type/no_locks/mapentry_symbol（含 mapentry 指针拷贝+mapped 位不拷贝判别）/mapentry_copy/identity_return(cc:107)/spacebase_exempt(cc:110) 6 行双侧逐字节 MATCH，stdout sha=9d856026…；A/B 旧实现即 MISMATCH（cvn_tl=0/type_meta=unknown/type_same=0）。E2E curl 同树 A/B 输出**逐字节不变**：Matched 123/defects=0/Summary 124/75/1 timeout 持平；numbering=16 为并发 printc/heritage WIP 增量（my_get_token/glob_word/next_url/match_url，与 UNKNOWN-PROTOMODEL-WARN-EMIT-0001 行归因一致，本改动零 stdout 字节）。cargo test --lib 1391/5 失败集与 HEAD 基线逐名一致（comment/dynamic/funcdata×2/ruleaction 预存）。annotations/refs 门禁过。**残差 R1**：`Funcdata::new_constant` 未接 assignHigh（funcdata_varnode.cc:72；现存 assign_high 也不接线 vn.high/instances）→ cvn.high 恒 None，cc:500-504 双侧不可达（fixture 不开 highlevel_on，NO_ORACLE 登记）；修复路径=补全 assignHigh 接线并从 new_constant/newVarnode 族调用（cc:72,89,110,135,157,182,196,212,231,604），影响面大（merge/cover 全链），建议独立任务。fixture_registry.json 登记=root 串行域（未动）。机制 C 复核待 root 派（funcdata 核心域） | 2026-08-17 |
| `FUNCDATA-LANEDLEG-WIRING-0001` | P3 | READY | unassigned | new_* 家族 `checkForLanedRegister` 腿未接线 | `src/funcdata.rs`, `docs/api/funcdata.md` | ASSIGNHIGH 复核 advisory：本体 check_for_laned_register 已在，new_* 十处的 `if (s>=minLanedSize)` 调用腿缺失（先前已存在缺口）；接线 + fixture 观察点 | 2026-08-17 |
| `PRINTC-BINARY-RPN-0001` | P2 | DONE| printc_binary_rpn_wave | 二元算术 tag_op 直发不经 RPN token | `src/printc.rs`, `docs/api/printc.md`, 本 TODO 行 | **已交付（WIP 未 commit，root 复核；printc 双 lease 串行——WARN-EMIT ②③ printc.rs 残差待接）**：①`optoken` 模块重写为逐字段 OpToken 注册表 `BINARY_TOKENS`（printc.cc:36-55 全 20 token：prec/assoc/spacing=1/negate=printc.cc:129-134 翻转对；opcode→token 按 printc.hh:283-318 虚拟 dispatch，INT_LESS/SLESS/FLOAT_LESS 同 less_than 实例等，以 token id 表达 OpToken 指针同一性；修正 BOOL_XOR `^`@30→`^^`@20 失配，printc.cc:54）；②`build_rpn_token_table` 追加 20 token（索引 9..=28）+`rpn_tok_binary`（printlanguage.cc:539-545 negatetoken 前奏）；③`dispatch_op_rpn` 二元臂操作符改 `rpn_push_op` token 流（emitOp spacing=1 文本与旧直发逐字节一致），操作数保持 make_atom 叶子（**刻意不用** pushVn/nodepend：隐式内联是 PRINT-RPN-0001 未完成域，队列化实测 numbering 0→16 已回退）；INT_ADD 字段短路走 pointer_member token（opPtrsub printc.cc:476-484 形态）；④`child_needs_parens` 重写为 printlanguage.cc:277-286 逐行移植（等优先级非 assoc 双侧括号=`(a - b) - c` 保守风；同 token+assoc 才免=`a + (b + c)`；推翻旧"左操作数永免括号"教科书倒置启发式）；⑤`emit_inline_expr`/`op_binary` 操作符文本改注册表生成；`emit_condition` Case-1/Case-2 操作数经 `push_input_parenthesized`（3 处残差实际路径=emit_structured_condition→capture_block_condition→emit_condition，[DBG2] 实证）+ BOOL_AND/OR 两侧 `condition_side_needs_parens`；删死代码 `c_binary_op_str`。**验收**：`+ 0 -` 裸形态 3→0、`(==|!=)…(<…)` 无括号邻接 0；3 站点现形 `(bool)uVar_20b == (bool)uVar_10 + (0 - …) < 0`（内层 `0-X` 已括号=286 决策；外层 EQUAL(38)⊃SLESS(42) 免括号=278 `38<42→false` 正确，IR 树 [DBG3]/[DBG4] def 链实证 EQUAL 在外层，非旧树 SLESS(EQUAL) 形——heritage dce02f7 后 IR 已变）；Matched 123 不降/defects 0/numbering 0/skeleton 3166（=并发 WARN-EMIT 树同值基线，其 clean-base 无其文件=3156）；gcc 106 OK/17 FAIL（=门禁 ≤17）；cargo test --lib 1412/5（失败集=comment/dynamic/funcdata×2/ruleaction 预存）；printc 9/9。**残差**：RPN 路径隐式操作数仍叶子化（PRINT-RPN-0001 域）；unary 前缀臂仍 tag_op 直发（一元非本任务范围；missing-in0 裸前缀缺陷=nodepend 实测时其 guard 引入文本漂移已回退，待 unary token 化任务）；fixture 未建（可选步骤；机制 B2 状态 NO_ORACLE——oracle 差分以 E2E 差分门禁+20 断言单测代偿，未升 MATCH） | 2026-08-17 |
| `UNKNOWN-PROTOMODEL-WARN-EMIT-0001` | P1 | REVIEW | warning_channel_wave | `ActionPrototypeWarnings` 警告的 C 输出发射（UNKNOWN-PROTOMODEL-0001 warning 通道子项；golden 51 vs Rugra 24） | coreaction 侧已完成（见下 evidence）；残差 write-set=`src/arch.rs`+`src/printc.rs`+`examples/curl_decompile.rs`+`src/action.rs`+`src/debugproto.rs` | **①④⑤ 已交付（WIP 未 commit，root 复核后入库；本轮 WARN-EMIT2 agent，域=arch/action/debugproto/examples/curl_decompile）**：①`examples/curl_decompile.rs::worker_architecture` 在 `Architecture::new()` 后、cspec 解析（restoreFromSpec 等价步）前 `set_commentdb(Arc<RwLock<CommentDatabaseInternal::new>>())`——oracle `Architecture::init` :1400 调 `buildCommentDB`（sleigh_arch.cc:244 分配，architecture.cc:166 构造器 null=arch.rs:523 `commentdb: None` 已对齐保持）；E2E stderr unknown-convention 48→0，警告以 Comment::warningheader 按函数地址入库（debugproto.rs 测试 `unknown_model_warning_stores_in_commentdb` 证明 0x4970 入库文本/type/addr 三元组）。④action.rs `build_default_pipeline`：base 组重排为 coreaction.cc:5477-5485 逐字序（start/constbase/defaultparams/extrapopsetup/prototypetypes/funclink）；`BUILDER_OWNED_ACTION_NAMES`(15) 过滤 build_full_pipeline_actions 消费——prototypewarnings 顶层恰 1 份（:5737），其余 14 个双注册（defaultparams/prototypetypes/varnodeprops/paramdouble/directwrite/activeparam/returnrecovery/nonzeromask/infertypes/unjustifiedparams/starttypes/activereturn/switchnorm/hideshadow）同除；测试 test_prototype_warnings_registered_once + test_base_group_order_matches_ghidra_5477_5485 锁定。**残差 R2 已收口（2026-08-17，R2-closeout agent，WIP 未 commit，root 复核）**：outputprototype/inputprototype 加入 `BUILDER_OWNED_ACTION_NAMES`（15→17，action.rs 跳过集；setcasts 已先由 HERITAGE-FLAGFREE-SSA-0001 移出）——两 Action 全树恰一份，唯一注册 coreaction.cc:5730/5731（universalAction :5462-5739 全函数体已核实无第二处），残余双注册清单**清空**；解除条件已由 533412a（printc 命名门控，PRINTC-WARN-EMIT-0001 R2）满足。历史 A/B 记录保留：全量去重曾暴露 printc 局部名碰撞（仅 glob_url，numbering 0→3），根因（hex 注解 ` */` 误判签名形状）已修。验证（pinned-base 004816a worktree + src/action.rs+docs/api/action.md 双文件 overlay，隔离并发 coreaction WIP 编译断裂）：E2E 新鲜捕获 stdout 与收口前**逐字节相同**（cmp 通过）——Matched 123/defects 0/numbering 0/skeleton 3092（1204 口径）/gcc 107 OK 16 FAIL 持平/multiple descendants 0/警告 51/glob_url 单声明块（per-func diff numbering=0）；11.3.2 golden 回归信号 defects 0/numbering 0。cargo test --lib（worktree）：1413 passed/5 failed=预存集逐名一致（comment/dynamic/funcdata×2/ruleaction），action::tests 192 全绿（含改写后的 test_prototype_warnings_registered_once 单注册断言与 test_base_group_order 前缀去双）；test_base_group_order 前缀中两名系 residual early double 临时项（读测试确认注释语义），随收口删除，唯一位置由 :5730/:5731 尾段测试锁定。⑤debugproto.rs `apply`：空参 DWARF 签名（main_init 0x4960/main_free 0x4970/hugehelp 0x4a00——golden 51 处中仅有的 3 个真实函数告警，恰=21 个 applied 签名中全部 3 个空参签名）钉 `set_model_name("unknown")`（fspec.cc:4690-4698 ATTRIB_MODEL 未识别名→createUnknownModel→UnknownProtoModel(placeholder=defaultfp,isUnknown,printInDecl=false)；空参 voidlock→modellock fspec.cc:4776=set_input_lock 空参已置），克隆模型 Arc 保留为 placeholder；带参签名保持 resolved（18 个带参 DWARF 函数 golden 全无警告）。**R3 更正（复核方拦下）**：锁定 12.0.4 golden 的 main_init(:2128)/main_free(:2139)/hugehelp(:2191) **全部带完整 ' -- yet parameter storage is locked' 后缀**——"裸 main_init"系旧 11.3.2 golden 误引，R3 残差在锁定 oracle 中不存在；Rugra 统一双锁+后缀行为已与 12.0.4 逐字一致。**禁止**在 DWARF apply 侧区分锁属性（那会反向引入与 12.0.4 的分歧，该提案已否决）。**度量（锁定 curl E2E 新鲜捕获，自测基线=动手前实测）**：Matched 123 不降/defects 0/numbering 16→**0**/skeleton 3152→3166（±14 在已远偏离 golden 的 18 个函数噪声带内，per-function diff 表见交接）；stdout `/* WARNING: Unknown calling convention` 24（②③ 未接线，C 输出发射链断点在 printc 租约域——②doc_function 补 setup_function_list(printc.cc:2650)+emit_comment_func_header(:2652)（printc.rs:8450 已移植零调用），③printc.rs:570-573 head/instr mask 对调（printlanguage.cc:579/582）；接线后预期 24+24+3=51=golden 逐位置）。cargo test --lib（最终，live tree 102c476+并发 WIP 编译恢复后）：**1412 passed/5 failed=预存集逐名一致**（comment/dynamic/funcdata×2/ruleaction），本轮新增 5 测试全绿（2×action+3×debugproto）；期间曾以 pinned-base 102c476 worktree+3 文件 overlay 隔离验证同结果（并发 op.rs/space.rs WIP 短暂破坏编译时）。E2E 终值（live tree 新鲜捕获）：stdout 24/stderr 0/Matched 123/defects 0/numbering 0/skeleton 3166；clean-base A/B：无我文件=stderr 48/3156，有我文件=stderr 0/3166，改动节=7 个 DWARF 带参函数（±180 行，皆远偏离 golden 的噪声带）。交接文档：docs/api/{action,debugproto,arch}.md 2026-08-17 节 | 2026-08-17 | 复核观察项登记（2026-08-17）：O2 warning_header 缺 jumptable 前缀分支（funcdata.cc:139-141，warning 有而 header 无的不对称，当前 set_jumptable_recovery 无生产调用不可达）；O3 `<indirect>` 回退用 prototype.name.is_empty() 而非 oracle fd!=nullptr（结构性死分支，谓词接线时复核） | 2026-08-17 |
| `PRINTC-WARN-EMIT-0001` | P1 | DONE| printc_warning_emit_wave | UNKNOWN-PROTOMODEL-WARN-EMIT-0001 ②③ 接线（docFunction 注释链 + mask 对调）与 WARN-EMIT2 R2 printc 侧命名碰撞修复 | `src/printc.rs`、`src/printlanguage.rs`、`src/prettyprint.rs`、`docs/api/{printc,printlanguage,prettyprint}.md`、本行 | **2026-08-17 交付（WIP 未 commit，root 复核后入库）**：③printc.rs 构造器 head/instr 掩码对调（oracle printlanguage.cc:579 head=header|warningheader、cc:582 instr=user2|warning；原两值互换致 emitCommentFuncHeader 把 warningheader 全滤掉）。②doc_function 在 emit_function_declaration 前按 printc.cc:2650-2653 逐字序补 setup_function_list(instr|head, fd, commentdb, option_unplaced)+begin_function+emit_comment_func_header+tagLine（emit_comment_func_header 原零调用方获得调用方）；配套实装 PrintLanguage::emitLineComment（printlanguage.cc:589-648 全量，PrintC trait 覆盖，trait 默认体原为 no-op）+ 新字段 line_commentindent=20（cc:580）。R2 修复：prettyprint.rs backfill_missing_locals 的 sig_shape `contains(" *")` 臂把 `if ((bool)(piVar1 <= 0x1000 /* 4096 */)) {` 误判为函数签名（十六进制注解 ` */` 含 " *"）→ if 体当嵌套函数整块重注入声明 → glob_url 双声明块（numbering 3 根因；flagfree_ssa_wave 121 行 ⑩(d) 已独立复现并确认本修复落盘后 numbering=0）；control_flow_opener 门控排除 if/while/for/switch/do/else/case/default 开行。验收（锁定 curl E2E 新鲜 stdout）：`/* WARNING: Unknown calling convention` 24→**51**，与锁定 12.0.4 golden（tests/golden/ghidra_curl_1204.c）**51/51 逐位置零失配**（24 PLT+24 EXTERNAL+3 真实函数，(地址,函数名,字节数,文本) 四元组对照）；双 golden 差分 defects=0/numbering=0（1204 口径 3→0）/Matched 123 不降；printc 9/9、printlanguage 30/30；7 个 lib 失败=跨域预存（comment/dynamic/funcdata×2/ruleaction 在 clean HEAD 亦败 + 2 个 action.rs 过时断言编码待删的双注册，worktree 归属复核见交接）。R3 复核结论：12.0.4 golden main_init/main_free/hugehelp 三处全带完整 locked 后缀（旧 11.3.2 golden 裸 main_init 为过版误引），Rugra 现行为逐字一致，无需动 DWARF apply。**action.rs 可删三名判据（root 执行，非本租约）**：outputprototype/inputprototype/setcasts 加入 BUILDER_OWNED_ACTION_NAMES 过滤后重跑 curl E2E——判据=defects=0/numbering=0/Matched 123/gcc 审计不降，且 glob_url 无双声明块（if 体内整组重注入已由 R2 修复消除；flagfree_ssa_wave A2 实验（setcasts 单注册+本修复）已实测 numbering=0）；同步改写 action.rs 两个过时断言测试（test_prototype_warnings_registered_once / test_base_group_order_matches_ghidra_5477_5485，现编码双注册形态）| 2026-08-17 |。复核观察 4 项登记：①emit_line_comment \n 分支应为注释起始列+commentfill（多行注释接入后成真差异）；②setEmitted 未落地防重门 inert（body 注释接线前必须补）；③R2 残余洞：续条件行/同行标签前缀（平坦发射器不可达）；④comment.rs 收集期 tp 预过滤 vs oracle 仅 tp==0 早退（当前不可观察） | 2026-08-17 |
| `TYPEUNION-CACHE-READSIDE-0001` | P2 | DONE| union_cache_readside_wave | `TypeUnion::findTruncation`（type.cc:2185-2199）(op,slot) 解析缓存读侧接线：Rugra `find_truncation` 原不带 op/slot（union 臂恒 None），printc `rpn_push_partial_symbol` 的 UNION 臂缺失 printc.cc:2003 的 findTruncation 调用（把"无缓存 miss"当成唯一行为） | `src/type_system/datatype.rs`、`src/printc.rs`、`docs/api/{printc,type_system/datatype}.md`、`tests/oracle/printc_subpiece_fieldextract_1204.{cc,rs,metadata.json}`、本 TODO 行 | **已交付（WIP 未 commit，root 复核；机制 C：printc 白名单域，待独立复核 APPROVE）**：①`Datatype::find_truncation` 签名扩展 `(off, sz, op: Option<&PcodeOp>, slot: i32, resolutions: Option<&UnionResolveMap>)`——Union 臂落地 type.cc:2185-2199 只读查询（`ResolveEdge::new(self, op, slot)` 键控快照；miss/`field_num<0`→None 不写缓存；命中 `newoff=off-field.offset`、`newoff+sz>field.size` 严格 `>` 跨字段拒绝）；PartialUnion 臂同 (op,slot) 透传给容器（容器=缓存键 parent，同 Ghidra type.cc:2440-2444 委托 + unionresolve.cc:74-75 PARTIALUNION 键臂语义）；Struct 臂不变（Ghidra override 忽略 op/slot）；新公开别名 `UnionResolveMap`。②printc 三消费点接线：RPN opSubpiece findTruncation 字段 atom 臂（printc.cc:862，slot=1 人工槽）、`rpn_push_partial_symbol` STRUCT 臂传参 + **UNION 臂补上 printc.cc:2003-2016 完整序列**（先 findTruncation 下降→miss 且 size==sz 才 break→否则合成条目）、legacy `op_subpiece` 同步。③新公开 `PrintC::snapshot_union_resolutions(&mut self, fd)`（RUGRA-GLUE，从 doc_function 提取；主管线安装点不变，op 级 fixture 复用同一通道）。**验收**：fixture 26→**31 records MATCH**（官方 runner，锁定 12.0.4 oracle 逐字节）——新增 5 条 union 读侧记录 `armB.unionhit=U.b`/`armA.unionhit=V.b`/`armA.unionmiss=W`/`armA.unionspan=X`/`armA.unionsynth=Z._0_2_`，C++ 侧经真 `Funcdata::setUnionField`（funcdata.cc:937）+`ResolvedUnion(parent,1,typegrp)`（unionresolve.cc:40）注入 artificial slot 1，Rust 侧镜像经 `Funcdata::set_union_field` 写端口+`snapshot_union_resolutions`；metadata `union_resolution_cache_hit_break` UNTESTED→MATCH（struct findResolve 自 break 拆出为独立 `struct_findresolve_self_break` 仍 UNTESTED）。**E2E 隔离归因零影响**：同工作树仅回退本改动两文件（/tmp 副本，不触碰共享树）重跑 curl，输出 diff 0 行——主管线 `fd.union_map` 无 SUBPIECE (op,slot=1) 边可命中条目（unionresolve 生产方未接主管线），miss 路径行为=改前；门禁 defects=0/numbering=0 维持。cargo test --lib 1427P/5F（基线 1426P/5F，+1=新测试 `test_find_truncation_union_cache`：miss/负 fieldNum/命中/异 slot/跨字段/PartialUnion 委托含非零偏移；失败集 comment/dynamic/funcdata×2/ruleaction 预存逐名一致）。**观察项（移交）**：(a) Rust `ResolveEdge::new`（unionresolve.rs:140）缺 Ghidra TYPE_PARTIALUNION 键臂（unionresolve.cc:73-74 按容器 id 键控）——findTruncation 委托路径不受影响（容器自身成为 self），但直接以 PartialUnion 为 parent 的其他 get/set 调用点键控偏差，属 unionresolve.rs 域未动；(b) Rust `get_high_type_read_facing`（varnode.rs:1003）未做 findResolve 查询（Ghidra varnode.cc:665-671）——slot=0 miss 场景行为等价（Ghidra miss 亦返回 this），slot=0 有缓存条目时偏差，varnode.rs 域未动；(c) 主管线 unionresolve 写侧生产方（ScoreUnionFields→set_union_field）仍未接入，union 读侧在主管线暂无可命中条目（与 STRUCT findResolve 现状同） | 2026-08-18 || `ACTION-LANEDIVIDE-RESIDUAL-0001` | P2 | mode1 SUBPIECE downcast 终结符、多 varnode bounds 重走、多 storage map 序×mode、pointer-size-4 归一化（x86-64 oracle 恒 8，NO_ORACLE） | `src/coreaction.rs`、fixture 扩展 | 排队 |
| `BUILDCOPY-SNAPSHOT-DEADOP-0001` | P2 | MERGED / IMPLEMENTED（证据并入 `BLOCK-BUILDCOPY-MIRROR-0001`） | root | 无独立 write-set | 2026-08-28 `build_copy` 已改为持有 live source alias 的真实 `BlockCopy`，不再克隆 op 快照；live-delegate 双侧 case 观察 build 后 source op 列表变化并逐字节 MATCH。该投影不单独证明完整函数 MATCH；opDestroy/PrintC 全闭包仍由 canonical overall MISMATCH 覆盖。 |
| `DB-JOINPIECE-FOLD-0001` | P2 | dbmap 复核观察：decode 的 join piece 展开（database.cc:1156-1177）缺失，XML 带 join 地址时不展开 piece | `src/database.rs` | 排队 |
| `MARSHAL-SPACE-ATTR-0001` | P3 | dbmap 复核观察：encode changepoint 缺 space 属性（跨实现 XML 互操作失败） | `src/database.rs`、`src/marshal.rs` | 排队 |
| `FLOW-FIXTURE-API-ADAPT-0001` | P2 | injectwire 集成后 flow_containedcall_1204.rs 需适配 inject 新 API（6 编译错：InjectPayload::inject 接收者/PcodeOpRaw 切片/类型）；spec pin 已修 34a3febf→87aaef2 | `tests/oracle/flow_containedcall_1204.rs`、runner | 排队 |
| `VARNODE-UPDATETYPE-HIGHDIRTY-0001` | P2 | lazytype 发现：Varnode::update_type/lock 未移植 high->typeDirty() 腿（cc:462/487，注释过时） | `src/varnode.rs` | 排队 |
| `VARNODE-SETSYMBOLENTRY-HIGHLEG-0001` | P2 | lazytype 发现：set_symbol_entry 未移植 cc:437-438 setSymbol 腿 | `src/varnode.rs` | 排队 |
| `SUBFLOW-SPLITFLOW-SIGABRT-0001` | P0 | rulebehav 返工发现：test_split_flow_full_transform_through_indirect 在 pristine master 即 SIGABRT（NonNull::new_unchecked UB），被 database.rs 编译错误掩盖至今 | `src/subflow.rs`、`src/transform.rs`、`docs/api/{subflow,transform}.md`、`tests/oracle/{lanedivide_infra,transform_multiequal_insert}_1204.metadata.json`、本行 | **已交付（wt/sigabrt wave，sigabrt agent，2026-08-23）**：双层根因——①UB 崩溃点 transform.rs `get_op_from_const_offset`（原:470-482）：占位符 `val==0` 时 `Arc::from_raw(null)` 触发 nightly `NonNull::new_unchecked` 前置检查 SIGABRT；Ghidra 同点位 op.hh:249 `(PcodeOp*)(uintp)addr.getOffset()` 解码为可空 NULL 指针、newVarnodeIop(funcdata_varnode.cc:176-184) 原样重编码 offset 0 从不解引用。修复=函数改返回 `Option<PcodeOpRef>`（offset 0 → None = Ghidra NULL 语义），`create_replacement` constant_iop 臂 None 时按 newVarnodeIop(NULL) 语义直接物化 Iop 空间 offset 0 注记 varnode（annotation 标志由 Varnode 构造器 varnode.cc:599-601 自动置位）；非零 offset 解码路径逐字节不变。②被①掩盖的第二缺陷 subflow.rs `SplitFlow::add_op`（原:3229-3237）INDIRECT 臂 lo/hi 共享单个 new_iop 占位符 → createReplacement 只物化一个 iop varnode 被 place_inputs 接入两个 op → "Free varnode has multiple descendants" panic（varnode.cc:333-336，Ghidra per-call emplace_back 不可达状态）；修复=对齐 subflow.cc:1806-1807 两次独立 `newIop(op->getIn(1))` 调用（LaneDivide::build_indirect subflow.rs:3819 本就正确）。**验收**：目标测试 PASS；cargo test --lib 1554P/2F（失败集=funcdata::tests::{test_infer_params_and_return_type,test_type_propagation} 预存已知，与本改动无关）；两 fixture 以新 overlay 活体重跑锁定 oracle（e40ed130）全绿——lanedivide_infra_1204 covered_projection=11/12 MATCH（residual=预登记 LANEDIVIDE-INFRA-RESIDUAL-0001 oracle_ub NO_ORACLE），transform_multiequal_insert_1204 covered_projection=11/11 MATCH residual=none；两 fixture 的 subflow/transform/funcdata overlay sha 已按新生效文件刷新（funcdata 漂移系 GETSTR-ZERODIFF-C/E d590a5b 遗留，本轮活体复验无回归并一并刷新入账）。同 commit 更新 docs/api/{subflow,transform}.md | 2026-08-23 |
| `PIPE-CLEANUP-COUNT-ZERO-0001` | P2 | emptyelse 发现：DoNothing/RedundBranch/DeterminedBranch 的 count 恒 0（oracle cc:3483/3524/3545 递增驱动 fullloop 重入）；曾因测试期望绕过 | `src/coreaction.rs` | 排队 |
| `FLOAT-OPTRUNC-OOB-0001` | P3 | floatfmt 发现：op_trunc 越界语义（float.cc:631-640 (intb)val + calc_mask(sizeout)）Rust 饱和 cast 分歧且缺 mask | `src/float_emulate.rs` | 排队 |
| `FUNCDATA-CALCNZM-0002` | P2 | op.rs PcodeOp::get_nz_mask_local divergent 且零调用方；完整 switch 暂在 funcdata.rs，合并回 PcodeOp 需 op.rs 租约 | `src/op.rs`、`src/funcdata.rs` | 排队 |
| `RULEACTION-ADDUNSIGNED-COPYSYMBOL-HIGH-0001` | P2 | highbranch 发现：RuleAddUnsigned（ruleaction.rs:10588）仍调字段半 copy_symbol 无 high 簿记；一行适配为 copy_symbol_arc | `src/ruleaction.rs` | rulebehav 租约释放 |
| `VARIABLE-GETTYPE-LAZY-UPDATETYPE-0001` | P2 | HighVariable::get_type（variable.rs:1401）缺 variable.hh:174 惰性 updateType（&self 签名无法变异） | `src/variable.rs` | 认领中 |
| `DATABASE-EQUATE-XMLCONTENT-0001` | P2 | equatereg 发现：真实 XML 的 <value> 是元素文本（ATTRIB_CONTENT），Decoder trait 不暴露；修法=marshal.rs 加 content 访问器 | `src/marshal.rs`、`src/database.rs` | 排队 |
| `DATABASE-EQUATE-VARMAP-BRIDGE-0001` | P2 | varmap→database 桥（funcdata_varnode.cc:1301 腿） | `src/{varmap,funcdata}.rs` | funcdata 租约 |
| `FUNCDATA-REMOVE-UNREACHABLE-0001` | P1 | containedcall 交付登记：possible_unreachable 消费端 removeUnreachableBlocks 本体的对齐深度（接线已存在） | `src/funcdata.rs`、fixture | funcdata 租约 |
| `FUNCDATA-NEWVARNODEOUT-SPACE-0001` | P2 | pullback 复核证实：Funcdata::new_varnode_out（funcdata.rs:1913）硬编码 AddressSpace::Register，无法表达 unique 空间输出；正式 API 应带 space 参数（split Address 模型族） | `src/funcdata.rs` | calcnzm 租约释放 |
| `BLOCK-FLIPPATH-BIT-0001` | P2 | PARTIAL_MATCH（selected structured-negate projection MATCH） | root | `src/block.rs`, `docs/api/block.md`, `tests/oracle/block_structured_negate_1204.*`, runner | 常量为 oracle `f_flip_path=0x10000`；13-case fixture 已覆盖 base/List/Condition selected FLIP_PATH、双半边与 double-negate round trip。BlockBasic/Copy 全分支、其他 flip consumers、错误与完整 structurer/PrintC 闭包仍 MISMATCH/UNTESTED，不升完整函数 MATCH；last_updated=2026-08-28。 |
| `TRANSFORM-CONSTANT-IOP-SPACE-0001` | P1 | laneresid 发现：transform.rs:445-450 constant_iop 物化 const-space，oracle（transform.cc:211-215）经 getOpFromConst 走 newVarnodeIop（iop 空间）；地基已备（funcdata.rs:3303/3325），~3 行路由；修完 lanedivide indirect 升 MATCH | `src/transform.rs` | 即时可认领 |
| `MERGE-MIXEDHIGH-0001` | P1 | varmapdyn 实证定位：merge_op 强并 explicit high 成 mixed high（Ghidra inflateTest 对已并 high 判 explicit，此态不可达）；分歧=check_implied_cover 静态近似 vs coreaction.cc:3378-3411 实 cover 求交；修复后 45 行注入组经已证明的 linkSymbol 链恢复命名 | `src/merge.rs`、`src/coreaction.rs` | merge/coreaction 租约 |
| `PIPE-MAINLOOP-REPEATAPPLY-0001` | P2 | poollrules 复核附带：mainloop 未启 RULE_REPEATAPPLY，后期 trivial op 不再简化（当前语料零可观测） | `src/action.rs` | 排队 |
| `PIPE-RULE-RAW-NAMES-0001` | P3 | ~154 规则 raw 诊断名仅归一比较，全量 sweep 待做 | `src/ruleaction.rs` | 排队 |
| `PIPE-POOL-RULE-BEHAVIOR-0001` | P3 | 池内规则 applyOp/getOpList 行为未观察（fixture 只投影注册序） | `src/ruleaction.rs` | 排队 |
| `PIPE-CPU-EXTRA-POOL-RULES-0001` | P3 | extra_pool_rules（architecture.cc:730 decodeDynamicRule）通道未观察 | `src/arch.rs` | 排队 |
| `FLOAT-FMT-STRUCT-0001` | P3 | getHostFloat/getEncoding 结构级非 1:1（host f64 位转换 vs oracle createFloat/extractExpSig 位阶梯+顶对齐 helper；值等价已由 float fixture 覆盖） | `src/float_emulate.rs` | 排队 |
| `FUNCPROTO-DECODE-0001` | P1 | codeflags 交付建议：live <prototype> 解码被 FuncProto::decode（fspec.cc:4675）门控，fspec.rs 侧未移植 | `src/fspec.rs`、fixture | fspec 租约 |
| `VARNODE-COPYSYMBOL-HIGHBRANCH-0001` | P2 | copySymbol 的 high!=0 分支（typeDirty+setSymbol）Rust 缺失；修复需拓宽 self 签名为 &Arc 并连同 op.rs 租 | `src/{varnode,op}.rs` | 排队 |
| `DATABASE-EQUATE-VALUE-REGISTRY-0001` | P2 | database.rs Scope::add_equate_symbol 未调 register_value（Symbol 无原生 equate 载荷），主管线 equate 接不通；EquateSymbol::new 未设 category=equate（database.hh:306） | `src/database.rs` | 排队 |
| `FSPEC-PHASE0-RESIDUAL-0001` | P1 | ①trial 绝对 slot 基数（Ghidra 1 基 cc:4062 vs Rust 0 基，修复需同步 funcdata/coreaction 消费方）②无空间查询的理论假命中③稳定排序 vs introsort 等值序④justified_contain_range 端序（审计漏报） | `src/{fspec,funcdata,coreaction}.rs` | 分域 |
| `CONDEXE-CONSTBASE-FLIP-0001` | P2 | trueout 跨域发现：coreaction.rs:8409 隐含布尔常量 flip_edge=true 时按 flip 换目标块，oracle 固定 out[0] 得 (flip?1:0)（coreaction.cc:4539-4540），块选择相反 | `src/coreaction.rs` | actionlane 租约释放 |
| `CONDEXE-TRUEOUT-RESIDUAL-0001` | P1 | execute/doReplacement/removeFromFlowSplit（依赖 CFG-0001）、heritage 门控（0004）、pullback（0005）、错误通道（0006）、RuleOrPredicate 端到端（0007） | `src/condexe.rs` 等 | 分项 |

> **SWITCH-BRIDGE-DUP-0001 修复(2026-09-22,Lane BX `wt/sb-bridge`,owner=fixer)**:
> 根因定位(发射层,backtrace+树 dump 实证):终树仅一个 Switch(gp 拓扑
> `InfLoop body → If → condition List 末位=Switch`,dispatcher 在 new_block_switch 清
> f_switch_out 后被 cat/proper_if 卷入条件位)。`emit_structured_if` 按 oracle
> emitBlockIf(printc.cc:2894-2913)对条件双访问:NO_BRANCH 一访=完整 switch(文本
> switch#1);ONLY_BRANCH 二访应只打分支表达式,但 `emit_structured_switch` 无视
> 修饰符整只重放→48 空 case 桥+孤立 `if`+`} {` 孤儿块(~121 行非法 C)。
> 修复:printc.rs `emit_structured_switch` 入口 ONLY_BRANCH 早退,走 opBranchind
> 表达式通道(printc.cc:582-591,头文本逐字节一致);头表达式解析提取为
> `emit_switch_head_expr`(共享渲染)。**验证**:gp 730→645 行、case 96→48、
> 桥+孤儿块消除;curl 全量(本仓基线 3751)→ **3653/defects=0/numbering=0**
> (−98);gp --func 978→**880/0/0**;printc 单测 12/12;gcc 审计 81/26 前后持平
> (gp 均不在 FAIL 列)。**httpd 门禁 SKIP**:master HEAD 起
> examples/httpd_decompile.rs 在 dff36e78 提交中截断(`worker_memory_image_bytes`
> 缺闭括号,rustc unclosed delimiter@71)——既有损坏,该文件属 BP/constbase lane
> write-set,本 lane 不碰;**新登记 `HTTPD-EXAMPLE-SYNTAX-0001`(P0,BP lane)**。
> **残留(拓扑域)**:条件槽 `if switch(iVar31) {`(1 行非法 C)、双 default-check
> (If body 侧)、case 0x4e goto 改道——待 guard-folding/BO lane 消条件位 If。
> **SWITCH-CASE-TAIL-0001 已修(2026-09-22,Lane CB `wt/sb-casetail`,owner=fixer)**:
> 根因=**文本后处理层**(非发射/非结构——case 树 dump 证 Copy@42f0 在场、emit trace
> 证 token 全入 Oppen 队列与 lowlevel 输出):post_process_output_legacy 第十遍把折行后
> 独占一行的 `goto …;` 当无条件终结符启动死区,删掉其后同缩进活语句直到 case 标签;
> 单行 `if (…) goto …;`(0x41)行首是 if 不命中,故仅两个复合长条件 case 受害。
> 修复=P10 goto 启动子加折行 if 臂判别(前一非空行以 `)`/`else` 结尾→条件臂不启动)。
> 验收:curl 全文恰 +7 行且全在 gp(0x23 恢复 3 行、0x35 恢复 4 行,含 break);
> 差分 curl 3661/0/0(base 3654,+7=恢复行本体,golden 侧枚举名形态属已登记 varmap 族)、
> httpd 2459/0/0 与基线恒等、gcc 81/26 与 7/22 不变、逐函数抽查恒等。
> 详见 docs/api/prettyprint.md 2026-09-22 追加节;证据=本 lane commit。
> (If body 侧)、case 0x4e goto 改道——待 guard-folding/BO lane 消条件位 If;
> SWITCH-CASE-TAIL-0001(0x23/0x35)未动,证据见 GP978_TRIAGE.md §3(a)。

> **OPPOOL28-EQUAL2ZERO-LADDER-0001 修复(2026-09-22,Lane CD `wt/sb-oppool28`,owner=fixer)**:
> Phase 2 新首分歧 ordinal 28 `universal:fullloop:mainloop:stackstall:oppool1`
> result/count 863(oracle) vs 826(rugra),差 37 = oppool1 首次 perform 内规则应用数
> (pool `count += res` 累计,action.cc:849)。drill 窗口分解(863/826 精确对齐):
> earlyremoval −22、equal2zero −6、lessequal −3、propagatecopy −3、subextcomm −2、
> lessnotequal −1。三处根因(均已修):
> ① `RuleEqual2Zero::applyOp` MULT 分支 else-if 阶梯倒置(ruleaction.cc:5884-5893):
>   旧 Rust 在 vn written 但非 INT_MULT 时提前 bail,永不检查 vn2;补 isHeritageKnown
>   双守卫(cc:5900-5901)与 copySymbolIfValid(cc:5880)。修复即回收 31/37。
> ② `Funcdata::cseElimination`(funcdata_op.cc:1356-1398)幸存者选择:旧 Rust 无视
>   parent 恒取 SeqNum order 小者;移植 findCommonBlock 支配树分支+公共块新 op 路径
>   (cc:1372-1386);cseEliminateList 补 isHeritaged 守卫(cc:1436-1437)。
> ③ `RulePullsubMulti::applyOp` 新 MULTIEQUAL 输出落在 unique(旧注释承认
>   "Rugra lacks newVarnodeOut at a computed address"——基础设施已在);改
>   smalladdr2+renormalize+newVarnodeOut 保持原空间(cc:921-940),插入改
>   opInsertBegin(cc:943),补 isPrecisLo/Hi 守卫(cc:889)。
> 验证:Phase 2 首分歧 28→39(`mainloop:redundbranch` 1 vs 0,ActionRedundBranch 域,
> 下一车道);窗口 863=863 事件序列全等;curl E2E defects=0/numbering=0
> (skeleton 3447 vs 基线 0804d8c8 的 3610,−163;next_url diff=120 不变——ordinal-28 窗口在管线中段,
> 39+ 后续阶段仍分叉,最终文本尚未收窄;match_url 94→91);httpd defects=0/numbering=0;
> next_url/main/getparameter.constprop.0 --func defects=0/numbering=0。
> ruleaction.rs+funcdata.rs 属主管线 Rule 改动 → 机制 C 待独立 Cross-Review。

> **SB-HERITAGE50-BLOCKCOVER-0001 修复(2026-09-22,Lane CM `wt/sb-heritage50`,owner=fixer)**:
> Phase 2 首分歧(stage seq 50)`universal:fullloop:mainloop:heritage`
> V1_OP_LINE_DIVERGENCE:SNAP 50 里 oracle 的 8 条 MULTIEQUAL(投影表槽位名 BUILD)
> 挂在 0x2534 而 rugra 挂在 0x50e7,其余 142 条新增 op(INDIRECT/CALL/COPY+42 条
> MULTIEQUAL)逐字节一致。根因**不在 heritage**——`Heritage::placeMultiequals`
> cc:2633 `fd->newOp(bl->sizeIn(), bl->getStart())` 忠实,而 `BlockBasic::getStart`
> (block.cc:2319)=**cover(多范围 RangeList)按 offset 排序的首范围首地址**;
> Rugra 把 cover 模型降级成了单一 `initial_range` 且 `splice_block_basic` 缺
> `mergeRange`(funcdata_block.cc:942),拼接块(next_url blk28 吸收了 0x2534 的
> 尾块,0x2534<0x50e7)永不产生第二范围。自底向上补齐:block.rs cover→RangeList
> +copy_range(block.hh:468)/merge_range(:469)/get_entry_addr(block.cc:2291),
> funcdata.rs splice 前接线 mergeRange、node_split_block_edge 接线 copyRange
> (funcdata_block.cc:832),printc label/goto 寻址(printc.cc:3170 emitLabel)
> 从 getStart/分支常量切换为 getEntryAddr。**Phase 2 首分歧 50→65**
> (event-ordinal 60,= 已登记 SB-OPPOOL-R4-COUNT-0001 的 oppool1
> result/count 85 vs 77,数字逐字一致,ordinals 1-64 全匹配)。
> curl E2E defects=numbering=0(skeleton 3959→3960,见 Differential);
> httpd defects=0 numbering=8(=master 侧预存集,逐字相同);cargo test --lib
> 失败集与 master 逐名一致(16 预存+flaky 家族)。block.rs/funcdata.rs/
> printc.rs 非机制 C 白名单;heritage.rs 本 commit 净零改动(半成品探针
> 未提交)。可见输出残差=3 条无引用 label(合法 C),登记
> `PRINTC-LABEL-WITHOUT-GOTO-0001`。oracle e40ed13014025f82488b1f8f7bca566894ac376b;2026-09-22。

> **SB-REDUNDBRANCH-ORD39-0001 修复(2026-09-22,Lane CJ `wt/sb-redund39`,owner=fixer)**:
> Phase 2 ordinal 39 `universal:fullloop:mainloop:redundbranch` result/count/apply
> 1(oracle) vs 0(rugra)。drill:oracle 在该窗口把 `50fa:53 BRANCH in=n:ram:2530:1`
> (尾跳 thunk 0x2530 的桥 BRANCH)dead 化——case 1 spliceBlockBasic 路径;CD fix3 的
> SNAP 39 显示 rugra 侧同 op 同样 dead 化,**IR 变换双侧逐字节一致,缺的只是计数**:
> Rust apply 从未镜像 cc:3509/cc:3525 `count += 1`(也无 take_count_delta 收割),
> perform 的 `lcount < count` 永不触发→count_apply 不增、@END 报 0。同趟补齐两处
> 忠实性:①case 1 splice 后 `i = -1` 重扫(cc:3510-3511,图尺寸每轮重求值;旧代码
> 提前 return);②isSwitchOut 守卫从 `(flags & 0)` 占位接上真实
> `block_flags::SWITCH_OUT`/`is_switch_out()`(cc:3506)。验证:CD 干净基线
> (wt/sb-oppool28 + cherry-pick 本修复,RUGRA_MIRROR=1)Phase 2 首分歧 39→**60**
> (ordinals 1-59 全匹配,redundbranch@39 = result=1 count=1 apply=1 与 oracle 逐字
> 节同,SNAP 39 `50fa:53 BRANCH d=1 out=- in=-` 行号 28399 都一致);本 lane 分支
> (master 22b5ad84 合并)被 SB-MASTER-ACTIVEPARAM-0001 在 ordinal 15 抢先,masked
> (master 单独构建实测回归,非本修复引入);curl/httpd E2E defects=numbering=0;
> coreaction 非机制 C 白名单,root 快速复核即可。


| ID | 优先级 | 状态 | owner | write-set | 说明 |
|---|---|---|---|---|---|
| `MYPROGRESS-ANDCOMMUTE-GATE-0001` | P1 | OPEN(已归因;ruleaction 被占域,sb-myprogress lane 2026-09-23 登记不写) | 未认领(writer 须与 GETPARAM-OPPOOL-COUNT-0001 串行,ruleaction.rs 单 writer) | `src/ruleaction.rs`(RuleAndCommute)+`docs/api/ruleaction.md`+本行;只读排查可并 | **myprogress Phase 2 首分歧(第六函数验证,wt/myprogress @ master 94f3bf58)**=ord 28 `universal:fullloop:mainloop:stackstall:oppool1` SNAP op-idx 24(op@3515:39e,原 PIECE(c:0,EBX:4) 双侧同型):oracle=Piece2Zext→PropagateCopy→SubZext 臂1 产物 `INT_AND(INT_RIGHT(u:23e00,10)→register:0:8, c:ffffffff)` 原样保留——RuleAndCommute(ruleaction.cc:1532-1626)对非 (LEFT+const+loneDescend) 情形**强制收益门** cc:1582-1603:orvn.def 必须 ∈{INT_OR,PIECE} 且掩码重叠测试通过,本例 orvn=LOAD@3503 输出→`else continue` 不 commute;Rugra ruleaction.rs:4679-4680 移植把 OR/PIECE 测试误当 LEFT 子情形,INT_RIGHT **无条件 commute**→`INT_RIGHT(INT_AND(u:23e00,c:3fffffffc00),c:a)`(3515:524)+2 个多余 8B unique(VarnodeBank::createUnique varnode.cc:1265-1269 全局 bump uniqid+=s)→级联:后续所有 unique 偏移 +0x10(367e zext 0x10000104→0x10000114)与 367e:523→525 时间戳漂移均为此残差,非独立缺陷。@END 28 计数双侧同为 729/729/0/8(纯 SNAP 内容分歧,池记账无差)。修复方向=1:1 补 cc:1582-1603 收益门(唯一无条件短路=LEFT+othervn.isConstant+shiftvn.loneDescend==op cc:1573-1580;注意 cc:1566 `(fullmask<<sa)&&fullmask` 疑似 `&` 的 Ghidra 原样怪癖须按字面移植;另补 othervn->isHeritageKnown 守卫 cc:1556)。验收=myprogress 投影(bash tools/run_stage_projection_oracle.sh curl 0x34d0 myprogress + RUGRA_MIRROR)首分歧 ord 28 后移 + next_url/match_url 双 MATCH 保持 + curl/httpd E2E 0/0。证据=/dev/shm/rugra-tests/sb-myprogress/(bisect_master.txt+snap26/28 双侧 ops);oracle e40ed13014025f82488b1f8f7bca566894ac376b |
| `MYPROGRESS-INT2FLOATCOLLAPSE-0001` | P1 | OPEN(已归因;同上登记不写) | 未认领(同上串行约束) | `src/ruleaction.rs`(新增 RuleInt2FloatCollapse+oppool1 注册)+`docs/api/ruleaction.md`+本行;只读排查可并 | **myprogress ord 28 SNAP 后段第二根因**:XMM0 4 字节 MULTIEQUAL@3594:501(RulePullsubMulti 拉低,双侧同建同 seqnum)oracle 随即被 **RuleInt2FloatCollapse**(ruleaction.cc:9834-9894,oppool1 注册 coreaction.cc:5637 紧随 RuleUnsigned2Float:5636)原地重定义为 `INT2FLOAT(INT_ZEXT(RSI register:30:8)→register:1200:4)`+新 zext op@3594:525(zext 尺寸=TypeOpFloatInt2Float::preferredZextSize(8)=**9 字节**,typeop.cc:1891-1902 inSize≥8→inSize+1;9 字节 unique 即源于此),signed INT2FLOAT@358f:177 与 36a0 Unsigned2Float 对(INT2FLOAT@36a0:224+ZEXT@36a0:526)悉数死灭(oracle 198 ops);Rugra grep 零命中无此 Rule→MULTIEQUAL(BUILD)@3594:501 存活+358f/36a0 对存活(201 ops,+3)。触发语料=x86 `cvtsi2ss r64` 负数双路径:`js` 负路径 (X>>1)|(X&1)+addss T+T(Unsigned2Float 双侧已 fire:367e R8 路+36a0 RSI 路)、jns 正路径 INT2FLOAT(X),MULTIEQUAL 合并点经 FlowBlock::findCondition+INT_SLESS(X,0) cbranch 判向;XMM1 族(367e,R8)双侧一致未折叠(pass1 内未达)。修复方向=1:1 移植 applyOp(opUninsert/opSetOpcode FLOAT_INT2FLOAT/opRemoveInput slot0/newzext@multiop.getAddr+newUniqueOut(preferredZextSize)+opInsertBefore;findCondition/opRemoveInput 基础设施若缺先补底)。验收=同上(首分歧后移,期望与 ANDCOMMUTE-GATE 修复叠加后 myprogress 全函数投影收敛或推进到 ord 28 之后新首分歧)。证据同上目录 |
| `OPPOOL28-EQUAL2ZERO-LADDER-0001` | P1 | FIXED_PENDING_REVIEW(候选 wt/sb-oppool28;机制 C 待独立复核) | oppool28-agent@wt-sb-oppool28 + root | `src/ruleaction.rs`(RuleEqual2Zero/RulePullsubMulti)+`src/funcdata.rs`(cse_elimination/cse_eliminate_list)+`docs/api/{ruleaction,funcdata}.md`+本行 | ordinal-28 三根因修复,详见上方块;oracle e40ed13014025f82488b1f8f7bca566894ac376b;2026-09-22 |
| `SB-REDUNDBRANCH-ORD39-0001` | P1 | FIXED(候选 wt/sb-redund39 1df1c4b1;coreaction 非机制 C 白名单) | redund39-agent@wt-sb-redund39 | `src/coreaction.rs`(ActionRedundBranch)+`docs/api/coreaction.md`+本行 | 根因=纯计数缺口:Rust apply 从未镜像 cc:3509/cc:3525 `count += 1`(无 take_count_delta 收割),perform 的 lcount<count 永不触发→@END result/count/apply=0 而 oracle=1;IR 变换本身(SNAP 39 `50fa:53 BRANCH` dead 化)双侧已逐字节一致。同趟补两处忠实性:case 1 splice 后 `i=-1` 重扫(cc:3510-3511,旧代码提前 return)、case 2 removeBranch 后继续扫描;isSwitchOut 守卫从 `&0` 占位接上 `block_flags::SWITCH_OUT`(cc:3506)。验证:CD 干净基线(wt/sb-oppool28+cherry-pick,RUGRA_MIRROR=1)Phase 2 首分歧 39→60(ordinals 1-59 全匹配,redundbranch@39 result/count/apply=1/1/1 与 oracle 逐字节同);本 lane 分支(master 22b5ad84 合并基)因 SB-MASTER-ACTIVEPARAM-0001 在 ordinal 15 先分歧,masked;curl/httpd E2E defects=numbering=0;oracle e40ed13014025f82488b1f8f7bca566894ac376b;2026-09-22 |
| `SB-OPPOOL-R4-COUNT-0001` | P1 | FIXED_PENDING_REVIEW(候选 wt/sb-oppool65;主管线 Rule 域,机制 C 待独立复核) | oppool65-agent@wt/sb-oppool65 | `src/subflow.rs`(try_call_pull)+`docs/api/subflow.md`+本行 | 根因=Rugra `try_call_pull` 是 CALLSPEC-0001 D0 残留的无条件 false stub:凡 4 字节 lane 直达 CALL/CALLIND 参数槽的 `RuleSubvarZext` 追踪全部夭折。8 次差分解(next_url oppool1 窗口 2,oracle 85 vs rugra 77):subvar_zext −2(ZEXT `5040:69a` R9 lane / `5044:689` R8 lane 入 `505d:131` call free 参数槽)+ 级联 propagatecopy −2 + earlyremoval −4。修复=subflow.cc:208-228 全语义接线(slot==0→consume 截断→getCallSpecs(funcdata.cc:484-497)→isInputActive→isInputLocked&&!isDotdotdot→parameter_patch+pullcount);`try_call_return_push` 保留保守 false(未被语料触发,仍绑 CALLSPEC-0001)。验证:镜像态 oppool1 四窗口 [863,85,15,12] 全等;Phase 2 首分歧 65→159(`universal:fullloop:activereturn`,dead DELAY_SLOT `2534:5a4` in=`-,-` vs `-`,master 同位置实测一致=pre-existing latent 家族非本修复回归);确定性双跑 sha256 相等;gp 投影 master/fix 逐字节零差(证明 gp ordinal-7 funclink 分歧与本品无关);main oppool1 680→778(调用重函数合法激活);curl E2E defects=0/numbering=0/skeleton 2964(基线 2978,−14);httpd defects=0/numbering=0/skeleton 2392(=基线);--func main defects=0,--func getparameter numbering=OK(skeleton 816=预存 funclink 族);cargo test --lib 失败集与 pristine master 逐名一致(funcdata 17+heritage 1 预存,基线 worktree 实测)。oracle e40ed13014025f82488b1f8f7bca566894ac376b;2026-09-22 |
| `SB-ORD159-NULLSLOT-0001` | P1 | FIXED(候选 wt/sb-ord159;op/funcdata 基础设施非机制 C 白名单,root 复核) | ord159-agent@wt-sb-ord159 | `src/op.rs`(null_slot_sentinel+set_num_inputs)+`src/funcdata.rs`(op_unset_input clearInput+op_destroy 保留槽数)+`src/transform.rs`(哨兵共享化)+`src/ruleaction.rs`(仅回归测试断言忠实化)+`examples/{curl,httpd}_decompile.rs`(stage_vn NULL 渲染)+`docs/api/{op,funcdata,transform,ruleaction}.md`+本行 | 根因=nullable input-slot 基础设施缺口(SB-OPPOOL-R4 暴露的 ordinal 159):Ghidra `opDestroy`/`opUnlink`(funcdata_op.cc:179-222)逐槽 `opUnsetInput`→`clearInput`(op.cc:98)置 NULL 保留 inrefs 槽数,死 op 的 numInput() 可观测为 N 个 NULL 槽(harness writeOp 每槽一 `-`,next_url ordinal 159 `2534:5a4 DELAY_SLOT(=INDIRECT) in=-,-`);Rugra `op_destroy` 清空 Vec(in=-),`op_unlink`/condexe 槽丢弃路径残留 stale Arc。修复=①`op.rs::null_slot_sentinel()` 进程级共享 size-0 脱离 bank 哨兵(Ghidra `(Varnode*)0` 替身,单一实例保证 NULL==NULL 的 ptr_eq 语义,op.hh:166 getSlot);②`op_unset_input` 实现 cc:98 clearInput(槽内置哨兵,幂等,descend 已断时纯 no-op);③`op_destroy` 删去 `.clear()`;④transform.rs 私有哨兵改委托共享实例;⑤双侧 stage_vn 渲染哨兵为 `-`(对齐 oracle writeVarnodeDescriptor null 臂);⑥`set_num_inputs` 忠实化(全槽置 NULL,旧实现增长 panic);⑦ruleaction PTRSUB char-constant 回归测试断言改为忠实后置(numInput==2 全 NULL,旧断言编码了本缺口)。验证:Phase 2 首分歧 159→**185**(`universal:fullloop:mainloop:constantptr` result/count 0vs4,V1_RESULT_COUNT_DIVERGENCE;ordinals 1-184 含全部 op-line 逐字节相等,SNAP184 双侧零差;master 投影 SNAP184 与 fix 逐字节相同且 185 处同为 result=4→**pre-existing,非哨兵引入**);确定性双跑 sha256 相等(09d9052b…);curl E2E(124 函数)defects=0/numbering=0/skeleton 2964(=任务基线);httpd E2E(29 函数)defects=0/numbering=0/skeleton 2392(=基线);config 域逐函数 main/getparameter/parseconfig/glob_set/glob_range/glob_url 全 0/0,witness `::config.`=266/DAT_00117[56]xx=0;gcc 审计 81 OK/26 FAIL(=预存基线);cargo test --lib 失败集=预存 flaky 家族(funcdata alignment/ssa + heritage::test_heritage_creation,同树三跑 19/18/22 摆动,非本修复引入;op/transform/ruleaction 域全绿)。残差:`new_op`/`PcodeOpBank::create` 创建期不预置 N NULL 槽(op.cc:71 `inrefs(s)` vs reserve-only),未填满即被 SNAP 的形态仍会暴露 `in=-` vs `in=-,-`(语料未激活,留 OPBANK-0001 家族);机制 B2 函数 fixture 未固化(root 集成阶段按需)。oracle e40ed13014025f82488b1f8f7bca566894ac376b;2026-09-22 |
| `SB-ORD332-SETCASTS-0001` | P1 | FIXED(候选 wt/sb-ord332,基于 wt/sb-ord186 merge;coreaction 非机制 C 白名单,root 快速复核) | ord332-agent@wt-sb-ord332 | `src/coreaction.rs`(ActionInferTypes::propagate_type 覆写集忠实化+cast_input 四族 getInputCast 臂+make_ptr intern 化)+`src/typeop.rs`(propagate_to_pointer_sized)+`src/type_system/cast.rs`(int_promotion_type 可见性 pub,语义零改动)+`docs/api/{coreaction,typeop}.md`+`docs/api/type_system/cast.md`+本行 | 根因三层(Phase 2 ordinal 332 setcasts oracle 33 vs rugra 41):①`propagate_type` 三组自造传播臂(INT_ZEXT/SEXT 前传、BOOL_* bool、MULT/DIV/移位/NEGATE/FLOAT 泛型整数前传)无 oracle 对应物——锁死 oracle 覆写集合仅 COPY/LOAD/STORE/4 比较+SLESS 族/INT_ADD/AND/XOR/OR/MULTIEQUAL/INDIRECT/PIECE/SUBPIECE/PTRADD/PTRSUB/SEGMENT/NEW(typeop.cc 逐行枚举),其余基类 null;ZEXT/SEXT 前传把 uint4 盖到 8 字节 sext 输出→尺寸错配恒 cast→4 个多余 cast(507d/5088/50b8/50c0);②比较/移位/除余的 getInputCast 覆写缺失(SLESS/SLESSEQUAL/LESS/LESSEQUAL cc:1023/1049/1075/1099 的 checkIntPromotionForCompare+care=TRUE 门、ZEXT/SEXT cc:1131/1157+cast.cc:126-138、RIGHT/SRIGHT cc:1543/1585、DIV/SDIV/REM/SREM cc:1639/1659/1679/1699)→泛型臂 care=false 使 EBX(uint4) 输入永不 cast→3 个缺失 cast;③make_ptr 非 intern 新建 Arc+TypePointer::type_equal=ptr_eq→TypeOpStore::getInputCast cast-already-in-place(cc:542-551)恒判不同→双 cast 守卫 lone-descend retype(cc:2675-2678)隐形计数×6;同族 propagate_to_pointer 以 alttype 尺寸建指针而 oracle propagateToPointer(typeop.cc:186-198)以 outvn 尺寸为 sz→6 个 STORE 地址 vn 得 ptr::1/2/4。修复=删自造臂+移植 17 个 oracle 覆写臂(含 INDIRECT/PIECE/SEGMENTOP/NEW 新臂、SUBPIECE getSubType 下降重写、AND/XOR/OR enum/floatSignManipulation 门、SLESS 族 TYPE_INT-only 拆分、FLOAT 比较移出传播)+make_ptr/propagate_to_pointer_sized 经 TypeFactory intern+LOAD/STORE 传播臂改传地址 vn 尺寸。验证:**Phase 2 next_url mirror 全函数投影 MATCH(335 stages+全部 @SNAP 逐字节一致,首分歧清零非后移)**;curl E2E 124 函数 defects=0/numbering=0/skeleton 3105→3024;httpd 29 函数 0/0/2392→2357;config 域 7 函数逐个 0/0;确定性双跑 sha256 相等+投影 cmp 通过;cargo test --lib 失败集与 master 基线逐名一致(见 lane 证据)。残差:acrossCompare PointerRel 降级臂(Rugra 无 TypePointerRel 不可达)、PIECE/SUBPIECE near/far resize(本 arch 无 alt pointer 不可达)、TypeUnion::resolveTruncation 无移植(stub None)、typeop.rs 旧 propagate_to_pointer 调用点(spacebase twin 臂族)未切换——typeop 域收敛。oracle e40ed13014025f82488b1f8f7bca566894ac376b;2026-09-22 |
| `SB-HERITAGE50-BLOCKCOVER-0001` | P1 | FIXED(候选 wt/sb-heritage50;block.rs/funcdata.rs/printc.rs 非机制 C 白名单,root 快速复核) | heritage50-agent@wt/sb-heritage50 | `src/block.rs`(cover 模型+copy/merge/entry)+`src/funcdata.rs`(splice mergeRange+nodeSplit copyRange)+`src/printc.rs`(label/goto→getEntryAddr)+`docs/api/{block,funcdata,printc}.md`+本行 | 根因=BlockBasic cover 基础设施缺口(单范围降级+splice 缺 mergeRange),heritage MULTIEQUAL@getStart 语义本身已忠实;Phase 2 首分歧 50→65;详见上方块;2026-09-22 |
| `PRINTC-LABEL-WITHOUT-GOTO-0001` | P2 | OPEN(SB-HERITAGE50-BLOCKCOVER-0001 暴露) | 未认领 | `src/printc.rs`(emit_any_label_statement/emit_goto_statement 传输层) | getEntryAddr/copyRange 忠实化后被抑制 goto 的 UNSTRUCTURED_TARG 叶现解析出真实地址并打印无引用 label(curl GetStr 域 0x37b2×1;httpd 0x2cb68×2;合法 C,defects=0,skeleton +3);Ghidra 侧该不对称不可达(结构化树 t_copy 全量+goto 必印),修复方向=抑制 goto 时同步清除对应叶的 label 打印义务;2026-09-22 |
| `SB-MASTER-ACTIVEPARAM-0001` | P0 | OPEN(master 侧回归,heritage symbol-tail 域) | 未认领 | TBD(疑 `src/heritage.rs`/`src/funcdata.rs` symbol-tail 插入路径,0804d8c8..22b5ad84 范围) | master 22b5ad84 起(不含 oppool28 合并,单独构建实测):Phase 2 投影 ordinal 15 `mainloop:activeparam` result 9(oracle) vs 2(master),returnrecovery@16 4 vs 0,且初始 SNAP 2 即分歧——`50fa:53` oracle=BRANCH→`n:ram:2530:1` 而 master=CALL→`f:50fa:53`(尾部跳 0x2530 在初始 IR 即被转成 call);嫌疑=heritage symbol-tail promotion 系列(978a0a80/6eab5da9/22b5ad84);阻断一切 master 基线的 Phase 2 首分歧推进(ordinal 1-38 匹配前提被破坏);wt/sb-oppool28 基线不受影响;2026-09-22 |
| `RULE-PULLSUBMULTI-JOINRENORM-0001` | P3 | OPEN | 未认领 | `src/ruleaction.rs`+`src/arch.rs`(暴露 AddrSpaceManager) | RulePullsubMulti join 基底 renormalize(translate.cc:870-916)需活体 AddrSpaceManager,Architecture 现仅简化 join_db;寄存器/unique 空间已精确,debug_assert 拦截 join 命中;2026-09-22 |
| `SB-MATCHURL-PHASE2-0001` | P1 | **FIXED 首分歧 12→55**(2026-09-23,wt/sb-matchurl;heritage.rs 机制 C 白名单,Cross-Review PENDING 集成前必须) | sb-matchurl@wt/sb-matchurl | `src/heritage.rs`(guard_output_overlap concat seqnum)+`src/block.rs`(has_loop_in)+`src/ruleaction.rs`(pullsub cc:883 守卫+addmultcollapse spacebase 臂)+`src/arch.rs`(decode_register_data)+`src/funcdata.rs`(inject_raw_ops_single laned 探针)+`examples/{curl,httpd}_decompile.rs`(pspec register_data 接线)+`docs/api/{heritage,block,ruleaction,arch,funcdata}.md`+本行 | 四层根因链(Phase 2 mirror,oracle 投影 sha 2bdabd73…,340 stages/80385 ops):①ordinal 12(heritage,op-idx 0)=guard_output_overlap 两 concat(heritage.rs:3054/3075)以 ret_addr(0x1200)当 op 地址,oracle cc:1259/1272 用 indOp->getAddr()(=call 地址,14 个 PIECE 的 SeqNum pc 翻转+投影 seqnum 排序头部错位);②ordinal 28(oppool1 861v872)=RULE-PULLSUBMULTI-LOOPIN-0001(循环头 phi 放行)+addmultcollapse spacebase 臂缺失(ruleaction.cc:4122-4169,52f1/52f5 的 RSP 常量折叠,修复后 872→858→861 精确对齐);③ordinal 29(lanedivide 2v0)=lane 记录生产恒空(ARCH-REGISTERDATA-LANE-0001:pspec register_data 未解析+min_laned_size=MAX→laned_map 空;装载后仍 0v2=emitter 直建 varnode 绕过 checkForLanedRegister,两处物化点补探针后 2=2);④新首分歧=ordinal 55(heritage 二轮,op-idx 27,stack 多槽 phi 的 uniq 分配序差 7:oracle 64d/668/683/69e vs rugra 646/65a/66e/682,oracle 前置 7 个即生死临时 op[52bd:643..5390:648 双 ** 记录],归 placeMultiequals 写列表/PQ 种子序或 buildADT 增广序,未追杀)。CX 比较极性族验证:match_url **不命中** 5077/50e5 SLESS vs LESS(该族=next_url 域,ord332 已修;match_url 比较点 52a6 `cmp+jle`=SLESSEQUAL 双侧已逐字节对齐)。验证:三门禁 curl 124 函数 defects=0/numbering=0(skeleton 3024→3029:match_url 81→80,helpf 79→82/myprogress 78→81=lane 域激活[XMM0 浮点用点]的下游变量类型/打印残差,非缺陷)、httpd 29 函数 0/0(skeleton 2357→**2337**)、config 域 10 函数逐个 0/0;next_url Phase 2 投影 **MATCH 保持**;cargo test --lib 串行 1650/18(失败集与 master 基线逐名一致)。残差:①ordinal 55(新行 SB-MATCHURL-ORD55-0001);②ARCH-REGISTERDATA-VOLATILE-0001;③RULE-ADDMULT spacebase 臂的 PIECE/SUBPIECE 族其余点未逐函数固化(机制 B2 fixture 待 root);2026-09-23 |
| `SB-MATCHURL-ORD55-0001` | P1 | **FIXED 首分歧 55→70**(2026-09-23,wt/sb-ord55;heritage.rs 机制 C 白名单,Cross-Review PENDING 集成前必须) | sb-ord55@wt/sb-ord55 | `src/heritage.rs`(discover_indexed_stack_pointers/protect_free_stores/generate_load_guard/generate_store_guard 新增+heritage() cc:2691-2697 接线+reprocess_free_stores cc:1117 换真实 discovery+guard_loads_range cc:1590-1599 COPY 插入体与窗口判定)+`docs/api/heritage.md`+本行 | 根因≠登记时的 PQ/写列表假设:**load-guard COPY 族**——oracle `Heritage::guardLoads`(cc:1570-1601)在二轮 heritage 每个 stack range 的 guard() 内为每条相交 `loadGuard` 记录创建 COPY 边界 op(match_url=7 条记录/range:52bd/52c5/52db/52f1/52f5/5390/52c5,uniq 643-649,即"双 **"即死 op——创建于 printDebug 无 parent 时,pass 尾被 `handleNewLoadCopies`→`propagateCopyAway` 销毁),phi uniq 因此从 64a 起;Rugra 侧 load_guard 记录生产链整体缺失(discovery 不在 heritage() 路径上,guard_loads_range COPY 体为登记 stub)→phi uniq 643 起,差 7。修复=discoverIndexedStackPointers(cc:986-1102 全函数:显式 DFS+mark 防梯+INT_ADD 常量/非常量臂+SEGMENTOP in(2) 门+LOAD/STORE traversals≠0 guard 记录+cc:1087 常量 STORE 仅 mark+protectFreeStores)接入 cc:2691-2697 与 reprocess cc:1117,COPY 体逐行落地(创建序 newOp→newVarnodeOut+setActiveHeritage+setAddrForce→COPY opcode→newVarnode 输入+setActiveHeritage→opSetInput→opInsertBefore→loadCopyOps),窗口判定改回 cc:1588-1589 range 起始 offset 逐侧 continue。验证:match_url Phase 2 首分歧 55→**70**(heritage 二轮 drill 块 oracle 847 vs rugra 846 行,除既有 ffunc_/i0x 调用名显示噪音外逐行一致,phi uniq 64d/725/741 双侧对齐);三门禁见 commit;残差=ordinal 70(新行 SB-MATCHURL-ORD70-0001) | 见 SB-MATCHURL-ORD70-0001;2026-09-23 |
| `SB-MATCHURL-ORD70-0001` | P1 | **FIXED 首分歧 70→164**(2026-09-23,wt/sb-ord70;varmap 核心算法层机制 C 白名单,Cross-Review PENDING 集成前必须) | sb-ord70@wt/sb-ord70 | `src/coreaction.rs`(ActionRestructureVarnode scope 跨趟持久+aliasyes 穿透+ActionRestrictLocal 逐行重写)+`src/varmap.rs`(restructure_varnode 加 aliasyes 参+移除每趟 reset_local_window+mark_unaliased/check_unaliased_return 入门)+`src/funcdata.rs`(set_input_varnode 补 cc:365-370 hasEffect 尾)+`src/fspec.rs`(FuncProto::try_has_effect)+`examples/diag_stack.rs`+`docs/api/{varmap,coreaction,funcdata,fspec}.md`+本行 | 根因≠登记时的"别名表内容差"假设——双侧探针(oracle 打补丁 ALIASPROBE/ICPROBE/RLPROBE/GATHERPROBE/MNM vs rugra 临时 env 探针,均 /dev/shm/rugra-tests/sb-ord70)实证**别名表逐 pass 完全一致**(pass1 5 项翻转集/pass2+ 8 项 0x8,58,58,60,68,68,6a,6c);真根因=**ScopeLocal 生命周期**:Ghidra 的 ScopeLocal 是 per-Funcdata 单例(funcdata.cc:63-71 构造一次,resetLocalWindow 仅构造/clear 时跑),FuncCallSpecs::buildInputFromTrials(fspec.cc:5737)对 outgoing 栈参数槽(-0x48/-0x40/-0x38=b8/c0/c8,parameter=true)与 ActionRestrictLocal(cc:1979/1997)对 saved-reg 溢出槽(a8/b0/e0/e8/f0/f8)的 markNotMapped 窄化**跨趟存活**,下一趟 MapState::addRange 的 range.inRange 门(varmap.cc:902)丢弃这些槽的 hint→无 entry→varnode 无 nolocalalias→RuleIndirectCollapse 的 hasNoLocalAlias 分支(ruleaction.cc:3199)保留 INDIRECT;Rugra 每趟 `ScopeLocal::new()`+每趟 reset_local_window 全量重装窗口→窄化被抹→b0/b8/c0/c8 四 entry 复活且 markUnaliased 判 aliason=0(unaliased:翻转 curoff-0x6c>0xffff 距离启发式)→sync 置 NOLOCALALIAS→39 个 call-guard INDIRECT 提前折叠(oracle 在 ordinal 214 才折叠其中 41 个)。次要修复:①aliasyes=(numpass!=0) 穿透 restructure_varnode(cc:1280-1283 门 markUnaliased/checkUnaliasedReturn;annotateRawStackPtr 不受门);②ActionRestrictLocal 双循环逐行重写(旧 loop1 用 >0x7FFF_FFFF 启发式替代 IPTR_SPACEBASE 空间判定+旧 loop2 遍历全 bank COPY 且把 REGISTER 输出当目标);③set_input_varnode 补 cc:365-370 hasEffect 尾(unaffected/return_address 标志,is_unaffected 数据源)+FuncProto::try_has_effect 非 panic 形态。**附发现(DRILL-FIXTURE-RESET-0001 基础设施)**:oracle DRILL 夹具(stage_drill_1204.cc)不调 root->reset(*fd)→ActionRestructureVarnode::numpass 构造器未初始化(coreaction.hh:855 无 numpass(0))读堆垃圾恒非零→drill 捕获全程 aliasyes=true,与 PROJECTION 夹具(stage_projection_1204.cc:425 有 reset)的 pass1 aliasyes=false 协议分叉;修后 rugra drill census 与 oracle drill 的残差(earlyremoval -14/propagatecopy -7/identityel -7/termorder -7/addmultcollapse -7/collect_terms +1,indirectcollapse 8=8)主要反映该夹具差,以 PROJECTION 为准 | match_url Phase 2 投影首分歧 70→**164**(universal:fullloop:activereturn,op-idx 139:53ee:1c5 CALL 输出 RDX 试探 INDIRECT 族,oracle CALL out=- 保留 DELAY_SLOT 53ee:479(out=RDX,in=c:0:8)vs rugra 直连 out=RDX+死 DELAY_SLOT;A/B 实证与 RestrictLocal/hasEffect 尾无关=ord70 墙后既有遮蔽分歧,新行 SB-MATCHURL-ORD164-0001);三门禁:curl 124 函数 defects=0/numbering=0(skeleton 3029→2795:main 929→693,match_url 81→80;残差 myprogress 78→82/helpf 79→82/file2string 129→130=pass1 忠实 aliasyes=false 后 in_RSP→register0x00000020 命名路径残差,非缺陷,defects=0)、httpd 29 函数 0/0(skeleton **2339**=基线零回退)、config 域 10 函数逐个 0/0 零回退;next_url Phase 2 投影 **MATCH 保持**(唯一差异=META producer 行);cargo test --lib 串行 1650/18(失败集与 master 基线逐名一致);VARMAP-CROSSPASS-PERSISTENCE-0001 关闭 | 2026-09-23 |
| `SB-MATCHURL-ORD164-0001` | P1 | **FIXED 首分歧 164→191**(2026-09-23,wt/sb-ord164;fspec 域非机制 C 白名单,建议 root 复核) | ord164@wt/sb-ord164 | `src/fspec.rs`(ModelRuleFillin/FillinAction `<rule>` fillin 投影+ParamListStandard.model_rules+decode rule 分支真解码+ParamListStandardOut::initialize 忠实扫描+fillin_map 规则步)+`docs/api/fspec.md`+本行 | 根因≠试探传播/isDotdotdot 门——**`ParamListStandardOut::initialize` 钉死空规则分支**:Rugra `use_fillin_fallback=true` 恒走 `fillin_map_fallback(active,false)`,firstOnly=false 让非-first-in-class 的 RDX output entry 参选,lone RDX 试探(exit@plt 调用,killedbycall guardCalls const0 DELAY_SLOT)被 markUsed→buildOutputFromTrials 单试探提交直连 CALL out=RDX+销毁 DELAY_SLOT;oracle 因 gcc __stdcall output 含 `<join_dual_class/>`(MultiSlotDualAssign ctor fillinOutputActive=true, modelrules.cc:1143)得 useFillinFallback=false→fillinMap 规则步 MultiSlotDualAssign::fillinOutputMap(cc:1242-1291)对 RDX 试探 `!isFirstInClass()`(RAX=general 类首,resolveFirst fspec.cc:76-88)拒绝→fillinMapFallback(true) firstOnly 过滤(cc:1649)跳过 RDX→全 markNoUse→getNumTrials()==0 早退,out=- 保持+const0 试探 INDIRECT 存活。修复=`<rule>` 解码进 ParamListStandard.model_rules(七种 assign action 的 fillin 投影:五种 fillinOutputActive=true 的 trial-walk 逐行移植 cc:731/902/1019/1242/1345,两种默认 false;datatype filter/qualifier/precondition/sideeffect 结构化跳过=fillin 路径不消费)+initialize 扫描 canAffectFillinOutput(仅 legacy 分支强制 auto_killed_by_call=true,同步修掉 __stdcall output 的 auto_killed 假 true)+fillin_map sort_trials 后按声明序走规则(cc:1746-1761)否则落 fallback(true)。双侧探针实证(rugra env 探针 /dev/shm/rugra-tests/sb-ord164/probe{1,2}.stderr):修前 post-derive RDX used=true entry=Some(3),修后 active=false used=false(=oracle markNoUse 形态);entry 表双侧一致(XMM0/XMM1 Float,RAX first=true,RDX first=false/excl/ngroups=1) | match_url Phase 2 投影首分歧 164→**191**(universal:fullloop:mainloop:oppool2,V1_RESULT_COUNT_DIVERGENCE result/count 23v15 apply 2v1;SNAP191 170v156 ops:oracle 建 CROSSBUILD+INT_ADD 常量折叠链 u:100001xx 族 vs rugra 直连 stackbase INT_ADD,新行 SB-MATCHURL-ORD191-0001);三门禁:curl 124 函数 defects=0/numbering=0(skeleton **2795**=亲父实测基线零回退,config 域 main 693/getparameter.cp 769/parseconfig.cp 128/glob_set 92/glob_range 80/glob_url 2/next_url 103/match_url 80 全 0/0)、httpd 29 函数 0/0(skeleton **2339**=基线)、next_url Phase 2 投影 **MATCH 保持**(335 stages/96457 ops,归档 /dev/shm/rugra-tests/sb-ord164/next_url.fix.projection);cargo test --lib 串行 1650/18 失败集与基线逐名一致;gcc 审计 81 OK/26 FAIL=预存基线;result/curl_cur.c 已回流 | 2026-09-23 |
| `SB-MATCHURL-ORD191-0001` | P1 | OPEN | 未认领 | 待定(oppool2 规则域,疑 RulePushMulti/multicarry CROSSBUILD 创建链) | match_url Phase 2 新首分歧 ordinal 191(universal:fullloop:mainloop:oppool2,V1_RESULT_COUNT_DIVERGENCE):result/count oracle=23 rugra=15,apply 2v1;@SNAP191 170v156 ops——oracle 为每个 stack 槽位偏移建 4-op 链 `INT_ADD(reg0,c)→INT_ADD(-c,·)→CROSSBUILD(reg20,c)→INT_ADD(·,·)`(52b0/52bd/52c5/52db…c∈{68,58,60,8}),rugra 只有直连 `INT_ADD(reg20,u:…)`(52bd:d9/52c5:62a/52db:f2)+少 14 ops;oppool2=mainloop 第二规则池,CROSSBUILD 创建者待定位(Funcdata::opCrossBuild 调用族,疑 RulePushMulti ruleaction.cc 或 multiequals 相关);注意 ord164 修复(auto_killed_by_call 假 true 消除)移除了 rugra 侧一批多余 const0 INDIRECT 后暴露 | 2026-09-23 |
| `DRILL-FIXTURE-RESET-0001` | P2 | OPEN(基础设施) | 未认领 | `tests/oracle/stage_drill_1204.cc`(+metadata 重钉)+上游 `ghidra` coreaction.hh:855(只读记录) | oracle DRILL 夹具的 perform 循环不调 `root->reset(*fd)`(PROJECTION 夹具 stage_projection_1204.cc:425 调),而 `ActionRestructureVarnode` 构造器(coreaction.hh:855)**不初始化 numpass**,reset(Funcdata&) 才置 0——drill 捕获全程 numpass=堆垃圾(实测 1115849089)恒非零→aliasyes 恒 true,pass1 也跑 markUnaliased,与 PROJECTION 的 pass1 aliasyes=false 协议分叉(2026-09-23 sb-ord70 双探针实证:numpass 垃圾值 + aliasyes=1)。影响:凡依赖 restructure_varnode pass 语义的 drill 对比(如 indirectcollapse census)在 pass1 语义上与 projection 不可互换;修 drill 夹具需重钉全部 stage_drill_1204 metadata pins(comparand_sha256.cpp_fixture)与依赖 capture | 2026-09-23 |
| `ARCH-REGISTERDATA-VOLATILE-0001` | P3 | OPEN | 未认领 | `src/arch.rs`(decode_register_data volatile 臂)+`src/database.rs`(setPropertyRange 接线) | pspec register_data 的 `volatile` 臂(architecture.cc:960-963 symboltab->setPropertyRange)未移植;锁定 x86-64.pspec 零 volatile 声明(grep 干净)故不可达,命中时 stderr [ARCH] 报告;2026-09-23 |
| `RULE-PULLSUBMULTI-LOOPIN-0001` | P3 | **FIXED**(2026-09-23,wt/sb-matchurl `FlowBlock::has_loop_in`+ruleaction.rs cc:883 守卫接入;block.rs/ruleaction.rs 非机制 C 白名单,root 复核) | sb-matchurl@wt/sb-matchurl | `src/block.rs`(has_loop_in trait 默认方法)+`src/ruleaction.rs`(守卫)+`docs/api/{block,ruleaction}.md`+本行 | 见 SB-MATCHURL-PHASE2-0001:match_url Phase 2 ordinal 28 oppool1 首发射错位(idx 358)根因=守卫保守放行 __libc_csu_init 循环头 phi@0x5440(5454→5440 回边,f_loop_edge 已由 find_spanning_tree 维护,查询器缺失);接入后 861=861 |
| `SB-ORD185-CONSTANTPTR-BAREDB-0001` | P1 | FIXED(候选 wt/sb-ord185;examples/驱动层非机制 C 白名单,root 复核) | ord185-agent@wt-sb-ord185 | `examples/curl_decompile.rs`(program_db 构造 mirror bare 分支 + worker_arch 全局 seeding mirror 门)+`docs/alignment_docs/blueprints/FLOW_MIRROR_0001_DESIGN.md`(§7.2 erratum)+本行 | 根因=镜像态数据环境不诚实(Phase 2 首分歧 ordinal 185 `universal:fullloop:mainloop:constantptr` result/count oracle 0/0 vs rugra 4/4):oracle 单函数 harness 是裸 BfdArchitecture,`Architecture::readLoaderSymbols`(architecture.cc:346-359)对**每个** loader 符号——含 ELF OBJECT 数据符号(如 curl 的 glob_buffer@0x17680)——只调 `scope->addFunction`(LoadImageFunc 无数据/函数之分),全局 scope 地址图**零数据 SymbolEntry**,`ActionConstantPtr::isPointer` 的 `queryContainer(rampoint,1,Address())`(coreaction.cc:1151)恒 NULL→永不 fire;且无 analyzer(无 DAT 标签/字符串 char 数组)、无 DWARF。Rugra 镜像态却装了两层全分析数据符号:①worker program_db(request 的 59999 rodata_dat_entries+164 db_symbol_entries+DWARF merge),②`build_worker_architecture` 无条件 DWARF globals+ELF OBJECT seeding(MAINDIFF-UNIQLEAK-0001 层)→rugra 在 0x14910(CALL slot2,DAT char[])、0x149b0(COPY,DAT char[])、0x17680×2(COPY+CALLIND,glob_buffer DWARF char[4096])fire 4 次 PTRSUB 改写。修复=bare-load 门(mirror_bare_load_enabled)下两层全部抑制,program_db 仅保留 oracle 裸环境确有的:cspec `<global>` scope range + loader readonly range(`fillinReadOnlyFromLoader` architecture.cc:1371-1383→`LoadImageBfd::getReadonly` loadimage_bfd.cc:286-302 的 SEC_READONLY 段镜像)。验证:Phase 2 首分歧 185→**186**(`universal:fullloop:mainloop:oppool2` result/count oracle 4 vs rugra 3,V1_RESULT_COUNT_DIVERGENCE,下一车道目标;ordinal 185 双侧 0/0 全等);curl E2E(124 函数)defects=0/numbering=0/skeleton **3105**(=任务基线,env-off 路径逐字节不变);httpd E2E(29 函数)defects=0/numbering=0/skeleton **2392**(=基线);config 域 main/getparameter/parseconfig/glob_set/glob_range/glob_url 全 0/0,witness `::config.`=265(src/ 未动 + env 门控默认关,E2E 路径结构性不变;skeleton/defects/numbering 全等于任务基线即为回退判据);cargo test --lib 1650 passed/18 failed(失败集=预存 funcdata alignment/ssa+heritage 家族,src/ 未动,失败与 HEAD 同集)。残差:①oppool2 4v3(新首分歧,未认领);②裸环境 readonly range 只覆盖 rodata_span,oracle 是全部 SEC_READONLY 段(.text/.eh_frame 等,语料未激活);③机制 B2 逐函数双侧 fixture 未固化(root 集成阶段按需)。oracle e40ed13014025f82488b1f8f7bca566894ac376b;2026-09-22 |
| `SB-ORD186-PTRARITH-0001` | P1 | FIXED(候选 wt/sb-ord186;coreaction.rs 机制 C 白名单域,待 root 复核+独立 Cross-Review) | ord186-agent@wt/sb-ord186 | `src/coreaction.rs`(ActionInferTypes::propagate_type MULTIEQUAL 分支)+`src/typeop.rs`(TypeOpMulti::propagate_type spacebase 尺寸/源选择)+`docs/api/{coreaction,typeop}.md`+本行 | 根因=phi 不透明(移植缺陷,非镜像态问题):2026-08-26 my_f_write 会话(5e75c6a0)以"Ghidra 无 TypeOpMulti::propagateType 覆写"为假前提把 MULTIEQUAL 分支改 None,切断指针类型跨 phi 传播;oracle typeop.cc:1951-1965 覆写存在且透明(inslot/outslot 单边,spacebase 源→getTypePointer(alttype 尺寸,unknown1,dataSpace wordsize))。Phase 2 首分歧 ordinal 186 `universal:fullloop:mainloop:oppool2`(oracle result/count 4/4 vs rugra 3/3):第 4 次 RulePtrArith INT_ADD→PTRADD(*#0x1) 在 `0x50db:c4 RBP(phi 0x50c0:65b)+RAX(free retval)` 未 fire,rugra 侧 phi base 停留 Int/8(TYPE_PTR 槽搜索失败);另 3 次(0x506a/0x5105/0x513f,base=input RBP)双侧同 fire。修复=coreaction.rs MULTIEQUAL 分支逐字恢复 oracle 语义(spacebase 源=coreaction.cc:5080 invn 选取:inslot==-1 时 op output;指针尺寸=alttype.get_size(),pointee=unknown1,ram wordsize=1,与已验证 COPY 分支同字面量)+typeop.rs 孪生实现同修(旧:propagate_to_pointer(unknown1) 把指针尺寸错取 1;spacebase 检查看目的 input 而非源 output)。my_f_write 无回退(其收敛真因=同 commit 的 LOAD/STORE propagate_to_pointer 截断修复)。验证:Phase 2 首分歧 186→**332**(`universal:setcasts` result/count oracle 33 vs rugra 41,独立缺口未认领,下一步 Lane 候选;ordinal 1-331 全匹配含 oppool2 4/4);drill 窗口 ptrarith 4/4 双侧同形;curl E2E(124 函数)defects=0/numbering=0/skeleton **3105→3049**(改善);httpd E2E(29 函数)defects=0/numbering=0/skeleton **2392→2348**(改善);`--func next_url` 0/0;cargo test --lib 1659 passed/9 failed(失败集⊆预存 funcdata alignment/ssa+heritage 家族,master 基线对照见 lane 笔记)。残差:①setcasts 33v41(新首分歧,未认领);②机制 B2 逐函数双侧 fixture 未固化(root 集成阶段);③coreaction=机制 C 白名单,需独立 Cross-Review 批准块后并入主管线。oracle e40ed13014025f82488b1f8f7bca566894ac376b;2026-09-22 |
| DD(wt/sb-sec44) | `VARGROUP-ABSORB-0001 §4-4`(P1,符号/打印层吸收收官) | writer | `src/{coreaction,merge,printc,varmap}.rs`+`src/type_system/typefactory.rs`+`docs/api/*` 同步+机制文档 §6,branch `wt/sb-sec44` | **DONE(2026-09-23,commit 0c90f7ef+86e3d79a)**:oracle 侧 print map/tree+gdb pushPartialSymbol 双臂实证补全(§6);激活条件缺口 6 处全修——①ActionRestrictLocal 参数谓词(offset 启发式→IPTR_SPACEBASE 空间测试,fc78..fd90 出参影子区 markNotMapped)②ActionRestructureVarnode 复用持久 fd.scope+窗口一次性安装(修 VARMAP-CROSSPASS-PERSISTENCE-0001 复活环)③inflate_test 全量对齐(intersect==2 边界放行+copyShadow/partialCopyShadow+修读锁跨写锁死锁)④markInternalCopies PIECE/SUBPIECE 两臂(CONCAT164/204 重组语句隐藏)⑤get_base twin 超尺寸→unknown1[] 数组(280B 影子 piece-structured 化,RuleSubRight 特殊打印点火)⑥push_symbol_detail_leaf 局部符号 partial 走向恢复;验收=main ~43 CONCAT→5(无关区既有)+glob.pattern[i].* 28 条字段路径语句 golden 形态;curl defects=0/numbering=0 skeleton 2993→2729(main 893→639);httpd 0/0 skeleton 2339==基线;next_url 103/103+glob_set 92/92+glob_range 80/80+glob_url 2/2+match_url 79/79 零回退;cargo test --lib 18 failed=HEAD 基线既有(checkout HEAD 复跑证实);**merge.rs 属机制 C 白名单——合并主管线前需独立 agent 复核加 Cross-Review:APPROVE 块**;残余=RHS finalcast((anon_union_...)auVarXX._8_16_,printc.cc:2018-2029)+union resolution 命名(content._8_8_ vs content.Set._8_8_)+main 无关区 5 处 CONCAT,均登记机制文档 §6 下一环候选 |
| DJ(wt/sb-getparam) | `GETPARAM-PHASE2-0001`(第三函数 getparameter.constprop.0 Phase 2 对拍+首分歧追杀) | writer | `examples/curl_decompile.rs`(镜像 callee 数据源两半门控)+`src/blockaction.rs`(两处 mutation-only 臂补 bump)+`docs/api/blockaction.md`+本行,branch `wt/sb-getparam`(基 master 1cecf7b5) | **DONE(2026-09-23,commits 59970f0b+dbd60ecd,首分歧 ord 7→55)**:①**ord 7 funclink**(op-idx 257,CALL@3ff2:4c3 GetStr@0x36d0):根因=**镜像环境不对称**——raw BFD oracle harness 无 DWARF,callee FuncProto 壳未锁,queryCall(flow.cc:656-672)只 setFuncdata+copyFlowEffects;Rugra link_call_specs 的 callee-DWARF 半(debug_db.locked_callsite_proto)在镜像下仍锁 28 个调用点签名(GetStr 2 参→funclink 走 locked 正序装参臂 coreaction.cc:1484-1509,oracle 走未锁栈占位臂+heritage guardCalls cc:1495-1506 逆序装 trial)。潜伏至今因 next_url/match_url callee 全是 import(bare-load 已清 libc 账本)。修复=镜像下抑制 callee-DWARF 半+FLOW-NORETURN-DATA-0001 segment(a) 调用点 no_return 标记(与 (b)/(c) 同 mirror_fixture_data_enabled 门)。②**ord 40 blockstructure count 27v26**:双侧规则轨迹+likelygoto 生成列表逐字节同(TraceDAG/LoopBody 移植已对齐,勘误 TRI2-STRUCT-IRREDUCIBLE-TRACE-0001 的嫌疑方向)至 pick 111;分叉=两个 mutation-only 规则臂 return true 不 bump structure_change_count(内层 fixpoint 以其增量建模 Ghidra 局部 change 布尔=规则返回值):checkSwitchSkips 标记臂(cc:1711-1712)+newBlockMultiGoto already 臂(cc:1726-1732,块 29 第二次 multigoto 剥边后 ruleBlockSwitch 饿死)→假收敛→selectGoto 挑走 (131,133)/(131,15) 外来边,oracle 以 Cat15+DoWhile15(+1 dfc)收 loop(15,131)。修复=两臂补 structure_change_count bump(dataflow 计数不动=oracle 同臂)。③**新首分歧 ord 55 activeparam**(op-idx 953,CALL@4151:20a strequal@plt 12 个 trial 输入 stage54 全同,oracle buildInputFromTrials 全剥,rugra 留 u:23e00=RSI trial)=独立新域 **GETPARAM-ACTIVEPARAM-TRIAL-0001(待认领,fspec 参数 trial 记账)**,非 blockstructure 下游(stage40-54 snapshot 全同证)。三门禁:curl 2689/0/0(基线 2691,config 域 getparameter 756→754/main 639→621/match_url 79→76/helpf 91→81 全向 golden),httpd **2331/0/0==基线零变化**;next_url+match_url Phase 2 投影复验=**双双 MATCH 保持**(335/340 stages snapshot-identical);getparameter 终树确定性 3 跑 sha 一致 b9f83cae(13≠14 差异=探针构建布局敏感,CODEGEN-DIVERGENCE-REDFLAG-0001 族非本 lane 引入);工具先例=oracle 探桩(锁定树 RAM 副本 stderr 补丁,drill sha 92c66176…=pin 零扰动)。blockaction.rs 机制 C 白名单:**Cross-Review PENDING(集成前必须)**;产物=/dev/shm/rugra-tests/sb-getparam/(bisect/rugra1-13 投影/双侧轨迹) |
