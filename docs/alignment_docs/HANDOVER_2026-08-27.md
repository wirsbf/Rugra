# 主 Agent 编排交接文档（2026-08-26 23:45）

> 交接对象：接手多 Agent 编排的主 Agent。goal 运行至 **2026-08-28（周五）16:00**。
> 本文档 + `/home/wirs/.cache/rugra-agent-ops/PLAYBOOK.md`（监控手册）是两份权威操作文件。

---

# 2026-08-27 02:35 增量更新

> 本节为 02:00-02:35 时段增量，**优先于下方 23:45 基底**；基底中与本节冲突的数字（master hash/E2E/三函数差/worktree 断点）以本节为准，基底保留作历史参照。

## A. 停止点（02:35）

- master：`7d8d8a2a`（E2E：**124 函数 / defects 0 / numbering 0 / skeleton 3128**）
- 三函数（严格字节差）：hugehelp **12** / progressbarinit **6** / my_fwrite **8**

## B. 本时段集成（02:00-02:30）

- **fieldsplit 三连**（`e038b355`）：`*bar=0`→5 行逐字段清零；真根因=ActionInferTypes STORE 值播种用 `IntTypes::sized`（>8B 饱和）而 oracle 用 base-UNKNOWN（typeop.cc:271）
- **orphandecl**（`e4c122dd`）：RulePullsubMulti build_subpiece 无条件插入（ruleaction.cc:776-839）杀孤儿声明——根因又在 rule 层非发射层
- **callout 四连**（`68a6d3d3`/`4b103b26`/`c10871c3`）：cast_output CALL/CALLIND 三卫链（typeop.cc:720-735/776-789）+ cast_type_string 折叠；集成时把其 source_basic 统一进 master 的 live_ops_source（同契约双轨消灭）
- **mainspin**（`ed827938`）：bumpDeadcodeDelay 1:1 + applyDeadcodeDelay 接线 + 有界重启

## C. ⚠️ 两个 P0 回归（tri3 终局台账 + 主 Agent 亲测确认；regressfix Agent 在途，worktree=/home/wirs/.cache/rugra-wt-regressfix）

- **回归 A**（`ed827938` 后出现；callout 验证时完好）：hugehelp 字面量退化为 `puts((char *)((int *)&DAT+1))`（全语料 7 处）+ progressbarinit `__nptr =` 赋值形丢失。嫌疑 bumpDeadcodeDelay 语义偏差触发额外死代码遍删 CAST/PTRSUB 链
- **回归 B**（`c243cdef` fieldsplit 引入）：my_fwrite 双判空塌成 `(__s != 0) && (__s == 0)` 永假合取——return -1 不可达，正确性缺陷

## D. 在途 10 worktree 断点（02:32 盘点，全部 wip 无正式交付）

- strconst2（6 commits：Oppen token-queue 折行算法骨架+merge）
- markimplied（4：cover 链+round2 implied-def 臂）
- switchemit（3：+remove_branch 委托）
- gotolabel（4：label 台账/backpatch+pre-seed+probes+尾调用 override）
- irreducible（5 轮：parent-chain→clip_extra_roots→删自创守卫×2→BlockSwitch 臂；exhausted 已 8 函数→6 函数/7 hits）
- microform/callindptr/regressfix（0 commits，刚起步或死于早期）
- ⚠️ **rfupreview 异常**：该 worktree 出现了 agent/callout 分支的 7 个 commits（三合一复核 Agent 疑似在错误分支上操作）——复核报告 R-ORPHANDECL/R-CALLOUT/R-MAINSPIN 均未写出，三笔机制 C 复核债仍悬空；**接手者需先核查 rfupreview 的分支状态再重派复核**

## E. 监控基础设施（继续用）

- PLAYBOOK.md（`/home/wirs/.cache/rugra-agent-ops/`）+ Cron automation-4e4d24f2 每 25 分钟（至周五 16:00）
- 死亡计数已 17 次；重拉判据=失败通知或三信号全灭跨两轮；wip-commit 纪律全程零丢失

---

# 主 Agent 编排交接文档（2026-08-26 23:45 基底）

## 0. 停止点快照（可复算）

- master：`19d81373`（本 wave 累计 **121 项集成**，工作区干净）
- E2E 基线：**124/124 函数、defects=0、numbering=0、0 panic、skeleton 3050（main 全输出口径）**
- 三函数（严格字节差）：hugehelp **12** / my_fwrite **9** / progressbarinit **12**（总差 33 行，每行有主）
- 锁定 oracle：Ghidra 12.0.4 `e40ed130`（勿动）；门禁四件套全绿（gate health/annotations/refs/Evidence）
- 25 分钟自动监控轮在运行：automation `automation-4e4d24f2`（死亡检测→重拉→集成→汇报；周五 16:00 后只终报）

## 1. 编排基础设施（先读这个）

- **监控手册**：`/home/wirs/.cache/rugra-agent-ops/PLAYBOOK.md`——10 任务表、存活判定、重拉模板、交付处理规程、最新基线。每轮监控先读它。
- **存活判定方法论**（血泪教训）：rollout 模型 I/O 文件只在请求完成时 flush，**零文件≠死亡**。重拉判据 = 明确失败通知，或全灭三信号（rustc=0 + I/O 静默 + 零写入）**跨两轮**。误拉会造成同 worktree 双写者，比死亡更危险。
- **死亡已知原因**：①会话重启/恢复（CLI v0.16.3 startup 挂死→进程关停级联，bug report 在 `/home/wirs/zcode-cli-bug-report-2026-08-26.md`）②账号 5h 限额（通知含 `[1308]`，等重置勿重拉）③供应商网络波（"Model request failed"，直接重拉）④流断（finishReason=other 空结果）。
- **防丢纪律**（已注入所有 Agent 提示词）：可编译里程碑立即 `git commit`（`wip:` 前缀可用）；**禁 git stash**（多 worktree 共享 stash ref 会被并行 Agent 覆盖）；A/B 用独立 CARGO_TARGET_DIR。
- **Cargo 纪律**：一律 `/usr/bin/flock /tmp/rugra-cargo-build.lock env CARGO_INCREMENTAL=0 CARGO_TARGET_DIR=<专属 target> TMPDIR=<专属 tmp> cargo ...`。

## 2. 在途 10 worktree（2026-08-26 23:45 状态）

重拉用 Agent 工具（run_in_background），提示词模板在 PLAYBOOK.md「二」。

| worktree | 任务 | 断点状态（commits/dirty） | 验收 |
|---|---|---|---|
| rugra-wt-strconst2 | PRINTC-LINEWRAP：hugehelp 长字面量折行（最后 12 行） | 0 commits / 7 dirty | hugehelp 与 golden **逐字节一致** |
| rugra-wt-markimplied | TRI2-UNNAMED-VN-IMPLIED：fopen 首参 unique0x→`*(char **)stream`（coreaction.cc:3412 markImplied 链；同族 9 处 missing-LHS） | 2 commits（cover 链 salvage）/5 dirty | unique/missing-LHS 清零 |
| rugra-wt-callout | TRI2-CALLOUT-ASSIGN：`curl_getenv("COLUMNS");`→`__nptr = ...` 赋值形 | 3 commits（ActionActiveReturn 链）/6 dirty | 赋值形 + defects 0 |
| rugra-wt-fieldsplit | TRI2-STORESPLIT：`*bar=0`→5 行逐字段清零 | 3 commits（LOAD/STORE reader 派发=机制本体）/3 dirty | 5 行清零 + FILE*+8 不误拆 |
| rugra-wt-orphandecl | VARMAP-ORPHAN-DECL：孤儿声明清理（printc.cc:2260 存活过滤） | 0/6 dirty | 三函数孤儿声明清零 |
| rugra-wt-switchemit | PRINTC-SWITCH-EMIT：跳表 case→switch 渲染（getparameter 533 行杠杆） | 2 commits/1 dirty | switch 形态 + skeleton 大降 |
| rugra-wt-gotolabel | GOTO-LABEL-UNPRINTED：httpd goto 三缺陷 | 2 commits（label 台账+backpatch）/1 dirty | httpd 症状清零/减少 |
| rugra-wt-guardgaps | **已 DONE 并集成（19d81373）**——可释放或改派 | — | — |
| rugra-wt-mainspin | MAIN-POSTSTRUCT-SPIN：main 不收敛（heritage.cc:2582 setRestartPending 嫌疑） | 0/5 dirty | main 稳定完成 |
| rugra-wt-tri3 | 第三轮分诊（只读+docs） | 0/0 | 三函数台账终局 |

下一认领空位：**TRI2-STRUCT-IRREDUCIBLE-TRACE-0001**（8 个残差 exhausted 函数：main/getparameter/glob_set/glob_range/glob_word/match_url/next_url/parseconfig；根因=LoopBody/TraceDAG 精度 + ruleBlockSwitch 恢复体消费，与 switchemit 域相邻注意分工）。guardgaps 完成后释放的空位优先给它。

## 3. 本 wave 已集成（关键项，全部过复核或差分门禁）

STRCONST 三连（hugehelp 六 puts 字节=golden、"COLUMNS"/"wb" 一箭三雕、`/* dec */` 清零）；tempvar 收敛链（my_fwrite `__s` 链贯通 18→9）；mapglobals 真移植+REJECT→fix→R2 APPROVE（in_ram 41→0、main 6.5s 收敛）；jumptable 管线+LoadImage（getparameter 88 表恢复、1866 ops/138 块）；callspecenv+callproto（DWARF callee 签名链、`__stack_chk_fail()` 0 参=golden）；selectgoto 三连（自环清零、exhausted 18→8）；guardgaps（三守卫全对齐）；uniqleak（RuleSborrow/Scarry 深形式、main 声明 163→96）；httpdpanic 六函数族（httpd panic 6→0、29/30 反编译）；actiondw 四偏差+28-record fixture；ptrarith（next_url `piVar+0x38` 归零为数组访问）；rawquar REJECT→返修（SLEIGH 1440 寄存器名、in_register 裸名清零）；numdecl（双声明 27→0）；funcparam（`__nptr` golden 同名、13 函数 DWARF 推荐命名）；argc 原型（`int main(int argc,char **argv)` 逐字节）；以及 f2s2/flatcbr/ifgoto/rfupstream/mainprof/mergeoverlap 等。

## 4. 机制 C 复核体系（继续执行）

- 白名单（heritage*/jumptable/blockaction/condexe*/varmap 核心/merge/主管线 Action/Rule）集成前需独立复核 Agent（工作目录 `/home/wirs/.cache/rugra-wt-rfupreview`，只读；纪律：不采信 Evidence、自读 oracle、四类决定性语义、E2E 复现、报告写 `/tmp/rugra-reports/R-*.md`）
- 历史：**20+ 轮复核、6 REJECT 全闭环**（两次 REJECT→fix→R2 APPROVE 双循环）。REJECT 处理=返修单 SendMessage 回实现 Agent（可复活）或重拉
- commit-msg hook 陷阱：`## Alignment Evidence` 块的 `Ghidra:`/`Rugra:` 行必须**单值行号**（`file.cc:6490` 可，`file.cc:6490-6532` 拒）

## 5. 交付处理规程（主 Agent 职责）

1. 交付 commit → 白名单判定 → 复核或直接集成
2. 集成 = `git cherry-pick`（src 冲突按语义取舍，docs 用 keep-both 正则解决）→ flock 构建 → E2E（`examples/curl_decompile`，stdout/stderr 分离）→ `python3 tools/compare_ghidra.py <out> tests/golden/ghidra_curl_1204.c --summary-only` → **defects 必须 0**
3. 三函数复测：`compare ... --func hugehelp/progressbarinit/my_fwrite`
4. 更新 TODO_BOARD + PLAYBOOK.md 基线节 + TodoWrite

## 6. 复算命令

```bash
python3 tools/check_gate_health.py && python3 tools/check_ghidra_annotations.py --all && python3 tools/check_ghidra_refs.py --all --strict
/usr/bin/flock /tmp/rugra-cargo-build.lock env CARGO_INCREMENTAL=0 CARGO_TARGET_DIR=/home/wirs/.cache/root-target TMPDIR=/home/wirs/.cache/root-tmp cargo build --profile fast-release --example curl_decompile
timeout 400 /home/wirs/.cache/root-target/fast-release/examples/curl_decompile > /tmp/out.c 2> /tmp/err.log
python3 tools/compare_ghidra.py /tmp/out.c tests/golden/ghidra_curl_1204.c --summary-only
```

## 7. 剩余优先级（至周五 16:00）

1. **三函数 3/3 字节一致**（五路在途，见 §2 前五行）
2. TRI2-STRUCT-IRREDUCIBLE-TRACE（8 函数 exhausted，最大语料桶）
3. PRINTC-CALLIND-PTR（45 PLT stub `FUN_16e80()`→`(*(code *)PTR_...)`）、MAIN-POSTSTRUCT-SPIN、FSPEC-CALLPROTO-LATENT、BLOCK-RECIPROCAL-RESIDUAL 等已登记 TODO
4. httpd 第二语料回归验证（集成后应重跑）
