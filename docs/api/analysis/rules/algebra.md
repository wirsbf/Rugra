# `analysis/rules/algebra.rs` API Reference（历史/待复核说明）

**文档路径**: `docs/api/analysis/rules/algebra.md`  
**对应旧源码路径**: `src/analysis/rules/algebra.rs`  
**当前状态**: ⚠️ **历史遗留文档，待根据当前源码主线重新核实**

---

## 1. 文档定位

本文档用于说明旧版 `analysis/rules/algebra.rs` 这类“代数化简规则”文档在当前 Rugra 文档体系中的正确定位。

它当前**不是**当前主干规则系统的权威说明，而更适合作为：

- 历史分析分层的参考材料
- 项目曾经如何规划“代数化简规则”主题的背景说明
- 后续 API 文档全面复核时的待核对入口
- 理解旧版规则系统为什么会独立列出代数恒等式规则的概念参考

在当前阶段，这份文档最合适的标签应当是：

> **历史遗留 / 待重新验证**

---

## 2. 为什么需要标记为历史文档

Rugra 当前文档体系已经明确区分：

- 当前主线对象与模块
- 历史分层架构
- 静态结构对齐
- 运行时验证
- 已实现能力与待验证能力

在这个基线下，`analysis/` 目录整体都不应再默认被视为当前主干 API 的准确映射。  
这同样适用于 `analysis/rules/algebra.rs` 这种旧版规则文档。

主要原因包括：

1. 当前主线已经更多围绕以下对象组织：
   - `Funcdata`
   - `PcodeOp`
   - `Varnode`
   - `Heritage`
   - `Action`
   - `Rule`
   - `ActionDatabase`
   - `PrintLanguage`
   - `PrintC`

2. 旧版 `analysis/rules/` 文档往往对应：
   - 旧的 `Program` 风格容器
   - 旧的分析入口
   - 旧的“analysis -> rules -> optimization”分层思路

3. 即使“代数化简规则”这个主题今天仍然重要，也**不能**因为主题仍然重要，就把旧文档直接当作当前代码事实。

---

## 3. 这份文档现在可以表达什么

在当前阶段，`algebra.md` 最稳妥的职责是说明：

- Rugra 曾经或计划将“代数化简规则”作为独立规则主题处理
- 代数化简通常会涉及：
  - 恒等律（identity）
  - 零化律（nullifying）
  - 幂等律（idempotence）
  - 抵消律（cancellation）
  - 一些基础强度削弱或局部规约
- 这些规则的目标通常是让 IR：
  - 更短
  - 更稳定
  - 更适合后续规则和输出层消费
- 这些能力今天依然属于项目的重要方向

但它**不能直接证明**：

- 当前 `src/analysis/rules/algebra.rs` 仍然是主线实现
- 当前代数化简规则已经稳定存在并可直接使用
- 当前代数规则系统已经完整接入当前默认动作链
- 当前代数化简行为已经与 Ghidra 完成一致性验证
- 旧文档中提到的 `RuleAlgebraicSimplification` 现在仍然准确映射当前源码主线

---

## 4. 代数化简规则主题本身的重要性

虽然本文档当前被标记为历史说明，但“algebraic simplification” 本身依然是反编译链路中的重要主题之一。

典型代数化简规则通常会回答以下问题：

### 4.1 哪些表达式可以直接规约
例如：

- `x + 0 -> x`
- `x * 1 -> x`
- `x * 0 -> 0`
- `x & 0 -> 0`
- `x | x -> x`
- `x & x -> x`
- `x - x -> 0`
- `x ^ x -> 0`

### 4.2 哪些中间节点其实没有必要继续保留
某些表达式即使在语义上正确，如果不做局部规约，会导致：

- IR 更冗长
- 后续规则更难匹配
- 输出层产生噪音表达
- 变量恢复和类型传播压力增大

### 4.3 这些化简如何影响输出质量
代数化简不只是“让表达式更短”，它也直接影响：

- 中间表示是否更稳定
- 输出是否更接近人类可读形式
- 后续规则是否更容易继续生效
- `PrintC` 最终生成的表达式是否更自然

因此，即使这份文档当前是历史说明，**代数化简规则这个主题本身依然是当前项目的重要能力方向**。

---

## 5. 当前阅读这份文档时的正确姿势

### 可以这样理解
- 它是“代数化简规则”这个主题的历史入口
- 它可能反映了项目早期对规则系统细分的想法
- 它可以帮助后续整理“当前规则系统应该包含哪些基础代数规则”
- 它能说明项目曾经希望将代数
规约独立建模

### 不应这样理解
- 它就是当前主干规则系统的准确实现说明
- 只要有这份文档，当前代数化简就已经成熟
- 文档里的规则名称现在一定还能直接对应到源码
- 当前默认动作链已经稳定包含这套规则
- 当前代数规约行为已经完成与 Ghidra 的一致性验证

---

## 6. 与当前主线更接近的阅读入口

如果你想理解 Rugra **当前更真实的规则与简化主线**，建议优先查看这些文档与对象：

### 优先文档
- `../../action.md`
- `../../coreaction.md`
- `../../ruleaction.md`
- `../../op.md`
- `../../funcdata.md`
- `../../printc.md`
- `../../../VERIFICATION_GUIDE.md`
- `../../../../CURRENT_STATUS.md`

### 优先对象
- `Action`
- `Rule`
- `ActionDatabase`
- `PcodeOp`
- `Funcdata`
- `PrintC`

原因是当前“代数化简”更可能以：

- 分散在当前 `Action` / `Rule` 体系中
- 依附于 `Funcdata` 主线
- 与输出层、变量恢复和其他规则共同协作

的方式存在，而不再严格等同于旧 `analysis/rules/algebra.rs` 分层。

---

## 7. 旧文档中典型会描述什么

按旧文档描述，这一层通常会强调或计划强调以下几类规则：

### 恒等律（Identity）
- `x + 0 -> x`
- `x * 1 -> x`

### 零化律（Nullifying）
- `x * 0 -> 0`
- `x & 0 -> 0`

### 幂等律（Idempotence）
- `x | x -> x`
- `x & x -> x`

### 抵消律（Cancellation）
- `x - x -> 0`
- `x ^ x -> 0`

这些规则在概念上都合理，也确实是规则系统常见的基础构件。  
但当前阶段应理解为：

- 它们是项目规则主题的一部分
- 它们可能在当前主线中以别的形式存在
- 它们**不能**因为旧文档存在，就自动被视为“当前已经完整实现并接入主线”

---

## 8. 当前推荐状态标签

后续如果继续维护这份文档，建议在页首或索引中为其保持如下状态：

- **状态**: 历史遗留
- **可信度**: 待核对
- **用途**: 主题参考 / 迁移参考
- **不应用途**: 当前实现权威说明

也可以在统一标签体系中归入：

> **历史遗留（仅供参考）**

---

## 9. 后续重写时应核对什么

未来如果要把这份文档重新升级为“当前有效 API 文档”，至少应核对以下问题：

1. 当前是否仍存在独立的代数化简规则实现  
2. 当前这些规则是挂在：
   - `Action`
   - `Rule`
   - `ActionDatabase`
   - `coreaction`
   - `ruleaction`
   的哪一层  
3. 当前是否已经：
   - 实现基础 identity / nullifying / idempotence / cancellation 规则
   - 将其接入当前默认动作链
   - 确保这些规则不会破坏后续语义
   - 对它们建立测试或验证证据  
4. 当前这些能力是：
   - 已实现
   - 部分实现
   - 计划中
   - 已验证
   - 尚未验证

在这些问题未核清之前，这份文档不应恢复为“当前主干说明”。

---

## 10. 一句话结论

`docs/api/analysis/rules/algebra.md` 当前应被理解为：

> **Rugra 旧版“代数化简规则”分层思路的历史文档入口，可用于理解 algebra rules 在 IR 简化中的重要性，但不能继续被当作当前主线规则实现或当前已验证能力的权威说明。**

---