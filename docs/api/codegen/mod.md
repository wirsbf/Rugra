# `codegen/` API Reference（历史层说明）

**源代码路径**: `src/codegen/`

## 文档状态

**状态**：⚠️ 历史文档 / 待校准  
**可信度**：低到中  
**适用范围**：仅用于说明 Rugra 曾经存在或规划过的旧式 `codegen` 分层，不应被当作当前主线实现的权威入口。

---

## 1. 这份文档为什么需要重写

当前 `rugra` 仓库中的源码主线，已经不再以一个稳定、独立的 `src/codegen/` 目录作为最终代码生成层来组织。  
因此，旧文档里把 `codegen/` 描述成“当前正式 C 代码生成后端”的说法，会制造明显失真。

更准确的表述应该是：

- `codegen/` 代表的是**历史分层概念**或**旧架构残留文档分区**
- 当前可见实现中，面向输出的主线更接近：
  - `src/prettyprint.rs`
  - `src/printlanguage.rs`
  - `src/printc.rs`
- 若旧文档中出现 `generate_c_code(...)`、`FunctionAnalysis`、`Program` 等组合，应优先视为**旧架构说明**，而不是当前稳定 API

---

## 2. 当前应如何理解 `codegen/`

`codegen/` 在文档层面可以理解为：

> Rugra 早期或历史架构中，用于承载“分析结果 → C 风格输出”的分层概念。

它说明项目曾试图把“代码生成”单独抽象成一个后端层，接受类似：

- `FunctionAnalysis`
- `Program`
- `Binary`

这样的分析结果输入，然后输出结构化 C 代码。

但按当前仓库状态，这套分层**不应再被当作现行主线**。

---

## 3. 当前主线中的替代位置

如果你现在要理解 Rugra 的输出层，应该优先查看这些真实存在且更贴近当前主线的模块：

### `src/prettyprint.rs`
负责格式化输出相关的基础设施。

### `src/printlanguage.rs`
负责输出语言层抽象，是更接近“语言无关打印接口”的位置。

### `src/printc.rs`
负责 C 风格输出，是当前最应优先阅读的“代码生成 / 文本输出”实现入口。

---

## 4. 为什么旧 `codegen` 文档有风险

旧 `codegen` 文档如果不加说明，容易造成以下误解：

1. 误以为当前仍存在稳定的 `src/codegen/` 主实现目录
2. 误以为 `generate_c_code(...)` 仍是当前正式主入口
3. 误以为旧版 `FunctionAnalysis + Program` 架构仍是当前主分析链路
4. 误以为 Rugra 当前的输出后端已经按旧文档方式完整可用

这些判断都不应直接成立。

---

## 5. 历史层可能描述过的内容

旧 `codegen` 分层通常涉及以下概念：

- 函数签名生成
- 局部变量声明生成
- 控制流结构化输出
- 表达式格式化
- 字符串字面量恢复
- 基于分析结果的 C 风格代码拼接

这些内容作为“设计方向”仍然有参考价值，但是否仍由独立 `codegen/` 层实现，必须以当前源码为准。

---

## 6. 当前阅读建议

如果你的目标是理解**当前 Rugra 如何输出 C 风格结果**，建议按以下顺序阅读：

1. `../printlanguage.md`
2. `../printc.md`
3. `../prettyprint.md`
4. `../funcdata.md`
5. `../block.md`
6. `../heritage.md`

如果你的目标是理解**旧架构如何规划代码生成层**，则本目录可作为历史参考，但不应脱离当前代码单独使用。

---

## 7. 与其他文档的关系

若要确认当前输出层真实状态，请同时查阅：

- `../../PROJECT_STRUCTURE.md`
- `../../../CURRENT_STATUS.md`
- `../../README.md`
- `../README.md`

这些文档能够帮助区分：

- 当前真实存在的输出模块
- 历史或旧分层残留文档
- 已实现能力
- 仅为架构设想或旧实现路径

---

## 8. 维护规则

后续如果继续维护本文件，应遵循以下规则：

1. 不得再把 `codegen/` 写成当前确定主线，除非源码结构重新恢复独立 `src/codegen/`
2. 若旧 API 已停用，应明确标注“历史接口”或“旧架构说明”
3. 若当前输出逻辑继续集中在 `printlanguage` / `printc`，本文件应始终保持“历史层”定位
4. 若将来重新引入新的独立 `codegen/` 目录，应重写本文件，而不是在旧说明上叠加乐观结论

---

## 9. 一句话结论

`docs/api/codegen/mod.md` 当前应被理解为：

> **用于说明 Rugra 旧式代码生成分层的历史文档入口，而不是当前输出主线的权威 API 索引。**

如果你要研究当前 Rugra 的真实输出实现，请优先查看 `printlanguage` 与 `printc` 相关文档和源码。